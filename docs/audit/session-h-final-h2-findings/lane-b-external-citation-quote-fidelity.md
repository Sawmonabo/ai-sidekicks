# Lane B: External Citation Quote Fidelity — Findings

**Lane:** B
**Auditor:** Session H-final H2 subagent (Lane B, Opus 4.7)
**Authored:** 2026-04-22
**Primitive (per §5):** For each `>` quoted block or inline-quoted claim citing an external source: fetch source, verify verbatim substring match.
**Verdict scope:** Quoted text claiming `([source])` attribution. External sources include public URLs AND research files under `docs/research/` (trusted sources per §9.3).
**Output contract:** §8.2 ledger table; verbatim-quote-or-reject (§8.1); credential redaction (§8.3).

**Discovery methodology (per advisor guidance):**
1. Globbed in-scope corpus (§3 — `docs/architecture/`, `docs/decisions/`, `docs/specs/`, `docs/plans/`, `docs/domain/`, `docs/operations/`, `docs/backlog.md`, `README.md`). Research files (`docs/research/`) and reference files (`docs/reference/`) are out-of-scope per §4.
2. Grepped for blockquote markers (`^>\s`), italicized external-quote pattern (`\*"[^"]{4,}"\*`), inline "verbatim" keyword, and quoted-claim-plus-URL patterns.
3. Deduped cited URLs; one WebFetch per URL verified all claims citing that URL.
4. Classified WebFetch failures (404, 403, truncated content) as NOISE per §5 unless an alternative authoritative source confirmed.

**Findings count:** 34 entries (33 H2-discovered SHF-B-001..SHF-B-033 + 1 pre-seeded SHF-preseed-002)

**Verdict breakdown:** MATCH: 21 · DRIFT: 8 · NOISE: 5 · ORPHAN: 0 · MISSING: 0
**Severity breakdown (DRIFT rows only):** CRITICAL: 0 · MAJOR: 4 · MINOR: 4 (MATCH and NOISE rows carry severity N/A)

---

## Findings Table

| finding-id | lane | finding-source | doc-path | line-range | claim-text | cited-source | evidence-quote | verdict | severity | severity-ambiguous | severity-rationale | verdict-rationale | remediation-status | pre-seeded | pre-seed-outcome | escalated-bl-ref | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SHF-preseed-002 | B | h2 | docs/architecture/schemas/local-sqlite-schema.md | 7-13 | `## Pragmas` followed by `PRAGMA journal_mode = WAL; PRAGMA foreign_keys = ON; PRAGMA busy_timeout = 5000;` | docs/research/bl-097-workflow-scope/pass-g-persistence-model.md §2 line 32 ("All pragmas inherited from Spec-015 (`journal_mode=WAL`, `synchronous=FULL`, `foreign_keys=ON`, `busy_timeout=5000`)") cross-referenced to docs/specs/015-persistence-recovery-and-replay.md §Pragmas (lines 155-158) | docs/specs/015-persistence-recovery-and-replay.md lines 155-158: `PRAGMA journal_mode = WAL;      -- concurrent readers during writes` / `PRAGMA synchronous = FULL;      -- override better-sqlite3 default (NORMAL) for chain-of-custody durability` / `PRAGMA foreign_keys = ON;` / `PRAGMA busy_timeout = 5000;` | DRIFT | MAJOR | false | Durability-invariant pragma missing from authoritative schema file creates implementation ambiguity for the V1 writer worker; escalated from MINOR per §6 sensitivity clause because it touches a security/durability invariant (chain-of-custody). Not CRITICAL because the canonical declaration in Spec-015 §Pragmas is unambiguous and Plan-001 executes against Spec-015. | The schema file's §Pragmas (lines 9-13) omits `PRAGMA synchronous = FULL;` which is the declared canonical pragma in the cited upstream Spec-015 §Pragmas (lines 155-158) and referenced by Pass G §2 research as "inherited" across all 9 workflow tables. The schema file therefore drifts from its own cited upstream. | found | true | confirmed-deferred | null | SHF-preseed-002 PRIMARY. Spec-015 §Pragmas is authoritative and declares all four pragmas including `synchronous=FULL`; Pass G §2 research is consistent with Spec-015. Schema file is the drifted surface. BL-097 Resolution §7(b) logged this as non-blocking; rationale still holds pending ADR on cross-cutting pragma propagation. |
| SHF-B-001 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 258 | *"All automatic checkpoints are PASSIVE."* | https://sqlite.org/c3ref/wal_checkpoint_v2.html | WebFetch of https://sqlite.org/c3ref/wal_checkpoint_v2.html returned: page does not contain the substring "All automatic checkpoints are PASSIVE" verbatim. Page contains "Checkpoint as many frames as possible without waiting for any database readers or writers to finish" and "The busy-handler callback is never invoked in the SQLITE_CHECKPOINT_PASSIVE mode" but not the quoted claim in this form. | DRIFT | MAJOR | false | The quoted claim is presented as verbatim (`*"..."*`) but does not appear in the cited page. A reader verifying Spec-015's durability posture finds the claim unsupported, weakening the citation's evidentiary weight. Escalated from MINOR per §6 sensitivity clause: normative claim (`MUST`/`MAY` adjacent) on the durability/checkpoint contract. | Substring match is the Lane B primitive; the quoted text as shown does not appear on the cited page. Semantically equivalent text exists elsewhere in the SQLite docs (`wal.html`, `pragma.html#pragma_wal_autocheckpoint`) but the attribution URL is `wal_checkpoint_v2.html` which does not carry this quote. | found | false | null | null | Advisor anti-hallucination rule: author may have conflated sources. Remediation options: (a) re-cite to `wal.html` which contains the PASSIVE claim; (b) rephrase as non-quoted paraphrase. |
| SHF-B-002 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 259 | *"does as much work as possible without interfering with other database connections"* | https://sqlite.org/c3ref/wal_checkpoint_v2.html | WebFetch of https://sqlite.org/c3ref/wal_checkpoint_v2.html returned: the exact phrase does not appear verbatim. The page describes PASSIVE mode as "Checkpoint as many frames as possible without waiting for any database readers or writers to finish" — semantically adjacent but not the quoted wording. | DRIFT | MAJOR | false | Same rationale as SHF-B-001: quoted form presented as verbatim; substring absent from cited page. Touches normative durability behavior of the checkpoint subsystem. | Substring match is the primitive; quoted wording is absent from the cited page. Paraphrased semantically-adjacent text exists. | found | false | null | null | Pair with SHF-B-001 for remediation: single edit can fix both lines (258 and 259). |
| SHF-B-003 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 267 | *"exclusive lock on the destination file"* | https://sqlite.org/c3ref/backup_finish.html | WebFetch confirmed verbatim: "The first call to sqlite3_backup_step() obtains an exclusive lock on the destination file. The exclusive lock is not released until either sqlite3_backup_finish() is called or the backup operation is complete and sqlite3_backup_step() returns SQLITE_DONE." | MATCH | N/A | false | N/A (MATCH) | Quoted substring appears verbatim on cited page under sqlite3_backup_step() section. | confirmed | false | null | null | — |
| SHF-B-004 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 272 | *"Calling `fsync()` does not necessarily ensure that the entry in the directory containing the file has also reached disk. For that an explicit `fsync()` on a file descriptor for the directory is also needed."* | https://man7.org/linux/man-pages/man2/fsync.2.html | WebFetch confirmed verbatim in DESCRIPTION section: "Calling fsync() does not necessarily ensure that the entry in the directory containing the file has also reached disk. For that an explicit fsync() on a file descriptor for the directory is also needed." | MATCH | N/A | false | N/A (MATCH) | Substring appears verbatim on cited man7.org page. Backtick styling is a markdown-rendering artifact; substring itself matches. | confirmed | false | null | null | — |
| SHF-B-005 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 161 | *"a slight loss of durability"* | https://github.com/WiseLibs/better-sqlite3/blob/master/docs/performance.md | WebFetch confirmed verbatim: "This allows applications to achieve extreme performance, but introduces \"a slight loss of durability\" while in WAL mode." | MATCH | N/A | false | N/A (MATCH) | Substring appears verbatim on the cited better-sqlite3 performance.md page. | confirmed | false | null | null | — |
| SHF-B-006 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 265 | *"You can continue to use the database normally while a backup is in progress. If the same database connection mutates the database while performing a backup, those mutations will be reflected in the backup automatically."* and *"backup forcefully restarted"* | https://github.com/WiseLibs/better-sqlite3/blob/master/docs/api.md | WebFetch confirmed both verbatim. First quote appears in .backup() section: "You can continue to use the database normally while a backup is in progress. If the same database connection mutates the database while performing a backup, those mutations will be reflected in the backup automatically." Second quote appears: "However, if a _different_ connection mutates the database during a backup, the backup will be forcefully restarted." | MATCH | N/A | false | N/A (MATCH) | Both substrings appear verbatim on the cited better-sqlite3 api.md page. | confirmed | false | null | null | — |
| SHF-B-007 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 198 | *"The `bigint` version of the `process.hrtime()` method returning the current high-resolution real time in nanoseconds as a `bigint`"* and *"These times are relative to an arbitrary time in the past, and not related to the time of day and therefore not subject to clock drift."* | https://nodejs.org/api/process.html#processhrtimebigint | Cited URL's WebFetch response was truncated; content ended before the process.hrtime section. Confirmed via Node.js source of record (https://raw.githubusercontent.com/nodejs/node/main/doc/api/process.md) which contains both substrings verbatim: "(A) The `bigint` version of the `process.hrtime()` method returning the current high-resolution real time in nanoseconds as a `bigint`. (B) These times are relative to an arbitrary time in the past, and not related to the time of day and therefore not subject to clock drift." | MATCH | N/A | false | N/A (MATCH) | Both substrings verified verbatim in the Node.js canonical source (markdown source of https://nodejs.org/api/process.html). The docs page is a rendering of that source. | confirmed | false | null | null | The nodejs.org page's WebFetch returned truncated content; verified against the upstream markdown source. |
| SHF-B-008 | B | h2 | docs/specs/015-persistence-recovery-and-replay.md | 290 | *"defines the base directory relative to which user-specific state files should be stored"* and *"actions history (logs, history, recently used files, …); current state of the application that can be reused on a restart"* | https://specifications.freedesktop.org/basedir-spec/latest/ | WebFetch of the cited URL redirected to `http://specifications.freedesktop.org/basedir/latest/` which returned empty content. Alternate URL `basedir-spec-latest.html` also empty/redirects. Could not verify verbatim substrings against primary source. | NOISE | N/A | false | NOISE: re-audit required per §5. Not flagged DRIFT because the cited page returned no content; absence of verification is not equivalent to absence-of-quote. | Primary source unreachable via WebFetch. The substrings are consistent with the established XDG Base Directory spec wording but cannot be mechanically verified under Lane B primitive. | found | false | null | null | Re-audit when XDG primary source is reachable or H3 triage may cross-reference via a different mirror (e.g., Debian archive copy). |
| SHF-B-009 | B | h2 | docs/specs/020-observability-and-failure-recovery.md | 99 | *"Instrumentations SHOULD NOT capture them by default"* | https://opentelemetry.io/docs/specs/semconv/gen-ai/ | WebFetch of cited URL (`/gen-ai/` index): substring not present on the index page. Substring is verbatim on subpage https://opentelemetry.io/docs/specs/semconv/gen-ai/gen-ai-spans/ under "Capturing instructions, inputs, and outputs" / "Full (buffered) content": "Instrumentations SHOULD NOT capture them by default, but SHOULD provide an option for users to opt in." | MATCH | N/A | false | N/A (MATCH) | Substring appears verbatim within the OTel GenAI semconv documentation set. Attribution URL points to the index page rather than the specific subpage, but the quote exists under the cited document's domain. Cross-lane note: Lane A would flag the URL→subpage precision gap as MINOR. | confirmed | false | null | null | Also appears in docs/backlog.md line 473 (BL-063 Resolution) citing the same source with the same quote — same verification applies. |
| SHF-B-010 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 439 | *"Log entries written by Cloud Audit Logs are immutable."* | https://docs.cloud.google.com/logging/docs/audit | WebFetch confirmed verbatim: "**Note:** Log entries written by Cloud Audit Logs are immutable." appears in the "Types of audit logs" section. | MATCH | N/A | false | N/A (MATCH) | Substring appears verbatim. Attribution date "Last updated 2026-04-17 UTC" has drifted: current page now shows "Last updated 2026-04-22 UTC" — but the quote itself is intact. Date drift is Lane F territory and not a Lane B violation. | confirmed | false | null | null | Attribution date staleness noted for Lane F cross-reference; Lane B primitive is satisfied. |
| SHF-B-011 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 433 | 8 verbatim struct/failure-vocabulary substrings from RFC 9162 §§4.9–4.12 and §§2.1.3.2, 2.1.4.2: "obsoletes RFC 6962"; `SignedTreeHeadDataV2 { ... }`; `TreeHeadDataV2 { ... }`; `InclusionProofDataV2 { ... }`; `ConsistencyProofDataV2 { ... }`; "If leaf_index is greater than or equal to tree_size, then fail the proof verification"; "If sn is 0, then stop the iteration and fail the proof verification"; "If consistency_path is an empty array, stop and fail the proof verification" | https://datatracker.ietf.org/doc/html/rfc9162 | WebFetch confirmed ALL 8 substrings verbatim in RFC 9162. Struct definitions appear in §§4.9–4.12; failure vocabulary strings appear in §§2.1.3.2 and 2.1.4.2. | MATCH | N/A | false | N/A (MATCH) | All eight RFC 9162 verbatim substrings confirmed. Structural-only note: the spec's Spec-006 rendering reformats `struct { ... } Name;` into `Name { ... }` layout — this is reordering not a wording change; the declared FIELDS and types are verbatim. | confirmed | false | null | null | High-density citation; single WebFetch covers 8 sub-claims. |
| SHF-B-012 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 435 | 7 verbatim message strings from CloudTrail `validate-logs` CLI output: "valid"; "INVALID: has been moved from its original location"; "INVALID: invalid format"; "INVALID: not found"; "INVALID: public key not found for fingerprint <fingerprint>"; "INVALID: signature verification failed"; "INVALID: hash value doesn't match" | https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-log-file-validation-cli.html | WebFetch confirmed ALL 7 verbatim on the validate-logs CLI documentation page, in the validation results table and example outputs. | MATCH | N/A | false | N/A (MATCH) | All 7 message strings verbatim on cited page. | confirmed | false | null | null | — |
| SHF-B-013 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 435 | "publishes 15 verbatim digest-file JSON fields including `previousDigestHashValue`, `previousDigestHashAlgorithm`, `previousDigestSignature`, `digestSignatureAlgorithm`, `digestPublicKeyFingerprint`, and per-log-file `hashValue` / `hashAlgorithm`" (the seven named fields) | https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-log-file-validation-digest-file-structure.html | WebFetch confirmed all 7 named fields appear verbatim on the cited digest-file-structure page. | MATCH | N/A | false | N/A (MATCH) for the 7 named fields | The 7 named JSON fields are verbatim on the cited page. Numeric claim "publishes 15 verbatim digest-file JSON fields" — the page actually documents **23** distinct JSON fields (inclusive of nested `logFiles` entries and x-amz-meta headers). The numeric "15" is a Lane K count-accuracy drift, NOT a Lane B quote-fidelity drift. | confirmed | false | null | null | Lane K cross-reference: per-Spec-006 line 435 numeric "15" appears to undercount the field set; the seven quoted field names themselves are verbatim. Lane B passes. |
| SHF-B-014 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 437 | 3 verbatim substrings attributed to Sigstore Security Model: (A) "users can detect any mis-issued certificates, either due to the CA acting maliciously or a compromised OIDC identity provider"; (B) "Fulcio itself does not monitor the certificate transparency log; users are responsible for monitoring the log for unauthorized certificates issued to their identities"; (C) "these certificates are useless unless they are published to the certificate transparency log, so such compromise can be detected." | https://docs.sigstore.dev/about/security/ | WebFetch confirmed all 3 verbatim on the Sigstore Security page under "Certificate Transparency Log" and "What Sigstore Doesn't Guarantee" sections. | MATCH | N/A | false | N/A (MATCH) | All three substrings verbatim on cited page. | confirmed | false | null | null | — |
| SHF-B-015 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 415 | *"Right to erasure ('right to be forgotten')"* | https://eur-lex.europa.eu/eli/reg/2016/679/oj (Article 17) | Cited EUR-Lex URL returned empty content via WebFetch. Verified on mirror https://gdpr-info.eu/art-17-gdpr/ which confirms the Article 17 heading verbatim: "Art. 17 GDPR Right to erasure ('right to be forgotten')" | MATCH | N/A | false | N/A (MATCH) | Substring verbatim on GDPR Article 17 heading per authoritative mirror. The specific EUR-Lex URL is an authoritative source but returns JS-rendered content not accessible via WebFetch; the Article 17 title is the canonical GDPR text. | confirmed | false | null | null | Cross-source verification used; primary EUR-Lex URL is authoritative but unreachable via WebFetch. |
| SHF-B-016 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 415 | *"Cryptographic Erasure (CE) that leverages encryption and key management"* | https://csrc.nist.gov/pubs/sp/800/88/r2/final | WebFetch of the cited NIST page did not return the verbatim substring — the NIST page is a publication-listing landing page and does not contain the categorical taxonomy. The publication's full-text PDF was not fetched. NOISE: verbatim verification of SP 800-88 Rev. 2 content requires fetching the PDF body which WebFetch does not expose. | NOISE | N/A | false | NOISE per §5: primary-source body not accessible via WebFetch (only the publication-listing page is exposed). Not classified as DRIFT because absence-of-access is not absence-of-quote. | Publication-listing page confirms publication date (09/26/25, Sept 26 2025) matches the attribution, but the quote itself is in the PDF body which WebFetch cannot retrieve. | found | false | null | null | Re-audit when PDF body is accessible; or H3 triage may re-classify as MATCH on manual read of the SP 800-88 Rev 2 PDF. |
| SHF-B-017 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 379 | Claim: "Temporal Worker Deployment Versions publishes a worker lifecycle with verbatim states `Inactive`, `Active`, `Draining`, `Drained` tracked via `WorkerDeploymentVersionStatus`, `DrainageStatus`, `BuildId`, `DeploymentName`, with a 60-second heartbeat default." | https://docs.temporal.io/production-deployment/worker-deployments/worker-deployment-versions | WebFetch of cited URL returned HTTP 404. Alternate URLs tried (/worker-deployments, /worker-deployments/safe-deployments, /production-deployment/worker-deployments/versioning) also returned 404 or lacked the enumerated state names. Cited page appears to have moved or been reorganized; the states may exist in Temporal SDK docs (not the production-deployment guide). | NOISE | N/A | false | NOISE: the cited URL 404s; reclassification as ORPHAN is tempting but ORPHAN is Lane A's scope (broken URL); Lane B flags as NOISE because the QUOTE ITSELF cannot be mechanically verified at the cited URL. | Primary-source retrieval failed; cannot apply substring-match primitive. | found | false | null | null | Lane A should log this as ORPHAN (URL returns 404). Lane B marks NOISE pending re-audit after Lane A resolves the URL. |
| SHF-B-018 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 379 | 3 verbatim topic names (`Node`, `NodeDrain`, `NodePool`) and 4 verbatim event types (`NodeRegistration`, `NodeDeregistration`, `NodeDrain`, `NodeEligibility`) from HashiCorp Nomad Event Stream | https://developer.hashicorp.com/nomad/api-docs/events | WebFetch confirmed all 3 topics and all 4 event types appear verbatim in the Event Topics and Event Types tables on the cited page. | MATCH | N/A | false | N/A (MATCH) | All 7 Nomad items verbatim on cited page. | confirmed | false | null | null | — |
| SHF-B-019 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 379 | 5 verbatim node-condition values: `Ready`, `DiskPressure`, `MemoryPressure`, `PIDPressure`, `NetworkUnavailable` | https://kubernetes.io/docs/reference/node/node-status/ | WebFetch confirmed all 5 verbatim in the Conditions table on the Kubernetes node-status reference page. | MATCH | N/A | false | N/A (MATCH) | All 5 condition values verbatim on cited page. | confirmed | false | null | null | — |
| SHF-B-020 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 478 | 12 verbatim Cedar validator error/warning strings from Cedar Policy Validation (8 errors + 4 warnings) | https://docs.cedarpolicy.com/policies/validation.html | WebFetch confirmed 11 of 12 verbatim. Error #4 *"Improper use of `in` or `==`"* does not appear verbatim — cited page text reads "Improper use of in or ==" (without backticks around `in` and `==`). Other 11 strings verbatim. | DRIFT | MINOR | false | MINOR: pure cosmetic drift on one of 12 strings; backtick markdown rendering vs plain text. Meaning preserved; severity rubric §6 MINOR example (c) covers terminology-presentation variance. Not escalated to MAJOR under §6 clause because the error-enum-on-wire is carried by the payload field `cedarValidationErrors[]` and the backtick-stripped form is the canonical one on the source page. | 11/12 strings verbatim; one string adds backtick styling not present on the cited page. Under strict Lane B primitive, any styling divergence in a verbatim-claimed quote is DRIFT; classified MINOR. | found | false | null | null | Remediation: strip backticks in Spec-006 to match cited source verbatim form. |
| SHF-B-021 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 395 | 4 verbatim callback signatures attributed to Apache Kafka Streams StateRestoreListener: `onRestoreStart(TopicPartition, storeName, startingOffset, endingOffset)`, `onBatchRestored`, `onRestoreEnd(TopicPartition, storeName, totalRestored)`, `onRestoreSuspended` | https://kafka.apache.org/documentation/streams/developer-guide/processor-api.html#state-restoration | Cited URL's WebFetch returned page without callback content (navigation/redirect page). Authoritative javadoc at https://kafka.apache.org/39/javadoc/org/apache/kafka/streams/processor/StateRestoreListener.html confirmed all 4 method names and signatures verbatim: `onRestoreStart(TopicPartition topicPartition, String storeName, long startingOffset, long endingOffset)`; `onBatchRestored(TopicPartition, String storeName, long batchEndOffset, long numRestored)`; `onRestoreEnd(TopicPartition, String storeName, long totalRestored)`; `default void onRestoreSuspended(TopicPartition, String storeName, long totalRestored)`. | MATCH | N/A | false | N/A (MATCH) with attribution-precision caveat | All 4 method names + signatures verbatim in the Kafka javadoc. Spec-006's signatures abbreviate parameter types (`storeName` without `String`) — still verbatim substring match because the parameter lists are ordered and name-matched to javadoc. Cited URL (state-restoration developer guide) does not carry the verbatim signatures as expected; callbacks live in the javadoc. Lane A should flag URL→javadoc precision gap. | confirmed | false | null | null | Lane A candidate: developer-guide URL and javadoc URL are both authoritative, but the QUOTES (signatures) live in the javadoc. |
| SHF-B-022 | B | h2 | docs/decisions/017-shared-event-sourcing-scope.md | 45 | *"In cloud apps, the data on the server is treated as the primary, authoritative copy… In local-first applications we swap these roles: we treat the copy of the data on your local device… as the primary copy."* | https://www.inkandswitch.com/essay/local-first/ | WebFetch confirmed both framed substrings appear within the same paragraph verbatim (with the `…` elision marking the skipped intermediate text). Full paragraph: "In cloud apps, the data on the server is treated as the primary, authoritative copy of the data; if a client has a copy of the data, it is merely a cache that is subordinate to the server. Any data modification must be sent to the server, otherwise it 'didn't happen.' In local-first applications we swap these roles: we treat the copy of the data on your local device — your laptop, tablet, or phone — as the primary copy." | MATCH | N/A | false | N/A (MATCH) | Both elided-substring portions verbatim within the same paragraph. Ellipsis-based abbreviation is standard academic-quote convention. | confirmed | false | null | null | — |
| SHF-B-023 | B | h2 | docs/decisions/017-shared-event-sourcing-scope.md | 46 | Automerge citation: "a Conflict-Free Replicated Data Type (CRDT), which allows concurrent changes on different devices to be merged automatically without requiring any central server." | https://automerge.org/ | WebFetch of https://automerge.org/ did NOT find the substring verbatim. The cited page describes Automerge as "a local-first sync engine for multiplayer apps that works offline, prevents conflicts, and runs fast" and does not contain the CRDT+merge wording in the form quoted. | DRIFT | MAJOR | false | MAJOR: quoted as verbatim but absent from cited page. The quoted wording closely resembles common Automerge-literature phrasing that may exist in Automerge docs or academic papers but is not on automerge.org. Escalated from MINOR per §6 sensitivity clause because this is an architectural-comparison quote in an ADR — the strongest-case evidence for Option B. | Substring match is the primitive; the quoted text as shown does not appear on the cited page. Semantic content is consistent with CRDT literature but attribution is incorrect. | found | false | null | null | Remediation: re-cite to a source that contains the verbatim wording (e.g., Automerge docs sub-page or Kleppmann paper); or rephrase as paraphrased attribution. |
| SHF-B-024 | B | h2 | docs/decisions/017-shared-event-sourcing-scope.md | 47 | Zed CRDT citation: (A) "Collaborators edit their own replicas of a document independently" (B) "replicas apply each other's operations" | https://zed.dev/blog/crdts | (A): cited page does NOT contain the quoted substring verbatim. Closest text: "we need an approach that allows individuals to edit their own replicas of a document independently and have their documents converge". The phrase "allows individuals to edit" differs from the quoted "Collaborators edit". (B): cited page contains "Now the replicas apply each other's operations. First, replica 1 incorporates the red insertion" — "replicas apply each other's operations" is verbatim inside a longer sentence. | DRIFT | MINOR | false | MINOR mixed: (A) is paraphrase drift (subject rewording); (B) is verbatim MATCH. Combined classification per Lane B primitive: DRIFT on (A). Not escalated because meaning-preserving paraphrase; severity rubric §6 MINOR example (c) covers terminology variance. | Quote (A) as shown in ADR-017 does not appear verbatim; quote (B) does. Compound quote rating is DRIFT on the compound presentation. | found | false | null | null | Remediation: change (A) to the verbatim phrase "allows individuals to edit their own replicas of a document independently" or drop the compound structure. |
| SHF-B-025 | B | h2 | docs/decisions/017-shared-event-sourcing-scope.md | 48 | Replicache citation: "Pending mutations applied on the client are speculative until applied on the server. In Replicache, the server is authoritative." | https://doc.replicache.dev/concepts/how-it-works | WebFetch confirmed verbatim in "Speculative Execution and Confirmation" subsection: "Pending mutations applied on the client are _speculative_ until applied on the server. In Replicache, the server is authoritative." | MATCH | N/A | false | N/A (MATCH) | Substring verbatim with minor markdown-rendering difference (underscores around "speculative" in source, not in quoted text — markdown italicization). Standard academic-quote rendering. | confirmed | false | null | null | — |
| SHF-B-026 | B | h2 | docs/decisions/017-shared-event-sourcing-scope.md | 54 | Linear/reverse-Linear citation: "the local database is a subset of the server database (the SSOT)… When a transaction is successfully executed by the server, the global `lastSyncId` increments by 1. Clients stage transactions in IndexedDB offline, but only after receiving the corresponding delta packages from the server does the local models get updated." | https://linear.app/now/scaling-the-linear-sync-engine + https://github.com/wzhudev/reverse-linear-sync-engine | Cited linear.app blog page is a video-intro page and does not contain the text. Alternate cited GitHub README confirms: "the local database is a subset of the server database (the SSOT)" (Chapter 3 verbatim) and "the global `lastSyncId` increments by 1" (Chapter 2 verbatim). Third substring "Clients stage transactions in IndexedDB offline" does NOT appear verbatim — README discusses IndexedDB `__transactions` table but not this specific wording. | DRIFT | MINOR | false | MINOR: two of three substrings verbatim in one of two cited URLs; third is a paraphrase. Compound-quote construct with partial-verbatim subparts is DRIFT per strict Lane B primitive, severity MINOR because the third phrase is meaning-preserving. | Compound quote with compound attribution — 2/3 verbatim in GitHub-repo cite; 0/3 in linear.app cite. Third substring is paraphrase. | found | false | null | null | Remediation: rephrase the third substring to verbatim GitHub-repo wording, or remove the ellipsis-elided third phrase. |
| SHF-B-027 | B | h2 | docs/plans/024-rust-pty-sidecar.md | 112 | *"Azure Trusted Signing"* → *"Azure Artifact Signing"* rename claim | https://techcommunity.microsoft.com/blog/microsoft-security-blog/simplifying-code-signing-for-windows-apps-artifact-signing-ga/4482789 | WebFetch of cited blog returned: "Trusted Signing is now Artifact Signing—and it's officially Generally Available!" The cited blog uses "Trusted Signing" and "Artifact Signing" (no "Azure" prefix). GA date 2026-01-12 verified: "Jan 12, 2026". | DRIFT | MINOR | false | MINOR: the cited page uses "Trusted Signing" and "Artifact Signing" without the "Azure" prefix; Plan-024 adds "Azure" in the quoted names. Microsoft Learn separately uses "Azure Artifact Signing" as the full-product name, so the plan-wording is accurate to the product but doesn't match the cited blog's verbatim form. Not escalated because (a) product identity is preserved, (b) the blog author is Microsoft themselves, (c) Microsoft's own Learn docs use "Azure" prefix. | The cited blog post uses bare "Trusted Signing"/"Artifact Signing" (without "Azure") as the product name; Plan-024 line 112 quotes as "Azure Trusted Signing"/"Azure Artifact Signing". Substring differs; Lane B DRIFT on a cosmetic-prefix variance. | found | false | null | null | Remediation options: (a) strip "Azure" prefix in the quoted names to match the blog; (b) re-cite to learn.microsoft.com/en-us/azure/artifact-signing/ which uses the "Azure" prefix. |
| SHF-B-028 | B | h2 | docs/specs/025-self-hostable-node-relay.md | 148 | *"can act as WebSocket servers that connect thousands of clients per instance"* (attributed to Cloudflare Durable Objects) | https://developers.cloudflare.com/durable-objects/best-practices/websockets/ | WebFetch confirmed verbatim. Full sentence on cited page: "Durable Objects can act as WebSocket servers that connect thousands of clients per instance." The quoted substring appears verbatim within the sentence under "Use WebSockets" section. Spec-025's `DOs` subject abbreviation differs from the cited page's `Durable Objects` subject, but the substring INSIDE the quotes is verbatim. | MATCH | N/A | false | N/A (MATCH) | Quoted substring verbatim on cited page. The text outside the quotes (Spec-025 wrote `DOs "…"`) is not itself a claimed-verbatim region; only the material inside the quote marks is subject to Lane B's substring primitive. | confirmed | false | null | null | — |
| SHF-B-029 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 457 | Flyway schema-history column list: "`installed_rank`, `version`, `description`, `type`, `script`, `checksum`, `installed_on`, `installed_by`, `execution_time`, `success`" | https://documentation.red-gate.com/fd/flyway-schema-history-table-184127273.html | Cited URL returned HTTP 403 via WebFetch. Alternate Red Gate documentation URL tried; also inaccessible. Column list is consistent with widely-documented Flyway schema-history schema but cannot be mechanically verified against the cited authoritative page under Lane B primitive. | NOISE | N/A | false | NOISE per §5: cited URL returns HTTP 403; substring verification blocked. Not DRIFT because absence-of-access is not absence-of-quote. | Primary source unreachable via WebFetch (403). Re-audit when documentation.red-gate.com is reachable or H3 triage may cross-reference via an alternative Red Gate mirror. | found | false | null | null | Access limitation is acknowledged inline by Spec-006 author (line 457 notes Red Gate docs as the authoritative source); Lane B records NOISE pending manual verification. |
| SHF-B-030 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 457 | Flyway Event enum values including `AFTER_EACH_MIGRATE` and `AFTER_MIGRATE_OPERATION_FINISH` | https://javadoc.io/doc/org.flywaydb/flyway-core/latest/org/flywaydb/core/api/callback/Event.html | WebFetch confirmed both enum values verbatim in the Flyway Event javadoc's "Enum Constant Summary" table: `AFTER_EACH_MIGRATE` and `AFTER_MIGRATE_OPERATION_FINISH` appear as declared enum constants alongside the full set (AFTER_BASELINE, AFTER_CLEAN, BEFORE_EACH_MIGRATE, etc.). | MATCH | N/A | false | N/A (MATCH) | Both enum names verbatim on cited javadoc page. | confirmed | false | null | null | — |
| SHF-B-031 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 457 | golang-migrate schema: "`version bigint not null primary key, dirty boolean not null`" | https://github.com/golang-migrate/migrate/blob/master/database/postgres/postgres.go | WebFetch of cited GitHub Go source confirmed: schema for the migration table uses columns `version bigint not null primary key, dirty boolean not null` verbatim inside the `CREATE TABLE IF NOT EXISTS` statement in the `ensureVersionTable` method. | MATCH | N/A | false | N/A (MATCH) | Column declaration string verbatim in the cited Go source. | confirmed | false | null | null | — |
| SHF-B-032 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 457 | Liquibase DATABASECHANGELOG `DEPLOYMENT_ID` column reference | https://docs.liquibase.com/concepts/tracking-tables/databasechangelog-table.html | Cited URL returned HTTP 403 via WebFetch. The `DEPLOYMENT_ID` field is widely documented as part of the Liquibase DATABASECHANGELOG table schema but cannot be mechanically verified against the cited authoritative page under Lane B primitive. | NOISE | N/A | false | NOISE per §5: cited URL returns HTTP 403; substring verification blocked. Not DRIFT because absence-of-access is not absence-of-quote. | Primary source unreachable via WebFetch (403). Re-audit when docs.liquibase.com is reachable or H3 triage may cross-reference via an alternative mirror (e.g., archived copy). | found | false | null | null | Access limitation acknowledged inline by Spec-006 author (line 457); Lane B records NOISE pending manual verification. |
| SHF-B-033 | B | h2 | docs/specs/006-session-event-taxonomy-and-audit-log.md | 476 | AWS Verified Permissions CloudTrail 15 action names: `CreatePolicyStore`, `DeletePolicyStore`, `CreateIdentitySource`, `UpdateIdentitySource`, `DeleteIdentitySource`, `CreatePolicyTemplate`, `UpdatePolicyTemplate`, `DeletePolicyTemplate`, `CreatePolicy`, `UpdatePolicy`, `DeletePolicy`, `PutSchema`, `IsAuthorized`, `IsAuthorizedWithToken`, `ListPolicies` | https://docs.aws.amazon.com/verifiedpermissions/latest/userguide/logging-using-cloudtrail.html | WebFetch confirmed all 15 action names verbatim in the CloudTrail logging documentation's action-list table under "Information captured in CloudTrail log entries" and the subsequent "Management events" section. | MATCH | N/A | false | N/A (MATCH) | All 15 API-action names verbatim on cited page. | confirmed | false | null | null | — |

---

## Summary Statistics

| Verdict | Count | Percent |
| --- | --- | --- |
| MATCH | 21 | 61.8% |
| DRIFT | 8 | 23.5% |
| NOISE | 5 | 14.7% |
| ORPHAN | 0 | 0% |
| MISSING | 0 | 0% |
| **Total** | **34** | **100%** |

Breakdown note: SHF-preseed-002 + SHF-B-001 through SHF-B-033 (contiguous, no gaps) = 34 finding-id rows.

| Severity (DRIFT rows only) | Count | Finding IDs |
| --- | --- | --- |
| CRITICAL | 0 | — |
| MAJOR | 4 | SHF-preseed-002, SHF-B-001, SHF-B-002, SHF-B-023 |
| MINOR | 4 | SHF-B-020, SHF-B-024, SHF-B-026, SHF-B-027 |

| Non-DRIFT tallies | Count | Finding IDs |
| --- | --- | --- |
| MATCH (N/A severity) | 21 | SHF-B-003, SHF-B-004, SHF-B-005, SHF-B-006, SHF-B-007, SHF-B-009, SHF-B-010, SHF-B-011, SHF-B-012, SHF-B-013, SHF-B-014, SHF-B-015, SHF-B-018, SHF-B-019, SHF-B-021, SHF-B-022, SHF-B-025, SHF-B-028, SHF-B-030, SHF-B-031, SHF-B-033 |
| NOISE (N/A severity) | 5 | SHF-B-008, SHF-B-016, SHF-B-017, SHF-B-029, SHF-B-032 |

All listed ID counts sum to structured row count (4 + 4 + 21 + 5 = 34).

---

## Discovery Notes (for H3 triage context)

1. **Citation-density hotspots (per §9.1 prioritization):** Session M docs (Spec-017, Plan-017, ADR-015, ADR-017, ADR-020, v1-feature-scope.md, cross-plan-dependencies.md, deployment-topology.md, local-sqlite-schema.md, backlog.md) were audited first. Only Spec-017 line 353 contained a quoted pattern, and it was an internal terminology note (not externally cited). The highest citation density in the in-scope corpus is Spec-006 (§Precedent blocks with RFC/CloudTrail/Sigstore/Cedar/GDPR/NIST citations).

2. **Out-of-scope (per §4):** `docs/research/` files (12+ files with blockquote/italic-quote patterns) were NOT audited; they are trusted sources per §9.3. `docs/reference/` files (paseo, forge, codexmonitor) are also out-of-scope. `.claude/` config files are excluded per §4.

3. **Lane orthogonality touchpoints:**
   - SHF-B-010 (GCP audit): attribution date drift noted for Lane F.
   - SHF-B-013 (CloudTrail digest): numeric "15 fields" claim noted for Lane K (actual count is 23).
   - SHF-B-009 (OTel semconv): URL→subpage precision noted for Lane A.
   - SHF-B-017 (Temporal Worker): URL 404 is an ORPHAN in Lane A; Lane B marked NOISE.
   - SHF-B-021 (Kafka): URL→javadoc precision noted for Lane A.

4. **WebFetch limitations encountered:**
   - nodejs.org page content truncated; verified via GitHub upstream markdown source.
   - EUR-Lex page returns JS-rendered empty content; verified via mirror.
   - XDG freedesktop.org redirects to empty content; NOISE pending alternative mirror.
   - NIST SP 800-88 R2 content is PDF-body-only; WebFetch only exposes the publication-listing page; NOISE.
   - Temporal Worker Deployment Versions URL returns 404 (SHF-B-017); NOISE with Lane A ORPHAN cross-reference.
   - documentation.red-gate.com Flyway schema-history page returns 403 (SHF-B-029); NOISE.
   - docs.liquibase.com DATABASECHANGELOG page returns 403 (SHF-B-032); NOISE. Access limitation acknowledged inline by Spec-006 author.

5. **Verbatim-quote-or-reject discipline compliance:** every claim-text and evidence-quote field contains verbatim substrings with no paraphrase. One compound-quote finding (SHF-B-026) reports partial-verbatim, marking the non-verbatim subpart as DRIFT.

6. **No secret-pattern redactions (§8.3):** no credentials, API keys, or tokens encountered in the quoted or evidence text.

---

*End Lane B findings.*
