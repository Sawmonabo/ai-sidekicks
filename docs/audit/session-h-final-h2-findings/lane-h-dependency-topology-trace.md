# Lane H — Dependency / Topology Trace Findings

**Lane ID:** H
**Primitive:** Parse `cross-plan-dependencies.md` §3 and §5; for each Plan: check claimed dependencies + tier placement + shared-resource claims match target plans' headers.
**Verdict scope:** Plan dependency graph; tier assignments; shared-resource declarations.
**Audit target:** `docs/architecture/cross-plan-dependencies.md` §2, §3, §5 cross-referenced against every plan header in `docs/plans/NNN-*.md`.
**Assigned pre-seed:** SHF-preseed-004 (secondary role — Lane E is primary).

---

## Summary

| Metric | Count |
| --- | --- |
| Total findings | 11 |
| MATCH | 0 |
| DRIFT | 10 |
| ORPHAN | 0 |
| MISSING | 1 |
| NOISE | 0 |
| CRITICAL | 0 |
| MAJOR | 7 |
| MINOR | 4 |
| Pre-seeded | 1 (SHF-preseed-004) |

---

## Findings Table

| finding-id | lane | finding-source | doc-path | line-range | claim-text | cited-source | evidence-quote | verdict | severity | severity-ambiguous | severity-rationale | verdict-rationale | remediation-status | pre-seeded | pre-seed-outcome | escalated-bl-ref | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SHF-H-001 | H | h2 | docs/plans/024-rust-pty-sidecar.md | 185-185 | "Tier 2 per BL-078 exit criteria — daemon-foundational; pairs with Plan-001 (shared session core) and Plan-007 (local IPC host)." | docs/architecture/cross-plan-dependencies.md §5 | "\| **1** \| Plan-001, Plan-024 \| None \| Plan-001 creates `sessions`, `session_memberships`, `session_events`... Plan-024 publishes the `packages/pty-sidecar/` umbrella + 5 platform packages and the `packages/contracts/src/pty-host.ts` contract; upstream of Plan-005." | DRIFT | MAJOR | false | Plan body explicitly claims a different tier than §5 placement; implementation agents reading the plan header would schedule Plan-024 at Tier 2 while §5 schedules it at Tier 1. | Plan-024 body line 185 states "Tier 2"; cross-plan §5 line 191 places Plan-024 in **Tier 1** alongside Plan-001. Plan-024 itself acknowledges "Placement update to cross-plan-dependencies.md §5 Canonical Build Order is out of scope for this plan and belongs to BL-054's propagation pass" but the drift remains. | found | false | null | null | Plan-024 line 193 also references "Tier 2 placement target for BL-054" in the References section — same drift. |
| SHF-H-002 | H | h2 | docs/plans/022-data-retention-and-gdpr.md | 237-237 | "This plan lands in **Tier 2** of the canonical build order per BL-045 exit criteria... BL-054's cross-plan-dependencies.md §5 propagation pass will... place Plan-022 in Tier 2 alongside Plan-002." | docs/architecture/cross-plan-dependencies.md §5 | "\| **5** \| Plan-004, Plan-008, Plan-018, Plan-022, Plan-025 (steps 1–4 only — see co-dep carve-out below) \| ... Plan-022 needs Plan-001 + Plan-007 (implementation code paths; schema already forward-declared in Tier 1)" | DRIFT | MAJOR | false | Plan body tier intent contradicts §5 Tier 5 placement; this is identical in structure to SHF-H-001 for Plan-024. | Plan-022 body line 237 claims "Tier 2 alongside Plan-002"; cross-plan §5 line 195 places Plan-022 in Tier 5 alongside Plan-004/008/018/025 steps 1-4. Body-declared placement is incorrect. | found | false | null | null | Schema forward-declaration ships at Tier 1 per §1 Contested rows; code-path placement is Tier 5 per §5 — Plan-022 body conflates these two. |
| SHF-H-003 | H | h2 | docs/plans/025-self-hostable-node-relay.md | 247-247 | "Tier 5-6, per `docs/architecture/cross-plan-dependencies.md` §5 Canonical Build Order." | docs/architecture/cross-plan-dependencies.md §5 | "\| **5** \| Plan-004, Plan-008, Plan-018, Plan-022, Plan-025 (steps 1–4 only — see co-dep carve-out below) \| ... \| **7** \| Plan-011, Plan-014, Plan-015, Plan-025 (remaining steps) \|" | DRIFT | MAJOR | false | Plan body claims "Tier 5-6" range but actual placement is Tier 5 (steps 1-4) + Tier 7 (remainder) — tier 6 is not involved; tier 7 is omitted from the body claim. | Plan-025's actual split is Tier 5 (steps 1-4) and Tier 7 (remainder). Plan-025 body stated range "Tier 5-6" is incorrect on both the upper bound (should include 7) and the middle (6 is not used). | found | false | null | null | This is the only tier-straddling plan in V1 per §5 carve-out; body text should reflect 5+7 not 5-6. |
| SHF-H-004 | H | h2 | docs/plans/011-gitflow-pr-and-diff-attribution.md | 49-49 | "Add local `diff_artifacts`, `branch_contexts`, and `pr_preparations` tables." | docs/architecture/cross-plan-dependencies.md §1 | "\| `branch_contexts` \| Plan-010 (Worktree Lifecycle And Execution Modes) \| Plan-011 (Gitflow PR And Diff Attribution) \| Plan-011 already declares Plan-010 as a dependency. Plan-010 creates the table as part of worktree infrastructure... Plan-011 extends it for PR and diff attribution." | DRIFT | MAJOR | false | Plan-011 body verb "Add local ... branch_contexts" reads as CREATE; §1 Contested row says Plan-011 EXTENDS (ALTER/USE only). Implementation agents reading Plan-011 would author a CREATE that clashes with Plan-010's migration. | Plan-011 owns `diff_artifacts` + `pr_preparations` (CREATE per §1 Uncontested line 41) but extends `branch_contexts` — grouping all three under "Add local X, Y, Z" conflates CREATE and EXTEND. | found | false | null | null | §1 Contested row line 22 explicitly names Plan-010 as creator and Plan-011 as extender. |
| SHF-H-005 | H | h2 | docs/plans/002-invite-membership-and-presence.md | 49-49 | "Add shared `session_invites` and `session_memberships` tables." | docs/architecture/cross-plan-dependencies.md §1 | "\| `session_memberships` \| Plan-001 (Shared Session Core) \| Plan-002 (Invite Membership And Presence) \| Plan-001 is the session foundation; Spec-002 depends on Spec-001. Plan-001 creates the table with core columns... Plan-002 adds invite-driven membership flows but does not own the schema." | DRIFT | MAJOR | false | Plan-002 body verb "Add shared ... session_memberships" reads as CREATE; §1 Contested row explicitly states Plan-001 creates the table and Plan-002 does not own the schema. Clash risk for migration authoring agents. | Plan-002 owns `session_invites` (CREATE per §1 Uncontested line 33) but extends `session_memberships` — the two are grouped under "Add shared X and Y" in a way that implies CREATE for both. | found | false | null | null | Same structural pattern as SHF-H-004. |
| SHF-H-006 | H | h2 | docs/architecture/cross-plan-dependencies.md | 31-55 | "\| Plan-018 \| `participants`, `identity_mappings` (Postgres) \|" (Uncontested Tables row) | docs/plans/001-shared-session-core.md | "Add the minimal `participants` identity-anchor table (`id UUID PK`, `created_at TIMESTAMPTZ`) to Collaboration Control Plane storage **before** any FK-bearing shared table... Plan-001 owns the physical CREATE of the minimal shape only; identity/profile columns (`display_name`, `identity_ref`, `metadata`) and the `identity_mappings` side table are added by Plan-018 via additive ALTER migrations." | DRIFT | MAJOR | false | §1 treats `participants` as uncontested with Plan-018 as sole owner, but Plan-001 body line 50 says Plan-001 does the physical CREATE. This is a classic Contested-Tables row (forward-declared split) miscategorized as Uncontested. | Plan-001's CREATE + Plan-018's ALTER pattern matches the Plan-001/Plan-022 forward-declared split already in §1 Contested for `participant_keys` and `session_events.pii_payload` — `participants` belongs in the same Contested shape. | found | false | null | null | Also: Plan-001 line 69 references `[Plan-018](./018-identity-provider-adapters.md)` — file does not exist (Lane C finding; noted for cross-lane reference). |
| SHF-H-007 | H | h2 | docs/architecture/cross-plan-dependencies.md | 69-69 | "\| `apps/desktop/renderer/` \| Plan-023 (creates the React + Vite renderer app at Tier 8) \| Plan-009, Plan-011, Plan-012, Plan-013, Plan-014, Plan-016, Plan-017, Plan-019, Plan-026 (all Tier 8 or later, composed as renderer views) \| Each extending plan adds renderer views as thin projections over the Spec-023 preload-bridge surface (`window.sidekicks`). Extending plans must not bypass the bridge to reach daemon or control-plane state directly. Plan-013's live timeline components land here under `src/timeline/` (Plan-013's Tier 8 placement is the earliest tier at which `apps/desktop/renderer/` exists)." | docs/plans/023-desktop-shell-and-renderer.md | "`apps/desktop/` — **new app package.** The Electron main process (`apps/desktop/electron/main/`), the preload bridge (`apps/desktop/electron/preload/`), and the React + Vite renderer (`apps/desktop/src/`)." | DRIFT | MAJOR | false | §2 claims Plan-023 creates `apps/desktop/renderer/`, but Plan-023 body declares the renderer at `apps/desktop/src/`, NOT `apps/desktop/renderer/`. This breaks the shared-resource ownership claim at the path-level: Plan-023 is not creating the directory that the nine extending plans say they extend. | Plan-023's target areas grep shows zero references to `apps/desktop/renderer/`; the renderer is uniformly rooted at `apps/desktop/src/` (lines 103-109 create `apps/desktop/src/main.tsx`, `apps/desktop/src/App.tsx`, `apps/desktop/src/features/timeline/TimelineView.tsx`, etc.). | found | false | null | null | Systemic path drift: either §2 must change to `apps/desktop/src/` or Plan-023 must re-root to `apps/desktop/renderer/`. Also affects `docs/architecture/container-architecture.md` §43-44 which says `apps/desktop/renderer/`. |
| SHF-H-008 | H | h2 | docs/architecture/cross-plan-dependencies.md | 69-69 | "Plan-009, Plan-011, Plan-012, Plan-013, Plan-014, Plan-016, Plan-017, Plan-019, Plan-026 (all Tier 8 or later, composed as renderer views)" | docs/plans (grep of all plans using `apps/desktop/renderer/src/...` in Target Areas) | 11 additional plans actually target `apps/desktop/renderer/src/<subdir>/` in their Target Areas: Plan-001 (`session-bootstrap/`), Plan-002 (`session-members/`), Plan-003 (`runtime-node-attach/`), Plan-004 (`run-controls/`), Plan-006 (`timeline/` for audit-stub rendering), Plan-007 (`daemon-status/`), Plan-008 (`session-join/`), Plan-010 (`execution-mode-picker/`), Plan-015 (`recovery-status/`), Plan-018 (`participants/`), Plan-020 (`health-and-recovery/`). | MISSING | MAJOR | false | §2 extenders list is incomplete — 11 plans create renderer subtrees under `apps/desktop/renderer/src/` but are not declared as extenders. Implementation agents for those plans will author renderer files without the "thin projection over bridge" discipline §2 imposes. | Shared-resource declaration coverage gap: §2 row is load-bearing for the renderer-untrusted-over-bridge architecture invariant; missing extenders bypass that commitment. | found | false | null | null | Note: if SHF-H-007 is resolved by re-rooting §2 to `apps/desktop/src/`, this finding's plan list also shifts. |
| SHF-H-009 | H | h2 | docs/plans/026-first-run-onboarding.md | 26-26 | "`apps/desktop/src/onboarding/` — **new renderer walkthrough.** VS-Code-walkthrough-patterned step-through UI..." | docs/architecture/cross-plan-dependencies.md §2 line 69 | "\| `apps/desktop/renderer/` \| Plan-023 (creates the React + Vite renderer app at Tier 8) \| Plan-009, Plan-011, Plan-012, Plan-013, Plan-014, Plan-016, Plan-017, Plan-019, Plan-026 (all Tier 8 or later, composed as renderer views) \|" | DRIFT | MINOR | false | §2 names Plan-026 as an extender of `apps/desktop/renderer/`, but Plan-026 body uses `apps/desktop/src/onboarding/` — consistent with Plan-023's `apps/desktop/src/` root, inconsistent with the §2 claim. | Finding is MINOR-not-MAJOR because Plan-026's path drift is coupled to and fully caused by SHF-H-007 (Plan-023's root mismatch); fixing SHF-H-007 resolves this automatically. | found | false | null | null | Coupled dependency with SHF-H-007. |
| SHF-H-010 | H | h2 | docs/plans/017-workflow-authoring-and-execution.md | 43-43 | "- `packages/contracts/src/workflows/`" | docs/architecture/cross-plan-dependencies.md §2 line 70 | "\| `packages/contracts/src/` \| No single owner — single-file-per-contract convention \| Plan-024 (`pty-host.ts` precedent), Plan-021 (`rate-limiter.ts`) \| The directory is a shared home for cross-plan contract files. No two plans edit the same file, so no shared-resource conflict exists." | DRIFT | MINOR | false | SHF-preseed-004 pre-seeded row — subdirectory convention tension. Plan-017 introduces `packages/contracts/src/workflows/` subdirectory, diverging from the §2 "single-file-per-contract" convention (Plan-024 used `pty-host.ts`, Plan-021 used `rate-limiter.ts`). The §2 convention text does not forbid subdirectories explicitly, so severity is MINOR — but the convention language is out-of-date. | Plan-017 body line 43 declares `packages/contracts/src/workflows/` subdirectory; §2 line 70 says the convention is "single-file-per-contract". Tension confirmed as still present — matches BL-097 Resolution §7(d) pre-seed claim. | confirmed | true | confirmed-deferred | null | Pre-seed SHF-preseed-004 (BL-097 Resolution §7(d)). BL-097 Resolution verbatim: "convention-extension call (single-file-or-single-subdirectory) deferred until a second subdirectory candidate surfaces." No second subdirectory exists yet, so deferral rationale still holds. Lane E is primary owner; this row is Lane H's secondary confirmation. |
| SHF-H-011 | H | h2 | docs/architecture/cross-plan-dependencies.md | 198-198 | "Plan-017 creates 9-table workflow schema (see §1) + `packages/contracts/src/workflows/` and `packages/runtime-daemon/src/workflows/`; Plan-017 also extends `apps/desktop/renderer/` per §2 (workflow renderer subtree at `src/workflows/`)" | docs/architecture/cross-plan-dependencies.md §2 | "\| `packages/contracts/src/` \| No single owner — single-file-per-contract convention \| Plan-024 (`pty-host.ts` precedent), Plan-021 (`rate-limiter.ts`) \|" — §2 has no row for `packages/contracts/src/workflows/` or `packages/runtime-daemon/src/workflows/` | DRIFT | MINOR | false | §5 Tier 8 names `packages/contracts/src/workflows/` and `packages/runtime-daemon/src/workflows/` as Plan-017's shared resources, but §2 (Package Path Ownership Map) does not carry a corresponding row for either path. The ownership map is the canonical surface for shared-resource declarations; §5's mention is load-bearing but duplicative only if §2 records it. | Missing §2 rows for two new Plan-017 path claims; §5 carries the substance but §2 is where maintenance-time reviewers look per §6 rule 3 ("Add the plan to the Package Path Ownership Map (Section 2) if it creates or extends a shared directory."). | found | false | null | null | Coupled with SHF-H-010 (subdirectory convention). Both point at the same missing §2 rows for Plan-017 directories. |

---

## SHF-preseed-004 Outcome

**Claim verified:** Plan-017 declares `packages/contracts/src/workflows/` as a subdirectory (line 43 of `docs/plans/017-workflow-authoring-and-execution.md`), and cross-plan-dependencies §2 line 70 still carries the "single-file-per-contract convention" language without an extension for subdirectory members.

**Pre-seed outcome:** `confirmed-deferred`

**Rationale:** Drift still exists in the corpus exactly as BL-097 Resolution §7(d) described it. BL-097's deferral rationale — "convention-extension call (single-file-or-single-subdirectory) deferred until a second subdirectory candidate surfaces" — still holds. No second subdirectory candidate has surfaced in any other plan's `packages/contracts/src/` target area (verified across Plans 001–026). This finding maps to `remediation-status: deferred`.

**Mapping:** SHF-H-010 is the ledger-facing row for this pre-seed. Lane E is primary owner of SHF-preseed-004; this Lane H row adds the `packages/contracts/src/workflows/` shared-resource consistency confirmation.

---

## Top 3 CRITICAL Findings

None. No Lane H finding rose to CRITICAL severity — all drift is contained within the documentation-only corpus and would manifest as migration-time conflicts or rework cost, not as implementation-breaking or security-critical errors. The two highest-impact MAJOR findings are:

1. **SHF-H-007** — Plan-023's renderer rooted at `apps/desktop/src/` while §2 declares `apps/desktop/renderer/` as the shared resource. Systemic path drift affecting 9+ downstream plans. This is the highest-priority Lane H finding.
2. **SHF-H-001** — Plan-024 body claims Tier 2 while §5 places it at Tier 1. Similar pattern to SHF-H-002 (Plan-022 body Tier 2 vs §5 Tier 5) and SHF-H-003 (Plan-025 body Tier 5-6 vs §5 Tier 5+7).
3. **SHF-H-008** — §2 extender list for `apps/desktop/renderer/` is missing 11 plans that actually target renderer subtrees. Shared-resource declaration coverage gap.

---

## Blockers

None. Lane H executed without dispatching subagents, without any URL fetches, and with the research directory excluded per §9.3. All findings are grounded in verbatim quotes from the cross-plan-dependencies.md §2/§3/§5 and each plan's header + body as required by §8.1.

## Notes for H3 Triage

- **Severity ambiguity policy (§6):** No findings are flagged `severity-ambiguous: true`. The MINOR-vs-MAJOR escalation clause was considered for SHF-H-010 and SHF-H-011 (§2 shared-resource declaration is a maintenance-sensitive surface) but the absence of MUST language and the explicit BL-097 §7(d) pre-seed deferral rationale pin these at MINOR.
- **Orthogonality (§5):** SHF-H-007 and SHF-H-009 are closely coupled (Plan-023 root vs Plan-026 path); SHF-H-010 and SHF-H-011 are closely coupled (both point at the same missing §2 rows). H3 triage may want to merge each pair under a single remediation plan section, while keeping distinct finding-IDs.
- **Cross-lane overlap:** SHF-H-006 flagged a Plan-001 broken link to `018-identity-provider-adapters.md` (file does not exist) — that is Lane C's domain; Lane H notes it only because it was encountered during header trace. H3 should expect the Lane C output to carry the authoritative row for that ORPHAN.
- **No Plan-017 body tier section:** Plan-017 (high-scrutiny per §9.1) has no `## Tier Intent` or `## Tier Placement` section in its body — tier placement lives only in the cross-plan-dependencies.md §5 row. Plan-017's header line 13 dependency list matches §3 line 105 exactly. Plan-017 is the cleanest post-rewrite plan in the Lane H sweep.
