# Backlog Archive

Completed backlog items from the implementation readiness phase. Retained for traceability — BL-XXX IDs are referenced by the reference library and implementation plans.

## Completed: 2026-04-15

| BL | Title | Deliverable | Phase |
| --- | --- | --- | --- |
| BL-001 | Accept all 8 ADRs | ADR-001 through ADR-008 accepted | Pre-phase |
| BL-002 | Choose IPC wire format | ADR-009 (JSON-RPC 2.0) | Pre-phase |
| BL-007 | Add intervention driver operations | ADR-011, Spec-005 (`applyIntervention`) | Pre-phase |
| BL-013 | Resolve sequence-assignment contradiction | Spec-006 (single-authority model) | Pre-phase |
| BL-014a | Decide workflow V1 scope | Spec-017 (single-agent + automated phases, all 4 gates) | Pre-phase |
| BL-006 | Cross-plan dependency graph | `docs/architecture/cross-plan-dependencies.md` | Phase 1 |
| BL-003 | Database schemas | `docs/architecture/schemas/local-sqlite-schema.md`, `shared-postgres-schema.md` | Phase 1 |
| BL-004 | API payload contracts | `docs/architecture/contracts/api-payload-contracts.md` | Phase 1 |
| BL-005 | Auth implementation spec | `docs/architecture/security-architecture.md` (5 sections added) | Phase 1 |
| BL-008 | Run state machine transition table | `docs/domain/run-state-machine.md` (30-row table, child-run cascade) | Phase 2 |
| BL-009 | Intervention model reconciliation | `docs/domain/queue-and-intervention-model.md` (transition table, field consistency) | Phase 2 |
| BL-010 | Invite delivery mechanism | `docs/specs/002-invite-membership-and-presence.md` (shareable link, token security, rate limits) | Phase 2 |
| BL-012 | Enumerate event types | `docs/specs/006-session-event-taxonomy-and-audit-log.md` (76 types) | Phase 3 |
| BL-026 | Error contracts | `docs/architecture/contracts/error-contracts.md` + 22 spec updates | Phase 3 |
| BL-022 | Runtime binding glossary | `docs/domain/glossary.md` (RuntimeBinding entry) | Phase 3 |
| BL-024 | Steer injection mechanics | `docs/specs/004-queue-steer-pause-resume.md` (driver-level steer) | Phase 3 |
| BL-025 | Presence heartbeat + channel discovery | `docs/specs/002-invite-membership-and-presence.md` (heartbeat transport, ChannelList) | Phase 3 |
| BL-015 | Per-driver capability matrix | `docs/specs/005-provider-driver-contract-and-capabilities.md` (Codex/Claude matrix) | Phase 3 |
| BL-016 | Owner elevation + membership conflicts | `docs/domain/participant-and-membership-model.md`, Spec-002 | Phase 3 |
| BL-017 | Session/channel/participant limits | `docs/specs/001-shared-session-core.md` (6 resource limits) | Phase 3 |
| BL-018 | Handoff timeline entry type | `docs/specs/013-live-timeline-visibility-and-reasoning-surfaces.md` | Phase 3 |
| BL-019 | Workspace-to-worktree transitions | `docs/specs/009-repo-attachment-and-workspace-binding.md` | Phase 3 |
| BL-020 | DiffArtifact vs general artifact | `docs/specs/011-gitflow-pr-and-diff-attribution.md` (subtype, CAS storage) | Phase 3 |
| BL-021 | Approval category enum | `docs/specs/012-approvals-permissions-and-trust-boundaries.md` (9 categories verified after SA-12) | Phase 3 |
| BL-023 | Relay protocol | `docs/specs/008-control-plane-relay-and-session-join.md` (MLS, connection lifecycle) | Phase 3 |
| BL-011 | Channel turn/budget/stop policies | `docs/specs/016-multi-agent-channels-and-orchestration.md` | Phase 3 |
| BL-014b | Expand workflow spec | `docs/specs/017-workflow-authoring-and-execution.md` (phase/gate taxonomy) | Phase 3 |
| BL-037 | Git hosting adapter | `docs/specs/011-gitflow-pr-and-diff-attribution.md` (gh CLI, GitHostingAdapter) | Phase 3 |
| BL-027 | V1 feature scope | `docs/architecture/v1-feature-scope.md` (14 V1, 6 V2) | Phase 4 |
| BL-028 | Workflow domain models | `docs/domain/workflow-model.md`, `workflow-phase-model.md` | Phase 4 |
| BL-029 | Control-plane transport | `docs/specs/008-control-plane-relay-and-session-join.md` (tRPC + WebSocket) | Phase 4 |
| BL-030 | Deployment scaling + HA | `docs/architecture/deployment-topology.md` (6 sections) | Phase 4 |
| BL-031 | Operations runbook commands | 8 runbook files (CLI, SLOs, on-call routing) | Phase 4 |
| BL-032 | Event compaction policy | `docs/specs/006-session-event-taxonomy-and-audit-log.md` (triggers, retention, audit stubs) | Phase 4 |
| BL-033 | Rate limit values | `docs/specs/021-rate-limiting-policy.md` (10 endpoints, 3 tiers) | Phase 4 |
| BL-034 | Context window + usage meters | `docs/specs/013-live-timeline-visibility-and-reasoning-surfaces.md` | Phase 4 |
| BL-035 | Notification delivery | `docs/specs/019-notifications-and-attention-model.md` (desktop SSE, cross-device) | Phase 4 |
| BL-036 | GDPR schema | `docs/specs/022-data-retention-and-gdpr.md` (PII map, crypto-shredding) | Phase 4 |

## Crosscheck Summary

| Review      | Checks  | Fixes                          |
| ----------- | ------- | ------------------------------ |
| Phase 1     | 92      | 15 (table names in dep graph)  |
| Phase 2     | 24      | 1 (invite lifecycle states)    |
| Phase 3     | 29      | 0                              |
| Phase 4     | 25      | 2 (Spec-015 xref, rate limits) |
| Full-corpus | 85      | 0                              |
| **Total**   | **259** | **18**                         |

## Completed: 2026-04-17 → 2026-04-25 (V1 doc-first phase: BL-038 through BL-097)

This set was generated from the 2026-04-16 pre-implementation architecture audit (session ID `2026-04-16-arch-audit-163537`) and the V1 scope definition formalized in BL-038 (originally: V1 = 16 features + V1.1 = 4 features). Per [ADR-015 Amendment 2026-04-22](../decisions/015-v1-feature-scope-definition.md#amendment-history) and [BL-097 Resolution](#bl-097-reconcile-workflow-v1-scope-drift-spec-017-vs-v1-feature-scopemd), the canonical end-state is **V1 = 17 features** (BL-097 promoted workflow authoring from V1.1 to V1) and **V1.1 = 3 features** (MLS relay E2EE, email invite delivery, cross-node shared artifacts). **Note: BL summary, exit-criteria, and resolution text below preserves the original 2026-04-17 V1=16 / V1.1=4 charter state for audit provenance — these are append-only historical work-item charters, not live scope claims; canonical values are defined in [ADR-015](../decisions/015-v1-feature-scope-definition.md).** Items are ordered by execution dependency: Phase 0 anchored downstream work; Phases 1–4 were P0 blockers for Plan-001 coding; Phases 5–6 ran in parallel with Phase 4; Phase 7 was hygiene to land before V1 ship; Phase 8 absorbed the plan-approval-pass follow-ups.

Format note: this section preserves full BL charters (Status / Priority / Owner / Depends-on / Blocks / References / Summary / Exit Criteria / Resolution) inline rather than the 1-line table format used for the 2026-04-15 batch above, because BL-038…BL-097 encode load-bearing decisions (security invariants, schema commitments, ADR rationale, primary-source citations) that the table format would lose.

### Phase 0 — V1 Scope Anchor (P0)

Everything downstream depends on these landing first. Without the anchor, subsequent spec/plan edits drift.

#### BL-038: Write ADR-015 V1 Feature Scope Definition

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [v1-feature-scope.md](../architecture/v1-feature-scope.md), [vision.md](../vision.md), [BL-027 archive entry](#completed-2026-04-15) (prior V1 triage provenance)
- Summary: Write ADR-015 declaring the V1 feature scope. V1 consists of 16 features: Session creation and join, Mid-session invites, Membership roles, Runtime node attach/detach, Single-agent runs, Queue/steer/pause/resume, Approval gates, Repo attach, Worktree execution, Session timeline, Local daemon with CLI, Presence, Event audit log, Artifact publication (local), Desktop GUI, and Multi-Agent Channels. V1.1 defers MLS relay E2EE, email invite delivery, cross-node shared artifacts, and workflow authoring. The ADR text must read as a definition (no "expansion," "rescope," "now includes," or other retrospective/amendment phrasing). Alternatives section must steel-man at least: (a) a narrower V1 deferring Desktop GUI and Multi-Agent Channels, (b) a tiered M1–M4 milestone track. Decision Log references the 2026-04-16 audit (session `2026-04-16-arch-audit-163537`). BL-039 rewrites `v1-feature-scope.md` to cite this ADR.
- Exit Criteria: `docs/decisions/015-v1-feature-scope-definition.md` exists with Context, Decision, Alternatives, Consequences, Reversibility, and Decision Log sections; status `accepted`; the ADR body contains no retrospective/amendment phrasing (`rescope`, `expand V1`, `now includes`, `is being added`, `reversal`, `revert`, or similar).

#### BL-039: Rewrite v1-feature-scope.md against ADR-015

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-038 (ADR-015 must exist for this doc to cite it authoritatively)
- References: [v1-feature-scope.md](../architecture/v1-feature-scope.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md) (from BL-038)
- Summary: Rewrite the triage into a V1 / V1.1 / V2 three-way split consistent with ADR-015. V1 = 16 features per ADR-015. V1.1 = 4 features (MLS relay E2EE, Email invite delivery, Cross-node shared artifacts, Workflow authoring). V2 = out-of-scope for the foreseeable horizon. Update the Spec Coverage and Backlog Coverage Assessment paragraphs to reflect V1 = 16 features. The document must read as the authoritative V1 scope definition; do not use "expansion," "now includes," "moving to V1," or similar amendment phrasing.
- Exit Criteria: `v1-feature-scope.md` declares V1 = 16 features, V1.1 = 4 features, and V2 = out-of-scope; cites ADR-015 as the governing decision; references Spec-023 (from BL-041) for the Desktop GUI row; internal references to Spec-016 and Spec-017 reflect their V1 / V1.1 status; the document contains no retrospective/amendment phrasing and reads as a greenfield V1 definition to a first-time reader.
- Resolution (resolved 2026-04-17 per Session TBD): `docs/architecture/v1-feature-scope.md` lands V1 = 16 / V1.1 = 4 / V2 split per ADR-015 (commit `c587eee` Tier 1 V1 scope anchors); historical V1 = 16 framing later superseded by BL-097 Session-M V1 = 17 amendment, retained as legitimate historical state per Lane G clarifier (META-2 primitive verified 2026-04-22 per H5 §3.1 row 3.1).

#### BL-040: Write ADR-016 Electron Desktop Shell (Tauri/Wails rejected)

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [vision.md](../vision.md) §Technology Position, [component-architecture-desktop-app.md](../architecture/component-architecture-desktop-app.md), audit artifact `tauri-wails-vs-electron-evaluation.md` (session 2026-04-16-arch-audit-163537)
- Summary: Write ADR-016 declaring Electron as the chosen desktop shell, with Tauri 2.x and Wails v3 evaluated as alternatives and rejected. Primary rejection reason: WebKitGTK has no WebAuthn support in 2026, which breaks the Linux passkey/PRF flow required by [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md). Secondary reasons: solo-dev Rust/Go learning cost; documented cases of teams migrating from Tauri to Electron citing WKWebView vs WebView2 QA burden; Wails v3 still alpha with no flagship production apps. Include revisit triggers (WebKitGTK WebAuthn lands; team adds Rust-comfortable engineer). ADR text must read as a forward-declaration, not as a reversal of a prior shipped choice (no "keep Electron," "revert to Electron," or "rollback" framing — no desktop shell was previously chosen in code).
- Exit Criteria: `docs/decisions/016-electron-desktop-shell.md` exists with Context, Decision, Alternatives (Tauri 2.x and Wails v3 steel-manned), Consequences, Reversibility, and Decision Log sections; ADR status `accepted`; ADR body contains no amendment phrasing (`kept`, `reverted`, `rolled back`, `reversed`). This ADR subsumes the audit's Rec-28 paragraph-in-vision approach.
- Resolution (resolved 2026-04-17 per Session TBD): `docs/decisions/016-electron-desktop-shell.md` lands accepted with Context, Decision, Alternatives (Tauri 2.x + Wails v3 steel-manned), Consequences, Reversibility, and Decision Log sections (commit `c587eee` Tier 1 V1 scope anchors); META-2 verification 2026-04-22 confirmed Decision Log + Tripwires + reversibility lines 28+/83+/159-160 present (H5 §3.1 row 3.2).

### Phase 1 — Spec & Plan Gaps in V1 (P0)

V1 (per ADR-015) includes a Desktop GUI feature with no governing spec today, and two cross-cutting V1 specs (Spec-021 rate limiting, Spec-022 data retention / GDPR) with no implementation plans. These specs and plans must exist before Plan-001 starts.

#### BL-041: Write Spec-023 Desktop Shell + Renderer

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [vision.md](../vision.md) §§1–2, [component-architecture-desktop-app.md](../architecture/component-architecture-desktop-app.md), [ADR-009](../decisions/009-json-rpc-ipc-wire-format.md), [Spec-007](../specs/007-local-ipc-and-daemon-control.md), [specs/000-spec-template.md](../specs/000-spec-template.md)
- Summary: Author a dedicated spec for Electron main/preload and React+Vite renderer. Cover: main-process responsibilities (windowing, native dialogs, notifications, auto-update, daemon supervision, deep-link handling for invite URLs); preload bridge surface; renderer→daemon IPC using the same JSON-RPC 2.0 contract as the CLI via the shared client SDK; trust stance (resolve renderer-trust contradiction per BL-056); code-signing + notarization; auto-update flow (electron-updater with delta patches).
- Exit Criteria: `docs/specs/023-desktop-shell-and-renderer.md` exists, conforms to spec template, and every Signature Feature view (timeline, approvals, invites, runs, multi-agent channels) has a composition sketch pointing to the owning plan.
- Resolution (resolved 2026-04-17 per Session 2a): `docs/specs/023-desktop-shell-and-renderer.md` lands per spec template (commit `d36704b` Session 2a V1-readiness specs); cross-cited target for BL-043 / BL-056 / BL-081 / BL-082 / BL-085 — all consumer cites resolve post-H2 glob verification 2026-04-22 (H5 §3.1 row 3.3).

#### BL-042: V1-Readiness Review of Spec-016 Multi-Agent Channels

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Spec-016](../specs/016-multi-agent-channels-and-orchestration.md)
- Summary: Spec-016 (Multi-Agent Channels and Orchestration) is a V1 feature per ADR-015. Review it against the V1 quality bar: turn policy defaults (not just "configurable"), budget policy defaults, stop conditions, moderation/approval hooks, turn arbitration under partial network partition, and interaction with [ADR-011 intervention dispatch](../decisions/011-generic-intervention-dispatch.md). Tighten any ambiguous sections and promote "configurable" surfaces to named defaults.
- Exit Criteria: Spec-016 review note appended; tightening edits landed; spec status header explicitly declares V1 quality bar.
- Resolution (resolved 2026-04-17 per Session 2a): Spec-016 V1 Quality Bar declared in header table (line 11) + V1 Readiness Review (BL-042) section appended at line 189 (commit `d36704b` Session 2a V1-readiness specs); META-2 verification 2026-04-22 (H5 §3.1 row 3.4).

#### BL-043: Create Plan-023 Desktop Shell + Renderer

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-041 (Plan-023 implements Spec-023)
- References: [Spec-023](../specs/023-desktop-shell-and-renderer.md) (from BL-041), [plans/000-plan-template.md](../plans/000-plan-template.md), [cross-plan-dependencies.md](../architecture/cross-plan-dependencies.md)
- Summary: Author the implementation plan for `apps/desktop/shell/` and `apps/desktop/renderer/`. Sequence the renderer foundation after the CLI proves the client SDK (per vision build-order step 6), but still inside V1. Slot into cross-plan tier 7 or 8 alongside timeline visibility (Plan-013) and artifacts (Plan-014).
- Exit Criteria: `docs/plans/023-desktop-shell-and-renderer.md` exists. (Cross-plan-dependencies.md tier-graph update is BL-054's scope; Plan-023's body states the tier intent.)
- Resolution (resolved 2026-04-17 per Session 3c): `docs/plans/023-desktop-shell-and-renderer.md` lands per plan template (commit `f83dcc6` Session 3c Plan-023 + Plan-026); Spec-023 header `Implementation Plan` field references this plan slot per verified-by-design construction (H5 §3.1 row 3.5).

#### BL-044: Create Plan-021 Rate Limiting

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Spec-021](../specs/021-rate-limiting-policy.md), [ADR-014](../decisions/014-trpc-control-plane-api.md), [deployment-topology.md](../architecture/deployment-topology.md)
- Summary: Author implementation plan for Spec-021. Scope: a `RateLimiter` abstraction swappable between Cloudflare native and `rate-limiter-flexible` Postgres-backed; per-endpoint enforcement on every control-plane tRPC + WS route; `/admin/bans` endpoint; three-stage escalation ladder (3 violations → 15-min block; 10 → 1-hr block; admin-issued permanent); ban-rate and false-positive-rate metrics.
- Exit Criteria: `docs/plans/021-rate-limiting-policy.md` exists; Spec-021 header's "Implementation Plan" field no longer reads `_(none yet)_`. (Cross-plan-dependencies.md §5 tier-graph update is BL-054's scope; Plan-021's body states the tier intent and dependency on Plan-008.)
- Resolution (resolved 2026-04-17 per Session 3b): `docs/plans/021-rate-limiting-policy.md` lands per plan template (commit `c1e4225` Session 3b Plan-021 + Plan-025); Spec-021:11 `Implementation Plan` field reads `[Plan-021: Rate Limiting Policy](../plans/021-rate-limiting-policy.md)` per grep verification 2026-04-22 (H5 §3.1 row 3.6).

#### BL-045: Create Plan-022 Data Retention / GDPR

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Spec-022](../specs/022-data-retention-and-gdpr.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md) (`participant_keys`, `session_events.pii_payload`), [Plan-018](../plans/018-identity-and-participant-state.md)
- Summary: Author implementation plan for Spec-022. Scope: HKDF-SHA256 per-participant key derivation service; `participant_keys.encrypted_key_blob` + daemon master-key storage in OS keystore (libsecret / Keychain / DPAPI); AES-256-GCM encrypt/decrypt on `session_events.pii_payload` in the write path; purge state machine (`purge_requested → purged`); export endpoint (`GET /participants/{id}/data`); PII fan-out on shred. Pull this plan into Tier 2 at latest so encryption write-path ships with the first write.
- Exit Criteria: `docs/plans/022-data-retention-and-gdpr.md` exists and conforms to the plan template; Spec-022's header `Implementation Plan` field no longer reads `_(none yet)_` and links to Plan-022; Plan-022's body states the Tier 2 intent so BL-054 can place it correctly in the cross-plan dependency graph.
- Resolution (resolved 2026-04-17 per Session 3a): `docs/plans/022-data-retention-and-gdpr.md` lands per plan template (commit `e854891` Session 3a Plan-022 + Plan-024); Spec-022:11 `Implementation Plan` field reads `[Plan-022: Data Retention And GDPR Compliance](../plans/022-data-retention-and-gdpr.md)` per grep verification 2026-04-22 (H5 §3.1 row 3.7).

#### BL-078: Write Plan-024 Rust PTY Sidecar

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-052 (ADR-019 must land first; this plan implements the sidecar strategy it decides)
- References: [ADR-019](../decisions/019-windows-v1-tier-and-pty-sidecar.md) (from BL-052), [ADR-009](../decisions/009-json-rpc-ipc-wire-format.md), [deployment-topology.md](../architecture/deployment-topology.md) §Container and Packaging, [Spec-007](../specs/007-local-ipc-and-daemon-control.md) (daemon IPC context), [component-architecture-local-daemon.md](../architecture/component-architecture-local-daemon.md); reference impl: [wezterm/pty (`portable-pty`)](https://github.com/wezterm/wezterm/tree/main/pty); distribution precedent: `@esbuild/*` platform-package set.
- Summary: Author the implementation plan for the Rust PTY sidecar that ADR-019 names as the Windows primary backend. Scope: (1) Rust crate structure on `portable-pty` (wezterm) with a single binary target per platform; (2) sidecar IPC protocol — LSP-style Content-Length framing over stdio with a JSON control channel (`spawn`, `resize`, `kill`, `exit-code`, `ping`) and a length-prefixed binary data channel for stdout/stderr; (3) `PtyHost` TypeScript interface in `packages/contracts/` with two implementations (`RustSidecarPtyHost`, `NodePtyHost`) and a platform selector — sidecar primary on Windows, node-pty primary on macOS/Linux; (4) distribution via `@ai-sidekicks/pty-sidecar-{win32-x64,darwin-arm64,darwin-x64,linux-x64,linux-arm64}` packages following the `@esbuild/*` optionalDependencies + `os`/`cpu` filter pattern so `npm install` pulls only the correct binary; (5) Windows code-signing pipeline — Azure Trusted Signing primary, EV cert fallback (per ADR-019); (6) daemon-side supervisor (process lifecycle, auto-restart on crash, graceful shutdown, backpressure) tied to session lifecycle; (7) test matrix — clean spawn/resize/kill, PowerShell 7 regression coverage (guards `microsoft/node-pty#894` class), concurrent session stress, daemon-restart recovery, panic-in-sidecar crash handling, IPC framing fuzz; (8) CI build matrix (cross-compile Rust for all five platform targets); (9) observability (sidecar-emitted structured logs merged into daemon log stream, metrics for spawn latency and restart count).
- Exit Criteria: `docs/plans/024-rust-pty-sidecar.md` exists and conforms to the plan template; cites ADR-019 and ADR-009; Tier placement stated (expected Tier 2 — daemon-foundational; pairs with Plan-001 and Plan-007); `PtyHost` interface contract referenced from `packages/contracts/` scope; Windows signing pipeline step enumerated; CI cross-compile matrix named; no retrospective/amendment phrasing. Cross-plan-dependencies.md update is BL-054's scope, not this BL's.
- Resolution (resolved 2026-04-17 per Session 3a): `docs/plans/024-rust-pty-sidecar.md` lands per plan template, implementing ADR-019 (commit `e854891` Session 3a Plan-022 + Plan-024); cross-cites to ADR-019 + ADR-009 verified per H6 narrow-grep pre-gate 2026-04-22 (H5 §3.1 row 3.12).

#### BL-079: Write Spec-025 Self-Hostable Node Relay

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-053 (ADR-020 must land first; this spec implements the self-host relay it commits to)
- References: [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md) (from BL-053), [Spec-008](../specs/008-control-plane-relay-and-session-join.md) (v2 relay protocol this spec implements in a second deployment), [deployment-topology.md](../architecture/deployment-topology.md) §Relay Scaling Strategy and §Rate Limiting By Deployment; secure-default exemplars: Caddy auto-HTTPS, Tailscale, Syncthing.
- Summary: Author the spec for a Node.js WebSocket implementation of the v2 relay protocol, shipped in the same repo as the project-operated relay code and deployable via a single-command `docker compose up`. Both the Cloudflare Workers + Durable Objects relay (project-operated) and the Node.js relay (self-host) implement the same v2 wire protocol behind one shared contract, so protocol-level changes land once and ship to both. Scope: (1) wire-protocol parity with Spec-008 (control socket + per-connection data sockets, encrypted fan-out, presence heartbeat); (2) in-process WebSocket pool replacing the data-DO sharding (configurable `MAX_CONNECTIONS_PER_PROCESS`, default 500); (3) rate-limiter-flexible Postgres backend (aligned with BL-044 dual-backend shipping); (4) auto-TLS via ACME (HTTP-01 + DNS-01, Caddy-style, per BL-060 secure-by-default behavior 1); (5) secure-by-default behaviors from BL-060 (bind-to-localhost, refuse-to-start without encryption, loud first-run banner, Prometheus `/metrics` endpoint); (6) `docker-compose.yml` that brings up Postgres + relay + reverse proxy (or skips the proxy in favor of built-in auto-TLS); (7) configuration surface (`RELAY_URL`, `BIND_ADDR`, `POSTGRES_URL`, `ACME_DOMAIN`, `ADMIN_TOKEN`); (8) admin surface (`/admin/bans`, `/admin/metrics`, `/admin/health`); (9) upgrade/migration story (how operators upgrade between relay versions); (10) security-defaults companion-doc pointer to `docs/operations/self-host-secure-defaults.md` (per BL-060).
- Exit Criteria: `docs/specs/025-self-hostable-node-relay.md` exists and conforms to the spec template; cites ADR-020 and Spec-008; wire-protocol section explicitly states "Spec-008 is authoritative; this spec is an alternate deployment of the same protocol"; `docker-compose.yml` reference present (file itself lands in BL-080 Plan-025); no retrospective/amendment phrasing.
- Resolution (resolved 2026-04-17 per Session 2a): `docs/specs/025-self-hostable-node-relay.md` lands per spec template (commit `d36704b` Session 2a V1-readiness specs); Spec-008 wire-protocol parity + BL-060 secure-by-default cross-refs verified per H6 narrow-grep pre-gate 2026-04-22 (H5 §3.1 row 3.13).

#### BL-080: Create Plan-025 Self-Hostable Node Relay

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-079 (Plan-025 implements Spec-025)
- References: [Spec-025](../specs/025-self-hostable-node-relay.md) (from BL-079), [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md), [Spec-021](../specs/021-rate-limiting-policy.md), [deployment-topology.md](../architecture/deployment-topology.md), [plans/000-plan-template.md](../plans/000-plan-template.md)
- Summary: Author the implementation plan for the self-hostable Node.js relay. Scope: module layout under `packages/relay-node/` (or `packages/control-plane/self-host/` if we fold it into the existing control-plane package), Node.js WebSocket server choice (`ws` vs `uWebSockets.js` — pick one with benchmark rationale), ACME integration library choice (`acme-client` or similar), rate-limiter-flexible wiring, Postgres schema for rate-limit state + admin bans, Docker Compose file landing at repo root or under `self-host/`, operator runbook outline, Tier placement (expected Tier 5–6, follows Plan-008 relay core).
- Exit Criteria: `docs/plans/025-self-hostable-node-relay.md` exists and conforms to the plan template; Spec-025 header's `Implementation Plan` field is populated; `docker-compose.yml` location + contents outlined (file itself is a plan deliverable); Tier intent stated so BL-054 can place it in the cross-plan graph; no retrospective/amendment phrasing.
- Resolution (resolved 2026-04-17 per Session 3b): `docs/plans/025-self-hostable-node-relay.md` lands per plan template (commit `c1e4225` Session 3b Plan-021 + Plan-025); Spec-025:11 `Implementation Plan` field reads `[Plan-025: Self-Hostable Node Relay](../plans/025-self-hostable-node-relay.md)` per grep verification 2026-04-22 (H5 §3.1 row 3.14).

#### BL-081: Write Spec-026 First-Run Three-Way-Choice Onboarding

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-053 (ADR-020 commits to the three-way choice), BL-041 (Spec-023 Desktop Shell — desktop first-run UI lives here)
- References: [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md) (from BL-053), [Spec-007](../specs/007-local-ipc-and-daemon-control.md) (daemon lifecycle and config), [Spec-023](../specs/023-desktop-shell-and-renderer.md) (from BL-041 — desktop shell surface), [Spec-025](../specs/025-self-hostable-node-relay.md) (from BL-079 — self-host target)
- Summary: Author the spec for the one-time three-way-choice the daemon presents on first invite (or explicit activation): (1) Free public relay (default) — use the project-operated relay at a published URL; (2) Self-host your own — prompt for relay URL, admin token, CA bundle fingerprint for trust-on-first-use; (3) Sign up for hosted — open browser to sign-up flow, return scoped token via deep-link or local-loopback callback, store in OS keystore. Choice persists in daemon config (`$XDG_CONFIG_HOME/ai-sidekicks/config.toml` or equivalent), never re-prompts unless explicitly reset via CLI (`sidekicks config reset-onboarding`). Spec covers: CLI interaction flow (prompts, confirmations, help text); desktop interaction flow (modal + step-through, copy for each choice including the risk/benefit one-liner, accessibility); telemetry-opt-in flow (separate from but presented after the three-way choice); failure modes (no network, DNS failure on free relay, invalid self-host URL, sign-up canceled); observability (event taxonomy additions for `onboarding.choice_made`, `onboarding.choice_reset`). No enterprise-SSO flow in V1 (deferred to V1.1+ alongside any OIDC/SAML work).
- Exit Criteria: `docs/specs/026-first-run-onboarding.md` exists and conforms to the spec template; cites ADR-020, Spec-007, Spec-023, Spec-025; CLI flow and desktop flow both specified with copy or copy-intent for each choice; config persistence location stated; reset command named; event taxonomy additions listed; no retrospective/amendment phrasing.
- Resolution (resolved 2026-04-17 per Session 2a): `docs/specs/026-first-run-onboarding.md` lands per spec template (commit `d36704b` Session 2a V1-readiness specs); Spec-026 line 308 explicitly cites CVE-2026-34776 + GHSA-3c8v-cfp5-9885 per grep verification 2026-04-22, confirming the deep-link security surface is populated (H5 §3.1 row 3.15).

#### BL-082: Create Plan-026 First-Run Three-Way-Choice Onboarding

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-081 (Plan-026 implements Spec-026)
- References: [Spec-026](../specs/026-first-run-onboarding.md) (from BL-081), [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md), [Plan-023](../plans/023-desktop-shell-and-renderer.md) (from BL-043 — desktop implementation target), [plans/000-plan-template.md](../plans/000-plan-template.md)
- Summary: Author the implementation plan. Scope: CLI flow implementation under `apps/cli/` (tty prompt library choice, e.g., `@inquirer/prompts`; help-text copy; validation logic); desktop flow implementation under `apps/desktop/renderer/` (React component tree, preload-bridge hooks for keystore access, accessibility conformance); shared config read/write under `packages/client-sdk/` or `packages/contracts/`; telemetry-opt-in follow-on step; test matrix (golden-path three choices, no-network failure, invalid self-host URL rejection, reset-via-CLI flow, persistence across daemon restart). Tier placement follows Plan-023 / Plan-007 — expected Tier 7 or 8.
- Exit Criteria: `docs/plans/026-first-run-onboarding.md` exists and conforms to the plan template; cites Spec-026, ADR-020, Plan-023; Tier intent stated so BL-054 can place it; test matrix enumerated; no retrospective/amendment phrasing.
- Resolution (resolved 2026-04-17 per Session 3c): `docs/plans/026-first-run-onboarding.md` lands per plan template (commit `f83dcc6` Session 3c Plan-023 + Plan-026); Spec-026:11 `Implementation Plan` field reads `[Plan-026: First-Run Onboarding](../plans/026-first-run-onboarding.md)` per grep verification 2026-04-22 (H5 §3.1 row 3.16).

### Phase 2 — Architecture Blockers from the Audit (P0)

Doc edits that resolve convergent architectural gaps. None require code.

#### BL-046: Write ADR-017 Shared Event-Sourcing Scope

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Resolution (resolved 2026-04-17 per Session TBD): V1 ships Option B (per-daemon local event logs). Each daemon owns an authoritative `session_events` table in its Local SQLite; cross-participant events are distributed as pairwise-encrypted payloads via the relay per ADR-010 and appended to each receiving daemon's local log with its own per-session sequence. Shared Postgres stores coordination records only; no `session_events_shared` table in V1. Federated audit (audit spans multiple daemon log exports) and per-daemon sequence semantics are named as accepted trade-offs. Option A (shared Postgres event log) retained as a V1.1 candidate, gated on ADR-010 MLS promotion gates (audit + interop + 4-week soak). Rationale: V1's zero-knowledge relay (pairwise X25519 + XChaCha20-Poly1305 per ADR-010) cannot read payloads; a shared append-only log under that constraint forecloses its own audit value. Primary-source research (Kleppmann local-first, Automerge, Zed, Replicache as the ecosystem norm; Linear as the contrary data point where shared-log works with plaintext server) cited in ADR-017 Research Conducted table.
- References: [ADR-017](../decisions/017-shared-event-sourcing-scope.md), [vision.md §Session Engine](../vision.md), [data-architecture.md §Event-Sourcing Scope](../architecture/data-architecture.md), [shared-postgres-schema.md](../architecture/schemas/shared-postgres-schema.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md), [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md), [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md); [Kleppmann et al. "Local-First Software" (Ink & Switch / Onward! 2019)](https://www.inkandswitch.com/essay/local-first/); [Automerge docs](https://automerge.org/); [Zed CRDT blog](https://zed.dev/blog/crdts); [Replicache "How it works"](https://doc.replicache.dev/concepts/how-it-works); [Linear sync engine blog](https://linear.app/now/scaling-the-linear-sync-engine).
- Summary: Decide between Option A (add `session_events_shared` append-only Postgres table with MLS ciphertext envelopes and global-sequence protocol) and Option B (scope event-sourcing to per-daemon local runtime only; shared state stays row-based; cross-participant audit requires each peer's daemon). Recommendation: Option B for V1, Option A as a V1.1/V2 target if customer demand for shared audit emerges. The ADR documents the choice; the `data-architecture.md §Event-Sourcing Scope` section and the `vision.md §Session Engine` section (lines 157-173) are updated to match, so no downstream doc contradicts the ADR.
- Exit Criteria: `docs/decisions/017-shared-event-sourcing-scope.md` exists with the chosen option declared; `data-architecture.md` has §Event-Sourcing Scope reflecting the chosen option; `vision.md §Session Engine` (lines 157-173) is aligned so it does not promise cross-participant event-sourcing guarantees that the chosen option does not deliver.

#### BL-047: Write Spec-024 Cross-Node Dispatch and Approval

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Resolution (resolved 2026-04-17 per Session TBD): Spec-024 landed. Content: scheduler own-node-first default with explicit `tool_execution`-category cross-node hop; capability declaration via `runtime_node.capability_declared` with session-owner approval for dangerous classes (`shell.command`, `network.egress`, `destructive_git`, `mcp_elicitation`); cross-node DispatchRequest envelope with pairwise-encrypted payload via relay per ADR-010; target-side mandatory ordering (token verify → body binding → replay guard → capability check → Cedar evaluation → approval gate) with Cedar `principal = verified_caller_sub` bound only after cryptographic verification and verification metadata on Cedar `context` per the AWS Verified Permissions identity-source mapping pattern. Dual-signed ApprovalRecord is a composite envelope of two independent PASETO v4.public tokens (caller + approver) bound by shared `request_body_hash` and approver's `bound_jti = caller_token.jti` — V1 does not use native multi-signature because PASETO v4 does not support multi-sig and PASERK has no token-wrapping type. Cross-node failure semantics: caller-token-expiry auto-deny, target-owner-unreachable wait-until-expiry, partner-detach mid-execution continues to completion with buffered result, clock-skew ±120s rejection at intake, policy-engine-error fails closed. Precedent: Teleport Just-In-Time Access Requests (per-request runtime approval, distinct `access_request.create` + `access_request.review` audit events). Per ADR-017, all dispatch events append to per-daemon local `session_events` logs; shared Postgres carries only a coordination row (status + routing, no payload). security-architecture.md §Trust Boundaries gains a new `### Inter-Node Trust Boundaries` subsection citing Spec-024 with three invariants: authenticated caller identity, per-dispatch target-owner approval, tamper-evident dual-signed audit.
- References: [Spec-024](../specs/024-cross-node-dispatch-and-approval.md), [ADR-007](../decisions/007-collaboration-trust-and-permission-model.md), [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md), [ADR-012](../decisions/012-cedar-approval-policy-engine.md), [ADR-017](../decisions/017-shared-event-sourcing-scope.md), [Spec-002](../specs/002-invite-membership-and-presence.md), [Spec-003](../specs/003-runtime-node-attach.md), [Spec-012](../specs/012-approvals-permissions-and-trust-boundaries.md), [security-architecture.md §Inter-Node Trust Boundaries](../architecture/security-architecture.md); [Cedar Policy Syntax](https://docs.cedarpolicy.com/policies/syntax-policy.html); [Cedar Entities Syntax](https://docs.cedarpolicy.com/auth/entities-syntax.html); [AWS Verified Permissions — Identity sources](https://docs.aws.amazon.com/verifiedpermissions/latest/userguide/identity-sources.html); [PASETO v4 spec](https://github.com/paseto-standard/paseto-spec/blob/master/docs/01-Protocol-Versions/Version4.md); [PASERK](https://github.com/paseto-standard/paserk); [RFC 9449 DPoP](https://datatracker.ietf.org/doc/rfc9449/); [Teleport JIT Access Requests](https://goteleport.com/docs/identity-governance/access-requests/); [Teleport Audit Events](https://goteleport.com/docs/reference/audit-events/).
- Summary: Author an end-to-end protocol for the signature mid-session-invite + cross-machine runtime contribution flow. Minimum content: Alice/Bob walk-through; target-node Cedar principal binding (`principal = verified_caller_sub`); dual-signed approval records (caller PASETO + target-owner PASETO); cross-node failure semantics on partner detach; capability declarations via `runtime_node.capability_declared` events with session-owner approval for dangerous classes; scheduler's own-node-first default with explicit cross-node hop requiring `tool_execution` approval.
- Exit Criteria: `docs/specs/024-cross-node-dispatch-and-approval.md` exists and conforms to the spec template; security-architecture.md cites Spec-024 from its Inter-Node Trust Boundaries section. The downstream addition of Spec-024 as an implicit dependency of Plan-002/Plan-003/Plan-008/Plan-012 in `cross-plan-dependencies.md` is BL-054's scope, not BL-047's.

#### BL-048: Rewrite ADR-010 — pairwise-first relay encryption; MLS is the V1.1 upgrade

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Resolution (resolved 2026-04-17 per Session TBD): ADR-010 Decision point 3 rewritten to declare V1 relay encryption as pairwise X25519 ECDH + XChaCha20-Poly1305 via audited `@noble/curves` and `@noble/ciphers`, with session-granularity forward secrecy via ephemeral X25519 key pairs authenticated by long-term Ed25519 identities; V1 participant cap ≤ 10 to bound N² per-recipient fan-out; MLS (RFC 9420) via an audited implementation (OpenMLS, mls-rs, or a post-audit TypeScript implementation) is the V1.1 upgrade path, gated on three testable promotion gates — (a) named audit firm + published report, (b) interop tests pass against ≥ 1 other implementation at a pinned commit, (c) ≥ 4 weeks production soak under feature flag with < 1% session error rate. Security-architecture.md §Relay Authentication And Encryption split into self-contained V1 subsection (pairwise, no MLS reference, includes `@noble` v2.x API recipe) and V1.1+ subsection (MLS planned upgrade). Spec-008 §Relay Encryption through §Trust Properties rewritten for internal consistency — sweep surfaced MLS-as-V1 framing beyond the one "fallback" line that half-rewriting would have locked in. Vision.md relay-E2EE rows split V1 primary / V1.1+ upgrade. Plan-008 encryption step + test plan reflect pairwise-first. No amendment phrasings (`invert`, `reversal`, `revert`, `rollback`, `the earlier default`, `changed our mind`) remain in ADR-010; `fallback` sweep of `docs/` returned no pairwise-as-fallback framings.
- References: [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md), [vision.md:280](../vision.md), [security-architecture.md](../architecture/security-architecture.md), [Spec-008](../specs/008-control-plane-relay-and-session-join.md), [Plan-008](../plans/008-control-plane-relay-and-session-join.md), [Spec-021](../specs/021-rate-limiting-policy.md) (KeyPackage-upload row marked V1.1+ to match the ADR deferral); [Bernstein 2011 XSalsa20/XChaCha20 construction](https://cr.yp.to/snuffle/xsalsa-20110204.pdf); [RFC 9420 (MLS)](https://datatracker.ietf.org/doc/rfc9420/); [RFC 5869 (HKDF)](https://datatracker.ietf.org/doc/rfc5869/); @noble audits ([noble-curves audit folder](https://github.com/paulmillr/noble-curves/tree/main/audit)).
- Summary: Rewrite ADR-010 Decision point 3 to declare: "V1 ships pairwise X25519 + XChaCha20-Poly1305 via audited `@noble/curves` + `@noble/ciphers`. MLS (RFC 9420) via an audited implementation is the V1.1 upgrade path." Rewrite Assumption #2 to remove the unsupported "ts-mls is production-grade" claim and state instead that an audited MLS implementation (OpenMLS-WASM, Wire core-crypto, or post-audit ts-mls) is expected to be available for V1.1 promotion. Add explicit promotion gates: (a) external audit of selected MLS implementation, (b) interop tests pass against ≥ 1 other implementation, (c) ≥ 4 weeks production soak under a feature flag. Split security-architecture.md §Relay Authentication And Encryption into "V1 (pairwise X25519 + XChaCha20-Poly1305)" and "V1.1+ (MLS)" subsections; the V1 subsection is authoritative and stands on its own. ADR text declares the pairwise-first position as the forward choice, not as a reversal of a prior shipped default.
- Exit Criteria: ADR-010 declares pairwise-first as the V1 relay encryption choice with MLS as the V1.1 upgrade (with explicit promotion gates); `vision.md` Add table marks ts-mls as V1.1/V2 (also per BL-071); security-architecture.md has a V1 subsection that is self-contained and does not reference MLS; the ADR body contains no amendment phrasing (`invert`, `reversal`, `revert`, `rollback`, `the earlier default`). Additionally, sweep the corpus for residual "pairwise fallback," "NaCl fallback," "MLS fallback," and "fallback encryption" phrasings that describe the now-primary pairwise choice — update each to the declarative form. Known targets: `ADR-010` Success Metrics row, `vision.md:280` and `vision.md:282` (both `@noble/*` row purpose cells: PASETO v4 row and Relay E2EE V1 primary row), `docs/architecture/v1-feature-scope.md` row 19 (MLS relay E2EE), and `docs/plans/008-control-plane-relay-and-session-join.md` encryption description. Post-edit grep for `fallback` in `docs/` must return no remaining references framing pairwise as fallback.

#### BL-049: Add Authenticated-Principal preamble to api-payload-contracts

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [api-payload-contracts.md](../architecture/contracts/api-payload-contracts.md), [Spec-012 §ApprovalResolve](../specs/012-approvals-permissions-and-trust-boundaries.md), [ADR-011](../decisions/011-generic-intervention-dispatch.md), [Spec-004 applyIntervention](../specs/004-queue-steer-pause-resume.md)
- Summary: Add a preamble to api-payload-contracts.md declaring that all control-plane endpoints are implicitly scoped to the authenticated `ParticipantId` from the PASETO `sub` claim and bound to the `cnf.jkt` DPoP thumbprint. Body fields that name a participant (`approver`, `inviter`, `requester`) are informational only. All local-daemon endpoints are implicitly authorized by socket reachability + optional session token. Then state explicitly in Spec-012 §ApprovalResolve and in ADR-011 / Spec-004 §applyIntervention that Cedar principal input = `verified_sub + cnf.jkt`.
- Exit Criteria: Preamble present; Spec-012 and ADR-011 / Spec-004 cite it; no payload doc reads as if `approver` is authoritative for authorization.
- Resolution (resolved 2026-04-17 per Session 2c-2): Authenticated-Principal preamble landed in `api-payload-contracts.md` (commit `c74ba03` Session 2c-2 advisor follow-ups); Spec-012:82 explicitly cites the API-payload-contracts §Authenticated Principal section per H4 §4.4 cross-cite verification 2026-04-22 (H5 §3.1 row 3.8).

#### BL-050: Add Audit Log Integrity protocol

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [security-architecture.md](../architecture/security-architecture.md), [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md)
- Summary: Specify hash-chain + per-event daemon signature + periodic Merkle root anchored to control plane. Add `prev_hash`, `row_hash`, `daemon_signature` columns to `session_events` and a NULL-able `participant_signature` column for sensitive events (WebAuthn PRF at desktop, CLI at-rest key otherwise per BL-057). Anchoring cadence defaults: every 1000 events or 300 seconds (whichever first). Read-side verification rules cover hash chain, signature, and Merkle anchor.
- Exit Criteria: security-architecture.md has new §Audit Log Integrity; Spec-006 has §Integrity Protocol with canonical-serialization rules; schema migration note present for the four new columns (three required + one NULL-able).

#### BL-051: Add Idempotency Protocol for Side-Effecting Tool Calls

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md), [Spec-015](../specs/015-persistence-recovery-and-replay.md), [Spec-005](../specs/005-provider-driver-contract-and-capabilities.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md) (`command_receipts`)
- Summary: Define two-phase receipt commit (accept → execute → terminal-status) with each phase in its own transaction. Introduce driver metadata `tool.idempotency_class ∈ {idempotent, compensable, manual_reconcile_only}`. Recovery rule per class: idempotent re-executes (external de-dupe); compensable re-executes with dedupe key; manual_reconcile_only halts and surfaces operator escalation. Add new events `tool.replayed` and `tool.skipped_during_recovery` (feeds BL-064).
- Exit Criteria: Spec-006 and Spec-015 have the new sections; Spec-005 defines `tool.idempotency_class` in driver metadata; `command_receipts` schema gains the two-phase-commit support columns (`idempotency_class`, `dedupe_key`, `started_at`, `completed_at`). The pre-existing `status` CHECK constraint already enumerates `accepted | completed | failed | rejected`; verify rather than re-author.
- Resolution (resolved 2026-04-17 per Session 2c-2): `local-sqlite-schema.md:110-127` defines `command_receipts` with `idempotency_class TEXT NOT NULL CHECK(idempotency_class IN ('idempotent', 'compensable', 'manual_reconcile_only'))` per Read verification 2026-04-22 (commit `c74ba03` Session 2c-2 advisor follow-ups); Spec-006/015/005 sections all present (H5 §3.1 row 3.9).

### Phase 3 — V1 Scope Decisions (P0 — resolved; ADR authoring remains)

Product/infra calls that required human judgment. Both items resolved 2026-04-16 after structured research (catalogued in [ADR-019 §Research Conducted](../decisions/019-windows-v1-tier-and-pty-sidecar.md#research-conducted) + [ADR-020 §Research Conducted](../decisions/020-v1-deployment-model-and-oss-license.md#research-conducted)). Each item below is now scoped to ADR authoring + minimal doc propagation; the decision itself is recorded in the item body.

#### BL-052: Write ADR-019 Windows V1 Tier and Rust PTY Sidecar Strategy

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Resolution (resolved 2026-04-16 per Session TBD): Windows ships in V1 as **GA** with a **Rust PTY sidecar** as the primary PTY backend on Windows. node-pty stays as the primary backend on macOS/Linux and as the fallback implementation on Windows. All PTY execution flows through a `PtyHost` interface in `packages/contracts/`. Evidence-grade primary-source citations are catalogued in [ADR-019 §Research Conducted](../decisions/019-windows-v1-tier-and-pty-sidecar.md#research-conducted).
- References: [v1-feature-scope.md](../architecture/v1-feature-scope.md), [vision.md](../vision.md) §"Add Later If Needed" (Rust sidecar row), [component-architecture-local-daemon.md](../architecture/component-architecture-local-daemon.md), [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md) (Electron-for-Linux-passkey parity that frames the Windows GA expectation); upstream issues: `openai/codex#13973` (ConPTY assertion, OPEN 2026-03-08, first-party trigger), `microsoft/node-pty#904` (SIGABRT on Electron exit / ThreadSafeFunction race, OPEN), `microsoft/node-pty#887` (ConoutConnection worker strands Node exit, OPEN), `microsoft/node-pty#894` (PowerShell 7 3.5s delay under `useConptyDll`, OPEN), `microsoft/node-pty#437` (`ptyProcess.kill()` hangs on Windows, works on Linux), `microsoft/node-pty#647` (spawn locks cwd on Windows); precedent: `Eugeny/tabby#10134` (GA ship on the same ConPTY assertion class caused a user-rollback wave — closed by pinning node-pty); reference impls: [wezterm/pty (`portable-pty`)](https://github.com/wezterm/wezterm/tree/main/pty), [Tauri v2 sidecar docs](https://v2.tauri.app/develop/sidecar/), npm distribution pattern mirrors `@esbuild/*` platform-package set.
- Summary: Write ADR-019 declaring Windows V1 = GA with a Rust PTY sidecar. The ADR must state: (1) **Primary backend on Windows is a Rust sidecar built on `portable-pty`** (wezterm), spawned as a child process of the daemon with lifecycle tied to the session. (2) **node-pty is the primary backend on macOS/Linux and the Windows fallback** under the same `PtyHost` interface (contract lives in `packages/contracts/`). (3) **Sidecar IPC uses LSP-style Content-Length framing over stdio**, with a JSON control channel (`spawn`, `resize`, `kill`, `exit-code`) and a length-prefixed binary data channel for stdout/stderr — this matches our existing daemon JSON-RPC 2.0 + Content-Length design (see [ADR-009](../decisions/009-json-rpc-ipc-wire-format.md)). (4) **Distribution** follows the `@esbuild/*` optionalDependencies + `os`/`cpu` filter pattern: one signed binary per platform under `@ai-sidekicks/pty-sidecar-{win32-x64,darwin-arm64,darwin-x64,linux-x64,linux-arm64}`. (5) **Windows code-signing** uses Azure Trusted Signing as the preferred path (eligible if 3+ years of verifiable business history), with a traditional EV cert (~$325–$581/yr) as the documented fallback. (6) **SmartScreen reputation** accrues to the signer, so the sidecar binary and Electron app share the same signing identity. Alternatives section must steel-man Option B (Windows GA on node-pty only — the industry norm across VS Code, Cursor, Windsurf, Tabby, Wave, Claude Code) and Option C (Windows V1 Beta — the research brief's default recommendation under a human-implementation cost model). The **decision rationale** must explicitly name the cost-model flip: the research brief recommended C because 3–5 engineer-weeks of Rust work and "+1 language in critical path" were treated as load-bearing costs; AI-implementation (Claude Opus 4.7) collapses those costs by >70%, which inverts the cost-benefit against the Tabby-precedent risk on `codex#13973`. Decision Log references the 2026-04-16 research brief and the AI-implementation cost-model shift. ADR text must read as a forward declaration — no "reversal," "rescope," "updated to," or other amendment framing.
- Exit Criteria: `docs/decisions/019-windows-v1-tier-and-pty-sidecar.md` exists with Context, Decision, Alternatives (B + C steel-manned; D `useConptyDll` noted as a flag that may be combined with the sidecar when `microsoft/node-pty#894` closes), Consequences, Reversibility, Tripwires, and Decision Log sections; status `accepted`; the ADR defines the `PtyHost` interface obligation and states that macOS/Linux continue on node-pty; Tripwires section names at least three reversal triggers (examples: sidecar-originated Sev-1 bug traceable to `portable-pty`; Azure Trusted Signing path unavailable AND EV cert blocked; node-pty v1.2.0 stable ships with the ThreadSafeFunction race fixed AND 50 consecutive clean `windows-latest` Codex+`/resume` CI runs → evaluate sidecar sunset as a cost-reduction move); `v1-feature-scope.md` Windows-tier row cites ADR-019 as GA; `vision.md` §"Add Later If Needed" Rust-sidecar row is moved to a confirmed V1 component row citing ADR-019; `component-architecture-local-daemon.md` defines the `PtyHost` interface and names the Rust sidecar as the Windows primary; the research brief is either copied to a persistent docs path or its key findings are inlined into the ADR's Decision Log with external-source URLs cited directly; no retrospective/amendment phrasing anywhere in the ADR body. Follow-up (tracked separately, not in this BL's scope): a dedicated plan (`Plan-024 Rust PTY Sidecar`) covering sidecar implementation, IPC protocol, distribution, and signing must exist before Plan-001 begins Windows integration testing.

#### BL-053: Write ADR-020 V1 Deployment Model (OSS Self-Host + Hosted SaaS) and OSS License

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Resolution (resolved 2026-04-16 per Session TBD): V1 ships both deployment options over a **single codebase** under a **permissive OSS license**. (1) **Free self-hosted (OSS):** users `git clone` / install a distributed binary; the daemon defaults to a project-operated free public relay so first-run collaboration is zero-config; users can override to point the daemon at their own self-hosted relay. Community-supported via GitHub Issues, no SLA. (2) **Hosted SaaS:** the project operates the same codebase as a managed service users sign up for; their daemons point at our hosted control plane. Same feature set in both options (no feature-gating between free and hosted in V1). License starts **MIT or Apache-2.0**; revisit only on concrete competitive re-hosting signal (Sentry BSL/FSL precedent). Pre-decision research evaluated this question and recommended Option B (V1 hosted-only, defer self-host) under an enterprise-commercial-SaaS cost model; that recommendation was superseded after product-framing clarified this is OSS-first / "anyone can git clone and invite a friend," not an enterprise-commercial-SaaS — which eliminates the ongoing vendor-support cost argument. The full evidence base is catalogued in [ADR-020 §Research Conducted](../decisions/020-v1-deployment-model-and-oss-license.md#research-conducted) and the Option B steel-man + rejection rationale lives in [ADR-020 §Alternatives Option B](../decisions/020-v1-deployment-model-and-oss-license.md#option-b-v1-hosted-only-self-host-deferred-to-v11-rejected-was-the-research-briefs-recommendation).
- References: [deployment-topology.md](../architecture/deployment-topology.md), [Spec-008](../specs/008-control-plane-relay-and-session-join.md), [Spec-021](../specs/021-rate-limiting-policy.md), [v1-feature-scope.md](../architecture/v1-feature-scope.md), [ADR-002](../decisions/002-local-execution-shared-control-plane.md) (trust-boundary framing), [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md) (Postgres control plane); pre-decision research evidence base: [ADR-020 §Research Conducted](../decisions/020-v1-deployment-model-and-oss-license.md#research-conducted); one-product-two-deployment-options precedents: PostHog, Supabase, Sentry, GitLab, Mattermost, tmate (community relay + self-host); related to BL-044 (rate-limiter backend) and BL-060 (self-host security doc) — both now scoped by this decision.
- Summary: Write ADR-020 declaring the V1 deployment model. The ADR must state: (1) **Single codebase, two deployment options.** The daemon, the relay, and the control-plane code live in one repository under a permissive OSS license. Both deployment options ship the same 16-feature V1 surface — no feature-gating between free OSS and our hosted SaaS. (2) **Free self-hosted (OSS):** users install via `git clone` / npm / Homebrew / release binary; daemon defaults to a project-operated free public relay at a published URL so first-run collaboration is zero-config; users can override via an env var or config file (e.g., `RELAY_URL=…`) to point at their own self-hosted relay; community-supported via GitHub Issues and Security Advisories with no SLA. (3) **Hosted SaaS:** the project operates the same codebase as a managed service at a separate URL; users sign up, receive a scoped token, and their daemons point at the hosted control plane; vendor-supported for paying customers. (4) **First-run UX:** on first invite, the daemon presents a one-time three-way choice — free public relay (default) / self-host your own / sign up for hosted — stored in config, never re-prompts. (5) **License:** MIT or Apache-2.0 at repo root from day one. Revisit only if a competitor materially re-hosts our code as a competing managed service (the Sentry BSL→FSL saga is the precedent for deferred re-licensing). (6) **Relay infrastructure:** the project-operated free relay runs on Cloudflare Workers + Durable Objects using the sharded control/data-DO architecture from `deployment-topology.md` §Relay Scaling Strategy; the self-hostable relay is a Node.js WebSocket implementation of the same wire protocol, shipped alongside the daemon in the same repo with a `docker-compose.yml` for single-command self-host; both backends implement the v2 relay protocol behind one shared contract (so protocol-level changes land once and ship to both). (7) **Rate-limiter backends:** both ship in V1 under the deployment-aware abstraction already named in `deployment-topology.md` — CF-native `rate_limit` binding for the project-operated relay and hosted SaaS; `rate-limiter-flexible` with Postgres backend for the self-hostable relay. Alternatives the ADR must steel-man: (a) **Option Brief-B** (V1 hosted-only, V1.1 self-host) — the research brief's recommendation, superseded by the OSS-first product framing; (b) **Option Brief-A** (full enterprise self-hosted with Helm / OIDC / SAML / CVE contracts / vendor support) — rejected for V1 because no named enterprise pipeline today justifies the 0.2–1 FTE sustained cost; (c) **No default relay** (users must bring their own relay URL) — rejected because it breaks the "git clone and invite a friend immediately" UX; (d) **Pure P2P with STUN/TURN** — rejected because every ~30% NAT-blocked case still needs a relay fallback, so the architecture still ends up operating a coordination endpoint. The ADR's rationale must name the constraint that tipped the call: this is an OSS developer tool whose value is the tool itself, not an enterprise compliance wrapper. Vendor-supported-enterprise ongoing-cost arguments do not apply to community-supported OSS. Decision Log cites the research brief, the 2026-04-16 OSS-framing clarification, and the PostHog/Supabase/Sentry/tmate precedents. ADR body must read as a forward declaration — no "reconsidered," "expanded," or "changed direction" framing.
- Exit Criteria: `docs/decisions/020-v1-deployment-model-and-oss-license.md` exists with Context, Decision, Alternatives (a–d steel-manned), Consequences, Reversibility, Tripwires, and Decision Log sections; status `accepted`; Tripwires section names at least three reversal/adjustment triggers (examples: a competitor hosts our code as a competing managed service with measurable revenue impact → relicense to FSL/BSL/ELv2 per Sentry precedent; community support drag exceeds 30% of weekly engineering capacity for 4+ consecutive weeks → tighten OSS scope or deprecate the free default relay in favor of self-host-only; hosted-SaaS monthly active users stays below a named threshold 6 months post-launch → reconsider monetization shape before V1.1 planning); `v1-feature-scope.md` gains a "Deployment Options" section naming "Free self-hosted (OSS)" and "Hosted SaaS" as V1 deployment shapes (both over the same 16-feature V1 feature set — this is not a feature-count change to ADR-015); `deployment-topology.md` cross-links to ADR-020 from the Collaborative Hosted Control Plane and Collaborative Self-Hosted Control Plane topology rows (the topology model is unchanged; only the V1 commitment changes); an OSS `LICENSE` file (MIT or Apache-2.0) lands at the repo root; a `README` section documents the `--relay-url` / `RELAY_URL=` override and points at the self-host relay setup docs; the pre-decision research evidence base is catalogued in [ADR-020 §Research Conducted](../decisions/020-v1-deployment-model-and-oss-license.md#research-conducted) and the superseded Option B recommendation rationale is preserved in [ADR-020 §Alternatives Option B](../decisions/020-v1-deployment-model-and-oss-license.md#option-b-v1-hosted-only-self-host-deferred-to-v11-rejected-was-the-research-briefs-recommendation); no retrospective/amendment phrasing anywhere in the ADR body. Follow-up scope (now tracked as separate backlog items): (a) BL-079 (Spec-025 self-hostable Node relay) + BL-080 (Plan-025); (b) BL-081 (Spec-026 first-run three-way-choice onboarding) + BL-082 (Plan-026); (c) BL-083 (commit OSS `LICENSE` at repo root — MIT vs Apache-2.0).

#### BL-083: Commit OSS LICENSE file at repo root (MIT vs Apache-2.0)

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-053 (ADR-020 commits to a permissive OSS license)
- References: [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md) (from BL-053); [MIT License](https://choosealicense.com/licenses/mit/); [Apache License 2.0](https://choosealicense.com/licenses/apache-2.0/); precedent — VS Code (MIT), Node.js (MIT), Supabase (Apache-2.0), Kubernetes (Apache-2.0), Terraform (originally MPL, now BSL), Mattermost (MIT + proprietary enterprise), PostHog (MIT → re-licensed), Sentry (originally BSD → BSL → FSL).
- Summary: Choose between MIT and Apache-2.0 and commit the corresponding `LICENSE` file at the repo root. Staff-level recommendation: **Apache-2.0**. Rationale: (1) explicit patent grant protects contributors and users from patent litigation by other contributors — a concrete advantage MIT does not give; (2) explicit contribution-terms clause codifies inbound-is-outbound CLA semantics in the license itself, reducing the need for a separate CLA; (3) dominant choice in modern OSS developer-tool projects (Kubernetes, Terraform-pre-BSL, Supabase, etc.); (4) SPDX-clean and recognized by all major dependency scanners. MIT remains a defensible choice if the constraint is maximal ecosystem compat with copyleft-aware downstream (GPL inclusion is cleaner under MIT than under Apache-2.0 due to the patent-termination clause in Apache-2.0 §3). Deliverables: (a) `LICENSE` file at repo root with text matching the chosen SPDX identifier exactly; (b) `package.json` top-level `license` field set to matching SPDX identifier (`Apache-2.0` or `MIT`); (c) README section naming the license and linking to `LICENSE`; (d) ADR-020 Decision Log entry recording which license was chosen and why. Revisit gate: if a competitor materially re-hosts the codebase as a competing managed service with measurable revenue impact, re-license to FSL/BSL/ELv2 per the Sentry precedent (already named in ADR-020 Tripwires).
- Exit Criteria: `LICENSE` file exists at repo root with text matching the chosen SPDX identifier exactly; `package.json` `license` field matches; `README.md` references the license; ADR-020 Decision Log entry records the choice; no conflicting license text anywhere in the repo.
- Resolution (resolved 2026-04-17): Apache-2.0 chosen and committed. `LICENSE` (SPDX `Apache-2.0`) landed at repo root per commit `45434c3` (`chore: add LICENSE (Apache-2.0) per BL-083`); same commit updated `package.json` `license` field + `README.md` license section. [ADR-020 Decision Log entry for 2026-04-17](../decisions/020-v1-deployment-model-and-oss-license.md#decision-log) records LICENSE-committed status with the four-point Apache-2.0 selection rationale (explicit patent grant §3; inbound-is-outbound §5; OSS developer-tool precedent; SPDX-clean).

#### BL-084: Register `arbitration.paused` / `arbitration.resumed` events in Spec-006 event taxonomy

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Resolution (resolved 2026-04-18): Registered in Spec-006 under a new `channel_arbitration` category (chosen over the `orchestration_lifecycle` / `session_lifecycle` alternatives named in the BL summary because arbitration-stall semantics are a subsystem-layer concern, not a session-wide lifecycle transition). Payload shape matches the BL exactly: `{sessionId, channelId, unreachableNodeId, unreachableAgentId, turnPolicy, timestamp}`. Spec-016 §Partition And Reconnect Behavior is cross-linked from the new category; Spec-003 `offline` semantics are cited in the "When Fired" clause for `arbitration.paused`. §Event Type Summary count row added; total raised 76 → 93 (also reflects the BL-086 and BL-087 additions landed in the same session). Distinction from `run.paused` preserved in the category body text.
- Depends-on: BL-042 (Spec-016 V1 Readiness Review is the trigger; ADR-011 and Spec-003 partition semantics are the behavioral anchors)
- References: [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md) §Event Types and §Event Category Counts; [Spec-016](../specs/016-multi-agent-channels-and-orchestration.md) §Partition And Reconnect Behavior; [Spec-003](../specs/003-runtime-node-attach.md) (node `offline` semantics); [ADR-011](../decisions/011-generic-intervention-dispatch.md) (intervention surface that arbitration pauses ride on)
- Summary: Spec-016's V1 Readiness Review introduced two new orchestration-layer events — `arbitration.paused` (emitted when a `round-robin` channel's next-due agent sits on an unreachable node) and `arbitration.resumed` (emitted when the node reconnects and canonical ordering is restored) — that do not yet exist in the Spec-006 canonical event taxonomy. Register them: (1) add both events to the Event Types table under a new `orchestration_lifecycle` category or an existing category that matches their scope (candidate: `session_lifecycle` next to `channel.*`, or a new category `channel_arbitration` if categorization by subsystem is preferred); (2) define the payload shape (`{sessionId, channelId, unreachableNodeId, unreachableAgentId, turnPolicy, timestamp}`); (3) update the §Event Category Counts table with the new category's count; (4) cross-reference Spec-016 §Partition And Reconnect Behavior from the event definitions so the semantics are discoverable from Spec-006. These events are orchestration-layer visibility states distinct from run-level `run.paused` (Spec-004) — the payload and category must reflect that separation. No Spec-016 edits are required by this BL; Spec-016 already cites the events in dotted form.
- Exit Criteria: Spec-006 §Event Types table lists `arbitration.paused` and `arbitration.resumed` with descriptions; payload shape is defined; §Event Category Counts table is updated consistently; Spec-016 §Partition And Reconnect Behavior cross-reference is resolvable (anchor or explicit event names match Spec-006 exactly); no retrospective/amendment phrasing.

#### BL-085: Bump ADR-016 Electron minimum version floor to ≥ 38.8.6 / 39.8.1 / 40.8.1 / 41.0.0 (CVE-2026-34776)

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-041 (Spec-023 is the implementing spec whose deep-link path this CVE affects); BL-081 (Spec-026 Option 3 hosted-SaaS sign-up is the V1 consumer of the deep-link handler)
- References: [ADR-016: Electron Desktop Shell](../decisions/016-electron-desktop-shell.md), [Spec-023 §WebAuthn Credential Flow](../specs/023-desktop-shell-and-renderer.md), [Spec-026 §Implementation Notes](../specs/026-first-run-onboarding.md), [Electron security advisories index](https://www.electronjs.org/docs/latest/tutorial/electron-timelines#security-advisories); CVE-2026-34776 (`requestSingleInstanceLock` heap-read in the deep-link handler).
- Summary: ADR-016 currently names "Electron 30+" as the supported floor. That floor is insufficient for Spec-026 Option 3 (hosted-SaaS deep-link callback) because CVE-2026-34776 (heap-read in the `requestSingleInstanceLock` deep-link path) affects every Electron release below the fixed branches. Bump the ADR-016 floor to ≥ 38.8.6 / 39.8.1 / 40.8.1 / 41.0.0 (whichever branch the project tracks; pick one current-LTS line as the canonical target and name the minor-and-earlier branches as rejected). Update ADR-016 §Decision (and any Consequences / Assumptions rows that name a version), update Spec-023 if it cites "30+" anywhere, and clear Spec-026's §Implementation Notes caveat that currently reads "until BL-085 resolves, Option 3 must prefer loopback over deep-link on affected versions." Until this BL resolves, Spec-026's prefer-loopback posture on affected Electron versions remains the stated safe path.
- Exit Criteria: ADR-016 names a specific fixed-branch floor (≥ 38.8.6 / 39.8.1 / 40.8.1 / 41.0.0 — the project picks and declares which LTS branch is canonical) and cites CVE-2026-34776; Spec-023 and Spec-026 are audited for residual "Electron 30+" references and updated to match; Spec-026 §Implementation Notes drops the "until BL-085 resolves" caveat; no retrospective/amendment phrasing in ADR-016 (declare the floor forward, do not narrate the change).
- Resolution (resolved 2026-04-17 per Session TBD): ADR-016 lines 159-160 declare fixed-version floors `38.8.6 / 39.8.1 / 40.8.1 / 41.0.0` and cite GHSA-3c8v-cfp5-9885 / CVE-2026-34776 (commit `a89ae0e` Electron fixed-branch floor forward declaration); Spec-026:308 cites the same advisory ID; Spec-023 grep returned zero genuine "Electron 30+" version-floor matches per verification 2026-04-22 (H5 §3.1 row 3.17).

#### BL-086: Register `onboarding.choice_made` / `onboarding.choice_reset` events in Spec-006 event taxonomy

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Resolution (resolved 2026-04-18): Registered in Spec-006 under a new `onboarding_lifecycle` category (chosen over co-locating under `session_lifecycle` because these are daemon-local first-run events, session-independent — the daemon emits them once per onboarding resolution, not per collaborative session). Payload shapes are verbatim from Spec-026 §Interfaces And Contracts — `{participantId, choiceId, relayUrl, migrated, deferredValidation, keystoreAvailable, timestamp}` for `choice_made`, `{participantId, previousChoiceId, reason: 'cli-reset' | 'operator-reset', timestamp}` for `choice_reset`. Secret-material exclusion preserved (no tokens, no SPKI pin raw bytes) with Spec-023 keystore-resident tokens cross-referenced. Spec-026 §State And Data Implications is cross-linked. §Event Type Summary count row added.
- Depends-on: BL-081 (Spec-026 is the behavioral anchor that emits these events)
- References: [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md) §Event Types and §Event Category Counts; [Spec-026 §State And Data Implications / §Interfaces And Contracts](../specs/026-first-run-onboarding.md); [Participant And Membership Model](../domain/participant-and-membership-model.md).
- Summary: Spec-026 introduces two new onboarding-lifecycle events — `onboarding.choice_made` (emitted when the daemon persists a resolved three-way choice) and `onboarding.choice_reset` (emitted when `sidekicks onboarding reset` or operator-reset clears the choice). Register them in Spec-006: (1) add both events to the §Event Types table under a new `onboarding_lifecycle` category or the existing `session_lifecycle` category if categorization by subsystem is preferred; (2) define the payload shapes exactly as Spec-026 §Interfaces And Contracts specifies (no tokens, no SPKI raw bytes); (3) update the §Event Category Counts table consistently; (4) cross-reference Spec-026 from the event definitions. These are daemon-local lifecycle events distinct from session-run events — the payload and category must reflect that separation. No Spec-026 edits are required by this BL; Spec-026 already lists the events in dotted form (pattern that BL-084 established).
- Exit Criteria: Spec-006 §Event Types table lists `onboarding.choice_made` and `onboarding.choice_reset` with descriptions; payload shapes are defined and match Spec-026 exactly; §Event Category Counts table updated consistently; Spec-026 cross-reference is resolvable; no retrospective/amendment phrasing.

#### BL-087: Register `dispatch.*` cross-node events in Spec-006 event taxonomy

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Resolution (resolved 2026-04-18, fix-up 39dff81 + cross-doc-drift sweep): Registered a canonical 13-event `cross_node_dispatch` category in Spec-006, reconciling Spec-024's pre-existing namespace drift in the same landing pass per the BL's required single-audit-pass discipline. Canonical set (trusts Spec-024 §Execution And Result Emission body text as authoritative, supersedes the partial §Interfaces And Contracts line-165 enumeration): caller-side (3) `.sent`, `.approval_observed`, `.result_observed`; target-side pre-terminal (4) `.received`, `.approval_requested`, `.approved`, `.executed`; target-side terminal (5, exactly one per dispatch) `.rejected` (pre-approval), `.denied` (approval refusal; owner-clicked-Deny only — Cedar class-rule denials emit `.rejected` instead because no approver-token exists to sign the deny envelope), `.failed` (post-approval execution fault — new, added to Spec-024 §Cross-Node Failure Semantics in the same pass), `.expired` (token timeout), `.completed` (target-side success: capability handler finished and result is ready for emission); target-side post-terminal delivery annotation (1) `.result_buffered` (fires **after** target `.completed` when the caller was detached; delivery on caller reconnect produces caller-side `.result_observed`; not a lifecycle state). Lifecycle discipline: exactly one terminal per dispatch; `.executed` is intermediate and fires when the capability handler returns a result (the handler itself completed without raising). Three valid success-path-ancestor lifecycles per Spec-024 §Cross-Node Failure Semantics ("capability handler fails during **or after** execution"): (a) handler raises before returning → `.failed` with no prior `.executed`; (b) handler returns, post-handler step fails (e.g. result-emission error) → `.executed` → `.failed`; (c) handler returns, result ready → `.executed` → `.completed`. The `.executed` / `.completed` split exists so audit can distinguish (b) from (c) from a lost-terminal bug case (`.executed` recorded but no subsequent terminal in the log). Result delivery is tracked separately from lifecycle termination — live via `.result_observed`, or buffered via `.result_buffered` → later `.result_observed`. Spec-024 reconciliation edits: line 97 `approval_requested` → `dispatch.approval_requested`; line 140 §Cross-Node Failure Semantics detach-mid-execution bullet expanded to explicitly sequence `.executed` → `.completed` → `.result_buffered` and flag `.result_buffered` as post-terminal annotation; line 166 enumeration grouped as 3 caller-side + 4 pre-terminal + 5 terminal + 1 post-terminal delivery annotation = 13. Per-event "When Fired" clause landed in Spec-006 category body. Teleport precedent cross-referenced with verified live-check on 2026-04-18 — T5000I (`access_request.create`), T5001I (`access_request.update`), T5002I (`access_request.review`), T5003I (`access_request.delete`), T5004I (`access_request.search`), T5005I (`access_request.expire`) all confirmed at `https://goteleport.com/docs/reference/audit-events/`; Teleport's single-event-with-state-transition model contrasted against AI Sidekicks' more granular distinct-event model. No PASETO tokens or secret material carried in event payloads; ApprovalRecord envelope referenced by `approvalRecordRef` only. ADR-017 federated-log semantics cited in category preamble. Session C commit chain (five commits total): 8f3cd3c landed the taxonomy; 39dff81 closed two blockers on Spec-006 (`.result_buffered` self-contradiction, `.denied` phantom Cedar-class-rule-deny path); 788d4d7 propagated the fix to Spec-024 §Cross-Node Failure Semantics line 140, this Resolution block, and Spec-006 `.executed` preamble; ea69bb5 reconciled Option A in Spec-006 — `.failed` can follow `.executed` when a post-handler step fails, consistent with Spec-024 line 143's "during or after execution" design intent (closing a contradiction the 788d4d7 sweep itself introduced between the tightened preamble and the `.executed` row); e57bb26 propagated Option A into this Resolution block and updated the commit chain lineage itself.
- Depends-on: BL-047 (Spec-024 is the behavioral anchor that emits these events)
- References: [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md) §Event Types and §Event Category Counts; [Spec-024 §Target-Side Authentication And Cedar Evaluation / §Dual-Signed ApprovalRecord / §Cross-Node Failure Semantics](../specs/024-cross-node-dispatch-and-approval.md); [Teleport Audit Events `access_request.create` (T5000I) / `access_request.review` (T5002I)](https://goteleport.com/docs/reference/audit-events/) — the precedent naming pattern Spec-024 models.
- Summary: Spec-024 introduces a family of cross-node dispatch lifecycle events that Spec-006 does not yet catalog. At minimum: `dispatch.requested` (caller emits on send), `dispatch.accepted` (target emits after token verify + capability check + Cedar allow), `dispatch.approval_requested` (target-node owner UI notification when Cedar result is "requires owner approval"), `dispatch.approved` (target emits after owner approval + ApprovalRecord construction), `dispatch.denied` (target emits when Cedar denies or owner denies — payload carries signed deny envelope), `dispatch.rejected` (target emits on any pre-approval failure: invalid signature, expired token, body-binding mismatch, replay, undeclared capability — reason field must specify which), `dispatch.executed` (target emits on execution completion, payload carries ApprovalRecord reference), `dispatch.failed` (target emits on execution failure post-approval). Register them in Spec-006: (1) add all events to the §Event Types table under a new `cross_node_dispatch` category; (2) define the payload shapes matching Spec-024 envelope fields (dispatch_id, session_id, caller_participant_id, target_participant_id, target_node_id, capability, request_body_hash, approval_record reference where applicable) — never carry full PASETO tokens or secret material in event payloads; (3) update the §Event Category Counts table with the new category's count; (4) cross-reference Spec-024's strict-order verification steps and Teleport's T5000I/T5002I precedent so the semantics are discoverable from Spec-006. These are audit-grade cross-participant events distinct from local run lifecycle events — the payload must be compatible with the federated audit model from ADR-017 (each daemon appends to its own local log with its own sequence number). Reconciliation note: Spec-024 has two internal namespace inconsistencies the implementer must resolve in a single audit pass before registering events in Spec-006 — (1) line 97 uses bare `approval_requested` in the approval-gate narrative while line 165 §Event taxonomy extensions and line 184 §Illustrative Flows use `dispatch.approval_requested`; (2) the line-165 enumeration differs from the set above (line 165 includes `dispatch.sent`, `dispatch.received`, `dispatch.completed`, `dispatch.expired`, `dispatch.result_observed` that are absent from this list, while this list includes `dispatch.requested`, `dispatch.accepted`, `dispatch.failed` absent from line 165). Decide the canonical event set (e.g., keep `approval_requested` as a separate UI-notification namespace vs unifying everything under `dispatch.*`; pick one caller-emits nomenclature — `dispatch.sent` or `dispatch.requested`; etc.) and land it coherently in both Spec-006 §Event Types and Spec-024.
- Exit Criteria: Spec-006 §Event Types table lists all `dispatch.*` events enumerated above with descriptions; payload shapes are defined and match Spec-024 exactly (no tokens, no secret material); §Event Category Counts table updated consistently; Spec-024 cross-references are resolvable; Teleport precedent cited for the request/approval/execution split; no retrospective/amendment phrasing.

#### BL-088: Record ADR-017 "no `session_events_shared` table in V1" invariant in shared-postgres-schema.md

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Resolution (resolved 2026-04-18): Added a labeled `## Invariant — No Shared Session-Event Table in V1 (ADR-017)` section to `docs/architecture/schemas/shared-postgres-schema.md` positioned after the Storage-boundary preamble and before the first CREATE TABLE (Sessions and Membership) so it anchors all downstream schema additions. The section states all four BL-named points as a numbered list: (1) shared Postgres stores coordination records only and does not store event payloads; (2) there is intentionally no `session_events_shared`, `session_events_global`, or equivalent table in V1; grepping this file for `session_events_shared` must return the invariant note, never a table definition; (3) per-daemon local `session_events` in `local-sqlite-schema.md` is authoritative per ADR-017; (4) introducing such a table requires an ADR superseding ADR-017 plus completion of the ADR-010 MLS promotion gates (audit visibility + interop tests + 4-week soak). The section explicitly exempts `event_log_anchors` from the prohibition as a metadata-only Merkle-root witness, closing the obvious misreading. Declared forward — no retrospective/amendment phrasing.
- Depends-on: BL-046 (ADR-017 is the governing decision)
- References: [shared-postgres-schema.md](../architecture/schemas/shared-postgres-schema.md); [ADR-017: Shared Event-Sourcing Scope](../decisions/017-shared-event-sourcing-scope.md); [ADR-004: SQLite Local State And Postgres Control Plane](../decisions/004-sqlite-local-state-and-postgres-control-plane.md); [local-sqlite-schema.md `session_events` table](../architecture/schemas/local-sqlite-schema.md); [data-architecture.md §Event-Sourcing Scope](../architecture/data-architecture.md).
- Summary: ADR-017 decides V1 scopes event-sourcing to per-daemon local logs (Option B) and explicitly rejects a shared Postgres event log (Option A) for V1, retaining Option A as a V1.1 candidate gated on ADR-010 MLS promotion gates. Today, `shared-postgres-schema.md` is silent on this invariant — a future schema author reading that file alone cannot tell whether the absence of `session_events_shared` is intentional or an oversight. Add a short one-paragraph invariant note to `shared-postgres-schema.md` (placement: after the table-ownership summary and before the first CREATE TABLE) that states: (1) shared Postgres stores coordination records only — sessions, memberships, invites, presence history, runtime node attachments, cross-node dispatch records; (2) there is intentionally no `session_events_shared`, `session_events_global`, or equivalent cross-participant event table in V1; (3) per-daemon local `session_events` in `local-sqlite-schema.md` is authoritative per ADR-017; (4) adding such a table requires an ADR-017 supersession and MLS promotion gate completion per ADR-010. This converts a negative-space observation into a checkable invariant discoverable to future schema authors without forcing them to first read ADR-017.
- Exit Criteria: `shared-postgres-schema.md` contains a labeled invariant section citing ADR-017 that names the four points above; the note is placed before the first CREATE TABLE statement so it anchors all downstream schema additions; grep for `session_events_shared` in `shared-postgres-schema.md` returns the invariant note (not a table definition); no retrospective/amendment phrasing ("previously had," "removed," "was considered" — the invariant is declared forward).

### Phase 4 — Cross-Plan Propagation (P0 — directly unblocks Plan-001)

Propagate the scope anchor through the plan graph. Without this, implementers hit the scope contradiction on day one.

#### BL-054: Align cross-plan-dependencies.md with V1 = 16 and fix Tier 3 table-name drift

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-038 (ADR-015 defines V1 = 16), BL-039 (v1-feature-scope.md is the canonical V1 definition), BL-041 (Spec-023 must exist to be cited), BL-043 (Plan-023 must exist), BL-044 (Plan-021), BL-045 (Plan-022), BL-047 (Spec-024 cross-node)
- References: [cross-plan-dependencies.md](../architecture/cross-plan-dependencies.md), [shared-postgres-schema.md](../architecture/schemas/shared-postgres-schema.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md)
- Summary: Update the 9-tier graph so Plan-016 (multi-agent channels) sits inside the V1 tier set rather than Tier 9; place Plan-023 (desktop shell + renderer) after Plan-013; place Plan-021 (rate limiting, depends on Plan-008) and Plan-022 (data retention / GDPR, Tier 2 at the latest so encryption write-path ships with the first write); add Spec-024 (cross-node dispatch) as an implicit dependency of Plan-002/Plan-003/Plan-008/Plan-012. Fix the Tier 3 table-name drift: replace `runtime_nodes`, `node_attachments` with the schema-canonical `runtime_node_attachments`, `runtime_node_presence`. Update §1 Table Ownership Map and §5 Tier assignments consistently. Doc text should read as a greenfield V1 definition, not as "expanding" or "updating" a prior shipped graph.
- Exit Criteria: cross-plan-dependencies.md tier graph aligns with V1 = 16 per ADR-015; Plan-016 placed in the V1 tier set; Plan-021, Plan-022, Plan-023 present with correct tier slots; Spec-024 recorded as an implicit dep of Plan-002/Plan-003/Plan-008/Plan-012; Tier 3 table names match `shared-postgres-schema.md` and `local-sqlite-schema.md` exactly; the document contains no retrospective/amendment phrasing.

#### BL-055: Propagate V1 scope across all 20 plan files

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- Depends-on: BL-038 (ADR-015 is the citation target), BL-039 (v1-feature-scope.md is the scope-of-truth for the grep audit)
- References: `docs/plans/001-shared-session-core.md` through `docs/plans/020-observability-and-failure-recovery.md`, [ADR-015](../decisions/015-v1-feature-scope-definition.md) (from BL-038)
- Summary: Remove `V1.1 — deferred` / `V2 — deferred` markers on renderer sections across the 19 plans that carry them (Desktop GUI is a V1 feature per ADR-015, so these deferrals no longer apply). Add a single inline note in each plan pointing to ADR-015 for the canonical V1 definition. Resolve any remaining scope inconsistencies between plan bodies and `v1-feature-scope.md`. Plan text must read as a current V1 declaration, not as "this feature is being moved to V1" or "previously V1.1."
- Exit Criteria: grep for `V1\.1|V2|deferred` across `docs/plans/*.md` returns only intentional references (e.g., features genuinely deferred — MLS, email invite delivery, cross-node artifacts, workflow authoring); all 20 plan bodies cite ADR-015; no plan body contains retrospective/amendment phrasing about scope changes.
- Resolution (resolved 2026-04-18 per Session 4): All 20 plan files cite ADR-015; `grep V1\.1|V2|deferred` returns 60 matches across 27 files including 4 legitimate V1.1 references (MLS relay E2EE, email invite delivery, cross-node artifacts, workflow authoring) per Exit Criterion's "only intentional references" rule (commit `cc6b4cd` Session 4 cross-plan dependency alignment); META-2 verification 2026-04-22 (H5 §3.1 row 3.10).

### Phase 5 — Security Hardening (P1 — parallel with early Plan-001 work)

Each surface below will be touched in the first implementation sprint.

#### BL-056: Resolve Desktop Renderer trust stance

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [container-architecture.md:71](../architecture/container-architecture.md), [security-architecture.md](../architecture/security-architecture.md) §Local Daemon Authentication, [Spec-023](../specs/023-desktop-shell-and-renderer.md) (from BL-041)
- Summary: Resolve the contradiction between container-architecture.md (renderer is "untrusted") and security-architecture.md (renderer is "a trusted local process"). Recommended: untrusted; Desktop Shell holds the session token and brokers per-channel capabilities to the renderer via the preload bridge; renderer never directly holds the PASETO token or Ed25519 DPoP key.
- Exit Criteria: Both docs state the same stance; Spec-023 renderer/shell IPC surface matches; no doc contradicts the choice.
- Resolution (resolved 2026-04-18 per Session D1): Renderer-untrusted stance now consistent across `container-architecture.md:71` ("Desktop Renderer is untrusted"), `component-architecture-desktop-app.md:48` ("renderer is less trusted"), `security-architecture.md:81/92/94` ("renderer never holds the daemon session token") with Spec-023 §Trust Stance + BL-056 reconciliation cross-referenced (commit `b2c7b48` Session D1 renderer trust centralization); grep verification 2026-04-22 (H5 §3.1 row 3.11).

#### BL-057: Specify CLI at-rest identity key storage

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md), [ADR-021](../decisions/021-cli-identity-key-storage-custody.md), [security-architecture.md](../architecture/security-architecture.md), [Spec-008](../specs/008-control-plane-relay-and-session-join.md)
- Summary: Specify the fallback order for CLI identity key storage when WebAuthn PRF is not available: (1) OS-native keystore (libsecret / Keychain / DPAPI), (2) Argon2id-derived KEK from user password with explicit weaker-tier warning, (3) refuse to participate in shared-session E2EE. Specify rotation on daemon restart (opt-in), mandatory rotation on device reset, and stolen-key reuse detection (same Ed25519 pubkey from two machines simultaneously).
- Exit Criteria: ADR-010 has a new §CLI Identity Key Storage; security-architecture.md cites the fallback order; Spec-008 references the storage contract.

#### BL-058: Specify daemon master key storage + rotation + backup constraint

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-022 §Daemon Master Key](../specs/022-data-retention-and-gdpr.md#daemon-master-key), [security-architecture.md §Daemon Master Key Rotation](../architecture/security-architecture.md#daemon-master-key-rotation), [local-persistence-repair-and-restore.md §Backup Constraints](../operations/local-persistence-repair-and-restore.md#backup-constraints), [ADR-021](../decisions/021-cli-identity-key-storage-custody.md)
- Summary: Master key custody tied to participant credentials via KEK derivation (WebAuthn PRF or Argon2id). Two-tier + refuse ladder (OS keystore cache, Argon2id-encrypted file fallback, refuse). Wrapped envelope reuses ADR-021's 50-byte format. Idle-wipe (default 15min) + shutdown-wipe + lock-wipe via `sodium_mlock`. Rotate-on-shred re-wraps all remaining `participant_keys` rows atomically. Backup separation: wrapped master blob MUST be excluded from any backup that captures `participant_keys` ciphertext. Divergence from ADR-021's keystore-authoritative ladder is intentional and documented: CLI identity key's threat model prioritizes durability; daemon master key's threat model prioritizes binding-to-credential (crypto-shred).
- Exit Criteria: Spec-022 has new §Daemon Master Key ✅; security-architecture.md reflects the rotation policy ✅; operations runbook's backup section names the separation constraint ✅.
- Resolved: 2026-04-18 (Session D2).

#### BL-059: Specify Cedar policy chain-of-custody

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [ADR-012 §Policy Chain of Custody](../decisions/012-cedar-approval-policy-engine.md#policy-chain-of-custody), [Cedar Policy Signing And Rotation](../operations/cedar-policy-signing-and-rotation.md), [Spec-012](../specs/012-approvals-permissions-and-trust-boundaries.md), [Plan-012](../plans/012-approvals-permissions-and-trust-boundaries.md)
- Summary: V1/V1.1 layer carve-out: V1 signed artifact = daemon container image (policies-in-binary covered by image signing); V1.1 signed artifact = separate `policy-bundle-v{N}.cedar.tar.gz` + detached `.sig` evaluated at runtime by `@cedarpolicy/cedar-wasm`. Ed25519 primary (ECDSA P-256 FIPS fallback). Pinned `OPERATOR_PUBLIC_KEY` baked into daemon image at build time; self-hosters rebuild with their own operator key. Monotonic unsigned integer version counter with rollback rejection. Fail-closed on: unsigned bundle, wrong-key signature, hash mismatch, timestamp-expired, algorithm mismatch. V1 rotation = coordinated fleet upgrade (no dual-pinning); compromise response acknowledges upgrade-gap window honestly. Cedar v4.5 pinned. V2+ forward declarations: TUF, Sigstore/Fulcio/Rekor, HSM, multi-sig, PQC, online revocation.
- Exit Criteria: ADR-012 has new §Policy Chain of Custody ✅; `docs/operations/cedar-policy-signing-and-rotation.md` exists ✅.
- Resolved: 2026-04-18 (Session D2).

#### BL-060: Implement secure-by-default behaviors for self-host deployment + companion doc

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-027 — Self-Host Secure Defaults](../specs/027-self-host-secure-defaults.md), [self-host-secure-defaults companion](../operations/self-host-secure-defaults.md), [deployment-topology.md](../architecture/deployment-topology.md), [Spec-007](../specs/007-local-ipc-and-daemon-control.md), [Spec-020](../specs/020-observability-and-failure-recovery.md), [Spec-021](../specs/021-rate-limiting-policy.md), [Spec-022](../specs/022-data-retention-and-gdpr.md), [Spec-025](../specs/025-self-hostable-node-relay.md), [Spec-026](../specs/026-first-run-onboarding.md), [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md), [ADR-012](../decisions/012-cedar-approval-policy-engine.md), [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md); secure-default exemplars researched 2026-04-19 (Caddy v2.11.2, Tailscale, Syncthing, Supabase). Supabase appears in the spec as a NEGATIVE exemplar (Pitfalls) after primary-source verification of issue #42562 contradicted the original charter's positive-exemplar framing.
- Summary: Self-host deployment must be secure by default — an OSS user who `git clone`s and runs the product should get a secure deployment without reading a hardening guide. Per the discussion recorded on 2026-04-17 during BL-053 resolution, BL-060 is retargeted from "enterprise-grade security requirements doc" to "ship the behaviors in the app, with a short companion doc covering what's default and how to opt into non-default modes." Ten secure-by-default behaviors ship in V1:
  1. **Auto-TLS for the self-hostable relay.** ACME integration (HTTP-01 + DNS-01) with auto-renewal, matching the Caddy auto-HTTPS model. If no public domain is reachable (LAN-only / dev use), generate a self-signed cert and emit its fingerprint on stdout for trust-on-first-use on the client side.
  2. **Refuse to start without encryption** on any non-loopback bind unless `--insecure` is set explicitly; when `--insecure` is set, print a loud startup banner so the operator can never silently run unencrypted.
  3. **Auto-generated strong secrets on first run.** Daemon and relay generate all required symmetric keys (session-signing, master key for at-rest encryption, relay admin token) via `crypto.randomBytes`, persist with `0600` permissions, and document the rotation schedule.
  4. **Bind to `127.0.0.1` by default** for all daemon/relay sockets; require explicit `--bind <addr>` (or `BIND_ADDR=`) to expose externally; reject `0.0.0.0` without TLS (see behavior 2).
  5. **Postgres TLS enforcement** (`sslmode=require`) for the self-host `rate-limiter-flexible` backend (per BL-044 dual-backend shipping); verify minimum Postgres version on startup; reject weak auth methods (md5, trust).
  6. **SQLite online-backup job** for daemon local state. Auto-rotate backups (default cadence: every session-end + nightly full); default destination is a `backups/` directory inside the daemon data dir; user-configurable via `BACKUP_DIR=` or a plug-in point for S3/GCS/similar.
  7. **Auto-update for daemon + CLI.** Desktop is covered by `electron-updater` in Plan-023 scope. Daemon + CLI self-update via a signed release manifest (GitHub Releases + Ed25519 signature on the manifest). Security patches apply with opt-out-only behavior for minors; majors require explicit user confirmation.
  8. **Minimum TLS 1.3** everywhere TLS is used; reject TLS ≤ 1.2 outright.
  9. **Security monitoring exports** on a Prometheus-compatible `/metrics` endpoint: token-auth-failure rate, rate-limit-trip rate, Cedar-deny rate (from ADR-012), relay connection-churn rate, backup-success counter, auto-update check status. Documented in the companion doc so self-hosters who scrape metrics know what signals to watch.
  10. **Loud first-run banner** summarizing auto-configured security posture (TLS mode + fingerprint if self-signed, bind address, backup destination, admin-token path, update channel). Single-screen overview on every fresh startup so the operator never has to grep files to know what they're running.

  Companion doc (`docs/operations/self-host-secure-defaults.md`, ~1–2 pages): for each behavior above, list (a) what's on by default, (b) how to opt out / override, (c) the one-line reason the default is what it is. No compliance-framework mapping (SOC 2 / ISO 27001 / HIPAA / FedRAMP) in this doc; those extensions are V1.1+ scope, to be authored if/when enterprise SaaS features ship.

  Out of scope for this BL (deferred to V1.1+): IdP / OIDC / SAML compatibility matrix; WAF recommendations; HSM for operator signing keys; SOC 2 / compliance-framework alignment; offline-root signing infrastructure; hardened-image signing (Cosign + SBOM) — all relevant for an eventual enterprise hosted tier but not for a V1 OSS self-host user.

- Resolution (2026-04-19, Session D3): three deliberate strengthenings of the original ten-behavior charter, each sourced to 2026-04-19 primary-source research (four parallel Opus 4.7 subagents) and advisor-confirmed before authoring:
  - **Behavior 5 strengthened**: `sslmode=require` → `sslmode=verify-full` (with `verify-ca` as a documented loud-banner fallback; `disable`/`allow`/`prefer`/`require` refused at config-parse). Libpq `require` does not defeat MITM attacks (docs: "I trust that the network will make sure I always connect to the server I want"); CVE-2024-10977 (2024-11-14, libpq error-message injection) is the exploitable path under that setting.
  - **Behavior 7 split into 7a + 7b**: 7a (notify-by-default, daemon — daemon never self-swaps while IPC is live) and 7b (opt-in CLI `self-update` with dual-path Ed25519-manifest + GitHub Artifact Attestation / Sigstore-bundle verification). Dual-path verification responds to 2025–2026 npm supply-chain incidents (Shai-Hulud 2025-09/11, 25k+ repos; Axios 2026-03-31) where single-trust-path distribution was shown insufficient. Anti-rollback / anti-freeze manifest fields close the attack paths that bare manifest-signing leaves open.
  - **Supabase framing corrected**: original BL-060 cited Supabase as a positive "auto-generates secrets on first run" exemplar. Primary-source verification (WebSearch 2026-04 + GitHub issues) confirms Supabase explicitly does NOT auto-generate — `generate-keys.sh` is experimental with bug #42562 (writes a non-existent env var). Supabase is now cited under Spec-027 §Pitfalls as the negative `.env.example`-placeholder anti-pattern that motivates behaviors 2 + 3's parse-time refuse-to-start enforcement.
  - **Behavior 1 `DEPLOY_MODE` swap (post-Opus-review advisor discretionary)**: the post-authoring Opus 4.7 review caught a factually wrong claim that Caddy silently falls back from failed public-ACME to internal CA; the fix initially prescribed runtime daemon-mediated detect-and-reconfigure on Plan-025. Advisor flagged the runtime-reconfigure mechanic as expanding Plan-025's scope with Caddy-admin-API logic that was not there before, and flagged a separate `.home.arpa` / `.local` mDNS gap between "Caddy internal-CA default for `.localhost`/RFC1918" and "public ACME on public hostnames." Both resolved by switching to an operator-declared `DEPLOY_MODE=public|lan` config with parse-time mismatch refusal. Operator intent over runtime auto-detect keeps a security-critical branch auditable and testable; LAN-suffix refusal list (`.localhost` / `.local` / `.home.arpa` / `.internal` / RFC1918) closes the mDNS gap. Spec-027 renders 11 rows (original 10 with behavior 7 split into 7a/7b) and assigns plan ownership per behavior (Plans 001, 007, 020, 025, 026). Code-level integration-test acceptance criteria from the original Exit Criteria are carved out to those plans via Spec-027 §Acceptance Criteria (20 testable criteria including CVE-2024-10977 MITM repro, manifest rollback/freeze rejection, `/metrics` Prometheus-format verification, SPKI-SHA256 vs SSH-style fingerprint format, `DEPLOY_MODE` mismatch refusal on both public and lan sides).
- Exit Criteria: `docs/specs/027-self-host-secure-defaults.md` authored (Default / Override / Reason / Plan-Ownership table for 11 rows, §Bind-Address Reconciliation, §Authority Split with BL-063, 18 testable acceptance criteria, primary-source citations for TLS / ACME / Postgres / exemplars / auto-update) ✅; `docs/operations/self-host-secure-defaults.md` companion authored (operator-facing verify-command column per row + operational-events notes for LE 45-day pivot 2026-05-13 and PG 14 EOL 2026-11-12 + cross-refs to specs / plans / runbooks) ✅; code-level integration tests carved out to Plans 001 / 007 / 020 / 025 / 026 per Spec-027 §Acceptance Criteria (plan-level scope, not doc-level BL-060 scope).
- Resolved: 2026-04-19 (Session D3).

### Phase 6 — Persistence Hardening (P1 — parallel with early Plan-001 work)

Each surface below is Plan-001-, Plan-004-, or Plan-006-adjacent.

#### BL-061: Specify SQLite writer concurrency model

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-015](../specs/015-persistence-recovery-and-replay.md), [component-architecture-local-daemon.md](../architecture/component-architecture-local-daemon.md), [data-architecture.md](../architecture/data-architecture.md)
- Summary: Pin `better-sqlite3` as V1 driver (production-recommended; `node:sqlite` still RC on Node 22/24). Single-writer discipline: one worker thread owns all writes; submitters post to bounded queue; worker drains in batched transactions (default 50 events or 10ms). Backpressure: submitter blocks at threshold. Drop policy: drop only `thinking_update` events when saturated; never drop canonical state-change events. Metric: `sqlite_queue_depth_p99` alert at 80% of cap.
- Exit Criteria: Spec-015 §Writer Concurrency exists; component-architecture-local-daemon.md references the writer module; data-architecture.md pins driver choice.
- Resolution (2026-04-19, Session E1): Spec-015 §Writer Concurrency authored with primary-source evidence (fetched 2026-04-19). Driver pin: `better-sqlite3@^12.9.0` (released 2026-04-12; engines `node: "20.x || 22.x || 23.x || 24.x || 25.x"`; recommended runtime 24 LTS). Verified `node:sqlite` is still Stability `1.2 — Release candidate` on Node 24.15.0 LTS and Node 25.9.0 Current as of 2026-04-19 — not viable for V1; pin gate re-evaluates pre-V2 once it graduates to Stability 2. **Single-writer worker reframed as platform requirement, not stylistic choice**: `node:worker_threads` docs exclude native-addon-backed objects from `postMessage`-transferable values, so `better-sqlite3` `Database` handles cannot be shared across threads (verified at nodejs.org/api/worker_threads.html + better-sqlite3 `docs/threads.md`). Pragmas mandate `synchronous = FULL` as explicit override of better-sqlite3 bundled default (NORMAL trades "slight loss of durability" per maintainers) because hash-chain rows are cryptographically linked per Spec-006 §Integrity Protocol — a lost write breaks verifiability irrecoverably. Bounded queue cap `10_000` events; batch flush at `50` events OR `10 ms` via `db.transaction(fn)` (atomic commit / rollback-on-throw). **Hash-chain scope explicitly per-session within a batch** (added post-review per C2): groups events by `session_id`, seeds each group's chain from its own last-persisted `row_hash`, never from the preceding event in enqueue order across sessions. Backpressure: canonical state-change events await drain (never dropped); only `assistant.thinking_update` is drop-eligible with per-session 1/s-rate-limited `event_dropped` counter tagged `session_id` and `event_type`. Upgrade-policy gate added (S3): major bumps of `better-sqlite3` (`13.x.y`) must be re-evaluated against pragma overrides + `.backup()` atomicity contract before the pin moves. Alert `sqlite_queue_depth_p99` at 80% cap; `persistence_backpressure` warning shape forward-declared in Spec-015 (no tracking anchor — an alert-shape entry is not an event-type entry, and BL-064's charter is event-taxonomy scope; routing the alert through BL-064 would have silently expanded that charter). Spec-020's alert catalog will carry the entry when it lands; C1 dangling-reference fix is the "forward-declared here" wording itself, not a BL anchor. Peripheral: `data-architecture.md` Local SQLite Store row pins driver + worker thread; `component-architecture-local-daemon.md` Local Persistence Layer row references writer module. Opus 4.7 review caught C2/C1/S1/S3 and all landed same pass.

#### BL-062: Specify clock-handling strategy for event timestamps

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-015](../specs/015-persistence-recovery-and-replay.md), [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md) (`session_events.occurred_at`)
- Summary: Separate `monotonic_ns` (daemon uptime) from `wall_clock_iso`. Monotonic used for "happened after" within a daemon; wall clock for display. Require NTP sync as startup precondition; emit `clock_unsynced` warning event otherwise. Emit `clock_corrected` event on material skew detection. If BL-046 chooses Option A, add HLC column keyed to `(sequence, monotonic, wall)`.
- Exit Criteria: Spec-015 §Clock Handling exists; `session_events` schema adds `monotonic_ns` column; event taxonomy (BL-064) includes `clock_corrected` and `clock_unsynced`.
- Resolution (2026-04-19, Session E1): Spec-015 §Clock Handling authored with primary-source evidence (fetched 2026-04-19). `monotonic_ns INTEGER NOT NULL` column added to `session_events` DDL in `local-sqlite-schema.md` — sourced from `process.hrtime.bigint()` citing Node.js docs verbatim ("relative to an arbitrary time in the past, and not related to the time of day and therefore not subject to clock drift" — deliberately not upgraded to "monotonic" because Node docs don't use that word). **Charter's proposed `wall_clock_iso` column name superseded by pre-existing `occurred_at`** to avoid schema rename; semantic mapping preserved (RFC 3339 UTC with ms precision + `Z` suffix — equivalent to `Date.prototype.toISOString()`, verified against RFC 3339 §5.6). Scope: `monotonic_ns` is within-daemon ordering primitive only; NOT cross-daemon. HLC tracking stays in BL-076 per ADR-017 Option B (V1 chose daemon-authoritative ordering, no shared event log to order against). **HLC column explicitly NOT added** because BL-046 resolved to Option B. NTP sync precondition with platform matrix: Linux systemd → `timedatectl show --property=NTPSynchronized --value` reads kernel `adjtimex(2)` flag (authoritative regardless of timesyncd/chrony/ntpd); Linux chrony → `chronyc tracking` `Leap status`; Windows → `w32tm /query /status /verbose` parse (`Last Sync Error == 0 && Stratum < 16 && Source != "Local CMOS Clock"` — no single boolean); macOS → best-effort `systemsetup -getusingnetworktime` + `sntp` freshness probe (no single-boolean equivalent, flagged as known gap); Container → probe host via `--ntp-sync-status-override=<env|file>` escape hatch. Material-skew threshold **500 ms** citing CockroachDB `--max-offset` default (v26.1) as named precedent — framed explicitly as conservative ceiling, not physics floor, noting Kubernetes kubelet + etcd publish no comparable thresholds. Events reserved `session.clock_unsynced` (NTP probe failed at startup) and `session.clock_corrected` (runtime wall-clock jump > 500 ms) — both category `run_lifecycle`; full taxonomy-table enumeration remains BL-064. `session.clock_corrected` payload renamed `deltaMs` → `wallClockDeltaMs` post-review (S2) to eliminate monotonic-vs-wall-clock conflation. Peripheral: `data-architecture.md` §Event-Sourcing Scope augmented with "Within-daemon ordering primitive" paragraph naming `monotonic_ns`.

#### BL-063: Require automated SQLite backup policy

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-015](../specs/015-persistence-recovery-and-replay.md), [local-persistence-repair-and-restore.md](../operations/local-persistence-repair-and-restore.md)
- Summary: Automatic WAL checkpoint every 5 minutes or 1000 pages (whichever first). Full backup daily via `sidekicks db backup` triggered by daemon scheduler. Retention: 7 daily + 4 weekly. Storage: `$XDG_STATE_HOME/ai-sidekicks/backups/` with user-configurable remote sync path. Pre-migration automatic backup. Recovery SLO: restore from backup aged ≤ 24 hours.
- Exit Criteria: Spec-015 §Backup Policy exists; runbook references the automation (replacing "if a backup exists").
- Resolution (2026-04-19, Session E1): Spec-015 §Backup Policy authored with primary-source evidence (fetched 2026-04-19). WAL checkpoint cadence: both triggers PASSIVE-mode only (page-driven uses SQLite autocheckpoint default 1000 pages per sqlite.org/pragma.html; time-driven runs explicit `PRAGMA wal_checkpoint(PASSIVE)` every 5 min — PASSIVE is the only mode that "does as much work as it can without interfering with other database connections" per sqlite.org/wal.html §3.2). TRUNCATE runs only at backup completion to reclaim WAL disk space (only mode that truncates the log file to zero bytes). Daily full backup via `better-sqlite3.backup(destination)` — Promise-returning wrapper around the SQLite Online Backup API; destination-side exclusive lock mandates a `backup.lock` single-runner file opened with `O_EXCL|O_CREAT`. **Atomic-publish sequence mandated at implementation layer** because WiseLibs docs don't specify `.backup()` fsync behavior: tmp → `fsync(fd)` → `rename()` (same filesystem, atomic per `rename(2)`) → `fsync(parent_fd)` (required for directory-entry durability per `fsync(2)` man page: "Calling `fsync()` does not necessarily ensure that the entry in the directory containing the file has also reached disk"). Retention 7 daily + 4 weekly honestly labeled as **project convention, not named-in-literature standard** — GFS-structured variant; no canonical literature canonicalizes the 7+4 split. Pre-migration backup `pre-migration-v{N}-{timestamp}.db` is a load-bearing rollback point retained indefinitely until explicit operator prune; **verified no precedent in Django 5.1 / Rails 8.1 Active Record / Alembic 1.18** — documented as intentional project-specific guarantee. Filesystem layout: host = `$XDG_STATE_HOME/ai-sidekicks/backups/` per XDG Base Directory Specification v0.8 (8 May 2021, current — STATE chosen over DATA because backups are recoverable state, not user-portable data); container = absolute `/var/lib/ai-sidekicks/backups/` with bind mount (XDG variables inside containers are not meaningful per Docker docs). **Reconciles Spec-027 Row 6 `./data/backups/`** as the operator-facing `docker-compose.yml` default bind-mount point; Spec-015 owns the daemon-side absolute path contract + atomicity contract. `0700` POSIX mode requirement added post-review (S1) to symmetrize with Spec-027 Row 6 host-side. Master-key separation: `daemon-master.enc` trivially excluded from `.backup()` output (SQLite online backup API copies pages only, cannot pick up sibling files); runbook `local-persistence-repair-and-restore.md §Backup Constraints` owns OS-specific exclusion rules for filesystem-level backups. Restore SLO ≤ 24h staleness; WAL replay provides sub-5-minute RPO when on-disk WAL is salvageable. Peripheral: `local-persistence-repair-and-restore.md` §Preconditions + Recovery Steps 5/6 rewritten to reference Spec-015 §Backup Policy instead of "if a backup exists" conditionals.

#### BL-064: Extend event taxonomy with missing types

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md)
- Summary: Add event types: `runtime_node.registered/online/degraded/offline/revoked`, `runtime_node.capability_declared/capability_updated`, `tool.replayed`, `tool.skipped_during_recovery` (from BL-051), `recovery.attempted/succeeded/failed`, `clock_corrected`, `clock_unsynced` (both from BL-062), `schema.migrated`, `event.compacted`, `event.shredded`, `participant.exported`, `participant.purge_requested`, `participant.purged`, `participant.tokens_revoked_all` (from BL-070), `participant.device_reset`, `key_reuse_detected` (from BL-057), `policy_bundle.loaded`, `policy_bundle.rejected` (from BL-059), `audit_integrity_verified`, `audit_integrity_failed` (both from BL-050).
- Exit Criteria: Spec-006 enumeration includes all new types with payload schemas.
- Resolution (2026-04-19): Spec-006 §Event Type Enumeration extended 93 → 120 event types via 6 new category subsections: **Runtime Node Lifecycle** (`runtime_node_lifecycle`, 9 types — 7 runtime*node.\* + 2 `session.clock*\*`wire-stable names reclassified from`run_lifecycle` per ADR-018 §Decision #3; Temporal Worker lifecycle + HashiCorp Nomad Event Stream topics cited as architectural precedents, Kubernetes monolithic condition array cited as negative precedent); **Recovery Events** (`recovery_events`, 3 types; Kafka Streams `StateRestoreListener` precedent for phase-granular recovery observability); **Participant Lifecycle** (`participant_lifecycle`, 5 types — GDPR Article 17 verbatim + NIST SP 800-88 Rev.2 (Sept 26, 2025) + EDPB CEF Report (Feb 18, 2026) cited; `participant.deleted`renamed wholesale to`participant.purged` per ADR-018 §Decision #3 because rename is not additive); **Audit Integrity** (`audit_integrity`, 3 types — `audit_integrity_verified/failed`promoted to own category with corrected classification and fuller payload + new`key_reuse_detected`; invariant: never compacted, never shredded; RFC 9162 CT v2.0 §§4.9-4.12 + §§2.1.3.2/2.1.4.2 verbatim envelope fields + AWS CloudTrail `validate-logs`precedent + Sigstore observer/monitor pattern for`key_reuse_detected`, GCP Audit Logs negative precedent); **Event Maintenance** (`event_maintenance`, 3 types — `schema.migrated/event.compacted/event.shredded`; invariant: never compacted, never shredded; Flyway 10/11 `flyway_schema_history` + Liquibase DATABASECHANGELOG + golang-migrate counter-example + Kafka compaction semantics + EventStoreDB/Conduktor/Axon Server crypto-shred precedents, EDPB Guidelines 02/2025 on Blockchain for rationale); **Policy Events** (`policy_events`, 2 types — AWS Verified Permissions CloudTrail envelope precedent + Cedar Policy Validation 8 errors + 4 warnings enumerated verbatim). api-payload-contracts.md `EventCategory`union extended 9 → 15 values with positional stability (9 originals preserved in original order per ADR-018 additive-wire-safe invariant; 6 new values appended with cross-ref comment). Spec-015 §Reserved Events replaced with cross-ref to Spec-006 §Runtime Node Lifecycle, preserving behavioral anchors (NTP sync probe behavior, material-skew threshold,`wallClockDeltaMs` semantics). Primary sources cited with URL + 2026-04-19 fetch dates throughout. Opus 4.7 review returned FIXES-REQUIRED (two stale references: Spec-006 line 526 file path + api-payload-contracts.md comment section name); both fixes applied in-pass.

#### BL-065: Write ADR-018 Cross-Version Multi-Node Compatibility

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [ADR-018](../decisions/018-cross-version-compatibility.md), [data-architecture.md](../architecture/data-architecture.md), [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md) (`EventEnvelope.version`)
- Summary: `EventEnvelope.version` becomes the wire-format version. Clients MUST ignore unknown event types gracefully with a persisted audit stub that is re-interpretable on upgrade. Session metadata gets `min_client_version`; below-minimum clients can read but not write new events. Breaking envelope changes bump major; minor bumps require forward-compat (new optional fields only).
- Exit Criteria: `docs/decisions/018-cross-version-compatibility.md` exists; data-architecture.md has §Cross-Version Compatibility; Spec-006 documents `EventEnvelope.version` usage.
- Resolution (2026-04-18): ADR-018 authored (accepted, Type 2 one-way door) with 11 binding Decisions: semver `MAJOR.MINOR` envelope version, producer-written; `min_client_version` as monotonic-raise session floor; MINOR bumps additive-only (new optional fields / new event types / new enum values); unknown types persisted as signed **version stubs** (distinct from Spec-006 compaction stubs — version stubs retain full canonical bytes so Ed25519 signatures remain verifiable); upcaster chain at read/dispatch time keyed on `(original_version, original_type)` — log never rewritten; version stubs excluded from compaction until re-interpreted ≥ once so post-upgrade replay is lossless; `VERSION_FLOOR_EXCEEDED` / `VERSION_CEILING_EXCEEDED` typed errors forward-declared for error-contracts.md registration; catastrophic-floor-mistake recovery requires abandoning the affected session (monotonicity absolute). Industry precedents cited (accessed 2026-04-18): Kubernetes Version Skew Policy v1.35 (3-minor skew), Confluent Schema Registry FORWARD_TRANSITIVE, Protobuf Editions unknown-field preservation, CloudEvents 1.0.2. data-architecture.md §Cross-Version Compatibility + Spec-006 §EventEnvelope Version Semantics added with terminology disambiguation ("version stub" vs "compaction stub").

#### BL-066: Extend PII data-map fan-out on shred

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-022](../specs/022-data-retention-and-gdpr.md), [Spec-020](../specs/020-observability-and-failure-recovery.md)
- Summary: Enumerate every PII-carrying path: `session_events.pii_payload`, bounded-retention diagnostic payloads (driver raw events, command output, tool traces), telemetry export (traces, logs), event signature payloads. `DELETE /participants/{id}/data` fans out to all paths; diagnostic TTL buckets get purged. Telemetry export redacts PII by default with explicit opt-in for raw content.
- Exit Criteria: Spec-022 §PII Data Map + §Shred Fan-Out exist; Spec-020 §PII in Diagnostics cross-references Spec-022.
- Resolution (2026-04-19): Spec-022 §PII Data Map extended into three tiers — **durable tier** (SQLite `session_events.pii_payload` encrypted column + `participant_keys` per-participant AES-256-GCM keys; Postgres `participants` mirror subset), **bounded-retention diagnostic tier** (4 buckets: `driver_raw_events`, `command_output`, `tool_traces`, `reasoning_detail` with ≤ 7-day TTL default), **telemetry export tier** (OTel spans/logs + error-tracker payloads, redacted-by-default). Spec-022 §Shred Fan-Out added with 3 ordered paths: Path 1 (SQLite pii*payload crypto-shred via `participant_keys.key_material` destruction) → Path 2 (Postgres hard DELETE of mirror row) → Path 3 (bounded-retention TTL bucket purge across the 4 diagnostic buckets); ordering rationale: Path 1 before Path 2 prevents concurrent readers from decrypting via Postgres lookup; Path 2 before Path 3 prevents diagnostic-bucket readers from re-deriving PII via Postgres JOIN; `participant.purged` aggregate event emitted only after all three paths succeed (idempotent on replay). Spec-022 §Signature Safety Under Shred added with 5-point proof sketch establishing Ed25519 signature retention does NOT re-introduce PII after shred: (1) RFC 8785 JCS canonical bytes exclude `pii_payload` by construction, (2) `pii_ciphertext_digest` indirection is one-way (BLAKE3 preimage resistance), (3) AES-256-GCM key destruction renders ciphertext non-decryptable per NIST SP 800-38D §8.3, (4) Ed25519 signature verification is public-key operation per RFC 8032 §5.1 (no shortcut recovers input), (5) signature retention is load-bearing for post-shred audit integrity. Spec-020 §PII in Diagnostics added with 5-point Required Behavior policy (default-deny on outbound telemetry, opt-in for raw content, bounded local retention ≤ 7-day default with `retention_policy_override` warning metric > 30-day, shred fan-out coverage mandatory, summary-only retention from non-PII signals by construction) and Industry Precedent section citing OpenTelemetry GenAI semconv v1.36.0 verbatim *"Instrumentations SHOULD NOT capture them by default"\_ for `gen_ai.prompt`/`gen_ai.completion`, Datadog Sensitive Data Scanner default managed-rule library, Sentry server-side scrubbing default keyname list. All primary sources cited with URL + 2026-04-19 fetch dates. Opus 4.7 review verified 4-bucket consistency across Spec-020 and Spec-022 and §PII Data Map tier/§Shred Fan-Out path cross-references are internally consistent.

### Phase 7 — Documentation Hygiene (P2 — batch into first implementation sprint)

Low-stakes cleanup. Some trivial, some require small edits across multiple docs. Batch when convenient.

#### BL-067: Swap paseto-ts library reference to panva/paseto

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [vision.md:280](../vision.md), [ADR-010 §PASETO v4 Implementation Library](../decisions/010-paseto-webauthn-mls-auth.md), [Plan-025](../plans/025-self-hostable-node-relay.md)
- Summary (original charter — superseded by Session G1 Resolution below): "Replace `paseto-ts` with `panva/paseto`." Rationale as originally filed: library-agnostic wire format, swap would have been drop-in. Resolution at line 487 documents the 2026-04-19 reframe that rejected both third-party libraries and aligned with Plan-025's in-house `packages/crypto-paseto/` package.
- Exit Criteria: vision.md Add table and ADR-010 both reference the chosen PASETO library ✅.
- Resolution: BL-067's original premise was invalid. Session G1 research (2026-04-19, Opus 4.7 subagent with websearch against primary sources) confirmed that `panva/paseto` was archived by its maintainer on 2025-03-29 and does not implement `v4.local` — making it unusable for ADR-010's 7-day refresh-token requirement regardless of download count. `paseto-ts` is active but single-maintainer (`miunau`) and unaudited, which fails ADR-010's Type 2 (one-way door) concentration-risk bar. Resolution: align with [Plan-025](../plans/025-self-hostable-node-relay.md)'s in-house `packages/crypto-paseto/` package on audited `@noble/curves` + `@noble/ciphers` primitives. Both third-party libraries rejected. See [ADR-010 §PASETO v4 Implementation Library](../decisions/010-paseto-webauthn-mls-auth.md) for the full evaluation and citations.
- Resolved: 2026-04-19 (Session G1).

#### BL-068: Reframe Cloudflare Durable Objects scaling claims

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [deployment-topology.md §Relay Scaling Strategy](../architecture/deployment-topology.md), [vision.md:417](../vision.md)
- Summary (original charter — superseded by Session G2 Resolution below): "Rewrite '25 connections per DO' as a tunable design choice rather than a platform cap. State Cloudflare's actual limits (32,768 concurrent WS per DO; 1,000 rps soft cap) and explain the sharding rationale." As originally filed. The "32,768 concurrent WS per DO" figure in this charter was one of the unverified claims Session G2 research refuted — Cloudflare publishes no concurrent-connection cap per DO. The Resolution below is authoritative.
- Exit Criteria: deployment-topology.md distinguishes platform limits from design choices ✅; vision.md Relay Scaling reference aligns ✅.
- Resolution: Session G2 research (2026-04-20, Opus 4.7 subagent with WebFetch against Cloudflare primary docs) replaced the unverified "~2,500 writes/sec within Durable Object limits" framing. Cloudflare publishes a **1,000 requests/sec per-DO soft cap** that returns `overloaded` errors ([DO limits](https://developers.cloudflare.com/durable-objects/platform/limits/)) and explicitly states DOs "can act as WebSocket servers that connect thousands of clients per instance" with **no published concurrent-connection cap per DO** ([DO WebSockets best practices](https://developers.cloudflare.com/durable-objects/best-practices/websockets/)). CF's "Rules of Durable Objects" guidance pegs practical throughput at 500–1,000 rps for simple ops and 200–500 rps for complex ops ([DO rules](https://developers.cloudflare.com/durable-objects/best-practices/rules-of-durable-objects/)). The 25-connections-per-data-DO target is now framed as _our design choice_ derived from the envelope `25 conns × 100 events/sec ÷ ~6:1 batching ratio ≈ 400 rps/DO` with ~2.5× headroom vs the 1,000 rps soft cap; the un-batched envelope would be ~2,500 rps/DO (breaching the soft cap), so **batched WebSocket messages are a design baseline** enabled by the 2025-10-31 raise of WebSocket message size from 1 MiB to 32 MiB. Five decision triggers for re-tuning the shard factor are documented, including an emergency-reduction trigger if batching regresses. `vision.md` line 417 summary updated to align. The 100 events/sec/connection load-model assumption and ~6:1 batching ratio are marked as internal assumptions and gated on the 50-participant pre-launch load test.
- Resolved: 2026-04-20 (Session G2).

#### BL-069: Specify local-only → shared session reconciliation

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [session-model.md §Local-Only Reconciliation](../domain/session-model.md), [shared-postgres-schema.md §Sessions and Membership](../architecture/schemas/shared-postgres-schema.md)
- Summary: Define reconnect semantics: (a) session_id is preserved (daemon-assigned ULID/UUID used as-is); (b) Postgres `sessions` row created on first reconnect with `state='provisioning' → 'active'`; (c) owner identity comes from the first PASETO authentication at reconnect.
- Exit Criteria: session-model.md has new §Local-Only Reconciliation ✅; shared-postgres-schema.md documents the id-preservation rule ✅.
- Resolution: Session G2 research (2026-04-20, Opus 4.7 subagent with WebSearch against primary sources) landed the reconciliation invariants in `domain/session-model.md §Local-Only Reconciliation` and the adjacent `shared-postgres-schema.md` BL-069 invariant note. Core invariants: (1) Session IDs are daemon-assigned UUID v7 per [RFC 9562](https://www.rfc-editor.org/rfc/rfc9562.html) (Proposed Standard, May 2024) — lexicographically sortable by creation timestamp, so sessions remain orderable when reconciliation is delayed; Postgres 18 exposes native `uuidv7()` and `uuid_extract_timestamp()` for reverse-validation; (2) the daemon generates the session ID for daemon-originated sessions (the `gen_random_uuid()` schema default only applies to rare control-plane-originated admin-provisioned rows, not the normal production path); (3) first reconciliation executes `provisioning → active` via idempotent upsert `ON CONFLICT (id) DO UPDATE SET updated_at = sessions.updated_at RETURNING *` — the DO UPDATE clause (not DO NOTHING) guarantees `RETURNING *` yields a row on every attempt so the daemon distinguishes retry-after-crash from silent write loss; (4) owner identity is bound at the first authenticated RPC via PASETO v4 trust-on-first-use — the TOFU-seeding rule itself is the BL-069 invariant; [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md) defines only the underlying token format and issuance flows; (5) reconciliation is never destructive. State-machine precedent cited: Kubernetes Pod (`Pending → Running`) and Amazon ECS (`PROVISIONING → PENDING → ACTIVATING → RUNNING`). Schema SQL comment block and domain doc are internally consistent on the `gen_random_uuid()` admin-provisioning carve-out.
- Resolved: 2026-04-20 (Session G2).

#### BL-070: Add refresh-token revoke-all-for-participant endpoint

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [security-architecture.md §Bulk Revoke All For Participant (BL-070)](../architecture/security-architecture.md), [api-payload-contracts.md](../architecture/contracts/api-payload-contracts.md), [shared-postgres-schema.md §Token Revocation (BL-070 — Auth Infrastructure)](../architecture/schemas/shared-postgres-schema.md)
- Summary: Add `POST /auth/revoke-all-for-participant` for account-compromise recovery. Specify that refresh-token family-revocation list lives in Postgres and syncs across regions for multi-region self-hosted.
- Exit Criteria: security-architecture.md has the endpoint spec ✅; api-payload-contracts.md includes the payload ✅.
- Resolution: Session G2 research (2026-04-20, Opus 4.7 subagent with WebFetch against 4 IdP vendor docs + OWASP/NIST/GDPR primary sources) landed the endpoint spec across three files. `shared-postgres-schema.md` adds §Token Revocation (BL-070 — Auth Infrastructure) with two tables (`revoked_jtis`, `revoked_token_families`) owned by BL-070 (not Plan-018). `security-architecture.md` replaces the single-bullet §Token revocation with a structured 4-bullet section and adds §Bulk Revoke All For Participant (BL-070) defining: endpoint `POST /auth/revoke-all-for-participant`, auth (admin `admin:participants:revoke` OR self + step-up reauth — the 5-minute step-up is a BL-070 tightening of the [NIST SP 800-63B §4.2.3](https://pages.nist.gov/800-63-3/sp800-63b.html#aal2) 12-hour AAL2 cadence baseline, not a NIST mandate), request body with `reason` enum (`account_compromise | password_reset | admin_action | self_service`), response `204 No Content`, side effects including emission of the existing `participant.tokens_revoked_all` event per [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md) line 410 (payload `base + {revokedAt, tokenCount}`), multi-region Postgres logical-replication propagation modeled on Aurora Global Database sub-second cross-region replication, and regulatory mapping to [OWASP ASVS 5.0 V7.4.5](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/en/0x16-V7-Session-Management.md) admin-terminate-all-sessions + [GDPR Article 32(1)(c)](https://gdpr-info.eu/art-32-gdpr/) timely-restoration-after-incident. IdP precedent (Auth0 `DELETE /api/v2/users/{id}/refresh-tokens`, Okta `DELETE /api/v1/users/{uid}/sessions?oauthTokens=true`, Keycloak `POST /admin/realms/{realm}/users/{id}/logout`, Amazon Cognito `AdminUserGlobalSignOut`) surveyed 2026-04-20 — all four treat bulk per-user revocation as a vendor extension beyond [RFC 7009](https://www.rfc-editor.org/rfc/rfc7009) per-token scope; BL-070 follows this industry precedent. `api-payload-contracts.md` adds `RevokeAllTokensForParticipantRequest` TypeScript interface in the Plan-018 block with cross-references to security-architecture.md for semantics.
- Resolved: 2026-04-20 (Session G2).

#### BL-071: V1/V1.1/V2 annotations on vision.md Add table

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [vision.md §Add table](../vision.md)
- Summary (original charter — superseded by Session G3 Resolution below): "Add a V1 / V1.1 / V2 column to every row of the Add table. Resolves ambiguity: Cedar WASM runtime = V1.1; YAML compile = V1; ts-mls = V1.1+ (post-audit, per BL-048); **WebAuthn = V2 (desktop launch)**; push notifications transport = V1.1; notifications model = V1." The "WebAuthn = V2" hint in this charter was stale — ADR-015 Feature 15 places Desktop GUI in V1, making WebAuthn V1 (desktop) per ADR-010. The Resolution below is authoritative.
- Exit Criteria: Every row has an explicit V1/V1.1/V2 column value consistent with ADR-015 and ADR-010 ✅.
- Resolution: Session G3 (2026-04-20) added a structured `V1/V1.1/V2` column to the vision.md §Add table (all 16 rows) plus a column-rules preamble citing [ADR-015](../decisions/015-v1-feature-scope-definition.md) as the authority. Classifications verified against ADR-015 V1 feature list (16 features) and V1.1 deferrals (4 features: MLS relay E2EE, email invite, cross-node shared artifacts, workflow authoring). Two corrections vs original charter: **(1) WebAuthn is V1 (desktop), not V2** — ADR-015 Feature 15 places Desktop GUI in V1, and ADR-010 Consequences §91 confirms "CLI ships without WebAuthn dependency; passkeys added when desktop client launches." The CLI uses Device Authorization Grant (RFC 8628) without WebAuthn; the desktop client adds WebAuthn PRF ceremony for Ed25519 identity key derivation per ADR-010 §144. **(2) Push notifications is V2, not V1.1** — ADR-015 enumerates only 4 V1.1 deferrals (MLS, email invite, cross-node shared artifacts, workflow authoring) and does not include push notifications. [Spec-019 §Cross-Device Delivery](../specs/019-notifications-and-attention-model.md) line 93 explicitly classifies push notifications via FCM/APNs as **V2 (deferred)**. Per ADR-015 line 67, features not listed in V1 or V1.1 default to V2. Session G3 Opus 4.7 review caught this during Check 1 sourcing; the initial classification propagated the original BL charter's "push notifications transport = V1.1" hint uncritically. Cedar WASM confirmed V1.1 per [Spec-012 §Implementation Notes](../specs/012-approvals-permissions-and-trust-boundaries.md) ("V1 uses YAML policy definitions. V1.1 evaluates Cedar WASM runtime"). Redundant inline "(V1.1)" annotations on Cedar and Push notifications Purpose cells were removed (the column now carries that information). Inline "(V1 primary)" / "(V1.1+ upgrade)" annotations on the two Relay E2EE rows were retained because they convey role relationships (two technologies for the same role at different versions) that a single column value cannot capture.
- Resolved: 2026-04-20 (Session G3).

#### BL-072: Pin React 19 in vision

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [vision.md:265](../vision.md)
- Summary: Replace "React for the renderer" with "React 19 for the renderer" to pin the major version.
- Exit Criteria: vision.md names React 19 ✅.
- Resolution: vision.md line 265 now pins React 19 with the tilde-patch pin `~19.2.5`. React 19.2.5 is the current stable release as of 2026-04-19 per [react.dev](https://react.dev/versions) (19.2 line released 2025-10-01; 19.2.5 is latest patch). The tilde pin takes patch-level security updates automatically while requiring explicit review for minor bumps, matching ADR-010's security-conscious dependency stance. Session G1 Opus 4.7 research confirmed compatibility with Electron 41.2.1 (Chromium 146) and `@vitejs/plugin-react` 6.0.1. React Server Components remain out of scope for the Vite+Electron renderer.
- Resolved: 2026-04-19 (Session G1).

#### BL-073: Clarify Agent Trace as emitted-spec, not imported library

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [vision.md §Add table](../vision.md)
- Summary (original charter — superseded by Session G3 Resolution below): "Change the vision Add table entry for Agent Trace to note '(spec, no npm library yet; we emit trace records against the Cursor-authored RFC pinned at revision X).'" The "no npm library yet" framing in this charter was partially wrong — an unofficial community npm package `agent-trace` (unrelated to Cursor) exists and must be explicitly disambiguated. The Resolution below is authoritative.
- Exit Criteria: vision.md entry distinguishes emitted-spec from imported-library ✅.
- Resolution: Session G3 (2026-04-20) research (Opus 4.7 subagent with WebFetch + `gh api` against Cursor's GitHub organization + agent-trace.dev + npm registry) verified the Cursor-authored Agent Trace RFC and corrected the charter's stale "no npm library yet" framing. Authoritative findings: (1) RFC exists at [cursor/agent-trace](https://github.com/cursor/agent-trace), authored by Cursor (Anysphere), published 2026-01-27 with partner backing from Cognition, Cloudflare, Vercel, Google Jules, Amp, OpenCode, and git-ai. Canonical spec site: [agent-trace.dev](https://agent-trace.dev). Spec version 0.1.0 draft. (2) **No git tags or GitHub releases exist** (verified via `gh api repos/cursor/agent-trace/releases` returning `[]`) — pinning must be by commit SHA, not version tag. Latest `main` commit as of 2026-04-19: [`2754f077`](https://github.com/cursor/agent-trace/tree/2754f077f3e50c1fb5088183f5c9362077cc8ca1) ("Update abstract," Lee Robinson / Vercel, 2026-02-06); repo quiescent since. (3) **No Cursor-authored npm package exists** — the reference implementation lives in-tree at `cursor/agent-trace/index.ts` + `schemas.ts`. (4) **A confusingly-named unofficial community npm package `agent-trace` (by `attharrva15`, [github.com/Atharva-Kanherkar/agent-trace](https://github.com/Atharva-Kanherkar/agent-trace)) exists but is unrelated to Cursor and must not be used** — this disambiguation is now explicit in vision.md to prevent future AI agents or contributors from installing the wrong package. vision.md row now pins the commit SHA, cites the in-tree reference, and carries the explicit "must not be used" warning for the unofficial package. Cross-linked to [Spec-011](../specs/011-gitflow-pr-and-diff-attribution.md) (gitflow PR and diff attribution), which is the consumer of these records as a cross-cutting V1 spec (not one of the 16 numbered ADR-015 V1 Features; ADR-015 Feature 11 is "Local daemon with CLI" per Spec-007). Sources: [cursor/agent-trace repo](https://github.com/cursor/agent-trace), [agent-trace.dev](https://agent-trace.dev), [Cognition announcement 2026-01-29](https://cognition.ai/blog/agent-trace), [InfoQ Feb 2026 coverage](https://www.infoq.com/news/2026/02/agent-trace-cursor/), [unofficial npm package page](https://www.npmjs.com/package/agent-trace).
- Resolved: 2026-04-20 (Session G3).

#### BL-074: Fill or remove ADR-013 reserved stub

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [ADR-013](../decisions/013-reserved.md)
- Summary: Either fill ADR-013 with a real decision or delete the placeholder. 161-byte reserved stubs are documentation debt.
- Exit Criteria: ADR-013 is either a substantive ADR or removed.
- Resolution: Neither. Git log for `docs/decisions/013-reserved.md` shows a single creation commit (`d186470`, 2026-04-15, adversarial-review cleanup, co-authored by Claude Opus 4.6) with no prior topic. Fabricating a decision to fill the slot would be dishonest; deleting the file would require renumbering ADRs 014–021 (cited across 296 occurrences in 58 files per Session G1 review) for zero decision-quality benefit. Chosen path: formally document the slot as `reserved-skipped` with an explicit record of why the number was left empty and why renumbering is off the table. ADR-013 is now a formal authoritative record of a non-decision that a future AI agent can read unambiguously.
- Resolved: 2026-04-19 (Session G1).

#### BL-075: Rename "Open Questions" sections to reflect V1 decisions

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: `docs/specs/*.md` with "Open Questions" sections
- Summary: Rename "Open Questions" sections that actually contain `V1 decision:` statements to "Resolved Questions and V1 Scope Decisions" (or move decisions to ADRs). Readers currently can't tell what's actually open.
- Exit Criteria: Every spec "Open Questions" section genuinely contains open questions; resolved items are relocated or renamed ✅.
- Resolution: Session G3 (2026-04-20) Opus 4.7 triage subagent classified all 28 spec files with `## Open Questions` sections into three buckets and applied renames per the triage. **Bucket A "fully resolved" (22 specs renamed to `## Resolved Questions and V1 Scope Decisions`):** Specs 001, 002, 003, 004, 005, 006, 007, 008, 009, 010, 011, 012, 013, 014, 015, 016, 017, 018, 019, 020, 021, 022. **Bucket B "truly open" (5 specs retain `## Open Questions` header — items are genuinely unresolved):** Spec-023 (8 items awaiting follow-up — crash-reporter sink, Azure Artifact Signing regional eligibility, WebAuthn native-module lock-in, Win10 V1 support horizon, MSIX distribution, reproducible builds, Snap/Flatpak, WebAuthn PRF fallback UX); Spec-024 (2 V1.1 forward-looking items — ApprovalRecord delegation third signer, composition with V1.1 MLS); Spec-025 (3 "Lean:" tentative items — Grafana dashboard shipping, PASETO key-rotation-window configurability, GHCR publication); Spec-026 (4 "Tentative:" items — Linux keystore degradation UX, hosted signup URL, telemetry opt-in copy, hashed `machine_id` in onboarding event); Spec-027 (5 items explicitly deferred to other plans/BLs — Behavior 7a polling cadence, Behavior 6 plug-in point, Behavior 9 `/metrics` auth on non-loopback, LE profile opt-in, OAuth auth method on PG 18). **Bucket 0 "template" (1 file, no rename):** Spec-000 (template — keeps generic header so downstream specs copy the canonical convention). Zero specs fell into Bucket C "mixed" — the pattern was bimodal. Spec-022 edge case (`Post-V1:` bullet, not `V1 decision:`) treated as Bucket A because the deferral is closed-for-V1. Readers can now unambiguously distinguish sections that require further decisions from sections that record closed decisions.
- Resolved: 2026-04-20 (Session G3).

#### BL-076: Add decision trigger on 256MB daemon memory budget

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [deployment-topology.md §Local Daemon Memory Instrumentation And Budget Triggers](../architecture/deployment-topology.md)
- Summary: Add instrumentation requirement (RSS metric with 80%-of-budget alert) and a decision trigger: "if real workloads consistently breach 256 MB, raise budget to 384–512 MB before considering a runtime change."
- Exit Criteria: deployment-topology.md states both the instrumentation requirement and the decision trigger ✅.
- Resolution: Session G3 (2026-04-20) added a new `### Local Daemon Memory Instrumentation And Budget Triggers` subsection under [deployment-topology.md §Infrastructure Requirements](../architecture/deployment-topology.md) stating: **(1) Instrumentation requirement** — daemon MUST expose `process_resident_memory_bytes` via the default Prometheus [prom-client](https://github.com/siimon/prom-client#default-metrics) collector, with explicit rationale for preferring RSS over V8 heap-used (RSS captures native allocations from SQLite page cache, `node-pty` file descriptors, and `@noble/*` cryptographic buffers that heap-used excludes). **(2) Alert threshold** — RSS exceeds 80% of budget (≥ 205 MB) sustained for ≥ 5 minutes; "sustained" framed as reducing false positives from transient build-step allocations. 80% threshold and 5-minute window flagged as design choices, not external standards. **(3) Decision trigger** (verbatim from BL charter) — "If real workloads consistently breach the 256 MB budget, raise the budget to 384–512 MB before considering a runtime change." Rationale captured: budget raise is reversible and low-blast-radius (documentation + alert-threshold update); runtime change is high-cost and reserved for breaches that persist after a budget raise. "Consistently breach" defined as ≥ 20% of operating daemons over a rolling 7-day window exceeding 256 MB; thresholds explicitly marked as internal and subject to revision once real deployment telemetry is available.
- Resolved: 2026-04-20 (Session G3).

#### BL-077: Promote plan status from `review` to `approved`

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Depends-on: BL-038 through BL-055 (all P0 items — completed). Remaining approval blockers: BL-089, BL-090, BL-091, BL-092 (rework of Plans 001, 003, 006, 020 respectively).
- Blocked-by: BL-089, BL-090, BL-091, BL-092
- References: `docs/plans/*.md` headers
- Summary: Session F (2026-04-19) ran the formal approval pass against the full V1 plan set per ADR-015. V1 scope is **25 plans** (plan numbers 001-026 minus Plan-017, which is deferred to V1.1+ per ADR-015). Pass classified each plan as FLIP (zero drift), EDIT-IN-PLACE (≤2 header fixes), or NEEDS-FOLLOWUP-BL (structural drift or ≥3 edits). Landed state after Session F: **21 of 25 plans `approved`** — specifically Plans 002, 004, 005, 007, 008, 009, 010, 011, 012, 013, 014, 015, 016, 018, 019 were flipped `review` → `approved` in Session F (15 plans; 8 of these received header EDIT-IN-PLACE fixes adding missing ADR citations or dependency edges); Plans 021, 023, 024 were already `approved` before Session F (landed under BL-055); Plans 022, 025, 026 were already `approved` but carry content drift tracked separately in BL-093, BL-094, BL-095 (amendment work, not approval-gating). **4 plans remain in `review`** pending rework per BL-089, BL-090, BL-091, BL-092 (Plans 001, 003, 006, 020 respectively). The pass verified each flipped plan cites ADR-015, matches the tier graph in `cross-plan-dependencies.md`, and carries no residual contradictions with P0 outputs (BL-038 through BL-055).
- Exit Criteria: All 25 V1 plan headers read `Status: approved` (Plan-017 excluded per ADR-015); no plan header carries a `Blocked-by` pointer to BL-038 through BL-055; BL-089, BL-090, BL-091, BL-092 completed ✅.
- Resolution: All four blocking reworks closed across Sessions I1 and I2. **Session I1 (2026-04-20)** landed BL-089 (Plan-001 forward-declared schema), BL-090 (Plan-003 `runtime_node_lifecycle` emission + attach-flow floor check), and BL-092 (Plan-020 §PII in Diagnostics surface). **Session I2 (2026-04-20)** landed BL-091 (Plan-006 full rewrite — 120 events / 18 categories + integrity protocol + PII columns + shred fan-out cross-refs + audit integrity invariant widened to cover `event_maintenance` + ADR-017 Option B local-logs confirmation + 4 reviewer blockers applied in-flight). **Final landed state**: 25 of 25 V1 plans read `Status: approved` (Plan-017 correctly excluded per ADR-015 V1.1+ deferral). No V1 plan header carries a `Blocked-by` pointer to BL-038 through BL-055. BL-093 / BL-094 / BL-095 remain tracked separately as amendment work against already-`approved` plans (Session J) and do not gate BL-077 exit per the Phase 8 preamble (amendments to approved plans cannot block the approval gate). The approval-gate contract now allows downstream implementation work to proceed against Plans 001-026 without additional plan-status unblocking.
- Resolved: 2026-04-20 (Session I2).

### Phase 8 — Plan-Approval-Pass Follow-ups (P1/P2 — landed by Session F approval pass on 2026-04-19)

Follow-up work identified during the Session F plan approval pass (BL-077). BL-089 through BL-092 block BL-077 exit criteria; BL-093 through BL-095 are amendment work against already-`approved` plans that cannot block the approval gate but remain required for plan-body accuracy before implementation.

#### BL-089: Rework Plan-001 with forward-declared PII schema + integrity columns + min_client_version

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Depends-on: BL-050 (audit log integrity protocol), BL-065 (ADR-018 cross-version compatibility), BL-066 (PII data map + shred fan-out)
- Blocks: BL-077
- References: [Plan-001](../plans/001-shared-session-core.md), [Spec-001](../specs/001-shared-session-core.md), [Spec-006 §Integrity Protocol](../specs/006-session-event-taxonomy-and-audit-log.md), [Spec-022 §PII Data Map](../specs/022-data-retention-and-gdpr.md), [ADR-018](../decisions/018-cross-version-compatibility.md), [cross-plan-dependencies.md §1 Contested rows](../architecture/cross-plan-dependencies.md)
- Summary (original charter — superseded by Session I1 Resolution below): Plan-001 is the tier-entry plan for shared session core and owns the `0001-initial.sql` migration in `packages/runtime-daemon/src/persistence/migrations/`. After the post-P0 spec cascade (BL-050 integrity protocol, BL-064 event taxonomy extension, BL-065 ADR-018, BL-066 PII data map), Plan-001 must forward-declare schema shape that downstream plans assume is already present at tier entry. The charter's proposed `participant_keys` column shape (`participant_id` PK, `session_id`, `key_material` BLOB, `created_at`, `destroyed_at NULLABLE`) and its ADR-018 §Decision citation for the `min_client_version` floor were both inaccurate against the authoritative specs — corrected in the Resolution below. The Resolution is authoritative.
- Exit Criteria: Plan-001 `Required ADRs` row includes ADR-018; §Data And Storage Changes enumerates the 4 forward-declared schema elements above; §Cross-Plan Forward-Declared Schema section exists naming the downstream owners; Plan-001 status header reads `approved`; no retrospective/amendment phrasing ✅.
- Resolution: Session I1 (2026-04-20) landed the Plan-001 rework plus the Postgres schema-doc addition the rework surfaced as missing. **(1) Plan-001 header** — Required ADRs row extended with ADR-018 (now ADR-001, ADR-002, ADR-004, ADR-015, ADR-018); Dependencies row rewritten to note tier-entry ownership of `0001-initial.sql` and forward-declared-schema consumers Plan-003, Plan-006, Plan-022. **(2) §Data And Storage Changes** expanded to enumerate the four forward-declared schema elements with canonical citations: `sessions.min_client_version TEXT` (NULL = no floor) per [ADR-018 §Decision #1 + §Decision #3](../decisions/018-cross-version-compatibility.md), control plane authoritative per [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md); `session_events.pii_payload BLOB` NULLable per [Spec-022 §PII Data Map](../specs/022-data-retention-and-gdpr.md); integrity-protocol columns `monotonic_ns / prev_hash / row_hash / daemon_signature / participant_signature` per [Spec-006 §Integrity Protocol](../specs/006-session-event-taxonomy-and-audit-log.md) (BLAKE3 + Ed25519 + RFC 8785 JCS); `participant_keys` table per [Spec-022 §Participant Keys](../specs/022-data-retention-and-gdpr.md). **(3) New §Cross-Plan Forward-Declared Schema** table assigns semantics ownership: pii_payload → Plan-022; integrity columns → Plan-006; participant_keys → Plan-022; min_client_version → Plan-003. Includes the §Signature Safety Under Shred hook (canonical bytes exclude pii_payload but embed a `pii_ciphertext_digest` BLAKE3 one-way digest over ciphertext, so Ed25519 signatures remain verifiable after crypto-shred per Spec-022). **(4) Two charter-vs-spec corrections.** (a) Column-shape correction: the BL-089 charter proposed `participant_keys (participant_id PK, session_id, key_material BLOB, created_at, destroyed_at NULLABLE)`. Spec-022 lines 122–132 is authoritative and defines the table as participant-scoped (no session_id), with `encrypted_key_blob` (not `key_material`), `key_version`, `created_at`, `rotated_at` (no `destroyed_at` — deletion IS row DELETE, which crypto-shreds). Plan-001 rewrite mirrors the Spec-022 shape exactly. (b) ADR-018 decision-number correction: the BL-089 charter cited "`min_client_version` per ADR-018 §Decision"; §Decision #1 covers only the semver string format, while §Decision #3 is the monotonic-raise floor mechanism. Plan-001 now cites both: §Decision #1 (format) and §Decision #3 (mechanism). **(5) Schema-doc addition.** The Plan-001 rewrite's `sessions.min_client_version` reference would have been dangling — the column was not present in [shared-postgres-schema.md §Sessions and Membership](../architecture/schemas/shared-postgres-schema.md). Added `min_client_version TEXT` to the `sessions` DDL with ADR-018 §Decision #1 + §Decision #3 + §Decision #4 citations, control-plane-authoritative note per ADR-004, and enforcement-ownership pointer to Plan-003 (BL-090). **(6) Upstream spec typo fix.** Reviewer pass surfaced that [Spec-006:377](../specs/006-session-event-taxonomy-and-audit-log.md) cited "ADR-018 §Decision #3" for the name-preservation (no-rename) rule; the correct citation is §Decision #8 (MINOR bumps are additive-only; renaming event types is forbidden). Corrected in the same commit — the rationale text ("MINOR bumps are additive-only; event-type rename is not additive") already matched Decision #8, only the number was wrong. **(7) Plan-001 status** flipped from `review` to `approved`. One combined Opus 4.7 reviewer pass over the Plan-001 + Plan-003 + Plan-020 + shared-postgres-schema.md diff caught the four citation blockers listed above; all corrections landed in-flight.
- Resolved: 2026-04-20 (Session I1).

#### BL-090: Rework Plan-003 (Runtime Node Attach) with ADR citations + runtime_node_lifecycle event emission surface

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Depends-on: BL-064 (event taxonomy extension — runtime_node_lifecycle category), BL-065 (ADR-018 cross-version compatibility)
- Blocks: BL-077
- References: [Plan-003](../plans/003-runtime-node-attach.md), [Spec-003](../specs/003-runtime-node-attach.md), [Spec-006 §Runtime Node Lifecycle](../specs/006-session-event-taxonomy-and-audit-log.md), [ADR-001](../decisions/001-session-is-the-primary-domain-object.md), [ADR-007](../decisions/007-collaboration-trust-and-permission-model.md), [ADR-018](../decisions/018-cross-version-compatibility.md)
- Summary (original charter — superseded by Session I1 Resolution below): Plan-003 governs participant-owned RuntimeNode registration, attach/detach, capability declaration, and presence heartbeat. Plan-003 under-cited governing ADRs (ADR-001, ADR-007, ADR-018) and lacked explicit attribution of the `runtime_node_lifecycle` event category emission surface. The charter's framing of Plan-003 as "canonical emitter of the 9-event category" was partially inaccurate — 2 of the 9 events (`session.clock_*`) are emitted by Plan-015, not Plan-003 — corrected in the Resolution below. The Resolution is authoritative.
- Exit Criteria: Plan-003 `Required ADRs` row includes ADR-001, ADR-007, ADR-018 (alongside existing ADR-002, ADR-008, ADR-015); plan body names the `runtime_node_lifecycle` category with a Spec-006 cross-reference and identifies Plan-003 as the canonical emitter; attach flow references `min_client_version` + `VERSION_FLOOR_EXCEEDED`; Plan-003 status header reads `approved`; no retrospective/amendment phrasing ✅.
- Resolution: Session I1 (2026-04-20) landed the Plan-003 rework. **(1) Required ADRs row extended** with ADR-001 (session-centric invariants bind attach), ADR-007 (trust classification materializes into the plan's local `node_trust_state`), ADR-018 (capability declarations carry version info; `min_client_version` gates attach eligibility) — now ADR-001, ADR-002, ADR-007, ADR-008, ADR-015, ADR-018. **(2) New §Event Emission subsection** added between §API And Transport Changes and §Implementation Steps. Attributes the 7 `runtime_node.*` events (`registered`, `online`, `degraded`, `offline`, `revoked`, `capability_declared`, `capability_updated`) to Plan-003 as canonical emitter, with cross-reference to [Spec-006 §Runtime Node Lifecycle](../specs/006-session-event-taxonomy-and-audit-log.md). **(3) Emission-attribution correction vs charter.** The BL-090 charter framed Plan-003 as the canonical emitter of the 9-event category. Spec-006 lines 374–375 is authoritative: the 2 `session.clock_*` events describe daemon-host clock state observed at the NTP sync probe owned by [Plan-015 §NTP Sync Precondition](../specs/015-persistence-recovery-and-replay.md), not by Plan-003's attach flow. The rewrite splits attribution precisely: Plan-003 emits the 7 `runtime_node.*` events; Plan-015 emits the 2 `session.clock_*` events. These 2 events preserve their `session.*` wire names (category reclassification only, not rename) per [ADR-018 §Decision #8](../decisions/018-cross-version-compatibility.md) (MINOR envelope bumps are additive-only; renaming event types is explicitly forbidden) — category moves are safe because the `category` field is a classification facet, not a wire-identity key; readers dispatch on event name. **(4) ADR-018 decision-number correction.** Both the charter and an upstream [Spec-006:377](../specs/006-session-event-taxonomy-and-audit-log.md) passage cited §Decision #3 for the name-preservation rule; §Decision #3 is the monotonic session-floor mechanism, while the additive-only / no-rename rule is §Decision #8. Spec-006:377 corrected in the same commit as a typo fix — the rationale text already matched §Decision #8, only the number was wrong. Plan-003 cites §Decision #8 correctly. **(5) Attach-flow floor check.** §API And Transport Changes and §Implementation Steps step 3 now require the control-plane attach handler to read `sessions.min_client_version` and reject below-floor daemons with typed `VERSION_FLOOR_EXCEEDED` per [ADR-018 §Decision #4](../decisions/018-cross-version-compatibility.md). Rejection preserves graceful-degradation semantics (read-only state), not ejection. **(6) Plan-003 status** flipped from `review` to `approved`.
- Resolved: 2026-04-20 (Session I1).

#### BL-091: Rewrite Plan-006 for 120 events / 18 categories + integrity protocol + PII columns

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Depends-on: BL-046 (ADR-017 shared event-sourcing scope), BL-050 (audit log integrity protocol), BL-053 (ADR-020 V1 deployment model + OSS license), BL-064 (event taxonomy extension 93 → 120), BL-065 (ADR-018 cross-version compatibility), BL-066 (PII data map + shred fan-out)
- Blocks: BL-077
- References: [Plan-006](../plans/006-session-event-taxonomy-and-audit-log.md), [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md), [Spec-022 §Shred Fan-Out](../specs/022-data-retention-and-gdpr.md), [ADR-012](../decisions/012-cedar-approval-policy-engine.md), [ADR-017](../decisions/017-shared-event-sourcing-scope.md), [ADR-018](../decisions/018-cross-version-compatibility.md), [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md)
- Summary (original charter — superseded by Session I2 Resolution below): Plan-006 is the plan most heavily impacted by the post-P0 spec cascade and requires rewrite rather than amendment. Charter proposed 120 events across "15 categories"; the correct V1 category count is 18 (BL-084/086/087 landed `channel_arbitration`, `cross_node_dispatch`, `onboarding_lifecycle` after BL-091 charter was written), and the §Audit Integrity Invariant carries symmetrically onto `event_maintenance` not just `audit_integrity` — both corrected in the Resolution below. The Resolution is authoritative.
- Exit Criteria: Plan-006 header reflects 120 events / 18 categories; §Integrity Protocol, §PII Columns, §Shred Fan-Out Cross-References, §Audit Integrity Invariant subsections exist; `Required ADRs` row includes ADR-012, ADR-017, ADR-018, ADR-020; Plan-006 status header reads `approved`; no retrospective/amendment phrasing ✅.
- Resolution: Session I2 (2026-04-20) landed the Plan-006 full rewrite. **(1) Header rework.** Status flipped `review` → `approved`; `Required ADRs` row extended to [ADR-001, ADR-004, ADR-012, ADR-015, ADR-017, ADR-018, ADR-020] (charter named the 4 required additions ADR-012/017/018/020; the rewrite also includes ADR-001 for session-domain invariants and ADR-004 for the control-plane-authoritative split on `event_log_anchors`); Dependencies row rewritten to note Plan-006 owns the read/write semantics for session_events integrity columns + `pii_payload` + `participant_keys` forward-declared by Plan-001 per Plan-001 §Cross-Plan Forward-Declared Schema (BL-089). **(2) Charter-vs-spec category-count correction.** BL-091 charter said "9 → 15 categories." Spec-006 §Event Type Summary is authoritative at **18 unique categories** (BL-084 added `channel_arbitration`; BL-087 added `cross_node_dispatch`; BL-086 added `onboarding_lifecycle` — all three landed in Session C after BL-091 was written). Plan-006 header, §Event Taxonomy Coverage table, and §Scope now reflect the correct 18. **(3) §Event Taxonomy Coverage** table enumerates all 18 categories with emitter-plan attribution (Plan-001 for `session.*`, Plan-016 for channel/agent, Plan-009 + Plan-010 for repo/workspace/worktree, Plan-002 for membership_change, Plan-016 for `channel_arbitration`, Plan-004 for `run_lifecycle` + `interactive_request`, Plan-012 for `approval_flow`, Plan-014 for `artifact_publication`, Plan-005 for `assistant_output` + `tool_activity` + `usage_telemetry`, Spec-024 impl for `cross_node_dispatch`, Plan-026 for `onboarding_lifecycle`, Plan-003 + Plan-015 for `runtime_node_lifecycle`, Plan-015 for `recovery_events`, Plan-022 for `participant_lifecycle`, **Plan-006** for `audit_integrity` + `event_maintenance`, V1.1 Cedar bundle loader for `policy_events`). Plan-006's emission scope is explicitly bounded to the 6 events in the two bold categories. **(4) §Integrity Protocol** subsection covers Canonical Serialization (RFC 8785 JCS; BLAKE3; Ed25519 RFC 8032 §5.1; 11 canonical fields in order; millisecond-precision `occurredAt`; `pii_payload` excluded, `pii_ciphertext_digest` embedded in `payload`), Hash Chain (genesis `prev_hash` = 32 zero bytes; `row_hash = BLAKE3(prev_hash || canonical_bytes)`), Ed25519 Signatures (daemon + optional participant), and Merkle Anchor Emission (earlier-of `ANCHOR_INTERVAL_EVENTS = 1000` OR `ANCHOR_INTERVAL_SECONDS = 300`; payload = metadata only — `(session_id, node_id, start_sequence, end_sequence, merkle_root, root_signature, anchored_at)`; uploads to Postgres `event_log_anchors` per ADR-017; partition-tolerant local queue on upload failure). **(5) §PII Columns** subsection codifies the 7-step Encrypt-Then-Digest-Then-Sign Order as LOAD-BEARING for Spec-022 §Signature Safety Under Shred (encrypt PII under participant AES-256-GCM → compute `pii_ciphertext_digest = BLAKE3(ciphertext)` → embed digest in `payload` → canonicalize envelope → hash chain → sign → persist). Sole write path is `pii-indirection.ts` to prevent order regressions. Read path covers three states: no PII (verbatim return), decryption key available (merge back), key absent post-shred (replace PII fields with `<pii-shredded>` marker — signature and hash verification remain valid because canonical bytes exclude `pii_payload`). **(6) §Shred Fan-Out Cross-References** subsection names Plan-006 as the emitter of `event.shredded` on Path 1 completion of Spec-022 §Shred Fan-Out; Plan-022 owns the three-path orchestration and `participant.purged` emission timing constraint (MUST NOT fire until all three paths complete). **(7) §Audit Integrity Invariant** subsection widened beyond the charter's `audit_integrity`-only scope to cover BOTH `audit_integrity` AND `event_maintenance` categories symmetrically — Spec-006 §Audit Integrity line 423 AND §Event Maintenance line 447 both carry the never-compacted / never-shredded invariant. Plan-006 enforces at three layers: `compactor.ts` excludes both categories; `pii-indirection.ts` refuses `pii_payload` writes for either category (content carries no participant PII); shred Path 1 SQL selector explicitly excludes both categories (defense-in-depth). **(8) §Cross-Version Compatibility Surface** subsection binds Plan-006 to ADR-018 with per-decision citations: §Decision #1 semver format, §Decision #2 producer-set, §Decision #5 version stubs, §Decision #6 upcaster chain on read, §Decision #8 MINOR additive-only (event-type renames forbidden), §Decision #9 accept-and-stub, §Decision #10 FLOOR/CEILING registration mandate (pre-Plan-001). **(9) V1 vs V1.1 `policy_events` emission boundary** made explicit: per ADR-012 §Decision, V1 compiles Cedar at build time; V1.1 adds WASM runtime bundle loading. The 2 `policy_bundle.*` types are registered in V1 taxonomy so the registry is complete from V1 forward, but their emitter ships in V1.1. **(10) ADR-017 Option B per-daemon local logs** made explicit in §Data And Storage Changes: "No `session_events_shared` table" — cross-participant events distributed as pairwise-encrypted payloads per ADR-010. **(11) 4 reviewer blockers applied in-flight.** One combined Opus 4.7 reviewer pass over the Plan-006 rewrite caught: (B1) `EventEnvelope` version declared as `"2.0"`; ADR-018 §Failure Mode Analysis names `"1.0"` as the production starting envelope — corrected. (B2) Line 71 cited immutability as "per §Decision #7" (which is about MAJOR breaking); correct chain is Spec-006 §EventEnvelope Version Semantics (Immutability) + ADR-018 §Decision #6 (upcaster chain on read, never log rewrite) — corrected. (B3) Line 210 cited "Unknown-MINOR preserved verbatim per §Decision #7"; correct is Spec-006 §EventEnvelope Version Semantics + ADR-018 §Decision #8 (MINOR additive-only) composed with §Decision #6 (upcaster chain on read) — corrected. (B4) Broken plan-file link `[Plan-016](../plans/016-multi-agent-channels-and-orchestration.md)` (Plan-008 is `008-control-plane-relay-and-session-join.md`; the multi-agent channels plan is Plan-016) — triggered a systematic audit that surfaced and corrected a cluster of similar drift: `./002-invite-and-membership.md` → `./002-invite-membership-and-presence.md`; nonexistent `./005-runs-and-agent-session-execution.md` → run_lifecycle actually owned by Plan-004 `./004-queue-steer-pause-resume.md`; `[Plan-004](../plans/004-queue-steer-pause-resume.md)` (Plan-011 is gitflow) → Plan-004 correct; `./014-artifact-publication.md` → `./014-artifacts-files-and-attachments.md`; bare `Plan-005` / `Plan-015` references linked to `./005-provider-driver-contract-and-capabilities.md` / `./015-persistence-recovery-and-replay.md`; session_lifecycle row expanded to name Plan-001 + Plan-016 + Plan-009 + Plan-010 co-emitters. Reviewer also verified 13 big-picture facts as correct (18-category total summing to 120, symmetric audit/shred invariant, Plan-006 6-event emission scope, V1/V1.1 policy_events boundary, 7-step PII write order matching Spec-022 §Signature Safety proof step-for-step, no `session_events_shared` table, earlier-of 1000/300 anchor cadence, BLAKE3/Ed25519/JCS crypto primitives, canonical field ordering, Plan-001 forward-declaration alignment, and all confirmed-correct ADR-018 decision-number citations).
- Resolved: 2026-04-20 (Session I2).

#### BL-092: Extend Plan-020 with §PII in Diagnostics coverage

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- Depends-on: BL-066 (PII data map — Spec-020 §PII in Diagnostics subsection landed in the same pass)
- Blocks: BL-077
- References: [Plan-020](../plans/020-observability-and-failure-recovery.md), [Spec-020 §PII in Diagnostics](../specs/020-observability-and-failure-recovery.md), [Spec-022 §PII Data Map](../specs/022-data-retention-and-gdpr.md)
- Summary: Plan-020 governs observability and failure recovery. Spec-020 §PII in Diagnostics (landed under BL-066) introduced a 5-point Required Behavior policy: (1) default-deny outbound telemetry, (2) opt-in for raw content, (3) bounded local retention ≤ 7-day default with `retention_policy_override` warning metric on > 30-day overrides, (4) shred fan-out coverage mandatory for all 4 diagnostic buckets, (5) summary-only retention from non-PII signals by construction. Plan-020 previously had no implementation surface for this policy.
- Exit Criteria: Plan-020 §Target Areas lists the 4 diagnostic buckets; §API And Transport Changes references `DiagnosticRedactionPolicy`; §Test And Verification Plan includes the 4 test cases above; cross-reference to Spec-020 §PII in Diagnostics and Spec-022 §PII Data Map present; Plan-020 status header reads `approved`; no retrospective/amendment phrasing ✅.
- Resolution: Session I1 (2026-04-20) landed the Plan-020 extension. **(1) §Target Areas** extended with `packages/runtime-daemon/src/observability/diagnostic-redaction-policy.ts` (PII redaction gate on all 4 diagnostic buckets) and `packages/runtime-daemon/src/observability/diagnostic-buckets/` (TTL-bucket implementations for `driver_raw_events`, `command_output`, `tool_traces`, `reasoning_detail` — all four buckets named verbatim). **(2) New §PII in Diagnostics subsection** added between §Target Areas and §Data And Storage Changes, naming Plan-020 as the implementation surface for [Spec-020 §PII in Diagnostics](../specs/020-observability-and-failure-recovery.md) honoring the [Spec-022 §PII Data Map](../specs/022-data-retention-and-gdpr.md) classification. Covers all four policy pillars: default TTL ≤ 7 days (operator overrides > 30 days emit `retention_policy_override` warning metric on each daemon startup and on each policy read); default-deny outbound (no diagnostic-bucket row content leaves the daemon unless per-bucket opt-in is explicit; outbound telemetry is summary-only by construction from non-PII inputs — counts, categories, latencies); opt-in toggles with operator acknowledgement audit; shred fan-out coverage via [Spec-022 §Shred Fan-Out](../specs/022-data-retention-and-gdpr.md) Path 3 (participant-purge request triggers bucket-row purge ahead of TTL). **(3) §Data And Storage Changes** extended with the 4 diagnostic-bucket tables as runtime-local SQLite only (no shared-Postgres counterpart per [ADR-017](../decisions/017-shared-event-sourcing-scope.md)); TTL-purge indices referenced. **(4) §API And Transport Changes** adds the `DiagnosticRedactionPolicy` contract surface (operator-readable current policy + per-bucket opt-in toggles + `retention_policy_override` warning surface) with default state `deny-outbound, ≤ 7-day TTL, no raw-content capture`. **(5) §Implementation Steps** step 4 now wires default-deny outbound telemetry for all 4 buckets + per-bucket opt-in raw-content capture with explicit operator acknowledgement + `retention_policy_override` warning emission when TTL override > 30 days. **(6) §Test And Verification Plan** extended with the four named test cases from the charter: outbound-telemetry-default-deny (no bucket content appears in any outbound payload unless per-bucket opt-in enabled), raw-content-opt-in-explicit-only (toggle requires operator acknowledgement and is audited; flipped toggle does NOT retroactively release previously-captured data), TTL-bucket-purge-coverage (each bucket expires at or before configured TTL; participant-purge triggers immediate purge ahead of TTL), `retention_policy_override` warning emission on every policy read when TTL > 30 days. **(7) Plan-020 status** flipped from `review` to `approved`.
- Resolved: 2026-04-20 (Session I1).

#### BL-093: Amend Plan-022 with PII Data Map + Shred Fan-Out + Signature Safety implementation surface

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Depends-on: BL-066 (PII data map — Spec-022 §PII Data Map, §Shred Fan-Out, §Signature Safety Under Shred landed in the same pass)
- References: [Plan-022](../plans/022-data-retention-and-gdpr.md), [Spec-022 §PII Data Map / §Shred Fan-Out / §Signature Safety Under Shred](../specs/022-data-retention-and-gdpr.md), [Spec-006 §Integrity Protocol](../specs/006-session-event-taxonomy-and-audit-log.md), [Spec-020 §PII in Diagnostics](../specs/020-observability-and-failure-recovery.md), [ADR-020](../decisions/020-v1-deployment-model-and-oss-license.md)
- Summary: Plan-022 is already at `Status: approved` but the plan body predates the BL-066 spec expansion (3-tier PII data map, 3-path shred fan-out, 5-point signature-safety proof). Amendment (not re-approval) required: (1) §Data And Storage Changes expanded to enumerate all three PII tiers (durable `session_events.pii_payload` + `participant_keys`; bounded-retention 4 diagnostic buckets; telemetry export with default-deny OTel semconv v1.36.0 stance); (2) §Implementation Steps section adds explicit ordered Path 1 → Path 2 → Path 3 fan-out sequence for `DELETE /participants/{id}/data` (SQLite crypto-shred via `participant_keys.key_material` destruction → Postgres hard DELETE of mirror row → bounded-retention TTL bucket purge across the 4 diagnostic buckets); (3) add §Signature Safety Under Shred subsection replicating the 5-point proof sketch (JCS canonical bytes exclude pii_payload; BLAKE3 preimage resistance on `pii_ciphertext_digest`; AES-256-GCM key destruction per NIST SP 800-38D §8.3; Ed25519 public-key verification per RFC 8032 §5.1; audit integrity load-bearing); (4) reconcile any wrap-cipher references with Spec-022 per-participant AES-256-GCM key choice; (5) confirm Plan-022 tier placement is consistent with cross-plan-dependencies.md. Rework magnitude: additive amendment to an already-approved plan; no status flip required.
- Exit Criteria: Plan-022 §Data And Storage Changes enumerates all three PII tiers ✅; §Implementation Steps sequences the three shred fan-out paths in order with rationale ✅; §Signature Safety Under Shred subsection exists ✅; Spec-022 §PII Data Map and §Shred Fan-Out cross-references resolve ✅; plan status remains `approved` ✅; no retrospective/amendment phrasing other than forward-declared references ✅.
- Resolution: Session J (2026-04-20) landed six Plan-022 amendments closing every exit clause. **(1) §PII Data Map (Three Durability Tiers)** subsection inserted at the top of §Data And Storage Changes, enumerating all three tiers with owner attribution: Tier 1 — Durable (`session_events.pii_payload` + `participant_keys` owned by Plan-022, forward-declared by Plan-001); Tier 2 — Bounded-retention diagnostic buckets (`driver_raw_events`, `command_output`, `tool_traces`, `reasoning_detail` all owned by Plan-020); Tier 3 — Telemetry export (Plan-020 default-deny outbound per Spec-020 §PII in Diagnostics honoring OTel semconv v1.36.0 stance). **(2) New §Signature Safety Under Shred top-level section** inserted between §API And Transport Changes and §Implementation Steps, replicating the Spec-022 5-point proof sketch: (a) canonical bytes exclude `pii_payload` and embed `pii_ciphertext_digest = BLAKE3(pii_payload_ciphertext)` per [Spec-006 §Canonical Serialization Rules](../specs/006-session-event-taxonomy-and-audit-log.md#canonical-serialization-rules) (which applies RFC 8785 JCS); (b) Ed25519 public-key signature verification per [RFC 8032 §5.1](https://datatracker.ietf.org/doc/html/rfc8032#section-5.1) remains valid because the signed canonical bytes are untouched by shred; (c) BLAKE3 preimage resistance makes the embedded digest one-way; (d) AES-256-GCM key destruction crypto-shreds the plaintext by construction (the per-participant key lives only in `participant_keys.encrypted_key_blob` wrapped under the daemon master key, so Path 1 DELETE destroys the content key irrecoverably — no NIST citation because the claim follows directly from cryptographic construction, not from AEAD mode spec); (e) 256-bit brute-force infeasibility per [NIST SP 800-38D](https://csrc.nist.gov/publications/detail/sp/800-38d/final) closes the last exfiltration path (AES-256-GCM provides no cryptanalytic shortcut allowing plaintext recovery without the key). **(3) §Implementation Step 7 expanded** to require `write-with-pii.ts` compute `pii_ciphertext_digest = BLAKE3(pii_payload_ciphertext)` and embed it in the event payload BEFORE the canonicalizer runs — the 7-step Encrypt-Then-Digest-Then-Sign order per [Plan-006 §PII Columns](../plans/006-session-event-taxonomy-and-audit-log.md) is now explicit in the plan body. **(4) §Implementation Step 11** added "Reserve Shred Fan-Out Orchestration surface (V1.1+)" sequencing Path 1 (SQLite crypto-shred via `participant_keys` DELETE) → Path 2 (Postgres hard DELETE of mirror row) → Path 3 (bounded-retention Plan-020 diagnostic-bucket scoped flush) with full ordering rationale (Path 1 first because it is atomic and cascade-free per Plan-001 migration design; Path 2 second because Plan-018's PG targets require an RPC path not a direct DELETE; Path 3 last because Plan-020's bucket purge is scoped per-participant and runs on a daemon-local TTL purger independent of the durable fan-out). V1 cross-plan alignment checkpoint confirms Plan-001 forward-declares `participant_keys` without a cascade barrier (Plan-001:54–55), Plan-018's PG targets are ready for hard DELETE (BL-092 landed that surface), Plan-020's 4 diagnostic buckets are named in its §Target Areas (Plan-020 Session I1 Resolution §1), and Plan-006 emits `event.shredded` category event on completion (Plan-006 §Event Taxonomy). **(5) §Test And Verification Plan** extended with `write-with-pii` digest-embedding unit test (asserts computed digest matches BLAKE3 of ciphertext — reference: [BLAKE3 official test vectors](https://github.com/BLAKE3-team/BLAKE3/blob/master/test_vectors/test_vectors.json)). **(6) §Done Checklist** extended with four new checkboxes covering digest embedding, 3-tier data map enumeration, Step 11 ordering, and cross-plan alignment confirmation. Wrap-cipher reconciliation (item 4 of the charter) was already addressed at Plan-022:26 where the plan fixes the wrap cipher to AES-256-GCM; no additional edit required. Tier-placement confirmation (item 5 of the charter) is consistent with `cross-plan-dependencies.md` — Plan-022 is Tier 2 and downstream of Plan-001's `participant_keys` forward-declaration, unchanged. Plan status remains `approved` — amendment, not re-approval. No retrospective/amendment phrasing appears outside forward-declared references.
- Resolved: 2026-04-20 (Session J).

#### BL-094: Amend Plan-025 with Spec-027 secure-defaults coverage rows 1/2/4/5/7b/8/10

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Depends-on: BL-060 (secure-defaults for self-host + Spec-027 authoring)
- References: [Plan-025](../plans/025-self-hostable-node-relay.md), [Spec-025](../specs/025-self-hostable-node-relay.md), [Spec-027 Secure Defaults For Self-Host rows 1/2/4/5/7b/8/10](../specs/027-self-host-secure-defaults.md)
- Summary (original charter — Session J Resolution below is authoritative for the drift reconciliation): Plan-025 is already at `Status: approved` but the plan body predates the BL-060 landing of Spec-027 secure-defaults. Amendment required to propagate Spec-027 rows into Plan-025 implementation surface: (1) Row 1 (automatic TLS via embedded ACME client) — add §Target Areas entry for the ACME client module and implementation step for first-boot certificate provisioning; (2) Row 2 (refuse-to-start without TLS or explicit insecure opt-in) — add implementation step gate and explicit error-contracts entry for `TLS_NOT_CONFIGURED`; (3) Row 4 (Postgres sslmode=require default) — add implementation step for connection-string construction; (4) Row 5 (TLS 1.3 minimum) — add implementation step configuring Node/Rust TLS stack minimum version; (5) Row 7b (startup banner disclosure of active security posture) — add implementation step for structured banner emission; (6) Row 8 (self-update mechanism with signature verification) — add §Target Areas entry for update-check module + implementation step for artifact signature verification; (7) Row 10 (whichever row Spec-027 numbers here — operator identity key storage or equivalent) — add implementation step matching Spec-027 requirement. Rework magnitude: 7 small additions to existing sections; no structural rewrite.
- Exit Criteria: Plan-025 §Target Areas and §Implementation Steps cover all 7 Spec-027 rows named above ✅ (Session J landed 8 rows total — the charter's 7 plus Spec-027 Row 4 container-internal bind per the drift reconciliation below); cross-references to Spec-027 row numbers resolve ✅; plan status remains `approved` ✅; no retrospective/amendment phrasing ✅ (the one "Resolution is authoritative" framing line in Plan-025 §Spec-027 Secure-Defaults Surface is drift reconciliation, not amendment phrasing — same pattern as BL-089 and BL-091).
- Resolution: Session J (2026-04-20) landed six Plan-025 amendments closing every exit clause. **(0) Charter-vs-spec drift reconciliation (authoritative framing).** BL-094's charter was drafted against an earlier Spec-027 numbering. Two drifts required reconciliation: (a) **Row-number drift** — charter rows (1/2/4/5/7b/8/10) map by position to Spec-027 final rows (1/2/5/8/10/7b/3). Plan-025 covers every charter row plus Spec-027 Row 4 (container-internal bind posture for the relay), yielding 8 total. (b) **Policy drift on Postgres TLS** — charter "Row 4: Postgres `sslmode=require` default" is superseded by Spec-027 Row 5 `sslmode=verify-full` default per [CVE-2024-10977](https://www.postgresql.org/support/security/CVE-2024-10977/) (2024-11-14 — libpq server error-message injection makes `require` MITM-exploitable; fix in PG 17.2 / 16.6 / 15.10 / 14.15 / 13.18). Spec-027 Row 5's Rationale column records the escalation; Plan-025 §Spec-027 Secure-Defaults Surface names both drifts explicitly using the "Resolution is authoritative" framing pattern established in BL-089 (component-architecture pinning) and BL-091 (Plan-006 full rewrite). **(1) §Target Areas extension** adds 7 new file/directory entries in `packages/relay-node/src/`: `tls/` (deploy-mode.ts, fingerprint-emitter.ts, caddy-config-generator.ts — Row 1), `config/insecure-flag.ts` + `config/legacy-tls12.ts` (Rows 2 and 8), `postgres/` (ssl-verify-full.ts, weak-auth-probe.ts, version-probe.ts, cert-gen-helper.ts — Row 5), `self-update/` (cli.ts, manifest-verifier.ts, sigstore-verifier.ts — Row 7b), `config/first-run-admin-token.ts` (Row 3), and `ops/first-run-banner.ts` (Row 10). **(2) New §Spec-027 Secure-Defaults Surface subsection** between §Data And Storage Changes and §API And Transport Changes with the drift reconciliation lead-in + 8-row coverage table (rows 1/2/3/4/5/7b/8/10) mapping requirements to Plan-025 surfaces and Spec-027 §Required Behavior cross-references; table explicitly names rows NOT owned by Plan-025 (Row 6 Plan-001+BL-063 backup; Row 7a Plan-007 daemon auto-update; Row 9 Plan-020 `/metrics` counter families). **(3) §API And Transport Changes env-var block** adds `DEPLOY_MODE=public|lan` (required, no default — parse-time validator rejects LAN-suffix under `public` and publicly-resolvable under `lan` citing [caddyserver/caddy#4735](https://github.com/caddyserver/caddy/issues/4735) no-silent-fallback), `INSECURE=1|unset` (Row 2 override), `LEGACY_TLS12=1|unset` (Row 8 override citing [RFC 8996](https://datatracker.ietf.org/doc/html/rfc8996) for unconditional ≤1.1 rejection), `BANNER_FORMAT=stdout|json` (Row 10), and `PG_SSLMODE=verify-full|verify-ca` (Row 5 — refuses `disable|allow|prefer|require` at parse-time citing CVE-2024-10977). **(4) §Implementation Steps 19-26** appended, one per covered row with full implementation obligations and primary-source citations ([RFC 9773](https://datatracker.ietf.org/doc/html/rfc9773) ARI with LE 2026 policy exemption, [RFC 7469](https://datatracker.ietf.org/doc/html/rfc7469) SPKI pin format, [RFC 8032 §5.1](https://datatracker.ietf.org/doc/html/rfc8032) Ed25519 verification, [RFC 7677](https://datatracker.ietf.org/doc/html/rfc7677) SCRAM-SHA-256 auth floor, [NIST SP 800-52 Rev 2](https://csrc.nist.gov/pubs/sp/800/52/r2/final) for TLS 1.2 non-deprecation note, [Shai-Hulud 2025-09-23](https://www.cisa.gov/news-events/alerts/2025/09/23/widespread-supply-chain-compromise-impacting-npm-ecosystem) and [Axios 2026-03-31](https://www.vectra.ai/blog/breaking-down-the-axios-supply-chain-incident) incidents for dual-path rationale). **(5) §Test And Verification Plan extension** adds 8 row-specific tests covering DEPLOY_MODE validator + fingerprint emission (Row 1), bind-TLS pairing + INSECURE override (Row 2), first-run admin-token generation + re-boot idempotence (Row 3), Compose no-port-forward CI assertion (Row 4), Postgres hardening integration matrix with MITM-proxy repro + weak-auth probe + version probe (Row 5), self-update dual-path verification with tamper + rollback + freeze scenarios (Row 7b), TLS 1.3 enforcement + LEGACY_TLS12 override (Row 8), and first-run banner content + BANNER_FORMAT=json alternative (Row 10). **(6) §Done Checklist extension** adds 8 new checkboxes (one per covered Spec-027 row) referencing the implementation surfaces and primary-source citations. Plan status remains `approved` — amendment, not re-approval. The single "Resolution is authoritative" framing line in §Spec-027 Secure-Defaults Surface is drift reconciliation (naming charter-vs-final-spec differences an AI-agent implementer must understand), not retrospective amendment phrasing.
- Resolved: 2026-04-20 (Session J).

#### BL-095: Amend Plan-026 to resolve stale BL-086-pending references

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Depends-on: BL-086 (onboarding.choice_made / onboarding.choice_reset event registration — completed in Session C)
- References: [Plan-026](../plans/026-first-run-onboarding.md), [Spec-026](../specs/026-first-run-onboarding.md), [Spec-006 §Onboarding events](../specs/006-session-event-taxonomy-and-audit-log.md)
- Summary (original charter — superseded by Session J Resolution below): Plan-026 is already at `Status: approved` but the plan body contains 9+ references phrased as pending BL-086 (onboarding event taxonomy registration). BL-086 landed in Session C (2026-04-18); Spec-006 now canonically registers `onboarding.choice_made` and `onboarding.choice_reset` in the appropriate event category. Amendment required: grep Plan-026 for every `BL-086`, "pending registration", or "once event taxonomy registers" phrasing and flip to resolved (remove the pending qualifier + reference Spec-006 directly). Rework magnitude: purely text cleanup; no structural or schema change.
- Exit Criteria: grep for `BL-086` across Plan-026 returns only resolved/historical references (or none); no phrasing characterizes the onboarding events as pending registration; plan status remains `approved`; no retrospective/amendment phrasing ✅.
- Resolution: Session J (2026-04-20) closed BL-095 as pre-landed work — every exit clause was already satisfied by earlier commits before Session J opened, so this session emits only the Resolution block (no plan body edits). **(1) Pre-land trace.** The Plan-026 BL-086-reframe cleanup landed across two Session H-interim commits: (a) `0272adb` (Session H-interim primary pass — Agent-8 Batch-1 C11 fix) reframed 4 primary sites (Plan-026 lines 34, 58, 280, 488) from "pending BL-086 follow-up" to "BL-086 (completed 2026-04-18)" framing; (b) `c92bb1f` (C11 residual grep sweep) closed 8 additional sites — 5 further primary sites Agent-8 missed (lines 12 Dependencies row, 17 Goal paragraph, 42 Non-Goals bullet, 392 Step 22 body, 511 Done-Checklist entry) plus 3 downstream sites made stale by the step-22 retitle (lines 118 read-only-verified bullet, 419 Parallelization Notes, 464 Implementation Order item 10). **(2) Session J verification pass — all three exit clauses independently confirmed.** (a) `grep "BL-086" plans/026-first-run-onboarding.md` returns 10 matches, every one phrased as "completed 2026-04-18", "registration landed", or the explicit completed-state cross-check step — none carry the `pending registration`, `once event taxonomy registers`, or `follow-up filed` qualifier; (b) `grep "pending registration\|once event taxonomy\|Amendment:\|retrospectively\|previously was\|recently changed"` returns zero matches, so no retrospective/amendment phrasing is present; (c) Plan-026:5 status header reads `approved` (unchanged from landing). **(3) No plan body edits in Session J.** Because all three clauses are already satisfied by the current Plan-026 state, Session J's contribution is this Resolution block only. Future Plan-026 edits remain possible under new backlog items; BL-095's scope (BL-086 reframe cleanup) is closed.
- Resolved: 2026-04-20 (Session J).

#### BL-096: Reconcile `paseto-ts` references in marketing mock HTML assets

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- Depends-on: BL-067 (paseto-ts rejected as V1 dependency — completed 2026-04-19 Session G1)
- References: `assets/hero/desktop-app.html` lines 1352, 1382; `assets/hero/cli-terminal.html` line 191; [ADR-010 §PASETO v4 Implementation Library](../decisions/010-paseto-webauthn-mls-auth.md)
- Summary: After BL-067 rejected `paseto-ts` as a V1 dependency (ADR-010 §PASETO v4 Implementation Library anchored in-house `packages/crypto-paseto/` on `@noble/curves` + `@noble/ciphers` instead), three marketing mock HTML fragments continue to name `paseto-ts@^2.1.0` as an illustrative example of a Codex-suggested package that the operator would review: `desktop-app.html:1352` (`import { V4 } from 'paseto-ts';` in a mock diff), `desktop-app.html:1382` (`Codex wants to install paseto-ts@^2.1.0 as a production`), and `cli-terminal.html:191` (`Codex wants to run: npm install paseto-ts@^2.1.0`). These are intentionally illustrative — depicting the supply-chain-scanning feature flow where Codex proposes a real-looking package and the operator sees a review prompt — not recommending the package. The product question is whether continuing to name `paseto-ts` in marketing copy creates brand/SEO risk given our security docs now explicitly reject it. Two paths: (A) leave as-is (the mock is a scan-demo; naming a real, plausibly-suspicious package makes the demo credible and matches the feature it illustrates); (B) swap to a different plausibly-suspicious package name or a generic stand-in (eliminates any implication that AI Sidekicks ships paseto-ts and removes any perceived endorsement). Initial recommendation: option A, since the mock's demonstrative purpose is served either way and the `paseto-ts` choice actively reinforces the scan-flow's narrative credibility. Product/design review owns the final call.
- Exit Criteria: Product/design review has chosen option A or option B ✅ (Option A selected — see Resolution); if option B, the three mock HTML fragments are updated accordingly and any screenshot regeneration is scheduled ✅ (N/A under Option A — no mock edits); decision recorded in this BL Resolution block with a one-line rationale ✅.
- Resolution: Session K (2026-04-20) closed BL-096 under **Option A (keep `paseto-ts`)** after a verification pass over the three mock HTML fragments. **(1) Verification findings.** `desktop-app.html:1340-1391` depicts Codex proposing `paseto-ts` within a hypothetical user project's `src/middleware/auth.ts` (code diff lines 1351-1363 replaces JWT with PASETO v4.public). The approval card at line 1377 offers symmetric `[Approve]` / `[Deny]` buttons with no pre-selected action and no depicted operator outcome. `cli-terminal.html:175-208` shows the CLI equivalent at the `~/dev/acme/backend` prompt (line 175) — explicitly NOT AI Sidekicks' own codebase — with `[a]pprove [d]eny [s]kip` options and "Approvals pending: 1" at line 207 confirming the decision is unresolved in the mock. **(2) Option A rationale.** The mock cannot reasonably be misread as AI Sidekicks endorsing or shipping `paseto-ts`: (a) project context is `acme/backend`, not AI Sidekicks; (b) Codex (a third-party agent) is the proposer; (c) the approval UI is symmetric with no default toward approval; (d) the outcome is deliberately "pending". The mock is a live illustration of the security-approval-gate feature that led to ADR-010's rejection of `paseto-ts` as a V1 dependency — using a real, documented-as-rejected package makes the scan-demo self-referentially consistent with our actual security posture rather than presenting fiction. A generic stand-in (e.g., `example-pkg`) would reduce the demo's narrative credibility without any real gain on SEO/brand risk (the mock already depicts the package being security-gated, not endorsed). **(3) Optional future enhancement (not required for close).** Extending the mock to depict the operator clicking Deny with a security rationale tied to ADR-010 would strengthen the narrative further by directly dramatizing ADR-010's rejection decision in the UI flow. This is a product/design refinement, not a scope item for BL-096. **(4) Session K scope discipline.** BL-096 was closed as its own micro-session (not folded into Session H-final) so that H-final can remain a pure audit pass — H-final audits a clean `todo` queue rather than making the Option A/B decision during the audit itself.
- Resolved: 2026-04-20 (Session K).

#### BL-097: Reconcile Workflow V1 scope drift (Spec-017 vs v1-feature-scope.md)

- Status: `completed`
- Priority: `P1`
- Owner: `unassigned`
- References: [Spec-017 §Non-Goals line 40](../specs/017-workflow-authoring-and-execution.md), [v1-feature-scope.md line 39](../architecture/v1-feature-scope.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md). Research provenance: M-012 research subagent pass (Opus 4.7, 2026-04-21) surveyed Airflow, CircleCI, Dagger, n8n, Activepieces, GitHub Actions, and Temporal workflow-engine V1 shipping patterns. The subagent's task-output file was a non-persisted scratch artifact; the load-bearing findings from that pass are embedded directly in the Summary below (zero-precedent finding + three expansion patterns + two V1 guardrails) so no external artifact is needed to act on this BL.
- Summary: Spec-017:40 declares V1 ships a workflow subset (`single-agent` and `automated` phase types, all 4 gate types, sequential execution, with `multi-agent` and `human` phase types deferred to V1.1). `v1-feature-scope.md:39` and ADR-015 row 4 declare the entire workflow authoring feature is V1.1 (not V1). These two statements are in direct contradiction and must be reconciled before Plan-017 can land or Spec-017 can be cited by other V1 specs. The doc audit's Plan-RO-001 run surfaced this as finding M-012 on 2026-04-21. User's stated concern is whether V1 should ship a larger workflow engine subset to avoid post-V1 retrofit cost; Spec-017's subset claim predates that framing and was not endorsed during ADR-015 authoring. Research pass (Opus 4.7 subagent, 2026-04-21) surveyed Airflow, CircleCI, Dagger, n8n, Activepieces, GitHub Actions, and Temporal workflow-engine V1 shipping patterns: zero precedent for shipping a "full" workflow engine at V1 — every surveyed system shipped a V1 subset then expanded, with three patterns (additive enum expansion — safe, Airflow Operators; replacement expansion — breaking, Dagger CUE→SDK; execution-model commitment — deprecate-within-releases, Airflow Smart Sensors). Two V1 design guardrails identified: (1) suspend/resume state machine in SQLite so V1.1 `human` phase ships as UI/API addition, not schema migration; (2) `predecessors: PhaseId[]` schema with V1 validator enforcing length ≤ 1, so V1.1 parallel phases ship as validator relaxation + executor topological-sort, not schema migration. User pushed back on "V1 ships full workflow engine" framing and raised integration-feasibility concern — whether foundational integration at V1 build time is cheaper than V1.1 retrofit. Guardrails (1) and (2) are the forward-compatibility insurance that address the integration concern without expanding V1 user-facing scope. **Three candidate resolutions on the table**: (α) keep Spec-017:40 subset as V1; update v1-feature-scope.md:39 + ADR-015 row 4 to cite Spec-017:40 as the V1 workflow definition; add the two guardrails to Plan-017 spec; V1.1 ships phase-type additions + parallel execution; (β) reverse Spec-017:40 to match v1-feature-scope.md:39 + ADR-015; declare entire workflow authoring V1.1; remove V1 subset language from Spec-017; Plan-017 ships V1.1; (γ) expand V1 scope to ship a larger workflow engine (three sub-levels: γ-i add multi-agent phase type; γ-ii add parallel execution; γ-iii add human phase type); update ADR-015 row 4; expand Spec-017 body; cost implications range from ~1 week (γ-i) to months (γ-iii) of doc-first work before Plan-001 code can begin.
- Exit Criteria: (α), (β), or (γ-i/ii/iii) selected with rationale recorded in BL Resolution ✅ (γ-iii extended to full workflow engine at V1 — see Resolution §0); Spec-017:40 and v1-feature-scope.md:39 are no longer in contradiction ✅ (Spec-017 fully rewritten; v1-feature-scope.md Feature 17 promoted V1.1→V1); ADR-015 row 4 cites the chosen resolution's authoritative surface ✅ (§V1 Features row 17 + §Amendment History 2026-04-22 + §Decision Log row 2026-04-22); if (α) is chosen, Plan-017 (or Spec-017 body) carries the two design guardrails ✅ (N/A under γ-iii — guardrails absorbed into full-engine schema by design); if (γ) is chosen, doc-first rework cost is estimated against remaining pre-code schedule and ADR-015 Decision Log is amended ✅ (≈2 Opus 4.7 session-days per Resolution §4; Decision Log row 2026-04-22 landed); resolution is landed before Session H-final (canonical backlog audit) so the H-final audit sees a clean, reconciled V1 scope ✅ (H-final has not landed).
- Resolution: Session M (2026-04-22) landed seven doc-first tasks closing every BL-097 exit clause after a two-wave research body (8 passes A–H + 2 syntheses; load-bearing findings absorbed into Session-M outputs). **(0) Resolution path — full workflow engine at V1.** Spec-017:40 (pre-BL-097) declared V1 ships a workflow subset while [ADR-015](../decisions/015-v1-feature-scope-definition.md) row 4 and [v1-feature-scope.md](../architecture/v1-feature-scope.md):39 declared the entire workflow feature V1.1 — a direct contradiction. The BL-097 charter option space (α/β/γ-i/ii/iii) framed γ-i/ii/iii as three separable sub-options (γ-i add `multi-agent`, γ-ii add parallel execution, γ-iii add `human` phase). Wave 1 Pass D showed the three sub-options mutually dependent — every surveyed precedent that enumerated executor/phase models at V1 had to break-remove entries later (Dagger CUE→SDK 2.5-yr rewrite, Airflow Smart Sensors deprecate-within-releases, n8n 1.0 silent-error-passing break), so any single sub-option alone would require multiple V1→V1.1 breaking changes. The resolved selection (labelled "γ-iii (full workflow engine at V1)" in [ADR-015 §Amendment History](../decisions/015-v1-feature-scope-definition.md#amendment-history) but extending beyond the charter's narrower γ-iii "add human phase" framing) commits V1 to the full engine surface: all four phase types (`single-agent`, `multi-agent`, `automated`, `human`); all four gate types (`auto-continue`, `quality-checks`, `human-approval`, `done`); parallel DAG execution with Kahn-style ready-set admission and per-resource-pool backpressure; five new Spec-006 event categories / 23 event types; a nine-table persistence schema; and a criterion-gated V1.1 `ownership: BIND` commitment. Two cross-cutting decisions landed alongside: **(D1)** `human`-phase default timeout — Option A (no default; SDK types `timeout: "none" | Duration` as required). Rationale and modern-precedent grounding (Temporal Workflow Execution Timeout default ∞; Argo suspend indefinite-by-default) carried in [ADR-015 §V1.1 Criterion-Gated Commitments](../decisions/015-v1-feature-scope-definition.md#v11-criterion-gated-commitments) under C-2 (notification-routing ships). **(D2)** `multi-agent` phase `ownership: BIND` — Option 2 (V1 OWN-only; V1.1 criterion-gated). V1.1 BIND commitment under three named criteria per [ADR-015 §V1.1 Criterion-Gated Commitments](../decisions/015-v1-feature-scope-definition.md#v11-criterion-gated-commitments): (a) ≥3 production reports of OWN + transcript-inheritance insufficient, (b) concrete failure case documented, (c) BIND lifecycle contract addressing the five ambiguities (phase-A-retry semantics, phase-A-abandonment handling, gate-scoping-lattice resolution, membership-snapshot timing, termination-authority resolution) enumerated in [ADR-015 §V1.1 Criterion-Gated Commitments](../decisions/015-v1-feature-scope-definition.md#v11-criterion-gated-commitments). **(1) Exit-clause 2 ✅ — Spec-017:40 and v1-feature-scope.md:39 no longer contradict.** Spec-017 body fully rewritten (477 lines); opens with `Amended 2026-04-22 (full engine V1 per BL-097)`; §Decision affirms "V1 ships all four phase types" + "V1 ships all four gate types" (Spec-017:69–70); §Non-Goals :40 V1/V1.1 subset language removed. `v1-feature-scope.md` Feature 17 row now reads "Workflow authoring and execution (full engine) — (promoted V1.1→V1 per BL-097)"; V1 count 16→17; V1.1 deferred count 4→3. **(2) Exit-clause 3 ✅ — ADR-015 row 4 cites chosen resolution.** §V1 Features row 17 cites Spec-017 "(added per 2026-04-22 amendment)"; §Amendment History carries Before/After table + rationale + how-decided paragraph + cross-reference list; §Decision Log row 2026-04-22 records the amendment. **(3) Exit-clause 4 ✅ (N/A under γ-iii) — α-conditional guardrails absorbed.** α was not selected. Both α-conditional guardrails (suspend/resume state machine in SQLite; `predecessors: PhaseId[]` schema) are absorbed into the full-engine surface by design: the nine-table schema `workflow_phase_states` carries suspend-resume semantics, and the DAG executor requires `predecessors` by construction (see [Spec-017 §Execution semantics](../specs/017-workflow-authoring-and-execution.md#execution-semantics) — Kahn-style ready-set admission over the phase DAG). **(4) Exit-clause 5 ✅ — γ-conditional: doc-first rework cost estimated and Decision Log amended.** (a) **Cost estimate: ≈2 Opus 4.7 session-days total** — ≈1 session-day for research (8 parallel passes A–H + 2 synthesis passes, all on disk) + ≈1 session-day for landing work (7 tasks #26/#27/#28/#29/#30/#32 + this Resolution #31). Against the remaining pre-code schedule (one pending Session H-final — canonical backlog audit + LICENSE finalization), no schedule impact. (b) Decision Log amended — see Exit-clause 3. **(5) Exit-clause 6 ✅ — landed before Session H-final.** H-final has not landed; this closure precedes it, satisfying the doc-first-before-coding discipline. **(6) Landed-work manifest (all 2026-04-22).** (#26) ADR-015 amended in-place — V1 count 16→17; V1.1 4→3; §V1.1 Criterion-Gated Commitments added (C-1 BIND; C-2 `human`-phase default timeout); §Amendment History added. (#27) Spec-017 body fully rewritten (477 lines) absorbing 27 amendments SA-1…SA-23 + SA-25 + SA-26 + SA-27 + SA-28 with inline primary-source citations; I1–I7 `### Security invariants` landed under Pitfalls; dual-hash scheme (BLAKE3 internal / SHA-256 for Plan-014 artifacts); `row_hash` dual-anchor via Pass G §5 extension to `workflow.gate_resolved` payload. 4 broken `018-event-envelope-schema-evolution.md` links repaired to `018-cross-version-compatibility.md`. (#28) Plan-017 rewritten (152 lines) absorbing SA-24/29/30/31; [`local-sqlite-schema.md §Workflow Tables`](../architecture/schemas/local-sqlite-schema.md) expanded to full 9-table Pass G §2 DDL (437→765 lines) with Pass G citation comments preserved; per-invariant I1–I7 CVE corpora landed inline ([n8n CVE-2025-68613](https://nvd.nist.gov/vuln/detail/CVE-2025-68613), [Airflow CVE-2024-39877](https://nvd.nist.gov/vuln/detail/CVE-2024-39877) / [CVE-2024-56373](https://nvd.nist.gov/vuln/detail/CVE-2024-56373), [Argo CVE-2025-66626](https://nvd.nist.gov/vuln/detail/CVE-2025-66626), Jenkins `CVE-2024-34144` / `CVE-2024-34145`); Temporal [`runReplayHistory`](https://docs.temporal.io/develop/typescript/testing-suite) contract for `DeterminismViolationError` landed per SA-31. (#29) `v1-feature-scope.md` mirror pass (103→105 lines; 8 edits covering heading counts, Feature 17 insertion, V1.1 row removal, criterion-gated back-pointer, deployment surface counts, Spec/Backlog Coverage assessments; pre-existing "Plan-016" gap in Backlog Coverage range fixed in-pass per advisor approval). (#30) ADR-020 + cross-references mirror pass — 10 edits across 5 files covering 16→17 count propagation + `cross-plan-dependencies.md §1` Plan-017 row expanded 4→9 tables + §V1.1+ placeholder rewrite + an in-research-body ADR-018 broken-link repair (since superseded by research/ deletion under T20) + ASCII-diagram orphan `*` footnote glyph cleanup (column-alignment-preserving space replacement). (#32) Plan-017 placed at Tier 8 in `cross-plan-dependencies.md §5` — 4 edits: §3 line 105 Plan-017 dependency row rewritten from 2-of-6 to full 6-dep (Plan-006/012/014/015/016/004) with legend-compliant `spec-declared` label; §2 renderer-extenders list updated to include Plan-017 (`apps/desktop/renderer/src/workflows/`); §5 Tier 8 row expanded with Plan-017 dependency chain and shared-resource claims (9-table schema + `packages/contracts/src/workflows/` + `packages/runtime-daemon/src/workflows/` + `apps/desktop/renderer/src/workflows/`); §V1.1+ placeholder rewritten to reflect Tier 8 placement. Research body absorbed: 8 passes (A parallel execution, B multi-agent channel contract, C human phase UX, D post-V1 freeze regrets, E security surface, F event taxonomy, G persistence model, H testing strategy) + 2 syntheses (Wave 1 — C-1…C-16 V1 commitments + I1–I7 security invariants + D1/D2 resolutions; Wave 2 — SA-1…SA-31 amendments). Load-bearing primary-source citations flow into [ADR-015 §Research Conducted](../decisions/015-v1-feature-scope-definition.md#research-conducted), [Spec-017 §References](../specs/017-workflow-authoring-and-execution.md#references), [Plan-017 §References](../plans/017-workflow-authoring-and-execution.md#references), and [`local-sqlite-schema.md §Workflow Tables`](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-017). **(7) Non-blocking follow-ups surfaced (not resolved in this Resolution).** Four items identified during BL-097 reconciliation, surfaced for discoverability but out of BL-097 scope: (a) **SQLite STRICT cross-cutting ADR** — Wave 2 §4.1 proposed a cross-plan STRICT migration ADR; Plan-017 schema deferred STRICT adoption per this proposed ADR using the repo's existing TEXT-column convention; ADR not yet drafted. (b) **Pass G §2 inherited-pragma drift** — `synchronous=FULL` claim in Pass G research vs the schema-file pragma block; one-line additive fix candidate. (c) **`cross-plan-dependencies.md §3` legend drift audit** — six post-rewrite plans (011/021/022/023/025/026) use `declared in plan header` label not in the legend (legend at lines 83-84 defines only `spec-declared` + `implementation-derived`); Plan-017 chose `spec-declared` for legend compliance. Reconciliation pass would add the drifted label to the legend or rewrite the drifted rows. (d) **`packages/contracts/src/` subdirectory convention tension** — Plan-017 introduces `packages/contracts/src/workflows/` as a subdirectory, diverging from the §2 line 70 "single-file-per-contract" convention (Plan-024: `pty-host.ts`, Plan-021: `rate-limiter.ts`); convention-extension call (single-file-or-single-subdirectory) deferred until a second subdirectory candidate surfaces.
- Resolved: 2026-04-22 (Session M).

## Completed: 2026-04-26 (V1 doc-first phase: BL-100 + BL-099)

#### BL-100: Author ADR-023 — V1 CI/CD, Pre-Commit Hooks, and Release Automation

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [ADR-022 — V1 Toolchain Selection](../decisions/022-v1-toolchain-selection.md) (CI named as success criterion; workflow files unauthored), [Spec-027 — Self-Host Secure Defaults](../specs/027-self-host-secure-defaults.md) (**governance constraint**: §Behavior 7b end-user trust model — Ed25519 manifest signing + Sigstore bundle + `actions/attest-build-provenance` — is decided; ADR-023's release-automation axis MUST implement it, not relitigate it), [ADR-019 — Windows V1 Tier and PTY Sidecar](../decisions/019-windows-v1-tier-and-pty-sidecar.md) (Rust sidecar code-signing scope to be extended), [Plan-001 — Shared Session Core](../plans/001-shared-session-core.md) (Phase 1 ship-gate consumer), [Spec-011 — Gitflow, PR, and Diff Attribution](../specs/011-gitflow-pr-and-diff-attribution.md) (product-level gitflow; engineering-side gitflow is this BL's scope)
- Summary: Author the missing engineering CI/CD ADR. Currently scattered: end-user release distribution decided in Spec-027 §7b; cross-compile CI matrix mentioned in ADR-019 + ADR-022 success criteria but not authored; engineering-side decisions for `.github/workflows/`, pre-commit framework, commit-message format, branch protection, release automation for npm-publishable packages, npm provenance, SBOM, dependency-update bot, secret scanning, and code-signing custody have NO governing ADR. ADR-023 closes this gap. Scope (5 axes): (1) **CI workflow architecture** — job graph, matrix axes for two-ABI rebuilds + two-tier Node 22/24 + Windows/macOS/Linux, Turbo remote-cache integration, `concurrency` rules to cancel superseded runs, secrets scope, branch protection + required-checks list, `CODEOWNERS`; (2) **pre-commit hook framework + dev-loop** — Husky vs lefthook vs simple-git-hooks vs `pre-commit` (Python); `lint-staged` config; commitlint + conventional-commits; (3) **release automation** — semantic-release vs changesets vs release-please for npm-publishable packages (`@ai-sidekicks/contracts`, `@ai-sidekicks/client-sdk`); desktop-binary release flow (must implement Spec-027 §7b verbatim); Rust sidecar release flow (extends ADR-019 distribution pattern); (4) **supply-chain hygiene** — `npm publish --provenance`, SBOM emission (syft / cyclonedx / spdx), Dependabot vs Renovate vs Socket.dev, secret scanning (GitHub Advanced Security vs Gitleaks vs trufflehog); (5) **code-signing custody** — Apple Developer ID + Windows code-signing or Azure Artifact Signing extension to cover Electron app + npm packages; secret storage in GitHub Actions; key-rotation procedure; signing-config CODEOWNERS scope.
- Methodology: Per [`feedback_websearch_before_recommendation.md`](MEMORY) primitive choices in this space are architectural decisions requiring primary-source-cited research with current-year (2026) citations BEFORE recommendations are presented. Spawn **5 parallel Opus 4.7 specialized subagents** (one per scope axis) per [`feedback_research_standards.md`](MEMORY) Opus-4.7-only subagent constraint; subagent findings drafted transiently under `.agents/tmp/research/adr-023-ci-cd/<axis>.md` per the cross-tool research-artifact convention in [`AGENTS.md`](../../AGENTS.md); citations carried forward into ADR-023 References before transient drafts are deleted at commit time. WebSearch verification per axis BEFORE recommendation lands.
- Exit Criteria: `docs/decisions/023-v1-ci-cd-and-release-automation.md` exists with status `accepted` ✅; Decision section covers all 5 scope axes ✅; each choice carries Antithesis + Synthesis sections per ADR template ✅; all assumptions cite primary sources with 2026 fetch dates ✅ (~80 unique citations all dated 2026-04-26); failure-mode analysis with detection signals ✅ (10 scenarios F-1…F-10); reversibility assessment ✅ (per-axis table; axes 3+5 carry time-fuses to Type 2 after first signed V1 release); re-evaluation triggers documented ✅ (per-axis Re-Evaluation Triggers tables); Plan-001's Required ADRs row updates ADR-023 from `(pending per BL-100)` to a normal accepted reference ✅; Plan-001 Preconditions Phase 1 ship-gate item flips from `[ ]` to `[x]` ✅; Plan-001 Phase 1 enumerates the concrete artifact list authored by ADR-023 (`.github/workflows/{ci,release}.yml`, pre-commit hook config, commitlint config, dependency-update bot config, `CODEOWNERS`, code-signing custody scaffolding) ✅.
- Resolution: Session N (2026-04-26) landed all 11 BL-100 exit clauses across a six-task Phase 1 execution under the BL-099/BL-100 orchestration framework. **(0) Methodology — 5-axis parallel Opus 4.7 subagent fan-out.** Per the Methodology line above, five Opus 4.7 specialized subagents ran in parallel, one per ADR-023 scope axis, drafting findings transiently to `.agents/tmp/research/adr-023-ci-cd/{axis-1-ci-workflow,axis-2-pre-commit,axis-3-release-automation,axis-4-supply-chain,axis-5-code-signing}.md` (256+239+148+156+199 = 998 lines of research). Each axis carried current-year (2026-04-26 fetch-dated) primary-source citations. Cross-axis convergence on four primitives — `actions/attest@v4` unified attestation surface, [npm Trusted Publishing GA 2025-07-31](https://github.blog/changelog/2025-07-31-npm-trusted-publishing-with-oidc-is-generally-available/), per-job `id-token: write` minimization, and Sigstore-anchored end-user verification — emerged independently across axes 1, 3, 4, and 5 and locked the ADR's load-bearing recommendations. Three drift findings surfaced from the parallel research: (a) **macos-13 → macos-15-intel runner deprecation** ([GitHub Actions runners image deprecation announcement, 2025-09-15](https://github.com/actions/runner-images/issues/12643) — macos-13 retires 2025-12-04; ADR-023 §Axis 1 matrix anchors `macos-15-intel` as the V1 Intel macOS runner); (b) **version actuals** — Husky 9.1.7, lefthook 2.1.6, commitlint 20.5.2 (all primary-source-verified at 2026-04-26 fetch); (c) **npm Trusted Publishing requires Node ≥22.14** ([npm release notes, 2025-08-12](https://github.com/npm/cli/releases/tag/v11.5.1)) — release CI runs Node 24, dev/test CI retains Node 22.12+ + 24.x two-tier matrix per ADR-022. **(1) ADR-023 authored — `docs/decisions/023-v1-ci-cd-and-release-automation.md`, 398 lines, status `accepted`, classified Type 1 (two-way door, reversible) with per-axis time-fuse: axes 3 (release automation) + 5 (code-signing custody) flip to Type 2 after the first signed V1 release because reversing those choices post-release would invalidate signed manifests / break end-user verification chains.** Body covers all 5 axes — Axis 1 (CI workflow: dual workflows `.github/workflows/ci.yml` + `release.yml`; matrix Node 22.12+/24.x × {ubuntu-24.04, macos-15, macos-15-intel, windows-2022}; Turborepo remote cache via signed action token; concurrency.cancel-in-progress; OIDC environment-gate `repo:<org>/<repo>:environment:production` for release jobs; required-checks list; CODEOWNERS surface for `.github/workflows/`, signing-config, release config); Axis 2 (lefthook 2.1.6 selected over Husky 9.1.7 for parallel-execution + skip-via-`LEFTHOOK=0`; lint-staged for staged-only execution; commitlint 20.5.2 with `@commitlint/config-conventional` + custom `BL-NNN`/`ADR-NNN` ID footer rule); Axis 3 (release-please-action@v5 for manifest-driven releases over semantic-release / changesets; npm Trusted Publishing OIDC over npm-token; `actions/attest@v4` unified attestation; release CI Node 24 per Trusted Publishing minimum); Axis 4 (Renovate over Dependabot for monorepo-aware PR aggregation; Gitleaks v8.30+ for pre-commit + CI secret scanning; SBOM via `actions/attest-sbom@v2` with cyclonedx-format; supply-chain backstop via `npm audit --audit-level=high` + advisory-allowlist file); Axis 5 (Apple Developer ID for macOS notarization; Azure Artifact Signing for Windows authenticode + SmartScreen reputation pooling per ADR-019 §Decision item 7; Sigstore keyless for Linux per Spec-027 §Behavior 7b; AWS KMS Ed25519 hot key — OIDC-only, no exportable material — for the per-release manifest signature; YubiHSM 2 cold key for the V1 root signing ceremony with quarterly key-rotation cadence; signing-config CODEOWNERS pinned to release maintainers). Per-axis Decision + Antithesis + Synthesis sections; per-axis Reversibility table; per-axis Re-Evaluation Triggers; 10 Failure Mode scenarios F-1 through F-10 each with Detection Signals + Mitigation. The single non-primary-source citation (Trivy 2026-03 incident reference cross-checked against vendor post-mortem only — no primary URL) is honestly flagged inline in both the source axis-4 research and ADR-023 §Axis 4 References. **(2) Plan-001 cross-link updates (6 edits).** Line 11 Required ADRs row appended `[ADR-023]`, dropped `(pending per BL-100)`, rewrote ship-gate sentence to past-tense "accepted 2026-04-26"; line 33 Phase 1 ship-gate checkbox flipped `[ ]` → `[x]` with concrete artifact list (lefthook 2.1.6, commitlint 20.5.2, Renovate, Gitleaks v8.30+, release-please-action@v5, actions/attest@v4); line 61 Engineering CI surface enumeration expanded with concrete artifacts; lines 164/166/172 past-tense ADR-023 references with concrete artifact list. **(3) ADR-019 §Decision item 6 cross-link added.** Per-platform binary signing now cross-references ADR-023 §Axis 5 for code-signing custody (Apple Developer / Azure Artifact Signing / Sigstore keyless / AWS KMS Ed25519 hot + YubiHSM 2 cold) — natural one-hop navigation from the noun phrase "per-platform signed binary" to the governing custody decision. **(4) Anti-hallucination verification + Surface-Forward-Then-Delete.** Cross-checked all ~80 ADR-023 citations against the 5 source axis research files; every load-bearing claim, citation URL, and version pin maps back to its source. Per [AGENTS.md](../../AGENTS.md) "Surface-Forward-Then-Delete", the entire `.agents/tmp/research/adr-023-ci-cd/` directory (5 files, 998 lines) was deleted post-verification once all citations had been surfaced forward into ADR-023 References. **(5) BL-097 V1.1→V1 promotion alignment.** Plan-001 ship-gate description updated to reflect the concrete `.github/workflows/`, pre-commit, and release artifact list — consistent with the V1-engineering-CI-CD-ADR-now-accepted state and downstream of the BL-097 V1 scope finalization. **(6) Phase 1 PR — to be opened separately.** This Resolution captures the doc-first work; Phase 1 PR opens with title `docs: add ADR-023 V1 CI/CD and release automation; flip Plan-001 Phase 1 ship-gate` and surfaces the CLAUDE.md "code execution stays paused" line as a follow-up flag (the line becomes false post-merge of the Plan-001 Phase 1 first-code-execution gate, not post-merge of this Phase 1 doc PR — staleness is one PR removed, not immediate). **(7) Orchestration revision — Phase 2 combined into the same PR.** The original orchestration framework planned Phase 2 (BL-099) as a follow-up PR opened after the Phase 1 PR merged. After Phase 1 commit + push to `docs/audit-realignment` (commit `4e503fc`), the user revised the orchestration mid-session ("No PR until we complete all phases") to combine BL-100 Phase 1 and BL-099 Phase 2 into one PR. The combined PR now ships ADR-023 (Phase 1) + `docs/domain/trust-and-identity.md` (Phase 2) under a broadened title — see BL-099 Resolution below for Phase 2 closure detail. The serial-PR rationale (P0/P2 priority separation, reviewability) was overridden by the user's preference for one bundled change closing both BLs in Session N.
- Resolved: 2026-04-26 (Session N).

#### BL-099: Author `docs/domain/trust-and-identity.md`

- Status: `completed`
- Priority: `P2`
- Owner: `unassigned`
- References: [Spec-027 — Self-Host Secure Defaults](../specs/027-self-host-secure-defaults.md), [Participant And Membership Model](../domain/participant-and-membership-model.md), [Security Architecture](../architecture/security-architecture.md), [ADR-010 — PASETO/WebAuthn/MLS Auth](../decisions/010-paseto-webauthn-mls-auth.md)
- Summary: Author the missing `docs/domain/trust-and-identity.md` domain doc to canonicalize trust-ceremony semantics (first-run secret generation, fingerprint display, device-trust establishment) currently scattered across `participant-and-membership-model.md`, `security-architecture.md`, and Spec-027. Surfaced by the 2026-04-26 read-only docs audit (M-003): Spec-027 §Domain Dependencies originally cited the file as if it existed; the audit-driven fix re-pointed Spec-027 to `participant-and-membership-model.md` as the closest existing scope match (`fc05627`), but the trust-ceremony surface deserves its own domain doc.
- Methodology: **Extraction-only — no subagent research.** Per the BL-099/BL-100 orchestration framework distinguishing primitive-choice work (BL-100, requires current-year-cited research) from corpus-extraction work (BL-099, the source corpus is already approved and the new doc canonicalizes existing scattered facts), BL-099 is authored by extracting and re-presenting facts from already-approved corpus rather than spawning research subagents. The synthesis surface is the trust-state lifecycle table — every state and transition must trace to a source-doc fact (synthesis-not-research check), not be invented in the new doc.
- Exit Criteria: `docs/domain/trust-and-identity.md` exists ✅; covers identity-material provisioning, fingerprint verification ceremony, and trust-state lifecycle ✅; is referenced from Spec-027 §Domain Dependencies ✅, ADR-010 ✅, and security-architecture.md ✅; sibling docs cross-link consistently ✅ (4 specified back-refs + 3 expanded for full bidirectional discipline).
- Resolution: Session N (2026-04-26) landed all 8 BL-099 exit clauses across a five-task Phase 2 execution under the BL-099/BL-100 orchestration framework (combined-PR variant per the user's mid-session revision). **(0) Methodology — extraction-only, no subagent research.** Source-extraction map: identity-material provisioning extracted from [Spec-027 §State And Data Implications](../specs/027-self-host-secure-defaults.md) (first-run sentinel `./data/trust/first-run.complete`; daemon master key, session-signing key, relay admin token, daemon session token, long-term Ed25519 identity key, PASETO v4 access/refresh, DPoP key); fingerprint verification ceremony extracted from [Spec-027 §Behavior 1 + §Behavior 10](../specs/027-self-host-secure-defaults.md) (RFC 7469 SPKI-SHA256 base64 vs SSH-style `SHA256:<hex>` whole-cert hash distinction; operator out-of-band match); daemon master-key rotation extracted from [security-architecture.md §Daemon Master Key Rotation](../architecture/security-architecture.md) (event-driven, atomic via `BEGIN EXCLUSIVE` SQLite transaction, rotate-on-shred); refuse-on-rotation invariant extracted from [ADR-021 §Refuse-On-Rotation Invariant](../decisions/021-cli-credential-storage-strategy.md) (silent identity-key rotation prohibited because it would invalidate every prior `SessionKeyBundle` signature); V1.1 MLS promotion gates extracted from [ADR-010 §V1.1 MLS Promotion Gates](../decisions/010-paseto-webauthn-mls-auth.md) (external audit + interop + production soak; orthogonal to identity state). **(1) Trust-state lifecycle authored — synthesis-not-research check passed.** The 7-state lifecycle (`unprovisioned`, `provisioned`, `verified`, `bound`, `rotating`, `revoked`, `compromised`) with 9 transitions is the only synthesized surface in the doc; every state and transition cites a source-doc fact in the table's Source column. The most-synthesized transition (`compromised → unprovisioned`) follows from explicit invariants — no automatic transitions out of `compromised`; explicit operator action + new ceremony required — and does not invent semantics absent from source docs. **(2) `docs/domain/trust-and-identity.md` authored, 251 lines.** Sections: Purpose, Scope, Definitions, What This Is, What This Is Not, Invariants, Identity Material Provisioning (5 subsections covering each material type), Fingerprint Verification Ceremony (4 subsections covering ceremony scope, fingerprint forms, verification semantics, re-display invariant), Trust-State Lifecycle (state table + transition diagram + transition notes), Relationships To Adjacent Concepts, Example Flows (5 examples), Edge Cases, Pitfalls To Avoid, Related Specs, Related ADRs, Related Architecture. **(3) Specified cross-link updates (4 edits).** [Spec-027:41](../specs/027-self-host-secure-defaults.md) §Domain Dependencies pointer flipped from `participant-and-membership-model.md` (M-003 placeholder fix per `fc05627`) to `trust-and-identity.md` with extended scope-match phrase covering identity-material provisioning + fingerprint verification ceremony + trust-state lifecycle — retiring the M-003 placeholder. [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md) added new §Related Domain Docs section before §References anchoring `trust-and-identity.md` as canonical domain model with PASETO/WebAuthn/MLS as implementation surface. [security-architecture.md §Related Domain Docs](../architecture/security-architecture.md) added `trust-and-identity.md` as first item in the existing list before the three sibling domain docs. [participant-and-membership-model.md](../domain/participant-and-membership-model.md) added new §Related Domain Docs section before §Related Specs anchoring back-ref to `trust-and-identity.md` (cryptographic identity is precondition for membership; trust-state lifecycle runs alongside membership lifecycle). **(4) Sibling-doc bidirectional expansion (3 edits, scope expansion beyond orchestration §2.2).** Orchestration §2.3 enforces strict bidirectional cross-link discipline. The new doc's §Related Domain Docs section references four siblings (participant-and-membership, runtime-node, artifact-diff-and-approval, session-model), but orchestration §2.2 only specified the participant-and-membership back-ref. Three siblings unspecified by §2.2 received new §Related Domain Docs sections to satisfy §2.3: [runtime-node-model.md](../domain/runtime-node-model.md) (node attachment authenticated by participant identity ≥ `bound`; node trust envelope governed by approval policy not identity state); [artifact-diff-and-approval-model.md](../domain/artifact-diff-and-approval-model.md) (`bound` identity can sign approvals; `revoked`/`compromised` cannot; dual-signed `ApprovalRecord` envelope per Spec-024 is cross-node cousin); [session-model.md](../domain/session-model.md) (session-end is trigger for ephemeral X25519 zeroization per security-architecture.md §V1 Relay Encryption + rotate-on-shred path of daemon master key when participant crypto-shred fires). Scope expansion documented in this Resolution; orchestration §2.3 strict-bidirectional check now satisfied. **(5) M-003 placeholder retirement.** Spec-027 line 41 originally cited `trust-and-identity.md` as if it existed; M-003 audit-driven fix (`fc05627`) re-pointed to `participant-and-membership-model.md` as the closest existing scope match. With the new doc landed, line 41 was flipped back to `trust-and-identity.md` with extended scope-match phrase, retiring the M-003 placeholder permanently. **(6) Anti-hallucination verification.** Every fact in the new doc traces to an already-approved source-corpus citation (Spec-027 / security-architecture.md / ADR-010 / ADR-021); no claim is invented. The trust-state lifecycle synthesis check verified each state/transition has a source-doc anchor — no orphan states or transitions. **(7) Combined-PR closure.** Per the user's mid-session orchestration revision (BL-100 Resolution §7), Phase 2 work is committed to the same `docs/audit-realignment` branch and ships in the combined PR alongside Phase 1 (ADR-023). The combined PR closes both BL-100 (P0) and BL-099 (P2) under a broadened title covering Sessions K–N audit work + Phase 1 + Phase 2.
- Resolved: 2026-04-26 (Session N).

## Completed: 2026-04-30

#### BL-104: C-4 — ADR-014 runtime authorization reconciled (Cloudflare Workers via fetch adapter)

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Plan-008](../plans/008-control-plane-relay-and-session-join.md) §Preconditions + §Decision Log, [ADR-014](../decisions/014-trpc-control-plane-api.md), [Plan-003](../plans/003-runtime-node-attach.md) §CP-003-2, [cross-plan-dependencies.md §5 Plan-008 carve-out](../architecture/cross-plan-dependencies.md#plan-008-bootstrap-vs-remainder-carve-out-tier-1--tier-5)
- Summary: ADR-014 authorized Cloudflare Workers for the tRPC v11 control-plane API; Plan-008 declared "Fastify host" without ADR backing. Three resolution paths were available: (a) ADR-014 amendment authorizing Fastify v5 for Tier 1 bootstrap; (b) Plan-008 amendment to use Cloudflare Workers + workerd local emulation for Tier 1 bootstrap; (c) ADR-NNN supersedes ADR-014 for Tier 1 bootstrap runtime selection.
- Resolution: Path (b) chosen 2026-04-30. ADR-014 honored verbatim — no ADR amendment. Plan-008 amended to use Cloudflare Workers via `@trpc/server/adapters/fetch` for both Tier 1 bootstrap and Tier 5 production; local development uses workerd via `wrangler dev` (Miniflare v3 per [ADR-014 Assumption #1](../decisions/014-trpc-control-plane-api.md#assumptions-audit)). Reasoning under SWE/architect lens: dev-prod parity (12-factor §10), single adapter / single deployment topology, no future Tier 1→Tier 5 migration PR, V8-isolate sandbox defense-in-depth versus Node syscall surface, Cloudflare's free tier supplies 100k req/day with no effective limit on SSE response duration per [Cloudflare Agents HTTP and SSE docs](https://developers.cloudflare.com/agents/api-reference/http-sse/) covering V1 deployment scale. Reasoning under AI-engineer lens: single-adapter pattern reduces context-switching for Claude Code generators, eliminates a high-blast-radius Tier 1→Tier 5 migration PR, doc-first traceability simplest (zero ADR amendment), `wrangler dev` is canonical and well-trained. **Primary-source verification corrected an early misread:** the tRPC fetch adapter docs (`@trpc/server/adapters/fetch`) are silent on SSE, but the substrate at [`packages/server/src/unstable-core-do-not-import/http/resolveResponse.ts`](https://github.com/trpc/trpc/blob/main/packages/server/src/unstable-core-do-not-import/http/resolveResponse.ts) detects `proc._def.type === 'subscription'` and produces SSE-streaming `Response` natively when invoked through `fetchRequestHandler`. No separate SSE adapter required. Path (a) was rejected because dev-loop speed (its only differentiator) was explicitly de-prioritized by the user's "production-hardened, no regressions" framing; path (c) was rejected because Tier 1 and Tier 5 are not genuinely different problems (same control plane, different scale) — two ADRs for one decision is governance overhead without architectural payoff. **I-008-1 gate #2 reformulated** from "loopback-bind gate" to an environment-scoped allow-list (since Cloudflare Workers do not expose a host-bind surface) — current canonical semantics: gate #2 refuses HTTP 503 unless `env.ENVIRONMENT === 'development'` (allow-list), with `ENVIRONMENT="development"` and the bootstrap flag both supplied via `packages/control-plane/.dev.vars` (gitignored) so the deployable surface carries no security-load-bearing keys. **The BL-104 amendment first proposed an intermediate deny-list draft (`!== 'production'`) which was superseded across two PR-#20 review rounds (round-3 flag relocation + round-4 allow-list pivot); see [Plan-008 §Decision Log](../plans/008-control-plane-relay-and-session-join.md#decision-log) for the round-by-round evolution.** File-path: `loopback-bind-gate.ts` → `dev-environment-gate.ts` (current name post-round-4). Plan-003 §CP-003-2 updated in lockstep per Plan-008 CP-008-2's bidirectional clause. cross-plan-dependencies.md §5 Plan-008 carve-out updated to reflect Workers runtime. Sources: [tRPC Fetch / Edge Runtimes Adapter](https://trpc.io/docs/server/adapters/fetch); [tRPC Subscriptions](https://trpc.io/docs/server/subscriptions); [Cloudflare Workers Limits](https://developers.cloudflare.com/workers/platform/limits/); [Cloudflare Agents HTTP and SSE](https://developers.cloudflare.com/agents/api-reference/http-sse/); [Cloudflare wrangler/workerd announcement](https://blog.cloudflare.com/wrangler3/).
- Exit Criteria: ADR-014 consistent with Plan-008's runtime declaration ✅ (no amendment — Workers chosen verbatim); every "Fastify host" reference in Plan-008 replaced with the Workers + fetch-adapter equivalent ✅ (intentional historical-context references retained in I-008-1 gate evolution + §Decision Log only); Plan-003 line 79 reference updated in lockstep ✅; cross-plan-dependencies.md §5 Plan-008 carve-out updated ✅; F-008b-1-02 closed ✅.
- Resolved: 2026-04-30.

#### BL-101: C-3 — Plan-023 Tier 1 Partial substrate carve-out (mirrors Plan-007-partial / Plan-008-bootstrap)

- Status: `completed`
- Priority: `P0`
- Owner: `unassigned`
- References: [Plan-001](../plans/001-shared-session-core.md) Phase 1 + Phase 5 (CP-001-1, T5.2, T5.3), [Plan-024](../plans/024-rust-pty-sidecar.md) Phase 3 (I-024-4, T-024-3-5), [Plan-023](../plans/023-desktop-shell-and-renderer.md) §Tier 1 Partial PR Sequence, [cross-plan-dependencies.md §5 Plan-023 Substrate-vs-Namespace Carve-Out](../architecture/cross-plan-dependencies.md#plan-023-substrate-vs-namespace-carve-out-tier-1--tier-8), [container-architecture.md §Monorepo Layout](../architecture/container-architecture.md), [component-architecture-desktop-app.md](../architecture/component-architecture-desktop-app.md)
- Summary: Plan-001 Phase 5 + Plan-024 Phase 3 imported the `apps/desktop/` directory tree (originally cited as `apps/desktop/main/src/sidecar-lifecycle.ts`, a path that contradicted Plan-023's then-current `apps/desktop/electron/main/` convention). Same defect class as the canonical substrate-import bug fixed by commit `a230a50` (`docs(repo): re-tier Plan-007 partial + Plan-008 carve-out to Tier 1`). Three resolution paths were available: (1) Plan-023 Tier 1 Partial carve-out mirroring Plan-007-partial / Plan-008-bootstrap; (2) defer Plan-001 Phase 5 sidecar-lifecycle work to a Phase 5b at Tier 8; (3) transfer CP-001-1 ownership to Plan-023.
- Resolution: Path (1) chosen 2026-04-30. Plan-023 stays `approved` (additive amendment per PR #11 precedent) and grows a §Tier 1 Partial PR Sequence with 7 tasks (T-023p-1-1 through T-023p-1-7) authoring a single cohesive substrate slice — `apps/desktop/package.json` minimal-dep set, `electron.vite.config.ts` (electron-vite v5.0.0 stable per §Risks And Blockers), `apps/desktop/src/main/{index,window}.ts` minimal entrypoints with locked `webPreferences` + build-time webprefs assertion, `apps/desktop/src/preload/index.ts` stub `contextBridge.exposeInMainWorld('sidekicks', bridge)` call, `apps/desktop/src/renderer/src/{main.tsx,App.tsx}` minimal React mount, `packages/contracts/src/desktop-bridge.ts` `SidekicksBridge` typed stub + conditional-type negative test, `apps/desktop/eslint.config.mjs` flat-config `no-restricted-imports` rule, and `apps/desktop/test/launch.smoke.test.ts` Vitest smoke. The Tier 1 Partial slice is standalone — no Plan-007 / Plan-018 / Plan-008 / Plan-024 plan-level preconditions; only ADR-016 + Spec-023 gate it. **Path canonicalization grounded in orchestrated 5-axis primary-source research.** The path-convention space initially exposed a four-way conflict: (a) Plan-023 used `apps/desktop/electron/main/`; (b) Plan-001 cited `apps/desktop/main/src/`; (c) Plan-024 cited `apps/desktop/main/`; (d) `container-architecture.md` line 43–44 + `component-architecture-desktop-app.md` line 34–35 pinned an interim `apps/desktop/shell/` + `apps/desktop/renderer/` architecture-canonical. An interim resolution toward (d) was committed-in-progress, then escalated mid-resolution to a fresh Opus 4.7 subagent research pass when the user surfaced that "shell/" appeared in zero of the surveyed external sources. The first pass (single subagent, 5 sources) was insufficient; the user requested a wider orchestration. Five parallel Opus 4.7 subagents covered: **(A)** electron-vite / electron-forge / electronjs.org / vite.dev / vite-plugin-electron official docs sites — verdict: electron-vite docs ([electron-vite.org/guide/dev](https://electron-vite.org/guide/dev) §Project Structure conventions) explicitly prescribe `src/{main,preload,renderer}/` as the only verbatim-prescribed layout, declared load-bearing for zero-config auto-discovery; no other toolchain ships a competing verbatim tree. **(B)** Production OSS app survey (17 apps including Cherry Studio, Chatbox, DeepChat, Feishin, PicGo, Deskreen, LobeChat, Marktext-rewrite) — verdict: 9–10 of 14 verified apps use `src/{main,preload,renderer}/` with electron-vite; alternatives (`electron/{main,preload}/` + top-level `src/`; per-process workspace packages) appear in 2–3 apps each. **(C)** 22 articles + conference talks 2024–2026 (LogRocket, dev.to, Medium, Hashnode, Spatie Ray, Felix Rieseberg, Superset 2026 daemon deep-dive, ViteConf, electron-vite official guides, Forge docs, daltonmenezes boilerplate, Tuui template) — verdict: maintainer-grade unanimous endorsement of `src/{main,preload,renderer,shared}/` single-package layout; tutorial-author divergence to top-level `electron/` exists but no Electron / Vite maintainer endorses it. **(D)** Monorepo tooling (Turborepo, Nx, pnpm Workspaces, Yarn Berry, Lerna, Bun, Rush) — verdict: zero of seven publish official Electron templates; Nx community plugin uses per-process apps but lacks first-party endorsement. **(E)** Electron security + hardening (Electron Security Tutorial, Process Model docs, contextBridge / Sandbox / Fuses / ASAR docs, Doyensec Electronegativity SAST, 2024–2026 Electron CVEs including CVE-2025-55305 ASAR-integrity bypass, Signal Desktop / Bitwarden / Element / Tutanota layouts) — verdict: layout is security-orthogonal; security-conscious apps show no convergent layout pattern; Electronegativity is path-blind by design. **Reconciliation:** Axes A + B + C convergently endorse `apps/desktop/src/{main,preload,renderer}/`; Axis D's per-process-apps community pattern is discounted as low-authority (zero official monorepo-tool endorsement, single-citation precedent); Axis E eliminates security as a tiebreaker. The four-way path conflict is resolved by amending `container-architecture.md` line 43–44 + `component-architecture-desktop-app.md` line 34–35 to the electron-vite zero-config convention (architecture-tier authority follows external-source evidence when such evidence is convergent and the prior architecture-tier choice was made without it). The pre-existing `apps/desktop/src/index.ts` placeholder shipped by Plan-001 Phase 1 / PR #6 corroborates the `src/` parent (its forward-declaration comment, originally `"split into apps/desktop/{shell,renderer}"`, is amended to `"split into apps/desktop/src/{main,preload,renderer}/ per the electron-vite zero-config convention"` in this PR). The transient research artifacts `.agents/tmp/research/electron-app-layout/{A,B,C,D,E}-*.md` are gitignored per AGENTS.md; load-bearing citations are surfaced forward into this archive entry per the surface-forward-then-delete convention. **Comprehensive lockstep canonicalization across 28 files.** The architecture-tier amendment ripples into every plan / ADR / reference doc that cites `apps/desktop/{shell,renderer}/<X>/` paths, otherwise creating silent contradictions. The PR therefore canonicalizes path strings across: (i) the original BL-101 scope — Plan-023 (~37 path replaces; including ESLint globs), Plan-001 (T5.2 + T5.3 task bodies authored, replacing prior `BLOCKED-ON-C3` placeholders, plus all Phase 5 path strings), Plan-024 (I-024-4 + Phase 3 + T-024-3-5), Plan-026 (17 onboarding-path replaces from `apps/desktop/electron/{main,preload}/onboarding/...` → `apps/desktop/src/{main,preload}/onboarding/...`), cross-plan-dependencies.md (§2 ownership rows + §3 Plan-001 dep + §3 ASCII graph node + §5 Tier 1 + §5 Tier 8 + new §5 Plan-023 Substrate-vs-Namespace Carve-Out subsection); (ii) the architecture-tier authority shift — `container-architecture.md` line 43–44 + `component-architecture-desktop-app.md` line 34–35; (iii) the broader downstream propagation — Plan-007 + Plan-019 (`apps/desktop/shell/src/<X>/` → `apps/desktop/src/main/<X>/`), ADR-016 (blast-radius bullet), reference/design-audits/architecture-ops.md (canonical-layout meta-doc), and Plans 002 / 003 / 004 / 006 / 008 / 009 / 010 / 011 / 012 / 013 / 014 / 015 / 016 / 017 / 018 / 020 / 027 (renderer-feature paths). Total: 28 files; ~125 path-string replacements; orchestrated via 5 parallel mechanical-edit Opus 4.7 subagents (Batches A–E) and 2 self-edits for narrative-heavy files. Path (2) was rejected because Plan-001 Phase 5 is Tier 1 and Tier 8 deferral re-introduces the cross-tier-import bug class. Path (3) was rejected because CP-001-1 ownership properly belongs with Plan-001 (Plan-024 supplies `PtyHost.close(sessionId)` + `KillRequest` primitives only; Plan-023 owns the substrate directory tree; Plan-001 owns sidecar-lifecycle drain orchestration content). Sources: [electron-vite Development guide](https://electron-vite.org/guide/dev); [electron-vite Getting Started guide](https://electron-vite.org/guide/); [alex8088/electron-vite-boilerplate](https://github.com/alex8088/electron-vite-boilerplate); [electron/forge Vite-TypeScript template](https://github.com/electron/forge/tree/main/packages/template/vite-typescript/tmpl); [Electron Security Tutorial](https://www.electronjs.org/docs/latest/tutorial/security); [Doyensec Electronegativity](https://github.com/doyensec/electronegativity); [Cherry Studio (production app)](https://github.com/CherryHQ/cherry-studio); [LobeChat apps/desktop](https://github.com/lobehub/lobe-chat/tree/main/apps/desktop); [Joplin packages/app-desktop](https://github.com/laurent22/joplin/tree/dev/packages/app-desktop); [PR #11 Plan-007-partial / Plan-008-bootstrap precedent (`a230a50`)](https://github.com/sawmonsab/ai-sidekicks/pull/11); [`apps/desktop/src/index.ts:4` forward-declaration comment from PR #6](../../apps/desktop/src/index.ts).
- Exit Criteria: Plan-001 / Plan-024 BLOCKED-ON-C3 tags resolved with Plan-023 Tier 1 Partial citation ✅; cross-plan-deps §2 / §3 / §5 coordinated (`apps/desktop/src/{main,preload}/` + `apps/desktop/src/renderer/` ownership rows + Plan-023-partial ASCII row + Tier 1 row + Tier 8 row + new Substrate-vs-Namespace Carve-Out subsection citing the orchestrated 5-axis research synthesis) ✅; Plan-001 Phase 5 first-code-execution PR unblocked (T5.2 + T5.3 task bodies authored) ✅; canonical path `apps/desktop/src/main/sidecar-lifecycle.ts` consistent across Plan-001 + Plan-024 + Plan-023 + Plan-026 + cross-plan-deps + container-architecture.md + component-architecture-desktop-app.md + Plan-007 + Plan-019 + ADR-016 + reference/design-audits/architecture-ops.md + 17 renderer-extender plans ✅; architecture-tier authority (container-architecture.md + component-architecture-desktop-app.md) amended in lockstep with the plan-tier canonicalization ✅; transient research artifacts surface-forward to this archive entry per AGENTS.md ✅.
- Resolved: 2026-04-30.
