# Plan-014: Artifacts Files And Attachments

| Field | Value |
| --- | --- |
| **Status** | `approved` (restored 2026-08-12 by the relay-scope targeted readiness-audit delta — the delta the 2026-07-08 cross-node relay growth obligated when Tasks 7–10, the two Postgres `artifact_relay_*` tables, and the new upload/fetch wire surface re-opened this plan's Preconditions boxes. One critical, four major, six minor, all repaired in the same swap; the critical **ratified** the unowned in-envelope application-payload discriminator as a Plan-008-owned registration seam (D-014-5 / CP-014-4 ⇄ Plan-008 CP-008-16) rather than leaving Task 7's attestation leg unbuildable. Flipped from `approved` 2026-07-08 under the Status Flip Rule's new-contract-surface row, the same rule that governed this plan's own Tier-7 flip) |
| **NNN** | `014` |
| **Slug** | `artifacts-files-and-attachments` |
| **Date** | `2026-04-14` (Tier-7 readiness audit 2026-06-15; flipped to `review` then re-promoted to `approved` in the audit swap; amended 2026-07-08 — cross-node relay scope growth per the [ADR-015 amendment](../decisions/015-v1-feature-scope-definition.md#amendment-2026-07-08-v11-deferred-features-3--2-cross-node-shared-artifacts-pulled-into-v1), flipped back to `review` pending the audit delta; key-model hardening 2026-07-09 per Codex rounds 2–4 — durable per-node artifact-encryption keys with retire-not-replace rotation, per-node ack-driven delivery, lifecycle envelope) |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-014: Artifacts Files And Attachments](../specs/014-artifacts-files-and-attachments.md) |
| **Required ADRs** | [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md), [ADR-010](../decisions/010-paseto-webauthn-mls-auth.md) (the relay crypto posture Tasks 7–8 are normative against: XChaCha20-Poly1305 content encryption, X25519 CEK wrapping, PASETO v4 fetch tokens, DPoP binding — and the session-ephemeral-key category I-014-6 forbids wrapping to; **added by the 2026-08-12 relay-scope targeted readiness-audit delta**, the Plan-006 T4.10 precedent) |
| **Dependencies** | [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (artifact_publication event taxonomy + EventLogService.append; **also the `DaemonCredentialProvider` credential seam, CP-006-13** — Tasks 7–8's daemon→control-plane upload and fetch-mint calls are the seam's fifth registered caller per CP-014-5, added 2026-08-12 by Codex PR #326 round 2), [Plan-007-partial](./007-local-ipc-and-daemon-control.md) (artifact.\* IPC namespace registry substrate — Task 7's `artifact.keyAttestationServe`/`artifact.keyAttestationDeliver` bridge pair registers on it alongside Task 5's handlers), [Plan-008-bootstrap](./008-control-plane-relay-and-session-join.md) (Tier 1, shipped — control-plane tRPC/HTTP server substrate hosting the Task-7/Task-8 artifact relay upload/fetch routes), [Plan-008-remainder](./008-control-plane-relay-and-session-join.md) (Tier 5 — the R4 participant relay-crypto layer: the Task-7 artifact-key attestation exchange rides its pairwise-encrypted channels, so bootstrap alone does **not** satisfy this edge; Tier 5 precedes Plan-014's Tier 7), [Plan-025 Tier 1 Partial](./025-self-hostable-node-relay.md) (Tier 1, shipped — `packages/crypto-paseto` PASETO v4.public primitives consumed by the Task-8 fetch-token leg), [Plan-021](./021-rate-limiting-policy.md) (Tier 6 — rate-limit middleware + the six Spec-021 relay registry rows; Plan-014 self-wires `rateLimitProcedure` at Tasks 7–9 per BL-146 / D-021-6), [Plan-018](./018-identity-and-participant-state.md) (Tier 5 — the PASETO v4.public **issuer key** and the `AuthenticatedIdentityContext` shape the Task-8 `ArtifactFetchAuthorize` mint binds `sub` to; `packages/crypto-paseto` supplies the signing primitive only, never a verified caller identity. Plan-008-remainder holds the same Tier-5 edge for the same surface — F-008r-C / CP-008-4. `RelayConnectionTokenIssuer` is **not** consumed here: it mints relay-connection tokens for the WSS handshake, a different token family from the `(participant, node)`-scoped fetch token, which is a Plan-014-owned composition over Plan-018's issuer key; edge declared by the 2026-08-12 relay-scope targeted readiness-audit delta), [Plan-003](./003-runtime-node-attach.md) (Tier 3 — `runtime_node_attachments`: the Task-8 `ArtifactFetchAuthorize` node selector is the attach response's server-minted `attachmentId`, and the issuer derives `node_id` from that row under equality against the authenticated `sub`, never from a client-supplied raw node identity — I-014-10; adopted by the 2026-08-12 relay-scope targeted readiness-audit delta on the recorded Plan-006 T4.10 shape — the selector's daemon-side **carrier** is owed per the §Preconditions "Fetch-selector daemon carrier registered" box, Codex PR #326 round 1) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |
| **References** | [Updated Spec-014](../specs/014-artifacts-files-and-attachments.md) (OCI manifest envelope, CAS) |

## Goal

Implement immutable artifact publication, attachment ingestion, and manifest-first visibility handling across local and shared session contexts.

## Scope

This plan covers artifact ids and manifests, attachment ingest, payload storage, visibility classification, shared-replication state, and (per the 2026-07-08 scope growth) the cross-node artifact relay: eager-pin ciphertext upload, authenticated `(participant, node)`-scoped fetch with ack-driven delivery, refcount/TTL GC with quota accounting, and erasure fan-out.

## Non-Goals

- Full artifact preview UX
- Notification behavior for artifact publication
- Remote object-store vendor selection

## Preconditions

- [x] Paired spec is approved — re-opened 2026-07-08: [Spec-014](../specs/014-artifacts-files-and-attachments.md) flipped `approved` → `review` in the same cross-node relay amendment. Tasks 1–6 trace to the previously-approved Spec-014 content (the Tier-7 audit basis); Spec-014 re-earning `approved` gates Tasks 7–10 dispatch together with the readiness-audit delta below. **Delivered 2026-08-12** — Spec-014 restored `review → approved` by the relay-scope targeted readiness-audit delta in the same swap as this plan.
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [ ] **Fetch-selector daemon carrier registered** (minted born-unchecked 2026-08-12 at the Codex PR #326 round-1 fold; scoped hold: **Task 8's `ArtifactFetchAuthorize` mint leg only** — Task 7, Task 9, Task 10, and the rest of Task 8 are unaffected). The delta adopted the attach response's server-minted `attachmentId` as the fetch-authorization node selector, but the mint's caller is the **daemon-side** relay transfer service while the attach response returns to the client — the daemon is not a party to `runtimenode.attach` per the canonical signing-key registry — and the renderer→daemon attachment-ID delivery machinery (`event.deliverAttachmentId`, `daemon_attachment_deliveries`, the AttachFlow forward/replay) was deleted doc-only 2026-08-11 by the admission-time registration decoupling (PR #323, NS-58). No durable daemon-accessible carrier of the selector therefore exists, and after a daemon restart the mint cannot present it. A lead-owned amendment must register the carrier — a client→daemon `attachmentId` delivery with daemon-local persistence, or a re-derived issuer-side lookup that resolves the caller's attachment row from authenticated identity without a client-carried selector — before Task 8's mint leg dispatches; the selector's **shape** (validated opaque `attachmentId` over the caller's own rows, node half issuer-derived, I-014-10 unchanged) stands as adopted either way. The same class as the NS-54 admitting-principal carrier box that holds Plan-012 T2.2/T2.7.
- [x] **Plan-readiness audit complete per [runbook](../operations/plan-implementation-readiness-audit-runbook.md)** — the box is re-opened 2026-07-08: the Tier-7 audit (2026-06-15) covered Tasks 1–6 and stands for them, but the cross-node relay growth (Tasks 7–10, the two `artifact_relay_*` Postgres tables, the daemon-local `artifact_encryption_keys` durable-key store, the upload/fetch wire surface, and the 2026-07-09 artifact-key attestation exchange — whose in-envelope payload demux tag the delta pins) post-dates it and needs a **readiness-audit delta** before any Plan-014 code dispatch. Tasks 7–10 additionally gate on **Plan-008-remainder (Tier 5)** shipping the R4 pairwise relay-crypto channels the attestation exchange rides — Plan-008-bootstrap alone does not satisfy that edge. Original audit record: 10 findings adjudicated via A-014-1..6; D-014-1..4 ratified. The plan was flipped to `review` during the audit (the OCI-envelope columns + CP-014-1 are a NEW contract surface) and **re-promoted to `approved`** in the same swap on ratification of the OCI-envelope/wire bundle (D-014-1 manifest columns, D-014-2 dedicated `annotations` column, D-014-3 embedded-manifest wire) and resolution of the `artifactType` discriminator (D-014-4 — six values). Companion amendments: api-payload-contracts.md (`ArtifactManifest` envelope + `ArtifactType` union), local-sqlite-schema.md (`artifact_manifests` `subject`/`size_bytes`/`annotations`/`replication_status`), spec-014.md (six-value discriminator at `Spec-014 §Interfaces And Contracts` + `Spec-014 §Resolved Questions and V1 Scope Decisions`; content-only, Spec-014 stays `approved`), cross-plan-dependencies.md §3/§4 (Plan-006 + Plan-007-partial edges). **Delivered 2026-08-12** — the relay-scope targeted readiness-audit delta ran against the 2026-07-08 amendment's growth and its 2026-07-09 / 2026-08-08 hardening rounds; one critical, four major, six minor, all repaired in-swap; Status restored `approved` on this plan and Spec-014 together. The clause above reading "whose in-envelope payload demux tag the delta pins" is **discharged**: the delta **ratified** the tag as D-014-5 — a Plan-008-owned envelope-interior application-payload discriminated union (`RelayApplicationPayload`, tag member `kind`) with a consumer registration seam, recorded here as CP-014-4 and on the Plan-008 side as CP-008-16 — rather than leaving Task 7's attestation leg unbuildable or minting a competing tag from a consumer plan. No new Preconditions box is minted for the demux tag. Tasks 7–10 clear the audit and Status gates on this swap; what still holds their dispatch is §5 tier order (Plan-008-remainder at Tier 5, Plan-018 at Tier 5, Plan-021 at Tier 6 — all hard upstreams) and phase decomposition, since this plan carries no `### Phase` headers — and, for Task 8's `ArtifactFetchAuthorize` mint leg alone, the "Fetch-selector daemon carrier registered" box above (minted born-unchecked at the Codex PR #326 round-1 fold).

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/artifacts/`
- `packages/runtime-daemon/src/artifacts/artifact-publish-service.ts`
- `packages/runtime-daemon/src/artifacts/attachment-ingest-service.ts`
- `packages/runtime-daemon/src/artifacts/payload-store.ts`
- `packages/control-plane/src/artifacts/artifact-manifest-service.ts`
- `packages/control-plane/src/artifacts/artifact-relay-service.ts` (2026-07-08 growth — blob-store coordination, fetch authorization, GC/quota)
- `packages/runtime-daemon/src/artifacts/relay-transfer-service.ts` (2026-07-08 growth — CEK generation/wrapping, chunking, manifest signing, resumable upload/fetch client)
- `packages/control-plane/src/artifacts/relay-blob-store.ts` (2026-08-12 relay-scope readiness-audit delta — the injected chunk-bytes port CP-014-3's "deployment-agnostic" promise composes against)
- `packages/control-plane/src/artifacts/fetch-token-issuer.ts` (2026-08-12 relay-scope readiness-audit delta — the Plan-014-owned fetch-token composition over Plan-018's issuer key, and both halves of the DPoP leg)
- `packages/client-sdk/src/artifactClient.ts`
- `apps/desktop/src/renderer/src/artifacts/`

## Data And Storage Changes

- Add durable `artifact_manifests`, `artifact_payload_refs`, and replication-status records with provenance, visibility class, and producer metadata.
- Keep manifest storage separate from large payload storage while preserving content-addressed lookup or equivalent immutable payload identity.
- Treat any redacted or summarized shared form as a separate derivative artifact record rather than as in-place mutation metadata on the original artifact.
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions.
- (2026-07-08 growth) Add the control-plane `artifact_relay_blobs` + `artifact_relay_recipients` tables per [Shared Postgres Schema §Artifact Relay Blob Store](../architecture/schemas/shared-postgres-schema.md#artifact-relay-blob-store-plan-014) — blob lifecycle metadata, per-`(participant, node)` wrapped CEKs (durable artifact keys, thumbprint-tagged), delivery refcounts, fetch grace leases. Ciphertext chunk bytes live in the deployment's object store, never in Postgres. Both participant references join the CP-022-6 erasure closure (see §Cross-Plan Obligations CP-014-2).
- (2026-07-08 growth) The `artifact_manifests.replication_status` CHECK realizes the value set the Tier-7 audit deferred (A-014-3), now spec-named: `pending_replication | pinned | over_cap | quota_exceeded | expired`.
- (2026-07-08 growth, enumerated by the 2026-08-12 relay-scope readiness-audit delta) Add `artifact_manifests.relay_cek_ciphertext` — the publisher-retained, master-key-wrapped per-artifact CEK ([Local SQLite Schema §Artifact Tables](../architecture/schemas/local-sqlite-schema.md#artifact-tables-plan-014)). Written at Task 7's publish leg and read at every later re-publish as the re-wrap source, so a late joiner is granted access without the publisher re-encrypting the payload. Never leaves the publishing daemon and is never event-carried.
- (2026-07-09 key model) Add the daemon-local `artifact_encryption_keys` table per [Local SQLite Schema §Artifact Tables](../architecture/schemas/local-sqlite-schema.md#artifact-tables-plan-014) — one row per `(participant, key_thumbprint)` with a partial unique index pinning exactly one active key; rotation **retires** (sets `retired_at`) rather than replaces, and retired rows persist until every relay row bearing their thumbprint is delivered or TTL-expired. The row joins the Spec-022 **Path-1** crypto-shred set (CP-014-2).

## API And Transport Changes

- Add `ArtifactPublish`, `ArtifactRead`, `ArtifactVisibilityUpdate`, and `AttachmentIngest` to shared contracts and the typed client SDK.
- (2026-07-08 growth, extended 2026-07-09) The relay wire surface adds `ArtifactUploadInit` / `ArtifactUploadChunk` / `ArtifactUploadComplete` / `ArtifactFetchAuthorize` / `ArtifactFetchComplete` — five named methods which, together with the per-chunk authenticated fetch GETs, carry the six [Spec-021](./021-rate-limiting-policy.md) registry rows (`artifact.upload.init/chunk/complete`, `artifact.fetch.authorize/chunk/complete`); `ArtifactFetchComplete` is the authenticated post-verification delivery ack (I-014-8).
- Return manifest metadata first and use explicit payload handles for large or deferred content reads.
- (2026-07-08 growth) Add the relay transfer surface per [Spec-014 §Cross-Node Artifact Relay (V1)](../specs/014-artifacts-files-and-attachments.md#cross-node-artifact-relay-v1): `ArtifactUploadInit` / `ArtifactUploadChunk` / `ArtifactUploadComplete` (resumable, received-set discoverable) and `ArtifactFetchAuthorize` (`(participant, node)`-scoped per I-014-10, DPoP-bound, 5–15 min PASETO fetch token) + per-chunk authenticated GETs, then `ArtifactFetchComplete` (the post-verification delivery ack, I-014-8); additive `artifact.published` event fields (`ciphertextDigest`, `chunkSize`, `chunkCount`, `chunkManifest`/`manifestDigest`, `cekCommitment`, `retentionTier` — wrapped CEKs are relay-held per-recipient rows returned at fetch-authorize, never event-carried); `replicationStatus` value-set refinement on the existing manifest field.

## Invariants

- **I-014-1 (Immutability):** A published artifact's payload is content-addressed (SHA-256) and never mutated in place; a live workspace path is never treated as durable payload identity. (`Spec-014 §Required Behavior`; verified by Task 2, Task 3.)
- **I-014-2 (Derivative-not-mutation):** A redacted/summarized shareable form is a separate manifest (own id, `subject` → original); the original manifest is never UPDATEd in place. (`Spec-014 §State And Data Implications`; verified by Task 4.)
- **I-014-3 (Relay ciphertext-only):** No relay-side store (Postgres coordination rows or object-store chunks) ever holds plaintext payload or an unwrapped CEK; every relay-held key is wrapped to a durable per-`(participant, node)` artifact-encryption key the relay does not possess. (`Spec-014 §Cross-Node Artifact Relay (V1)` Required Behavior; verified by Task 7, Task 8, Task 10.)
- **I-014-4 (Loud integrity):** A chunk failing its signed-manifest digest is rejected and re-requested; an unwrapped CEK failing the signed `cekCommitment` refuses decrypt; a payload failing the post-decrypt CAS digest surfaces an integrity error — no code path yields silently-wrong bytes. (`Spec-014 §Cross-Node Artifact Relay (V1)` failure modes; verified by Task 8.)
- **I-014-5 (Pinned means available):** `replicationStatus` reaches `pinned` only after every ciphertext chunk is relay-acknowledged **and the upload finalize has verified the assembled ciphertext against the ciphertext digest declared at upload-init** (the relay's CAS key — never the plaintext CAS digest, which the relay cannot compute; the CAS key is verified, never trusted; a re-pin re-verifies the stored copy, so re-publish repairs at-rest corruption, and re-anchors `expires_at` to the tier TTL — a fresh grant of the same bytes), and a `pinned` artifact's fetch path never depends on publisher reachability; publishes that cannot pin surface `over_cap` / `quota_exceeded` on the manifest (no blob row exists for them) — never a silent local-only fallback. (`Spec-014 §Cross-Node Artifact Relay (V1)`; verified by Task 7, Task 9.)
- **I-014-6 (Durable-key wrap):** A relay CEK is wrapped only to a durable artifact-encryption key (daemon-local `artifact_encryption_keys`, Spec-022 master-key custody, Ed25519-identity-attested) — never to an ADR-010 session-ephemeral X25519 key; a recipient daemon restarted between publish and fetch still unwraps its relay-held CEK, thumbprint-selected. (Spec-014 Publish step 3 + §Resolved Questions 2026-07-09; verified by Task 7, Task 8.)
- **I-014-7 (Per-node delivery):** Delivery tracking and the refcount-zero GC trigger operate at `(participant, node)` granularity — one participant's fetch on one node never truncates another of their nodes' pending delivery; erasure removes all of a participant's node rows in one participant-keyed statement. (Spec-014 Delete steps 8–9; verified by Task 9, Task 10.)
- **I-014-8 (Ack-driven delivery):** `delivered_at` is written **only** by an authenticated `ArtifactFetchComplete` ack sent after the fetching node's chunk-manifest, `cekCommitment`, and CAS verifications all succeed — never inferred from a chunk GET; an un-acked fetch leaves the blob to its TTL rather than risking a premature GC of the last relay copy. The acked row is resolved from the fetch token's own `(participant, node)` claims (I-014-10), never from a caller-supplied node id, and the ack is idempotent. (Spec-014 Fetch step 6 + Delete step 8; verified by Task 8, Task 9.)
- **I-014-9 (Rotation retains):** A retired artifact-encryption key is retained (thumbprint-keyed, never used for new wraps) until every relay row wrapped to it is delivered or TTL-expired, so rotation never strands a within-TTL pinned blob behind an offline publisher; exactly one active key exists per participant per node. (Spec-014 Publish step 3; verified by Task 7, Task 8.) The daemon evaluates the retention predicate locally as `retired_at + 30 d + a 48 h skew margin` (the `extended`-tier TTL ceiling bounds it on the relay clock; the margin absorbs daemon-vs-relay skew) rather than querying the relay. The rule's second leg is **no late wraps**: rotation immediately re-attests the new active key to session peers and publishers wrap to the latest attestation per `(participant, node)`, so a wrap to a retired thumbprint is confined to the seconds-scale re-attestation race and every row bearing it TTL-expires inside the purge bound — a session-frozen attestation cache would let stale wraps outlive the purge.
- **I-014-10 (Node-bound fetch authorization):** A relay fetch token is scoped to `(participant, node)`, the binding made at mint per RFC 9449 §5: the issuer stamps `(participant, node)` as issuer-signed token claims, and sets the `cnf` confirmation claim from that call's DPoP proof, with no standing thumbprint→node registry (a bare `cnf`/jkt thumbprint is possession material, not an identity). **What the invariant prohibits, precisely** (narrowed 2026-08-08 at round 3): the node-claim **value** is always **issuer-derived from the caller's own attach-state rows** and a **client-supplied raw node identity is never trusted**. It does _not_ prohibit a request field as such — a **validated opaque selector that picks among the caller's own attachment rows** is permissible if the deferred audit adopts one, because the issuer still derives the value and still refuses any row not owned by the authenticated `sub` (this is what keeps the invariant compatible with Task 8's recorded `attachmentId` candidate). The relay selects the wrapped CEK and resolves the `ArtifactFetchComplete` target row from the issuer-signed claims after verifying possession, so a node presenting a token minted for itself can neither read a sibling node's wrapped CEK nor mark that sibling delivered. **Mint-time authorization is participant-granular, not node-granular** (scoped 2026-08-08 — the prior wording, "the authenticated node-attach context", named a mechanism V1 does not supply: `ArtifactFetchAuthorize` is a control-plane route whose access token carries `sub` and `cnf` and no node claim, and **V1 defines no node-identity credential** per [security-architecture.md §Per-Event Daemon Signature](../architecture/security-architecture.md#per-event-daemon-signature)), the same bound the Plan-006 T4.10 registrar resolves at, so a same-participant sibling daemon obtaining a token for another of that participant's nodes is not cryptographically excludable. **The V1 residual is availability and attestation/attribution integrity — not delivery attribution alone, and not confidentiality** (corrected across PR #301 rounds 1 and 3: round 0's "delivery attribution only" was too narrow, round 1's "confidentiality" was wrong). **(a) Availability:** `ArtifactFetchComplete` proves no CEK unwrap, so a compromised sibling forges the ack, clears the blob's last outstanding row, and refcount-zero GC destroys it; the honest node's own row cascades away with the blob so it does surface `artifact.no_access_key`, but that error's remedy is a publisher re-publish — unavailable in exactly the publisher-offline case the guarantee exists for. **(b) Attestation integrity (victim-node lockout):** the verifier's check is only that an attestation is signed by _a_ long-term Ed25519 identity key registered to that participant, and a participant carries several — each workstation holds its own, separately registered ([trust-and-identity.md §Edge Cases](../domain/trust-and-identity.md#edge-cases); no key is shared or copied between nodes) — so nothing binds the signing identity to the `node_id` it attests, and any participant-authorized registered identity can bind an attacker-held X25519 key to a victim node's `node_id` and receives the CEK of every artifact published after the spoof (Spec-014 Publish step 3). Neither leg widens plaintext reach — an attached sibling is already an intended recipient holding the CEK under its own attested key, and plaintext reach by an authorized node is participant-granularity sharing working as designed; the reachable harm is that the honest sibling is locked out and attribution is corrupted. Cross-participant availability and confidentiality are untouched. Spec-014 §Required Behavior, the §Failure modes publisher-offline row, and both §Acceptance Criteria rows now carry the matching threat-model scope; the closure for all three legs is the [cross-plan-dependencies.md §V1.1+ Cross-Plan Extensions](../architecture/cross-plan-dependencies.md#v11-cross-plan-extensions) Plan-014 row (added here — four rows now share the Plan-018/Plan-003 node-identity primitive). (Spec-014 Fetch steps 5–6; verified by Task 8.)

## Cross-Plan Obligations (provided)

- **CP-014-1 → Plan-011:** Plan-014 ships the OCI manifest envelope (incl. `subject`, `annotations`, `size`, `digest`=content_hash) and the `artifact_manifests`/`artifact_payload_refs` tables that Plan-011's `diff_artifacts.artifact_manifest_id` FK references. Plan-011's DiffArtifact (`artifactType: "diff"`) consumes this envelope (`Plan-011 §Cross-Plan Obligations`, `Spec-011 §DiffArtifact and General Artifact Relationship`).
- **CP-014-2 ⇄ Plan-022 (CP-022-6 reciprocal):** Plan-014's `artifact_relay_recipients.participant_id` (hard-DELETE class — dropping the rows IS the wrapped-CEK crypto-shred and removes the participant from the intended-recipient set) and `artifact_relay_blobs.publisher_participant_id` (anonymize class — born nullable + `ON DELETE SET NULL`; the blob survives to refcount-zero/TTL) join the `REFERENCES participants(id)` Path-2 erasure closure. Plan-014's daemon-local `artifact_encryption_keys` row additionally joins the **Path-1** local crypto-shred set (its private half unwraps relay-held CEKs, so leaving it behind would preserve the reach the Path-2 deletion severs — `Spec-022 §Shred Fan-Out` Path 1). Plan-014 owes both reciprocals at its relay-table shipment: Path-1 deletes the participant's `artifact_encryption_keys` row on every node they run; Path-2 erasure fan-out reaches the relay rows on every relay node (all of the participant's node rows in one participant-keyed DELETE), an audit event is emitted, and the backup posture holds one of `Spec-014 §State And Data Implications`' two mechanisms — PITR/backup window bounded ≤ the erasure SLA, or wrapped-CEK envelopes under a separately-destroyable KEK (`Spec-022 §Daemon Master Key` precedent). (Task 10.)
- **CP-014-3 → Plan-025:** Plan-014 CREATEs the artifact-relay service (deployment-agnostic contract + control-plane implementation); Plan-025's self-host relay node EXTENDs its composition to serve the same blob-store HTTP surface with filesystem/S3-compatible backing and exposes the Spec-014 operator knobs (`node_relay_storage_max`, `artifact_relay_ttl` tier presets, quotas) through its config surface — deployment composition only, no service-logic fork. The composition is a **named Plan-025 deliverable** — step 28 / T-025d-28-1 (registered 2026-08-12, Codex PR #326 round 2 — previously no Plan-025 task owned this obligation): the `relay-blob-store.ts` filesystem adapter selection backed by a Compose `artifact-blobs` named volume (S3-compatible backing = operator endpoint configuration, not a new service), the `relay-limits.ts` operator overrides through `env.ts`, and the artifact HTTP surface mounted on the relay-node server.
- **CP-014-4 → [Plan-008](./008-control-plane-relay-and-session-join.md):** Plan-014 **consumes** the envelope-interior application-payload discriminated union Plan-008 owns in `packages/contracts/src/session-join.ts` (`RelayApplicationPayload`, discriminating on the required string tag member `kind` drawn from `RelayApplicationPayloadKind`), and **registers one arm** through its consumer registration seam: `artifact_key_attestation`, carrying Task 7's Ed25519-identity-signed artifact-key attestation. The union's tag, its exhaustiveness rule, and its fail-closed unknown-kind behavior are authored under Plan-008 (reciprocal CP-008-16); the attestation payload schema itself stays in this plan's own contract domain, `packages/contracts/src/artifacts/`, and is referenced by the arm rather than moved. **Fail-closed:** a decrypted envelope payload whose `kind` is absent or unrecognized is dropped with a logged refusal and never dispatched — an unregistered kind is never treated as an attestation. Ratified 2026-08-12 by the relay-scope targeted readiness-audit delta as D-014-5; additive-only and unshipped on both sides, so neither Plan-008 nor Spec-008 changes Status. Task 7's attestation leg names the registered kind; nothing about this obligation is owed forward.
- **CP-014-5 → [Plan-006](./006-session-event-taxonomy-and-audit-log.md) / [Plan-018](./018-identity-and-participant-state.md):** Plan-014's daemon-resident relay transfer service **consumes** the `DaemonCredentialProvider` credential seam (Plan-006 CP-006-13; interface authoring decomposed at Plan-018's NS-62 promotion pass, 2026-08-15, per CP-018-12 — T5.4–T5.7) as the seam's **fifth registered caller** (registered 2026-08-12, Codex PR #326 round 2 — the CP-024-5 fourth-caller registration precedent, PR #279, which widened the enumerations without flipping either provider plan): every daemon→control-plane HTTP call this plan makes — Task 7's `ArtifactUploadInit` / `ArtifactUploadChunk` / `ArtifactUploadComplete` and Task 8's `ArtifactFetchAuthorize` mint — presents the participant access token + per-request DPoP proof minted fresh per attempt via `mintForAttempt({sessionId, nodeId, htm, htu})`, never a bespoke credential path. Task 8's fetch legs additionally scope the seam's one growth: the **proof-for-a-presented-token affordance** — chunk GETs and `ArtifactFetchComplete` present the _fetch token_ rather than the daemon's access token, so the daemon needs DPoP proofs whose `ath` hashes that presented token, computed under the **same daemon-held proof key** the token's `cnf.jkt` was bound from at mint. This bullet widens the caller enumeration and scopes the affordance; it authors no Plan-006- or Plan-018-owned symbol — the affordance is authored at Plan-018 T5.6 (`mintProofForPresentedToken`, registered on the Plan-006 T3.3 declaration by the NS-62 promotion pass, 2026-08-15; the seam-wide composition-root wiring is Plan-018 T5.7).

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

#### Tasks

- **Task 1 — Artifact contracts.** In `packages/contracts/src/artifacts/`, define `ArtifactManifest` (full OCI envelope per D-014-1), `PayloadHandle`, `ArtifactVisibility` (`"local-only" | "shared"`), `ArtifactState` (`"pending" | "published" | "superseded"`), and the `AttachmentIngest` request/response; expose the subdirectory through an `artifacts/index.ts` barrel re-exported from the package root `packages/contracts/src/index.ts` (the repo single-import-surface convention — the Plan-021 T21.1-1/T21.1-2 `index.ts` re-export precedent), so consumers import these symbols from `@ai-sidekicks/contracts` and `package.json` keeps exporting only `"."`. The artifact event/manifest payload schemas are **non-strict — unknown-key passthrough** — per [ADR-018](../decisions/018-cross-version-compatibility.md) MINOR additivity: a pre-relay client must ignore-and-preserve, never reject, the relay-pinned `artifact.published` additive fields (Spec-006 §Artifact and Diff Publication names this an enforcement requirement, not intent — the package's `.strict()` convention is deliberately NOT applied to `artifact.*` payloads). **Assertion:** schemas parse the api-payload-contracts.md ArtifactPublish/Read/VisibilityUpdate/AttachmentIngest shapes round-trip; parsing an `artifact.published` payload bearing unknown additive fields succeeds and preserves them (the ADR-018 mixed-version guarantee). The `ArtifactType` union covers the closed six-value discriminator (Spec-014 line 79: `file | diff | summary | log | design | workflow_output`) — RESOLVED (D-014-4). **Spec coverage:** Spec-014 lines 72-77 (interfaces), lines 78-79 (envelope + discriminator). **Consumes:** ArtifactState, ArtifactVisibility, ArtifactId ← api-payload-contracts.md lines 171,172,57 (by SHAPE); ArtifactType ← Spec-014 line 79 reconciled (RESOLVED D-014-4: closed six-value union `file | diff | summary | log | design | workflow_output` in api-payload-contracts.md).

- **Task 2 — Attachment ingest + immutable payload store.** Implement `runtime-daemon/src/artifacts/attachment-ingest-service.ts` and `payload-store.ts`: normalize name/media-type/size, compute SHA-256, write the CAS-keyed payload blob, and — **in one transaction** — persist the attachment's own `artifact_manifests` row (`state` = `published`, `visibility` from session policy, default `local-only`: an uploaded attachment _is_ a published local artifact the moment it is ingested, Spec-014 line 58 + line 153) followed by its `artifact_payload_refs` row (manifest first, to satisfy the `manifest_id NOT NULL REFERENCES artifact_manifests(id)` FK — [`artifact_payload_refs`](../architecture/schemas/local-sqlite-schema.md#artifact-tables-plan-014)), and return the minted `ArtifactId` (the manifest row's `id`). AttachmentIngest and ArtifactPublish (Task 3) are **two independent producers** — ingest does **not** defer manifest persistence to publish; each writes its own manifest from its own payload, so the `artifactId` `AttachmentIngestResponse` returns resolves immediately via `ArtifactRead` (no orphaned id). **Assertion:** ingesting the same bytes twice yields one CAS entry (dedup at the content-hash level) but two distinct manifests, each with its own payload-ref; the ingested artifact has a stable, resolvable id and normalized media_type/size_bytes. **Spec coverage:** Spec-014 line 47 (stable ids **and** provenance), line 58 (uploaded attachments default to local artifact storage), line 77 (normalize name/media/size), AC line 177 (ingestion produces stable ids **and manifests**). **Verifies invariant:** I-014-1 (immutability — A-014-6).

- **Task 3 — Artifact publication + manifest persistence.** Implement `runtime-daemon/src/artifacts/artifact-publish-service.ts` and `control-plane/src/artifacts/artifact-manifest-service.ts`: compute the SHA-256 of `ArtifactPublishRequest.payload` (this producer's **own** payload — a run output, not a prior ingest's blob), write the CAS blob, and persist `artifact_manifests` (incl. `subject`, `size_bytes`, `annotations` per D-014-1 / D-014-2 — `annotations` a dedicated OCI string-map column, not freeform `metadata`) and, **in the same transaction**, the `artifact_payload_refs` row linking the manifest to that own CAS blob (`manifest_id` → the just-inserted manifest id, `storage_path` → the CAS key) — manifest first because the payload-ref FK (`manifest_id NOT NULL REFERENCES artifact_manifests(id)`, [`artifact_payload_refs`](../architecture/schemas/local-sqlite-schema.md#artifact-tables-plan-014)) requires the manifest first; default visibility from session policy, set state. ArtifactPublish is an **independent producer** — like AttachmentIngest (Task 2) it derives its content hash from its own payload and writes its own manifest+ref; neither feeds the other (they are distinct interfaces, never a two-phase pipeline). `ArtifactPublish` returns the embedded `ArtifactManifest` envelope (Spec-014 line 74 "must return artifact id **and manifest metadata**"; `manifest.id` is the artifact id) — **not** a `manifestUrl` pointer (D-014-3: the prior pointer was wire drift from that "must" clause — line 75 grants handle/inline latitude to the _payload_ on `ArtifactRead` only, never to the manifest, so publish embeds the metadata; see api-payload-contracts.md `ArtifactManifest`). **Assertion:** published artifact is readable + attributable (producer/session/run) after the producing run ends; the `ArtifactPublish` response embeds the `ArtifactManifest` (`id`, `digest`, `size`, `annotations`, `subject?`, `visibility`, `state`, `replicationStatus?`, `createdAt`); two artifacts produced from identical bytes — whether via publish, via ingest, or one of each — yield two distinct manifests, each with its own `artifact_payload_refs` row, both pointing at the one shared CAS blob (dedup is CAS-level, not manifest-level). **Spec coverage:** Spec-014 line 40 (immutable publication + durable manifests), line 74 (ArtifactPublish returns id+manifest metadata), line 78 (OCI envelope shape), AC line 178. **Verifies invariant:** I-014-1.

- **Task 4 — Replication-status + derivative artifacts.** Persist the manifest-first replication-status surface (per A-014-3) and model derivatives as separate manifests with `subject` linkage (per A-014-4). ArtifactVisibilityUpdate enforces policy/auth. **Assertion:** a shared-visible artifact with no payload yet replicated shows the pending-replication status; a derivative is a distinct manifest row (own id) whose `subject` points to the original, and the original row is never UPDATEd in place. **Spec coverage:** Spec-014 line 66 (pending_replication fallback), line 69 (policy-blocked share retains the original and may publish a derivative), line 145 (visibility changes auditable), line 147 (derivative must be a separate artifact with its own manifest, never an in-place mutation), line 76 (visibility-update policy/auth). **Verifies invariant:** I-014-2 (derivative-not-mutation — A-014-6). **Consumes:** `replication_status` column ← Plan-014-owned (RESOLVED A-014-3): authored nullable, spec-named `pending_replication` per Spec-014:66; the multi-state CHECK the Tier-7 audit deferred arrived with the 2026-07-08 relay amendment (`pending_replication | pinned | over_cap | quota_exceeded | expired` — see Task 7 and [Local SQLite Schema §Artifact Tables](../architecture/schemas/local-sqlite-schema.md#artifact-tables-plan-014)); Task 4 exercises only the `pending_replication` arm.

- **Task 5 — Artifact events.** Emit `artifact.published` / `artifact.visibility_updated` / `artifact.superseded` via Plan-006's `EventLogService.append` with the Spec-006 payload shape; register `artifact.*` IPC handlers under Plan-007's registry. (A-014-5.) **Assertion:** publishing an artifact appends an `artifact.published` event to the session timeline with `{sessionId, artifactId, runId?, visibility, state}`; visibility change appends `artifact.visibility_updated`. **Spec coverage:** Spec-014 line 143 (manifests part of replayable session history). **Consumes:** EventLogService.append ← Plan-006; `artifact_publication` taxonomy ← Spec-006 §Artifact and Diff Publication (artifact_publication) (by SHAPE); IPC registry ← Plan-007-partial.

- **Task 6 — Desktop artifact surfaces.** In `apps/desktop/src/renderer/src/artifacts/`, render manifest rows, explicit payload-fetch action for large payloads, and visibility state. **Assertion:** a large artifact shows a manifest row + explicit fetch control and does NOT force inline payload render. **Spec coverage:** Spec-014 line 67 (large payload → manifest row + explicit fetch), AC line 179. **Consumes:** artifactClient ← `packages/client-sdk/src/artifactClient.ts` (Task 1 contracts).

- **Task 7 — Relay eager-pin upload (2026-07-08 growth).** Implement `runtime-daemon/src/artifacts/relay-transfer-service.ts` (publish leg) and the control-plane upload half of `artifact-relay-service.ts`: generate the per-artifact CEK (retained master-key-wrapped in the local manifest row's `relay_cek_ciphertext` — the re-wrap source for every later re-publish, Spec-014 Publish step 1), encrypt (XChaCha20-Poly1305 + the signed `cekCommitment` binding — AEAD alone is not key-committing, Spec-014 Publish step 1), chunk at 8 MiB, hash each ciphertext chunk, build the signed chunk manifest (incl. `cekCommitment`) into the `artifact.published` event, wrap the CEK per attested `(participant, node)` durable artifact-encryption key — the publishing node itself excluded: it holds the plaintext and takes no recipient row, so no blob's refcount ever waits on its publisher (Spec-014 step 3) — into `artifact_relay_recipients` rows (thumbprint-tagged; the wrapped CEKs' only store — never event-carried; **never wrapped to session-ephemeral keys**, I-014-6) — the key plane ships here too: generate the daemon keypair into `artifact_encryption_keys` **at the node's first session attach — never lazily at first fetch** (master-key-wrapped per Spec-022 custody; rotation sets `retired_at` and keeps the old row until its thumbprint's relay rows are delivered or TTL-expired — I-014-9, never an in-place replace), send/receive the Ed25519-identity-signed artifact-key attestations **as a join-sequence obligation immediately after the pairwise handshake, and re-attest immediately on rotation** — application payloads inside the existing pairwise ciphertext envelope over Plan-008-remainder's R4 channels (no new relay frame type; the payload is the typed **`ArtifactKeyAttestationPayload`** ([api-payload-contracts.md §Plan-014](../architecture/contracts/api-payload-contracts.md) — named 2026-08-12, Codex PR #326 round 2, so Plan-008's T-008r-1-3 has a schema to build the union arm from; the runtime Zod schema lands in `packages/contracts/src/artifacts/`, and its signature preimage is the literal Spec-014 concatenation `session_id ‖ node_id ‖ artifact_public_key`, injective because both ids are fixed-length UUIDs — the Plan-008 bundle-signature convention), riding Plan-008's envelope-interior `RelayApplicationPayload` discriminated union under the registered tag value `kind: "artifact_key_attestation"` — ratified 2026-08-12 as D-014-5 and registered through CP-014-4, with an absent or unrecognized `kind` dropped fail-closed rather than dispatched), reject unattested or signature-invalid peer keys, and cache the latest attestation per `(participant, node)` (last-received-wins on the ordered authenticated channel — never a session-frozen snapshot, I-014-9) — the attestation legs cross the local client↔daemon boundary through the Task-7-registered **`artifact.keyAttestationServe`** / **`artifact.keyAttestationDeliver`** JSON-RPC pair ([api-payload-contracts.md §Artifact-Attestation Bridge Method Registry](../architecture/contracts/api-payload-contracts.md#artifact-attestation-bridge-method-registry-tier-7-plan-014-task-7); added 2026-08-12, Codex PR #326 round 2 — CP-008-14's four methods carry Plan-008's own arms only, and the custody split forces a Plan-014 pair: the durable artifact key and the recipient-key cache are daemon-held while the envelope coordinator, decryption, and the injected `Ed25519IdentitySigner` are client-side, so the daemon **serves unsigned attestation material and caches coordinator-verified bindings**, and signing plus inbound verification stay coordinator-side where identity custody and the pairwise trust layer live; registered on the `artifact.*` namespace this plan already owns, no `session.*` root extension) — and eagerly upload via `ArtifactUploadInit` — skipped entirely when the intended-recipient set is empty (a solo session): the manifest stays `pending_replication` and the first explicit re-publish performs the initial pin — (carrying the relay-visible lifecycle envelope: digest, size, chunk accounting, `retentionTier`, recipient entries)/`ArtifactUploadChunk`/`ArtifactUploadComplete` — whose finalize re-hashes the assembled ciphertext against the init-declared **ciphertext digest** (the relay CAS key — never the plaintext CAS digest, which the relay cannot compute) before anything pins (I-014-5; a mismatch rejects the upload and nothing becomes fetchable) — with received-set resume; upload-init on an already-pinned digest is the idempotent re-pin that re-verifies the stored copy's digest before short-circuiting (a damaged-at-rest copy is discarded and re-uploaded — re-publish repairs relay-side corruption), adds recipient rows, and re-anchors the blob's `expires_at` to now + the tier TTL (a re-pin is a fresh grant of the same bytes — without the lease refresh, a near-expiry re-publish grants access to a blob that dies before the joiner fetches), paired with re-emitting the `artifact.published` event into the current membership epoch (identical signed manifest content, idempotent by digest — relay rows alone are never an access grant, Spec-014 step 3) — the late-join re-publish path; write `artifact_relay_blobs` + `artifact_relay_recipients`; flip `replication_status` to `pinned` only after the last chunk acknowledges; over-cap/over-quota publishes set `over_cap`/`quota_exceeded` without uploading — the over-cap arm refuses the upload with the reserved `artifact.too_large` (413, [error-contracts.md](../architecture/contracts/error-contracts.md)) so the caller distinguishes a per-artifact size refusal from the session/participant quota refusal Task 9 answers with `429` + `Retry-After` (binding added 2026-08-12 by the relay-scope readiness-audit delta). **The chunk bytes are reached only through `packages/control-plane/src/artifacts/relay-blob-store.ts`** — the injected chunk-bytes port (`putChunk` / `getChunk` / `deleteBlob` / `statBlob`, keyed by `(ciphertextDigest, chunkIndex)`), with a single in-repo implementation selected at composition. `artifact_relay_blobs` is metadata only and never carries bytes; the concrete backing engine is a Plan-014 §Non-Goal (vendor selection) and a Plan-025 composition choice per CP-014-3 — this file is the seam that makes "deployment-agnostic" mean something, and `artifact-relay-service.ts` reaches chunk bytes only through it (no direct filesystem or SDK call in the service), so a composition swapping the implementation changes no service code. **Buy-vs-build (criteria recorded; the 2026-08-12 relay-scope readiness-audit delta left the choice at implementation — it carries no contract consequence either way, and the blob-store port above makes the swap cheap):** evaluate adopting [`@tus/server`](https://github.com/tus/tus-node-server) + `@tus/s3-store` (official tus Node implementation; actively maintained 2026; disk/S3 stores; runs on serverless runtimes incl. Cloudflare) for the resumable-upload substrate versus minimal bespoke chunk endpoints — the crypto/manifest/authorization envelope is ours either way; the evaluation criteria are the standing four (correctness, modern practice, regression surface, vulnerability surface) plus dependency health. **Assertion:** a publish within cap yields relay rows with every chunk stored and `replication_status='pinned'`; a node that attached before the publish but never fetched anything is in the wrapped-key recipient set (keygen + attestation happened at its attach, not lazily); killing the uploader mid-transfer and re-running resumes from the received-set without re-sending acknowledged chunks; an over-cap publish uploads nothing, is refused with `artifact.too_large` (413), and surfaces `over_cap`; a finalize whose assembled digest mismatches the declaration is rejected and never pins; a re-pin against a corrupted stored blob discards it and re-uploads; a publish after a peer's rotation wraps to the peer's new key (re-attestation replaced the cache), while a publish racing the in-flight re-attestation wraps to the retired key and stays fetchable until delivered/TTL — inside the purge bound; a late joiner granted access by re-publish receives a current-epoch `artifact.published` event and verifies the fetch end-to-end against it; a re-pin near expiry re-anchors `expires_at` so the joiner's fetch window is the full tier TTL; the publishing node has no recipient row of its own (no blob's refcount waits on its publisher); a solo-session publish uploads nothing, retains the CEK, and stays `pending_replication`, and the re-publish after the first peer attaches performs the initial pin that the peer then fetches end-to-end. **Spec coverage:** Spec-014 §Cross-Node Artifact Relay (V1) Publish steps 1–4, size/quota table; Spec-014 AC line 181 (relay holds ciphertext and wrapped CEKs only), AC line 188 (artifact-key generation + attestation at node attach), AC line 192 (rotation re-attests immediately). **Verifies invariant:** I-014-3, I-014-5, I-014-6, I-014-9. **Consumes:** durable artifact-encryption keys ← `artifact_encryption_keys` (Spec-022 master-key custody); attestation transport ← Plan-008 R4 pairwise-encrypted relay channels, carried as the `artifact_key_attestation` arm of Plan-008's `RelayApplicationPayload` envelope-interior union (CP-014-4); chunk-bytes persistence ← `relay-blob-store.ts` port (Plan-014-owned, this task); `artifact.published` event append ← Plan-006 `EventLogService.append`; control-plane HTTP credentials ← `DaemonCredentialProvider.mintForAttempt` (Plan-006 CP-006-13 / Plan-018 CP-018-12 — **CP-014-5**, registered 2026-08-12: the relay transfer service is the seam's fifth caller, and every upload call this task makes — `ArtifactUploadInit`/`ArtifactUploadChunk`/`ArtifactUploadComplete` — presents the participant access token + per-request DPoP proof that seam mints, never a bespoke credential path).

- **Task 8 — Authenticated relay fetch + integrity (2026-07-08 growth).** Implement the fetch half: `ArtifactFetchAuthorize` mints a `(participant, node)`-scoped, DPoP-bound (RFC 9449), 5–15 min PASETO fetch token against session membership — `node_id` stamped as an issuer-signed claim and `cnf` bound from that call's DPoP proof per RFC 9449 §5, never a trusted client-supplied raw node identity (I-014-10). **Why the node selector is needed at all** (2026-08-08, PR #301 round 1): with two nodes of one participant attached, `sub` names only the participant, `cnf` is possession material rather than an identity, and no request field disambiguates — so the issuer has multiple candidate attach rows and no rule to choose among them. **The selector is now specified** (adopted 2026-08-12 by the relay-scope targeted readiness-audit delta, on the recorded Plan-006 T4.10 shape): the caller presents the attach response's server-minted `attachmentId`; the issuer resolves the `runtime_node_attachments` row it names, requires row equality against the authenticated `sub`, and derives `node_id` from that row. It is a **validated opaque selector over the caller's own attachment rows** — the node half stays issuer-derived and a client-supplied raw node identity is still never trusted, so I-014-10 is unchanged — and a selector naming another participant's row is refused with `artifact.fetch_unauthorized`, never silently re-scoped. Mint-time authorization stays **participant-granular** per I-014-10: the selector disambiguates among the caller's own rows and deliberately does not close the compromised same-participant-sibling residual, whose closure is the §V1.1+ Cross-Plan Extensions Plan-014 row. **Carrier owed (Codex PR #326 round 1):** the mint's caller is the daemon-side relay transfer service, but the attach response carrying `attachmentId` returns to the client and the renderer→daemon delivery machinery was deleted 2026-08-11 (PR #323) — no durable daemon-accessible carrier of the selector exists yet, so this mint leg holds on the §Preconditions "Fetch-selector daemon carrier registered" box; the selector's shape is unaffected by how the carrier lands. The mint itself is implemented in `packages/control-plane/src/artifacts/fetch-token-issuer.ts` — the Plan-014-owned composition: it mints the `(participant, node)`-scoped PASETO fetch token over Plan-018's issuer key with `sub` taken from `AuthenticatedIdentityContext`, and owns **both halves of the DPoP leg** (RFC 9449 §5) — binding `cnf` from the mint call's proof, and running the **full RFC 9449 §4.3 proof validation on every token-bearing request, in both proof populations** — the mint call's own proof validates against the **participant access token it presents** (`ath` = that token's hash, `jkt` equality against _its_ `cnf.jkt`), and only after that check passes does the proof's key copy into the minted fetch token's `cnf`; every fetch-token-bearing request (every chunk GET and `ArtifactFetchComplete`) then validates against the **fetch token it presents** (two-stage split corrected 2026-08-12, Codex PR #326 round 2 — the round-1 wording required the mint proof to hash a fetch token that does not exist until the call returns; RFC 9449 §4.3 binds `ath` to whichever token the proof accompanies, exactly as the canonical statement reads: the hash of the **presented** token) — per the canonical check set [security-architecture.md §Control-Plane Authentication (Task 5.2)](../architecture/security-architecture.md#control-plane-authentication-task-52) already documents (the `{jti, htm, htu, iat, ath}` proof members): signature under the proof's own key, `jkt` equality against the presented token's `cnf.jkt`, **`htm`/`htu` equality against the receiving request's method and URI** (a proof captured from a chunk GET is refused on `ArtifactFetchComplete` — without this binding, one captured proof would let an attacker ack delivery without fetching, driving refcount-zero deletion of a blob the recipient never received), **`ath` equality against the hash of the token presented with the proof**, an `iat` freshness window, and **single-use `jti` replay rejection within that window** (a Codex PR #326 round-1 hardening — the prior text checked `cnf.jkt` key-match alone). Single-use enforcement is the injected **`DpopReplayGuard`** port (`packages/control-plane/src/artifacts/dpop-replay-guard.ts`, keyed `(jkt, jti)`, retention = the `iat` freshness window; added 2026-08-12, Codex PR #326 round 2 — process-local state cannot enforce single-use on a horizontally scaled stateless control plane, so the guard must be an atomic shared substrate or routing that gives one key one authority): the hosted composition backs it with a **per-thumbprint self-evicting Durable Object** (the Plan-021 `RateLimitEscalationDO` horizon-bounded pattern — routing by `jkt` guarantees every validation for one key reaches one authority, and the window-bounded state self-evicts), the self-host composition with the **same `rate-limiter-flexible` Postgres substrate Plan-025 already deploys** (Spec-025 AC line 174 — an atomic single-point consume on the `(jkt, jti)` key with TTL = the window is exactly single-use semantics, shared-state correct even beyond D-025-10's single-process topology, and it adds no hand-authored table). **Both in-repo backings are authored by this task** against that injected port, in `packages/control-plane/src/artifacts/`: the Durable Object class — **`DpopReplayGuardDO`**, named 2026-08-12 at the Codex PR #328 round-1 fold — is hosted-composition-only Worker code the self-host composition never consumes, while the `rate-limiter-flexible` Postgres adapter is Worker-free — so Plan-025 step 28 / T-025d-28-1 injects that adapter into the self-host composition without pulling Cloudflare code into the relay bundle (Spec-025 AC-181). **The hosted backing's deployment wiring is owned by this task too** (Codex PR #328 round 1 — a Durable Object class with no entry-point export and no binding is undeployable, so the promised cross-instance single-use enforcement could never run): the same commit that lands the class re-exports `DpopReplayGuardDO` from the Worker entry module `packages/control-plane/src/server/host.ts` (an export-only edit — Cloudflare serves a Durable Object class only when the deployed script exports it — registered on the [cross-plan-dependencies.md](../architecture/cross-plan-dependencies.md) §2 `src/server/` row per the Plan-021 `RateLimitEscalationDO` export precedent, never a semantic edit to the host's gate or dispatch behavior) and extends the hosted `packages/control-plane/wrangler.toml` (Plan-008-owned file, extended here exactly as Plan-021's Phase-2 landing extends it) with the `[[durable_objects.bindings]]` entry `{ name = "DPOP_REPLAY_GUARD", class_name = "DpopReplayGuardDO" }` and a `[[migrations]]` entry adding `DpopReplayGuardDO` to `new_sqlite_classes` — all three wiring pieces in the class's own commit, so the hosted replay guard is deployable the moment Task 8 lands, never a class with no binding. The `{jti, htm, htu, iat, ath}` check set is control-plane-wide per security-architecture.md, so this port is deliberately the seam's **first implementation** — Plan-014-owned in its own directory until a second consumer forces the hoist (the OD-008r-4 no-second-consumer rule). DPoP has no provider elsewhere in the corpus; `packages/crypto-paseto` is a PASETO package and supplies the signing primitive only, never a verified caller identity. (Consumes `packages/crypto-paseto` primitives ← Plan-025 Tier 1 Partial, shipped Tier 1.) per-chunk authenticated GETs verify each chunk against the signed manifest on arrival (reject + re-request on mismatch), unwrap the CEK returned by `ArtifactFetchAuthorize` with the thumbprint-selected durable artifact key from `artifact_encryption_keys` (I-014-6 — unwrap works after daemon restart because the key survives in the master-key-wrapped store), verify it against the signed `cekCommitment` before decrypt, decrypt, verify the whole-payload CAS digest, and only then send the authenticated `ArtifactFetchComplete` ack — the sole writer of `delivered_at`, which also releases the fetch grace lease (I-014-8; the relay never infers delivery from a chunk GET). The relay resolves the acked row as `(ciphertext_digest, participant_id, node_id)` from the digest plus the token's own claims, and the write is idempotent (a replayed ack is a no-op returning success). Non-member, expired-token, and unbound-DPoP fetches are refused with `artifact.fetch_unauthorized`; a member with no recipient row (post-publish joiner) receives the reserved `artifact.no_access_key` (404) error — distinct from the 403, so clients surface the re-publish remedy. **Assertion:** a participant on a second node fetches and verifies a pinned artifact with the publishing daemon stopped; a recipient daemon restarted between publish and fetch still unwraps (durable key, not session-ephemeral); a corrupted chunk is re-requested and the fetch still completes; a tampered whole-payload fails post-decrypt with a loud integrity error; a wrapped CEK failing the `cekCommitment` check refuses decrypt; a fetcher killed after its last chunk GET but before verification leaves `delivered_at` NULL and the blob intact; a node that rotated its artifact key still unwraps a blob pinned to its retired key; a non-member token mint is refused; a mint presenting no DPoP proof is refused, and a chunk GET whose presented proof thumbprint differs from the token's `cnf.jkt` is refused with `artifact.fetch_unauthorized` (a token replayed without its proof-of-possession key is unusable); a valid proof captured from a chunk GET and replayed on `ArtifactFetchComplete` is refused (`htm`/`htu` mismatch — the ack-without-fetch replay), a proof whose `ath` does not hash the token presented with it is refused **in both populations** (a mint proof validates against the access token — never against the not-yet-minted fetch token — and a fetch proof against the fetch token), a proof outside the `iat` freshness window is refused, and a byte-identical proof presented twice is refused the second time (`jti` replay) **even when the two presentations land on different control-plane instances** (the `DpopReplayGuard` is shared atomic state, never process-local); a mint whose `attachmentId` names another participant's attachment row is refused with `artifact.fetch_unauthorized`, never re-scoped to one of the caller's own rows; a post-publish joiner's fetch fails with `artifact.no_access_key`, not `artifact.fetch_unauthorized`; a token minted for node A cannot fetch node B's wrapped CEK nor ack node B's row (the sibling-node attack **at the token-scope layer** — B's `delivered_at` stays NULL and the blob survives; the mint layer is participant-granular per I-014-10, so this assertion pins the relay's claim-resolution leg, not a closed sibling-daemon threat); a replayed ack is an idempotent no-op. **Spec coverage:** Spec-014 §Cross-Node Artifact Relay (V1) Fetch steps 5–6, failure modes (digest mismatch, publisher offline); Spec-014 AC line 180 (fetchable on node B while node A is offline), AC line 182 (authenticated node-scoped DPoP-bound fetch), AC line 183 (chunk rejection, post-decrypt digest, loud integrity error), AC line 186 (restarted recipient still unwraps its durable-key CEK), AC line 187 (post-publish joiner gets `artifact.no_access_key`), AC line 190 (acked row resolved from the token's own claims; replayed ack idempotent), AC line 191 (rotated key still unwraps a blob pinned to the retired key). **Verifies invariant:** I-014-3, I-014-4, I-014-6, I-014-8, I-014-9, I-014-10. **Consumes:** PASETO v4.public signing primitives ← `packages/crypto-paseto` (Plan-025 Tier 1 Partial, shipped Tier 1); issuer key + `AuthenticatedIdentityContext` ← Plan-018 (Tier 5 — see the Dependencies row); the `attachmentId` node selector + `runtime_node_attachments` ← Plan-003 (Tier 3 — see the Dependencies row); durable artifact keys ← `artifact_encryption_keys` (Task 7, by SHAPE — thumbprint-selected); signed chunk manifest + `cekCommitment` ← the `artifact.published` event Task 7 emits (by SHAPE); control-plane HTTP credentials ← `DaemonCredentialProvider` (Plan-006 CP-006-13 / Plan-018 CP-018-12 — **CP-014-5**, registered 2026-08-12, Codex PR #326 round 2: the `ArtifactFetchAuthorize` mint presents the participant access token + per-request DPoP proof via `mintForAttempt`, and the chunk GETs + `ArtifactFetchComplete` present the minted **fetch token** with proofs computed under the **same daemon-held DPoP key** the token's `cnf` was bound from — the seam's **proof-for-a-presented-token affordance**, whose interface authoring rides the credential-seam task decomposition Plan-018's promotion pass already owes).

- **Task 9 — Relay GC, quotas, and backpressure (2026-07-08 growth).** Implement the lifecycle half of `artifact-relay-service.ts`: refcount-zero delete (all `delivered_at` set across every `(participant, node)` recipient row, each written only by that node's `ArtifactFetchComplete` ack — I-014-7 + I-014-8: neither a sibling node's fetch nor a bare chunk GET advances delivery), hourly TTL sweep over `expires_at`, 90%-of-`node_relay_storage_max` watermark eviction (delivered/nearest-TTL first), incomplete-upload reaping at `incomplete_upload_ttl`, grace-lease respect (no eviction while a lease is live), per-session/per-participant byte-quota accounting, and `429` + `Retry-After` backpressure per the six Spec-021 relay rows. Every threshold this task enforces is read from `packages/contracts/src/artifacts/relay-limits.ts` — the seven normative quota/TTL defaults from `Spec-014 §Cross-Node Artifact Relay (V1)` as a typed, operator-overridable config object (chunk size, per-artifact / per-session / per-participant / per-node byte caps, the three retention tiers, incomplete-upload TTL), owned here so CP-014-3's "operator-tunable" obligation has an import target rather than a table Plan-025 must transcribe (added 2026-08-12 by the relay-scope readiness-audit delta). **Assertion:** the last intended recipient's _acked_ fetch deletes the blob and its recipient rows, while an identical fetch that never acks leaves them until TTL; an expired-TTL blob is swept and a subsequent fetch returns `artifact.relay_expired` (410, [error-contracts.md](../architecture/contracts/error-contracts.md)) — not a bare 404, so a client distinguishes "was pinned, TTL elapsed, re-publish to restore" from "never had a grant"; an in-flight lease blocks eviction until released; an over-quota upload receives `429` with `Retry-After` and the publish degrades to `quota_exceeded`; the `relay-limits.ts` defaults equal the Spec-014 table value-for-value, and an operator override changes enforcement without a service-code edit. **Spec coverage:** Spec-014 §Delete (refcount + TTL + crypto-shred) step 8; Spec-014 §Cross-Node Artifact Relay (V1) size/quota/retention table; Spec-014 §Failure modes (normative responses) relay full, mid-fetch expiry, publisher dies mid-upload, recipient crashes before verifying; Spec-014 AC line 184 (over-cap/over-quota degraded status), AC line 185 (refcount-zero or TTL GC), AC line 189 (`delivered_at` advances only on the verified ack). **Verifies invariant:** I-014-5, I-014-7, I-014-8. **Consumes:** Spec-021 registry rows (`artifact.upload.*`, `artifact.fetch.*`) ← Plan-021 enforcement substrate.

- **Task 10 — Erasure fan-out + degraded-status surfaces (2026-07-08 growth).** Ship the CP-014-2 Path-2 reciprocal: participant erasure DELETEs their daemon-local `artifact_encryption_keys` row (the Path-1 half — the durable artifact key's only copy) and hard-DELETEs their `artifact_relay_recipients` rows across all their nodes in one participant-keyed statement (the Path-2 half; together the crypto-shred, I-014-7), relies on `ON DELETE SET NULL` for `artifact_relay_blobs.publisher_participant_id`, fans out to every relay node holding the blob, and emits the erasure audit event without logging keys or plaintext; verify the deployment satisfies one of the two Spec-014 §State And Data Implications backup mechanisms (PITR window ≤ erasure SLA, or separately-destroyable KEK). Surface `replicationStatus` in the desktop artifacts views (Task 6 tree): pinned/pending, and the explicit "unavailable while the publisher is offline" state for `over_cap`/`quota_exceeded`/`expired`. **V1 scope (2026-08-12 relay-scope readiness-audit delta; V1 caller corrected at the Codex PR #326 fold):** Plan-022's automated `gdpr.*` orchestration handlers are V1.1+ per that plan's §Non-Goals — V1 ships schema, write-path, `501` stubs, and the manual erasure runbook — and that runbook already executes this disposition **directly as SQL**: its Path-1 step 2 deletes the participant's daemon-local `artifact_encryption_keys` row and its Path-2 step 1 hard-DELETEs their `artifact_relay_recipients` rows inside the runbook's single Postgres transaction ([gdpr-manual-erasure-runbook.md §Recovery Steps](../operations/gdpr-manual-erasure-runbook.md#recovery-steps)). **The V1 erasure contract is that SQL sequence, deliberately not a function call** — Path 2's one-transaction discipline and its FK-ordering rationale (a surviving recipient row blocks the anchor delete) live in the runbook, and interposing a function would fragment them. Task 10's deliverable is the **callable, participant-keyed disposition function** — `disposeArtifactRelayForParticipant(participantId)` in the relay service, plus its daemon-local counterpart over `artifact_encryption_keys` — whose caller is Plan-022's V1.1 orchestrator when it replaces the manual runbook; in V1 the function's tests pin **equivalence with the runbook SQL** (function and runbook statements sweep exactly the same rows for the same participant, so the V1.1 cutover changes the invoker, never the disposition). **Assertion:** erasing a participant removes their local `artifact_encryption_keys` row and exactly their recipient rows (others' delivery state untouched), decrements the intended-recipient set so refcount semantics stay consistent, and emits the audit event; an erased participant's fetch authorization is refused; the desktop view renders the degraded status for an over-cap artifact; and the disposition function's row sweep is asserted equivalent to the runbook's Path-1 step 2 + Path-2 step 1 SQL for the same participant (same rows deleted, no more, no fewer — the V1.1-cutover equivalence pin). **Spec coverage:** Spec-014 §Delete (refcount + TTL + crypto-shred) step 9; Spec-014 §Failure modes (normative responses) participant erased mid-life, over-cap; Spec-014 AC line 184 (over-cap/over-quota degraded status), AC line 185 (participant erasure crypto-shred, GC, audit event). **Verifies invariant:** I-014-2 pattern (no in-place mutation of others' rows), I-014-3, I-014-7. **Consumes:** erasure **execution** ← Plan-022's V1 manual erasure runbook (direct SQL, the V1 contract; the function's own caller arrives with the V1.1 `gdpr.*` orchestrator — invoker changes, disposition doesn't); table dispositions ← Plan-014-owned per CP-014-2.

## Parallelization Notes

- Manifest-contract work and payload-store implementation can proceed in parallel once visibility classes are fixed.
- Shared-replication work should wait for manifest schema and pending-replication semantics to stabilize.

## Test And Verification Plan

- Attachment-ingest tests covering stable ids, normalized metadata, and immutable payload reads
- Visibility tests covering `local-only`, shared-visible, and pending-replication transitions
- Large-artifact tests proving timeline manifests remain usable without forcing inline payload rendering
- Derivative-artifact tests proving redacted or summarized shared forms preserve separate provenance and do not mutate the original artifact
- Relay-pin tests covering eager upload, received-set resume after publisher crash, pinned-only-when-complete, and over-cap/over-quota honest degradation (2026-07-08 growth)
- Relay-fetch tests covering publisher-offline fetch, per-chunk reject/re-request, post-decrypt digest failure, and non-member/expired/DPoP-unbound refusals (2026-07-08 growth)
- Relay-lifecycle tests covering refcount-zero delete, TTL sweep, watermark eviction, grace-lease blocking, quota backpressure, and erasure fan-out with audit events (2026-07-08 growth)

## Rollout Order

1. Land manifest contracts and local payload storage
2. Enable attachment ingest and artifact publication in local sessions
3. Enable shared-visible replication and visibility-update flows
4. Land the relay contracts + eager-pin upload (Task 7), then authenticated fetch (Task 8), then GC/quota (Task 9), then erasure fan-out + degraded-status surfaces (Task 10) — fetch never ships before pin, GC never before both (2026-07-08 growth)

## Rollback Or Fallback

- Keep artifacts local manifest-first and disable shared replication if replication-state handling regresses.

## Risks And Blockers

- Manifest-first versus synchronous small-payload replication remains unresolved (deferral tracked in parent [Spec-014](../specs/014-artifacts-files-and-attachments.md))
- Artifact immutability will be undermined if live workspace paths are allowed to masquerade as durable payload identity
- Pressure for participant-specific redaction can create accidental in-place mutation semantics unless derivative-artifact handling stays explicit

## Ratified Design Decisions (Tier-7 audit)

- **D-014-1 — The OCI manifest envelope is realized as dedicated `artifact_manifests` columns + a named `ArtifactManifest` wire shape.** `Spec-014 §Interfaces And Contracts` specifies an OCI-inspired envelope (`subject`, `size`, `annotations`, `digest`=content_hash). The audit authored `artifact_manifests.subject` (self-referential FK), `size_bytes`, and `annotations` columns (local-sqlite-schema.md) and the matching `ArtifactManifest` wire shape (api-payload-contracts.md), plus the CP-014-1 obligation that Plan-011's `diff_artifacts.artifact_manifest_id` FK references. This is a NEW contract surface — it is why Plan-014 was flipped to `review` and re-promoted only on ratification.
- **D-014-2 — `annotations` is a dedicated OCI string→string column, not folded into freeform `metadata`.** The OCI `annotations` map is a first-class manifest property ([OCI image-manifest spec](https://github.com/opencontainers/image-spec/blob/main/manifest.md)); it gets its own `artifact_manifests.annotations` column and a distinct `ArtifactManifest.annotations` wire field so the at-rest shape is 1:1 with the wire, while `metadata` stays purely freeform daemon-side provenance.
- **D-014-3 — `ArtifactPublish`/`ArtifactRead` compose a single named `ArtifactManifest`; publish embeds the manifest metadata (no `manifestUrl` pointer).** `Spec-014 §Interfaces And Contracts` mandates that publish return "artifact id **and** manifest metadata"; the prior `manifestUrl` pointer was drift from that clause (`Spec-014 §Interfaces And Contracts` grants handle/inline latitude to the _payload_ on `ArtifactRead` only, never to the manifest). The wire now embeds `manifest: ArtifactManifest` on publish and `manifest` + `payloadHandle?`/`payload?` on read; producer inputs (`subject?`, `annotations?`) are accepted at publish so the `annotations` column and derivative `subject` are not write-dead. Aligns the wire to the spec — not a new owner decision.
- **D-014-4 — The `artifactType` discriminator is a closed six-value union: `file | diff | summary | log | design | workflow_output`.** `Spec-014 §Required Behavior` enumerates five families (`summary` covers the plan/summary family); the audit adds `workflow_output` (the `Spec-017 §Output Mode Specification` Tier-8 workflow phase-output type) as an additive sixth value. The Required-Behavior families list admits "at least" the five, so this is an additive discriminator value, not a families-list rewrite. The closed typed union is the contract (api-payload-contracts.md `ArtifactType`); the schema CHECK mirrors it (`CHECK(artifact_type IN ('file','diff','summary','log','design','workflow_output'))`).
- **D-014-5 — The envelope-interior application-payload discriminator is a Plan-008-owned discriminated union with a consumer registration seam; Plan-014 registers `artifact_key_attestation` through it.** _(Ratified 2026-08-12 by the relay-scope targeted readiness-audit delta, not by the Tier-7 audit above.)_ Task 7's artifact-key attestation and Plan-008's history backfill both ride application payloads inside the one pairwise ciphertext envelope, each refusing a new relay frame message type, and neither side defined the discriminator that separates them on receipt — Task 7's attestation leg was unbuildable as written. The envelope and its frame-type surface are Plan-008's per [cross-plan-dependencies.md §2](../architecture/cross-plan-dependencies.md), so the union is **authored under Plan-008** in `packages/contracts/src/session-join.ts`: `RelayApplicationPayload`, a Zod `discriminatedUnion` over the required string tag member `kind` whose vocabulary is `RelayApplicationPayloadKind`. Plan-008's own three arms are `history_backfill_request`, `history_backfill_chunk`, and `relayed_session_event`; `HistoryBackfillEntry` is chunk-interior and carries no tag. **Unknown-kind is fail-closed** — the client coordinator's demux drops an absent or unrecognized `kind` with a logged refusal and never dispatches it. **Registration seam:** a consuming plan appends its own arm and keeps its payload schema in its own contract domain, so Plan-014's `artifact_key_attestation` arm references a schema under `packages/contracts/src/artifacts/` and Plan-008 owns only the tag, the union, and the exhaustiveness rule (CP-014-4 ⇄ Plan-008 CP-008-16). **Ratifying this from a Plan-014 delta rather than deferring it is precedented three ways:** the NS-19 Tier-7 readiness audit ratified D-025-1's hoist of the `RelayFrameType` wire enum into Plan-008's `session-join.ts` on the ground that the values are a wire-protocol fact contracts must own, without flipping Plan-008; the `SessionEventSchema` union-registration seam is the corpus's established one-owner-many-registrants shape (cross-plan-dependencies.md §2 `packages/contracts/src/` row); and CP-003-5 was registered by a delta as an additive cross-plan row plus additive unshipped exports in another plan's table without flipping that plan. All of it is additive-only and unshipped on both sides, so no version bump, no Plan-008 or Spec-008 status flip, and no Spec-008 body change.

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped: []
```

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

- 2026-07-08 — Cross-node relay scope growth (owner directive; [ADR-015 amendment 2026-07-08](../decisions/015-v1-feature-scope-definition.md#amendment-2026-07-08-v11-deferred-features-3--2-cross-node-shared-artifacts-pulled-into-v1)): cross-node shared artifacts pulled from V1.1 into V1 as feature-14 growth. This plan gains Tasks 7–10 (eager-pin upload; authenticated DPoP-bound fetch + two-level integrity; GC/quota/backpressure; erasure fan-out + degraded-status surfaces), invariants I-014-3/4/5, CP-014-2 (⇄ Plan-022 CP-022-6 — the two `artifact_relay_*` tables join the participants FK closure) and CP-014-3 (→ Plan-025 self-host composition), the two Postgres tables ([Shared Postgres Schema §Artifact Relay Blob Store](../architecture/schemas/shared-postgres-schema.md#artifact-relay-blob-store-plan-014)), and the `replication_status` value-set refinement A-014-3 deferred. Status flipped `approved` → `review`; the Preconditions audit checkbox is re-opened — Tasks 1–6 remain audit-covered, Tasks 7–10 need the readiness-audit delta before any code dispatch. Normative design: [Spec-014 §Cross-Node Artifact Relay (V1)](../specs/014-artifacts-files-and-attachments.md#cross-node-artifact-relay-v1) (also flipped to `review`). Rate rows: Spec-021 (stays `approved`, additive registry rows).
- 2026-06-15 — Tier-7 plan-readiness audit (NS-19): 10 findings adjudicated via A-014-1..6; D-014-1..4 ratified. The plan was flipped to `review` (the OCI-envelope columns + CP-014-1 are a NEW contract surface) and re-promoted to `approved` in the same swap on ratification of the OCI-envelope/wire bundle (D-014-1/D-014-2/D-014-3) and resolution of the `artifactType` discriminator (D-014-4 — six values `file|diff|summary|log|design|workflow_output`). Adjudications: A-014-1 — CP-014-1 records the envelope/tables Plan-011's DiffArtifact consumes; A-014-2 — the six-Task `#### Tasks` block was authored as audit backfill; A-014-3 — the `replication_status` column realizes the `Spec-014 §Fallback Behavior` manifest-first surface (nullable, spec-named `pending_replication`, no multi-state CHECK — terminal values are a deferred owner refinement, anti-fabrication); A-014-4 — derivatives are separate manifests via the `subject` self-FK (I-014-2), never in-place mutation; A-014-5 — Dependencies row + dep-map §3/§4 Plan-006 (`EventLogService.append` / `artifact_publication` taxonomy) + Plan-007-partial (`artifact.*` IPC registry) edges; A-014-6 — §Invariants I-014-1 (immutability) / I-014-2 (derivative-not-mutation). Companion amendments: api-payload-contracts.md, local-sqlite-schema.md, spec-014.md (content-only, stays `approved`), cross-plan-dependencies.md §3/§4. No upstream-tier or sealed-plan amendments.
- 2026-06-18 — Codex review round 15 (NS-19 PR finalization): two findings touch this plan, both fixed at the requirement level (no new contract surface — Plan-014 stays `approved`). **KcMXr (ingest/publish producer model — Design A, advisor-confirmed):** the prior Task 2/3 wording described an ingest→publish _pipeline_ (Task 2 "mints the `ArtifactId`" but deferred manifest persistence to Task 3), which orphaned the `AttachmentIngestResponse.artifactId` — no manifest was ever written for it, so the id never resolved via `ArtifactRead`. Corrected to **two independent producers**: AttachmentIngest (Task 2) now persists its OWN `artifact_manifests` row (`state='published'`, `visibility='local-only'` — an uploaded attachment _is_ a published local artifact, `Spec-014 §Default Behavior` + `Spec-014 §Example Flows`) + `artifact_payload_refs` in one transaction, returning a resolvable id; ArtifactPublish (Task 3) stays the single-shot run-output producer deriving its `digest`/`size` from its OWN `ArtifactPublishRequest.payload`. The OCI-style two-phase alternative (ingest → opaque content-handle, id + manifest deferred to a later publish) was **rejected** — it contradicts the approved `Spec-014 §Required Behavior` / `Spec-014 §Default Behavior` / `Spec-014 §Example Flows` + `Spec-014 §Acceptance Criteria` ("ingestion produces stable artifact ids **and manifests**") and would flip Spec-014 out of `approved`. No wire / DDL / Spec change: only the buggy pipeline prose is corrected (Task 2 + Task 3; the schema `content_hash`/`size_bytes` inline notes + the schema ratified-design note; the api-payload `ArtifactManifest` ratified-design note). No Plan-011 ripple — Plan-011 publishes diffs single-shot (its computed diff mints the manifest in one step), already correct. **KcMXv (contracts barrel):** Task 1's `packages/contracts/src/artifacts/` subdir exposes an `artifacts/index.ts` barrel re-exported from `packages/contracts/src/index.ts` (the repo single-import-surface convention — the Plan-021 T21.1-1/T21.1-2 `index.ts` re-export precedent); `package.json` keeps exporting only `"."`. Companion amendments: local-sqlite-schema.md + api-payload-contracts.md (content-only, stay `approved`); cross-plan-dependencies.md §2 `artifacts/` row. No upstream-tier or sealed-plan amendments.
- 2026-08-08 — **I-014-10 node-attach-context re-scope (honesty amendment; extends the relay-scope readiness-audit delta's scope).** I-014-10 and Spec-014 Fetch step 5 promised that the issuer resolves `node_id` "from the authenticated node-attach context" of the `ArtifactFetchAuthorize` call. No V1 mechanism supplies that context: `ArtifactFetchAuthorize` is a control-plane route (§Target Areas `artifact-relay-service.ts`, hosted on the Plan-008-bootstrap substrate) whose PASETO v4.public access token carries `sub: ParticipantId` + `cnf.jkt` and **no node claim** ([security-architecture.md §Control-Plane Authentication (Task 5.2)](../architecture/security-architecture.md#control-plane-authentication-task-52)), and **V1 defines no node-identity credential** at all ([security-architecture.md §Per-Event Daemon Signature](../architecture/security-architecture.md#per-event-daemon-signature)). The Spec-008 relay connection token does carry a `nodeId` claim, but it rides a different transport, is stamped from a client-supplied negotiation field, and is presence-checked only — so it does not rescue the promise either. **Adjudication: re-scope, not a new mechanism.** This is the identical shape the Plan-006 T4.10 registrar landed on at Codex PR #274 round 5 — both ownership predicates resolve at PARTICIPANT granularity because no node credential exists — and the corpus already names its closure, the [cross-plan-dependencies.md §5 V1.1+ Cross-Plan Extensions](../architecture/cross-plan-dependencies.md#v11-cross-plan-extensions) Plan-018/Plan-003 node-identity leg (four rows now gated on that same primitive, counting the Plan-014 row this amendment adds at round 1). I-014-10 now states the honest V1 posture — `node_id` issuer-stamped, never a request-body node identifier (the wire property the round 5–6 decision actually bought, which survives intact) — plus the named residual and its closure. **Residual (corrected at round 1 — see the round-1 record below; this bullet originally claimed "delivery attribution, not confidentiality", which Codex falsified on both legs):** the V1 residual is an **availability** loss (forged `ArtifactFetchComplete` → premature refcount-zero GC, no remedy while the publisher is offline) **and** a **confidentiality** loss (participant-signed artifact-key attestation spoofed under a sibling's `node_id`). Sealing the wrapped CEK to the target node's durable key under Spec-022 custody (I-014-6, I-014-9) bounds only the already-published blob, not either residual. **I-014-8 is unchanged by design** — its acked-row clause describes the relay's claim-resolution leg, which the re-scope leaves true, and it inherits the residual through its existing `(I-014-10)` cross-reference; Task 8's sibling-node assertion is relabelled to the token-scope layer it actually tests rather than deleted. Task 8's pre-existing "issuer-integration seam pinned at the audit delta" deferral now records the T4.10 `attachmentId` shape as the candidate to evaluate there (an evaluation pointer in the Task 7 buy-vs-build style — no wire field and no Plan-003 dependency edge is minted here; a dep-map edge **is** added at round 1, the §V1.1+ Cross-Plan Extensions Plan-014 row). **Companion amendment:** Spec-014 §Cross-Node Artifact Relay (V1) Fetch steps 5 and 6 + the round 5–6 §Resolved Questions entry — all three edited in place at **zero net line change** so the Tasks 1–6 prose line-cites into Spec-014 do not drift. Those three sites were **erratum-class on two independent legs** as first written — but leg 1 was retired at round 1, when the Codex findings forced §Required Behavior and §Acceptance Criteria edits (see the round-1 record). What holds is **leg 2 alone**, which is unconditional: Spec-014 has been `review` since 2026-07-08, so there is no status to flip and no [readiness-audit runbook §Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate) to re-trigger regardless of amendment class. The amendment's honest class is therefore a **normative threat-model narrowing of a V1 required property**, not an erratum. The §Acceptance Criteria clause "one node of a participant cannot mark a sibling node delivered" was left unedited at first and is **now scoped in place** at round 1; Spec-014 Fetch step 6 carried the same over-claim in body prose ("otherwise a single compromised or buggy node could mark its siblings delivered") and is scoped in place under the same erratum-class envelope — the buggy-node and cross-participant cases do close, the same-participant compromised one is the named residual. **Corpus sweep of the sibling surfaces carrying this design fact.** [shared-postgres-schema.md §Artifact Relay Blob Store (Plan-014)](../architecture/schemas/shared-postgres-schema.md#artifact-relay-blob-store-plan-014) asserted the same absolute on the `artifact_relay_recipients` block ("so one node cannot mark a sibling delivered") and is corrected in this diff, likewise at zero net line change: its claim-resolution leg survives, and the conclusion is scoped to a node presenting a token minted for itself, with the mint-layer residual named. [api-payload-contracts.md §Plan-014 — Artifacts Files And Attachments](../architecture/contracts/api-payload-contracts.md#plan-014--artifacts-files-and-attachments) survives unedited — its relay block names no node-resolution mechanism, defers the schemas outright ("Deliberately not typed here yet — no invented shapes"), and points at the Spec-014 section corrected here as normative. The [error-contracts.md §Artifact](../architecture/contracts/error-contracts.md#artifact) `artifact.fetch_unauthorized` / `artifact.no_access_key` rows survive — both describe membership and auth outcomes, neither asserts node-granular authorization. The sweep enumerated every `docs` / `packages` / `apps` occurrence of the sibling-delivery phrasings and of `artifact_relay_recipients` / `artifact.no_access_key` / `delivered_at`; the remaining carriers all survive because none makes an authorization claim — Plan-022 / Spec-022 and the [GDPR manual-erasure runbook](../operations/gdpr-manual-erasure-runbook.md) touch the rows only as erasure/crypto-shred targets, [deployment-topology.md](../architecture/deployment-topology.md) only as a PITR/backup carve-out, [local-sqlite-schema.md](../architecture/schemas/local-sqlite-schema.md) only for the retire-not-replace thumbprint match, [cross-plan-dependencies.md §1 Table Ownership Map](../architecture/cross-plan-dependencies.md#1-table-ownership-map) and [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md) only as ownership/split records, and Plan-008 / Spec-008 only to borrow the `artifact.no_access_key` no-silent-failure posture for peer history backfill. **I-014-7 is unchanged for the same reason as I-014-8:** its per-node claim is a data-model granularity statement traced to Spec-014 Delete steps 8–9 (recipient rows are keyed `(digest, participant, node)`, so an ack on one row never clears another's), which the re-scope leaves true; it inherits the mint-layer residual through I-014-8's `(I-014-10)` cross-reference. **Status and Preconditions untouched:** Plan-014 stays `review` (flipped 2026-07-08) and both re-opened Preconditions boxes stay unchecked — this amendment restores nothing. Tasks 7–10 remain non-dispatch-eligible until the relay-scope readiness-audit delta lands, and this amendment **extends that delta's audit scope** to the fetch-authorization node-resolution seam.
- 2026-08-08 — **PR #301 Codex round 1: four P1/P2 findings folded; the residual narrowing retracted.** Round 1 falsified two claims the bullet above made when first written, and both are corrected at their source rather than softened. **(1) Forged delivery is not a recoverable residual (P1, Spec-014 Fetch step 5).** `ArtifactFetchComplete` carries no evidence that the CEK was unwrapped or the payload verified, so a compromised same-participant node holding a token minted under a sibling's claims acks that sibling's row without fetching; if that clears the blob's last outstanding row, refcount-zero GC destroys it. The honest node's own recipient row cascades away with the blob (`ON DELETE CASCADE`), so it _does_ surface `artifact.no_access_key` — but that error's documented remedy is a publisher re-publish, unavailable in exactly the publisher-offline case the guarantee exists for. V1 cannot prevent the forged ack (no node credential to bind it to), so the **normative guarantees are explicitly weakened instead**: Spec-014 §Required Behavior, the §Cross-Node Artifact Relay (V1) preamble, the §Failure modes publisher-offline row, and both §Acceptance Criteria rows (offline fetch; sibling-delivery) now carry a **threat-model scope** — the guarantees hold against the relay, non-members, and every other participant, and are not asserted against a compromised node inside the fetching participant's own trust domain. **(2) Attestation spoofing (P1, I-014-10) — round 1 called this a confidentiality hole; round 3 reclassified it to victim-node lockout plus attestation-integrity corruption, see the round-3 record below.** Artifact-key attestations are signed by the participant's long-term Ed25519 identity key over a caller-chosen `(session_id, node_id, artifact_public_key)`, and publishers accept the latest per `(participant, node)`. That custody model was mis-stated in this record and is corrected at round 2 — see the round-2 entry below; the attack survives with the corrected mechanism (multiple separately-registered per-workstation identities, none bound to the `node_id` it attests). The **"delivery attribution, not confidentiality" narrowing is therefore retracted outright**, at Spec-014 Publish step 3 / Fetch steps 5–6 / the round 5–6 decision entry, at I-014-10, in the `artifact_relay_recipients` schema comment, and in this log. Cross-participant confidentiality and availability are untouched — forging either leg requires that participant's own identity key or device. **(3) The node selector is unspecified, so Task 8 no longer asserts the server resolves the node (P1).** With two nodes of one participant attached, `sub` names only the participant, `cnf` is possession material rather than an identity, and no request field disambiguates — the multi-node authorize path was unimplementable as written. Task 8 now asserts only that the node half is issuer-signed and never caller-supplied, and the **fetch-authorization node-selector decision** (wire commitment vs server-side rule) is folded into the same relay-scope readiness-audit delta Tasks 7–10 already wait on. No wire field is minted here. **(4) The V1.1 closure is now a recorded obligation, not a pointer (P2).** [cross-plan-dependencies.md §V1.1+ Cross-Plan Extensions](../architecture/cross-plan-dependencies.md#v11-cross-plan-extensions) gains a **Plan-014 row** — its own contract is that an entry is what surfaces a waiting consumer at V1.1 planning time, and the three pre-existing rows are Plan-006 consumers only, so pointing at the shared primitive recorded no Plan-014 obligation. The row names what the Plan-018/Plan-003 primitive **restores**: node-scoped fetch authorization, non-forgeable attestation binding, and delivery-attribution integrity. **Class reclassification (flagged, not silent).** Editing §Required Behavior and §Acceptance Criteria retires erratum leg 1; the amendment is now a normative threat-model narrowing. Leg 2 is unconditional and unaffected — Spec-014 and Plan-014 have both been `review` since 2026-07-08, so **no status flips and neither re-opened Preconditions box is touched**; this amendment still restores nothing. Spec-014 stays at **zero net line change** (272 lines) so the Tasks 1–6 prose line-cites do not drift, as does the Postgres schema (586 lines). Tasks 7–10 remain non-dispatch-eligible, and the delta's audit scope now also covers the weakened offline-availability guarantee, the attestation-binding residual, and the node-selector decision.
- 2026-08-08 — **PR #301 Codex round 2: four findings folded; two of them correct round-1's own wording.** **(1) Surviving resolution claim (sweep miss from round 1).** Spec-014 Fetch step 5 still said the control plane resolves `node_id` from attach state while Task 8 already conceded multi-node participants are unresolvable without a selector — two surfaces, incompatible answers. The spec's normative step now carries the same second-horn wording as Task 8: the node half is **issuer-signed and never caller-supplied**, single-attached-node resolution is well-defined, **multi-node resolution is deferred with the selector decision** to the relay-scope readiness-audit delta. The two surfaces are worded in parallel so they cannot drift apart again. **(2) ADR-015 incoherence.** Round 1 scoped the spec but left [ADR-015](../decisions/015-v1-feature-scope-definition.md)'s own text — the feature-14 row's offline-availability guarantee and the C4 rationale's "guaranteed fallback" — unscoped, leaving two authorities with incompatible answers. ADR-015 now carries a dated **§Decision Log** entry recording the threat-model scoping, both accepted V1 residuals, and their **named** closure criteria (the Plan-018/Plan-003 primitive per the §V1.1+ Cross-Plan Extensions Plan-014 row), plus in-place scope pointers on the feature-14 row and the C4 rationale. Status stays `accepted`; the event is logged as **"Scope recorded"**, not "Amended", because it changes no feature, count, or deferral and therefore owes no §Amendment History section. Ripple swept: `CLAUDE.md` and `README.md` §14 now name the scoping. `README.md` §V1 features intro and [cross-plan-dependencies.md §Build Order](../architecture/cross-plan-dependencies.md) were deliberately **not** touched — their "amended 2026-07-02 … — was 17" phrasing is scoped to the feature-count amendment, and 2026-08-08 moves no count. **(3) Availability overclaimed against the relay (round-1 overcorrection).** Round 1 said the guarantee holds "against the relay" — false for availability. §Required Behavior now states the trust split explicitly: the relay is **untrusted for content** (confidentiality and integrity hold against it — ciphertext only, signed chunk manifest) and **trusted for storage** (availability necessarily assumes it keeps and serves what it accepted, and cannot be guaranteed against relay deletion or refusal). The same-participant residual scopes availability further. **(4) Custody model mis-stated (round-1 error, corrected against the canonical source).** Round 1 wrote that the participant's identity key is "present on every node the participant runs", which reads as shared private-key material and would encourage key copying. [trust-and-identity.md §Edge Cases](../domain/trust-and-identity.md#edge-cases) is authoritative: **each workstation independently holds its own** long-term Ed25519 identity key, and the participant carries **multiple `bound` identities, one per workstation, each registered separately**. The attack survives with the corrected mechanism — the verifier checks only that an attestation is signed by _a_ key registered to that participant, nothing binds the **signing identity** to the `node_id` being attested, and publishers accept the latest attestation per `(participant, node)` regardless of which identity signed it — so any participant-authorized registered identity can bind an attacker-held X25519 key to a sibling's `node_id`. Rewritten at Spec-014 Publish step 3, I-014-10, the §V1.1 Plan-014 row, and the round-1 record above. **Sweep.** Both drift families re-swept across `docs` / `packages` / `apps` with the round-1 phrasings (server-resolves-the-node; shared-identity-key): six sites found and repaired, zero remaining. **Scope discipline unchanged:** no status flips, neither re-opened Preconditions box touched, no feature-count or table census moves. Spec-014 holds at zero net line change (272 lines).
- 2026-08-08 — **PR #301 Codex round 3 (final round): four findings folded; the confidentiality framing reclassified.** **(1) Attestation spoofing is not a confidentiality residual (reclassification, not deletion).** Verified against Spec-014 Publish step 3: the intended-recipient set is the attested `(participant, node)` set at publish time **minus the publishing node**, and "the publisher's **other** nodes are ordinary recipients" — so an _attached_ compromised sibling already holds that artifact's CEK under its **own legitimate** attested key, and the spoof buys it no plaintext it did not already have. A V1.1 node-identity key could not exclude a node that remains an authorized recipient, and **no node-scoped visibility is defined anywhere in the corpus** (checked before writing, per the review instruction: `docs/domain/trust-and-identity.md` and a repo-wide sweep both come back empty; Spec-014 §Required Behavior keeps visibility class-based). Plaintext reach by an attached, authorized node is therefore **participant-granularity sharing working as designed — a compromised-endpoint problem, not a protocol residual**. The real harm is **(a) victim-node lockout** (the victim's recipient row carries a CEK wrapped to a key it does not hold, so the honest node cannot decrypt an artifact it is entitled to — an availability harm) and **(b) corrupted attestation and delivery attribution**. Every rounds-1–2 "confidentiality residual" claim is corrected: Spec-014 Publish step 3, the §Cross-Node Artifact Relay preamble, Fetch step 5, the §Resolved Questions entry, I-014-10, the §V1.1 Plan-014 row, the ADR-015 §Decision Log entry and its feature-14 pointer, and the round-1/2 records above. The §5 row's leg (b) is renamed **attestation integrity** and now states plainly that the primitive restores binding the **signer** to the attested `node_id` — **not** node-level content exclusion. **(2) §Acceptance Criteria still demanded an impossible property.** Round 2 fixed §Required Behavior but left the offline-fetch AC row asserting fetchability "against the relay" — a criterion no implementation can satisfy, since the relay is the byte holder. That row is now scoped to **an operational relay that serves the bytes it accepted** (against the relay, the spec asserts confidentiality and integrity only). The re-sweep with fresh eyes on acceptance surfaces caught a second instance the round-2 sweep missed — the retired-key rotation AC row — now carrying the same inherited scope. **(3) Invariant vs. selector tension (self-inflicted at round 1).** I-014-10 said claims come "never from a client-supplied request field" while Task 8 preserved the recorded `attachmentId` candidate — and a validated opaque selector _is_ a client-supplied field. The invariant is narrowed to what it must actually prohibit: the node-claim **value** is always **issuer-derived from the caller's own attach-state rows**, and a **client-supplied raw node identity is never trusted**; a **validated opaque selector over the caller's own attachment rows is permissible** if the deferred audit adopts one, because the issuer still derives the value and refuses any row not owned by the authenticated `sub`. Task 8 and the Spec-014 §Resolved Questions entry are reworded in the same terms so the three surfaces state one rule. **(4) Stale architecture summaries.** [v1-feature-scope.md](../architecture/v1-feature-scope.md) (the feature-14 row and the C4-only-deferred claim) and [data-architecture.md](../architecture/data-architecture.md) still carried the pre-scoping guarantee; all three sites now carry the threat-model qualifier and the recorded closure, worded from the **reclassified** framing rather than the retiring confidentiality one. Swept `docs/architecture/` for other offline-fetch promises: the only other hit is the `local-sqlite-schema.md` retired-key retention comment, which states rotation mechanics rather than an acceptance-level guarantee and is deliberately left alone. **Merge, not rebase.** `origin/develop` (PR #299 / NS-49) was merged into this branch — one CLAUDE.md conflict in the features paragraph, resolved by taking develop's text as authoritative for the censuses and Spec-004 clauses and re-applying only the ADR-015 scoping addition on top; verified character-level that both files are develop's content plus exactly one insertion each. **Scope discipline unchanged:** no status flips, neither re-opened Preconditions box touched, no feature-count or table census moves. Spec-014 holds at zero net line change (272 lines).
- 2026-08-12 — **relay-scope targeted readiness-audit delta (restoring).** Ran the targeted delta the 2026-07-08 cross-node relay amendment obligated, scoped to that amendment's growth and its two follow-on hardening rounds (2026-07-09 key model; 2026-08-08 PR #301 threat-model scoping): [Spec-014 §Cross-Node Artifact Relay (V1)](../specs/014-artifacts-files-and-attachments.md#cross-node-artifact-relay-v1) and every Plan-014 task, invariant, and contract/schema row consuming it. **One critical, four major, six minor; every one repaired in this swap.** The critical was a genuine build blocker: Task 7's artifact-key attestation and Plan-008's history backfill both ride application payloads inside the one pairwise ciphertext envelope with no new frame message type on either side, and neither spec defined the discriminator that separated them on receipt. Rather than defer it, this delta **ratifies** the seam as **D-014-5** — a Plan-008-owned envelope-interior discriminated union (`RelayApplicationPayload` over the required tag member `kind`, vocabulary `RelayApplicationPayloadKind`, in `packages/contracts/src/session-join.ts`) with a consumer registration seam, recorded here as CP-014-4 and on the Plan-008 side as the additive CP-008-16; Plan-014 registers the `artifact_key_attestation` arm and keeps its payload schema in its own `packages/contracts/src/artifacts/` domain, and an absent or unrecognized `kind` is dropped fail-closed. Ratifying from a consumer-plan delta is precedented three ways — the NS-19 audit's `RelayFrameType` hoist into the same file (D-025-1) without flipping Plan-008, the `SessionEventSchema` union-registration seam, and CP-003-5's additive registration into another plan's table — and everything landed is additive-only and unshipped, so **Plan-008 and Spec-008 keep `approved` and no Spec-008 body text was touched**; the earlier reading that this had to wait for Plan-008's own restoring delta is obsolete, that delta having landed 2026-08-11 (PR #323, NS-58) without the seam. The four majors: the relay chunk-bytes object store had no named port — and the walk found the containing directory `packages/control-plane/src/artifacts/` carries no cross-plan §2 ownership row at all, though Task 3 has populated it since before the amendment, so the delta lands that missing row too — (CP-014-3's "deployment-agnostic" promise had nothing for Plan-025 to compose against); the Task-8 fetch token resolved by name to `packages/crypto-paseto` alone, which covers the signing primitive and neither the issuer key / authenticated `sub` (Plan-018, now a declared Tier-5 edge) nor DPoP proof verification (Plan-014-owned, now a named file); Task 10 consumed a Plan-022 orchestration contract whose V1 form is a `501` stub, leaving its fan-out with no caller — now scoped to a callable participant-keyed disposition function whose V1 grounding is the manual erasure runbook's own direct-SQL sequence (the function's tests pin row-sweep equivalence with it; its caller arrives with the V1.1 orchestrator — corrected at the Codex PR #326 fold from the "runbook invokes it" first form, which the runbook's actual Path-1/Path-2 SQL falsified); and ADR-010 was absent from Required ADRs while four of its crypto choices bind Tasks 7–8 and I-014-6 names it, added here on the Plan-006 T4.10 precedent. The minors: the invariant↔task marker asymmetry re-derived in both directions (I-014-6/7/8/9 restored to the four tasks' `Verifies invariant:` lines, and I-014-3's own cell widened to name Task 10 — the preflight Gate-4 corpus pin moves 717/134 → 724/134 with the seven restored bold references, re-pinned in this same PR); AC-line cites added to Tasks 7–9 and the missing Task-8 `**Consumes:**` field authored; the §5 Tier-7 Prerequisites cell and the "could move as early as Tier 5" Optimization Note re-derived from the post-amendment dependency set (Plan-021 at Tier 6 makes Tier 7 exact, not conservative); `artifact_manifests.relay_cek_ciphertext` enumerated in §Data And Storage Changes; the seven quota/TTL defaults given an owned typed surface for CP-014-3 to import; and `artifact.relay_expired` (410) / `artifact.too_large` (413) bound to task assertions. The four decisions the plan handed this delta are disposed explicitly: the demux tag **ratified** (above), the fetch-authorization node selector adopting the recorded Plan-006 T4.10 `attachmentId` shape — a validated opaque selector over the caller's own attachment rows, which keeps the node half issuer-derived per I-014-10 and obliges the Plan-003 edge Task 8 named as its condition; its **daemon-side carrier** is owed on the born-unchecked "Fetch-selector daemon carrier registered" §Preconditions box minted at the Codex PR #326 round-1 fold, which caught that the attach response returns to the client while the mint's caller is the daemon and the renderer→daemon delivery machinery was deleted by PR #323 — the issuer seam settled as a Plan-018 edge plus a Plan-014-owned composition, and the tus buy-vs-build call left at implementation with its criteria recorded: it has no contract consequence, and the new blob-store port makes the swap cheap. Verified live rather than assumed: CP-014-2 ⇄ Plan-022 CP-022-6 bidirectional, CP-014-3 ⇄ Plan-025, the six Spec-021 relay registry rows with Plan-021's union and the BL-146 self-wire, the three reserved `error-contracts.md` rows, and all four schema surfaces. Doc-first attested: zero `Spec-014` citations under `packages/` or `apps/` — no Plan-014 code has shipped. Status `review → approved` restored on Plan-014 and Spec-014 in this same swap; both re-opened §Preconditions boxes re-checked with Delivered records, and **one born-unchecked scoped box minted at the Codex PR #326 round-1 fold** ("Fetch-selector daemon carrier registered" — holding Task 8's `ArtifactFetchAuthorize` mint leg only). What still gates Tasks 7–10: §5 tier order (Plan-008-remainder and Plan-018 at Tier 5, Plan-021 at Tier 6) and phase decomposition for all four, plus that carrier box for Task 8's mint leg specifically. **Escalated, not resolved:** Plan-014 carries no `### Phase` headers, so preflight Gate 4's phase-section extraction cannot resolve and `/plan-execution` cannot dispatch it. Plan-011 and Plan-015 share the shape — it is a Tier-7-wide decomposition obligation predating this amendment, and repairing it inside a targeted delta would be scope the amendment never opened.
- 2026-08-12 — **PR #326 Codex round 2 (final round): six P1 findings folded, every one additive contract surface — nothing deferred.** All six are correct catches on surfaces this delta authored, one on the round-1 fold's own wording. **(1) Mint-proof `ath` contradiction (round-1 self-inflicted).** The round-1 DPoP hardening required every proof's `ath` to hash "the presented fetch token" _including the mint call's own proof_ — unbuildable, since the fetch token does not exist until the mint returns. Task 8 now states the two-stage split RFC 9449 §4.3 actually prescribes: the mint proof validates against the **participant access token** it presents, and only then does its key copy into the minted token's `cnf`; chunk GETs and `ArtifactFetchComplete` validate against the **fetch token**. Both stages instantiate security-architecture.md's canonical "hash of the presented token" rule, so no security-architecture amendment is owed; the `ath` assertion is scoped to both populations. **(2) Attestation payload named** — `ArtifactKeyAttestationPayload` ([api-payload-contracts.md §Plan-014](../architecture/contracts/api-payload-contracts.md#plan-014--artifacts-files-and-attachments)), so Plan-008's T-008r-1-3 has a schema to build the union arm from: `sessionId` / `nodeId` / `artifactPublicKey` (base64, 32 bytes) / `identityKeyFingerprint` (a selector resolved within the attesting participant's registered identity set only — it can narrow the check, never widen trust) / `identitySignature` (lowercase hex 64, the Plan-006 T2.3 convention). The attesting participant is the pairwise envelope's authenticated sender, never a payload member; no timestamp member — the ordered channel is the sequencing authority (last-received-wins per Spec-014); the signature preimage is the **literal** Spec-014 concatenation (injective — both ids are fixed-length UUIDs), matching Plan-008's bundle-signature convention rather than introducing a re-canonicalization the spec doesn't state. **(3) Attestation bridge methods registered** — `artifact.keyAttestationServe` / `artifact.keyAttestationDeliver` ([api-payload-contracts.md §Artifact-Attestation Bridge Method Registry](../architecture/contracts/api-payload-contracts.md#artifact-attestation-bridge-method-registry-tier-7-plan-014-task-7)): CP-008-14's four methods carry Plan-008's arms only, and the custody split (durable artifact key + recipient-key cache daemon-side; envelope coordinator, decryption, and the injected `Ed25519IdentitySigner` client-side per Plan-008 T-008r-4-2 / ADR-021) left the attestation legs no way across the local boundary — the daemon serves unsigned material and caches coordinator-verified bindings; signing and inbound verification stay coordinator-side where identity custody and the pairwise trust layer live. Registered on the plan-owned `artifact.*` namespace — no `session.*` root extension, no Plan-008 involvement. **(4) CP-014-3 given a named Plan-025 owner** — step 28 / T-025d-28-1 (Plan-025 is `review`, pre-promotion, so the decomposition is free of Status consequence): the filesystem `relay-blob-store.ts` adapter backed by an `artifact-blobs` named volume (the reference default — S3-compatible backing is operator endpoint configuration, never a sixth Compose service, so the five-services shape stands), `relay-limits.ts` operator overrides through `env.ts`, and the artifact HTTP surface mounted via the Worker-free control-plane sub-path export (the T-025d-19-1 precedent); T-025d-14-1's only-writable-mounts claim is forward-scoped in the same diff. **(5) Daemon HTTP credentials registered as CP-014-5** — the relay transfer service is the `DaemonCredentialProvider` seam's **fifth caller** (CP-006-13 / CP-018-12; the CP-024-5 / PR #279 widening precedent — the enumerations at Plan-018 CP-018-12, Plan-006 §Files / §unreachable-leg list / CP-006-13, and the cross-plan §3 Plan-018 + Plan-014 rows all widened, flipping no provider plan), covering Task 7's upload legs too — the same-class gap the finding did not name; Task 8's fetch legs scope the seam's one growth, the **proof-for-a-presented-token affordance** (`ath` = the presented fetch token's hash, under the same daemon-held key), with interface authoring riding Plan-018's already-owed promotion-pass decomposition. **(6) Single-use `jti` made enforceable** — the injected `DpopReplayGuard` port (`packages/control-plane/src/artifacts/dpop-replay-guard.ts`, keyed `(jkt, jti)`, retention = the `iat` window): hosted = per-thumbprint self-evicting Durable Object (the Plan-021 `RateLimitEscalationDO` pattern — per-key routing is the single-authority alternative the finding itself named); self-host = the `rate-limiter-flexible` Postgres substrate Plan-025 already deploys (an atomic consume is exactly single-use; no hand-authored table, so no census move); named as the control-plane-wide check set's first implementation, hoisted only when a second consumer arrives (the OD-008r-4 rule). **Scope discipline:** no status flips, no new Preconditions box (the "one born-unchecked scoped box" record stays exact across Plan-014 / cross-plan §6 NS-59 / README / CLAUDE.md), no table / event / feature census moves, and the preflight Gate-4 corpus pin re-verified after the fold — **unmoved**: round 2 adds no `I-NNN-M`-form marker (T-025d-28-1's `Verifies invariant:` is Spec-AC-form, like its sibling T-025d-19-1), so the pinned bold-resolved multiset is unchanged and the census test passes as-is.

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated
