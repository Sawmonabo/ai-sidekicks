# Plan-012: Approvals Permissions And Trust Boundaries

| Field | Value |
| --- | --- |
| **Status** | `review` |
| **NNN** | `012` |
| **Slug** | `approvals-permissions-and-trust-boundaries` |
| **Date** | `2026-04-14` (Tier-6 readiness audit 2026-06-10) |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-012: Approvals Permissions And Trust Boundaries](../specs/012-approvals-permissions-and-trust-boundaries.md) |
| **Required ADRs** | [ADR-002](../decisions/002-local-execution-shared-control-plane.md), [ADR-007](../decisions/007-collaboration-trust-and-permission-model.md), [ADR-012](../decisions/012-cedar-approval-policy-engine.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md), [ADR-017](../decisions/017-shared-event-sourcing-scope.md) (added by the Tier-6 audit — the D-012-6 daemon-only projection posture rests on it) |
| **Dependencies** | [Plan-001](./001-shared-session-core.md) (daemon SQLite substrate — migration-runner guarded-block seam + `openDatabase` + projector/replay substrate, shipped Tier 1; `SessionIdSchema`/`ParticipantIdSchema`), [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (Tier 4 — `approval_flow` event taxonomy + `EventLogService.append` sole append path + `membership.*` events consumed by rule invalidation), [Plan-007](./007-local-ipc-and-daemon-control.md)-partial (Tier 1 — `MethodRegistry` + SDK `JsonRpcClient`; `approval.*` registration gated on BL-142 + BL-143), [Plan-004](./004-queue-steer-pause-resume.md) (Tier 5 — run-blocking seam reciprocal per CP-012-5), [Plan-003](./003-runtime-node-attach.md) (shipped Tier 3 — `node_trust_state` trust read + attach-binding owner identity per CP-012-10), [Plan-005](./005-provider-driver-contract-and-capabilities.md) (Tier 4 — `RunIdSchema` + `RunId` brand from `provider-driver.ts` per CP-012-3; `interactive_request` driver events feeding the CP-012-6 normalizer), [Plan-009](./009-repo-attachment-and-workspace-binding.md) + [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (Tier-6 siblings — `workspaces.fs_root` / `worktrees.fs_root` envelope roots via `run_execution_contexts`; within-tier ordering per CP-012-9), [Plan-023](./023-desktop-shell-and-renderer.md)-partial (Tier 1 — renderer substrate + `window.sidekicks` bridge stub; live IPC at Tier 8 per CP-012-8); [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) (implicit cross-node dispatch surface per [cross-plan-dependencies.md §Spec-024 Implementation Plan](../architecture/cross-plan-dependencies.md#spec-024-implementation-plan) — Plan-027 owns the V1 implementation; Plan-012 provides approval categories per CP-012-1) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |

## Goal

Implement the canonical approval, remembered-grant, and permission-check model that keeps shared collaboration membership separate from local execution trust.

## Scope

This plan covers approval records, remembered grants, daemon-side permission checks, trust invalidation, and approval visibility projections — plus, per the capability-enhancement campaign's B13 amendment (2026-07-20): driver-ask normalization with bounded fail-closed expiry, execution-posture authorization with the content-addressed credential-policy artifact store, and the allow-listed spawn-environment boundary.

## Non-Goals

- Identity-provider implementation details
- Notification routing for approval prompts
- Provider-specific permission UX beyond normalized capability requests
- Cross-node dispatch approval envelopes (Spec-024 — Plan-027 at Tier 9; this plan provides the category enum only)

## Preconditions

- [x] Paired spec is approved — re-checked 2026-07-18 by the campaign Task-28 / W1.5 batch spec re-promotion (supersedes the 2026-07-13 re-open note): Spec-012 returned to `approved`; its campaign amendment window closed.
- [x] **Driver-ask expiry leg authored (Part-B fail-closed follow-up, 2026-07-17; authored by campaign B13, 2026-07-20):** [Spec-012 §Resolved Questions and V1 Scope Decisions](../specs/012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions) requires the bounded ask-expiry machinery — durable timer-OR-answer, per-kind fail-closed outcomes, `driverAskPayloadRefinementFor`, the persisted `ask_id` association — **authored as T1.5 (driver-ask contract registration) + T2.8 (`driver-ask-normalizer.ts`)** below (the reciprocal run-engine park-edge task landed in [Plan-004](./004-queue-steer-pause-resume.md) as T3.15 via B9). Phase-2 code does not dispatch while this box is unchecked — enforced mechanically by Phase 2's `preconditions:` block (`precondition_box_checked` entry, preflight Gate 5; Gate 7's governance scan deliberately covers only the plan-template trio, so a scoped box needs the phase-level entry); the B13 bundle PR authored T1.5/T2.8 and checked this box.
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [x] **Plan-readiness audit complete per [runbook](../operations/plan-implementation-readiness-audit-runbook.md)** — Tier-6 audit (2026-06-10), this revision: 64 findings adjudicated across A-1..A-28; four-phase `#### Tasks` structure, §Invariants, §Cross-Plan Obligations, and §Ratified Design Decisions authored; api-payload §Plan-012 + §Approval Method-Name Registry, error-contracts §Approval, Spec-006 `approval.canceled`, ADR-012 clarifications, and the Plan-004/Plan-005/Plan-009/Plan-010/Spec-023 reciprocals landed in the same audit PR.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/approval.ts` (D-012-1 — single file, singular noun; replaces the pre-audit `approvals/` directory form)
- `packages/contracts/src/index.ts` + `packages/contracts/src/event.ts` (EXTEND — barrel + event-union registration)
- `packages/runtime-daemon/src/migrations/` (two guarded-block migrations: the three approval tables; the `credential_policy_artifacts` store — campaign B13) + `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND)
- `packages/runtime-daemon/src/approvals/` (Plan-012-owned directory per cross-plan §2: `approval-service.ts`, `approval-event-emitter.ts`, `approval-projection-service.ts` + `__tests__/`)
- `packages/runtime-daemon/src/policy/` (Plan-012-owned directory per cross-plan §2: `policy-artifact.ts`, `permission-check-service.ts`, `grant-evaluator.ts`, `grant-invalidation-service.ts`, `driver-ask-normalizer.ts` + `execution-posture-service.ts` (campaign B13) + `__tests__/` incl. `permission-check-service.bench.ts`)
- `packages/runtime-daemon/policies/` (YAML policy sources) + `scripts/compile-policies.mjs` (D-012-9 compile step, wired into the package build)
- `packages/runtime-daemon/src/ipc/handlers/` (5 `approval.*` binder files + `index.ts` EXTEND)
- `packages/client-sdk/src/approvalClient.ts` + `packages/client-sdk/src/index.ts` + `packages/client-sdk/test/approvalClient.integration.test.ts`
- `apps/desktop/src/renderer/src/approvals/` (`PendingApprovalsView.tsx`, `ApprovalHistoryView.tsx`, `RememberedGrantsView.tsx`, `index.ts`, `__tests__/`)
- ~~`packages/control-plane/src/approvals/approval-projection-service.ts`~~ (STRUCK per D-012-6 — no V1 control-plane approval storage or feed exists under ADR-017; the projection is daemon-local)

## Data And Storage Changes

- Add durable local `approval_requests`, `approval_resolutions`, and `remembered_approval_rules` storage plus invalidation hooks tied to runtime-node trust and membership changes. See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) §Approval Tables for column definitions — the audited DDL there is the verbatim migration content (D-012-2).
- Serve pending and historical approval state from a daemon-local projection (SQL over the approval tables — D-012-6). Cross-participant visibility in V1 rides roster-gated relay distribution of `approval_flow` events into each peer daemon's own local projection (ADR-017 Option B); no control-plane approval storage exists.
- Trust-evaluation inputs are derived at check time, never duplicated into a dedicated trust store (D-012-15; the B13 `credential_policy_artifacts` store below is not one — it persists cited credential-policy documents, not trust-evaluation inputs): node trust ← `node_trust_state.trust_level`; own-node determination ← the daemon's attach-binding owner-participant identity (Plan-003); envelope roots ← Plan-009 `workspaces.fs_root` / Plan-010 `worktrees.fs_root` via `run_execution_contexts` (CP-012-9); unestablishable provenance → strict per-request approval (`Spec-012 §Fallback Behavior`).
- (Campaign B13) Add the content-addressed `credential_policy_artifacts` store: `sha256`-keyed rows persisting the RFC 8785 JCS-canonicalized credential-policy artifact document that `executionPosture.credentialPolicyRef` cites (`Spec-012 §Required Behavior`, campaign B20 posture semantics). Rows persist **write-ahead** — before the first `run.running` posture stamp citing the ref, the ADR-019 spawn-intent ordering discipline — and are retained at least as long as any run event citing them — indefinitely in V1: compaction preserves the stub's posture object (ref included) and the Plan-022 crypto-shred clears `pii_payload` only, so no V1 path deletes a citing row, and growth stays bounded by content-addressing (one row per distinct effective policy, never per run). The deletion leg is T2.9's `pruneUnreferencedArtifacts`, invoked at the V1.1 `gdpr.sessionPurge` completion — the only operation that removes citing rows; Plan-022 reserves its stub in V1 — an explicit future consumer, never a V1 cleanup guarantee. See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) §Credential Policy Artifacts — the audited DDL there is the verbatim migration content (the T1.3 convention).

## API And Transport Changes

- Five `approval.*` JSON-RPC methods (D-012-5): `approval.requestCreate`, `approval.resolve`, `approval.projectionRead`, `approval.ruleList`, `approval.ruleRevoke` — wire contract in [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-012 + §Approval Method-Name Registry (Tier 6). `PermissionCheck` is deliberately NOT a wire method: it is the daemon-internal pre-execution gate (`Spec-012 §Interfaces And Contracts`; D-012-18) — its typed shapes ship in `approval.ts` as the in-process API.
- Error vocabulary in [Error Contracts](../architecture/contracts/error-contracts.md) §Approval — 8 codes (D-012-4; the eighth, `approval.policy_artifact_corrupt`, minted by campaign B13 for T2.9's read-boundary integrity check).
- Driver-native permission requests normalize into canonical approval categories, scopes, and resource descriptors before any record is written; Plan-012 owns the normalizer — authored as T2.8 with the T1.5 driver-ask contract registration (campaign B13; CP-012-6) — and until driver integration executes, the schema-validated `approval.requestCreate` boundary remains the enforced normalization gate. No new wire method lands with the campaign amendment: the ask surface and the execution-posture layer are in-process seams whose durable records ride Spec-006 events (the `driver_ask.*` rows and the `run.running` posture stamp).

## Invariants

| ID | Invariant | Tasks |
| --- | --- | --- |
| **I-012-1** | Single-definition approval symbols: `approval.ts` is the sole definer of `ApprovalCategory`, `ApprovalDecision`, `ApprovalState`, `ApprovalRequestId`, `RememberedRuleId`, and `RememberedScope`; downstream consumers (Plan-015/017/027) import, never redefine. | T1.1 |
| **I-012-2** | Contract↔DDL lockstep: the category / state / decision / scope-kind / invalidation-trigger value sets are byte-identical between `approval.ts` and the migration `CHECK` constraints — pinned by a bidirectional conformance suite. | T1.1, T1.3, T1.4 |
| **I-012-3** | Every approval request row carries requester and session provenance: `requested_by` and `session_id` are NOT NULL, so audit records, the session-keyed projection, and `approval_flow` payload emission never depend on a runs join that cannot exist (run state is event-sourced). | T1.3, T2.3 |
| **I-012-4** | At most one resolution per request, and resolutions carry only explicit decisions (`approved`/`rejected`): expiry and cancellation settle on the request row (`state` + `updated_at`) and the event stream — never a resolution row. | T1.3, T2.3, T2.12 |
| **I-012-5** | Every remembered rule is revocation-targetable: NOT NULL `session_id` / `participant_id` (grantor) / `node_id` / `scope_kind` / origin `created_from_request_id`; `run_id` present iff `scope_kind = 'run'`; `revoked_at` ⇔ `invalidation_trigger` co-presence with the trigger drawn from the closed 4-value set. | T1.3, T2.4, T3.4 |
| **I-012-6** | Effective scope never exceeds requested scope: `approval_resolutions.effective_scope` defaults to the request's scope and is refused when broader (`Spec-012 §Required Behavior`). | T1.3, T2.3 |
| **I-012-7** | No sensitive local action executes before a durably committed allow decision — explicit approval, valid remembered grant, or policy permit; if approval state cannot be durably persisted the action does not proceed (typed `approval.persistence_unavailable`, fail-closed). | T2.2, T2.3, T2.6, T2.7, T2.8, T2.12, T3.10 |
| **I-012-8** | Policy evaluation is fail-closed at both edges: the daemon refuses to start when the compiled policy artifact fails signature verification against the pinned operator key, and an evaluation error yields deny, never allow. | T2.1, T2.2, T2.7, T2.10 |
| **I-012-9** | The 7 `approval_flow` events are the canonical approval record: `EventLogService.append` commit is the durability gate, row projections update after, and replay rebuilds the three approval tables byte-equal — every record shows who granted what scope (AC-3). | T2.5, T2.7 |
| **I-012-10** | Revoke-before-next-use: the grant-match path re-validates rule preconditions (unrevoked; grantor membership active; bound node trust intact) at every permission check before honoring a rule; failed re-validation durably revokes (`revoked_at` + trigger) and emits `approval.rule_revoked` before the check returns, falling back to per-request approval. Push seams shrink the live window; use-time re-evaluation is the correctness mechanism. | T2.4, T2.7, T3.4, T3.9 |
| **I-012-11** | Own-node envelope trust never bypasses escalation approvals, and session membership alone never authorizes execution on another participant's node (`Spec-012 §Required Behavior` + `Spec-012 §Default Behavior` + `Spec-012 §Resolved Questions and V1 Scope Decisions`; AC-2). | T2.2, T2.7, T2.10 |
| **I-012-12** | A body-supplied `approver` never overrides the verified caller identity: absent on the local socket → the daemon records its node-owner participant binding; present → cross-checked (local: vs the owner binding; PASETO surfaces: vs the verified `sub`) — mismatch is rejected with `auth.principal_mismatch` (D-012-12). | T2.3, T3.1 |
| **I-012-13** | Every `approval.*` method validates daemon-side: each of the five methods registers with its canonical Zod request AND response schema, so the substrate rejects malformed params before the handler runs and malformed results before the wire; the three mutations carry `mutating: true`. | T3.1, T3.2, T3.5, T3.7, T3.10 |
| **I-012-14** | `approval.projectionRead` is served by the local daemon only in V1; no control-plane approval read/storage path exists — the projection is SQL over the daemon's own `approval_requests` ⋈ `approval_resolutions` (D-012-6). | T3.3, T3.8 |
| **I-012-15** | The SDK marshals, never derives: `approvalClient` caches nothing, computes no allow/deny verdicts, no grant matching, and no projection joins client-side; the daemon-only transport stance is explicit in the single-factory surface. | T3.6, T3.8 |
| **I-012-16** | One explicit action ⇒ exactly one mutation: no approval-surface code path fires `approval.resolve` or `approval.ruleRevoke` on mount, render, timer, or fanned across records; an in-flight mutation disables its trigger. | T4.1, T4.3, T4.4 |
| **I-012-17** | Remember opt-in is off by default, structurally: the remember control initializes off in every render path; an untouched approve produces a resolve payload with no `rememberedScope` key (`request_only` default); `rememberedScope` is present iff the separate control was explicitly engaged, with a ratified enum kind only. | T4.1, T4.4 |
| **I-012-18** | Admit-not-eject + verbatim state: every record/rule the read returns renders — all five `ApprovalState` members labeled, revoked rules with `revokedAt` + `invalidationTrigger`; the renderer never derives state client-side (`expired` renders only from wire state — no `expiryAt`-vs-now comparison; revocation only from wire fields). | T4.2, T4.3, T4.4 |
| **I-012-19** | Canonical per-record granularity: rendered approvals map 1:1 to canonical records — N wire records yield N cards with N independent action sets; no client-side merging, batching, or group actions in V1 (D-012-16). | T4.1, T4.2, T4.4 |
| **I-012-20** | Allow-listed spawn environment (campaign B13; P0-4): every provider process spawns with the daemon-constructed environment `{...curatedBase, ...runProvisionedVars}` — an explicit allow-list, never ambient daemon-environment inheritance — and the effective credential policy's `denyEnvVars` are stripped at that construction for every subprocess class, with provider-native layers as defense-in-depth beneath it (Codex `shell_environment_policy` `inherit: "none"` + explicit `set`; Claude `sandbox.credentials` env unsets + `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB`) — the spawn-side enforcement leg is Plan-005 T3.14 P0-4, which names this invariant (`Spec-012 §Required Behavior`). | T2.7, T2.9 |
| **I-012-21** | Resident authorizer, no decision cache (campaign B13; ADR-012 P1-7, 2026-07-02): the signature-verified compiled policy set parses once and stays resident in-process; every `PermissionCheck` evaluates per-request — no decision cache exists, so a decision is never served stale against a changed rule, trust level, membership, or posture. | T2.2, T2.11 |
| **I-012-22** | Cedar-pipeline-exclusive adjudication (campaign B13; C-6): every approval adjudication — driver-surfaced permission asks included — routes through this plan's Cedar pipeline; the posture-derived provider configuration pins `approvalsReviewer: "user"` and a turn supplying a divergent reviewer is refused (`Spec-012 §Required Behavior`); provider guardian / auto-review surfaces normalize onto the Plan-005 diagnostic surface (`DriverDiagnosticRecord`, off-timeline), never as adjudications; and expiry never auto-approves — silence is denial. | T2.7, T2.8, T2.10 |

## Cross-Plan Obligations

- **CP-012-1 — Approval category enum + `ApprovalRequestId` provided to Plan-027.** Plan-027's Cedar principal-action mapping and Spec-024 dispatch envelopes consume the canonical 9-category enum from `approval.ts` (cross-plan §3). **Direction:** Provide to [Plan-027](./027-cross-node-dispatch-and-approval.md); return-cite owed at its Tier-9 audit. **Tasks:** T1.1.
- **CP-012-2 — `approval_flow` event registration with Plan-006.** Phase 1 registers the 7 `approval.*` types — `approval.requested`, `approval.approved`, `approval.rejected`, `approval.expired`, `approval.canceled` (added by this audit per D-012-8), `approval.remembered`, `approval.rule_revoked` (`Spec-006 §Approval Flow (approval_flow)`) — into the Plan-006-owned `SessionEventSchema` union as an additive-MINOR EXTEND; Plan-012 authors `ApprovalFlowEventPayloadSchema` (emitter-authors-payload precedent); type names + `approval_flow` category mapping ship in Plan-006 T1.2; this plan's delta is +1 (`approval.canceled`, 125 → 126 at its audit step) — the Tier-6-final census is **130 types across 19 categories** after Plan-016's four same-audit additions (`Spec-006 §Event Type Summary`;`approval_flow` totals 8 there incl. Plan-016's `moderation.review_flagged`). (Campaign B13, 2026-07-20) Plan-012 additionally registers the four `driver_ask.*` types — the third `interactive_request` subfamily, Spec-006 B1 — via T1.5 (payload schemas, category-map entries, union variants; ownership per [Plan-006 §Event Taxonomy Coverage](./006-session-event-taxonomy-and-audit-log.md#event-taxonomy-coverage)'s `interactive_request` row); the four are already counted in the Spec-006 B1 census, so the registration moves no census. Phase 2 consumes `EventLogService.append` as the sole append path. **Direction:** Extend [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (additive; reciprocal recorded in Plan-006 T1.1/T1.2 and the §Event Taxonomy Coverage row). **Tasks:** T1.1, T1.5, T2.5, T2.8.
- **CP-012-3 — `RunIdSchema` consumed from Plan-005's `provider-driver.ts`** (Tier 4; request/payload run bindings). **Direction:** Consume from [Plan-005](./005-provider-driver-contract-and-capabilities.md). **Tasks:** T1.1, T1.2. _(Corrected from Plan-004's `runControl.ts` — `RunIdSchema` homes with the `RunId` brand in `provider-driver.ts` per Plan-005 CP-005-6; authoring it at Plan-004's Tier 5 would be a backward import for Plan-005's Tier-4 `InterruptRunParamsSchema` consumer.)_
- **CP-012-4 — Approval records + contracts provided to Plan-015 / Plan-016 / Plan-017.** [Plan-015](./015-persistence-recovery-and-replay.md) (Tier 7) consumes the approval tables for record recovery; [Plan-016](./016-multi-agent-channels-and-orchestration.md) (Tier 6 sibling — within-tier ordering: its moderation-gate task lands after this plan's Phase 2) consumes `PermissionCheckService.check({category: 'gate'})` as the pre-turn moderation gate plus the CP-004-9 blocking flow for gate-pending runs (Plan-016 D-016-10, reciprocal CP-016-5 — Tier-6 audit); [Plan-017](./017-workflow-authoring-and-execution.md) (Tier 8) consumes the `gate` category, `approval_requests.id` workflow-gate references, and Cedar policy reuse through the `policy/` services (cross-plan §3). **Direction:** Provide; Plan-016's return-cite lands at this audit, Plan-015/017 return-cites owed at their Tier-7/8 audits. **Tasks:** T1.1, T1.3, T2.1, T2.2.
- **CP-012-5 — Run-blocking seam with Plan-004 (reciprocal of CP-004-9).** Plan-012's approval-service provides `registerApprovalOutcomeListener({onBlocking(runId, approvalRequestId, blockingState), onResolved(runId, approvalRequestId, outcome)})` (outcome ∈ `approved | rejected | expired`) plus the inbound `cancelPendingForRun(runId, cause)` — which also closes the run's pending driver asks, both kinds, through T2.8 (campaign B13; one terminal arm, dual-surface settle); Plan-004's run-engine registers the listener and calls cancel from its terminal path (Plan-004 Phase 3 — doc-only reciprocal until it executes). Category→blocking-state mapping: `user_input` / `mcp_elicitation` → `waiting_for_input`; the other 7 categories → `waiting_for_approval`. A rejected or expired outcome continues the run with the action refused — never terminates it (run-state-machine §Complete Transition Table), except the denied pre-turn moderation gate (`category: 'gate'`), which Plan-016 system-cancels with `trigger: 'moderation_denied'` (Spec-016 D-016-10). (Campaign B13) An expired permission-kind driver ask rides exactly this seam's `expired` outcome — deny-and-continue; a driver **input** ask never traverses it: its expiry takes Plan-004 T3.15's `waiting_for_input → paused` park edge (`Spec-012 §Resolved Questions and V1 Scope Decisions`). The blocking states double as the idle sweep's pending-blocking-work exemption carrier — entered through this seam's `onBlocking` for pipeline-backed blocks and through T2.8's input-wait signal for input-kind asks (`running → waiting_for_input`, Codex round 3): Plan-016's T2.7 blocking-state hard-skip (its scheduled campaign-B15 sweep leg, reading run state via Plan-004's CP-004-7 `getRun`) skips a blocked run, and resolution/expiry exiting the state ends the exemption — no Plan-012-owned sweep predicate exists. **Direction:** Provide to [Plan-004](./004-queue-steer-pause-resume.md) (reciprocal CP-004-9). **Tasks:** T2.6, T2.8.
- **CP-012-6 — Driver approval-request normalization owed toward Plan-005 (reciprocal of CP-005-7).** Plan-012 owns the normalizer mapping Plan-005's driver-surfaced permission asks (`interactive_request` driver events / driver-host permission callbacks) into canonical `approval.requestCreate` inputs (`Spec-012 §Required Behavior`) — **authored as T1.5 + T2.8 by the campaign's B13 bundle (2026-07-20)**. The registered seam serves both CP-005-7 classes under **per-class capability gates**: provider-native `interactive_request` driver events are admission-gated on the driver's persisted `capabilities.flags.interactive_requests === true` (P2-8), while driver-host permission callbacks arrive through Plan-005's `callback-tool-host` — available on the independent `callback_tools` flag, so a `callback_tools`-capable driver is served even with `interactive_requests: false` — both entering T2.2's composed `check()` per T2.10: evaluate first, create-on-ask, never a bare create. Drivers lacking granular permission requests inherit the stricter daemon boundary (`Spec-012 §Fallback Behavior`), and until driver integration executes, the schema-validated `requestCreate` boundary remains the enforced normalization gate. **Direction:** Consume from [Plan-005](./005-provider-driver-contract-and-capabilities.md) (reciprocal CP-005-7). **Tasks:** T1.5, T2.3, T2.8.
- **CP-012-7 — Five `approval.*` methods registered under Plan-007's `MethodRegistry`.** New singular `approval` namespace root (ratified into the Tier-1 root enumeration at this audit, D-012-5); registration gated on BL-142 (four of five tails are camelCase) and wire-observable typed errors on BL-143 (`DaemonDomainError` projection). Plan-007 owns the registry surface, not these handlers. **Direction:** Register with [Plan-007](./007-local-ipc-and-daemon-control.md). **Tasks:** T3.1, T3.2, T3.5, T3.7.
- **CP-012-8 — Plan-023 owns the `window.sidekicks` preload bridge the Phase 4 views project over.** Views are thin bridge projections (exported + barreled, not App-shell-mounted); live IPC dispatch lands at Plan-023 Tier 8 — until then bridge methods throw `NotImplementedAtTier1Error` and rejected/error branches are the production-observable paths (Plan-009 CP-009-6 / Plan-010 CP-010-10 posture). Plan-023's Tier-8 audit also reconciles the Spec-023 §Approvals View gloss against the D-012-5 registry (the Tier-6 working copy already supersedes the pre-audit `approvals.listPending` form). **Direction:** Honor [Plan-023](./023-desktop-shell-and-renderer.md). **Tasks:** T4.1–T4.5.
- **CP-012-9 — Workspace/worktree envelope roots consumed from Plan-009 + Plan-010 (reciprocals CP-009-8 / CP-010-11).** The remembered-scope matcher and the own-node envelope evaluation resolve path-category prefix containment against Plan-009 `workspaces.fs_root` and Plan-010 `worktrees.fs_root` via the `run_execution_contexts` per-run binding; reads only — Plan-012 never writes sibling-owned rows; unestablishable provenance falls back to strict per-request approval (`Spec-012 §Fallback Behavior`). Within-tier ordering: the phases owning those tables build before Plan-012 Phase 2. **Direction:** Consume from [Plan-009](./009-repo-attachment-and-workspace-binding.md) + [Plan-010](./010-worktree-lifecycle-and-execution-modes.md). **Tasks:** T2.2, T2.4.
- **CP-012-10 — `node_trust_state.trust_level` read + elevation write with Plan-003.** The evaluator reads trust at every check with the fail-closed READ boundary (any out-of-set value is treated as `untrusted` — D-012-15); the Plan-012-owned elevation write parses the V1 value set (`'untrusted' | 'envelope_trusted'`) at the boundary; node-trust transitions feed the rule-invalidation push seam. **Direction:** Consume from [Plan-003](./003-runtime-node-attach.md) (dep-map-recorded; plan-body return-cite tracked in the Tier-6 REVIEW). **Tasks:** T2.2, T2.4, T3.4.
- **CP-012-11 — Resolved-posture read for the `run.running` stamp, provided to Plan-004 (campaign B13).** T2.9 registers its posture-resolution leg on the CP-004-8 `RunSetupGate` seam — whose `assertRunReady` is void-returning (Plan-004 T3.10) — and exposes the run-keyed `resolvedPostureFor(runId)` accessor beside it; Plan-004's T3.7 `run.running` emitter reads the accessor at stamp time so the event carries the complete validated posture object (`Spec-012 §Required Behavior`), and a stamp finding no resolved posture fails closed — a sandboxed run never starts unstamped. **Direction:** Provide to [Plan-004](./004-queue-steer-pause-resume.md) (consumed at its Phase-3 execution; recorded in its Tier-6 reciprocal note beside CP-004-9). **Tasks:** T2.9.

## Ratified Design Decisions (Tier-6 audit, 2026-06-10)

- **D-012-1 — Contracts file is `packages/contracts/src/approval.ts`** (single file, singular noun; replaces the pre-audit `approvals/` directory form): one-file-per-domain per the shipped `session.ts`/`event.ts`/`repo.ts`/`worktree.ts` convention; avoids firing the BL-097 §7(d) deferred subdirectory-convention call.
- **D-012-2 — Persistence model (3 tables; DDL verbatim in local-sqlite-schema.md §Approval Tables).** `approval_requests` carries `session_id` + `requested_by` NOT NULL (provenance; no FK targets — sessions are control-plane rows, runs are event-sourced), required `resource_descriptor` (JSON), nullable `ask_id` (the originating driver-ask association, set iff normalizer-minted — Part-B fail-closed follow-up, 2026-07-17), nullable `expiry_at` guarded by the ask-implies-deadline CHECK (`ask_id IS NULL OR expiry_at IS NOT NULL` — an association never persists without its shared deadline, else restart could not re-arm the timeout), 5-state CHECK incl. `canceled`, and `updated_at` (expiry/cancel settle on the row — no resolution row). `approval_resolutions` carries `request_id` as PRIMARY KEY (the durable wire id `approvalRequestId` — enforces the 1:1 decision row, replaces the former minted `id` + unique per-request index, and keeps every projection column wire-derivable per I-012-9), `approver_id` NOT NULL, 2-value `decision` CHECK, `effective_scope` NOT NULL, and nullable `remembered_scope_kind`/`remembered_scope_pattern`. `remembered_approval_rules` carries grantor `participant_id`, `session_id`, `node_id`, origin FK `created_from_request_id` (→ `approval_resolutions(request_id)`, the durable id carried on `approval.remembered`), `run_id` with CHECK `(scope_kind = 'run') = (run_id IS NOT NULL)`, explicit `scope_kind` enum, nullable `scope_pattern`, and the `revoked_at` ⇔ `invalidation_trigger` co-presence CHECK over the closed trigger set `{explicit, membership_change, node_trust_change, session_end}`.
- **D-012-3 — Consolidated wire block.** Six request/response pairs + `RememberedRuleId` brand + `RememberedScope` + `InvalidationTrigger` + `ApprovalFlowEventPayload` ratified in api-payload-contracts.md §Plan-012: create carries required `resourceDescriptor`; resolve carries optional `approver` + `effectiveScope?` + `rememberedScope?` and returns the recorded `approverId` + `effectiveScope`; the projection row carries `requestedBy`, `resourceDescriptor`, `updatedAt`, and the resolved quad (`resolvedAt`/`decision`/`approverId`/`effectiveScope`) present iff state ∈ {approved, rejected}; rule rows carry grantor, node, optional `runId`, and revocation metadata; the `ApprovalFlowEventPayload` carries the projection-rebuild fields (`requestedBy`/`resourceDescriptor`/`expiryAt` on `requested`, `approver`/`effectiveScope` on the resolution events, and the rule projection on `remembered` — grantor = `approver`, bound `nodeId`, binding = `rememberedScope`/`runId`, origin = `approvalRequestId`) so relayed or replayed events alone rebuild each daemon's projection rows, `remembered_approval_rules` included (D-012-6/D-012-7, I-012-9).
- **D-012-4 — Error vocabulary (§Approval, 7 rows).** `approval.not_found` 404, `approval.already_resolved` 409, `approval.request_expired` 410 (renamed from the pre-audit `approval.expired` — error codes never collide byte-for-byte with Spec-006 event names), `approval.request_canceled` 409 (new; non-colliding with the `approval.canceled` event), `approval.persistence_unavailable` 503 (fail-closed), `approval.rule_not_found` 404, `approval.rule_already_revoked` 409. Deliberate absence: no `approval.permission_denied` in V1 — the local IPC surface carries no caller principal, `PermissionCheck` denial is a normal `{allowed: false}` response, and minting a cross-participant authorization code is Plan-027's call.
- **D-012-5 — Method registry: five strings on the new singular `approval` root** (`approval.requestCreate`, `approval.resolve`, `approval.projectionRead`, `approval.ruleList`, `approval.ruleRevoke`), ratified in api-payload-contracts.md §Approval Method-Name Registry with the root added to the Tier-1 namespace-root enumeration. `PermissionCheck` registers NO wire method string: zero V1 wire consumers, and a wire preflight would invite stale-verdict (TOCTOU) authorization against the security gate. "Rule" (not "grant") is the operation vocabulary — matching `remembered_approval_rules`, `approval.rule_revoked`, and `RememberedRuleId`. Supersedes the pre-audit Spec-023 plural `approvals.listPending` gloss.
- **D-012-6 — Projection is daemon-only.** The pre-audit control-plane `approval-projection-service.ts` Target Area is struck: shared Postgres stores zero approval tables (ADR-017 anchors-only; shared-postgres-schema invariants), so a control-plane projection would violate the ratified storage posture, not extend it. `approval-projection-service.ts` lands in Plan-012-owned `packages/runtime-daemon/src/approvals/`; cross-participant visibility rides roster-gated relay distribution of `approval_flow` events into each peer daemon's own projection (ADR-017 Option B). V1.1 revisit rides the MLS promotion gates.
- **D-012-7 — Events-canonical; rows are projections.** `EventLogService.append` commit IS the `Spec-012 §Fallback Behavior` durability gate; row projection updates after; startup reconcile replays `approval_flow` events newer than the rows' high-water mark; a failed append surfaces `approval.persistence_unavailable` and the gated action does not proceed.
- **D-012-8 — `approval.canceled` is an additive durable event (7 `approval_flow` variants).** Emitted when `cancelPendingForRun` settles pending requests on run-terminal; `Spec-006 §Approval Flow (approval_flow)` gains the row and the registry moves 125 → 126. Expiry/cancel settle on the request row (`state` + `updated_at`) — never a resolution row. Explicit-over-derived: a projector-derived cancellation would leave the `approval_flow` audit timeline incomplete.
- **D-012-9 — V1 policy engine is `@cedar-policy/cedar-wasm` in-process (`isAuthorized`) over the build-embedded, signature-verified compiled policy set.** The verified artifact (as enforced in-process) is the canonical bytes of the compiled Cedar policy set + a detached signature under the operator release key with the daemon's pinned signing algorithm (Ed25519 default, ECDSA P-256 compliance fallback — `ADR-012 §Signing Algorithm`), verified against the pinned `OPERATOR_PUBLIC_KEY` on every daemon start BEFORE any `PermissionCheck`; verification failure → refuse to start (typed log `policy-artifact-signature-invalid`). Container-image signing remains the distribution pipeline's supply-chain layer; the compiled-policy-set verification above applies in **both** topologies (the shell-spawned desktop daemon where no image exists, and containerized/self-hosted daemon builds), and containerized daemons additionally refuse to start if the launched image cannot be verified against the pinned operator public key (`ADR-012 §Scope By Phase` / 2026-06-10 Decision Log — both topologies). YAML sources live at `packages/runtime-daemon/policies/default-policy.yaml`; `scripts/compile-policies.mjs` compiles them in the package build; content transcribes security-architecture §Permission Matrix + `Spec-012 §Default Behavior`. Package name corrected from the pre-audit `@cedarpolicy/cedar-wasm` (nonexistent on npm — dependency-confusion hazard); version pin 4.11.x (current stable at amendment date). **V1.1** (criterion-gated, ADR-012): (a) `policy-bundle-v{N}.cedar.tar.gz` build/sign/verify CLI surface (`sidekicks policy bundle build/sign/verify`), (b) daemon-side verifier with monotonic version counter and rollback rejection, (c) `ApprovalPolicyEngineUnavailable` recovery-status plumbing, (d) freshness-window timestamp enforcement — the V1→V1.1 delta is policy SOURCE only (embedded → runtime signed bundle); the evaluator does not change. Operational procedures: [Cedar Policy Signing And Rotation](../operations/cedar-policy-signing-and-rotation.md).
- **D-012-10 — Remembered-scope matching semantics.** `RememberedScope { kind: 'run' | 'session'; pattern?: string }`; `request_only` is expressed by OMITTING `rememberedScope`, never an enum member. Candidate set: unrevoked ∧ same `session_id` ∧ same `node_id` ∧ same `category` ∧ (kind = `session` ∨ (kind = `run` ∧ rule.run_id = request.runId)). Pattern absent → category-wide within that boundary; pattern present → category-derived semantics: path categories (`file_write`, `destructive_git`) = normalized-absolute-path prefix containment against the envelope roots; `network_access` = exact host equality (no wildcards in V1); all other categories = exact scope-token equality. The rule's `participant_id` is the GRANTOR — an audit + membership-invalidation key, never a match key (the spec's scope axes are run/session, never actor).
- **D-012-11 — Invalidation: on-next-use re-evaluation is the correctness mechanism.** The grant-match path re-validates every rule's preconditions at use time; push seams — a synchronous `node_trust_state` transition hook and a `membership.*` event-application hook (the daemon's local projection of relay-delivered membership events) — call `revokeDependentRules` to shrink the live window and emit timely `approval.rule_revoked` events, but they are the audit/timeliness layer: relay delivery is asynchronous, so only use-time re-validation satisfies revoke-before-next-use (`Spec-012 §Fallback Behavior`). Unresolvable membership input → fail closed (`Spec-012 §Fallback Behavior`).
- **D-012-12 — Approver identity model.** Wire `approver?: ParticipantId` is OPTIONAL and informational: absent on the local socket → the daemon records its node-owner participant binding (the same attach-binding read as D-012-15's own-node determination); present → cross-checked (local: vs the owner binding; PASETO-authenticated surfaces: vs the verified `sub`) — mismatch → `auth.principal_mismatch`. `approval_resolutions.approver_id` stays NOT NULL (the RECORD is mandatory per `Spec-012 §Required Behavior`); `ApprovalResolveResponse.approverId` returns the recorded server truth (AC-3 observable). The renderer never populates an approver identity.
- **D-012-13 — Run-blocking seam shape.** Provider-registration directionality mirrors shipped CP-004-8 (`RunSetupGate`): Plan-012 exposes `registerApprovalOutcomeListener` + `cancelPendingForRun`; Plan-004's engine registers and drives run-state transitions — Plan-012 never mutates run state. `onBlocking` fires after the durable commit of the pending request; `onResolved` fires per resolution path (approved / rejected / expired) exactly once; rejected/expired continue the run with the action refused.
- **D-012-14 — Expiry is hybrid.** Lazy settle on every read/match touching a pending request (zero staleness at decision time) + a 60-second daemon sweep (timely `approval.expired` emission + parked-run unblock via the D-012-13 seam with outcome `expired`); expiry/cancel events carry `actor: null` (system actor, node-emitter convention).
- **D-012-15 — Trust-evaluation inputs are derived, never duplicated.** No dedicated trust store (the campaign-B13 `credential_policy_artifacts` store persists credential-policy documents, not trust-evaluation inputs): node trust ← `node_trust_state.trust_level` with the V1 value set ratified as `'untrusted' | 'envelope_trusted'` and fail-closed READ-boundary enforcement (any out-of-set value evaluates as `untrusted`; the Plan-012-owned elevation write parses at the boundary; no retroactive DDL CHECK on Plan-003's shipped migration); own-node determination ← the daemon's attach-binding owner-participant identity; envelope roots ← `run_execution_contexts` joined to `workspaces.fs_root` / `worktrees.fs_root` (CP-012-9); unknown provenance → strict per-request (`Spec-012 §Fallback Behavior`).
- **D-012-16 — Renderer approval posture: allow-only remembered rules, ungrouped V1 rendering.** `rememberedScope` is schema-refined valid only with `decision: "approved"` (`{rejected, rememberedScope}` → typed rejection; the remember control binds to Approve only; deny-by-default remains Cedar's job). V1 renders one card per canonical record with per-record actions — grouping is a permitted Tier-8+ presentation addition that must preserve per-record identity/actions (deliberate non-exercise of `Spec-012 §Implementation Notes`'s allowance; group-approve would fan one click into N mutations against AC-1's explicit-approval semantics).
- **D-012-17 — `PermissionCheckResponse.reason` is a 5-value enum** (`policy_allow | remembered_rule | approved | pending_approval | denied`): `policy_allow` = Cedar/own-node-envelope permit with no human approval artifact; `remembered_rule` = matched an unrevoked rule that passed at-use re-validation; `approved` = a recorded approved resolution covers the exact request; `pending_approval` = request created/open (`allowed: false`, `approvalRequestId` present); `denied` = Cedar forbid, rejected/expired resolution, or fail-closed refusal (the typed 503 additionally surfaces on fail-closed paths so audit distinguishes them). Invariants: `allowed === (reason ∈ {policy_allow, remembered_rule, approved})`; the `policy_allow`/`approved` split preserves the audit distinction between machine permit and human grant.
- **D-012-18 — No wire preflight; the in-process `PermissionCheckService.check()` is the composed gate.** Evaluate → on an ask-policy outcome create the request + emit `approval.requested` + notify the run-blocking seam → return `pending_approval` with the new id. The composition is `Spec-012 §Interfaces And Contracts`'s enforcement seam; splitting evaluate-from-create at a wire boundary would reintroduce the TOCTOU window.

## Implementation Phase Sequence

Contracts: see [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-012 + §Approval Method-Name Registry for the typed schemas and method strings every phase consumes verbatim.

### Phase 1 — Approval contracts + persistence

**Goal.** The `approval.ts` contract domain (brands, enums, `RememberedScope`, six wire pairs, 7-variant `approval_flow` event registration) and the three-table migration, with contract↔DDL conformance pinned. **Campaign B13 adds:** the T1.5 driver-ask contract registration — `DriverAskEventPayloadSchema` + `driverAskPayloadRefinementFor` with the four `driver_ask.*` union variants, riding the same union seam.

**Preconditions.**

- [ ] Plan-006 Phase 1 merged (`SessionEventSchema` union seam + `SESSION_EVENT_TYPES` / `SESSION_EVENT_CATEGORY_BY_TYPE` registry — Tier 4; the re-opened Phase 1's T1.9 `withEpochStamp` composition helper is additionally consumed by T1.5 — campaign B13)
- [ ] Plan-005 Phase 4 merged (`packages/contracts/src/provider-driver.ts` — `RunIdSchema` import origin per CP-012-3; Tier 4)
- [x] D-012-1 contracts filename ratified — `approval.ts` (Tier-6 audit)
- [x] D-012-2 DDL ratified — local-sqlite-schema.md §Approval Tables (Tier-6 audit)
- [x] D-012-3 wire shapes ratified — api-payload-contracts.md §Plan-012 (Tier-6 audit)
- [x] D-012-8 `approval.canceled` adjudicated — seven `approval_flow` variants, Spec-006 registry 126 (Tier-6 audit)
- [x] `Spec-006 §Approval Flow (approval_flow)` payload amendment ratified — `ruleId?` / `invalidationTrigger?` fields (`Spec-006 §Approval Flow (approval_flow)`)

#### Tasks

- **T1.1 — `approval.ts` contract core + event registration.**
  - **Files:** `packages/contracts/src/approval.ts` (CREATE), `packages/contracts/src/index.ts` (EXTEND — `export * from "./approval.js";`), `packages/contracts/src/event.ts` (EXTEND — additive registration of the 7 `approval.*` types per CP-012-2)
  - Define and export: `ApprovalRequestId` + `RememberedRuleId` branded UUID schemas via `brandedUuidIdSchema`; `ApprovalCategorySchema` over the 9 categories (`tool_execution | file_write | network_access | destructive_git | user_input | plan_approval | mcp_elicitation | gate | human_phase_contribution` — SA-12 member included); `ApprovalDecisionSchema` over `approved | rejected`; `ApprovalStateSchema` over `pending | approved | rejected | expired | canceled`; `RememberedScopeSchema` `{kind: 'run' | 'session', pattern?}` (D-012-10); `InvalidationTriggerSchema` over `explicit | membership_change | node_trust_change | session_end`; `ApprovalFlowEventPayloadSchema` `.strict()` over `{sessionId, runId?, approvalRequestId?, askId?, category, scope, requestedBy?, resourceDescriptor?, expiryAt?, approver?, effectiveScope?, nodeId?, rememberedScope?, ruleId?, invalidationTrigger?}` (`Spec-006 §Approval Flow (approval_flow)` shape; the projection-rebuild fields ride `requested` and the resolution events per D-012-3/D-012-6/D-012-7; free-form `scope` rides the event.ts length-cap + NUL-rejection guards); plus the exported per-variant emission refinement `approvalFlowPayloadRefinementFor(eventType)` — `approval.requested` ⇒ `runId`/`approvalRequestId`/`requestedBy`/`resourceDescriptor` present, plus the origin-blind conditional `askId` present ⇒ `expiryAt` present (the ask-implies-deadline pairing at the emission boundary — schema-level mirror of T1.3's projection CHECK; the refinement tests pin an `askId`-bearing requested payload without `expiryAt` refusing at parse); `approval.approved`/`approval.rejected` ⇒ `approvalRequestId`/`approver`/`effectiveScope`; `approval.expired`/`approval.canceled` ⇒ `approvalRequestId`; `approval.remembered` ⇒ `approvalRequestId`/`approver`/`nodeId`/`rememberedScope`/`ruleId` (+ `runId` iff `rememberedScope.kind = 'run'`, the rules-table CHECK mirror); `approval.rule_revoked` ⇒ `ruleId`/`invalidationTrigger`; `askId` rides `approval.requested` on driver-ask-originated requests — its presence is required at the CP-012-6 normalizer seam, the sole such emitter, not in the origin-blind refinement, which cannot know origin and enforces the `askId` ⇒ `expiryAt` pairing above instead (Part-B fail-closed follow-up, 2026-07-17) — a malformed event fails at the emission parse, never at peer/restart projection (I-012-9). Register the seven `approval_flow` variants — `approval.requested`, `approval.approved`, `approval.rejected`, `approval.expired`, `approval.canceled`, `approval.remembered`, `approval.rule_revoked` — into the Plan-006-owned `SessionEventSchema` discriminated union as an additive-MINOR EXTEND (typed interface + named `*PayloadSchema` const + inline rebuilt variant + `SESSION_EVENT_TYPES` tuple + `SESSION_EVENT_CATEGORY_BY_TYPE` entries under `approval_flow`).
  - **Tests:** `packages/contracts/src/__tests__/approval.test.ts` (CREATE) — parse-accept all 9 categories, both decisions, all 5 states; parse-reject a 10th category, a 3rd decision, a 6th state, a non-UUID brand input, an out-of-set `RememberedScope.kind`; barrel-resolution tripwire (`ApprovalCategorySchema` AND `SessionEventSchema` both resolve from the package root — a colliding `export *` name is silently dropped); event-union: `SessionEventSchema` parses an `approval.requested` fixture and an `approval.rule_revoked` fixture carrying `ruleId` + `invalidationTrigger`, and rejects an approval payload missing `category`, a variant under a wrong category, and an unknown extra payload key (`.strict()`); per-variant refinement rows: an `approval.requested` fixture missing `requestedBy` / `resourceDescriptor` / `runId` rejects at the refinement while passing the broad schema, an `approval.remembered` fixture missing `nodeId` rejects, an `approval.rule_revoked` fixture missing `invalidationTrigger` rejects.
  - **Spec coverage:** Spec-012 line 48 (canonical 9-category enum), Spec-012 line 57 (SA-12 member), Spec-012 line 118 (remembered scope as explicit enum), Spec-006 line 251 (payload shape), Spec-006 lines 255–261 (the seven `approval_flow` rows)
  - **Verifies invariant:** I-012-1, I-012-2 (contract half)
  - **Consumes:** `brandedUuidIdSchema` ← `packages/contracts/src/internal/branded.ts` (shipped); `SessionIdSchema` / `ParticipantIdSchema` ← Plan-001 Phase 2 `session.ts` (shipped Tier 1); `RunIdSchema` ← Plan-005 Phase 4 `provider-driver.ts` (CP-012-3); `NodeId` brand ← Plan-003 `runtime-node.ts` (shipped Tier 3); `SessionEventSchema` union seam + type literals + category mapping ← Plan-006 Phase 1 T1.1/T1.2 (CP-012-2 — Plan-006 working assigns `approval_flow` payload/emission ownership to Plan-012)
- **T1.2 — Six wire request/response schema pairs.**
  - **Files:** `packages/contracts/src/approval.ts` (EXTEND)
  - Zod schemas + inferred types implementing api-payload-contracts.md §Plan-012 verbatim: `ApprovalRequestCreateRequest/Response` (required `resourceDescriptor`; optional `expiryAt`); `ApprovalResolveRequest/Response` (optional informational `approver` per D-012-12; `effectiveScope?` defaulting server-side; `rememberedScope?` schema-refined valid only with `decision: "approved"` per D-012-16; response carries recorded `approverId` + `effectiveScope` + `resolvedAt`); `PermissionCheckRequest/Response` (in-process shapes — D-012-5 registers no wire string; `reason` 5-enum with the D-012-17 refinements `allowed === (reason ∈ {policy_allow, remembered_rule, approved})` and `approvalRequestId` iff `pending_approval`); `ApprovalProjectionReadRequest/Response` (state/category filters; items per D-012-3 incl. `requestedBy`, `resourceDescriptor`, `updatedAt`, resolved quad iff state ∈ {approved, rejected}, `rememberedScope` iff the resolution minted a rule); `RememberedRuleListRequest/Response` (`includeRevoked?`; rule items incl. grantor `participantId`, `nodeId`, `runId?` iff kind = `run`, revocation metadata); `RememberedRuleRevokeRequest/Response` (`invalidationTrigger: "explicit"`).
  - **Tests:** parse-accept one fixture per pair; parse-reject a create missing `resourceDescriptor`; refinement rejections — `{allowed: true, reason: "denied"}`, `reason: "pending_approval"` without `approvalRequestId`, `{decision: "rejected", rememberedScope}` (allow-only), a projection item with `state: "approved"` missing `approverId` and one with `state: "pending"` carrying `decision`; parse-reject a 6th `reason` and an out-of-set rule `invalidationTrigger`; assert resolve-`approver` optionality and response-`approverId` requiredness (D-012-12).
  - **Spec coverage:** Spec-012 line 96 (create carries category, scope, requested resource, expiry policy), Spec-012 line 58 (request record fields), Spec-012 line 97 (resolve carries approver, decision, remembered-scope request, audit metadata), Spec-012 line 59 (resolution records approver, decision, effective scope), Spec-012 line 98 (PermissionCheck runs inside the local daemon), Spec-012 line 47 (the approval-or-prior-grant outcome space the `reason` enum discriminates), Spec-012 line 99 (projection surfaces pending + historical state), Spec-012 AC3 (line 132 — who granted what scope), Spec-012 line 106 (explicit revocation path)
  - **Verifies invariant:** none (contract-shape task; enforcement invariants bind on Phase-2/3 services)
  - **Consumes:** brands/enums/`RememberedScopeSchema` ← T1.1; `RunIdSchema` ← Plan-005 Phase 4 (CP-012-3); `SessionIdSchema` / `ParticipantIdSchema` ← Plan-001 (shipped); `NodeId` ← Plan-003 (shipped)
- **T1.3 — Three-table migration + runner registration.**
  - **Files:** `packages/runtime-daemon/src/migrations/0NNN-approvals.ts` (CREATE — NNN = next free version per migration-runner append order at PR-open time; no cross-plan FK target, so within-tier ordering is free), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — version-N guarded `if (!hasMigrationApplied(db, N))` block with `db.transaction(...).immediate()` + in-transaction re-check, per the runner's documented extension contract), `packages/runtime-daemon/src/approvals/__tests__/approval-migration.test.ts` (CREATE — first content of the Plan-012-owned `approvals/` directory)
  - SQL is an inlined TypeScript string constant copied verbatim from local-sqlite-schema.md §Approval Tables (D-012-2), including the `-- Owner: Plan-012` headers and semantics comments, plus its own `schema_version` INSERT — the `0002-runtime-node.ts` convention. Creates ONLY the three approval tables + indexes (`idx_approval_requests_run`, `idx_approval_requests_session`, partial pending `idx_approval_requests_state`, unique partial ask `idx_approval_requests_ask` on `(run_id, ask_id)` (Part-B fail-closed follow-up, 2026-07-17 — one approval row per native ask; the migration tests pin the uniqueness collision on a duplicate normalizer re-mint and the ask-implies-deadline CHECK), `idx_remembered_rules_participant`, partial unrevoked `idx_remembered_rules_session` / `idx_remembered_rules_node`). The only FKs are internal (`approval_resolutions.request_id` — now the PRIMARY KEY, `remembered_approval_rules.created_from_request_id`); `run_id` / `session_id` / `participant_id` / `node_id` / `requested_by` carry NO REFERENCES (event-sourced / control-plane parents — `interventions.target_run_id` precedent).
  - **Tests:** exact column sets per table; CHECK rejections — 10th `category` (both CHECK-bearing tables), 6th `state`, 3rd `decision`, out-of-enum `scope_kind` / `remembered_scope_kind` / `invalidation_trigger`, a rule with `revoked_at` set but `invalidation_trigger` NULL (co-presence, both directions), a `scope_kind = 'run'` rule with NULL `run_id` (and the converse); NOT NULL rejections — `requested_by`, `session_id`, `effective_scope`, rule `node_id`; UNIQUE — second resolution for one `request_id` fails; FK enforcement under `foreign_keys = ON`; idempotent re-run via `openDatabase` reopen.
  - **Spec coverage:** Spec-012 line 105 (durable + replayable), Spec-012 line 58 (request row fields), Spec-012 line 59 (resolution row fields), Spec-012 line 106 (revocation audit columns), Spec-012 line 82 (session-scoped rules), Spec-012 line 107 (invalidation linkage representable), Spec-012 line 111 (run-scoped rule binding), Spec-012 line 90 (durable persistence gates sensitive actions)
  - **Verifies invariant:** I-012-2 (DDL half), I-012-3, I-012-4, I-012-5, I-012-6 (column half)
  - **Consumes:** migration-runner guarded-block seam + `openDatabase` ← Plan-001 Phase 3 (shipped Tier 1); DDL ← local-sqlite-schema.md §Approval Tables (D-012-2)
- **T1.4 — Contract↔DDL enum-conformance suite (drift tripwire).**
  - **Files:** `packages/runtime-daemon/src/approvals/__tests__/approval-contract-conformance.test.ts` (CREATE)
  - Over a real temp-file handle from `openDatabase`: bidirectional lockstep — every value each contract schema accepts INSERTs into its CHECK-bound column(s), and a canary set the schema rejects (10th category, casing variants) is rejected by the CHECKs; asserted for `ApprovalCategorySchema` ↔ both `category` CHECKs, `ApprovalStateSchema` ↔ `state`, `ApprovalDecisionSchema` ↔ `decision`, `RememberedScopeSchema.kind` ↔ both scope-kind CHECKs, `InvalidationTriggerSchema` ↔ `invalidation_trigger`.
  - **Tests:** the suite IS the tests (plus a mutation canary — a deliberately-wrong fixture set fails).
  - **Spec coverage:** Spec-012 line 48 (closed canonical category set), Spec-012 line 118 (Remembered scopes as explicit enums)
  - **Verifies invariant:** I-012-2
  - **Consumes:** `openDatabase` ← Plan-001 (shipped); contract schemas ← T1.1; migration ← T1.3
- **T1.5 — Driver-ask contract registration: `driver_ask.*` payload schema + union variants + emission refinement (campaign B13, 2026-07-20).**
  - **Files:** `packages/contracts/src/approval.ts` (EXTEND — the Plan-012 contracts domain per D-012-1), `packages/contracts/src/event.ts` (EXTEND — additive registration of the 4 `driver_ask.*` types per CP-012-2's driver-ask clause), `packages/contracts/src/__tests__/approval.test.ts` (EXTEND)
  - Plan-012 registers the four `driver_ask.*` types — `driver_ask.requested`, `driver_ask.responded`, `driver_ask.expired`, `driver_ask.canceled`, the third `interactive_request` subfamily (Spec-006 B1, 2026-07-02) — per [Plan-006 §Event Taxonomy Coverage](./006-session-event-taxonomy-and-audit-log.md#event-taxonomy-coverage)'s `interactive_request` row: payload schemas, category-map entries, and union variants land with this task (the four types are already counted in the Spec-006 census — no census change). Define and export: `DriverAskKindSchema` (`permission | input`), `DriverAskStateSchema` (`requested | responded | expired | canceled`), `DriverAskEventPayloadSchema` `.strict()` over `{sessionId, runId, askId, kind, toolName?, prompt?, input?, expiresAt?, state, response?}` (`Spec-006 §Driver Ask Events` shape; `state` MUST equal the emitting type's suffix — denormalized so latest-row projections read one field, and a mismatch fails loud at emission, never repairs silently); plus the exported per-variant emission refinement `driverAskPayloadRefinementFor(eventType)` — sibling of T1.1's `approvalFlowPayloadRefinementFor`: `driver_ask.requested` ⇒ `expiresAt` present (the bounded fail-closed deadline stamped at creation, `Spec-012 §Required Behavior` Part-B); `kind: 'permission'` ⇒ `input` present on **every** state (`Spec-006 §Driver Ask Events`' kind-scoped MUST — the normalized requested resource stays auditable on terminal rows, echoed forward like the deadline, Plan-004 T3.11's closed-pair `canceled` row included); `driver_ask.responded` ⇒ `response` present (the delivered answer) — and `response` is **exclusive to it**: the refinement rejects `response` on `requested` / `expired` / `canceled`, so a timeout or cancellation row can never carry a fabricated answer; and the three later states — `responded` / `expired` / `canceled` — ⇒ `expiresAt` present (the requested row's deadline echoed forward unchanged, `Spec-006 §Driver Ask Events` + the api-payload-contracts refinement pre-documentation: every post-amendment ask carries a deadline, so a later-state row without one is an emitter bug — Plan-004 T3.11's closed-pair late-append composes this same refinement, its epoch-stamped `requested` + `canceled` pair carrying the deadline on both rows). Base-type optionality admits pre-amendment history at replay only (formally — no `driver_ask.*` rows predate the amendment in any real log) — the refinement binds every emission, so a malformed row refuses at the emission parse, never at peer/restart projection. The union variants register into the Plan-006-owned `SessionEventSchema` discriminated union as an additive-MINOR EXTEND under `interactive_request` (typed interface + named `*PayloadSchema` const + inline rebuilt variant + `SESSION_EVENT_TYPES` tuple + `SESSION_EVENT_CATEGORY_BY_TYPE` entries), with the `requested` + `canceled` variants — and **only** those two — composed through Plan-006 T1.9's `withEpochStamp(...)` helper: Spec-006's carrier registration admits the `sourceEpoch`/`sourcePosition` pair on exactly the `interactive_request` **closed pair** among these types (`Spec-006 §Event Type Enumeration`), so the `responded` / `expired` variants never admit it — their `.strict()` schemas reject a stamped terminal at the parse, keeping a stale provider answer or timeout from validating as an epoch-attributed row when Plan-004 T3.11's closed-pair/absorbed path is the only legal late form — while without the closed pair's composition T3.11's atomic late-append would be rejected at subscription/replay validation.
  - **Tests:** parse-accept a fixture per type; refinement rejections — a `driver_ask.requested` without `expiresAt`, a permission-kind row without `input` at **every** state (`requested`, `responded`, `expired`, and the closed-pair `canceled`), a `responded` without `response`, a `requested` / `expired` / `canceled` **carrying** `response` (the exclusivity negative — no fabricated answer on a timeout or cancellation), and a `responded` / `expired` / `canceled` without the echoed `expiresAt` (each passing the broad schema); `state` ≠ type-suffix rejection; `.strict()` unknown-key rejection; an epoch-stamped `driver_ask.requested` + `driver_ask.canceled` closed-pair fixture validates end-to-end through `SessionEventSchema` while a stamp missing `sourcePosition` rejects (the T1.9 pairing refinement); a stamped `responded` / `expired` rejects at the union parse (`.strict()` — the carrier admits only the closed pair); the category map resolves `interactive_request` for all four types.
  - **Spec coverage:** Spec-006 line 238 (payload shape + emission-refinement contract), Spec-006 lines 242–245 (the four rows), Spec-012 line 63 (driver-native flows normalize into the canonical model)
  - **Verifies invariant:** none (contract-shape task; enforcement binds on T2.8)
  - **Consumes:** `SessionEventSchema` union seam + type registry ← Plan-006 Phase 1 T1.1/T1.2 (CP-012-2); `withEpochStamp` + `SourceEpochSchema`/`SourcePositionSchema` ← Plan-006 T1.9; `RunIdSchema` ← Plan-005 Phase 4 (CP-012-3); `SessionIdSchema` ← Plan-001 (shipped)

### Phase 2 — Daemon policy + approval services (Cedar gate, persistence, events, seams)

**Goal.** The `policy/` + `approvals/` service layer: signature-verified Cedar policy evaluation, the composed in-process permission gate, approval create/resolve/expiry/cancel with events-canonical durability, remembered-rule match with at-use re-validation, the 7-variant emission seam, and the run-blocking seam toward Plan-004. **Campaign B13 adds:** the T2.8 driver-ask surface (bounded fail-closed expiry), the T2.9 execution-posture + credential-artifact layer, T2.10 posture-as-authorization + callback-tool governance, the T2.11 ADR-012 benchmark, and the T2.12 session-terminal guard.

**Preconditions.**

- [ ] Phase 1 merged
- [ ] Plan-006 Phase 3 merged (`EventLogService.append` — sole append path; Tier 4)
- [ ] Plan-009 Phase 2 merged + Plan-010 Phases 1/3 merged (`workspaces.fs_root` / `worktrees.fs_root` + `run_execution_contexts` — the CP-012-9 envelope-root reads; within-tier ordering)
- [x] D-012-9 engine + custody ratified (Tier-6 audit — `@cedar-policy/cedar-wasm`, signed compiled policy set, fail-closed start; ADR-012 Decision Log entries)
- [x] D-012-7/-10/-11/-12/-13/-14/-15 ratified (Tier-6 audit — canonicity, scope semantics, invalidation, approver identity, seam shape, expiry, derived trust inputs)
- [x] CP-004-9 reciprocal recorded on Plan-004 (run-blocking seam — doc-only until Plan-004 Phase 3 executes)

```yaml
preconditions:
  # Machine-enforced subset (preflight Gate 5); the checkbox rows above stay
  # the human-tracked record, with entries added as they become expressible.
  - { type: plan_phase, plan: 012, phase: 1, status: merged }
  - { type: precondition_box_checked, box: "Driver-ask expiry leg authored" }
```

#### Tasks

- **T2.1 — `policy/policy-artifact.ts`: compiled Cedar policy set — load + verify at daemon start, fail-closed refuse-to-start.**
  - **Files:** `packages/runtime-daemon/src/policy/policy-artifact.ts` (CREATE), `packages/runtime-daemon/policies/default-policy.yaml` (CREATE — transcribes security-architecture §Permission Matrix + `Spec-012 §Default Behavior`), `scripts/compile-policies.mjs` (CREATE — YAML → compiled Cedar policy set + pinned-algorithm detached signature (Ed25519 default per `ADR-012 §Signing Algorithm`), wired into the package build), `packages/runtime-daemon/src/policy/__tests__/policy-artifact.test.ts` (CREATE)
  - Verify the pinned-algorithm detached signature (Ed25519 default, ECDSA P-256 fallback — `ADR-012 §Signing Algorithm`) over the compiled policy set's canonical bytes against the pinned `OPERATOR_PUBLIC_KEY` on every daemon start BEFORE any `PermissionCheck` is served; failure → refuse to start with typed log `policy-artifact-signature-invalid` (D-012-9).
  - **Tests:** bad signature → daemon start aborts before any `approval.*` method registers (spawn-level test per the repo's bin-guard spawn-test convention); tampered bytes → abort; verified artifact → policy set available to T2.2.
  - **Spec coverage:** Spec-012 line 120 (Cedar engine + ADR-012 custody), Spec-012 line 142 (V1 product-default policy with local daemon enforcement)
  - **Verifies invariant:** I-012-8
  - **Consumes:** compiled policy artifact + signature ← the D-012-9 build step (this task creates it); `OPERATOR_PUBLIC_KEY` pin ← `ADR-012 §Policy Chain of Custody`; `@cedar-policy/cedar-wasm` 4.11.x ← npm (D-012-9 pin)
- **T2.2 — `policy/permission-check-service.ts`: the composed in-process gate every sensitive local action calls.**
  - **Files:** `packages/runtime-daemon/src/policy/permission-check-service.ts` (CREATE), `packages/runtime-daemon/src/policy/__tests__/permission-check-service.test.ts` (CREATE)
  - `check(request)` per D-012-18: Cedar `isAuthorized` over the T2.1-verified set (principal = run-initiator participant via the attach-binding owner identity, action = category, resource = target descriptor, context = session state per `Spec-012 §Implementation Notes`) → remembered-rule match via T2.4 → on an ask-policy outcome create the request via T2.3 (whose `create` persists and emits `approval.requested` — the single emission seam; the check path never emits a second copy), notify the T2.6 seam (`onBlocking` with the D-012-13 category→blocking-state mapping), return `pending_approval` + id. Own-node envelope evaluation resolves roots per CP-012-9; evaluation error → deny (I-012-8); persistence unavailable → typed `approval.persistence_unavailable`, action does not proceed. (Campaign B13) Evaluation is per-request against the resident compiled authorizer with **no decision cache** — a decision is never served stale against a changed rule, trust level, membership, or posture (I-012-21; ADR-012 2026-07-02 Decision Log).
  - **Tests:** the four spec-named sensitive classes gated (destructive git, out-of-boundary file write, unrestricted network, high-risk tool execution); network request with no allowing policy/approval → not allowed; out-of-boundary write → explicit approval required; in-envelope own-node action allowed without prompt while an unrestricted-network escalation from the same run still prompts; persistence-unavailable → fail-closed typed refusal; unestablishable provenance → strict per-request, never own-node default; membership alone never authorizes execution on another participant's node; reason-enum semantics per D-012-17 (policy_allow vs approved distinguished); an ask-policy outcome appends exactly one `approval.requested` (emission lives in T2.3's `create`, never the check path); evaluation-error → deny; no-decision-cache control — two identical back-to-back checks both evaluate, and a rule/trust change between them changes the verdict (campaign B13).
  - **Spec coverage:** Spec-012 line 47 (sensitive-action classes), line 60 (membership ≠ machine authority), line 62 (envelope trust does not bypass escalation), line 83 (envelope default), line 84 (network default-deny), line 85 (own-node envelope default), line 90 (fail-closed persistence), line 92 (unknown-provenance fallback), line 98 (check runs inside the daemon), line 120 (principal/action/resource/context mapping), AC1 (line 130), AC2 (line 131)
  - **Verifies invariant:** I-012-7, I-012-8, I-012-11, I-012-21
  - **Consumes:** verified policy set ← T2.1; `PermissionCheckRequest/Response` shapes ← Phase 1; request creation ← T2.3; rule match ← T2.4; seam ← T2.6; `node_trust_state.trust_level` fail-closed read + attach-binding owner identity ← Plan-003 (shipped; CP-012-10); envelope roots ← Plan-009 `workspaces.fs_root` / Plan-010 `worktrees.fs_root` via `run_execution_contexts` (CP-012-9)
- **T2.3 — `approvals/approval-service.ts`: create / resolve / expiry / cancel — the schema-validated canonical boundary.**
  - **Files:** `packages/runtime-daemon/src/approvals/approval-service.ts` (CREATE), `packages/runtime-daemon/src/approvals/__tests__/approval-service.test.ts` (CREATE)
  - `create` validates category/scope/resourceDescriptor through the contracts schemas at the boundary (the CP-012-6 interim normalization gate — a driver-native category string is rejected before any record is written), persists requester + session provenance, emits `approval.requested` (events-canonical ordering per D-012-7: append commit first, row projection after). `resolve` enforces the D-012-12 approver model (absent → node-owner binding; present → cross-checked, mismatch → `auth.principal_mismatch`), records decision + effective scope (refusing a broader-than-requested scope, I-012-6), emits `approval.approved`/`approval.rejected`, and mints a remembered rule via T2.4 ONLY on explicit opt-in (allow-only per D-012-16). Expiry per D-012-14: lazy settle on every read/match + a 60-second sweep emitting `approval.expired` (`actor: null`). Resolution/expiry/cancel paths are the lifecycle moments T2.6 wires `onResolved` notification onto (T2.6 extends this file; no notification surface exists in this task). `cancelPendingForRun(runId, cause)` settles pending requests as `canceled` on the request row (`state` + `updated_at` — no resolution row) and emits `approval.canceled` (D-012-8). Typed refusals per D-012-4: `approval.not_found`, `approval.already_resolved`, `approval.request_expired`, `approval.request_canceled`, `approval.persistence_unavailable`.
  - **Tests:** create persists requester/session/category/scope/descriptor/expiry and rejects a non-canonical category before any write; resolve records approver + decision + effective scope, rejects a mismatched body `approver` (`auth.principal_mismatch`), refuses a broader `effectiveScope`; double-resolve → `approval.already_resolved` (UNIQUE index arbiter); resolve-after-expiry → `approval.request_expired`; resolve-after-cancel → `approval.request_canceled`; unknown id → `approval.not_found`; remember opt-in absent → no rule row (`request_only` default); expiry sweep settles + emits with `actor: null`; cancel settles pending rows + emits `approval.canceled` with no resolution row; append-failure → fail-closed, no row mutation; one row per request when UX groups (no batch row).
  - **Spec coverage:** Spec-012 line 58 (request fields), line 59 (resolution fields), line 63 (normalization boundary), line 81 (`request_only` default), line 82 (opt-in rules), line 89 (stricter-boundary fallback for non-granular drivers), line 96 (create contract), line 97 (resolve contract + principal rule), line 117 (granular canonical records)
  - **Verifies invariant:** I-012-3, I-012-4, I-012-6, I-012-7, I-012-12
  - **Consumes:** tables ← T1.3; schemas ← Phase 1; emission ← T2.5; rule minting ← T2.4; node-owner binding read ← Plan-003 (CP-012-10); driver normalizer inputs ← Plan-005 `interactive_request` events at driver-integration time (CP-012-6)
- **T2.4 — `policy/grant-evaluator.ts`: remembered-rule create / match with at-use re-validation.**
  - **Files:** `packages/runtime-daemon/src/policy/grant-evaluator.ts` (CREATE), `packages/runtime-daemon/src/policy/__tests__/grant-evaluator.test.ts` (CREATE)
  - Rule creation only via T2.3 resolve-with-remember (writes grantor/session/node/run bindings + origin resolution FK; emits `approval.remembered` with `ruleId`). Match per D-012-10: candidate set (unrevoked ∧ session ∧ node ∧ category ∧ kind/run binding), then category-derived pattern semantics (path prefix containment against CP-012-9 envelope roots; exact host for `network_access`; exact token otherwise). The evaluator owns the exported validity predicate (rule unrevoked ∧ grantor membership active ∧ bound node trust intact) and the use-time revocation write: every match re-validates through the predicate before honoring (I-012-10), and a failed re-validation durably revokes (`revoked_at` + ratified trigger, `approval.rule_revoked` via T2.5) before the check returns, falling back to per-request approval — Phase 2 is correctness-complete without the Phase-3 push seams (D-012-11 layering).
  - **Tests:** creation only via opt-in; match honors only unrevoked in-boundary rules; category-derived semantics table (path prefix in/out of envelope root, host exact, token exact, absent pattern = category-wide); kind = `run` rule matches only its originating run; grantor `participant_id` is NOT consulted as a match key (D-012-10 negative control); revoked rule never matches; at-use re-validation refuses a rule whose node trust dropped between minting and use.
  - **Spec coverage:** Spec-012 line 81 (default scope), line 82 (opt-in + session-wide rules), line 91 (invalidate before next use), line 111 (run-scoped grant with path scope), line 118 (explicit enum scopes), line 119 (immediate propagation into evaluation)
  - **Verifies invariant:** I-012-5 (behavioral half), I-012-10
  - **Consumes:** rules table ← T1.3; `RememberedScopeSchema` ← T1.1; `node_trust_state` read ← Plan-003 (CP-012-10); envelope roots ← CP-012-9; emission ← T2.5
- **T2.5 — `approvals/approval-event-emitter.ts`: the single emission seam for the 7 `approval_flow` events.**
  - **Files:** `packages/runtime-daemon/src/approvals/approval-event-emitter.ts` (CREATE), `packages/runtime-daemon/src/approvals/__tests__/approval-event-emitter.test.ts` (CREATE)
  - Standalone seam module per the shipped `node/node-event-emitter.ts` precedent: validates payloads at the boundary through `ApprovalFlowEventPayloadSchema.parse()` composed with the per-variant `approvalFlowPayloadRefinementFor(eventType)` refinement (T1.1 — an under-populated variant refuses at emission, I-012-9), routes through `EventLogService.append`, never computes integrity bytes. Exposes exactly seven emit surfaces mapped to lifecycle moments (requested/approved/rejected/expired/canceled on the request lifecycle — emission driven by T2.3; remembered/rule_revoked on the rule lifecycle — driven by T2.4/T3.4).
  - **Tests:** each emitted event carries the `Spec-006 §Approval Flow (approval_flow)` payload shape and the correct type per moment — `approval.requested`, `approval.approved`, `approval.rejected`, `approval.expired`, `approval.canceled`, `approval.remembered` (`ruleId` present), `approval.rule_revoked` (`ruleId` + `invalidationTrigger` present); a payload failing parse never reaches append; a structurally-valid payload failing its per-variant refinement (e.g. `approval.remembered` without `ruleId`, `approval.requested` without `requestedBy`) never reaches append; envelope/payload `sessionId` cannot diverge (single-input reconciliation); expiry/cancel events carry `actor: null`.
  - **Spec coverage:** Spec-012 line 105 (durable + replayable); Spec-006 line 251 (payload shape), Spec-006 lines 255–261 (the seven variants)
  - **Verifies invariant:** I-012-9 (emission half)
  - **Consumes:** `EventLogService.append` ← Plan-006 Phase 3 T3.1 (CP-012-2); type names + category mapping ← Plan-006 Phase 1 T1.2; `ApprovalFlowEventPayloadSchema` + union registration ← T1.1
- **T2.6 — Run-blocking seam (`registerApprovalOutcomeListener` + `cancelPendingForRun`).**
  - **Files:** `packages/runtime-daemon/src/approvals/approval-service.ts` (EXTEND — the seam registration surface lives on the service), `packages/runtime-daemon/src/approvals/__tests__/approval-service.test.ts` (EXTEND)
  - Implements D-012-13 / CP-012-5: a `pending_approval` outcome notifies the registered listener exactly once with `{runId, approvalRequestId, blockingState}` (`user_input`/`mcp_elicitation` → `waiting_for_input`; other 7 → `waiting_for_approval`); each resolution path (approved / rejected / expired) notifies `onResolved` exactly once AFTER the durable commit of the triggering record; `cancelPendingForRun` is the inbound terminal-path arm (T2.3 settles + emits; the same call closes the run's pending driver asks — both kinds — `driver_ask.canceled` through T2.8, `Spec-006 §Driver Ask Events`' run-ended cause, so an input-kind ask with no approval row never dangles past its run). Zero-listener operation is a no-op — Plan-012 ships the seam; Plan-004 Phase 3 registers (CP-004-9). (Campaign B13 — P1-3 run half) A run blocked on a pending driver ask holds the idle sweep's pending-blocking-work exemption **expiry-bounded**: the exemption ends the moment the ask expires or resolves — `driver_ask.expired` closes it with the ask, an expired ask never counts as pending blocking work, and the run returns to the ordinary idle path — mechanically, the exemption's carrier is the blocking state itself: `waiting_for_*` is entered through this seam's `onBlocking` (pipeline-backed blocks) or T2.8's input-wait signal (input-kind asks — Codex round 3), Plan-016's T2.7 blocking-state hard-skip (its scheduled campaign-B15 sweep leg, reading run state via CP-004-7) skips runs in those states, and the resolution/park transitions exit them, so no dedicated pending-ask sweep predicate exists to drift (`Spec-012 §Required Behavior` decoupled-but-coordinated rule; domain mirror `run-state-machine.md §Invariants`).
  - **Tests:** exactly-once notification per blocking + per resolution path; notification ordered after durable commit; the category→blocking-state mapping table; zero-listener no-op; cancel settles every pending request for the run; the pending-ask exemption ends on expiry and on resolution (campaign B13).
  - **Spec coverage:** Spec-012 line 130 (AC-1 — the pending approval blocks the sensitive action until explicit approval or valid grant), Spec-012 line 98 (the check-and-block runs inside the daemon)
  - **Verifies invariant:** I-012-7 (blocking half)
  - **Consumes:** request lifecycle + cancel ← T2.3; seam contract ← D-012-13 (CP-004-9 reciprocal recorded on Plan-004; run-state-machine §Complete Transition Table carries the generalized approval-resolution trigger)
- **T2.7 — Phase gate: replay/restart + trust-invalidation + envelope acceptance suite.**
  - **Files:** `packages/runtime-daemon/src/approvals/__tests__/approval-replay.test.ts` (CREATE), `packages/runtime-daemon/src/policy/__tests__/permission-enforcement.test.ts` (CREATE)
  - **Tests:** restart mid-lifecycle (pending request, unresolved expiry, live remembered rule) → replay reconstructs identical approval state byte-equal (D-012-7 high-water-mark reconcile; AC-3 approver + effective-scope assertions); trust-invalidation end-to-end (node-trust downgrade between grant and next use → rule revoked before reuse); own-node envelope matrix (in-envelope allowed without prompt / escalation prompted); the four-class permission-enforcement matrix; grants survive recovery; (campaign B13) the ask-expiry restart matrix — a pending ask with a persisted deadline re-arms and a crash-after-first-effect expiry completes its sibling set deterministically at startup, never auto-approving — and stamped-posture replay — `run.running` rows carry the complete posture object and the cited `credential_policy_artifacts` row survives at least as long as any citing run event.
  - **Spec coverage:** Spec-012 line 47 (enforcement matrix), line 59 (who granted what scope), line 62 (no envelope bypass), line 85 (own-node default), line 91 (revoke before next use), line 105 (durable + replayable), AC3 (line 132), line 143 (envelope-bound own-node trust)
  - **Verifies invariant:** I-012-7, I-012-8, I-012-9, I-012-10, I-012-11, I-012-20, I-012-22
  - **Consumes:** T2.1–T2.6 surfaces, plus the T2.8 ask surface and T2.9 posture/artifact store (campaign B13 — the ask-expiry restart matrix and stamped-posture replay verify them, so the gate runs after they land); Plan-001 projector/replay substrate (shipped, [GitHub PR-#9](https://github.com/Sawmonabo/ai-sidekicks/pull/9))
- **T2.8 — `policy/driver-ask-normalizer.ts`: the driver-ask surface — normalization into the Cedar pipeline + bounded fail-closed ask expiry (campaign B13; satisfies CP-012-6; Spec-012 Part-B follow-up, 2026-07-17).**
  - **Files:** `packages/runtime-daemon/src/policy/driver-ask-normalizer.ts` (CREATE), `packages/runtime-daemon/src/policy/__tests__/driver-ask-normalizer.test.ts` (CREATE), `packages/runtime-daemon/policies/default-policy.yaml` (EXTEND — ask-expiry policy content)
  - The daemon-side owning surface for provider-initiated mid-run asks. **Admission (P2-8 — provider-native asks only):** the wire-sourced ask surface engages only for drivers whose persisted capability row carries `capabilities.flags.interactive_requests === true` (Plan-005 `driver_capabilities`; CP-005-7) — a flagless driver never registers an ask surface and its runs inherit the equal-or-stricter daemon boundary (`Spec-012 §Fallback Behavior`). The flag never gates the callback route: T2.10's invocations reach this task's normalizer through the `callback-tool-host` seam, available on the independent `callback_tools` capability. **Ask lifecycle:** wire-sourced asks (Claude `can_use_tool` / `--permission-prompt-tool` round-trip; Codex `item/tool/requestUserInput` + `mcpServer/elicitation/request` for the input kind and the approval server requests `item/commandExecution/requestApproval` / `item/fileChange/requestApproval` / `item/permissions/requestApproval` + legacy `execCommandApproval` / `applyPatchApproval` for the permission kind (the pinned Codex wire reference's RPC catalog, Codex round 5) — the Plan-005 T3.14 P1-4-driver mechanisms) register under a **daemon-minted `askId`** (ULID — the durable identity carried on all four `driver_ask.*` payloads, the `approval_requests.ask_id` association, the receipt dedupe key, and the answer address; the provider-native request id is leg-local correlation state for delivery routing and live-leg duplicate detection, never the durable identity — so a provider restart resetting native id counters can never collide a post-resume ask with a settled row, and a dead leg's pending ask needs no native id to settle daemon-side) with `expiresAt` stamped at creation (audit-visible, never an implicit internal timer) and a durable timer armed **timer-OR-answer, whichever first** — armed for every ask still pending past its admission pass; a policy-decided permission ask settles inside the pass (the evaluate-first route below) and never arms one; the deadline persists (for permission-kind, the `approval_requests` row's `expiry_at`; for both kinds, the emitted `driver_ask.requested` row), so restart re-arms from the stored deadline — never loses, never extends. Registration is **idempotent for both kinds**: a duplicate wire delivery of a still-pending native request (same leg, same native id) resolves to the already-registered ask — no second pending ask, no second `driver_ask.requested`, no second timer — while a replayed registration re-mint collides its same daemon `askId` on the T1.3 index for the permission kind (the input kind mints no approval row, so this surface-level dedupe is its guard); a post-resume re-raise arrives as a new native request and registers a **new** ask, the settled predecessor untouched. V1 defaults: **4 hours** (`permission`) / **24 hours** (`input`), policy-configurable per daemon in `policies/default-policy.yaml` under the fail-closed floor — durations tune, expiry never disables (no wait-forever configuration exists) — with risk-tiered per-category refinement of the defaults as the same policy-content surface, bounded by the same floor and the same never-auto-approve invariant (`Spec-012 §Resolved Questions and V1 Scope Decisions`). Emission validates through T1.5's `driverAskPayloadRefinementFor` composed with `DriverAskEventPayloadSchema.parse()` and routes through `EventLogService.append`; later-state rows echo the requested row's `expiresAt` unchanged — the T1.5 refinement enforces the echo, so a later-state row missing the deadline refuses at the emission parse. **Permission-kind** asks route into the Cedar pipeline **evaluate-first (D-012-18 — the same discipline as T2.10's callback route, Codex round 5)**: the normalizer maps the driver-native ask into canonical category/scope/resource-descriptor inputs and runs T2.2's composed `check()` **before any mint** — a policy-allow or valid remembered rule delivers the decision to the driver at once and settles the pair in the same admission pass (`driver_ask.requested` + `driver_ask.responded` append atomically, the expiry timer never arms, no approval row is minted, no blocking state is entered; a Cedar deny is final and never becomes a human-approvable prompt, a remembered allow never blocks); only an ask-policy outcome mints the approval request through T2.3's `create` **daemon-internally carrying the originating `askId`** with the one shared deadline `expiryAt` = the ask's `expiresAt` — never client-suppliable (the public `ApprovalRequestCreateRequest` carries no `askId`; a caller-forged association could route another ask's outcome or expiry to the wrong native ask); the T1.3 unique partial index on `(run_id, ask_id)` collides a retry/replay re-mint, the ask-implies-deadline CHECK plus T1.1's origin-blind emission refinement guard the pairing, and the persisted association is the provenance Plan-004's rollback void consumes (its T3.12 settles BOTH surfaces). Resolution routes back through the ask: an approved/rejected decision delivers the provider's native allow/deny and emits `driver_ask.responded` carrying `response`; expiry resolves **auto-deny, deny-and-continue** — the structured denial rides the provider's native deny path, the pipeline request settles through `approval.expired` (the D-012-14 machinery drives the shared-deadline passage), and the run continues via the T2.6 seam's `expired` outcome — never a session kill, never a fabricated approval. **Cancellation is the third settle (timer-OR-answer-OR-cancel):** a run-terminal or session-close cancellation closes the run's pending asks — **both kinds** — as `driver_ask.canceled` (`Spec-006 §Driver Ask Events`' run-ended cause) with the durable timer disarmed and no park signal fired (the run is already past waiting): `cancelPendingForRun` closes them beside its request settle (the single Plan-004 inbound arm — no second wiring), T2.12's session sweep closes them across the session's runs, and a permission-kind ask's `approval.canceled` settle always closes its originating ask in the same atomic pass (the provenance rule Plan-004's rollback void also rides); a late answer no-ops against a canceled ask exactly as against an expired one. **Provider retraction settles immediately (the live-leg cancel ingress):** a provider withdrawing a still-pending ask on its live leg (each P1-4-driver mechanism's wire-native cancellation) settles the ask `driver_ask.canceled` at once — timer disarmed, a permission-kind ask's associated approval request settled `approval.canceled` (no resolution row) in the same atomic pass, and the run's blocking state **released** through the same run-engine transition applicator (either kind: `waiting_for_*` → `running` with no outcome delivered — the provider no longer wants an answer, so the run continues and never idles to the 4h/24h deadline on a dead question); a late answer no-ops as above. **Answer ingress (the answer half of timer-OR-answer):** the surface exposes the daemon-internal `respondToAsk(runId, askId, response)` — keyed on the durable (`run_id`, `ask_id`) ask identity (the T1.3 index and the receipt dedupe key), never `askId` alone: the run binding is authorization scope (an answer addressed under the wrong `runId` no-ops even with a valid `askId`) and matches Plan-005's wire `RespondToRequestParams` `{runId, requestId}` pair, whose `requestId` for an input-kind answer is the event-visible `askId` the client read off `driver_ask.requested` — validate the ask is pending, settle the race against the durable timer (a late answer no-ops against a settled ask), deliver through the Plan-005 driver op `ProviderDriver.respondToRequest` (the P1-4-driver mechanisms' response leg), and emit `driver_ask.responded` carrying `response`, atomically. Its two canonical callers: permission-kind answers arrive as the approval resolution above (the pipeline decision IS the answer); input-kind answers arrive on Plan-005's **client-facing `driver.respondToRequest` wire method** (Plan-005 Phase 4 — consumed, never authored), whose daemon handler routes an ask-associated response through this seam so settle + emission + native delivery stay one atomic, race-guarded passage — this is the run's interactive surface `Spec-006 §Driver Ask Events` routes input-kind asks to, so a structured-input ask is answerable for its whole pending window, never expiry-only. **Input-kind** asks never traverse the approval pipeline (`Spec-006 §Driver Ask Events` routes them to the run's interactive surface); their run-state lifecycle rides three T2.8 signals the Plan-004 run-engine consumes through the same transition applicator its CP-004-9 listener drives (the Tier-6 reciprocal note beside T3.15 — consumed, never authored here): registering a live input-kind ask signals the **input-wait leg** — `running → waiting_for_input`, atomic with the `driver_ask.requested` append, so T3.15's park edge is reachable and the blocking-state idle exemption exists for the ask's whole pending window; answer delivery signals the **resume leg** — `waiting_for_input → running`, atomic with `driver_ask.responded`; and expiry closes the ask (`driver_ask.expired`) and signals the run-engine park leg — Plan-004 T3.15's `waiting_for_input → paused` edge, the B9-landed reciprocal — with no input ever fabricated. **Replay-atomic sibling effects:** an expiry's effects — permission: `approval.expired` + `driver_ask.expired` + the native denial; input: `driver_ask.expired` + the park signal — commit as one unit under `withSessionAppendLock` (Plan-006 T3.1, campaign B11), and daemon startup reconciliation derives a missing sibling deterministically from the persisted (`ask_id`, `expiry_at`) pair, so a crash between effects never strands a resolved approval against a still-pending native ask or a closed ask against a still-waiting run. **Native-path delivery is receipt-guarded (staged on its owning migration):** the provider-directed effect of a settle — the expiry denial or the delivered answer riding `respondToRequest` — records through a `command_receipts` write seam that **ships dormant until Plan-015's two-phase idempotency columns land** (the Plan-005 T3.13 dormant-receipt-seam precedent, cross-plan §1: Plan-004 CREATEs only the shell — `id`, `command_id`, `run_id`, `status`, `created_at` — at Tier 5; Plan-015 EXTENDs the protocol columns, `dedupe_key` included, at Tier 7 — consumed, never authored). Pre-activation, startup reconciliation re-issues any unconfirmed delivery against a live binding unconditionally — safe because a duplicate response to an already-answered single-shot native request is provider-side inert and never re-adjudicates (the daemon-side settle already committed) — and records moot against a dead leg (a resumed provider re-raises the ask if still needed). Once Plan-015 activates the seam, the write-ahead receipt row (dedupe-keyed on the (`run_id`, `ask_id`, effect) identity, committed in the same unit as its sibling events) lets reconciliation distinguish delivered from undelivered and skip the redundant re-issue — never a stranded ask, never an unrecorded delivery, at either stage. **Adjudication authority (I-012-22):** every permission ask adjudicates through the Cedar pipeline; provider guardian / auto-review surfaces (Codex `guardianWarning` / `item/autoApprovalReview/*`) normalize onto the Plan-005 diagnostic surface only — `DriverDiagnosticRecord` (`provider/driver-diagnostics.ts`): structured daemon logs + OTel counters, off-timeline by design; no registered `approval_flow` kind exists for them, so a `session_events` row would be an unregistered-literal invention (Codex round 5) — never as an adjudication — and silence is denial at every tier and posture. **Liveness (P1-3 run half):** a pending ask holds the run's pending-blocking-work exemption from the idle sweep — carried by the blocking state itself, which Plan-016's T2.7 blocking-state hard-skip consults (its scheduled campaign-B15 sweep leg; no Plan-012-owned predicate) — and the exemption ends the moment the ask expires or resolves, the state exiting with it (the T2.6 corollary; domain mirror `run-state-machine.md §Invariants`).
  - **Tests:** deadline stamped at creation and persisted (both kinds); restart re-arms from the stored deadline (never extends); the timer-OR-answer race both ways (answer first cancels the timer; timer first denies — a late answer no-ops against the settled ask); an input-kind answer through the `driver.respondToRequest` ingress settles atomically (timer canceled + `driver_ask.responded` + native delivery, one passage); permission expiry commits the full sibling set atomically, with a crash-after-first-effect fixture proving startup reconciliation derives the missing sibling from (`ask_id`, `expiry_at`); the receipt guard at both stages — pre-activation (Plan-015 columns absent) a crash in the delivery window re-issues unconditionally against a live leg and the benign duplicate never re-adjudicates, post-activation reconciliation reads the receipt (undelivered → re-issued; dead leg → recorded moot; delivered → no re-issue); a live input-kind `requested` fires the input-wait signal atomically with its append and a delivered answer fires the resume signal (the opening/resume legs the run-engine consumes); an answer addressed to a colliding `askId` under the wrong `runId` no-ops against the other run's ask (the run-scoped identity negative); input expiry emits `driver_ask.expired` + signals the park seam and never touches the approval pipeline; run-terminal cancellation closes pending asks of both kinds (permission: the dual `approval.canceled` + `driver_ask.canceled` settle; input: `driver_ask.canceled` with the timer disarmed and no park signal) and a late answer no-ops against the canceled ask; a live-leg provider retraction settles immediately for either kind — `driver_ask.canceled` + timer disarm + the blocking-state release back to `running` (permission additionally settles its approval request), never waiting out the deadline; never-auto-approve negative control (no expiry path yields an approved outcome); a duplicate normalizer re-mint collides on `(run_id, ask_id)` and a duplicate still-pending input-kind delivery registers nothing and arms no second timer (the surface-level idempotency — no approval row exists to collide); a post-resume re-raise under a recycled native id registers a new daemon-minted ask while the settled predecessor stays settled (native ids are never the durable identity); the public create schema refuses a smuggled `askId`; a flagless driver gets no ask surface (P2-8 negative); a guardian/auto-review frame lands on the diagnostic surface (`DriverDiagnosticRecord`), appends no `session_events` row, and resolves nothing; a policy-denied native permission ask delivers the deny with no minted request and no blocking state and a remembered allow settles the pair in the admission pass (evaluate-first, allow and deny verdicts alike) while an ask-policy outcome mints exactly one request; later-state rows echo `expiresAt`; resolution and expiry each release the blocking state that carries the run's idle-sweep exemption.
  - **Spec coverage:** Spec-012 line 63 (bounded fail-closed asks — stamped deadline, timer-OR-answer, never auto-approve, exemption coordination), Spec-012 line 89 (stricter-boundary fallback), Spec-012 line 144 (per-kind outcomes, shared deadline, replay-atomic siblings, 4h/24h defaults under the floor), Spec-006 line 244 (expiry-row semantics)
  - **Verifies invariant:** I-012-7 (ask half), I-012-22
  - **Consumes:** `DriverAskEventPayloadSchema` + `driverAskPayloadRefinementFor` ← T1.5; request mint + expiry machinery ← T2.3 (the daemon-internal `askId` seam; D-012-14); run-blocking seam ← T2.6; `withSessionAppendLock` ← Plan-006 T3.1 (campaign B11); `EventLogService.append` ← Plan-006 Phase 3 (CP-012-2); driver ask mechanisms + `driver_capabilities` flag read + `ProviderDriver.respondToRequest` op + the client-facing `driver.respondToRequest` ingress ← Plan-005 (CP-005-7 / CP-012-6; Phase 4 wire surface); `command_receipts` receipt discipline ← Plan-004 CREATE (Tier-5 shell) + Plan-015 two-phase protocol (Tier-7 activation gate — the write seam ships dormant until its columns land, the Plan-005 T3.13 precedent; consumed, never authored); park-leg reciprocal ← Plan-004 T3.15 (signaled, never authored here)
- **T2.9 — `policy/execution-posture-service.ts` + the `credential_policy_artifacts` store: posture validation, artifact persistence, and the `run.running` stamp (campaign B13; R8, consumes B20).**
  - **Files:** `packages/runtime-daemon/src/policy/execution-posture-service.ts` (CREATE), `packages/runtime-daemon/src/migrations/0NNN-credential-policy-artifacts.ts` (CREATE — NNN = next free version per migration-runner append order at PR-open time; DDL verbatim from local-sqlite-schema.md §Credential Policy Artifacts per the T1.3 convention), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — guarded block), `packages/runtime-daemon/src/policy/__tests__/execution-posture-service.test.ts` (CREATE)
  - Owns the run's `executionPosture` from request to stamp (`Spec-012 §Required Behavior`, campaign B20). **Validation (fail closed):** the cross-field invariants are enforced before Cedar evaluation or spawn — `networkAccess: 'allowed-domains'` requires a present, non-empty `allowedDomains` (absent under `'none'` / `'full'`); `readonly-sandboxed` requires `writableRoots: []`; `trusted` MUST be supplied as the canonical `networkAccess: 'full'` + `writableRoots: []` with `credentialPolicyRef` absent; every violating combination is **rejected** — the run refuses, never silently normalized. **Canonicalization:** `writableRoots` symlink-resolved absolute paths (relative rejected); `allowedDomains` lowercase punycode FQDNs / single-form `*.suffix` wildcards. **Credential-policy artifact:** resolves the daemon's curated credential policy to the artifact document `{schemaVersion: 1, denyPaths, denyEnvVars, envNameMatch}` — daemon-expanded canonical absolute `denyPaths`, `denyEnvVars` canonicalized to the host's env-name case semantics with the match mode recorded as `envNameMatch`, both arrays lexicographically sorted + deduped — RFC 8785 JCS-canonicalized and content-addressed as `"sha256:<hex>"`; the row persists **write-ahead** into `credential_policy_artifacts` BEFORE the first `run.running` posture stamp citing the ref (the ADR-019 spawn-intent ordering discipline — the ref can never dangle), idempotently by PK (identical policy ⇒ identical ref), and is retained at least as long as any run event citing it (the audit-stub retention class). **The content address is enforced, not assumed:** every artifact read — the idempotent re-persist's existing-row check, audit reconstruction, any resolution by ref — recomputes the JCS SHA-256 over the stored `artifact` bytes and **fails closed on mismatch** (the typed `approval.policy_artifact_corrupt` error — the error-contracts §Approval row this amendment mints; the citing posture is unresolvable, so the run takes `Spec-012 §Fallback Behavior`'s strict per-request posture rather than trusting corrupt policy bytes) — a format-valid ref over foreign bytes is a corruption/migration bug the schema comment promises re-verification against, and an unverified read would let a stamp cite one policy while audit reconstructs another. The artifact's `denyEnvVars` bind through the daemon-owned spawn-environment construction — I-012-20's allow-list `{...curatedBase, ...runProvisionedVars}`, never ambient inheritance — with provider-native layers as defense-in-depth beneath it (Codex `shell_environment_policy` `inherit: "none"` + explicit `set`, C-7; Claude `sandbox.credentials` env unsets + `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB`); the spawn-side enforcement leg is Plan-005 T3.14 P0-4 (consumed reciprocal — its text names this invariant). **Floor verdicts:** per-axis minimum-posture comparison computed daemon-side and passed to Cedar as context — the `mode` ladder, the `networkAccess` tier with the intra-`'allowed-domains'` domain-subset relation under the canonical pattern rules, and `writableRoots` canonical path containment (`readonly-sandboxed`'s ∅ satisfies any roots floor; `trusted`'s `[]` denotes _no write limit_ and satisfies only a `trusted` floor) — axes never trade off cross-axis, an incomparable pattern form fails closed, and Cedar never re-implements pattern math. **Stamp:** the complete validated posture object — `credentialPolicyRef` included on the two sandboxed modes — is resolved pre-spawn on the run-setup path (registration surface: the Plan-004 CP-004-8 `RunSetupGate` seam, the Plan-010 execution-root-gate precedent) and stamped on `run.running` (the post-setup-gate spawn-success transition; never `run.starting`), riding `RunStateChangeEvent.executionPosture` (Spec-006 B1 item 11; api-payload-contracts mirror incl. `credentialPolicyRef`). The gate's `assertRunReady` is void-returning (Plan-004 T3.10), so the stamp's read-back rides a sibling seam: the service records the resolved posture run-keyed at gate pass and exposes **`resolvedPostureFor(runId)`** — the accessor Plan-004's T3.7 `run.running` emitter reads at stamp time (CP-012-11; consumed at Plan-004's Phase-3 execution, recorded in its Tier-6 reciprocal note) — and a stamp finding no resolved posture **fails closed**: the transition refuses, a sandboxed run never starts unstamped. **Retention (prune leg):** artifact deletion is Plan-012-owned — `pruneUnreferencedArtifacts` is an idempotent maintenance entry deleting rows whose ref no remaining `session_events` row or audit stub cites. In V1 it has **no wired caller**: compaction never frees a ref (the Spec-012 stub contract preserves the posture object, ref included) and the Plan-022 crypto-shred fan-out clears `pii_payload` only — signed event rows, posture stamps included, survive it — so no V1 path deletes a citing row; retention is indefinite by design and bounded by content-addressing (one row per distinct effective policy, never per run). The invocation seam is the V1.1 `gdpr.sessionPurge` handler's completion — the only operation that removes citing rows; Plan-022 reserves its stub in V1 — an explicit **future consumer** (registered when Plan-022's V1.1 deletion handler ships), never a V1 cleanup guarantee.
  - **Tests:** every violating cross-field combination rejects (incl. `trusted` + `credentialPolicyRef`, `trusted` supplied non-canonically, `'allowed-domains'` with an absent/empty list, `readonly-sandboxed` with non-empty roots) — and none is rewritten; ref stability (same policy → same ref; differing `envNameMatch` → differing refs; array order and duplicates irrelevant); write-ahead ordering (a failed artifact persist refuses the stamp — fail closed; the artifact row exists before any citing `run.running`); idempotent re-persist; the floor-verdict matrix per axis (satisfied / unsatisfied / incomparable → fail closed; the bare suffix is not covered by `*.suffix`; a wildcard is never ⊆ an exact; narrowed roots satisfy their floor; `readonly-sandboxed` satisfies any roots floor; `trusted` fails every sandboxed `mode` floor); malformed refs reject at both layers — service boundary and DDL CHECK (empty suffix, non-hex text, uppercase hex, wrong length) — and a NULL ref rejects on the explicit `NOT NULL` (a rowid-table TEXT PRIMARY KEY admits NULL and a NULL-valued CHECK passes — the SQLite legacy quirk, empirically verified); the read-boundary integrity check — a row whose format-valid `ref` does not re-hash from its stored `artifact` bytes fails closed with the typed `approval.policy_artifact_corrupt` error (mismatch fixture), and the citing read falls back to the strict per-request posture; `resolvedPostureFor` returns the complete resolved object for a gated run and nothing for an unknown one (the emitter's fail-closed read); prune (driven directly — it has no V1 caller): a shared ref survives while any citing row remains (two sessions, one ref — deleting one session's rows keeps it) and deletes only once the final citing row is gone, idempotently on re-run; migration columns + CHECK + idempotent re-run via `openDatabase` reopen.
  - **Spec coverage:** Spec-012 line 67 (posture object, credential-policy artifact, cross-field invariants, per-axis floors), Spec-012 line 68 (complete-object `run.running` stamp)
  - **Verifies invariant:** I-012-20
  - **Consumes:** `ExecutionPosture` shape ← api-payload-contracts §Plan-005 (campaign B3/B20 mirror); `RunSetupGate` registration seam ← Plan-004 CP-004-8 (shipped seam contract; consumed, never authored); `resolvedPostureFor` read → Plan-004 T3.7 emitter (CP-012-11 — provided); `gdpr.sessionPurge` completion ← Plan-022 V1.1 (the prune's future invocation seam — V1 reserves the stub; consumed, never authored); migration-runner guarded-block seam + `openDatabase` ← Plan-001 (shipped); DDL ← local-sqlite-schema.md §Credential Policy Artifacts (campaign B13); RFC 8785 JCS canonicalization ← the `Spec-006 §Canonical Serialization Rules` substrate
- **T2.10 — Posture as a Cedar authorization input + callback-tool governance (campaign B13; R8, consumes B20 + B3) — EXTENDs the T2.1/T2.2 surfaces.**
  - **Files:** `packages/runtime-daemon/src/policy/permission-check-service.ts` (EXTEND), `packages/runtime-daemon/policies/default-policy.yaml` (EXTEND — minimum-posture policy content), `packages/runtime-daemon/src/policy/__tests__/permission-check-service.test.ts` (EXTEND)
  - The stamped posture is a **Cedar authorization input**, not merely a spawn-time knob: `check()`'s context gains the run's effective posture plus T2.9's per-axis floor verdicts, and policy MAY require a minimum posture per trust level (`node_trust_state`-keyed rules in `default-policy.yaml` — a less-trusted context cannot select a weaker sandbox than policy allows), evaluated inside the same principal-action-resource-context decision as every approval category; a floor-unsatisfied or incomparable posture denies (I-012-8's fail-closed edge). **Callback-tool governance:** every daemon-registered session callback tool (the Spec-005 campaign-B3 registry shape) is Cedar-governed identically to provider tools — Plan-005's `callback-tool-host` hands each `CallbackToolInvocation` to the seam Plan-012 registers (the same registration whose absence keeps Plan-005's spawn withholding the `callbackTools` registry — CP-005-7 fail-closed availability): the invocation normalizes through T2.8's normalizer into T2.2's composed `check()` under the same category mapping — admission rides the host seam's own `callback_tools` capability gate, never P2-8's `interactive_requests` flag, which governs only provider-native asks — (D-012-18 — **evaluate first**: a policy-allow or valid remembered rule completes with no minted request; only an ask-policy outcome creates the pending request, via exactly the canonical requestCreate-shaped inputs CP-005-7 names — never a bare create that would force every invocation to a human), the outcome answers back as the three-arm `CallbackToolResult`, and the invocation lands as a `tool_activity` record; no callback tool bypasses the approval pipeline (`Spec-012 §Required Behavior` callback-tool bullet). **Adjudication routing (I-012-22):** the posture-derived provider configuration pins `approvalsReviewer: "user"` — a turn supplying a divergent reviewer is refused (the driver-side derivation + refusal is Plan-005's posture-realization leg per `Spec-012 §Required Behavior`; this plan owns the invariant and its policy half) — so approval adjudication can never reroute to a provider guardian / auto-review path around Cedar.
  - **Tests:** the minimum-posture floor matrix per trust level (per-axis; a weaker-than-floor posture denies pre-spawn; cross-axis trade-offs never substitute); callback-tool invocations traverse the same gate (an ungoverned callback invocation cannot execute — negative control); a policy-allowed callback invocation completes with no minted approval request (the evaluate-first positive); a guardian-adjudicated outcome never satisfies a pending check; a `callback_tools`-capable driver with `interactive_requests: false` still governs through the pipeline (the per-class-gate split — P2-8 never withholds callback governance); the posture context reaches Cedar carrying the daemon-computed verdicts (Cedar never re-derives pattern math).
  - **Spec coverage:** Spec-012 line 67 (posture as authorization input; per-axis minimum-posture comparison), Spec-012 line 77 (callback-tool authorization), Spec-012 line 70 (the `approvalsReviewer` pin — invariant leg)
  - **Verifies invariant:** I-012-8, I-012-11, I-012-22
  - **Consumes:** verified policy set ← T2.1; composed gate ← T2.2; posture + floor verdicts ← T2.9; callback normalization ← T2.8 (host-seam-admitted — per-class gates); callback-tool registry shape + `CallbackToolInvocation` / `CallbackToolResult` dispatch shapes ← Spec-005 (campaign B3) / Plan-005 T3.15 leg 3 + api-payload-contracts §Plan-005 (campaign B10; CP-005-7); trust read ← Plan-003 (CP-012-10)
- **T2.11 — `permission-check-service.bench.ts`: the empirical end-to-end WASM latency benchmark (campaign B13; ADR-012 P1-7).**
  - **Files:** `packages/runtime-daemon/src/policy/__tests__/permission-check-service.bench.ts` (CREATE — Vitest bench, excluded from the gate test run)
  - Benchmarks the composed `check()` path end-to-end on the WASM build **including JS↔WASM marshaling** against ADR-012's `< 1 ms at p95` empirical target (`ADR-012 §Success Criteria` — the published microsecond figures are native-engine only, so the target is validated empirically here before the `2026-10-01` Check Date, then tracked by approval-service metrics per that row's measurement method). The bench asserts the resident-authorizer posture it measures: the signature-verified compiled policy set parses once and stays resident; evaluation is per-request with **no decision cache** (I-012-21) — the benchmark measures real evaluations, never cache hits.
  - **Tests:** the bench IS the deliverable — p50/p95/p99 over representative fixtures (policy-allow, remembered-rule match, ask-policy, deny; posture-context included), plus the no-cache guard assertion (two identical back-to-back checks both evaluate).
  - **Spec coverage:** Spec-012 line 120 (the Cedar engine posture the target binds to)
  - **Verifies invariant:** I-012-21 (measurement half — the enforcement half rides T2.2)
  - **Consumes:** composed gate ← T2.2 (+ T2.10 posture context); verified artifact ← T2.1; target + Check Date ← `ADR-012 §Success Criteria` (2026-07-02 amendment)
- **T2.12 — `cancelPendingForSession` + the late-CREATE guard (campaign B13; P2-4) — EXTENDs the T2.3/T2.6 surfaces.**
  - **Files:** `packages/runtime-daemon/src/approvals/approval-service.ts` (EXTEND), `packages/runtime-daemon/src/approvals/__tests__/approval-service.test.ts` (EXTEND)
  - The session-terminal sibling of `cancelPendingForRun`: `cancelPendingForSession(sessionId, cause)` settles every pending request in the session as `canceled` on the request row (`state` + `updated_at`, no resolution row) emitting `approval.canceled` per D-012-8, with any ask-associated request's originating native ask settled `driver_ask.canceled` through T2.8's surface in the same pass — and every remaining pending **input-kind** ask of the session's runs closed the same way (no approval row exists to sweep for those; T2.8's pending-ask registry is the source, its timers disarmed). **Caller wiring:** the sweep registers on the daemon's `session.closed` event application — the same local event-application seam D-012-11's `membership.*` invalidation hook rides (Plan-001 owns session-lifecycle emission; no Plan-001 symbol authored) — so locally-initiated and relay-delivered closes both trigger it, and startup reconciliation settles any pending rows of an already-closed session (the crash window between close and sweep). The **late-CREATE guard** closes the create-after-terminal window: a `create` arriving for an already-ended run or an already-closed session refuses with the existing `approval.request_canceled` 409 carrying a **typed `reason` extension member** — `'run_ended' | 'session_closed'` — per RFC 9457 extension-member practice (error-contracts.md §Approval row, campaign B13; code reused, no new code minted), the value derived from live terminal state at refusal time and deliberately **not** persisted onto `approval.canceled` (the D-012-3 payload is unchanged; the resolve-path `approval.request_canceled` 409 carries no `reason` — audit reconstructs cause from the adjacent run/session terminal rows on the timeline), so a straggler ask surfacing after terminal can neither mint a zombie pending row nor hold a closed session's retention open.
  - **Tests:** the `session.closed` event application invokes the sweep; session close settles all pending requests across multiple runs (no resolution rows; one `approval.canceled` each; ask siblings settled) and closes pending input-kind asks that have no approval row (the T2.8 registry sweep — timers disarmed); startup reconciliation settles an already-closed session's straggler pending rows (the crash window); a late create after run end → 409 with `reason: 'run_ended'`; after session close → `reason: 'session_closed'`; the settled rows replay byte-equal (the I-012-9 posture).
  - **Spec coverage:** Spec-012 line 96 (the create boundary the guard binds), Spec-012 line 105 (durable settled state)
  - **Verifies invariant:** I-012-4, I-012-7 (terminal half)
  - **Consumes:** cancel path + typed refusals ← T2.3 (D-012-4/D-012-8); ask settle ← T2.8; seam precedent ← T2.6 (CP-012-5's `cancelPendingForRun`); `session.closed` application hook ← the daemon's local event-application seam (D-012-11's membership-hook sibling; the event is Plan-001-emitted, no Plan-001 symbol authored); `reason` member registration ← error-contracts.md §Approval (campaign B13)

### Phase 3 — Approval IPC namespace + SDK + projection + invalidation flows

**Goal.** Five `approval.*` binder files on the Plan-007 registry, the daemon-local projection service, the grant-invalidation service with its push seams, and the typed `approvalClient` SDK with registration/dispatch/error round-trip coverage.

**Preconditions.**

- [ ] Phases 1–2 merged
- [x] BL-142 landed — registry regex conformance (camelCase tails: `approval.requestCreate`-class strings throw at boot without it)
- [x] BL-143 landed — `DaemonDomainError` projection branch in `mapJsonRpcError` (T3.10's wire assertions observe anonymous `-32603` without it)

```yaml
preconditions:
  # Machine-enforced subset (preflight Gate 5). The Part-B box entry is carried
  # onto every post-Phase-1 phase: the auto-walk soft-skips an ineligible
  # phase and tries later ones, so gating Phase 2 alone would let the walk
  # select Phase 3 past the unmet ask-expiry leg.
  - { type: plan_phase, plan: 012, phase: 1, status: merged }
  - { type: plan_phase, plan: 012, phase: 2, status: merged }
  - { type: precondition_box_checked, box: "Driver-ask expiry leg authored" }
```

- [x] D-012-5 method strings ratified — api-payload-contracts.md §Approval Method-Name Registry (Tier 6)
- [x] D-012-4 error codes ratified — error-contracts.md §Approval (Tier-6 audit)

#### Tasks

- **T3.1 — Mutation binders: `approval.requestCreate` + `approval.resolve`.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/approval-request-create.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/approval-resolve.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND — barrel)
  - One binder file per method (`register<X>` + `<X>Deps` deps-closure, NS-26 `presence-read.ts` exemplar); both register `{mutating: true}` with the Phase-1 request/response schemas; handlers are thin delegators into T2.3 — normalization, persistence, emission, rule minting, and the D-012-12 approver verification live in the service, not the binder. Resolve conflicts surface as deps-thrown domain errors mapped per T3.10.
  - **Tests:** covered by T3.7.
  - **Spec coverage:** Spec-012 line 96 (create wire surface — schema-enforced at the registry validation boundary), Spec-012 line 97 (resolve wire surface), Spec-012 line 90 (fail-closed refusal seam)
  - **Verifies invariant:** I-012-12 (wire half), I-012-13
  - **Consumes:** `MethodRegistry` + `registry.register` ← Plan-007-partial (shipped Tier 1); schemas ← T1.2; service ← T2.3; strings ← D-012-5; camelCase registration ← BL-142 (§Preconditions)
- **T3.2 — Query binder: `approval.projectionRead`.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/approval-projection-read.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND)
  - Registers `{mutating: false}` (read-only-continues posture per `presence.read`); serves T3.3's projection. No `PermissionCheck` binder exists — D-012-5/D-012-18 keep the gate in-process (the pre-audit assumption of a wire preflight is struck).
  - **Tests:** covered by T3.7.
  - **Spec coverage:** Spec-012 line 99 (pending + historical state surfaced)
  - **Verifies invariant:** I-012-13
  - **Consumes:** as T3.1; projection ← T3.3
- **T3.3 — `approvals/approval-projection-service.ts`: daemon-local projection read.**
  - **Files:** `packages/runtime-daemon/src/approvals/approval-projection-service.ts` (CREATE), `packages/runtime-daemon/src/approvals/__tests__/approval-projection-service.test.ts` (CREATE)
  - SQL over `approval_requests LEFT JOIN approval_resolutions ON request_id`, keyed directly by the `session_id` column (D-012-2), filtered per the ratified request shape; rows project to the D-012-3 item shape (resolved quad iff resolved; `updatedAt` carries expiry/cancel settlement). Authorization is the local socket boundary in V1 — the service takes no caller principal by design (D-012-6; cross-participant visibility rides relayed events into each peer's own projection).
  - **Tests:** pending rows surface without resolution fields; resolved rows carry `decision`/`approverId`/`effectiveScope`/`rememberedScope` verbatim from the resolution row; expired/canceled rows carry settled `state` + `updatedAt` and no resolution fields; `state`/`category` filters restrict; unknown session → `{approvals: []}` (empty projection, not an error — the `presence.read` posture); reads resolve entirely against the local SQLite handle.
  - **Spec coverage:** Spec-012 line 99 (one surface for pending + historical), Spec-012 line 59 (resolved rows carry approver, decision, effective scope), Spec-012 line 105 (reads serve durable replayable state)
  - **Verifies invariant:** I-012-14
  - **Consumes:** tables ← T1.3; response shape ← T1.2; SQLite handle ← Plan-001 (shipped)
- **T3.4 — `policy/grant-invalidation-service.ts`: revocation writer + trigger seams.**
  - **Files:** `packages/runtime-daemon/src/policy/grant-invalidation-service.ts` (CREATE — `revokeRule(ruleId, trigger)` + `revokeDependentRules({sessionId?, participantId?, nodeId?}, trigger)` over T2.4's exported validity predicate + revocation write)
  - Implements D-012-11's timeliness/audit layer over T2.4's correctness layer: the push seams — a synchronous `node_trust_state` transition hook (CP-012-10) and a `membership.*` event-application hook over the daemon's local membership projection — call `revokeDependentRules`, which sweeps candidate rules through T2.4's validity predicate and revokes the now-invalid ones. Every revocation (push-triggered, use-time, or explicit) writes exactly one ratified trigger value + `revoked_at` and emits exactly one `approval.rule_revoked` (via T2.5) before the triggering check or hook returns; explicit revocation (`approval.ruleRevoke`) rides `revokeRule(ruleId, 'explicit')`.
  - **Tests:** T3.9 owns the behavioral suite.
  - **Spec coverage:** Spec-012 line 91 (revoked before next use), Spec-012 line 106 (explicit path + audit history), Spec-012 line 107 (membership/node-trust changes invalidate dependent rules), Spec-012 line 119 (immediate propagation — the node-trust seam is a synchronous same-process hook)
  - **Verifies invariant:** I-012-5 (revocation half), I-012-10
  - **Consumes:** rules table ← T1.3; validity predicate + use-time revocation write ← T2.4; `node_trust_state` transitions ← Plan-003 (CP-012-10); `membership.*` events ← Plan-006 taxonomy (Spec-006 §Membership Change; local projection of relay-delivered events per D-012-11); emission ← T2.5
- **T3.5 — Rule binders: `approval.ruleList` + `approval.ruleRevoke`.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/approval-rule-list.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/approval-rule-revoke.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND)
  - `ruleList` registers `{mutating: false}` (`includeRevoked: true` is the audit-history read); `ruleRevoke` registers `{mutating: true}` and delegates to T3.4 `revokeRule(ruleId, 'explicit')`. Unknown / already-revoked rules surface the D-012-4 typed codes via T3.10's mapping.
  - **Tests:** covered by T3.7; revoke-path behavior in T3.9.
  - **Spec coverage:** Spec-012 line 106 (explicit revocation path + enumerable audit history), Spec-012 line 82 (rules are explicit opt-in state the operator can enumerate and retract)
  - **Verifies invariant:** I-012-13
  - **Consumes:** schemas ← T1.2; revocation writer ← T3.4; rules table ← T1.3; strings ← D-012-5; camelCase registration ← BL-142 (§Preconditions)
- **T3.6 — `approvalClient.ts` typed SDK (daemon transport).**
  - **Files:** `packages/client-sdk/src/approvalClient.ts` (CREATE — `ApprovalClient` interface + `createDaemonApprovalClient(client: JsonRpcClient)` with the five methods `requestCreate`, `resolve`, `projectionRead`, `ruleList`, `ruleRevoke`), `packages/client-sdk/src/index.ts` (EXTEND — barrel)
  - Factory per the shipped `createDaemonRepoClient`/`createDaemonRuntimeNodeClient` convention; wire strings authored as local consts; schema-validates requests before send and responses on receipt. Deliberately NO `createControlPlaneApprovalClient` sibling and NO `permissionCheck` method — the file header records the daemon-only surface (D-012-6) and the in-process gate (D-012-5) as the V1 structural enforcement of membership-is-not-execution-authority; no subscribe path (none of the five methods streams).
  - **Tests:** covered by T3.8.
  - **Spec coverage:** Spec-012 line 96 (requestCreate), line 97 (resolve — `approver` threaded only when supplied, never synthesized), line 99 (projectionRead), line 106 (ruleList + ruleRevoke), AC2 (line 131 — structural; no control-plane approval transport exists)
  - **Verifies invariant:** I-012-15
  - **Consumes:** `JsonRpcClient` transport ← Plan-007-partial SDK substrate (shipped); schemas ← T1.2; strings ← D-012-5
- **T3.7 — Daemon handler registration + dispatch round-trip tests.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/approval-handlers.test.ts` (CREATE — combined file per the `session-handlers` precedent)
  - Per-method: registers under the exact D-012-5 string (literal equality against the registry table); duplicate registration throws (substrate I-007-6 inheritance); schema-validates-before-dispatch (malformed params — missing `approvalRequestId`, out-of-enum `category`/`decision` — produce the substrate invalid-params error with the deps spy at zero calls); `mutating` flags correct (3 true / 2 false); deps round-trip (parsed params reach the callback typed; a deps result missing a required response field is rejected before the wire); barrel exports complete.
  - **Tests:** the file IS the tests.
  - **Spec coverage:** Spec-012 line 96 (create round-trip — category/scope/expiry reach the deps callback schema-validated), Spec-012 line 97 (resolve round-trip — `approver` + `decision` reach the callback; response carries `state` + `approverId` + `resolvedAt`), Spec-012 line 99 (projection round-trip)
  - **Verifies invariant:** I-012-13
  - **Consumes:** `MethodRegistryImpl` test harness ← Plan-007-partial (shipped); binders ← T3.1, T3.2, T3.5
- **T3.8 — SDK integration tests.**
  - **Files:** `packages/client-sdk/test/approvalClient.integration.test.ts` (CREATE — per the `runtimeNodeClient.integration.test.ts` location convention)
  - In-process daemon registry + real transport: five happy-path round-trips with branded-type preservation; `projectionRead` returns historical rows whose `approverId` + `decision` + `effectiveScope` + `rememberedScope` equal the daemon payload verbatim (AC-3 wire-observable); the `state` filter round-trips; structural assertion — `approvalClient.ts` exports exactly one factory requiring a `JsonRpcClient` (no control-plane constructor); outbound frames carry the ratified strings; schema-invalid daemon responses throw the SDK validation error; the SDK computes no approval state and caches nothing.
  - **Tests:** the file IS the tests.
  - **Spec coverage:** Spec-012 AC3 (line 132 — visibility half), Spec-012 line 99 (pending + historical in one surface), Spec-012 AC2 (line 131 — structural)
  - **Verifies invariant:** I-012-14, I-012-15
  - **Consumes:** SDK ← T3.6; binders ← T3.1, T3.2, T3.5; services ← Phase 2; in-process transport harness ← Plan-007-partial (shipped)
- **T3.9 — Trust-invalidation behavioral tests (revoke-before-next-use).**
  - **Files:** `packages/runtime-daemon/src/policy/__tests__/grant-invalidation.test.ts` (CREATE)
  - Sequence test: mint rule via resolve-with-rememberedScope → flip the bound node's `node_trust_state` → the next permission check returns `allowed: false` AND the rule row already carries `revoked_at` + `invalidation_trigger = 'node_trust_change'` when the check returns. Race-window negative control: a check issued AFTER the trust flip but BEFORE any push seam runs still refuses (use-time re-validation is the correctness mechanism). Membership trigger: applying a `membership.revoked` event for the granting participant revokes that participant's dependent session rules (`invalidation_trigger = 'membership_change'`). Each revocation appends exactly one `approval.rule_revoked` and survives a daemon restart (re-open the handle — rule stays revoked). Post-revocation, the previously-allowed action requires explicit approval again (no `remembered_rule` allow).
  - **Tests:** the file IS the tests.
  - **Spec coverage:** Spec-012 line 91 (ordering — revoked before next use), Spec-012 line 107 (membership trigger), Spec-012 line 106 (one event per revocation + durable history), Spec-012 line 130 (AC-1 post-revocation re-approval)
  - **Verifies invariant:** I-012-10
  - **Consumes:** invalidation service + seams ← T3.4; match path ← T2.4; `node_trust_state` fixture writes ← Plan-003 (shipped migration `0002-runtime-node.ts`); `membership.*` fixtures ← Plan-006 taxonomy
- **T3.10 — Typed wire-error round-trips for resolve/rule conflicts.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/approval-handlers.test.ts` (EXTEND), `packages/client-sdk/test/approvalClient.integration.test.ts` (EXTEND)
  - Wire-asserts the D-012-4 codes end-to-end per the BL-143 envelope (standard JSON-RPC numeric + `data.type === 'approval.already_resolved'` etc. per error-contracts.md §JSON-RPC Wire Mapping; SDK rejection surfaces `data.type`): double-resolve → `approval.already_resolved` (first record unchanged); resolve past expiry → `approval.request_expired`; resolve after cancel → `approval.request_canceled`; unknown request → `approval.not_found`; persistence-unavailable fixture → `approval.persistence_unavailable` fail-closed with no partial row; unknown rule → `approval.rule_not_found`; double-revoke → `approval.rule_already_revoked`. Negative control: a `PermissionCheck` denial is a NORMAL in-process response (`allowed: false`), never a wire error — pinning the no-`approval.permission_denied` decision.
  - **Tests:** the file extensions ARE the tests.
  - **Spec coverage:** Spec-012 line 97 (second resolve refused, record immutable), Spec-012 line 96 (expiry refusal), Spec-012 line 90 (fail-closed persistence refusal)
  - **Verifies invariant:** I-012-7 (wire half), I-012-13
  - **Consumes:** `mapJsonRpcError` + `DaemonDomainError` projection ← BL-143 (§Preconditions — tracked-not-satisfied until the Plan-007-owned PR lands); typed error classes ← T2.3/T3.4 services; codes ← D-012-4

### Phase 4 — Desktop approval surfaces (pending, history, remembered grants)

**Goal.** The `approvals/` renderer subtree: pending-approval cards with explicit per-record approve/reject and a separate off-by-default remember opt-in, the full-record approval history, and the remembered-grants list with explicit two-step revocation — all thin bridge-only projections with mock-bridge test coverage now (Plan-002 P6 harness) and the Tier-8 manual leg dispositioned.

**Preconditions.**

- [ ] Phase 3 merged (projection + invalidation semantics stable per §Parallelization Notes)
- [x] Plan-023-partial renderer substrate + bridge stub shipped (Tier 1); Plan-002 Phase 6 happy-dom harness shipped (test-now posture)
- [x] D-012-3 projection row fields + D-012-10 `RememberedScope` enum + D-012-16 allow-only/ungrouped postures ratified (Tier-6 audit)

```yaml
preconditions:
  # Machine-enforced subset (preflight Gate 5); Part-B box carried per the
  # auto-walk soft-skip rationale on Phase 3's block.
  - { type: plan_phase, plan: 012, phase: 3, status: merged }
  - { type: precondition_box_checked, box: "Driver-ask expiry leg authored" }
```

#### Tasks

- **T4.1 — `PendingApprovalsView.tsx` (pending cards + explicit resolve + remember opt-in).**
  - **Files:** `apps/desktop/src/renderer/src/approvals/PendingApprovalsView.tsx` (CREATE)
  - Mount-triggered snapshot read with the server-side filter `{sessionId, state: "pending"}` (`loading | loaded | error` register; the client never filters by state). Subscribe-before-initial-read ordering, effect-scoped `cancelled` flag, monotonic request-sequence guard, same-session no-flicker contract, `sessionId`-change reset — the NodeRoster/ParticipantRoster postures; the five request-lifecycle events (`approval.requested`/`approved`/`rejected`/`expired`/`canceled`) are OPAQUE re-read signals (payload never decoded — a canceled card drops on the signal re-read, never via client inference). One card per record (D-012-16) rendering `category` (enum token verbatim), `scope`, `resourceDescriptor` (formatted JSON, opaque), `expiryAt` (verbatim ISO; absent renders an explicit "no expiry" label — no client expiry math), `requestedBy` + `runId`, with `data-approval-category`/`data-approval-state` test selectors. Per-card Approve and Reject each fire exactly ONE `approval.resolve` (`idle | pending | resolved | rejected` per card; buttons disabled while pending). The remember control is a SEPARATE affordance adjacent to Approve: initially off in every code path; engaging it reveals a kind select constrained to exactly the ratified `RememberedScope.kind` members (+ optional pattern field per D-012-10 — no free-text kind anywhere); untouched → the resolve payload OMITS `rememberedScope` (`request_only` default); absent/disabled on Reject (allow-only, D-012-16). The renderer performs no client-side permission gating and never populates an approver identity (D-012-12). Resolved branch renders the response verbatim; rejected branch renders the normalized wire-error envelope (`approval.already_resolved` from a concurrent resolver renders as that typed code, then the lifecycle signal re-read drops the card). Tier-1 sync-throw funnels through the async-IIFE `try/catch` (SessionBootstrap precedent). Type-only `@ai-sidekicks/contracts` imports; no `electron` / `node:*` / `@ai-sidekicks/client-sdk` (renderer-untrusted boundary).
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-012 line 99 (pending state surfaced), Spec-012 line 58 (request fields rendered), Spec-012 line 96 (category/scope/resource/expiry rendered), Spec-012 line 130 (AC-1 explicit per-record action), Spec-012 line 81 (`request_only` default), Spec-012 line 82 (separate off-by-default opt-in), Spec-012 line 118 (scope kinds as explicit enum members), Spec-012 line 117 (one card per canonical record)
  - **Verifies invariant:** I-012-16, I-012-17, I-012-19
  - **Consumes:** `approval.projectionRead` + `approval.resolve` strings ← Phase 3 (D-012-5); `ApprovalProjectionRead*`/`ApprovalResolve*` + enums + brands ← Phase 1; `RunId` ← Plan-005 Phase 1 `provider-driver.ts`; `SessionId` ← Plan-001 (shipped); the five lifecycle event names ← `Spec-006 §Approval Flow (approval_flow)` with emitters ← Phase 2; typed codes ← error-contracts §Approval (D-012-4); `window.sidekicks.daemon.call` + `daemon.subscribe` + `Unsubscribe` ← Plan-023-partial (shipped stub; live ← Tier 8 per CP-012-8); ambient bridge type ← Plan-002 Phase 6 T6.0 (shipped)
- **T4.2 — `ApprovalHistoryView.tsx` (full-record audit surface).**
  - **Files:** `apps/desktop/src/renderer/src/approvals/ApprovalHistoryView.tsx` (CREATE)
  - Mount-triggered UNFILTERED projection read rendering EVERY record returned — all five `ApprovalState` members render as labeled rows; no `.filter(...)` drops a record (admit-not-eject). Each row renders `state` verbatim, `approverId` (present iff resolved — the AC-3 "who granted" leg), `scope` + `effectiveScope` + `rememberedScope` (requested vs granted, both verbatim), `resolvedAt`/`updatedAt`/`expiryAt` with explicit absent-value labels, and `data-approval-state` attributes. `expired`/`canceled` render ONLY from wire state — the view never compares `expiryAt` to local time (the daemon owns the transitions and signals them via `approval.expired`/`approval.canceled`). Re-read posture, guards, error normalization, and the import boundary as T4.1.
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-012 line 99 (historical state surfaced), Spec-012 line 59 (approver, decision, effective scope rendered), Spec-012 AC3 (line 132 — who granted what scope), Spec-012 line 111 (a run-scoped decision renders its recorded scope verbatim)
  - **Verifies invariant:** I-012-18, I-012-19
  - **Consumes:** `approval.projectionRead` (state-unfiltered) ← Phase 3; shapes/enums ← Phase 1; the five lifecycle event names ← `Spec-006 §Approval Flow (approval_flow)` (emitters ← Phase 2); bridge ← Plan-023-partial (CP-012-8)
- **T4.3 — `RememberedGrantsView.tsx` (active rules + explicit revocation) + subtree barrel.**
  - **Files:** `apps/desktop/src/renderer/src/approvals/RememberedGrantsView.tsx` (CREATE), `apps/desktop/src/renderer/src/approvals/index.ts` (CREATE — barrels the three views + props types, `.js` specifiers; App-shell composition is Plan-023's per CP-012-8)
  - Mount-triggered `approval.ruleList` read with explicit `includeRevoked: true` (the audit-history form — the default-false active read would filter the dead rows this view must render, `Spec-012 §State And Data Implications`) rendering EVERY rule row: `category`, scope kind + pattern (enum token verbatim), grantor `participantId`, `grantedAt`, and — for dead rules — `revokedAt` + `invalidationTrigger` verbatim (a revoked rule renders labeled, never filtered; `data-rule-revoked` selector). Active rows carry a Revoke action with an explicit two-step confirm (`idle | confirming | pending | resolved | rejected` per row; only the confirming click fires exactly ONE `approval.ruleRevoke`; cancel returns to idle with zero mutations — I-012-16-conformant hardening of the destructive action). Rejected branch renders the typed code verbatim (`approval.rule_already_revoked` from a concurrent revoker, then the `approval.rule_revoked` signal re-reads). Subscribes to `approval.remembered` + `approval.rule_revoked` as opaque re-read signals — rules revoked by trust/membership invalidation surface without any client-side trust logic.
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-012 line 106 (active rules + explicit revocation path; revoked rules remain in the audit history), Spec-012 line 118 (explicit enum scope rendering), Spec-012 line 119 (trust-change propagation surfaces on the next signal re-read)
  - **Verifies invariant:** I-012-16, I-012-18
  - **Consumes:** `approval.ruleList` + `approval.ruleRevoke` strings ← Phase 3 (D-012-5); `RememberedRuleList*`/`RememberedRuleRevoke*` + `RememberedScope` ← Phase 1; `approval.remembered` (`Spec-006 §Approval Flow (approval_flow)`) + `approval.rule_revoked` (`Spec-006 §Approval Flow (approval_flow)`) event names with emitters ← Phases 2–3; `approval.rule_not_found`/`approval.rule_already_revoked` codes ← D-012-4; `ParticipantId` ← Plan-001 (shipped); bridge ← Plan-023-partial (CP-012-8)
- **T4.4 — Mock-bridge component test suites.**
  - **Files:** `apps/desktop/src/renderer/src/approvals/__tests__/PendingApprovalsView.test.tsx`, `apps/desktop/src/renderer/src/approvals/__tests__/ApprovalHistoryView.test.tsx`, `apps/desktop/src/renderer/src/approvals/__tests__/RememberedGrantsView.test.tsx` (CREATE ×3)
  - Per-suite mock `window.sidekicks` bridge (Plan-002 P6 / Plan-009/010 Phase 4 precedent). Pinned assertions: the resolve spy is NOT invoked on mount and fires exactly once after one explicit approve click; double-click while pending fires no second mutation; an N-same-category-record fixture renders N cards with N independent action sets (no grouping); the remember control's initial state is off in every render path and an untouched flow produces a payload with `"rememberedScope" in payload === false`; the engaged flow carries the selected ratified kind; a reject flow exposes no remember affordance; the kind select's DOM options equal exactly the ratified enum members (no free-text); the history suite renders all five states from an unfiltered fixture (5/5 rows, zero dropped) incl. `approverId` + effective scope on the resolved row, and asserts no time-comparison against `expiryAt` exists for a non-expired fixture; the grants suite renders a revoked-rule fixture labeled (never filtered) and revoke fires exactly one mutation on the confirming click; rejected branches render typed codes as `Error.name`; a subscribe-push re-read updates in place without flashing loading; each suite carries (a) the bridge-projection source-text assertion via `import.meta.glob(..., { query: "?raw" })` — view sources never import `electron`, `node:*`, runtime-daemon, control-plane, or `@ai-sidekicks/client-sdk` — and (b) the Tier-1 stub-throw path: a bridge throwing `NotImplementedAtTier1Error` synchronously lands every view in its rendered error/rejected branch, never an unhandled rejection.
  - **Tests:** the suites ARE the tests.
  - **Spec coverage:** Spec-012 line 130 (AC-1 explicit-action leg), Spec-012 line 82 (off-by-default opt-in), Spec-012 line 118 (ratified-enum-only scope control), Spec-012 AC3 (line 132 — rendered), Spec-012 line 99 (pending + historical fixtures verbatim), Spec-012 line 106 (one revoke mutation; revoked rows persist), Spec-012 line 117 (per-record granularity)
  - **Verifies invariant:** I-012-16, I-012-17, I-012-18, I-012-19
  - **Consumes:** views ← T4.1, T4.2, T4.3; happy-dom renderer vitest project + per-suite mock-bridge pattern ← Plan-002 Phase 6 (shipped); `@testing-library/react` ← shipped devDep; `NotImplementedAtTier1Error` (value import) ← Plan-023-partial (shipped); contract types ← Phase 1
- **T4.5 — Manual desktop approval-flow smoke — Tier-8-gated disposition record.**
  - **Files:** none (verification-disposition task; recorded in this plan's §Progress Log Notes on the Phase 4 PR)
  - The daemon-side AC-1 legs (enforcement, replay, grant validity) run in Phases 2–3 verification; THIS is the desktop-renderer leg — a real pending request approved from the desktop UI unblocking the sensitive action end-to-end — impossible until Plan-023 Tier 8 wires the live bridge. Dispositioned NOW per the Plan-003 T5.4 / Plan-009 T4.5 / Plan-010 T4.5 precedent (deferred-to-Tier-8, owner = user, tracked on the Plan-023 remainder), never silently skipped.
  - **Tests:** n/a (disposition record).
  - **Spec coverage:** Spec-012 line 130 (AC-1 — the live desktop leg)
  - **Verifies invariant:** none (disposition record — the live desktop leg executes at Plan-023 Tier 8)
  - **Consumes:** live `window.sidekicks` backend ← Plan-023 Tier 8 remainder (tracked-not-satisfied — CP-012-8)

## Parallelization Notes

- Phase 1 contract work and Phase 2 service scaffolding can proceed together once the D-012-2/D-012-3 shapes are fixed (they are — this audit).
- Within Phase 2: T2.1 (policy artifact) is independent of the service chain T2.5 → T2.4 → T2.3 → T2.6 (emitter → evaluator → service → seam); T2.2 composes T2.1 + T2.3 + T2.4 + T2.6; T2.7 gates the phase.
- Renderer approval flows (Phase 4) wait for Phase 3's projection + invalidation semantics to stabilize.
- Within Tier 6, Plan-012's Phase 2 builds after the Plan-009/Plan-010 phases owning `workspaces`/`worktrees`/`run_execution_contexts` (CP-012-9); Phases 1 and 3–4 have no within-tier ordering constraint.
- Campaign B13 additions: T1.5 rides Phase 1 beside T1.1/T1.2 (the same union seam — no new ordering); within Phase 2, T2.8 builds after T1.5 + T2.3 + T2.6, T2.9 is independent of the approval-service chain, T2.10 composes T2.1 + T2.2 + T2.8 + T2.9, T2.12 extends T2.3/T2.6, T2.7 — the phase gate — runs after T2.8/T2.9/T2.10/T2.12 land (its B13-extended matrix verifies them), and T2.11 benches the composed gate last (excluded from the gate test run; order-free against T2.7).

## Test And Verification Plan

- Permission-enforcement tests covering destructive git, out-of-boundary file writes, network access, and high-risk tool execution (T2.2, T2.7)
- Replay and restart tests proving approval state and remembered grants survive recovery byte-equal (T2.7)
- Trust-invalidation tests proving membership or node-trust changes revoke dependent grants before reuse (T3.9, T2.7)
- Own-node trust tests proving normal local execution is allowed within the node envelope while escalated sensitive actions still require explicit approval (T2.2, T2.7)
- Wire + SDK round-trips incl. typed error codes (T3.7, T3.8, T3.10)
- Renderer mock-bridge suites (T4.4)
- Manual verification of the desktop approval flow — Tier-8 leg, dispositioned per T4.5
- Bounded ask-expiry: stamped deadlines, timer-OR-answer with the wire answer ingress, receipt-guarded native delivery, restart re-arm, replay-atomic sibling effects with crash-after-first-effect coverage, never-auto-approve (T2.8, T2.7 — campaign B13)
- Execution-posture: cross-field fail-closed validation, the per-axis floor matrix, write-ahead credential-artifact ordering + retention (T2.9, T2.10, T2.7 — campaign B13)
- The ADR-012 empirical end-to-end WASM benchmark before the 2026-10-01 Check Date (T2.11 — campaign B13)
- Session-terminal settlement + the late-CREATE guard (T2.12 — campaign B13)

## Rollout Order

1. Land approval contracts and durable storage (Phase 1)
2. Enforce daemon-side permission checks with per-request approvals only (Phase 2)
3. Enable remembered grants and revocation controls once invalidation behavior is verified (Phase 3 — T3.9 is the verification gate)
4. Enable desktop approval surfaces (Phase 4)

## Rollback Or Fallback

- Disable remembered grants and fall back to `request_only` approvals everywhere if grant invalidation or replay behavior regresses — the grant-evaluator seam makes rule matching removable without touching the permission gate, and `request_only` is the schema default (omitting `rememberedScope` is always valid).
- The run-blocking seam is deregistrable without touching Plan-004's engine (zero-listener operation is a tested no-op).

## Risks And Blockers

- Organization-level policy defaults remain unresolved for the first implementation (deferral tracked in parent [Spec-012](../specs/012-approvals-permissions-and-trust-boundaries.md))
- Provider-native permission semantics may drift unless normalization is enforced before approval records are written — mitigated by the schema-validated `requestCreate` boundary now and the CP-012-6 normalizer at driver-integration time
- Own-node trust can be over-broadened unless envelope boundaries remain explicit in permission checks and UI copy — mitigated by D-012-15 derived inputs + the strict per-request fallback (`Spec-012 §Fallback Behavior`)
- Membership-change invalidation rides asynchronous relay delivery — mitigated by D-012-11 (use-time re-validation is the correctness mechanism; push seams are timeliness only)
- `@cedar-policy/cedar-wasm` is the V1 evaluator (4.11.x pin) — engine-version drift surfaces in T2.1/T2.2 fixtures; the V1→V1.1 custody delta is policy source only (D-012-9)
- BL-142 / BL-143 are Plan-007-owned Phase-3 preconditions; if they slip, Phases 1–2 still land

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped: []
```

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

- 2026-06-10 — Tier-6 plan-readiness audit (NS-18): plan rewritten to the audited four-phase Tasks structure; 64 findings adjudicated (13 critical / 42 major / 9 minor) across 28 cross-phase adjudications (A-1…A-28). Ratified in the same audit PR: the api-payload §Plan-012 wire block + §Approval Method-Name Registry (5 strings, new `approval` root), the three-table DDL, the §Approval error vocabulary (7 codes), the `approval.canceled` taxonomy addition (Spec-006 → 126/19 at Plan-012's step; 130/19 tier-final after Plan-016's four), the ADR-012 engine/custody clarifications + `@cedar-policy/cedar-wasm` package-name correction, and the Plan-004 (CP-004-9), Plan-005 (CP-005-7), Plan-009 (CP-009-8), Plan-010 (CP-010-11), and Spec-023 reciprocal amendments. The control-plane projection Target Area was struck (D-012-6); `PermissionCheck` was adjudicated in-process-only (D-012-5/D-012-18).
- 2026-07-20 — Campaign B13 amendment (capability-enhancement campaign Task 16; design §4 B13 row): authored T1.5 (driver-ask contract registration — the four `driver_ask.*` payload schemas, union variants, and `driverAskPayloadRefinementFor`, per the Plan-006 §Event Taxonomy Coverage ownership row) and T2.8–T2.12 (the driver-ask normalizer with bounded fail-closed expiry, satisfying CP-012-6 and checking the Part-B Preconditions box; the execution-posture service + content-addressed `credential_policy_artifacts` store — daemon table census 51 → 52; posture-as-authorization + callback-tool Cedar governance; the ADR-012 P1-7 end-to-end WASM benchmark; `cancelPendingForSession` + the late-CREATE guard with the typed `reason` extension member on `approval.request_canceled`), extended T2.2/T2.6/T2.7 and CP-012-2/-5/-6, and added invariants I-012-20 (allow-listed spawn env), I-012-21 (resident authorizer, no decision cache), I-012-22 (Cedar-pipeline-exclusive adjudication). **Status flipped `approved` → `review`** (Status Flip Rule row 3 — new invariants); restored at the campaign's W2.5 targeted re-audit (Task 23). Reciprocals consumed, never authored: Plan-004 T3.15 (input-ask park edge) + CP-004-8 (`RunSetupGate` posture-resolution registration), Plan-005 T3.14 P0-4/P1-4-driver (spawn-env enforcement; ask mechanisms). Same-PR drift correction: the cross-plan dep-map's two narrative sites that labeled the no-decision-cache invariant "I-012-20" now cite I-012-21 — the campaign design and the landed Plan-005 P0-4 text both pin I-012-20 as the spawn-env invariant. Pre-push ripple audit (same PR): Phase 1's Goal gained its **Campaign B13 adds** sentence (T1.5 was invisible in the phase summary); D-012-15's ordinal phrasing was retired for the dedicated-trust-store form in both its sites — the amendment adds a literal fourth table that is not a trust store; and the §Invariants Tasks cells were reconciled to enumerate exactly the tasks whose **Verifies invariant** lines claim them (adding the new T2.7/T2.8/T2.10/T2.12 claims and closing the pre-existing T2.7→I-012-8, T3.10→I-012-7, T4.2→I-012-19 gaps). Codex round 1 (same PR, ten findings folded): CP-012-11 authored — the run-keyed `resolvedPostureFor(runId)` read-back beside the void-returning CP-004-8 gate, with Plan-004's Tier-6 reciprocal note extended (execution delta f); T2.8 gains the answer-ingress seam (`respondToAsk` fed by the approval resolution and Plan-005's client-facing `driver.respondToRequest`) and the `command_receipts`-guarded native delivery; T2.12's sweep registers on the `session.closed` event application (the D-012-11 membership-hook sibling) and the `reason` member is scoped to the late-CREATE refusal (derived live, never persisted onto `approval.canceled`); T2.9 gains the `pruneUnreferencedArtifacts` retention leg on the Plan-022 shred-fan-out callback and the hardened DDL ref CHECK (length + case-sensitive hex, replacing the permissive `LIKE`); T1.5's refinement binds the later-state `expiresAt` echo (the api-payload pre-documentation transcribed in full); T2.10 names the callback route precisely (T2.8 admission → composed `check()`, evaluate-first, never a bare create); T2.7's Consumes and the Parallelization order now place the phase gate after the B13 services it verifies. Codex round 2 (same PR, three findings folded): the CP-012-6/T2.8/T2.10 admission text gained **per-class capability gates** — P2-8's `interactive_requests` governs provider-native asks only, the callback route rides the independent `callback_tools` host seam, so neither flag withholds the other class's governance; the T2.9 prune re-anchored from the shred fan-out — which deletes no event rows (crypto-shred clears `pii_payload` only) — to the V1.1 `gdpr.sessionPurge` completion as an explicit future consumer, V1 retention indefinite-by-design and content-address-bounded; and the P1-3 idle-sweep exemption is carried by the blocking state itself (Plan-016 T2.7's blocking-state hard-skip, the campaign-B15 scheduled sweep leg), never a Plan-012-owned predicate. Codex round 3 (same PR, seven findings folded): the T2.8 receipt guard staged on its owning migration — a dormant write seam until Plan-015's two-phase columns land (Plan-005 T3.13 precedent), pre-activation reconciliation re-issuing unconditionally on the inert-duplicate argument; the input-kind ask lifecycle gained its opening/resume legs — T2.8's input-wait and resume signals drive `running → waiting_for_input` / `waiting_for_input → running` through the run-engine's transition applicator (Plan-004 Tier-6 reciprocal note; the R2 blocking-state exemption text corrected — `onBlocking` never fires for input asks), making T3.15's park edge reachable; `respondToAsk` re-keyed `(runId, askId)` (provider-native ask ids are per-leg unique only; the wire params already carry both); registration made idempotent on that identity for both kinds (the input kind has no approval row to collide); the T1.5 epoch composition restricted to the `requested`+`canceled` closed pair per Spec-006's carrier registration (stamped terminals reject); the DDL gained `NOT NULL` on `ref` (a rowid-table TEXT PRIMARY KEY admits NULL and a NULL CHECK passes — empirically verified); and CLAUDE.md's audit-gate exception clause now names the campaign-flipped `review` plans alongside Plan-014. Lead self-audit in the round-4 window (same PR, one finding folded): cancellation ratified as the third settle — timer-OR-answer-OR-cancel — with run-terminal (`cancelPendingForRun`, one Plan-004 arm) and session-close (T2.12 sweep) cancellation closing pending asks of **both kinds** (`Spec-006 §Driver Ask Events`' run-ended cause; the input kind has no approval row, so the T2.8 registry is the sweep source), timers disarmed, no park signal, late answers no-oping — closing the input-kind-orphaned class the round-3 findings exposed at its remaining two call sites — and the ask identity ratified **daemon-minted** (`askId` a ULID; the provider-native request id is leg-local correlation state), so a provider restart recycling native id counters can never collide a post-resume ask with a settled (`run_id`, `ask_id`) row and the T1.3 partial-unique index is safe for the log's whole life. Codex round 4 (same PR, six findings folded): the provider-retraction ingress authored — a live-leg withdrawal settles `driver_ask.canceled` at once, timer disarmed, the permission-kind approval settled `approval.canceled` in the same atomic pass, and the blocking state released through the run-engine's transition applicator (`waiting_for_*` → `running`, no outcome delivered; run-state-machine.md's release edges annotate the cause); T2.9's content address enforced, not assumed — every artifact read by ref recomputes the RFC 8785 JCS SHA-256 over the stored bytes and fails closed on mismatch, the run taking `Spec-012 §Fallback Behavior`'s strict posture; T1.5's refinement tightened both ways — `response` exclusive to `driver_ask.responded` (rejected on the other three states) and the permission-kind `input` echo required on **every** state, Plan-004 T3.11's closed-pair `canceled` row included; Spec-006's two canceled rows gained their missing causes (session-close on both, per T2.12's sweep, and provider retraction on `approval.canceled`); and Plan-005 T3.15 leg 3's callback route re-pointed from the published `approval.requestCreate` seam to the Plan-012-registered evaluation seam — canonical request-shaped inputs into the composed `check()`, evaluate-first per T2.10/D-012-18 — the cross-PR reciprocal recorded in Plan-005's Notes. Codex round 5 (same PR, six findings folded, the final round): native permission asks made **evaluate-first** — T2.2's composed `check()` runs before any mint, a policy allow / valid remembered rule settles the pair in the admission pass (no approval row, no blocking state, timer never armed; a Cedar deny never becomes a human prompt) and only an ask-policy outcome mints through T2.3's `create`, the same D-012-18 discipline as the callback route; the Codex approval server requests (`item/commandExecution|fileChange|permissions/requestApproval` + legacy `execCommandApproval` / `applyPatchApproval`) named on the permission-kind ingress in both T2.8 and Plan-005 P1-4-driver; guardian / auto-review frames re-pointed off the unregistered "observability row" phrasing onto Plan-005's `DriverDiagnosticRecord` diagnostic surface at all three sites (off-timeline by design — no registered `approval_flow` kind exists for them); the retraction release mirrored into run-state-machine.md's authoritative Complete Transition Table as two dedicated rows; `approval.request_canceled`'s error row broadened with the retraction cause and the paired `driver_ask.canceled` audit anchor; and Plan-005 P0-4 re-stated to carry I-012-20's full layering — the daemon-constructed `{...curatedBase, ...runProvisionedVars}` allow-list primary, provider-native mechanisms beneath (Plan-005 Notes reciprocal). Lead self-audit in the same window: the T1.5 refinement-summary mirror in api-payload-contracts.md aligned to round 4's tightened rules (permission-kind `input` on every state; `response` refused off `responded`), and the T2.9 integrity failure given its typed code — `approval.policy_artifact_corrupt` minted into the error-contracts §Approval vocabulary (7 → 8 codes; 500, deliberately distinct from Spec-014's `artifact.hash_mismatch` fetch-conflict domain).

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated
