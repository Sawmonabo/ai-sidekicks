# Plan-011: Live Timeline Visibility And Reasoning Surfaces

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `011` |
| **Slug** | `live-timeline-visibility-and-reasoning-surfaces` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-011: Live Timeline Visibility And Reasoning Surfaces](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md) |
| **Required ADRs** | [ADR-001](../decisions/001-session-is-the-primary-domain-object.md), [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md), [ADR-018](../decisions/018-cross-version-compatibility.md) |
| **Dependencies** | [Plan-005](./005-session-event-taxonomy-and-audit-log.md) (event taxonomy), [Plan-003](./003-queue-steer-pause-resume.md) (superseded-turn read seam — the exported `supersededTurns(runId)` attribution read, CP-003-13; and the edit-and-resend entry component this plan's timeline row mounts without authoring, CP-003-15), [Plan-014](./014-multi-agent-channels-and-orchestration.md) (durable child-run events + carrier consumed for timeline summary rows; Plan-014 publishes events only, D-014-14), [Plan-004](./004-provider-driver-contract-and-capabilities.md) (`usage_telemetry` driver events feeding the composer usage meters — `usage.context_window_update` / `usage.context_compacted` for the context-window meter, consumed off Plan-005's `event.subscribe` read path; declared from this side only, no Plan-011-minted ledger row), [Plan-006](./006-local-ipc-and-daemon-control.md) (the local IPC substrate this plan's four `timeline.*` method strings register against and whose framer bounds every page they return — the daemon `MethodRegistry`, the shared `StreamingPrimitive` `timeline.subscribe` binds through, and the declared message-size limit both the framer and this plan's page builders read; CP-006-16, gated on Plan-006 Phase 2, which shipped both) |
| **Cross-Plan Deps** | Cross-Plan Dependency Graph |

## Goal

Implement the replay-backed session timeline, child-run visibility, policy-aware reasoning surfaces, and composer usage meters used by the primary session experience.

## Scope

This plan covers timeline projections, live subscribe plus replay recovery, child-run summaries and expansion, reasoning-availability surfaces, and the composer usage meters (the context-window meter and the rate-limit indicator, [Spec-011 §Context Window and Usage Meters](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#context-window-and-usage-meters)).

## Non-Goals

- Notification routing
- Provider-specific reasoning rendering beyond normalized surfaces
- Full timeline design polish

## Invariants

- **I-011-1** — Every run-scoped timeline row carries the complete attribution triple `runId` + `position` + `epoch`, all-or-none; a partial row fails SDK schema parse rather than being delivered. **Run-scopedness is decided by three tests, not by category alone**: the row's category where that category is closed on the run-scoped side (`run_lifecycle`), the row's canonical event type against the census of `Spec-005` types whose registered payload names a run unconditionally, and — for the types whose run identity is registered OPTIONAL, which no type-level test can decide — the row's own payload naming one. A row answering any of the three and carrying the attribution-free `kind` is refused, because such a row is reachable by no run filter and permanently current under rollback projection. Grounds in [Spec-011 §Required Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#required-behavior).
- **I-011-2** — A rolled-back turn is never dropped from the timeline; it is marked superseded and stays renderable. Grounds in [Spec-003 §Required Behavior](../specs/003-queue-steer-pause-resume.md#required-behavior).
- **I-011-3** — The `superseded` marker is single-field (`targetPosition`); run identity and source epoch are read from the containing row, so live marking and replay marking cannot disagree. **The same no-second-source rule binds the row's payload**: where a projection echoes the canonical run identity or the `sourceEpoch` / `sourcePosition` origin keys, each must equal the row's own outer `runId` / `epoch` / `position`. Run identity is checked under **both** registered spellings — `runId`, and the intervention family's `targetRunId`, which is the same fact named from the other side of the relationship ([Spec-005 §Queue and Intervention (interactive_request)](../specs/005-session-event-taxonomy-and-audit-log.md#queue-and-intervention-interactive_request)) — because a rule reading only the first leaves every intervention row free to be filed under one run while naming another, and the row's own general-arm refusal already treats both spellings as run attribution. The run-identity half binds every arm carrying an outer `runId`, the compacted-stub arm included; the origin-key half binds the arm that carries ordinals. Echoing is optional and absence passes; disagreement is refused, because consumers key filtering and superseded treatment on the outer triple while the row's detail and provenance are read from the payload, so a row stating two attributions is filed under one turn and sourced from another. Grounds in [Spec-011 §Required Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#required-behavior).
- **I-011-4** — Superseding is epoch-scoped: a re-executed reused ordinal in a later epoch renders current while the earlier epoch's same ordinal stays superseded. Grounds in [Spec-003 §Required Behavior](../specs/003-queue-steer-pause-resume.md#required-behavior).
- **I-011-5** — Every `run.rolled_back` boundary payload validates into the typed `RunRolledBackEvent` shape at projection; no consumer receives an untyped cutoff or performs a cast. Grounds in [Spec-011 §Required Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#required-behavior).
- **I-011-6** — The rollback boundary is delivered visibility-resolved to every filtered subscription admitting any of the affected run's rows, never keyed on the event's optional `channelId`. Grounds in [Spec-011 §Required Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#required-behavior).
- **I-011-7** — Reasoning unavailability always produces a visible explanation surface; redaction never renders as absence. Grounds in [Spec-011 §Acceptance Criteria](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#acceptance-criteria).
- **I-011-8** — Detailed-reasoning expiry or compaction never erases the durable summary or the policy marker. Grounds in [Spec-011 §Fallback Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#fallback-behavior).
- **I-011-9** — No renderer surface settles a driver ask or a compaction: an input-ask card's answered and canceled states and the compaction affordance's completed state each render **only** from the corresponding daemon event row (`driver_ask.responded` / `driver_ask.canceled`; `usage.context_compacted`), never from a dispatch being accepted or a locally predicted value. An input-ask card holds no countdown at all, because the ask waits until it is answered. Grounds in [Spec-011 §Timeline Entry Types](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#timeline-entry-types) (the `driver_ask` entry) and [Spec-011 §Context Window and Usage Meters](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#context-window-and-usage-meters).
- **I-011-10** — A child-run summary whose detail is incomplete stays visible and says so **explicitly**: its completeness marker is required, its incomplete arm names a cause from the closed set (`detail_fetch_failed` / `compacted`) together with the time observed, and the row's event count on that arm is a lower bound rather than a total. Incompleteness is never inferred from a low count and never rendered as absence. Grounds in [Spec-011 §Fallback Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#fallback-behavior).
- **I-011-11** — Every reply of `TimelineRead`, `ReasoningSurfaceRead`, and `ChildRunExpand` rides **one** transport message body. The bound is stated on the reply's **encoded size** against the substrate's own declared limit (CP-006-16), never on a row count — the row's free-form text fields and its opaque payload are each bounded independently of any count, so no count ceiling bounds the page — and it is a parse-time refusal rather than a transport-time discovery, because an oversized response cannot carry its own error and closes the connection instead. A producer that stops short of the caller's window reports `hasMore` and hands back the cursor that continues it; a terminal page may carry a cursor and is never required to. **The budget bounds aggregation and never bounds a page below one entry.** A continuing reply carries at least one entry — a page that promises more and delivers none re-offers the same cursor forever while reading like progress — so where the first candidate alone exceeds the budget the producer pages that single entry rather than answering with zero, and the reply is refused **for its size** at the response boundary, naming the member and its measured bytes on an error frame the substrate can deliver. The undeliverable row is therefore reported structurally, on every producer, instead of as a bare zero each caller had to remember to special-case; the budget itself is unchanged and nothing oversized reaches the wire. A **terminal** page carries no such floor: an empty final page is the honest answer to a continuation whose cursor already sat at the end and to a filtered read that matched nothing. Grounds in [Spec-011 §Interfaces And Contracts](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#interfaces-and-contracts).
- **I-011-12** — A returned page is internally consistent with the header it carries: its entries never move backwards in sequence, every run-scoped entry of a `ChildRunExpand` reply belongs to the run that reply names, and a child-run summary is never its own parent. Each of the three is a schema-level refusal on the response, not a renderer convention, so a projection bug surfaces at the contract boundary rather than as a mis-ordered or misattributed surface. Grounds in [Spec-011 §Interfaces And Contracts](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#interfaces-and-contracts).
- **I-011-13** — Every timeline reply and every timeline emission is scoped to the request that asked for it: a `TimelineRead` page contains rows of the requested session only, a `ChildRunExpand` reply names the requested run, and a `TimelineSubscribe` stream carries rows of the subscribed session only. No response schema can state this — a schema never sees the request — so it is enforced at the daemon binder, on the caller's own parsed request. A cross-scope reply is refused as an **internal** failure and never delivered, because the caller asked a well-formed question and the daemon assembled a well-formed answer about a different subject, which correlates to the caller's own request id and is therefore indistinguishable from a correct answer. **Two further replies are judged against the request rather than against themselves**, and by the same mechanism for the same reason. A page may not exceed the window the caller asked for: the effective ceiling is `TimelineRead`'s own `limit` where the request named one and the default row ceiling where it did not, and a response schema can bound a page only at the default, so a read for ten rows answering with the maximum is otherwise well-formed and a client sizing a viewport, a budget, or a render pass from its own request is handed more with nothing on the reply saying so — `ChildRunExpand` declares no `limit`, so its ceiling is that default, stated on the same binder so one rule covers both paged reads. And: a `ReasoningSurfaceRead` answering `available` with **no entries** is correct on a continuation whose cursor already sat at the end of the surface, and a defect on a **first** read, where it renders as a surface that exists and shows nothing — indistinguishable from the unavailable state while asserting the opposite. The response schema cannot separate the two, because the request is what separates them; it carries the half it can see (the continuing arm's non-empty floor, which is the arm that can loop) and this binder carries the half it cannot. Grounds in [Spec-011 §Interfaces And Contracts](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#interfaces-and-contracts).

## Cross-Plan Obligations

| ID | Direction | Counterparty | Obligation |
| --- | --- | --- | --- |
| CP-003-13 | consumes | Plan-003 | Consumes T3.14's exported `supersededTurns(runId)` attribution read to compute each row's `superseded` marker on read, replay, and live append. Ledger row is Plan-003's; this plan mints none. |
| CP-003-15 | consumes | Plan-003 | Exposes a footer composition point on the user `user.message` timeline row and **mounts** Plan-003's edit-and-resend entry component there (T4.2), passing the row facts its eligibility predicate reads — run id, target position, the row's authorship, and the superseded marker this plan already computes. Plan-003 authors the component and the predicate in its own `run-controls/` subtree (its T4.8); this plan authors no run-control symbol and Plan-003 edits no `timeline/` file — the host-mounts/owner-authors split T4.4 already follows against Plan-021's composer-area point. Ledger row is Plan-003's; this plan mints none. |
| CP-005-8 | consumes | Plan-005 | Renders compacted superseded rows through Plan-005 T4.8's `<CompactedStubSegment>` audit-stub render contract — consumed, never re-implemented. Bidirectional. |
| D-014-14 | consumes | Plan-014 | Consumes Plan-014's durable child-run events and carrier for timeline child-run summary rows; Plan-014 publishes events only. |
| CP-006-16 | consumes | Plan-006 | Consumes Plan-006's IPC substrate and authors none of it: the daemon `MethodRegistry` this plan's four `timeline.*` method strings register against (T1.4), the shared `StreamingPrimitive` `timeline.subscribe` binds its producer through (T1.5, inheriting I-006-10 and I-006-11 unchanged), and the framer's declared message-size limit every page is bounded against (T1.5, I-011-11). The limit's **value** moves to `packages/contracts/src/jsonrpc.ts` as the single declared source both readers share, and `local-ipc-gateway.ts` re-exports it under its own name, so Plan-006's enforcement sites, its spelling, and its importers are all unchanged — which is why Plan-006 records a no-flip. Ledger row is Plan-006's; this plan mints none. |
| CP-006-17 | consumes | Plan-006 | Consumes the daemon-resolved calling principal Plan-006 Phase 2B (T-006p-2B-1) stamps on `HandlerContext`, and resolves none of its own. T3.2's reasoning-surface read is policy-aware — it emits `policy_redacted` with a reason — so it must evaluate a caller, while `ReasoningSurfaceReadRequest` deliberately carries **no** principal member (the settlement, registered on [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model)'s informational-body-fields bullet). Those two facts together make the dispatch context the only source, and this plan authors none of that resolution: an absent principal is refused here, never substituted. Held by a §Preconditions box scoped to T3.2. Ledger row is Plan-006's; this plan mints none. |
| CP-004-12 | consumes | Plan-004 | Consumes two Plan-004 surfaces for the 2026-08-29 console-parity legs, and authors neither: (a) the already-registered client-facing `driver.respondToRequest` as the input-ask answer ingress, whose `response` is unconstrained, so the free-text arm needs no wire growth; and (b) the client-facing `driver.compactContext` verb plus the `context_compaction` capability declaration the composer affordance gates on, read through the existing `driver.listCapabilities` cache. That verb takes `{ sessionId, runId }` — the run being what its authorization adjudicates — and the daemon resolves the live provider binding, so this plan's control dispatches with the run it already renders meters for and never handles a binding identifier. Plan-004 owns both verbs, the capability declaration, and the `options?` producer at its normalize boundary; this plan renders and dispatches only. |

## Preconditions

- [x] Paired spec is approved
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [ ] **Plan-006 Phase 2B merged — the authenticated principal on the dispatch context.** T3.2's reasoning-surface read is policy-aware: it decides between `available` and `policy_redacted`, which is an authorization decision, and [Spec-011 §Acceptance Criteria](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#acceptance-criteria) requires the redacted case to render as an explanation rather than as absence (I-011-7). It therefore needs a caller. The settlement decided that `ReasoningSurfaceReadRequest` carries **no** principal member — the caller is resolved from the transport, and a supplied one is refused rather than stripped — so the dispatch context is the only place a principal can come from, and `HandlerContext` carries only `transportId` until Plan-006's Phase-2B supplement (T-006p-2B-1) lands it under I-006-21 and CP-006-17. The hold is **task-scoped and reviewer-checkable** on T3.2 alone — deliberately not a phase-level `external_plan_phase_merged` entry, which would hold T3.1 / T3.3 / T3.4 for a dependency none of them has. Until it clears, T3.2 does not dispatch; it is not implemented against a placeholder identity, because a placeholder is indistinguishable from a real caller to the policy check and would turn a fail-closed read into a fail-open one.
- [ ] **Plan-003 Phase 3B merged** — T3.14 provides the `supersededTurns(runId)` read seam CP-003-13 obliges this plan to consume, and T3.14 rides Plan-003's Phase-3B explicit-label rollback supplement. Phase 2 below carries the matching `external_plan_phase_merged` gate.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/timeline/`
- `packages/contracts/src/jsonrpc.ts` — Plan-006 owned; EXTENDed by the declared message-size limit and its byte measure, which the page bound reads and the gateway re-exports unchanged (CP-006-16)
- `packages/runtime-daemon/src/ipc/handlers/timeline-methods.ts` — this plan's registration seam against Plan-006's `MethodRegistry` and `StreamingPrimitive`, both consumed and neither authored (CP-006-16)
- `packages/runtime-daemon/src/ipc/subscription-ack-barrier.ts` — Plan-006 substrate; authored here by extraction from its `handlers/session-subscribe.ts` on this plan's second use, so both streaming binders share one I-006-10 implementation (CP-006-16)
- `packages/runtime-daemon/src/timeline/timeline-projector.ts`
- `packages/runtime-daemon/src/timeline/reasoning-surface-service.ts`
- `packages/runtime-daemon/src/timeline/child-run-summary-service.ts`
- `packages/client-sdk/src/timelineClient.ts`
- `apps/desktop/src/renderer/src/timeline/`
- `apps/desktop/src/renderer/src/reasoning-surfaces/`
- `apps/desktop/src/renderer/src/usage-meters/`

## Data And Storage Changes

- Add or extend replayable timeline projection storage for ordered rows, child-run summary rows, and reasoning availability metadata.
- Preserve provenance links from timeline rows back to canonical event ids, run ids, runtime nodes, and policy-redaction reasons.
- Store durable reasoning summaries and policy markers separately from any bounded detailed-reasoning diagnostic payloads.
- Carry the projection-derived superseded marker on timeline rows of rolled-back turns (CP-003-13): computed from Plan-003 T3.14's exported `supersededTurns(runId)` read, epoch-scoped per [Spec-003 §Required Behavior](../specs/003-queue-steer-pause-resume.md#required-behavior) (a re-executed reused ordinal stays current), provenance to the accepted `run.rolled_back` boundary (run and carried rewind cutoff) plus the projection-derived source epoch, surviving projection rebuilds and composing with audit-stub rows over compacted regions ([Spec-005 §Compacted Event Format](../specs/005-session-event-taxonomy-and-audit-log.md#compacted-event-format)).

## API And Transport Changes

- Add `TimelineRead`, `TimelineSubscribe`, `ReasoningSurfaceRead`, and `ChildRunExpand` to shared contracts and the typed client SDK.
- Ensure live subscription payloads and replay windows use the same row schema so reconnect recovery does not require projection translation.
- Give all three reads the same continuation shape: `TimelineReadRequest`, `ReasoningSurfaceReadRequest`, and `ChildRunExpandRequest` each take an optional `afterCursor`, and each response carries `hasMore` with `nextCursor` **required** on the continuing arm and optional on the terminal one — the asymmetry deliberate, so a caller moving from a completed read to `timeline.subscribe` has an after-cursor to hand it wherever the daemon has one. The reasoning surface takes the split inside its `available` state only; `unavailable`, `compacted`, and `policy_redacted` carry no continuation, having no entries to continue.
- Bound every page on its **encoded size** against the substrate's declared message-size limit (CP-006-16), checked once per reply and paired with the existing row ceiling, with the producer's page fill stopping at whichever trips first. The limit is declared once in `packages/contracts/src/jsonrpc.ts` and re-exported unchanged by the gateway that enforces it, so the framer and the page builder can never read different numbers. The reserve the budget subtracts is likewise derived from enforced rules rather than assumed ones: the echoed request `id` is bounded at the request boundary by Plan-006 (CP-006-16), so no id the substrate accepted can displace a page this budget sized. A row-count ceiling alone cannot carry this: `TimelineRow` mirrors the canonical event envelope's unbounded `payload` record, and even excluding it the ceiling's worst case over the row's independently-bounded free-form text fields exceeds the limit outright.
- The timeline row model carries an optional single-field `superseded` marker — `{targetPosition}`, present exactly when the row's turn is superseded, absence meaning current; the marker's run identity and source epoch are the containing row's own `runId` + `epoch`, so no duplicated fields exist to disagree and live marking is identical to replay marking by construction — identical on `TimelineRead` windows and `TimelineSubscribe` replay ([API Payload Contracts](../architecture/contracts/api-payload-contracts.md) mirrors the shape); run-scoped rows are the required-attribution variant `RunScopedTimelineEntry` — `runId` + `position` + `epoch` all required, all-or-none by construction, a partial row failing SDK schema parse (the projection-resolved originating run position and execution epoch — Plan-003 T3.14's uniform row-to-turn assignment and row attribution; the session `sequence` is never a run position, and reused ordinals mean `position` can never recover the epoch) — and every timeline surface returns the `TimelineRow` union, genuinely discriminated on the literal `kind` field (`rollback_boundary` | `run` | `legacy_stub` | `general` — consumers narrow structurally on `row.kind`, never probing the free-form `type`; `legacy_stub` carries a vacuous-attribution-era compacted row: `runId` preserved, position/epoch structurally absent because unknowable), whose boundary arm is the typed `TimelineRollbackBoundary` (payload validated into the `RunRolledBackEvent` shape at projection; the SDK parser hands consumers the narrowed arm, never a cast) carrying the rewind cutoff, the comparands of the live-stream client rule: cached rows of that run with `position` above it mark — each marker just the boundary's cutoff against the row's own identity, identical to replay by construction — and rows delivered after the boundary arrive marker-precomputed — a late straggler pre-marked exactly when its stamped attribution ranks above the run's effective lineage-minimum cutoff for its epoch, current within the surviving history; new-epoch rows unmarked — per [Spec-011 §Required Behavior](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#required-behavior).

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

1. Define timeline-row, child-run-summary, and reasoning-availability contracts in shared packages.
2. Implement daemon-owned timeline projection and replay-aware subscription delivery from canonical events — consuming Plan-003 T3.14's exported `supersededTurns(runId)` (CP-003-13) to compute each row's `superseded` marker on read and replay windows, stamping each run-scoped row's `runId` + `position` + `epoch` at emission and marking live-appended rows there too (a late pre-rollback straggler appends pre-marked exactly when its stamped attribution ranks above the run's effective lineage-minimum cutoff for its epoch and current when it ranks into the surviving history; a new-epoch row appends unmarked), emitting the typed `run.rolled_back` boundary entry (`TimelineRollbackBoundary` — payload validated into the `RunRolledBackEvent` shape, never delivered untyped) with its rewind cutoff for the client-side rule over already-delivered rows — delivered visibility-resolved to every filtered subscription admitting any of the affected run's rows, never keyed on the event's optional `channelId`, and composing the marker with audit-stub rows over compacted regions — an attributed stub populates the run arm from its stub-preserved keys, while a vacuous-era position-less legacy stub takes the `legacy_stub` arm, rendering the placeholder alone, exempt from marking by construction since its run can never admit a rollback while it exists.
3. Implement child-run expansion plus summary-first, policy-aware reasoning-surface reads emitting the closed `availability` states (`available` / `unavailable` / `compacted` / `policy_redacted`).
4. Add desktop timeline rendering for live rows, summarized child runs, visible placeholders for every non-`available` reasoning state, and the distinct superseded treatment for rolled-back turns — applied live via the `run.rolled_back` boundary entry's idempotent already-delivered-rows rule and rendered from the row marker on read/replay, never dropping rewound history and never marking a re-executed reused ordinal — a compacted superseded row rendering as Plan-005's `<CompactedStubSegment>` (the CP-005-8 audit-stub render contract, consumed here, never re-implemented) composed with the superseded treatment. Expose the user `user.message` row's footer composition point and mount Plan-003's edit-and-resend entry component there, passing the row facts its predicate reads and authoring no part of the component or its eligibility logic (CP-003-15). Also add the composer usage meters ([Spec-011 §Context Window and Usage Meters](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#context-window-and-usage-meters)): the always-visible context-window meter fed by `usage.context_window_update` / `usage.context_compacted` off the canonical event stream (T4.4), and the rate-limit indicator held by the "Rate-limit indicator carrier reconciled" §Preconditions box (T4.5).

## Implementation Phase Sequence

Plan-011 implementation lands as a sequence of small PRs. Each PR exercises one slice of the plan's vertical and carries a `**Precondition:**` line so the merge order is reviewer-checkable. Phase N corresponds 1:1 to §Implementation Steps step N; the ordering is the one §Rollout Order and §Parallelization Notes state.

### Phase 1 — Timeline Contracts

**Shipped.** Phase 1 has merged; its contracts live under `packages/contracts/src/timeline/`, which is why [cross-plan-dependencies](../architecture/cross-plan-dependencies.md) lists Phases 2, 3 and 4 and not this one.

**Precondition:** none beyond the plan's own §Preconditions. Implementation Step 1; gates both parallel legs §Parallelization Notes names ("once row schemas are fixed").

#### Tasks

- **T1.1** — Define the `TimelineRow` discriminated union in `packages/contracts/src/timeline/` — arms `TimelineRollbackBoundary` | `RunScopedTimelineEntry` | `LegacyStubTimelineEntry` | `TimelineEntry`, genuinely discriminated on the literal `kind` field, mirroring [API Payload Contracts](../architecture/contracts/api-payload-contracts.md). `RunScopedTimelineEntry` requires `runId` + `position` + `epoch` all-or-none with the optional single-field `superseded` marker; `LegacyStubTimelineEntry` preserves `runId` with position and epoch structurally absent. The `general` arm's refusal is three-legged per I-011-1 — the closed `run_lifecycle` category, the derived census of `Spec-005` types whose registered payload names a run unconditionally, and the row's own payload naming one under `runId` or the intervention family's `targetRunId` — with the census **derived from Plan-005's per-category type arrays** rather than transcribed, and its size pinned in a test so a taxonomy growth fails loudly instead of changing the set silently. The `run` arm additionally refuses a payload whose echoed run identity — under **either** spelling, `runId` or `targetRunId` — or whose echoed `sourceEpoch` / `sourcePosition` disagrees with the row's own outer triple, per I-011-3; the run-identity half of that check is shared with the `legacy_stub` arm, which carries an outer `runId` and can split its identity the same way, and reads the same exported key list the general arm's third leg reads, so the two guards cannot drift apart.
  - **Spec coverage:** Spec-011 §Timeline Entry Types
  - **Verifies invariant:** I-011-1, I-011-3
- **T1.2** — Define `ChildRunSummary` (`runId`, `parentRunId`, `state`, `producingNodeId?`, `eventCount`, `completeness`) in `packages/contracts/src/timeline/`, mirroring [API Payload Contracts](../architecture/contracts/api-payload-contracts.md). `completeness` is a **required** two-arm discriminated union on `state`: `complete` carries nothing else, `incomplete` requires both `cause` — the closed set `detail_fetch_failed` / `compacted`, both members terms the corpus already owns — and `observedAt`. A Zod `discriminatedUnion` with strict arms, so a complete row cannot carry a cause and an incomplete one cannot omit it.
  - **Spec coverage:** Spec-011 §Timeline Entry Types
  - **Verifies invariant:** I-011-10
  - **Tests:** the complete arm round-trips; each of the three causes round-trips on the incomplete arm; an absent `completeness` fails; the incomplete arm without `cause` or without `observedAt` fails; a cause outside the set fails; a `state` outside the two arms selects no arm; the complete arm carrying `cause` or `observedAt` fails strict parse; a non-ISO `observedAt` fails; the exported cause census agrees with the schema's accepted set.
- **T1.3** — Define `ReasoningSurfaceReadRequest` / `ReasoningSurfaceReadResponse` in `packages/contracts/src/timeline/` — the response a closed discriminated union on `availability: 'available' | 'unavailable' | 'compacted' | 'policy_redacted'` mirroring [API Payload Contracts](../architecture/contracts/api-payload-contracts.md): `available` requires the bounded `reasoningEntries`, `policy_redacted` requires `policyReason`, and `unavailable` / `compacted` carry neither — a Zod `discriminatedUnion` with strict arms, so no two states serialize identically and a state-inconsistent field set fails parse. The request takes an optional `afterCursor` and the `available` state splits again on `hasMore` — a nested `discriminatedUnion` inside the outer one — `nextCursor` required where more remains and **at least one entry** required with it, since a page that promises more and delivers none re-offers the same cursor forever. The terminal arm carries neither requirement: an empty final page is how a continuation says it reached the end of a surface that does exist, which `unavailable`, `compacted`, and `policy_redacted` would each misstate. The floor that remains — an empty `available` page on a **first** read, which collapses onto `unavailable` while asserting the opposite — cannot be a schema rule, because only the request separates a first read from a continuation; it is enforced at the daemon binder under I-011-13 (T1.4). The other three states carry no continuation members at all, having no entries to continue, so a paging field on them fails strict parse rather than reading as an empty remainder. The request is strict, so the `cursor` spelling is refused rather than ignored: the member is `afterCursor` on all three reads, matching `TimelineReadRequest`.
  - **Spec coverage:** Spec-011 §Interfaces And Contracts
  - **Verifies invariant:** I-011-7, I-011-11
  - **Tests:** each of the four states round-trips; `available` without `reasoningEntries` fails; `policy_redacted` without `policyReason` fails; entries or a policy reason on `unavailable` or `compacted` fail strict parse; the prior `available: boolean` shape fails parse (no tolerant fallback arm); the `available` state without `hasMore` fails; `hasMore: true` without `nextCursor` fails while `hasMore: false` parses with and without one; `hasMore: true` with an empty entry list fails and the same page with one entry parses; `hasMore: false` with an empty entry list parses; a continuation member on any non-`available` state fails; a request spelling the cursor `cursor` fails.
- **T1.4** — Register the four timeline method strings — `timeline.read`, `timeline.subscribe`, `timeline.reasoningSurfaceRead`, `timeline.childRunExpand` — in the [Timeline Method-Name Registry](../architecture/contracts/api-payload-contracts.md#timeline-method-name-registry) and against the daemon IPC `MethodRegistry`, so every operation's method string resolves and not merely its schema name. Both binders additionally enforce **request scope** on the way out (I-011-13): the query binder wraps its handler so a `TimelineRead` page of another session's rows and a `ChildRunExpand` reply naming another run are refused before the registry sees the value, reusing the substrate's own `invalid_result` result-validation code so the refusal lands as `-32603` with the offending coordinates in `data.fields.issues` and mints no error code; the subscription binder gates each emission on the subscribe request's `sessionId` **below** the ack barrier, so a cross-session row takes the identical cancel-log-stop posture a schema-invalid emission takes and never reaches the wire. The same binder carries two checks that are about the request rather than the reply's subject. A page exceeding the window the caller asked for — the request's own `limit` where it named one, the default row ceiling where it did not, and that same default for the expansion, whose request declares no limit — is refused, which no response schema can do, since the caller's number is on the request. And: a `ReasoningSurfaceRead` answering `available` with no entries is refused when the request carried no `afterCursor`, and admitted when it did, which is the half of that floor no response schema can state. Every check tests the shape it reads — down to each element it dereferences, not merely the container — and defers to the response schema when that shape is absent, so a malformed result is reported once, by the registry, with its real reason and its issue paths, rather than escaping as an untyped exception from the check that was reading it. The subscription binder and its registration types are exported from the daemon's handler barrel beside the query binder, so the bootstrap surface can register all four methods without reaching past it.
  - **Spec coverage:** Spec-011 §Interfaces And Contracts
  - **Verifies invariant:** I-011-13
- **T1.5** — Define the shared paging contract in `packages/contracts/src/timeline/` and bind the subscription producer. Three parts. **(a) Continuation**: `TimelineReadResponse`, `ReasoningSurfaceReadResponse`'s `available` state, and `ChildRunExpandResponse` each become a two-arm union on `hasMore` with `nextCursor` required on the continuing arm and optional on the terminal one, their requests each taking `afterCursor`. All three continuing arms additionally require a **non-empty** entry list: a page promising more and delivering none advances no cursor while reading like progress, so the client re-asks and loops. The terminal arms keep no such floor, an empty final page being the honest answer to an exhausted continuation and to a filtered read that matched nothing. **(b) The page bound**: read the substrate's declared message-size limit from `packages/contracts/src/jsonrpc.ts` (CP-006-16), subtract a reserve covering the JSON-RPC envelope, the echoed request `id`, and the widest non-entry member set any of the three replies can carry — the derivation recorded in a comment beside the constants — and refuse at parse any reply whose encoded size exceeds the remainder, **measured** rather than estimated. Estimation cannot deliver the guarantee and the reasons are arithmetic: `TimelineRow` mirrors the canonical envelope's unbounded `payload` record, so no summary-length proxy bounds a row at all; and the worst case is **six** bytes per string unit, not four, because the free-form string rule admits control characters and `JSON.stringify` escapes each to six ASCII bytes. Ship the page-fill helper beside the bound so a producer gets the count the validator accepts by construction rather than guessing at one. That helper **floors at one** for a non-empty candidate list, per I-011-11: the budget bounds aggregation and never a page below a single entry, so a first candidate that alone exceeds the budget is paged as that one entry and the reply is then refused for its size by the bound above — a typed refusal naming the member, on an error frame the substrate can deliver — instead of the helper returning a bare zero whose only representable response is the empty continuing page the schema now refuses. `maxCount` still wins where it is the smaller of the two, so the floor answers how little a page may be and never whether one exists. **(c) Page integrity** (I-011-12): a nondecreasing-sequence refinement over every entry list — the timeline window, the expansion, **and the reasoning page**, which carries the same `sequence` and the same oldest-to-newest rule, so the refinement is parameterized by member name rather than duplicated — an expansion refinement binding each run-scoped entry to the response's own `runId`, and a `ChildRunSummary` refinement refusing `runId === parentRunId`. Finally, bind `timeline.subscribe` through its own registration seam that fixes the producer's value schema from the method descriptor's `emissionSchema`, and type the query registration to exclude the subscribe verb — so a handler cannot emit a non-`TimelineRow` notification while its acknowledgement still validates, and the two registration paths cannot be crossed. That seam hands its projection a **gated** producer: every emission routes through the shared subscribe-init ordering barrier (`packages/runtime-daemon/src/ipc/subscription-ack-barrier.ts`), which this task extracts verbatim from Plan-006's `handlers/session-subscribe.ts` on its second use per CP-006-16, so I-006-10 holds for `timeline.subscribe` by construction and a Phase-2 projection cannot bypass it by holding the handle it was given. The frame budget's own `id` allowance stops being an assumption in the same swap — Plan-006 bounds the request `id` at the boundary (CP-006-16 leg (a)), so the reserve subtracts an enforced rule rather than a hoped-for one.
  - **Spec coverage:** Spec-011 §Interfaces And Contracts
  - **Verifies invariant:** I-011-11, I-011-12
  - **Tests:** a page built to the worst case the bounds admit encodes, as a JSON-RPC result, to fewer bytes than the substrate's limit; the row ceiling alone is shown insufficient (a page inside it is refused by the byte bound); the byte measure agrees exactly with `Buffer.byteLength` across ASCII, two- and three-byte sequences, surrogate pairs, and control characters; the page-fill helper's count is accepted by the validator and one entry more is refused; a single candidate that alone exceeds the budget counts as one rather than zero, and the one-entry page it yields is then refused by the byte bound naming the member; a zero `maxCount` still yields zero; `hasMore: true` with an empty entry list is refused on both the read window and the expansion while the same page with one entry parses, and the terminal arm accepts an empty list on both; out-of-order entries fail on each paged response, the reasoning page included, with the issue path naming the member the caller actually sent; a foreign run's run-scoped entry fails an expansion while a session-scoped entry does not; a self-parenting summary fails on both the summary and the expansion response; the subscription binder rejects a wrong-shape emission before any notification frame is written, then admits a real row on a fresh subscription; rows emitted synchronously DURING projection setup reach the transport only after the ack and in the order they were produced; a subscribe request without transport identity is refused; and a projection that throws leaves no subscription registered.

### Phase 2 — Projection And Replay-Aware Subscription

**Precondition:** Phase 1 merged; **Plan-003 Phase 3B merged** — T3.14 provides the `supersededTurns(runId)` read seam CP-003-13 obliges this phase to consume; **Plan-006 Phase 2D merged** — the batched subscription frame this phase's replay-aware subscription rides ([cross-plan-dependencies §Dispatch groups](../architecture/cross-plan-dependencies.md#dispatch-groups)). Implementation Step 2.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: plan_phase, plan: 011, phase: 1, status: merged }
  - { type: external_plan_phase_merged, plan: 003, phase: 3B }
  - { type: external_plan_phase_merged, plan: 006, phase: 2D }
```

#### Tasks

- **T2.1** — Implement `packages/runtime-daemon/src/timeline/timeline-projector.ts` building ordered rows from canonical events, stamping each run-scoped row's `runId` + `position` + `epoch` at emission, and preserving provenance to canonical event ids, run ids, runtime nodes, and policy-redaction reasons.
  - **Spec coverage:** Spec-011 §State And Data Implications
  - **Verifies invariant:** I-011-1
- **T2.2** — Consume Plan-003 T3.14's `supersededTurns(runId)` (CP-003-13) to compute each row's `superseded` marker on read and replay windows and to mark live appends at emission — a late pre-rollback straggler appending pre-marked exactly when its stamped attribution ranks above the run's effective lineage-minimum cutoff for its epoch, current when it ranks into surviving history, and a new-epoch row appending unmarked. Marker survives projection rebuild.
  - **Spec coverage:** Spec-011 §Required Behavior
  - **Verifies invariant:** I-011-2, I-011-3, I-011-4
- **T2.3** — Emit the typed `run.rolled_back` boundary entry with its rewind cutoff, payload validated into the `RunRolledBackEvent` shape at projection, delivered visibility-resolved to every filtered subscription admitting any of the affected run's rows.
  - **Spec coverage:** Spec-011 §Timeline Entry Types
  - **Verifies invariant:** I-011-5, I-011-6
- **T2.4** — Implement replay-window reads and live subscription delivery over the identical row schema so reconnect recovery needs no projection translation; compose the marker with audit-stub rows over compacted regions, an attributed stub populating the run arm from its stub-preserved keys and a vacuous-era stub taking the `legacy_stub` arm.
  - **Spec coverage:** Spec-011 §Fallback Behavior
  - **Verifies invariant:** I-011-1, I-011-3, I-011-11, I-011-12

### Phase 3 — Child-Run Expansion And Reasoning Surfaces

**Precondition:** Phase 1 merged. Implementation Step 3; runs parallel to Phase 2 per §Parallelization Notes.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: plan_phase, plan: 011, phase: 1, status: merged }
```

#### Tasks

- **T3.1** — Implement `packages/runtime-daemon/src/timeline/child-run-summary-service.ts` producing summary rows and expansion from Plan-014's durable child-run events and carrier (D-014-14). Stamps each summary's `completeness`: `complete` where the child's rows were read in full, and the `incomplete` arm with a `cause` and `observedAt` where they were not — `detail_fetch_failed` when this daemon's own expansion read fails, `compacted` where they were compacted away — keeping `eventCount` the count actually held rather than an estimate, and never dropping the row.
  - **Spec coverage:** Spec-011 §Required Behavior
  - **Verifies invariant:** I-011-10, I-011-11, I-011-12
  - **Tests:** a failed expansion read yields `incomplete` / `detail_fetch_failed` with the row still returned; a compacted child yields `incomplete` / `compacted`; a full read yields `complete` and carries no cause; `eventCount` on an incomplete row equals the number of rows held, never a total inferred from the parent.
- **T3.2** — Implement `packages/runtime-daemon/src/timeline/reasoning-surface-service.ts` — summary-first, policy-aware reads emitting T1.3's closed `availability` states (`available` / `unavailable` / `compacted` / `policy_redacted`, each with its mandated field set), storing durable summaries and policy markers separately from bounded detailed-reasoning diagnostic payloads. Evaluates the caller from the **dispatch context** — Plan-006's `HandlerContext` principal (CP-006-17), never a request member, since the request carries none — and refuses when that principal is absent rather than serving the read unauthenticated. **Held** by the "Plan-006 Phase 2B merged" §Preconditions box until that carrier lands.
  - **Consumes:** Plan-006 T-006p-2B-1's `HandlerContext` principal (CP-006-17, I-006-21).
  - **Spec coverage:** Spec-011 §Required Behavior
  - **Verifies invariant:** I-011-7, I-011-8, I-011-11
- **T3.3** — Project the `handoff` timeline entry type and intervention-visibility rows into the timeline surface, with tests proving each renders from canonical events.
  - **Spec coverage:** Spec-011 §Timeline Entry Types
  - **Verifies invariant:** none
- **T3.4** — Implement `packages/client-sdk/src/timelineClient.ts` exposing the four operations, parsing rows into the narrowed `TimelineRow` arm — a run-scoped row missing any attribution field failing its `kind`-selected Zod arm rather than falling through to the general or `legacy_stub` arm.
  - **Spec coverage:** Spec-011 §Interfaces And Contracts
  - **Verifies invariant:** I-011-1, I-011-5

### Phase 4 — Desktop Timeline Rendering

**Precondition:** Phase 2 and Phase 3 merged — §Parallelization Notes holds renderer work until replay-catch-up semantics and unavailable-reason payloads stabilize. Implementation Step 4.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: plan_phase, plan: 011, phase: 2, status: merged }
  - { type: plan_phase, plan: 011, phase: 3, status: merged }
```

#### Tasks

- **T4.1** — Render live rows, replay-recovered rows, and summarized child runs in `apps/desktop/src/renderer/src/timeline/`, narrowing structurally on `row.kind` and never probing the free-form `type`. Renders an incomplete child-run summary with its state visible and its cause named — never as a bare row whose low count reads as completed work, and never by hiding the row.
  - **Spec coverage:** Spec-011 §Required Behavior
  - **Verifies invariant:** I-011-1, I-011-10
  - **Tests:** an `incomplete` summary renders the row plus an explicit incomplete affordance naming its cause; the same row with `complete` renders neither; a summary whose fetch failed is present in the rendered output rather than omitted; the rendered count is not presented as a total on the incomplete arm.
- **T4.2** — Render the distinct superseded treatment for rolled-back turns — applied live via the boundary entry's idempotent already-delivered-rows rule and from the row marker on read/replay, never dropping rewound history and never marking a re-executed reused ordinal — with a compacted superseded row rendering as Plan-005's `<CompactedStubSegment>` (CP-005-8, consumed never re-implemented) composed with the superseded treatment. **Also exposes the user `user.message` row's footer composition point and mounts Plan-003's edit-and-resend entry component there** (CP-003-15): this task supplies the mount and the row facts the component's predicate reads — run id, target position, the row's authorship, and the superseded marker computed at T2.2 — and renders whatever visibility state the component resolves. It **authors no part of that component and no eligibility logic**: the predicate is Plan-003's (its T4.8, verifying I-003-24), exactly as T4.4 mounts at Plan-021's composer-area point without editing Plan-021's shell subtree. Rows an in-flight composite would rewind take the same dim-not-remove treatment this task already implements for superseded rows — dimmed while pending, never removed or renumbered before the `run.rolled_back` boundary arrives (I-011-2), so the pending state reuses the marked-not-dropped machinery rather than introducing a second one.
  - **Files:** `apps/desktop/src/renderer/src/timeline/` (row rendering + the row-footer composition point). The mounted entry component lives in Plan-003's `run-controls/` subtree and is not edited here.
  - **Provides:** the row-footer composition point CP-003-15 names, and the row facts passed at the mount.
  - **Dispatch note (mount leg only):** the composition point may be authored at any time, but the **mount** cannot land before Plan-003 Phase 4 has merged — that phase is the mounted component's home (its T4.8). The hold is task-scoped and reviewer-checkable, deliberately **not** a phase-level `external_plan_phase_merged` entry, which would hold T4.1 / T4.3 / T4.4 as well, none of which touches the entry point (the same shape T4.5's carrier hold takes). Until the component exists the footer simply renders nothing, which is also its dormant state afterwards — fail-safe in both orders.
  - **Spec coverage:** Spec-011 §Fallback Behavior; Spec-021 §Signature Feature Composition Sketches (Timeline View — the edit-and-resend placement bullet)
  - **Verifies invariant:** I-011-2, I-011-4
  - **Tests:** the mount renders the component with the row's own facts and re-renders it when the superseded marker changes; the timeline row supplies no eligibility decision of its own (a row whose component resolves to hidden renders the footer with no affordance, never a locally-computed substitute); and rows a pending composite would rewind stay present at their original ordinals while dimmed, taking the superseded treatment only once the boundary appends.
- **T4.3** — Render a visible reasoning placeholder for every non-`available` `availability` state (`unavailable` / `compacted` / `policy_redacted`) in `apps/desktop/src/renderer/src/reasoning-surfaces/`, and the `handoff` and intervention entries from T3.3.
  - **Spec coverage:** Spec-011 §Acceptance Criteria
  - **Verifies invariant:** I-011-7
- **T4.4** — Implement the always-visible context-window meter in `apps/desktop/src/renderer/src/usage-meters/` — a thin projection over the `window.desktopBridge` bridge, mounted at the renderer shell's composer-area composition point (the shell mounts, never authors, feature views; no edit inside Plan-021's shell subtree), consuming session-attributed `usage.context_window_update` rows off Plan-005's `event.subscribe` read path. Latest-wins over the delivered stream: derive `usagePercent` / `tokenCount` / `maxTokens` from the `windowUsedTokens` / `windowMaxTokens` pair (consume, never re-derive — the composer meter is [Spec-014 §Budget Policies](../specs/014-multi-agent-channels-and-orchestration.md#budget-policies)' independent human-facing consumer of the same telemetry, distinct from Plan-014's budget projection), render the informational compaction hint when `usagePercent` exceeds 80% (never auto-triggering compaction), render an explicit indeterminate state on a counts-absent update (never a fabricated denominator), and treat `usage.context_compacted` as invalidating the last reading — `postCompactionTokens` when present, indeterminate otherwise.
  - **Files:** `apps/desktop/src/renderer/src/usage-meters/ContextWindowMeter.tsx` plus co-located tests.
  - **Provides:** the [Spec-011 §Context Window and Usage Meters](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#context-window-and-usage-meters) composer meter — always rendered, above-80% hint, explicit indeterminate state.
  - **Consumes:** `usage.context_window_update` / `usage.context_compacted` ([Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry); Plan-004 driver emitters) over Plan-005's `event.subscribe` via the preload bridge — the Plan-004 edge the header Dependencies row declares.
  - **Spec coverage:** Spec-011 §Context Window and Usage Meters
  - **Verifies invariant:** none
  - **Tests:** renders in the composer area with zero usage rows (indeterminate, never absent); derives the percent from a counted pair; the hint appears above 80% and not at or below it, and issues no compaction call; a counts-absent update renders indeterminate; `usage.context_compacted` resets the reading to the post-compaction count when present, else indeterminate.
- **T4.5** — Implement the rate-limit indicator in `apps/desktop/src/renderer/src/usage-meters/`, rendering [Spec-011 §Rate-Limit Display](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#rate-limit-display)'s threshold coloring, reset countdown, and visibility rule (shown below 50% remaining, hidden when healthy) over the Plan-004-emitted `usage.rate_limit_update` ([Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry): `{provider, providerAccountId, credentialGeneration, limitId?, windowMins, usedPercent, resetsAt?}`), from which coloring and visibility derive off `usedPercent`, with the countdown rendered only when the reset time is present (an explicit no-countdown arm otherwise) and the surfaced quota labeled as the provider account's — the event is account-scoped with no owning session, never presented as a per-session figure. Three further legs (CP-026-9). (a) A **second Source**: the daemon's per-account quota-window registry carried on the provider-account read and its subscription with `source: 'probe'`, merged with the event by **newest `observedAt` per `(providerAccountId, limitId)`**, `source` breaking only exact ties — never by preferring a source, which would let a stale reading mask real consumption. A registry reading also carries the generation it was observed under; where that generation is behind the account's current `credentialGeneration` the indicator renders it **stale rather than current** and never drops it, because a credential-home rebuild clears the home but not the provider-side window the reading describes. (b) The indicator is **per `(account, limit)` pair**, not per account: an account standing against several limits renders several indicators independently, each labeled with the provider's own limit label where it publishes one and with the identifier verbatim where it does not, and the drop-a-lower-reading rule requires the same `limitId` as well as the same `resetsAt`. (c) **Absence renders as absence, never as health**, and carries three possible causes — healthy, never observed, or a home that is not plan-limited at all; where the daemon reports which applies the surface says so, and where it does not the surface says nothing rather than guessing. **Tests:** three readings across three limits sharing one window length render three indicators; an older registry reading does not displace a newer event reading for the same key, and the reverse; an equal-timestamp pair resolves by source deterministically; a behind-generation registry reading renders with the stale marking and is not dropped. Absence of observed data renders as the hidden state and never as an affirmative healthy claim — fail-safe by construction.
  - **Files:** `apps/desktop/src/renderer/src/usage-meters/RateLimitIndicator.tsx` plus co-located tests.
  - **Provides:** the [Spec-011 §Rate-Limit Display](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#rate-limit-display) indicator — threshold colors, reset countdown with an explicit unknown arm, below-50% visibility, one indicator per `(account, limit)` pair, the behind-generation stale marking, and the three-cause absence rendering (observer silence is not a cause, because the observer sources no quota reading at all — attributing an absence to it would hand the operator a remedy that cannot change the outcome).
  - **Consumes:** both sanctioned Sources the section names — the `usage.rate_limit_update` event ([Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)) and the per-account quota-window registry reading carried on the provider-account read ([Spec-026 §Per-limit provider quota](../specs/026-provider-accounts-and-credential-homes.md#per-limit-provider-quota)), whose entries carry the provenance they were **observed** under — so a stored window legitimately arrives with `source: 'run'` as well as `source: 'probe'`, and the renderer accepts **both** rather than assuming the transport determines the provenance — merged by newest `observedAt` per `(providerAccountId, limitId)`; consume, never re-derive.
  - **Spec coverage:** Spec-011 §Rate-Limit Display
  - **Verifies invariant:** none
  - **Tests:** hidden with no observed data and at or above 50% remaining; visible below 50%; yellow at 20–50% and red below 20% (the green band coincides with the hidden-when-healthy range by the spec's own visibility rule); the countdown renders toward the reset time when present and is omitted — indicator still rendering — when absent; the quota label names the provider-account scope.

- **T4.9** — Render both provider-switch status rows in `apps/desktop/src/renderer/src/timeline/` — a single line each, never a message row and never a block.
  - **Files:** `apps/desktop/src/renderer/src/timeline/ProviderSwitchRow.tsx` plus co-located tests.
  - **Provides:** the [Spec-011 §Timeline Entry Types](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#timeline-entry-types) rows for **both** provider-switch terminals. `agent.provider_switched`: agent name, the binding moved from and to, and — only when `declaredLosses` is non-empty — the conversation-did-not-arrive-whole clause with the declared losses named. All four continuity arms render the same single line; `'in_place'` and `'resumed'` carry no loss clause and **no caution treatment**, because nothing was lost, and which of the four arms produced the row is not surfaced. `agent.provider_switch_failed`: agent name, the binding the agent **stayed on**, and the closed reason in plain words — the one arm of the pair that is a caution, and the one that must never read as a switch that happened.
  - **Consumes:** both provider-switch event payloads, **verbatim**; it derives nothing, never infers loss from the presence of a switch, and never renders a failed application as a completed one.
  - **Spec coverage:** Spec-011 §Timeline Entry Types (the `agent.provider_switched` and `agent.provider_switch_failed` rows)
  - **Verifies invariant:** none
  - **Tests:** a `continuity: 'resumed'` event with an empty `declaredLosses` renders no loss clause and no warning styling — the assertion that prevents the row from being decorated into a false alarm; an `'in_place'` event renders identically to it, so the mechanism split never leaks into the user's view; a `'brief'` event names each declared loss, and a `'replayed'` event names the restart its own arm is required to declare; an unrecognized loss kind renders the kind rather than being dropped, so a value added to the set later is never silently invisible; a failed event renders the binding it stayed on and its reason and **never** the `to*` phrasing of a completed switch — asserted on the rendered text, since the whole risk of the pair is the two arms looking alike.

- **T4.10** — Render the input-kind driver-ask card in `apps/desktop/src/renderer/src/timeline/`.
  - **Files:** `apps/desktop/src/renderer/src/timeline/DriverAskCard.tsx` plus co-located tests.
  - **Provides:** the [Spec-011 §Timeline Entry Types](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#timeline-entry-types) `driver_ask` entry — the ask's `prompt`, its structured `options` when the payload carries them, and an **unconditional** free-text answer arm present on every card including cards that carry options; no countdown of any kind, the ask having no deadline to count toward; and the `responded` / `canceled` states rendered from their own `driver_ask.*` rows. Filtered to `kind: 'input'` — a `kind: 'permission'` ask is **not** rendered here and its approvals routing is untouched. Answers dispatch through `driver.respondToRequest` (CP-004-12).
  - **Consumes:** the three `driver_ask.*` event types ([Spec-005 §Driver Ask Events](../specs/005-session-event-taxonomy-and-audit-log.md#driver-ask-events)) over Plan-005's `event.subscribe` via the preload bridge; `driver.respondToRequest` (CP-004-12). Authors no wire method and no event type.
  - **Spec coverage:** Spec-011 §Timeline Entry Types (the `driver_ask` entry)
  - **Verifies invariant:** I-011-9
  - **Tests:** an ask carrying no `options` still renders an answerable free-text arm — the assertion that keeps the card from being unanswerable on a provider that declares no choice set; an ask carrying `options` renders both arms and both dispatch the same ingress; a `kind: 'permission'` ask renders **no** card here (asserted on absence, since the failure mode is a duplicate surface rather than a missing one); a card left unanswered stays answerable and shows no terminal state and no elapsed-time treatment however long it stands; an unrecognized state value renders as itself rather than being dropped.
- **T4.11** — Render the user-triggered compaction affordance in `apps/desktop/src/renderer/src/usage-meters/`.
  - **Files:** `apps/desktop/src/renderer/src/usage-meters/CompactionControl.tsx` plus co-located tests.
  - **Provides:** the [Spec-011 §Context Window and Usage Meters](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md#context-window-and-usage-meters) composer-area control — offered at every usage level rather than unlocking at the 80% hint, **absent** (not disabled) where the bound driver declares `context_compaction: false`, dispatching `driver.compactContext` for the run whose meters it sits beside, and rendering its completed state **only** on the `usage.context_compacted` row. EXTENDs T4.4's meter surface without changing the hint T4.4 already renders: the hint stays informational and neither task triggers a compaction on a threshold.
  - **Consumes:** `driver.compactContext` and the `context_compaction` declaration through `driver.listCapabilities` (CP-004-12); `usage.context_compacted` and `usage.context_window_update` over `event.subscribe`, the same carriers T4.4 already consumes.
  - **Spec coverage:** Spec-011 §Context Window and Usage Meters
  - **Verifies invariant:** I-011-9
  - **Tests:** the control is absent — asserted on absence, not on a disabled attribute — when the capability read reports `context_compaction: false`; it is present at 5% usage as well as at 95%, so the offer is not threshold-gated; an accepted dispatch that is followed by **no** `usage.context_compacted` row never renders a completed state, which is the assertion that stops an empty provider ack from being read as a compaction; T4.4's above-80% hint still issues no compaction call with this control mounted.

## Parallelization Notes

- Projection work and reasoning-surface normalization can proceed in parallel once row schemas are fixed.
- Renderer work should wait for replay-catch-up semantics and unavailable-reason payloads to stabilize.
- The composer meters (T4.4 / T4.5) have no dependency on the timeline row schemas — they ride Phase 4 for renderer placement, not for data.

## Test And Verification Plan

- Projection tests covering ordered messages, run-state changes, tool activity, approvals, artifacts, and child-run summaries
- Replay-gap tests proving clients can recover missing rows without rebuilding from free-form text
- Policy-redaction tests proving unavailable reasoning still produces visible explanation surfaces
- Retention tests proving detailed reasoning expiry or compaction does not erase durable summary and policy surfaces
- Superseded-rendering tests proving live marking keys on the exposed `runId` + `position` pair (already-delivered rows of the rewound run with `position` above the boundary's cutoff take the treatment — never a `sequence` comparison; an above-effective-cutoff late straggler delivered after the boundary arrives pre-marked while a straggler ranking into the surviving history arrives current — the T3.14 late-delivered-sibling case; new-epoch rows never mark), read/replay marker parity with the live outcome (a bounded-window subscriber holding current cached rows across earlier rollbacks marks them on a later boundary identically to replay — the single-field marker composes with the row's own `runId` + `epoch` by construction; a channel-filtered subscriber holding the affected run's rows receives the boundary and marks them — the filter never suppresses a rollback cutoff; every `run.rolled_back` boundary payload validates into the typed shape — a subscriber never receives an untyped cutoff; a run-scoped row missing any attribution field fails its `kind`-selected Zod arm — the malformed-row test, never a fall-through to the general or `legacy_stub` arm; a vacuous-attribution-era stub parses on the `legacy_stub` arm and renders the compaction placeholder without attribution — never invented, never dropped, never a parse failure; a boundary whose outer attribution disagrees with its payload — `runId`, `sessionId`, or `position` vs `targetPosition` — fails parse), reused-ordinal correctness across the two-rollback sequence Plan-003 T3.14 pins (10→5, re-execute, 7→6 — the second epoch's re-executed turn 6 renders current while the first epoch's turn 6 stays superseded, the epoch-scoping [Spec-003 §Required Behavior](../specs/003-queue-steer-pause-resume.md#required-behavior) owns), the lineage sequence T3.14 pins (10→5, re-execute to 7, 7→3 — the inherited epoch-0 turn 4 renders superseded via the second rollback's cutoff), and the compacted-stub composition (stub placeholder + superseded marker)
- Composer-meter tests proving the context-window meter renders in every usage state — counted pair, counts-absent indeterminate, post-compaction reset — with the above-80% hint informational only, and the rate-limit indicator's visibility, threshold-color, and countdown rules over observed carrier values, including the no-reset-time no-countdown arm and the provider-account scope label

## Rollout Order

1. Land row schemas and replay-backed projection reads
2. Enable live subscribe plus replay recovery
3. Enable reasoning surfaces and child-run expansion in the primary session experience
4. Enable the composer usage meters — the context-window meter and the rate-limit indicator, both with Phase 4

## Rollback Or Fallback

- Collapse to summarized timeline rows and disable detailed reasoning expansion if payload shape or policy gating regresses.

## Risks And Blockers

- Per-session verbose reasoning opt-in remains unresolved (deferral tracked in parent [Spec-011](../specs/011-live-timeline-visibility-and-reasoning-surfaces.md))
- Timeline projections will drift if row schemas are allowed to diverge from canonical event provenance
- Detailed reasoning payloads can be mistaken for canonical history unless summary-first storage stays explicit across contracts and UI

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated
