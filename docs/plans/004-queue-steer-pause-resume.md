# Plan-004: Queue Steer Pause Resume

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `004` |
| **Slug** | `queue-steer-pause-resume` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-004: Queue Steer Pause Resume](../specs/004-queue-steer-pause-resume.md) |
| **Required ADRs** | [ADR-003](../decisions/003-daemon-backed-queue-and-interventions.md), [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-011](../decisions/011-generic-intervention-dispatch.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md) |
| **Dependencies** | [Plan-001](./001-shared-session-core.md) (session core + `session/` run-engine dir + Phase 5 renderer bootstrap), [Plan-005](./005-provider-driver-contract-and-capabilities.md) (driver capability checks + `DriverInterventionResult`), [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (T1.9's `sourceEpoch`/`sourcePosition` epoch-attribution carrier per CP-004-12, and T3.1's `session-append-lock.ts` dependency leaf whose `withSessionAppendLock` T3.7's terminal emitter wraps guard-swap-append in — Phase-3 gated per this plan's Phase-3 `plan_phase` entry; F-006-HALT-06, PR #282, 2026-08-02), [Plan-007](./007-local-ipc-and-daemon-control.md) (`api-payload-contracts.md` method-name registry), [Plan-023 Tier 1 Partial](./023-desktop-shell-and-renderer.md#tier-1-partial-pr-sequence) (`window.sidekicks` preload bridge + renderer substrate — the Tier-1-shipped substrate slice, not the Tier-8 remainder; this plan's `run-controls/` renderer subtree consumes only the bridge, so the substrate is the sole dependency and no Tier-5 → Tier-8 back-edge is created) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |
| **References** | [Updated Spec-004](../specs/004-queue-steer-pause-resume.md) (6 intervention states, pause as orchestration-layer), [Run State Machine](../domain/run-state-machine.md) (9 states) |

## Goal

Implement daemon-backed queue state and authoritative intervention handling for active runs.

## Scope

This plan covers queue persistence, intervention records, run-state transitions, and client control surfaces for queue, steer, pause, resume, and interrupt.

## Non-Goals

- Workflow scheduling
- Provider-specific pause emulation beyond declared capabilities
- Notification routing

## Preconditions

- [x] Paired spec is approved — **re-opened 2026-08-03, scoped to the cross-user run-control authorization amendment** (supersedes the 2026-07-18 campaign Task-28 / W1.5 re-check): Spec-004 §Interfaces And Contracts gained normative Cedar text — the `Action::"intervene"` action on the target `Run` resource, the hosting-node context input, and the authorize-on-session-role-never-run-authorship rule — so the spec flipped `approved → review` per the audit runbook's spec-amendment rule, and this plan flips with it under the runbook's plan behavior-change row, carrying the new **I-004-19**. A targeted readiness-audit delta audits the amendment, re-checks this box, and restores `approved` — that delta PR is the delivery-and-promotion vehicle (the Plan-006 PR #282 flip-and-restore-in-one-swap shape). The gate this re-opens is **plan-wide, not phase-scoped**: preflight Gate 7 checks the plan-template governance trio together with the header Status row, so no lane-1 code PR dispatches for **any** phase — Phase 3 included — until the delta lands; the three named scoped boxes below that this 2026-08-03 re-open concerns (principal binding, hosting-node carrier, dispatch capability) carry Phase 3's Gate-5 `precondition_box_checked` entries and are untouched by this amendment. **Delivered 2026-08-08:** the cross-user run-control authorization targeted readiness-audit delta (PR #299; §6 node NS-49) ran the re-open's named scope — the Spec-004 §Interfaces And Contracts Cedar shape, I-004-19's T2.5 / T3.4 / T3.5 reach, and the owed local + remote acceptance arms — plus an adversarial cross-surface coherence pass: one minor finding (the api-payload §Authenticated Principal registration gap, amended in the same swap) beside the scheduled acceptance-arm delivery; zero critical / zero major; the consumer-readiness and structure re-verifications are recorded in the dated Notes bullet. Status restored `review → approved` on this plan and Spec-004 (closing PR #299, the promotion vehicle this row named). The restoration clears the audit and status gates only: lane-1 dispatch of I-004-19's carriers (T2.5 / T3.4 / T3.5) additionally rides [AGENTS.md §Doc-First Discipline](../../AGENTS.md#doc-first-discipline) on the cross-referenced [Spec-012](../specs/012-approvals-permissions-and-trust-boundaries.md) — `review` at that time, pending its own separate restoring delta — a named hold this box's re-check did not and could not clear (Codex round 1 on the closing PR), **discharged 2026-08-10** when that delta landed (PR #317; §6 node NS-54), restoring Spec-012 and Plan-012 `approved`; Codex round 2 additionally registered the three unchecked sibling prerequisites below (principal binding, hosting-node carrier, dispatch capability), each holding T2.5 / T3.4 / T3.5 dispatch until its lead-owned amendment lands. **Re-opened 2026-08-16, scoped to the rewind-hardening amendment** (compaction-boundary-aware rewind targeting + atomic edit-and-resend, both legs user-ratified 2026-08-16): Spec-004 gained normative §Required Behavior, §Interfaces And Contracts, §Driver-Level Rollback Mechanics, §Pitfalls To Avoid, and §Acceptance Criteria text, plus a §Resolved Questions and V1 Scope Decisions restatement that the V1 control set is **unwidened** by either leg, so it flipped `approved → review` per the audit runbook's spec-amendment rule and this plan flipped with it under the runbook's plan behavior-change row, carrying the new **I-004-20** and **I-004-21** plus tasks **T3.16** and **T3.17**. This re-open is again **plan-wide, not phase-scoped** (preflight Gate 7 reads the governance trio together with the header Status row), and it leaves the scoped boxes below untouched. **Delivered 2026-08-17, in the same diff** (the PR #278 flip-and-restore-in-one-swap shape this row already cites): the rewind-hardening targeted readiness-audit delta — recorded as this amendment's §6 audit-delta node and narrated in the dated §Notes bullet — audited the re-open's named scope (the compaction-boundary classification with its three-valued fail-closed disposition, the edit-and-resend composite's run-bound admit-not-dispatch mapping and four structural refusal guards (the fourth joining at the PR's round-2 fold), I-004-20 / I-004-21's T3.16 / T3.17 / T3.13 / T3.5 reach, the set-quantifier sweep across every enumeration the two legs touch, the T4.7 client-reachability leg that makes the composite invocable, the invariant-matrix symmetry pass in both directions — which corrected I-004-10's Verified-by column to include T4.7, a pre-existing asymmetry of the W2.5 disposition-(7) class — and a seeded-refutation negative control), re-checked this box, and restored `approved` on this plan and Spec-004. The restoration clears the audit and Status gates only: T3.16 / T3.17 / T4.7 dispatch additionally rides the new unchecked replacement-send wire-arm box below, and T3.17's principal-stamp leg the round-2 fold's unchecked admitting-principal producer box beside it. **Re-opened 2026-08-17 (Codex PR #346 round 2), scoped to the vanished-checkout rootless convergence:** Spec-004's rollback bullet, guard (iv), and the composite acceptance criterion now name the live context whose recorded checkout root is gone — released into the rootless class at the confirmed re-open, no third conversation-only class and no new error code. **Delivered in the same diff** (Spec-004 `approved → review → approved` in-swap). **Re-opened 2026-08-18, scoped to the admitting-principal carrier and queue-PII envelope amendment (§6 node NS-71):** Spec-004 §Required Behavior gained two normative bullets — every accepted intervention durably records the daemon-resolved principal that admitted it, and participant-authored queue-item bodies rest encrypted exactly as intervention bodies do — with the paired §State And Data Implications entries, so Spec-004 flipped `approved → review` per the audit runbook's spec-amendment rule and this plan flipped with it under the runbook's plan behavior-change row, carrying the new **I-004-22** and **I-004-23** and design decision **D-004-4**. **Delivered in the same diff** (Spec-004 `approved → review → approved` in-swap, its fourth flip cycle): the in-swap targeted readiness-audit delta recorded at the audit box below ran the re-open's named scope and restored both. No table is added — two `interventions` columns (`origin`, `admitting_principal_id`) with one table-level CHECK, and three `queue_items` columns (`pii_payload`, `pii_participant_id`, `admitting_intervention_id`), all joining unshipped `CREATE`s on this plan's own T1.4 — so the SQLite census is unmoved at 55. **Re-opened 2026-08-25, scoped to the replacement-send wire-arm registration's three narrowings (§6 node NS-75):** registering the 2026-08-16 amendment's wire arm in `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` found three places where Spec-004's prose under-determined the shape it named, and each is narrowed in that spec rather than settled in the registry alone — `resendDisposition` REQUIRED on the composite-only `resend-unapplied` arm (the counterexample to §Required Behavior's schema-optional-on-every-result rule, whose stated premise that very disposition falsifies), that arm's two displaced never-silent restore enumerations, and the nullable `boundary-diverged` settlement comparand — so Spec-004 flipped `approved → review` per the audit runbook's spec-amendment rule and this plan flipped with it under the runbook's plan behavior-change row, T1.3's mirrored statement of the same arm moving with it. **Delivered in the same diff** (Spec-004 `approved → review → approved` in-swap, its fifth flip cycle): the in-swap targeted readiness-audit delta recorded at the audit box below ran the re-open's named scope and restored both. Nothing is minted — no intervention type, wire method, event type, error code, table, or column, and no disposition or terminal state — since all three narrowings tighten shapes both docs already carried, so the SQLite census stays 56 and the V1 control set stays closed. (Erratum, PR #449 round 2: this record read **55**, the value the 2026-08-18 record above correctly carries; the `provider_accounts` mint later that same day took it to 56, which is what `local-sqlite-schema.md` carried at every commit from 2026-08-18 through 2026-08-25 — counted over the tree's own history, not recalled. Nothing else in the record moves: this amendment still minted no table.) **Re-opened and re-checked in one swap 2026-08-26 (turn-scoped effective-principal publication, §6 node NS-84):** Spec-004 flipped `approved → review` for the §Required Behavior publication clause and is restored `approved` by the targeted readiness-audit delta riding this same diff; this plan flips with it and is likewise restored. The delta audits T3.19, I-004-25, and CP-004-16 against the amended spec. No shipped phase re-opens (T3.19 homes in the already-gated Phase 3), no census moves, and **no** born-unchecked box is minted. **Re-opened 2026-08-26**, scoped to the Codex rewind rebinding to `thread/fork` (the binding-lineage V1 instance and T3.5's two withdrawn rotation-keyed assertions); **Delivered in-diff** by the NS-85 targeted readiness-audit delta riding this same PR. **Re-opened and re-checked in one swap 2026-09-06 (typed composite refusal guard, §6 node NS-101):** `Spec-004 §Required Behavior`'s four-structural-refusal-guards paragraph gains the closed-union `rejectionGuard` member the composite's `rejected` response carries, and §Acceptance Criteria's atomic edit-and-resend criterion is extended **in place** to match, so Spec-004 flipped `approved → review` per the audit runbook's spec-amendment rule and this plan flipped with it under the runbook's plan behavior-change row; **Delivered in the same diff** by the typed-composite-guard targeted readiness-audit delta recorded at the audit box below, restoring both. No intervention type, wire method, event type, error code, or table is minted, and **no invariant**: I-004-21 is widened in place. **One column is minted** at the PR #449 round-2 fold — the additive nullable `interventions.rejection_guard`, without which an idempotent replay of a guarded refusal could not return the recorded literal (the wire carries no `result` on `rejected`, and `rejection_reason` holds the open, contract-unenumerated cause vocabulary the member exists to close — round 3 correcting rounds 1–2's "free-form human sentence" reading of it); the table shipped at PR #402, so the column lands by Plan-004's own next-ordinal migration and never by an edit of the shipped statement. Re-derived by counting the files: local SQLite stays **59** `CREATE TABLE` statements (a column is not a table), Postgres **28**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 event taxonomy **159**; the V1 control set stays closed and the criteria set stays ten.
- [x] **Driver input-ask park leg authored (Part-B fail-closed follow-up, 2026-07-17; authored by campaign B9, 2026-07-20):** [Spec-012 §Resolved Questions and V1 Scope Decisions](../specs/012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions) and [run-state-machine.md](../domain/run-state-machine.md) require the run-engine to handle `driver_ask.expired` for `kind: 'input'` asks via the `waiting_for_input → paused` park edge — **authored as T3.15** (reciprocal of Plan-012's B13-authored T2.8 normalizer; the CP-004-9 expired→continue mapping stays approval-pipeline-only). Phase-3 run-engine code does not dispatch while this box is unchecked — enforced mechanically by Phase 3's `preconditions:` block (`precondition_box_checked` entry, preflight Gate 5; Gate 7's governance scan deliberately covers only the plan-template trio, so a scoped box needs the phase-level entry); the B9 bundle PR authored T3.15 and checks this box.
- [x] **Plan-006 epoch-attribution carrier registered (CP-004-12; Codex round 5, 2026-07-20; amendment landed 2026-07-20):** the durable typed epoch-attribution field on the event-append surface — `Spec-004 §Required Behavior` mandates the stamp; T3.11 writes it, T3.14's supersede projection consumes it — registered by the lead-owned 2026-07-20 amendment PR (#231) as [Plan-006](./006-session-event-taxonomy-and-audit-log.md) **T1.9's cross-cutting `sourceEpoch` + `sourcePosition` payload carrier pair**: `SourceEpochSchema` / `SourcePositionSchema` + their payload-key consts and the `withEpochStamp` pairing helper in `packages/contracts/src/event.ts`, with the Spec-006 registrations at §Event Type Enumeration (the cross-cutting-pair paragraph — run-scoped-variant admission) and §Compacted Event Format (audit-stub preservation of the stamped-row attribution triple and the `run.rolled_back` boundary cutoff, so the stamp survives compaction for T3.14). That PR checks this box (the Part-B precedent above — a one-line cross-plan edit riding the amendment). Phase-3 run-engine code still does not dispatch until the paired `{ plan: 006, phase: 1 }` entry resolves: the re-opened Plan-006 Phase 1 — T1.9 included — must have fully shipped (declared-⊆-shipped).
- [x] **Plan-010 partial-restore enumerations registered (Codex re-audit round 4, 2026-07-20; amendment landed 2026-07-20):** the `files-partially-restored` wire arm REQUIREs `overwrittenIgnoredPaths` / `divergentGitlinks` (empty-when-none, covering every effect applied before the failure), but [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) T5.2's `partial_restore` variant named only the failed command — the producer-side enumerations were **not yet in the Plan-010 corpus**, so T3.13 could not map the promised arrays without fabricating empties. The lead-owned 2026-07-20 amendment PR (#230) amended Plan-010 T5.2 (+ the `Spec-010 §Turn-Boundary Snapshots` restore bullet) to carry them on the partial result — pinned name-identical to this plan's wire fields (`overwrittenIgnoredPaths` / `divergentGitlinks` / `failedStep`), so the T3.13 mapping is an identity — and checks this box (the CP-004-12 pattern — cross-plan §2 one-writer kept the amendment out of this plan's PRs). Phase-3B rollback code does not dispatch while this box is unchecked (Phase 3B's `precondition_box_checked` entry).
- [ ] **Local node-owner principal binding registered (Codex PR #299 round 2, 2026-08-08):** the local-socket run-control adjudication (T2.5's local arm; T3.4 / T3.5 identically) requires the daemon's **node-owner participant identity** as its Cedar principal — the daemon's standing control-plane-authentication posture names the identity ([component-architecture-local-daemon.md §Responsibilities](../architecture/component-architecture-local-daemon.md#responsibilities)), but `runtimenode.attach` is client-driven and the daemon is never a party to it, so no attach hook supplies the value: the binding must arrive through the Plan-018-gated credential/identity provider seam (the CP-006-13 constructor-injected credential-provider and `resolveCurrentParticipantId` precedent — [api-payload-contracts.md §Signing-Key Registration Method Registry](../architecture/contracts/api-payload-contracts.md#signing-key-registration-method-registry-tier-4-plan-006-t410)). This box fails until the lead-owned amendment registers the daemon-side node-owner principal provider for the run-control path (naming producer and consumer); T2.5 / T3.4 / T3.5 do not dispatch while it is unchecked (Phase-2 + Phase-3 `precondition_box_checked` entries), and until wired the local arm fails closed — an unresolved principal adjudicates as a deny.
- [ ] **Per-run hosting-node carrier registered (Codex PR #299 round 2, 2026-08-08):** the `Action::"intervene"` request context requires the target run's hosting node resolved daemon-side from the run itself, but no shipped surface durably names it — CP-004-7's `getRun` returns `{ version, sessionId, state }`, `run_execution_contexts` carries no node column, [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md)'s `producingNodeId` is optional orchestration-linkage only, and an agent's mutable `defaultNodeId` would misclassify existing runs after rebinding. This box fails until the lead-owned amendment registers a durable, replay-stable per-run hosting-node carrier (naming its producer at run creation and its authorization-path consumer); T2.5 / T3.4 / T3.5 do not dispatch while it is unchecked (Phase-2 + Phase-3 `precondition_box_checked` entries).
- [ ] **Cross-node run-control dispatch capability registered (Codex PR #299 round 2, 2026-08-08):** the remote arm's reachability requires a declared dispatch capability for run-control — a handler contract (capability name, action/result payload shapes) plus the caller-side dispatch branch and the Plan-004 ↔ Plan-027 reciprocal registration — which no plan currently authors ([Spec-024](../specs/024-cross-node-dispatch-and-approval.md) treats `action_payload` as capability-owned and Plan-027 invokes only already-declared handlers). Absent it the arm stays fail-closed but unreachable: an undeclared capability is rejected at intake before target-owner approval is ever requested (Spec-024's declared-capability rule). This box fails until the lead-owned Plan-004 ↔ Plan-027 amendment registers the capability contract; T2.5 / T3.4 / T3.5 do not dispatch while it is unchecked (Phase-2 + Phase-3 `precondition_box_checked` entries).
- [x] **Rollback replacement-send wire arm registered (2026-08-16 rewind-hardening amendment):** the edit-and-resend composite's optional `replacementSend` member on the `InterventionRequestPayload` `rollback` arm, plus the `resend-unapplied` and `boundary-diverged` members of `RollbackDegradedResult` and the schema-optional, producer-obligated `resendDisposition` member of `RollbackInterventionResult` (the round-2 fold's widenings ride the same registration), must be registered in `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` before T1.2 / T1.3 author their code mirrors — T1.2 mirrors that section **byte-for-byte**, so an unregistered field would leave the mirror unverifiable and the code its own sole definition. The registration is deliberately **not** carried by this amendment: `api-payload-contracts.md` is a high-traffic shared surface under concurrent amendment traffic, and the write-ordering discipline that made CP-004-12's carrier a named merge prerequisite applies identically here. Producer: a follow-up registration PR against that file's existing `### Plan-004 — Queue Steer Pause Resume` section (tracked as [BL-153](../archive/backlog-archive.md), which stands on this registration alone) — the section T1.1 / T1.2 / T1.5 already mirror byte-for-byte, so the registration widens a shape this plan already owns and authors no cross-plan symbol; it adds no method name, so none of that file's method-name registries (Plan-007-owned per §5's Substrate-vs-Namespace carve-out) is touched. Consumers: T1.2 (the payload arm), T1.3 (the `RollbackDegradedResult` widening), T3.17 (the composite), T4.7 (the `--replace-message` option that populates the member), and — since the 2026-08-18 placement amendment — T4.8 (the desktop entry affordance whose confirm action populates the same member). T1.2 / T1.3 / T3.17 / T4.7 do not dispatch while this box is unchecked (Phase-1 + Phase-3B `precondition_box_checked` entries); **T4.8 is held by task-scoped prose in its own body rather than a fifth Gate-5 entry**, because the only entry available to it is Phase-4-level and would hold T4.1–T4.6 — none of which touches the composite (the task-scoped-hold shape Plan-013 T4.5 and Plan-014 Tasks 7–10 already use). Until this box lands the composite is simply unreachable on the wire — the fail-closed direction, since a `rollback` request with no `replacementSend` member is an ordinary bare rollback. **Delivered 2026-08-25:** the registration landed in `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` (§6 node NS-75; [BL-153](../archive/backlog-archive.md) closes `completed` in the same diff) — the `replacementSend?: { content: string }` member on the `rollback` payload arm (request-side only, presence-discriminating, non-empty at parse, no attachment member in V1); the two `RollbackDegradedResult` members (`boundary-diverged` carrying `confirmedPosition` + `newestBoundaryPosition`, the comparand typed `number | null` so the position-less-compaction-row conclusion Spec-004 routes into this same disposition is representable rather than unconstructible; `resend-unapplied` carrying the REQUIRED `resendDisposition: 'unapplied'` its composite-only reachability makes expressible plus `files-restored`'s two enumerations, since it is the one disposition that **displaces** a completed file leg and Spec-010's never-silent mandate must survive the displacement — but no locator for the staged text, which is reachable from the response's own `interventionId`, so the arm mints no second source of truth for a row the response already names); and `resendDisposition` registered as a pair of class-scoped fragments (`RollbackAppliedResendOutcome` / `RollbackDegradedResendOutcome`), each intersected with its own terminal state-split arm, so the round-5 state-scoping (`applied` admits only the applied class, `degraded` only the degraded class) is **preserved rather than widened** while the V1 state-determined value mapping is **encoded rather than asserted** — verified by compiling the section's declarations with seven seeded negative controls — an `applied` response carrying a degraded-class disposition, an `applied` result carrying `'unapplied'`, and a `resend-unapplied` arm dropping the displaced restore enumerations among them — each failing to typecheck. The section's degraded-class count claim moved **five → seven** with the arms (the live normative statement in T1.3 already read seven; the dated §Notes provenance is append-only and unrewritten). **Status: `approved → review → approved` in the same diff on this plan and Spec-004 (§6 node NS-75).** The registration itself is a mirror — content both docs already carry as `approved`, landing in a canonical architecture doc that has no Status lifecycle — and the three otherwise-uncoined identifiers (`replacementSend.content`, `newestBoundaryPosition`, and `resendDisposition`'s placement, which the round-6 fold splits into two class-scoped fragment names, coining no further wire member; the round-7 fold reuses two existing member names and coins none) were user-ratified in advance. What moves both Status rows is what the registration **found**: three places where `Spec-004 §Required Behavior` under-determined the shape it named — the composite-only arm's REQUIRED `resendDisposition` (the counterexample to that section's schema-optional-on-every-result rule), its two displaced never-silent restore enumerations, and the nullable settlement comparand — each narrowed in the spec and mirrored into T1.3 here. That is the runbook's plan behavior-change row rather than its citation / narrowing row, so both docs flip and the restoring targeted readiness-audit delta rides this same diff at the audit box below; no task, invariant, cross-plan obligation, phase, or Required ADR is minted, and the README plan- and spec-status censuses are unmoved because both rows read `approved` at merge exactly as at basis. **Released** — the flip verified against the Gate-5 `precondition_box_checked` resolver's own capture (leading bold markers stripped, the entry prefix matching exactly one box), with three negative controls confirming an unchecked box, a leading-`**` entry spelling, and a drifted prefix each fail closed: the Phase-1 and Phase-3B Gate-5 holds on T1.2 / T1.3 / T3.17 / T4.7, and T4.8's task-scoped hold. **Still held, unchanged by this check:** Phase 1 on its remaining `plan_phase` gate, Phase 3B on its own gate set, and T4.8's dormancy — conjunct (1) is false at Phase-4 merge, so the affordance still ships hidden and its activation still rides the Phase-3B supplement.
- [x] **Admitting-principal carrier registered (2026-08-16 rewind-hardening amendment, round-2 fold — Plan-004 producer leg):** the drained replacement turn executes under the admitting intervention caller's effective principal (`Spec-004 §Required Behavior`'s admits-paragraph; `Spec-012 §Required Behavior`'s rule as extended 2026-08-17 to the run-bound replacement send), which requires the durable, replay-stable admitting-principal carrier that [Plan-012 §Preconditions](./012-approvals-permissions-and-trust-boundaries.md#preconditions)' same-named consumer box holds Plan-012 T2.2 / T2.7 for — the client-supplied `initiator_id` is informational, never a principal source, so no shipped column carries the verified identity. Producer: the same lead-owned Plan-004 ↔ Plan-012 amendment that box names — it registers the carrier's schema column, T3.17's acceptance-time producer stamp, the row-anchored linkage by which T3.5's drain resolves a run-bound item's admitting intervention (a run accumulates interventions, so the resolution is durable on the row, never inferred from run history), the authorization-path consumer, and the api-payload §Authenticated Principal registration, checking both plans' boxes in one landing. T3.17's principal-stamp leg does not dispatch while this box is unchecked (Phase-3B `precondition_box_checked` entry); the composite is separately unreachable on the wire until the replacement-send wire-arm box above lands, so the two holds compose fail-closed rather than widening any surface, and until wired an unresolved effective principal on the drained turn adjudicates as a deny, never as the run initiator (Plan-012's box states the same fail-closed direction). **Delivered 2026-08-18** by the admitting-principal carrier amendment (§6 node NS-71), which registers exactly the five deliverables this box named: the schema column (`interventions.admitting_principal_id`, beside the `origin` discriminator that makes its required-ness checkable in the database rather than by convention — D-004-4), T2.4's acceptance-time producer stamp (universal to every intervention record, so the composite inherits it rather than carrying a private path — T3.17's leg then rides its own existing durable transaction), the row-anchored linkage (`queue_items.admitting_intervention_id`, by which T3.5's drain resolves a run-bound item's admitting intervention off the row instead of inferring it from run history), the authorization-path consumer (Plan-012 T2.2 / T2.7 via CP-004-14 ⇄ CP-012-12), and the `api-payload-contracts.md §Authenticated Principal And Authorization Model` class registration. Both this box and [Plan-012](./012-approvals-permissions-and-trust-boundaries.md)'s same-named consumer box are checked in that one landing, as this row promised. The hold on T3.17's principal-stamp leg is released; the composite's separate wire-arm hold above is untouched and still governs its reachability.
- [ ] **Snapshot-callee checkout-root parameter shipped (2026-08-17 normalized-root transport closure, §6 node NS-69):** T3.13's capture and restore call-sites supply the caller-resolved `checkoutRoot` — read from `run_execution_contexts.checkout_root` beside the execution root and mode — as [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) CP-010-12's seventh input, but the shipped `captureTurnSnapshot` / `resolveRestoreTarget` signatures are still the five-field form (the parameter joins them only when Plan-009 Phase 2B ships the CP-010-15 consented EXTEND of `git/turn-snapshot-service.ts`) and the `checkout_root` column itself lands with Plan-010 Phase 3 / T3.2, its only writer. This plan's caller-side surfaces (I-004-16, CP-004-11, T3.13) were restated to the seven-input contract by the same amendment that grew CP-010-12, so no doc surface here is stale — this box holds the **code**: T3.13 does not dispatch while it is unchecked (Phase-3B `precondition_box_checked` entry, beside that supplement's mechanical `{ plan: 010, phase: 3 }` entry — the Phase-3B re-home that broke the CP-010-9 reciprocal deadlock a phase-scoped Phase-3 entry had created, Codex PR #346 round 1), because a caller authored against the shipped five-field form would compile cleanly today and then be broken in-repo by the Phase-2B signature growth — a Plan-009-lane PR with no consent row on `run-engine.ts` — and tier order alone cannot close that window (this plan is Tier 5; Plan-009 Phase 2B and Plan-010 Phase 3 are Tier 6). Checks when Plan-009 Phase 2B **and** Plan-010 Phase 3 have both merged — the parameter exists on the callee and the column exists under the caller's read; either alone still strands the caller, so the box names both.
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [x] **Plan-readiness audit complete per [runbook](../operations/plan-implementation-readiness-audit-runbook.md)** — Tier-5 audit (NS-17, 2026-05-30, PR #129): D-004-1..3 ratified in [§Ratified Design Decisions (Tier 5 audit, 2026-05-30)](#ratified-design-decisions-tier-5-audit-2026-05-30) and folded as the plan's design of record, with their cross-cutting corpus amendments executed at swap. (Row normalized 2026-08-10, PR #317: the original "Plan-implementation-readiness audit passed (Tier 5 / NS-17)" line predates the normalized Gate-2 checkbox-row convention — Tier-6 C-6-08, 2026-06-12 — so preflight Gate 2, which parses only the bold `Plan-readiness audit complete` form, read this plan as unaudited; the Plan-022 2026-07-08 row-record precedent.) **Re-opened 2026-08-17, scoped to the normalized-root transport reconciliation (§6 node NS-69):** growing [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) CP-010-12's caller-resolved input list to seven — the `checkoutRoot` the snapshot callees now receive, read from the new `run_execution_contexts.checkout_root` column — left this plan's caller-side enumerations stating the superseded field set, so I-004-16's resolved-input clause, CP-004-11's supplied-input list, and T3.13's two literal call signatures plus its turn-boundary test clause are restated to the seven-input contract, and the born-unchecked "Snapshot-callee checkout-root parameter shipped" box above is minted with its Gate-5 entry — carried Phase-3-scoped at first, then re-homed to the Phase-3B explicit-label supplement by the PR's round-1 fold (Codex, 2026-08-17), which minted Phase 3B itself: a re-home of the existing rollback cluster (T3.12–T3.14, T3.16, T3.17, T4.7) breaking the CP-010-9 reciprocal deadlock the phase-scoped entry had created, with T3.13's conversation-only short-circuit narrowed to the released-context class beside it, the PR #346 round-2 fold then converging the checkout-root-absent arm into that class by release-at-confirmation — no task, invariant, CP row, or Required ADR minted here; a Status Flip Rule row-4 flip (a plan-body behavior change; the transport itself is a Plan-010-owned storage-and-parameter mechanism below Spec-004's normative surface, but the round-2 convergence names the vanished-checkout arm in Spec-004's rollback bullet, guard (iv), and the composite acceptance criterion, so Spec-004 flips and restores in the same diff and the paired-spec box above takes a scoped Re-opened/Delivered record). **Delivered 2026-08-17, in the same diff:** restored `review → approved` by the NS-69 targeted readiness-audit delta's neighbor-swap record (the NS-63/NS-65 in-swap shape; this plan's third flip-and-restore — drafted plan-only at round 1, paired with Spec-004 at the PR #346 round-2 fold). The delta's findings on this plan's side: the reconciliation is doc-complete but deliberately code-gated — the box holds T3.13 because the shipped callee signatures stay five-field until Plan-009 Phase 2B and the column until Plan-010 Phase 3, and tier order (this plan Tier 5, both producers Tier 6) closes neither window, a five-field caller compiling cleanly today only to be broken by the Phase-2B growth in a PR with no `run-engine.ts` consent row; the T3.13 test clause sources the checkout root from the run-context row exactly as the production read does, never a hand-passed literal; and the seven-input restatement was swept for the set-quantifier ripple — T3.13's two signatures are this plan's only literal callee-input forms, I-004-16 and CP-004-11 its only prose enumerations of them, and no other Plan-004 surface enumerates the callee inputs. **Re-opened 2026-08-18, scoped to the admitting-principal carrier and queue-PII envelope amendment (§6 node NS-71):** the amendment adds plan-body behavior — T2.4's acceptance-time principal-and-origin stamp, T2.9's queue-body split, T3.5's row-anchored admitting-intervention resolution, T3.17's stamp consumption — plus **I-004-22**, **I-004-23**, **D-004-4**, and **CP-004-14**, a Status Flip Rule row-4 flip taken jointly with Spec-004 above. **Delivered 2026-08-18, in the same diff** (the PR #278 flip-and-restore-in-one-swap shape): the admitting-principal targeted readiness-audit delta audited the re-open's named scope and restored `review → approved` on this plan and Spec-004. The delta's findings on this plan's side, across the readiness dimensions: **(a) Carrier completeness** — the box's five named deliverables are each discharged by a named surface (column, producer task, linkage column, consumer plan, contracts registration), and the consumer half lands in the same diff, so neither plan's box is checked against a promise. **(b) Fail-closed direction** — `origin` is `NOT NULL` with **no DEFAULT**, so an unstamped insert fails at the database rather than silently defaulting into the `system` arm that requires no principal; the paired table-level CHECK makes "principal required iff `origin = 'participant'`" a database-enforced biconditional rather than an application convention, which is what lets I-004-22 be asserted rather than merely intended. **(c) One-writer discipline** — every column minted here sits on a table [cross-plan-dependencies.md §1](../architecture/cross-plan-dependencies.md) assigns to this plan uncontested, and the Plan-017 leg registers the **class** without authoring Plan-017's own column, which that plan instantiates when its surface lands. **(d) Erasure reachability** — the queue-envelope leg closes a genuine gap rather than adding a column: `queue_items` previously carried no participant-naming column at all, so the Spec-022 Path-1 fan-out could not address the table even in principle; the `pii_participant_id` stamp is what makes the selector expressible, and the Spec-022 map row plus the Plan-022 durable-tier inventory row land in the same diff so the map's own exhaustiveness claim stays true. **(e) Set-quantifier sweep** — every enumeration the two legs touch was re-derived: the Path-1 table set (two tables to three, with the excluded classes stated rather than implied), the durable-tier inventory, the intervention-record field statement in the domain model, and the SQLite census (unmoved at 55 — columns and one CHECK only). **(f) Negative control** — the audit's seeded refutation was the plausible-but-wrong reading that the wire's `initiatorId` could serve as the principal source; it is refuted by I-004-8's existing informational-only rule and by the deliberate never-read-from-the-wire clause on the new column, which reuses `auth.principal_mismatch` on cross-check failure rather than minting a duplicate error code. Zero critical, zero major. **Re-opened 2026-08-18, scoped to the desktop edit-affordance placement amendment (§6 node NS-73):** the placement the 2026-08-16 rewind-hardening amendment left as its one named residual is settled and assigned here — a hover-revealed pencil in the participant `user.message` row footer opening an inline editor, authored by the new **T4.8** and carrying the new **I-004-24** and the forward obligation **CP-004-15** to [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md), whose T4.2 exposes the row-footer composition point and mounts the component. A Status Flip Rule row-3 flip (a new invariant and a new CP entry) compounded with row 4 (the §Risks residual is retired into assigned plan-body behavior); the flip is **plan-wide** as every Gate-7 flip is, and it re-opens no scoped box below. Spec-004 takes **nothing normative** — the composite's contract is unchanged, the V1 control set stays closed exactly as `Spec-004 §Resolved Questions and V1 Scope Decisions` states it, and the affordance is an entry point into the existing `rollback` control rather than a control of its own — so it neither flips nor moves; the renderer-placement sentence lands in `Spec-023 §Signature Feature Composition Sketches` instead, and **Spec-023 flips and restores `approved` in the same diff** beside this plan — new normative sketch text plus two materially re-derived interaction enumerations is the spec-amendment class, recorded as a header amendment blockquote in that spec's own convention with the scoped Re-opened/Delivered record on [Plan-023](./023-desktop-shell-and-renderer.md)'s paired-spec Preconditions box; Plan-023 itself does not flip, since this amendment authors nothing in its owned subtrees and mints no bridge capability, method string, or shell behavior. **Delivered 2026-08-18, in the same diff** (the PR #278 flip-and-restore-in-one-swap shape this row already cites): the placement targeted readiness-audit delta — recorded as §6 node NS-73 and narrated in the dated §Notes bullet — audited the re-open's named scope (the eight-conjunct predicate traced conjunct-by-conjunct to the daemon rule it mirrors, the three-state fail-closed visibility mapping, the authorship-blind divergence and its guard-(iii)-constrains-the-target reading, the dim-not-remove in-flight treatment against `Spec-004 §Required Behavior`'s MAY/MUST NOT pair, the admits-not-dispatches settlement copy, the host-mounts/owner-authors split across the CP-004-15 pair, and the invariant-matrix symmetry pass in both directions), re-checked this box, and restored `approved`. The restoration clears the audit and Status gates only: T4.8's dispatch additionally rides the task-scoped hold in its own body on the replacement-send wire-arm box, and its activation arm rides the Phase-3B supplement. **Re-opened 2026-08-25, scoped to the replacement-send wire-arm registration's three narrowings (§6 node NS-75):** the registration the wire-arm box above named as its producer landed, and three narrowings rode with it into `Spec-004 §Required Behavior` and this plan's T1.3 — the composite-only `resend-unapplied` arm's REQUIRED `resendDisposition`, that arm's two displaced never-silent restore enumerations, and the nullable `boundary-diverged` settlement comparand. A Status Flip Rule row-4 flip (a plan-body behavior change: T1.3 states the wire shape, and each narrowing changes what that shape admits), taken jointly with Spec-004 above; no task, invariant, CP row, phase, or Required ADR is minted, and the re-open is plan-wide as every Gate-7 flip is, leaving the scoped boxes above untouched. **Delivered 2026-08-25, in the same diff** (the PR #278 flip-and-restore-in-one-swap shape this row already cites): the wire-arm-registration targeted readiness-audit delta audited the re-open's named scope and restored `review → approved` on this plan and Spec-004. The delta's findings on this plan's side, across the readiness dimensions: **(a) Mirror fidelity** — the registered section is T1.2's byte-for-byte source, so every narrowing is mirrored into T1.3 in the same diff and the section's degraded-class count is re-derived by direct enumeration (**five → seven**) rather than incremented. **(b) Fail-closed direction** — all three narrowings tighten: a REQUIRED member replaces an omissible one, two REQUIRED enumerations replace silence about a restore that actually ran, and a required-and-nullable comparand replaces a member no settlement of that class could construct, so nothing the wire previously refused becomes admissible and no absent member can pass for an unstated cause. **(c) Encoded rather than asserted** — the class-scoped resend fragments put Spec-004's state-determined V1 mapping (`applied` ⇒ `'admitted'`, every `degraded` arm ⇒ `'unapplied'`) into the arm types, verified by compiling the section's declarations under `--strict` against seven seeded negative controls, each proven bound to the contract by a perturbation that turns exactly one of them slack. **(d) Set-quantifier sweep** — two sets moved, and each was swept corpus-wide rather than within these two docs. The disposition union: the degraded-class count, both class-scoping sentences, and the disposition-orthogonality note were re-derived against it, and neither doc carries another enumeration of it. The carrier set of the two never-silent restore enumerations, grown from two wire arms to three: swept across `docs/`, `packages/`, `apps/`, `tools/`, and `.claude/`, which surfaced three closed-set statements this plan itself carried — I-004-16's two-outcome carrier clause, I-004-21's suppression sentence, and T4.7's two-render warning clause — each extended here with the displacing arm, and **two** further sites assigned rather than restated: T3.17, the composite's settlement author, now carries the displaced restore result forward onto the arm in its behavior prose and asserts both fields in its test list — so the three extended statements are satisfiable by a named producer instead of describing a carry nothing performs, with the registry's arm comment naming that producer as the two other carrier arms already do — and T4.8, the composite's **second** client surface, surfaces the same two warnings beside its not-applied settlement line with a matching test arm, since a renderer sweep that stopped at the CLI would have left the desktop affordance hiding exactly what the mandate forbids hiding (Codex round 3). No invariant `Verified by` column moves: the composite leg settles under I-004-21, which T3.17 already verifies. Outside this plan nothing goes stale: [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s callee-side statement pins the two fields on `restored` and `partial_restore`, and the callee shape gains no variant here; [Spec-010 §Turn-Boundary Snapshots](../specs/010-worktree-lifecycle-and-execution-modes.md#turn-boundary-snapshots) states the never-silent mandate over the restore result **unqualified** and already extends it to a second disposition in its own words, so a third wire carrier instantiates that mandate rather than contradicting it; and [the queue-and-intervention domain model](../domain/queue-and-intervention-model.md) names `resendDisposition` structurally, with no optionality claim to go stale. **(e) Negative control** — the audit's seeded refutation was the plausible-but-wrong reading that the `resend-unapplied` arm should carry a locator for the caller's staged text; it is refuted by the response's own `interventionId`, which already names the durable row holding it, so a wire-side locator would be a second source of truth for a row the response identifies. **(f) Criteria coverage** — the two narrowings that change what a conformance test must assert about the composite are mirrored into [Spec-004 §Acceptance Criteria](../specs/004-queue-steer-pause-resume.md#acceptance-criteria)'s atomic edit-and-resend criterion in the same diff — the `resend-unapplied` arm's REQUIRED `resendDisposition` and that arm's two displaced never-silent restore enumerations — appended inline to the criterion that already governs the composite rather than added as a new bullet, so the criteria set is narrowed in place and no criterion is renumbered; the nullable settlement comparand deliberately takes none, since the criteria already carry the observable that class settles (`degraded` naming `boundary-diverged`, file leg skipped fail-closed, staged replacement suppressed, divergence cause persisted on the durable row) and the comparand refines a §Required Behavior sentence that has no criteria mirror to drift from. Zero critical, zero major. The restoration clears the audit and Status gates only: the Gate-5 holds released here are the wire-arm box's alone (T1.2 / T1.3 / T3.17 / T4.7, plus T4.8's task-scoped hold), while Phase 1's remaining `plan_phase` gate, Phase 3B's own gate set, and T4.8's Phase-4 dormancy are unchanged. **Re-opened and re-checked in one swap 2026-08-26 (turn-scoped effective-principal publication, §6 node NS-84):** Spec-004 flipped `approved → review` for the §Required Behavior publication clause and is restored `approved` by the targeted readiness-audit delta riding this same diff; this plan flips with it and is likewise restored. The delta audits T3.19, I-004-25, and CP-004-16 against the amended spec. No shipped phase re-opens (T3.19 homes in the already-gated Phase 3), no census moves, and **no** born-unchecked box is minted. **Re-opened 2026-08-26**, scoped to the Codex rewind rebinding to `thread/fork` (the binding-lineage V1 instance and T3.5's two withdrawn rotation-keyed assertions); **Delivered in-diff** by the NS-85 targeted readiness-audit delta riding this same PR. **Re-opened 2026-09-06, scoped to the typed composite refusal guard (§6 node NS-101):** the amendment adds plan-body behavior — T1.3 declares the `rejectionGuard` union on the `rejected` arm, T3.17 carries its producer obligation across all four guards, and T4.8 reads it for the per-guard remedy — a Status Flip Rule row-4 flip (a plan-body behavior change: T1.3 states the wire shape and the arm now admits a member it did not) taken jointly with Spec-004 above; **no invariant, task, CP row, phase, or Required ADR is minted**, and the re-open is plan-wide as every Gate-7 flip is, leaving the scoped boxes above untouched. **Delivered 2026-09-06, in the same diff** (the PR #278 flip-and-restore-in-one-swap shape this row already cites): the typed-composite-guard targeted readiness-audit delta audited the re-open's named scope and restored `review → approved` on this plan and Spec-004. The delta's findings on this plan's side, across the readiness dimensions: **(a) Derivation fidelity** — the four literals are Spec-004's own guard names kebab-cased with the leading article dropped, checked name-by-name against that paragraph, so the vocabulary is derived from the normative text rather than invented beside it and a fifth guard amends both together. **(b) Fail-closed direction** — the member is declared **arm-scoped** on the rollback `rejected` arm rather than on the shared response base, so a strict parse REFUSES it on a steer / interrupt / cancel rejection and on every non-`rejected` state; within the arm it is additive-optional, so nothing the wire previously admitted is refused and no existing producer breaks. **(c) Not minted ahead of a reader** — the producer (T3.17) and a named reader (T4.8's remedy render) land in the same amendment, with the reader's absent-literal degrade specified as rendering `rejectionReason` alone rather than defaulting to a remedy, which would attach the wrong advice to a refusal the composite did not raise. **(d) Requiredness honestly stated** — requiredness is **not** expressible at the strict-parse boundary, since no member of a `rejected` response identifies its request as composite, so the obligation is the producer's on the `resendDisposition` precedent and is asserted by T3.17's tests plus their non-composite negative control, rather than claimed of the schema. **(e) Set-quantifier sweep** — the guard set is four in `Spec-004 §Required Behavior`, in I-004-21, in T3.17's behavior prose, in the api-payload registry union, and in the shipped `runControl.ts` enum; each was re-derived by counting and each states the same four. **(f) One column, no census, no pin** — no intervention type, wire method, event type, error code, or table is minted (these guards settle as `rejected` **results**, never `JsonRpcError` envelopes), and I-004-21 is widened in place rather than a second invariant minted (the NS-100 precedent), so the preflight Gate-4 invariant-reference census pin does not move. The round-2 fold adds the one durable carrier the round-1 shape lacked: `interventions.rejection_guard`, additive and nullable, because a `rejected` intervention's recorded outcome is reconstructed from its row alone and the round-1 claim that the literal was "derivable from the persisted cause on replay" was **false** — `rejection_reason` is a machine-readable cause whose **vocabulary** no contract enumerates (`error-contracts.md §Intervention` registers no code for an intervention outcome), which is the openness this member exists to close; round 3 corrected the "free-form human sentence" reading rounds 1–2 carried, and the correction leaves the carrier's necessity untouched. Its `CHECK` is **column-attached rather than a table constraint**, which is what keeps the shipped table off the 12-step rebuild: `ALTER TABLE … ADD COLUMN` accepts a column-attached `CHECK` referencing sibling columns (measured), so the closed vocabulary and the `rollback`-`rejected` arm binding land in one appended column, and no census moves — the SQLite **table** census is unchanged at 59. Zero critical, zero major. The restoration clears the audit and Status gates only: Phase 1's remaining `plan_phase` gate, Phase 3B's own gate set, and T4.8's Phase-4 dormancy are unchanged.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/runControl.ts`
- `packages/runtime-daemon/src/queue/queue-store.ts`
- `packages/runtime-daemon/src/interventions/intervention-service.ts`
- `packages/runtime-daemon/src/session/run-engine.ts`
- `packages/client-sdk/src/runControlClient.ts`
- `apps/desktop/src/renderer/src/run-controls/`

## Invariants

| ID | Invariant | Verified by |
| --- | --- | --- |
| **I-004-1** | Queue items are persisted by the runtime before admission; queue creation fails closed (rejects) when persistence is unavailable rather than admitting un-durable work. | T1.4, T2.1 |
| **I-004-2** | Every intervention outcome (`applied` / `degraded` / `rejected` / `expired`) is durably recorded with a daemon-owned receipt on every path. | T1.4, T2.6, T2.7 |
| **I-004-3** | Request:dispatch:record cardinality is 1:1:1 — one `applyIntervention` call produces exactly one driver dispatch and exactly one durable record. **Amended (campaign B2):** the `rollback` composite from `running` is the sole dispatch-axis exception — one intervention, one durable record, but the atomic application makes two driver-facing calls (the internal pause's interrupt, then the `rollbackTo` conversation leg — [Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior)); authored by T3.12 (handler) + T3.13 (provider-leg), campaign B9. **Still the sole exception after the 2026-08-16 amendment:** the edit-and-resend composite's replacement leg **admits** a queue item and makes no driver call of its own (I-004-21), and it never takes the pause-first path, so it adds no third call and no second record. | T2.4, T3.12, T3.13, T3.17 |
| **I-004-4** | `target_run_id` is stable across `waiting` / `blocked` transitions — an intervention never silently retargets to a different run. | T1.4, T3.3 |
| **I-004-5** | Queue admission is FIFO by `created_at ASC`; queue priority is excluded from admission ordering (`Spec-004 §Resolved Questions and V1 Scope Decisions`). | T2.2 |
| **I-004-6** | Run-state and queue-item transitions are monotonic — no backward transitions; terminal states are absorbing — **absent rollback** (campaign B2: the `rollback` intervention is the sole backward/terminal-exit path, [Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); authored by T3.12, campaign B9). | T2.3, T3.4, T3.5, T1.6, T3.12 |
| **I-004-7** | Stale-replay version guard: a run-mutating client command — an intervention, `pause`, or `resume` — whose `expectedRunVersion` does **not equal** the current run version is rejected and applies nothing (an intervention transitions `requested → expired`; `pause` / `resume` reject the request with the run left untouched). **Any** mismatch fails closed — stale (older) _or_ anomalous-future — per `Spec-004 §Interfaces And Contracts` ("a version guard mismatch produces `expired`"); a `<`-only guard would silently admit a future comparand no honest client can hold. **The version comparand is the any-run-progression counter (D-004-1); the guard authors the comparison _behavior_ and the version _derivation_ is fixed by the ratified D-004-1 semantics. The comparand is mandatory (D-004-2, covering interventions + pause/resume) — an absent `expectedRunVersion` is rejected, not skipped.** | T2.8, T3.4, T3.5 (read T1.7) |
| **I-004-8** | Intervention authorization uses the caller's verified participant identity as the Cedar principal — the verified PASETO `sub` on every identity-carrying transport, and on the daemon's local socket (which carries no PASETO token) the daemon-resolved node-owner participant binding (§Preconditions' provider-seam prerequisite; [API Payload Contracts §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model)); any client-supplied `initiatorId` is informational only and never authoritative. | T2.5 |
| **I-004-9** | Runtime-truth: the daemon run-state stream is canonical; desktop run-controls render an optimistic projection that is reconciled to the daemon truth on every update. | T4.1, T4.3, T4.4 |
| **I-004-10** | Capability-gate: only `steer` is gated on the driver steer capability. `pause` / `resume` / `interrupt` / `cancel` are orchestration-layer (daemon-composed) and are **never** capability-gated. **Amended (campaign B2):** `rollback` is the second capability-gated intervention — statically refused (`driver.capability_unsupported`, no orchestration fallback) when the driver does not declare the `rollback` flag ([Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts)); the daemon-side static gate is authored by T3.12, the desktop control gate by T4.2, campaign B9. The CLI carries the invariant's negative arm — T4.7 adds **no** local capability gate, rendering the daemon's authoritative `rejected` decision instead (column corrected 2026-08-16: T4.7's task row has claimed this invariant since campaign B9 while this cell omitted it — the W2.5 disposition-(7) asymmetry class, caught here by the in-swap delta's matrix symmetry pass). | T4.2, T3.12, T4.7 |
| **I-004-11** | Bridge-only: the `run-controls/` renderer subtree reaches daemon / control-plane state exclusively via `window.sidekicks`; it never imports `packages/runtime-daemon` or `packages/control-plane` directly. | T4.5 (= CP-004-5) |
| **I-004-12** | Setup-gate: a run transitions `starting -> running` only after every registered `RunSetupGate.assertRunReady` resolves; a gate refusal parks the run in `starting` (never a silent skip, never `running`), and registered `onRunTerminal` hooks fire exactly once per terminal transition — once per `runVersion` epoch: a campaign-B2 rollback out of a terminal state re-opens the run, and a later terminal fires them again ([Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); the terminal-source re-open + execution-root re-acquisition is authored by T3.12, campaign B9). (Tier-6 audit.) | T3.10, T3.12 |
| **I-004-13** | Terminal exactly-once (campaign B9, P1-2): the terminal `run_lifecycle` emitter is the **primary** at-most-once-per-`(runId, runVersion)` guard — a guard→swap→append sequence run inside `withSessionAppendLock` ([Plan-006](./006-session-event-taxonomy-and-audit-log.md) T3.1) with read-your-writes state from `getRun`, so the check-and-append is atomic against concurrent same-session appends. Plan-006's partial-unique backstop index + terminal-key trigger trio are defense-in-depth behind this emitter guard, never a replacement for it ([Spec-006 §Run Lifecycle](../specs/006-session-event-taxonomy-and-audit-log.md#run-lifecycle-run_lifecycle)). | T3.7 |
| **I-004-14** | Late-event absorption (campaign B9, P1-8): a provider run-lifecycle event for a run already in an absorbing terminal state — or one attributed to a pre-rollback execution epoch — is **absorbed, never appended**: an already-terminal run's terminal-class straggler routes into the T3.7 emitter and no-ops there; a pre-rollback-epoch straggler — terminal-class included — is absorbed at the epoch check itself, before the emitter (the reopened run's current `runVersion` carries no terminal row, so the emitter guard alone would re-terminalize the rewound run); a non-terminal lifecycle straggler is dropped with only a `run.late_event.absorbed` OTel diagnostic **non-event** — no `session_events` row, no state transition, no `runVersion` advance ([Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior)). Non-lifecycle families (usage telemetry foremost) append normally, carrying a durable source-epoch stamp when pre-rollback. | T3.11 |
| **I-004-15** | Rollback intervention (campaign B9, R8/RA-2): `rollback` is version-guarded (mandatory `expectedRunVersion`; every fail-closed pre-dispatch validation completes before any effect), state-gated (legal from `paused` / `waiting_for_approval` / `waiting_for_input` / the three terminal states; a `running` target is paused-first via the existing pause path — **scoped to a bare rollback** by the 2026-08-16 amendment, a rollback carrying `replacementSend` refusing a `running` source outright per I-004-21), and lands the run in `paused` exactly on conversation-leg-confirmed application — a non-confirming outcome (whole-rejection, or a failed / no-rewind conversation leg) leaves the source state in place: a `waiting_for_*` source keeps its pending block, a terminal source stays terminal with its re-acquired root released, and only a `running` source whose internal pause applied rests `paused` recording `pause-only`; a conversation-leg-confirmed rollback appends a **forward** `run.rolled_back` under the append lock and advances `runVersion` — the authoritative log never truncates or rewrites, and rolled-back turns stay queryable marked superseded by projection ([Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); [ADR-017 Decision Log](../decisions/017-shared-event-sourcing-scope.md#decision-log)). | T3.12, T3.14 |
| **I-004-16** | Rollback file-restore caller contract (campaign B9, CP-004-11 / CP-010-12): the file-restore leg resolves the execution root + checkout root (`checkout_root` — the normalized enclosing working-tree top level the Plan-010 T3.2 gate captured at context creation; CP-010-12's seventh caller-resolved input, 2026-08-17) + `execution_mode` + epoch from the `run_execution_contexts` row, derives the `epochLineage` (`{epoch, rewindBase}` list) from durable epoch / intervention records, validates the driver-confirmed floor equal to the accepted `targetPosition`, and invokes [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s turn-snapshot restore under exclusive execution-root tenancy — never authoring Plan-010's callee. A root-less run (execution context released with no existing root — a disposed ephemeral clone / retired worktree; never a live checkout whose nested execution directory was deleted, which restores through the recorded checkout root) rolls back **conversation-only** and is non-resumable, and the restore result's per-path disposition (`files-restored` / `files-partially-restored` / `files-unrestored` / `conversation-only`) is recorded on the intervention outcome, never collapsed — the `files-restored` and `files-partially-restored` outcomes carrying the restore result's overwrite/divergence enumerations (`overwrittenIgnoredPaths` / `divergentGitlinks`; on the partial arm every effect applied before the failure, the failing command's partial writes included), never dropped — and, since the 2026-08-25 wire registration, the composite-only `resend-unapplied` outcome carrying them too wherever it **displaces** a completed `files-restored`, so the displacement never drops what this row forbids dropping (settled under I-004-21, whose T3.17 composes this leg's result). | T3.13 |
| **I-004-17** | Driver input-ask expiry park (campaign B9, Spec-012 Part-B fail-closed follow-up): an input-kind `driver_ask.expired` parks the run `waiting_for_input → paused` **replay-atomically** with the ask closure (one `withSessionAppendLock` unit); no input is ever fabricated (fail-closed), the park deliberately does NOT traverse the CP-004-9 approval-outcome seam (that seam's expired outcomes continue-with-refusal, correct only for approval-pipeline categories), and daemon startup reconciliation derives a missing sibling effect from the persisted `(ask_id, expiry_at)` pair ([Spec-012 §Resolved Questions and V1 Scope Decisions](../specs/012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions)). | T3.15 |
| **I-004-18** | `user.message` durable row (campaign B18): the queue-submit surface appends exactly one `user.message` row per accepted participant send — atomically with admission, `message` routed through the Plan-022 call-site splitter (`pii_payload`), `correlation_id` stamped so the provider `user_text` wire echo folds into the row as delivery confirmation and never appends a second row ([Spec-006 §User Message Events](../specs/006-session-event-taxonomy-and-audit-log.md#user-message-events); the row-31 correlate disposition stays Plan-006-owned). | T2.9, T3.17 |
| **I-004-19** | Cross-node intervention never bypasses node-owner authority (2026-08-03 cross-user run-control amendment): an intervention whose target run's hosting node is not the caller's local node MUST route through target-node-owner approval — an approval in the `tool_execution` category per [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) — and when that approval is absent or denied the request MUST terminate in Spec-024's dispatch lifecycle (`dispatch.denied` on an approver's refusal, `dispatch.expired` on unresolved caller-token expiry, the post-receipt `dispatch.rejected` on the target's pre-approval role-check failure) with no intervention state created or settled — no `InterventionResult` exists to reach `applied` or `degraded`, and no run mutation composes; the own-node authorization-failure `rejected` settlement is the local arm's (Codex PR #299 round 3 alignment). Session membership role admits the intervention on the role axis only and never substitutes for the node-owner leg ([Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts); [Security Architecture §Inter-Node Trust Boundaries](../architecture/security-architecture.md#inter-node-trust-boundaries)). The obligation covers the orchestration-layer `run.pause` / `run.resume` verbs identically even though they never ride `applyIntervention`: T3.4 / T3.5 evaluate the same `Action::"intervene"` adjudication — role axis plus the non-local node-owner approval leg — before composing any run-state mutation, rejecting the request untouched on deny (pause/resume hold no `InterventionType` membership and settle no `InterventionResult`), the same deliberate cross-cutting extension shape D-004-2's mandatory comparand took ([Spec-012 §Required Behavior](../specs/012-approvals-permissions-and-trust-boundaries.md#required-behavior)'s governed-identically rule). | T2.5, T3.4, T3.5 |
| **I-004-20** | Compaction-boundary-aware rewind targeting (2026-08-16 rewind-hardening amendment): rewind-target validation classifies `targetPosition` against **B**, the greatest originating position among the target run's **current** (not already superseded) `usage.context_compacted` rows — the **provider's** context-window compaction, never the daemon retention pass's `event.compacted` that the uncompacted-rewind-span check governs — ranking each row by its operation association (T3.14's rule, linkage-less slot fallback included) rather than off a bare arrival-order read. A target at or above B is admitted unchanged; a target strictly below B, or any target of a run holding a current compaction row that resolves to no originating position at all, is **refused whole, fail-closed, pre-dispatch**, with no leg applied and the machine-readable cause persisted to `interventions.rejection_reason` — identically on both driver legs, and liftable only by a driver declaring re-verified boundary-crossing rewind semantics ([Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior); [Spec-004 §Driver-Level Rollback Mechanics](../specs/004-queue-steer-pause-resume.md#driver-level-rollback-mechanics) owns the pre-compaction-leaf anchoring obligation that re-verification must settle) — a future lift: V1 registers no such capability member, so the classification reads **no capability input** and the refusal is unconditional on every leg. The classification is **two-point**: the confirmed floor is reclassified at settlement against the then-current boundary set — a crossing conclusion settles `degraded` naming `boundary-diverged`, file leg skipped fail-closed, any staged replacement suppressed, run `paused` at the confirmed floor — and `run.resume` refuses fail-closed (`run.compaction_boundary_diverged`) a run whose current position sits strictly below its newest current boundary, the backstop for post-settlement boundary delivery. Undoing a compaction is definitionally the refused class: no provider wire exposes a compaction-restore operation. | T3.16, T3.13, T3.5 |
| **I-004-21** | Atomic edit-and-resend (2026-08-16 rewind-hardening amendment): the composite rides the **existing** `rollback` intervention carrying the optional `replacementSend` — one durable `interventions` row, one `clientIdempotencyKey`, one `expectedRunVersion` guard evaluated once at admission, minting no new intervention type, wire method, event type, error code, or table (the round-2 `run.compaction_boundary_diverged` refusal is the compaction leg's shared resume backstop, never a composite minting). The corrected text is persisted on the write-ahead intervention row **before** dispatch — through the participant-keyed PII envelope (`interventions.pii_payload` via Plan-006's `PiiEncryptor`, stamped `pii_participant_id` = the requester; never plaintext `payload`), so the Plan-022 Path-1 key deletion that shreds the event copy shreds this copy identically — and is recoverable under the requester's key in all three failure arms of an admitted composite (refused / committed-then-failed `resend-unapplied` / transport-lost, the last resolved by the existing reconciliation, whose completion branch drives the staged send forward rather than stranding it; the cross-node pre-approval refusal creates no intervention state and the caller-side surface retains the text). Replacement admission and intervention settlement commit in **one durable transaction** — on the live path and the reconciliation path alike — so an unconcluded intervention proves no admission occurred and recovery never double-admits. Four structural refusal guards fail closed pre-dispatch and whole-intervention: a `running` target (no pause-first for this leg), a run holding a pending send (accepted-but-undelivered, or undrained-queued ahead of the composite — FIFO would otherwise deliver the older send first, and front-insertion is a V1-deferred priority override), a `targetPosition` whose boundary was not opened by a participant-authored `user.message` of that run, and a non-resumable rootless target (the promised next-resume delivery can never occur) — and **which** of the four refused is reported structurally rather than only in prose (2026-09-06 typed-composite-guard amendment): the `rejected` response carries the closed-union `rejectionGuard` (`'no-active-turn'` \| `'no-pending-send'` \| `'participant-authored-target'` \| `'resumable-target'`) beside the unchanged machine-readable `rejectionReason`, declared **arm-scoped** on the rollback `rejected` arm (never the shared response base, since only a composite raises these guards) and within it **schema-optional and producer-obligated** on the `resendDisposition` precedent — no member of a `rejected` response identifies its request as composite, so requiredness is not expressible at the strict-parse boundary — with the producer obligation being T3.17's: a guard refusal always populates it, every other refusal family never does — all **eight** a rollback reaches (capability, authorization, target-position domain, compaction-boundary, incompatible target run state, the Spec-010 restore precondition, uncompacted-rewind-span, execution-root `busy`; T3.17's total negative control enumerates them) — and no error code is minted because these guards settle as `rejected` **results**. The literal is **replay-durable**: T3.17 persists it to `interventions.rejection_guard` in the same write that settles `state = 'rejected'`, and T3.12's replay reconstruction returns it unchanged, so a replay of the same `clientIdempotencyKey` — across a daemon restart included — carries a `rejectionGuard` **equal to the recorded one**, never omitted and never re-derived by re-evaluating the guards against a run that has since moved on. The column is owed rather than optional for the same reason `rejection_reason` is: the wire contract forbids `result` on `rejected`, so the row is the whole of what a replay has to answer from, and the open-vocabulary cause beside it cannot yield the literal without the unpublished-value-set match this member abolishes. Its `CHECK` is column-attached and closes the vocabulary to the four literals while binding their presence to the `rollback` `rejected` arm, mirroring at rest the arm-scoping the strict parse enforces on the wire (2026-09-06 amendment, PR #449 round 2). On a fully-successful rewind — confirmed floor equal to the target, settlement reclassification boundary-clear, file leg complete or legitimately conversation-only — the replacement is **admitted** as a queue item **bound to the rewound run** (`queue_items.target_run_id`), never dispatched, persisted in queue-state `queued` until delivered, delivering into its bound run as the next provider send on `run.resume` under the admitting intervention caller's effective principal (Spec-012's extended rule, via the Plan-012-registered admitting-principal carrier); any degraded arm suppresses it with `resendDisposition: 'unapplied'` — REQUIRED on the `resend-unapplied` arm, whose composite-only reachability makes requiredness expressible, and that arm carries the displaced `files-restored` outcome's `overwrittenIgnoredPaths` / `divergentGitlinks` with it **wherever the rewind's file leg actually ran** (2026-08-25 wire registration), so a restore that ran to the fixpoint is never silenced by the displacement, while the conversation-only branch — which ran no file leg and therefore displaces nothing — carries the same two empty arrays `files-restored` already reads as none; the run still lands `paused` with no auto-resume and no second driver call (I-004-3); idempotency-key reuse is adjudicated by decrypt-and-compare under the requester's live key (an undecryptable stored body settles `intervention.idempotency_conflict`, fail-closed); and superseded marking applies **solely** on the accepted `run.rolled_back` boundary, never on the composite's admission, dispatch, or replacement send ([Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior)). | T3.17, T3.5 |
| **I-004-22** | Admitting-principal recording (2026-08-18 admitting-principal carrier amendment): every accepted intervention durably records, on its own `interventions` row and in the same write that creates it, the `origin` it was admitted through and — on the `participant` arm — the `admitting_principal_id` the **daemon itself resolved from the transport**, by the `approval_resolutions.approver_id` resolution semantics (D-012-12): node-owner binding on the local socket, verified PASETO `sub` on identity-carrying transports, `caller_token.sub` on the Spec-024 cross-node arm. A wire-supplied principal identifier is informational and cross-checked only, a mismatch refusing with the existing `auth.principal_mismatch`; `initiator_id` remains routing-and-audit metadata and is never an authorization input (I-004-8). The two arms are exhaustive and mutually exclusive, and required-ness is a **database-enforced biconditional** — `origin` is `NOT NULL` with no DEFAULT and a table-level CHECK binds principal-present to `origin = 'participant'` — so an unstamped or half-stamped row cannot be written at all. Every later authorization decision about the act reads this stamp and nothing else: an unresolved principal adjudicates as a deny, never as the run initiator, and the drain resolving a run-bound item's admitting principal follows the item's own `admitting_intervention_id` linkage rather than inferring from run history. | T1.4, T2.4, T3.5, T3.17 |
| **I-004-23** | Participant-authored queue bodies rest encrypted (2026-08-18 queue-PII envelope amendment): a queue item whose body a participant authored persists that body in `queue_items.pii_payload` under that participant's DEK with `pii_participant_id` stamped, the plaintext `payload` retaining only non-PII members — the same at-rest split, the same `PiiEncryptor` seam, and the same Path-1 key deletion that governs `interventions.pii_payload` and `session_events.pii_payload`. The stamp is what makes the table addressable by a per-participant erasure or export selector at all; before it, no `queue_items` column named a participant. Orchestration-authored items carry both columns NULL with content in plaintext `payload` — they have no participant key to encrypt under, so the mechanism is split-not-replace. The row's posture is otherwise unchanged: queue rows are durable, are never deleted on drain, and are never evented, which is exactly why the body cannot be left in plaintext to outlive the runs that consumed it. | T1.4, T2.9, T3.5, T3.17 |
| **I-004-24** | Edit-and-resend entry-point projection (2026-08-18 desktop edit-affordance placement amendment): the renderer's entry point into the composite — the hover-revealed pencil affordance in a participant `user.message` row's footer, opening an inline editor with a Cancel action and a confirm action that dispatches `applyIntervention('rollback', {targetPosition, replacementSend})` — is an **entry point to the existing `rollback` control and mints no new run control** (`Spec-004 §Resolved Questions and V1 Scope Decisions`, whose V1 control set the 2026-08-16 amendment already restated as unwidened). It **mirrors** the daemon's admission predicate and is never a second source of truth for eligibility (the I-004-9 projection discipline): every conjunct it evaluates traces to a daemon rule — the Phase-3B activation arm, the declared `rollback` capability (I-004-10), the four structural refusal guards and the participant-authored non-superseded boundary they turn on (I-004-21, I-004-15), and the caller's session write-access role (I-004-8, I-004-19) — and it is **authorship-blind**: guard (iii) constrains the target row's author, never the viewer, so any owner or collaborator may correct another participant's boundary. Visibility is **three-state and fail-closed**: hidden while the activation arm is dormant (no handler exists to receive the dispatch), disabled when the driver declares no `rollback` capability, disabled **with a stated reason** on a transient guard failure, and structurally ineligible rows carry no affordance at all — never a control that silently no-ops. While an admitted composite is in flight the rows the rewind would cut are **dimmed** — the client treatment `Spec-004 §Required Behavior` permits, elected here as the ratified placement's in-flight feedback — and are **never removed or renumbered** before `run.rolled_back` appends, which that same rule forbids outright (I-004-15's never-truncate log rule as rendered). Confirm-action and settlement copy state that the replacement is **queued and sends on the next Resume** — never "sent" or "re-sent" — because the replacement is admitted run-bound and is not dispatched, the run landing `paused` with no auto-resume (I-004-21). | T4.8 |
| **I-004-25** | Turn-scoped effective principal is resolved daemon-side and published, never derived downstream (2026-08-26): at every turn boundary the run engine opens, the turn's effective principal — the admitting participant of the `interventions` row that opened it, otherwise the run starter, otherwise the `system` arm — is resolved from durable rows and published on the run execution context for the metered row to carry. No consumer re-derives it by ordering usage rows against turn-boundary events, and no wire field supplies it. Source: `Spec-004 §Required Behavior`, [Spec-006 §Usage Telemetry](../specs/006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry), [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) instance (4). Test: a run started by A and steered by B publishes A then B at the context; a recovery turn publishes the `system` arm and carries no participant reference. | T3.19 |

## Cross-Plan Obligations

| ID | Obligation | Direction | Tasks |
| --- | --- | --- | --- |
| **CP-004-1** | Driver capability flags + `DriverInterventionResult` (`applied` / `degraded`, plus the additive-optional closed-literal `refusalCode` added 2026-08-25 by Plan-005 T3.18 — T2.6's mapper surfaces it on the intervention response; no shipped field changes type) | **Consume** from [Plan-005](./005-provider-driver-contract-and-capabilities.md) | T2.6, T2.7 |
| **CP-004-2** | `command_receipts` table — Plan-004 CREATEs the forward-declared table shell; Plan-015 OWNS its column semantics + read model | **Provide shell** to [Plan-015](./015-persistence-recovery-and-replay.md) | T1.5 |
| **CP-004-3** | `packages/contracts/src/runControl.ts` — new contract surface, no prior owner (verified against the ownership map) | **Create** | T1.1–T1.3, T1.6, T1.7 |
| **CP-004-4** | `run.*` method-name namespace (`run.queueList` / `run.queueCreate` / `run.queueCancel` / `run.intervene` / `run.pause` / `run.resume` / `run.subscribeState` / `run.subscribeQueue`) — wire contract registered in `api-payload-contracts.md §Method-Name Registry`. **The eight concrete strings are ratified (D-004-3).** | **Register** with [Plan-007](./007-local-ipc-and-daemon-control.md) | T4.1, T4.4 |
| **CP-004-5** | `run-controls/` renderer subtree consumes daemon / control-plane state only via `window.sidekicks` (Spec-023 bridge discipline) | **Honor** [Plan-023](./023-desktop-shell-and-renderer.md) | T4.5 (= I-004-11) |
| **CP-004-6** | `packages/runtime-daemon/src/session/run-engine.ts` — **Plan-004 CREATEs this file** (its unexecuted Phase 3 is the file's first author; corrects the prior EXTEND-of-Plan-001 attribution — P-1, campaign B9). The `session/` **directory** is Plan-001-owned, but this specific file crosses per the CP-001-1 per-file-content-ownership precedent ([cross-plan-dependencies.md §2 Package Path Ownership Map](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map)); Plan-001's completed Shipment Manifest is never touched. | **Create** (in Plan-001's `session/` dir) | Phase 3 (T3.1 CREATE; T3.2–T3.11, T3.15 EXTEND) + Phase 3B (T3.12–T3.14, T3.16, T3.17 EXTEND — the rollback cluster's re-home, PR #346 round-1 fold) |
| **CP-004-7** | `getRun(runId): { version, sessionId, state }` run-state read accessor — Plan-004 is the run-state-read **origin**; per [cross-plan-dependencies.md §3 Inter-Plan Dependency Graph](../architecture/cross-plan-dependencies.md#3-inter-plan-dependency-graph), the Plan-016 row names this `getRun` accessor (per CP-004-7), while the Plan-015 (queue-state recovery) and Plan-017 (queue/steer) rows depend on Plan-004 without naming this accessor | **Provide (forward)** to [Plan-016](./016-multi-agent-channels-and-orchestration.md) (the §3-named `getRun` consumer) | T1.7 |
| **CP-004-8** | `RunSetupGate` registration seam (`{ assertRunReady, onRunTerminal? }`, ordered array on `run-engine.ts`) + the `QueueItemCreateRequest.workspaceId?` run-binding carrier — Plan-004 ships the seam with zero gates; Plan-010's execution-root gate registers at Tier 6 (Tier-6 audit, 2026-06-10); Plan-028's MCP run-admission drift gate registers at Tier 7 per Plan-028 CP-028-5 (campaign B18, registered at Plan-028's 2026-08-12 targeted readiness audit) — a registration call, never an edit to this plan's owned files | **Provide (forward)** to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-9) + [Plan-028](./028-mcp-server-configuration-and-governance.md) (reciprocal CP-028-5) | T3.10, T1.1 |
| **CP-004-9** | Approval run-blocking seam — Plan-012's approval-service provides `registerApprovalOutcomeListener({ onBlocking(runId, approvalRequestIds, blockingState), onResolved(runId, approvalRequestIds, outcome) })` + `cancelPendingForRun(runId, cause)` (the id arrays carry Plan-012 D-012-19's per-principal request set — one element for ordinary turns; `outcome` is the set's aggregate, approved iff every member approved — the PR #317 reciprocal of that provider-side ratification); the run-engine registers the listener at engine construction (same registration directionality as the CP-004-8 seam). `onBlocking` drives `running -> waiting_for_input` for `user_input` / `mcp_elicitation` categories and `running -> waiting_for_approval` for the other 7; `onResolved` returns the run to `running` — rejected/expired outcomes continue-with-refusal, never terminate (run-state-machine.md `waiting_for_approval -> running` row), except a denied — or expired, silence-is-denial (campaign B15) — pre-turn moderation gate (`category: 'gate'`), which Plan-016's moderation gate system-cancels with `trigger: 'moderation_denied'` (Spec-016 D-016-10; the expired-gate arm records Plan-016/Plan-012-owned behavior on this doc-only reciprocal — non-normative here, its normative homes riding those plans' standing `review` windows, and this plan's own first-PR gate requires their completed promotions per AGENTS.md §Doc-First Discipline — the B13 one-clause reciprocal precedent); every terminal transition calls `cancelPendingForRun` so still-pending requests settle as `approval.canceled` (`Spec-006 §Approval Flow (approval_flow)`; Tier-6 audit 2026-06-10, Plan-012 A-6/A-10) | **Consume** from [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) (reciprocal CP-012-5) | Phase 3 (doc-only at this audit — Phase 3 unexecuted; listener registration + terminal-hook call land with the run-engine implementation) |
| **CP-004-10** | Orchestration queue-admission seam — Plan-016's orchestration-run-service composes **in-process** with the daemon queue-admission service after its own admission pipeline passes, passing the typed `OrchestrationRunLinkCarrier` ([api-payload-contracts §Plan-016](../architecture/contracts/api-payload-contracts.md#plan-016--multi-agent-channels-and-orchestration)); the admission API returns the minted `RunId` alongside `queueItemId`; `run.queued` threads the carrier fields into its payload as optional additive fields (`{agentId?, parentRunId?, linkType?, internalHelper?, producingNodeId?}` plus the admission-resolved `effectiveRunConfig?`); independent of the carrier, the queue write stamps `admittedUnpricedCapCents?` (native-cap unpriced admissions only) + `admittedModelFamily?` (every admitted run) on EVERY provider run's `run.queued` — the ordinary `run.queueCreate` path stamps directly via **Plan-016's admission-stamp resolver** (the T2.3 family/pricing/`cost_cap` accessor set), which Plan-016 T2.3 wires into this queue-write seam as an EXTEND (Plan-004's own Phase-2 queue tasks carry no pricing/budget machinery — the ordinary-path stamp code lands with Plan-016 T2.3 and is gated on it); the orchestration path supplies the same resolver's values via the carrier — the durable replay sources for cost reservations, worst-case terminal debits, and as-of-admission family keying, never client-suppliable (campaign B6, `Spec-016 §Cost Derivation And Absent-Cost Semantics`) — `Spec-006 §Run Lifecycle (run_lifecycle)`, Plan-016 D-016-3); the wire `run.queueCreate` never accepts the carrier (child-run creation goes through `orchestration.runCreate` only); the `run.completed` `trigger` value set gains `'turn_limit'` for Plan-016's turn-limit completion (D-016-8); Plan-016's system-initiated interventions (budget/idle/moderation) enter through an in-process entrypoint below the wire authz boundary with NULL-for-system actor + the standard `expectedRunVersion` reject-re-read-retry loop (D-016-7) — and, per the 2026-08-18 admitting-principal carrier amendment, that entrypoint composes with T2.4's single record-creating path, which stamps `origin = 'system'` with a NULL `admitting_principal_id` for it; because `origin` carries no DEFAULT, an in-process admission that bypassed that path rather than composing with it would fail closed at the database (D-004-4 / I-004-22) | **Provide (forward)** to [Plan-016](./016-multi-agent-channels-and-orchestration.md) (reciprocal CP-016-7; Tier-6 audit, Plan-016 walk) | Phase 2 (doc-only at this audit — carrier threading lands with the queue-create implementation) |
| **CP-004-11** | Rollback leg consumes (campaign B9): (a) the file-restore leg calls [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s Phase-5 turn-snapshot ops — `captureTurnSnapshot` at the turn boundary and `resolveRestoreTarget` / `restoreToTurn` for the rewind (whose `TurnSnapshotResolution` / `TurnSnapshotRestoreResult` result unions T3.13 maps onto the file-leg arms of the `RollbackInterventionResult` `disposition`) — supplying the caller-resolved execution root, checkout root (`run_execution_contexts.checkout_root` — CP-010-12's seventh caller-resolved input, 2026-08-17; read from the same row as the root and mode, never a `workspaces` query, and joining the shipped five-field callee signatures only at Plan-009 Phase 2B per the §Preconditions checkout-root box), `execution_mode`, epoch, turn ordinal, and `epochLineage`; Plan-004 is the caller side (CP-010-12), never authors `git/turn-snapshot-service.ts` (cross-plan §2 one-writer). (b) the conversation leg calls Plan-005's `rollbackTo` driver op + `DriverRollbackResult` (campaign B3) — the daemon resolves the run's live binding at dispatch; the driver-confirmed floor is domain-validated equal to the accepted `targetPosition` before the file leg. | **Consume** from [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-12) + [Plan-005](./005-provider-driver-contract-and-capabilities.md) | T3.13 |
| **CP-004-12** | Durable source-epoch attribution carrier (campaign B9, round-2 F5): a **typed epoch-attribution field on the event-append surface**, stamped on pre-rollback-epoch non-lifecycle rows at late-append (T3.11) and read by the supersede projection's cross-epoch keying (T3.14). `Spec-004 §Required Behavior` mandates the durable stamp and delegates its typed shape to the campaign's Plan-006 bundle — registered 2026-07-20 as **Plan-006 T1.9's cross-cutting `sourceEpoch` + `sourcePosition` payload carrier pair** (`SourceEpochSchema` / `SourcePositionSchema` + payload-key consts + the `withEpochStamp` pairing helper; Spec-006 §Event Type Enumeration + §Compacted Event Format), the lead-owned follow-up recorded in §Notes — the position companion carries the late row's turn-boundary ordinal, since no run-scoped family payload has a native position key for the `turn > targetPosition` cutoff to read. It stays a **named merge prerequisite** gating T3.11's late-append leg and T3.14's epoch consumption (that code cannot merge until the carrier _ships_ — machine-enforced since Codex round 5 by Phase 3's paired `precondition_box_checked` + `{ plan: 006, phase: 1 }` Gate-5 entries, the latter holding until the re-opened Plan-006 Phase 1 lands on `develop`; T3.14 rides Phase 3B since the PR #346 round-1 re-home, whose `plan_phase` Phase-3 gate inherits that enforcement transitively — a merged Phase 3 proves its own Gate-5 chain held) and is not authorable here (cross-plan §2 one-writer — Plan-004 writes no Plan-006 symbol). | **Consume** from [Plan-006](./006-session-event-taxonomy-and-audit-log.md) | T3.11, T3.14 |
| **CP-004-13** | Superseded-turn read seam (campaign B9, round-2 F10): T3.14's projection **exports `supersededTurns(runId)`** — the per-turn supersede attribution the position cutoff + epoch keying produce: each superseded (epoch, turn) carries its source epoch and the superseding rollback's `targetPosition`, lineage-resolved across every accepted rollback at the row's epoch or later — as the seam [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md)'s `timeline-projector.ts` + `TimelineRead` consume to render rewound turns distinctly (without it the marking never reaches the rendered timeline). Plan-004 provides-forward the export; the **Plan-013-side consumer registration landed 2026-07-20** (Plan-013 §Implementation Steps + Spec-013 §Required Behavior superseded-turn rendering), no Plan-013 symbol authored here (cross-plan §2 one-writer). | **Provide (forward)** to [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md) | T3.14 |
| **CP-004-14** | Admitting-principal carrier (2026-08-18 amendment): the durable, replay-stable identity of the participant who admitted an intervention — `interventions.admitting_principal_id` beside the `origin` discriminator (T1.4's CREATE), stamped daemon-side at acceptance by T2.4 from the transport-resolved caller and never from the wire, plus the row-anchored `queue_items.admitting_intervention_id` linkage (stamped by T3.17, followed by T3.5) that lets a run-bound queue item resolve which intervention admitted it. [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) consumes the stamp as the turn-scoped effective principal of a drained replacement turn (reciprocal CP-012-12; its T2.2 authorization check and T2.7 remembered-grant actor axis are the consumers, and its same-named §Preconditions box is checked by the same landing). The class rule this instantiates — every admitting write records the daemon-resolved transport-authenticated principal on its own durable row — is registered in [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) alongside the shipped `approval_resolutions.approver_id` instance (D-012-12), whose naming and resolution semantics this column follows deliberately rather than inventing a second convention. Plan-004 authors only its own tables' columns; Plan-017's chat-borne workflow start is the class's third instance and instantiates its **own** column when that surface lands (cross-plan §2 one-writer — no Plan-017 symbol is authored here). | **Provide** to [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) (reciprocal CP-012-12) | T1.4, T2.4, T3.5, T3.17 |
| **CP-004-15** | Edit-and-resend entry-point mount seam (2026-08-18 desktop edit-affordance placement amendment): Plan-004 **authors** the entry component and its eligibility predicate inside its own `run-controls/` subtree (T4.8) and exports it as a mountable unit taking the row's run id, target position, and authorship/state facts; [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md)'s participant `user.message` timeline row **exposes a footer composition point and mounts it there** (Plan-013 T4.2), the host-mounts/owner-authors discipline Plan-013 T4.4 already follows against Plan-023's composer-area point — neither plan edits the other's subtree, and no renderer symbol crosses ownership in either direction (cross-plan §2 one-writer). The rewound-row dim treatment stays Plan-013's own rendering of the CP-004-13 supersede seam; this obligation carries only the entry point. | **Provide (forward)** to [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md) (reciprocal registered on that plan's §Cross-Plan Obligations as a `consumes` row keyed to this ID) | T4.8 |
| **CP-004-16** | Turn-scoped effective-principal publication on the run execution context — the run engine resolves the value from `interventions` rows and publishes it; Plan-005's usage normalizer reads it and stamps `usage.cost_update`, and Plan-016's receipt folds the stamped rows. This plan neither emits the event nor mints a column: the carrier is the metered event's own payload ([Spec-006 §Usage Telemetry](../specs/006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)), registered as instance (4) of the §Authenticated Principal class rule. A driver that cannot read the context stamps nothing rather than guessing, and the receipt's stated degrade — an unstamped turn attributes to the run starter — is the fail-safe | **Provide** to [Plan-005](./005-provider-driver-contract-and-capabilities.md) (reciprocal CP-005-10) and [Plan-016](./016-multi-agent-channels-and-orchestration.md) (consumed at CP-016-9) | T3.19 |

## Ratified Design Decisions (Tier 5 audit, 2026-05-30)

The Tier-5 plan-readiness audit (NS-17) surfaced three open decisions; all three were ratified at the user-review pause and are folded here as the plan's design of record. The cross-cutting corpus amendments they entail (Spec-004 / Spec-006 version-derivation + emission; `api-payload-contracts.md §Method-Name Registry`; `error-contracts.md`) are executed at swap alongside this `approved → review` flip.

- **D-004-1 (was F-004-2-10) — `version` = any-run-progression counter.** The `expectedRunVersion` stale-replay guard (I-004-7 / T2.8) compares against a run `version` derived as an **any-run-progression counter** (reading (b)): it increments on every run progression — applied interventions included — not only on `run.*` state-machine transitions (reading (a)). **Rationale (the hardened call):** reading (a) fails to catch two steers racing on a still-`running` run (zero new `run.*` events → the second stale steer passes undetected); reading (b) closes that replay window. Plan-004 authors T1.7's accessor _shape_ and T2.8's guard _behavior_; the `version` _derivation_ follows this ratified semantics. Entails a Spec-004 / Spec-006 amendment defining the `version` field on the `run.*` payload + its increment/emission point (executed at swap).
- **D-004-2 (was F-004-2-06) — `expectedRunVersion` is mandatory (fail-closed).** The comparand is **required** on every intervention request (`Spec-004 §Interfaces And Contracts` mandatory reading); the absent-comparand case is **rejected**, not applied. An optional comparand would let a caller bypass the stale-replay guard by omitting the field — the hardened reading fails closed. T2.8 authors the present-comparand guard; the absent case rejects. Entails a Spec-004 clarification (executed at swap).
  - **Scope extension (Tier-5 audit) — the mandatory comparand binds `run.pause` / `run.resume`, not only interventions.** The ratified `RunPauseRequest` / `RunResumeRequest` contracts (`docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume`) carry the **same mandatory** `expectedRunVersion` guard, with `RunControlAck` echoing the advanced `runVersion` so the caller threads the fresh comparand forward. This **extends D-004-2 beyond its original intervention-only scope**: a stale `pause` replayed against a run that has since progressed is the identical replay hazard the guard exists to close, so the comparand fails closed the same way (T3.4 / T3.5 apply the T2.8 guard before composing). `pause` / `resume` remain **orchestration-layer run-control verbs, not interventions** — they hold no `InterventionType` (`steer | interrupt | cancel | rollback`, campaign B2) membership and are not serialized through the intervention queue (ADR-011) — so this is a deliberate cross-cutting extension of the mandatory-comparand obligation, recorded here and reflected in the `api-payload-contracts.md` run-control comment at swap.
- **D-004-3 (was F-004-4-04) — the eight `run.*` method strings are ratified.** `run.queueList` / `run.queueCreate` / `run.queueCancel` / `run.intervene` / `run.pause` / `run.resume` / `run.subscribeState` / `run.subscribeQueue` (CP-004-4) are the wire contract, registered in `api-payload-contracts.md §Method-Name Registry` at swap. Reciprocal `provides` is recorded on [Plan-007](./007-local-ipc-and-daemon-control.md) (the `run.*` namespace owner) in the cross-plan dependency map.

Later ratifications join this section as the plan's design of record for `D-004-N`; the three decisions above remain exactly the Tier-5 audit's own set (the heading and its anchor are unchanged, so inbound cites are undisturbed).

- **D-004-4 — the intervention admission path carries an explicit `origin` discriminator, and the principal is required by database constraint on the participant arm** (2026-08-18 admitting-principal carrier amendment; user-ratified). The `interventions` row records `origin TEXT NOT NULL CHECK(origin IN ('participant', 'system'))` beside the nullable `admitting_principal_id`, with a table-level CHECK binding them: the principal is present exactly when `origin = 'participant'`. **Rationale (the hardened call):** the alternative readings both fail. Inferring the origin from `initiator_id IS NULL` conflates a genuinely system-originated intervention with a participant intervention whose initiator was simply not supplied, and it makes the security-relevant distinction a side effect of an unrelated column's nullability — a property no constraint can defend and any later change to `initiator_id`'s optionality would silently invert. Making the principal unconditionally `NOT NULL` is likewise wrong: Plan-016's budget / idle / moderation interrupts enter in-process below the wire authorization boundary with a NULL-for-system actor (CP-004-10), so there is no participant to name, and forcing a synthetic one would put a fabricated identity into the audit record. The explicit discriminator keeps both arms honest and — because `origin` carries **no DEFAULT** — fails an unstamped insert closed at the database rather than defaulting into the arm that requires no principal. The daemon-resolved principal itself follows the shipped `approval_resolutions.approver_id` model (D-012-12) rather than minting a second convention, and a wire-supplied principal that contradicts the daemon's resolution refuses with the existing `auth.principal_mismatch` — deliberately reusing that error rather than adding a near-duplicate, since it already names this exact class of caller-asserts-vs-transport-proves conflict. Entails the T1.4 CREATE columns, T2.4's acceptance-time stamp, and the paired Plan-012 consumption (CP-004-14 ⇄ CP-012-12, ratified there as D-012-20).

## Data And Storage Changes

- Add local `queue_items`, `interventions`, and `command_receipts` tables.
- Project queue and intervention read models from the durable `queue_items` / `interventions` tables — run state is event-sourced (per ADR-001 / ADR-017), so the `session_events` log is **not** extended with new queue/intervention columns.
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions.

## API And Transport Changes

- Add queue list/create/cancel endpoints and intervention request or result events to the client SDK.

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

1. Define run-control contracts and migrations.
2. Implement queue store and serialized intervention application in the daemon.
3. Integrate run-engine state transitions and capability checks.
4. Add desktop queue and intervention controls plus status rendering.

## Implementation Phase Sequence

### Phase 1 — Contracts, Migrations, and the Run-Read Accessor

**Goal:** Land the `runControl.ts` contract surface, the `queue_items` / `interventions` / `command_receipts` migrations, the client-facing pause/resume trigger, and the run-state read accessor that Phase 2's guard and Phase 4's surfacing both consume.

**Precondition:** D-004-1 (`version` = any-run-progression counter) and D-004-2 (`expectedRunVersion` mandatory, fail-closed) ratified — T1.7's accessor `version` field and T1.6's request shape implement these outcomes.

```yaml
preconditions:
  # T1.2's `'rollback'` InterventionType arm imports the Plan-005-owned union
  # member that the campaign B10 bundle widens together with its consumers
  # (CP-005-6; the widening is Plan-005 T1.8 — the Phase-1 contract catch-up
  # task, which re-opens the shipped phase). Tier order (4 < 5) makes this
  # near-automatic — the entry makes it machine-enforced (preflight Gate 5):
  # declared-⊆-shipped resolves false until the re-opened Plan-005 Phase 1
  # (T1.7/T1.8 included) has fully shipped.
  - { type: plan_phase, plan: 005, phase: 1, status: merged }
  # 2026-08-16 rewind-hardening amendment: T1.2's `replacementSend` payload
  # field and T1.3's widened result members (`resend-unapplied`,
  # `boundary-diverged`, `resendDisposition` — round-2 fold) mirror the
  # api-payload §Plan-004 section byte-for-byte, so the doc-side registration
  # must land first or the mirror has no source (the CP-004-12
  # named-merge-prerequisite pattern; prose alone is Gate-5-invisible).
  - { type: precondition_box_checked, box: "Rollback replacement-send wire arm registered" }
```

#### Tasks

- **T1.1 — QueueItem contracts** (`QueueItemCreate` / `QueueItemList` / `QueueItemCancel` Zod schemas, incl. the Tier-6-amended `QueueItemCreateRequest.workspaceId?` run-binding field per api-payload-contracts.md §Plan-004 — CP-004-8)
  - **Files:** `packages/contracts/src/runControl.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (QueueItemCreate / QueueItemList / QueueItemCancel against runtime-owned durable state)
  - **Verifies invariant:** I-004-1
  - **Consumes:** `QueueItemState` enum (queued / admitted / superseded / canceled / expired) + branded `QueueItemId` / `RunId` / `SessionId` (in-package)
- **T1.2 — `InterventionRequestPayload` discriminated union** (`steer` | `interrupt` | `cancel` | **`rollback`**) — the `rollback` arm `{ type: 'rollback'; targetPosition: number }` is authored by this bundle (campaign B9), mirroring `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` byte-for-byte (`targetPosition`: normalized session position, Zod int + nonnegative at parse; the daemon boundary-existence + strictly-below-current-position check is the fail-closed admission validation, not a parse concern — Spec-004 §Required Behavior). **Widened by the 2026-08-16 rewind-hardening amendment** to `{ type: 'rollback'; targetPosition: number; replacementSend?: … }` — the optional corrected message body of the atomic edit-and-resend composite, whose presence alone selects that composite's four additional pre-dispatch refusal guards and its `resend-unapplied` disposition (I-004-21); the field's registration in the mirrored api-payload-contracts §Plan-004 section is a **named merge prerequisite** (the §Preconditions box "Rollback replacement-send wire arm registered" + its Phase-1 Gate-5 entry), so this arm's widening does not dispatch until that registration lands — the byte-for-byte mirror has nothing to mirror otherwise. **Registered 2026-08-25** (§6 node NS-75): the member is `replacementSend?: { content: string }` — the corrected message body alone, non-empty at parse, with no attachment member in V1 — and the §Preconditions box is checked, so this arm carries no hold. The daemon-side compaction-boundary classification (I-004-20) is likewise admission validation, not a parse concern.
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionRequest — target run id, type, mandatory expectedRunVersion guard)
  - **Verifies invariant:** I-004-4
  - **Consumes:** `RunId`, `InterventionType` (`'steer' | 'interrupt' | 'cancel' | 'rollback'` — the `'rollback'` member is Plan-005-owned (`provider-driver.ts` §Shared Enums per CP-005-6) and imported upward; the shipped `provider-driver.ts` union stays three-membered until the campaign's Plan-005 bundle (B10) widens union + consumers together, so this arm is gated on that widening — the api-payload-contracts code-mirror gate)
- **T1.3 — `InterventionRequestResponse` (6 states) + `RunStateChangeEvent` (9 states)** (campaign B9; Codex round 2 restructures the rollback outcome to the discriminated `RollbackInterventionResult` `disposition` union — `files-restored` / `files-partially-restored` (with `failedStep` + the same two REQUIRED enumerations covering every effect applied before the failure — Codex re-audit round 2, widened by PR #230 round 1) / `files-unrestored` / `conversation-only` / `pause-only` / `nothing-applied` / `position-mismatch` (with `requestedPosition` + `confirmedPosition`) / `resend-unapplied` (2026-08-16 rewind-hardening amendment) / `boundary-diverged` (the same amendment's settlement-reclassification arm — the confirmed floor reclassified against the then-current compaction-boundary set concluded crossing, carrying `confirmedPosition` + `newestBoundaryPosition`, the then-newest boundary position the settlement classification compared against — typed `number | null`, `null` exactly when the crossing conclusion rests on a **position-less compaction row**, which `Spec-004 §Required Behavior` routes into this same disposition ("classifies as crossing for every target of that run") and which by definition has no position to name; required-and-nullable rather than optional, so an absent member never passes for an unstated cause) — mirroring `Spec-004 §Required Behavior`'s full vocabulary, carried only on a `rollback` result; the response is discriminated on `interventionType` so the seam parses `result` STRICTLY per type — a malformed rollback result fails validation, never a generic fall-through (test); the rollback arm is additionally state-split (Codex round 3) and state-scoped per disposition class (Codex round 5): `applied` REQUIRES `result: RollbackAppliedResult` (`files-restored` / `conversation-only`; the `files-restored` arm carries the two REQUIRED restore-warning enumerations `overwrittenIgnoredPaths` / `divergentGitlinks` — empty when none, Codex post-merge round) and `degraded` REQUIRES `result: RollbackDegradedResult` (the degraded-only dispositions — **five until the 2026-08-16 rewind-hardening amendment added `resend-unapplied` and, at its round-2 fold, `boundary-diverged`, seven after**: `resend-unapplied` is the committed-then-failed arm of the edit-and-resend composite, where the conversation rewind confirmed and every other leg succeeded but the `replacementSend` admission itself failed; the arm carries the REQUIRED `resendDisposition: 'unapplied'` its composite-only reachability makes expressible, and — because it is the one disposition that **displaces a completed file leg** rather than following a skipped or pre-mutation-refused one — `files-restored`'s `overwrittenIgnoredPaths` + `divergentGitlinks` on the same REQUIRED + empty-when-none contract, so a restore that did mutate the tree can never go silent under `Spec-010 §Turn-Boundary Snapshots`; it carries **no locator of its own** for the staged text, whose durable location is the intervention row the response's own `interventionId` already names (I-004-21 persists the body there under the requester's key before dispatch), so no arm loses the caller's message and none needs a second wire-side locator; `boundary-diverged` is the settlement-reclassification arm every rollback shares — I-004-20; both widenings ride the same named merge prerequisite as T1.2's payload field). A composite's terminal `applied` / `degraded` results additionally carry the `resendDisposition: 'admitted' | 'unapplied'` member — **schema-optional, producer-obligated**: no member of a rollback result identifies its request as composite except the composite-only `resend-unapplied` arm, which therefore REQUIRES it, so requiredness is not expressible anywhere else at the strict-parse boundary; the member parses optional on every other rollback result, the obligation that a composite settlement always populates it and a bare rollback's never does is the daemon producer's, asserted by T3.17's settlement tests, and presence therefore reports a composite settlement without being a parse-time discriminator (`Spec-004 §Required Behavior`'s admits-paragraph: in V1 the value is state-determined, `applied` ⇒ `'admitted'` and every `degraded` arm ⇒ `'unapplied'`, carried anyway so the resend outcome stays typed if the admission-condition arms widen; the member rides the same merge prerequisite, **registered 2026-08-25** as a pair of class-scoped fragments — `RollbackAppliedResendOutcome` admitting only `'admitted'`, `RollbackDegradedResendOutcome` only `'unapplied'` — each intersected with its own terminal state-split arm, which leaves the round-5 state-scoping intact and **encodes** that state-determined mapping in the arm types instead of asserting it in prose, on the round-5 precedent; a widening — an `applied` composite whose resend was unapplied — is a named future amendment) — `RollbackInterventionResult` is their union, each terminal arm intersected with its own class-scoped resend fragment; a disposition-less terminal rollback response fails parse (test), and so does a state/disposition mismatch (`applied` carrying `files-unrestored`; test) — while `requested` / `accepted` / `rejected` / `expired` rollback responses carry no `result`; a `rejected` outcome REQUIRES `rejectionReason` (Codex round 5 — the machine-readable cause is mandatory on the rollback `rejected` arm, `driver.capability_unsupported` foremost with every `Queue And Intervention Model §Intervention State Transition Table` refusal family included, T2.5 authorization refusals too; the field stays optional on every other state) so the static refusal rides the lifecycle response, not the `JsonRpcError` channel; that same `rejected` arm additionally declares the **additive-optional** closed-union `rejectionGuard` (`'no-active-turn'` | `'no-pending-send'` | `'participant-authored-target'` | `'resumable-target'` — 2026-09-06 typed-composite-guard amendment), **arm-scoped rather than base-scoped** so a strict parse REFUSES it on a `steer` / `interrupt` / `cancel` rejection and on every non-`rejected` state (only a `rollback` request can be a composite, and only a `rejected` composite settles on one of these guards), and within the arm **schema-optional and producer-obligated** for the reason `resendDisposition` already is — no member of a `rejected` response identifies its request as composite — with T3.17 carrying the obligation and an unknown literal failing parse (test), so a renderer maps guard → remedy off a typed value by an exhaustive switch instead of matching `rejectionReason`, which stays the unchanged machine-readable cause and whose vocabulary no contract enumerates; no error code is minted, these guards being `rejected` **results** rather than `JsonRpcError` envelopes; the 6 `InterventionState` values are unchanged, and non-rollback results omit the disposition)
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionResult 6-state + RunStateChange event)
  - **Verifies invariant:** I-004-2
  - **Consumes:** `InterventionId` / `RunId`, `InterventionState` (requested / accepted / applied / rejected / degraded / expired), `RunState` (9-state), `RunFailureCategory` (**import** from the Plan-001 / Spec-006 taxonomy — do not redefine). The `RollbackInterventionResult` `disposition` vocabulary is Plan-004-owned rollback-outcome content (semantics in T3.13; `files-partially-restored` is never collapsed into `files-unrestored`, per [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) T5.2).
- **T1.4 — Migration: `queue_items` + `interventions` tables**
  - **Files:** `packages/runtime-daemon/src/migrations/00NN-queue-and-interventions.ts` (CREATE)
  - **Spec coverage:** Spec-004 §State And Data Implications (queue items durable storage + intervention audit records)
  - **Verifies invariant:** I-004-1, I-004-2, I-004-3, I-004-4, I-004-22, I-004-23
  - **Consumes:** local SQLite migration runner (Plan-001 substrate)
  - **Note:** the `interventions` DDL matches the canonical `local-sqlite-schema.md` block — including the nullable `rejection_reason` column (campaign B9, Codex round 4): the durable machine-readable cause of a `rejected` outcome (`driver.capability_unsupported` foremost), written at T3.12's refusal path and read back on idempotent replay so the reconstructed response carries the same `rejectionReason` — the wire contract forbids `result` on `rejected` (T1.3 state-split), so the cause needs its own column, never a value fabricated at replay time. The 2026-08-16 rewind-hardening amendment adds two further columns to this task's CREATEs, on the same T1.4-is-the-CREATE-owner basis as `rejection_reason` (no shipped migration exists to alter — T1.4 is unshipped): the `interventions` participant-keyed PII pair `pii_payload` / `pii_participant_id` (the round-1 fold — the composite's at-rest `replacementSend` envelope, I-004-21) and the nullable `queue_items.target_run_id` (the round-2 fold — the run-bound admission arm: stamped solely by T3.17's composite admission in V1, read by T3.5's resume-time run-bound drain, NULL on every ordinary follow-up item, whose admission continues to mean conversion into a new run). The 2026-08-18 admitting-principal carrier and queue-PII envelope amendment adds five more columns and one table-level constraint on that same basis: on `interventions`, `origin` (`TEXT NOT NULL`, `CHECK(origin IN ('participant', 'system'))` — **no DEFAULT by design**, so an unstamped insert fails rather than defaulting into the principal-free arm) and the nullable `admitting_principal_id`, plus the table-level `CHECK` binding the two together (principal present iff `origin = 'participant'`) — the biconditional I-004-22 asserts, enforced by the database rather than by convention; and on `queue_items`, the participant-keyed PII pair `pii_payload` / `pii_participant_id` (the participant-authored body's at-rest envelope — I-004-23) plus the nullable `admitting_intervention_id` (the row-anchored linkage T3.5's drain resolves an item's admitting intervention through — a run accumulates interventions over its lifetime, so the association is durable on the row and never inferred from run history). The DDL matches the canonical `local-sqlite-schema.md` block for both tables, the `queue_items` stamp deliberately **unindexed**, matching its `interventions` sibling — the Plan-022 Path-1 selector is a maintenance scan, not a hot path. **The unshipped-CREATE basis quoted above expired on 2026-09-01:** this task shipped as `0015-queue-and-interventions.ts` (PR #402), so every later column on either table is an EXTEND of a shipped statement rather than an edit of this CREATE — the 2026-09-06 typed-composite-guard amendment's `interventions.rejection_guard` being the first, owned by T3.17 and landing by its own next-ordinal migration (`packages/runtime-daemon/src/migrations/00NN-…`, the ordinal resolved at that task's own PR, the Plan-006 `content_payload` placeholder idiom). The canonical DDL stays the `local-sqlite-schema.md` block for both tables, and this task's shipped file is never amended to carry it.
- **T1.5 — Migration: `command_receipts` table (forward-declared shell)**
  - **Files:** `packages/runtime-daemon/src/migrations/00NN-queue-and-interventions.ts` (EXTEND)
  - **Spec coverage:** none (forward-declared shell — column semantics + read model owned by Plan-015, no fabricated spec anchor per anti-fabrication rule)
  - **Verifies invariant:** none (structural shell only — see CP-004-2)
  - **Consumes:** local SQLite migration runner
- **T1.6 — `RunPauseRequest` / `RunResumeRequest` + `RunControlAck` (client-facing pause/resume trigger)**
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (pause/resume as orchestration-layer triggers)
  - **Verifies invariant:** I-004-6, I-004-7
  - **Consumes:** `RunId`, `RunState`. Authors the two request types `RunPauseRequest { targetRunId: RunId; expectedRunVersion: number }` and `RunResumeRequest { targetRunId: RunId; expectedRunVersion: number }` plus the shared `RunControlAck { runId: RunId; currentState: RunState; runVersion: number }`, matching `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` byte-for-byte. Authored as **separate request types** (not an `InterventionType` member) because `pause` / `resume` are absent from `InterventionType` (`steer | interrupt | cancel | rollback`, campaign B2) by design (orchestration-layer, ADR-011) — the client needs a typed trigger distinct from `applyIntervention`. The **mandatory** `expectedRunVersion` carries the stale-replay guard (I-004-7; D-004-2 as extended to pause/resume).
- **T1.7 — Run-read accessor `getRun(runId): { version, sessionId, state }`**
  - **Files:** `packages/contracts/src/runControl.ts` (accessor contract, CREATE) + `packages/runtime-daemon/src/session/run-engine.ts` (forward-ref only — the `getRun` engine-side read lands in Phase 3 when Plan-004 CREATEs the file at T3.1, CP-004-6; Phase 1 authors only the accessor contract)
  - **Spec coverage:** Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` run-state payload — {sessionId, runId, previousState, newState}, event-sourced run state, no standalone runs table); ADR-001; ADR-017
  - **Verifies invariant:** I-004-7 (provides the version comparand for the stale-replay guard) — = CP-004-7 forward-provide
  - **Consumes:** `RunId`, `RunState`. `sessionId` + `state` are derivable from the `Spec-006 §Run Lifecycle (run_lifecycle)` projection. **`version` is the any-run-progression counter ratified as D-004-1** — this task authors the accessor _shape_; the `version` _derivation_ follows the ratified D-004-1 semantics.

### Phase 2 — Queue Admission and Serialized Intervention Application

**Goal:** Daemon-owned, serialized queue admission and the generic `applyIntervention` dispatcher with durable per-outcome records.

**Precondition:** Phase 1 contracts + migrations landed; D-004-1 ratified (T2.8 guard implements the any-run-progression version semantics); Plan-006 Phase 1 merged (the `user.message` literal + union-registration seam T2.9's payload variant registers through — campaign B18).

```yaml
preconditions:
  # Machine-enforced form of the prose line above (preflight Gate 5).
  - { type: plan_phase, plan: 004, phase: 1, status: merged }
  # Campaign B18: T2.9 registers the user.message SessionEventSchema payload
  # variant against the Plan-006 union-registration seam and stamps rows with
  # the registered literal — both exist only once Plan-006 Phase 1 (T1.10's
  # census closure) has fully shipped. Tier order already sequences this; the
  # entry makes it machine-checked rather than assumed (the CP-004-12 pattern).
  - { type: plan_phase, plan: 006, phase: 1, status: merged }
  # Codex PR #299 round 2 (2026-08-08): T2.5's Action::"intervene" adjudication
  # consumes the daemon-side node-owner principal binding and the durable
  # per-run hosting-node carrier, and its remote arm presumes the run-control
  # dispatch capability registration — three lead-owned amendments not yet
  # landed (the CP-004-12 two-layer pattern; the boxes live in §Preconditions).
  - { type: precondition_box_checked, box: "Local node-owner principal binding registered" }
  - { type: precondition_box_checked, box: "Per-run hosting-node carrier registered" }
  - { type: precondition_box_checked, box: "Cross-node run-control dispatch capability registered" }
```

#### Tasks

- **T2.1 (Q1) — Queue admission gate (fail-closed when persistence unavailable)**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Fallback Behavior (reject new queue creation when persistence unavailable); ADR-003 §Failure Mode Analysis (block new queued work when persistence unavailable)
  - **Verifies invariant:** I-004-1
  - **Consumes:** QueueItem contracts (T1.1); error code `queue.persistence_unavailable` (F-004-2-07 — register in `error-contracts.md`)
- **T2.2 (Q2) — FIFO admission (`created_at ASC`; priority excluded)**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Default Behavior (FIFO admission, created_at ASC); Spec-004 §Resolved Questions and V1 Scope Decisions (queue priority deferred — excluded from ordering)
  - **Verifies invariant:** I-004-5
  - **Consumes:** QueueItem store (T2.1)
- **T2.3 (Q3) — Pre-admission cancellation + supersede / expire transitions**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (queue items support cancellation before admission; supersede / expire transitions)
  - **Verifies invariant:** I-004-6
  - **Consumes:** `QueueItemState` (T1.1)
- **T2.4 (I1) — `applyIntervention` generic dispatch (ADR-011)**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Required Behavior (generic intervention dispatch; the admitting-principal-and-origin stamp every accepted intervention records — 2026-08-18 amendment); ADR-011 (applyIntervention(type, payload) generic dispatcher)
  - **Verifies invariant:** I-004-3 (1:1:1 request:dispatch:record), I-004-22
  - **Consumes:** `InterventionRequestPayload` (T1.2); the daemon's transport-resolved caller identity — the same resolution T2.5's authorization reads, never a second derivation
  - **Behavior (authored, 2026-08-18 amendment):** the record-creating path stamps `origin` and, on the participant arm, `admitting_principal_id` onto the durable `interventions` row **in the same write that creates it** — so the stamp is universal to every intervention rather than a private path any one type carries, and no code path can produce an intervention record lacking it. The principal is **daemon-resolved from the transport**, following the shipped `approval_resolutions.approver_id` precedent (D-012-12) exactly: the node-owner participant binding on the local socket, the verified PASETO `sub` on identity-carrying transports, and `caller_token.sub` on the Spec-024 cross-node arm. Any principal identifier arriving on the wire is treated as informational and **cross-checked, never trusted**: a mismatch against the daemon's own resolution refuses with the existing `auth.principal_mismatch` (no new error code — the reuse is deliberate, since this is the same class of caller-asserts-vs-transport-proves conflict that error already names). An unresolvable principal on the participant arm is a refusal, never a NULL stamp — the fail-closed direction, and the reason `origin` carries no DEFAULT. System-originated interventions (Plan-016's budget / idle / moderation interrupts, entering in-process below the wire authorization boundary per CP-004-10) stamp `origin = 'system'` with a NULL principal, which is exactly what the table-level CHECK admits on that arm. `initiator_id` keeps its existing routing-and-audit role and is never read as an authorization input (I-004-8, unchanged). **Tests:** an accepted intervention on each transport stamps the principal its transport resolves and never the wire-supplied value (asserted on the raw row); a wire-supplied principal contradicting the resolved one refuses `auth.principal_mismatch` and creates no row; a system-originated intervention stamps `origin = 'system'` with a NULL principal; an insert attempting `origin = 'participant'` with a NULL principal — or a NULL `origin` — is rejected by the database constraint, not merely by application code.
- **T2.5 (I2) — Authorization (Cedar principal = verified PASETO `sub`)**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (Cedar principal = verified PASETO sub; initiatorId informational only; the Cedar action is intervene on the target Run resource with the run's hosting node carried in the request context; authorization evaluates against session membership role, never run authorship; a non-local hosting node adds the target-node-owner approval requirement rather than substituting for the role check); Spec-012 §Required Behavior (run-control interventions authorize on session write access, bounded by the node-trust rule); ADR-012 (Cedar); ADR-010 (PASETO sub)
  - **Verifies invariant:** I-004-8, I-004-19
  - **Consumes:** the caller's verified participant identity — verified PASETO claims (from the Plan-001 / Plan-008 auth context) on identity-carrying transports, and the daemon-resolved node-owner principal binding on the local socket (§Preconditions' provider-seam box; no PASETO exists on that transport); client `initiatorId` is informational only; the target run's hosting-node identity as Cedar request context; and — for a non-local hosting node — the [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) target-node-owner approval leg in the `tool_execution` category (consumed, never authored here). Neither `initiatorId`, the run's creating participant, nor the target agent's owner is read as an authorization input on this path.
- **T2.6 (I3) — Map `DriverInterventionResult` {`applied` | `degraded`} → 6-state**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionResult 6-state mapping from DriverInterventionResult)
  - **Verifies invariant:** I-004-2
  - **Consumes:** `DriverInterventionResult` (Plan-005 driver contract — CP-004-1), `InterventionState` (T1.3)
- **T2.7 (I4) — Steer-no-capability degrades to queue + interrupt; durable audit every path**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Driver-Level Steer Mechanics (steer-no-capability degrades to queue + interrupt, durable audit every path; Plan-005 capability checks)
  - **Verifies invariant:** I-004-2
  - **Consumes:** driver capability flags (Plan-005 — CP-004-1)
- **T2.8 (I5) — Stale-replay version guard + per-outcome event**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (expectedRunVersion stale-replay guard, mandatory fail-closed)
  - **Verifies invariant:** I-004-7
  - **Consumes:** T1.7 `getRun` accessor (reads current run version)
  - **Behavior (authored):** read current run version via `getRun(targetRunId)`; if `expectedRunVersion !== current` → transition `requested → expired`, apply nothing, and emit an `intervention.*` event `{ sessionId, interventionId, targetRunId, type, state, actor }`. The guard rejects **any** mismatch — stale (older) _or_ anomalous-future — per `Spec-004 §Interfaces And Contracts` ("a version guard mismatch produces `expired`"); a `<`-only comparison would silently admit a future comparand no honest client can hold. **Comparand evaluated against the any-run-progression version semantics (D-004-1).** The absent-`expectedRunVersion` case is rejected (fail-closed) per D-004-2 (mandatory).
- **T2.9 (Q4) — `user.message` durable row: payload variant + admission-time emitter** (campaign B18 — closes the Spec-006 ownership assignment: emission belongs to this plan's queue-submit surface, and no task carried it)
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND — `UserMessageEventPayloadSchema`) + `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND — the admission-time append)
  - **Spec coverage:** Spec-006 §User Message Events (the durable user-message row; the accepting daemon appends it when it accepts the send; the `user_text` wire echo correlates as delivery confirmation and never appends a second row)
  - **Verifies invariant:** I-004-18, I-004-23
  - **Consumes:** the `user.message` `SessionEventType` literal + the Plan-006 union-registration seam (`packages/contracts/src/event.ts` — Plan-006-owned; this task registers the variant through the seam, never edits Plan-006's registry symbols; gated on Plan-006 Phase 1 by this phase's `preconditions:` entry); the Plan-022 call-site splitter (`splitPii` → `pii_payload`) for `message`; the T2.1 queue store's admission transaction.
  - **Behavior (authored, campaign B18):** (a) contracts — author `UserMessageEventPayloadSchema`, the [Spec-006 §User Message Events](../specs/006-session-event-taxonomy-and-audit-log.md#user-message-events) shape `{sessionId, queueItemId?, runId?, channelId?, actor, message}`, and register it as the `user.message` `SessionEventSchema` payload variant through the Plan-006 union-registration seam (the emitter-authors-payload precedent — the Plan-012 `ApprovalFlowEventPayloadSchema` shape; Plan-006 T1.10 registers literal/category/disposition only, and its flip-≠-emission rule holds `user.message` emission until this variant lands). (b) emitter — the queue-admission accept path appends **exactly one** `user.message` row atomically with the accepted queue item's persistence (one durable unit: an acknowledged send is never admitted without its timeline row; a rejected admission appends nothing), with `message` routed through the Plan-022 call-site splitter (`pii_payload`; the plaintext payload keeps only the non-PII identifiers) and the envelope's `correlation_id` stamped so the provider-side `user_text` echo folds into this row as delivery confirmation (the row-31 correlate disposition — Plan-006-owned registry data; the echo-side handling is the Plan-005 normalizer's disposition consumption, not this task). **(c) queue-row envelope (2026-08-18 amendment, I-004-23)** — the queue row this same transaction persists carries the body under its own copy of the participant-keyed envelope: `message` is routed through the identical `splitPii` call-site into `queue_items.pii_payload` with `pii_participant_id` stamped to the sending participant, and the plaintext `queue_items.payload` retains only non-PII members. This costs no queryability, because every field the drain and the admission ordering select on — `state`, `priority`, `target_run_id`, `channel_id`, `session_id`, `created_at` — is already its own column, and the payload was never the selector. The two rows encrypt under the **same** participant DEK, so one send resting in two durable places is still shredded by the one Path-1 key deletion (`Spec-022 §Shred Fan-Out`), and the queue row needs no DELETE — which matters because queue rows are durable forever and are deliberately never deleted on drain, so without the envelope a participant's send bodies would outlive every run that consumed them in plaintext. Orchestration-authored queue items (Plan-016's in-process admissions, CP-004-10) have no participant DEK to encrypt under and therefore carry **both** PII columns NULL with their content in plaintext `payload` — the same split-not-replace shape `interventions` already takes, and the reason this is an envelope beside the column rather than a replacement of it. The queue row's posture is otherwise unchanged: durable, never deleted on drain, and never evented. Tests: an accepted send appends exactly one `user.message` row with pii-routed `message` and a stamped `correlation_id`; a failed admission (persistence unavailable, I-004-1) appends nothing; the payload variant round-trips append/replay validation; a participant-authored queue row carries its body in `pii_payload` with `pii_participant_id` stamped and no body in plaintext `payload` (asserted on the raw row, both directions), decrypting to the same plaintext as its paired event row under the one key; an orchestration-authored queue item carries both PII columns NULL and its content in plaintext `payload`; and the drain path reads the body back through the envelope, so a queue row whose participant key has been shredded fails the drain closed rather than delivering an empty send.

### Phase 3 — Run-Engine Orchestration (Interrupt / Pause / Resume / Recover)

**Goal:** Wire intervention outcomes into the 9-state run machine; compose pause/resume as orchestration (not a driver capability); deterministic restart recovery. **Campaign B9 adds:** the P1-2 terminal-emitter enforcement detail (T3.7), P1-8 late-event absorption (T3.11), and the driver-input-ask expiry park leg (T3.15). **The rollback time-travel cluster — campaign B9's T3.12–T3.14, the 2026-08-16 rewind-hardening amendment's T3.16 / T3.17, and the T4.7 CLI surface — ships as [Phase 3B](#phase-3b--rollback-time-travel-cluster-explicit-label-supplement) below** (re-homed 2026-08-17, Codex PR #346 round 1: the cluster's Gate-5 boxes — checkout-root foremost — gate on Plan-010 Phase 3, which itself gates on this phase's T3.10 seam per CP-010-9, so carrying them phase-scoped here was a mechanical two-plan deadlock; the Plan-024 Phase-3B / Plan-017 Phase-2B supplement shape).

**Precondition:** Phase 2 intervention service landed. **Plan-004 CREATEs `packages/runtime-daemon/src/session/run-engine.ts` at T3.1 (CP-004-6 — Plan-004 is the file's first author; the `session/` directory is Plan-001-owned, this file crosses per the §2 per-file precedent); T3.2–T3.17 EXTEND it — the Phase-3B cluster's tasks included: one file, two dispatch units.**

```yaml
preconditions:
  # Machine-enforced form of the prose line above (preflight Gate 5).
  - { type: plan_phase, plan: 004, phase: 2, status: merged }
  # This phase's driver-facing tasks (T3.9's steer routing, T3.10's
  # `driver.startRun` admission) type against the ProviderDriver contract that
  # Plan-005 Phase 1 owns, and that phase is re-opened (campaign B10 lands the
  # four-op widening as its T1.8 catch-up task), so declared-⊆-shipped makes
  # this entry resolve false until the re-opened Phase 1 has fully shipped.
  # The rollback-specific T1.8 symbol consumption (`rollbackTo` /
  # `DriverRollbackResult`, consumed by T3.12/T3.13) moved to Phase 3B with
  # those tasks — covered there transitively through its plan_phase chain,
  # since this entry gates the Phase-3 merge that chain requires.
  - { type: plan_phase, plan: 005, phase: 1, status: merged }
  # CP-004-12: the Plan-006-owned typed epoch-attribution field (T3.11 writes
  # it here; T3.14 — Phase 3B — consumes it) is a named merge prerequisite — registered 2026-07-20
  # as Plan-006 T1.9's sourceEpoch+sourcePosition carrier pair; prose alone was Gate-5-invisible
  # (Codex round 5). Two-layer
  # gate: the box fails until the lead-owned Plan-006/Spec-006 amendment lands
  # (that PR checks it — the Part-B precedent), and the plan_phase entry then
  # holds dispatch until the re-opened Plan-006 Phase 1 (the carrier's
  # contracts phase) has fully shipped — declared-⊆-shipped closes the window
  # where an already-shipped Phase 1 would satisfy the entry pre-amendment.
  - { type: precondition_box_checked, box: "Plan-006 epoch-attribution carrier registered" }
  - { type: plan_phase, plan: 006, phase: 1, status: merged }
  # Codex PR #299 round 2 (2026-08-08): T3.4 / T3.5's run.pause / run.resume
  # handlers evaluate the identical Action::"intervene" request, consuming the
  # same three seams T2.5 does (§Preconditions boxes; Phase-2 entries mirror).
  - { type: precondition_box_checked, box: "Local node-owner principal binding registered" }
  - { type: precondition_box_checked, box: "Per-run hosting-node carrier registered" }
  - { type: precondition_box_checked, box: "Cross-node run-control dispatch capability registered" }
  # T3.7's terminal emitter imports `withSessionAppendLock` from Plan-006 T3.1's
  # dependency-leaf `session-append-lock.ts` — a Phase-3 deliverable, so the
  # Phase-1 entry above alone leaves the lock module absent at dispatch.
  # Registered by the Plan-006 T3.1-seam targeted readiness-audit delta
  # (F-006-HALT-06, PR #282, 2026-08-02) — the Plan-024 Phase-3B CP-024-5
  # sibling shape.
  - { type: plan_phase, plan: 006, phase: 3, status: merged }
  - { type: precondition_box_checked, box: "Driver input-ask park leg authored" }
  # The four rollback-cluster boxes (partial-restore enumerations, replacement-
  # send wire arm, snapshot-callee checkout-root, admitting-principal carrier)
  # moved to Phase 3B with the tasks that read their surfaces (Codex PR #346
  # round 1) — this phase's entries hold none of them, so Plan-010 Phase 3's
  # reciprocal { plan: 004, phase: 3 } gate resolves on this phase's merge.
```

#### Tasks

- **T3.1 (INTERRUPT) — Interrupt application** (`running → interrupted`)
  - **Files:** `packages/runtime-daemon/src/session/run-engine.ts` (**CREATE** — Plan-004 is this file's first author, CP-004-6; T3.2–T3.17 EXTEND it)
  - **Spec coverage:** Spec-004 §Required Behavior (interrupt transitions run directly to interrupted); Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` taxonomy)
  - **Verifies invariant:** I-004-6
- **T3.2 (CANCEL) — Cancel application** (`running → interrupted`)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (cancel intervention → interrupted run-terminal)
  - **Verifies invariant:** I-004-6
  - **Note:** the cancel intervention maps the active run to the canonical `interrupted` run-terminal — cancel is a user-initiated interruption, distinct from T3.1's `interrupt` in actor/intent but **not** in run-terminal (per [run-state-machine.md](../domain/run-state-machine.md): "the cancel intervention maps to the `interrupted` terminal state … distinct from queue-level `QueueItemCancel`"; the run terminals are `completed` / `interrupted` / `failed` — there is no `canceled` run-state). `canceled` is a **`QueueItemState`** (queued / admitted / superseded / canceled / expired; T1.1), applied to queued items by the `QueueItemCancel` path (T2.3), never a run-terminal — this task transitions only the run.
- **T3.3 (WAIT-GUARD) — `waiting` / `blocked` guard (target_run_id stable)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §State And Data Implications (waiting / blocked stays on the same run id — target_run_id stable)
  - **Verifies invariant:** I-004-4
- **T3.4 (PAUSE) — Pause orchestration** (compose: interrupt + persist resumable state + enqueue resume marker — **not** driver-dependent)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (pause composes interrupt + persist + queue resume, orchestration-layer); Spec-004 §Interfaces And Contracts (pause/resume route through the same intervene adjudication); ADR-011 (orchestration dispatch, not a driver capability)
  - **Verifies invariant:** I-004-6, I-004-7, I-004-19
  - **Consumes:** T1.7 `getRun` accessor (reads current run version)
  - **Behavior (authored):** adjudicate the caller first — the identical `Action::"intervene"` evaluation the intervention path runs (role axis + the non-local node-owner approval leg, I-004-19; `Spec-012 §Required Behavior`'s governed-identically rule), rejecting the request untouched on deny — then apply the **same** stale-replay version guard as T2.8 before composing the pause — read current run version via `getRun(RunPauseRequest.targetRunId)`; reject the request untouched if `RunPauseRequest.expectedRunVersion !== current` (`Spec-004 §Interfaces And Contracts`, mandatory per D-004-2 as extended to pause/resume); otherwise interrupt the active run, persist resumable state, enqueue the resume marker, and acknowledge via `RunControlAck { runId, currentState, runVersion }`.
- **T3.5 (RESUME) — Resume orchestration** (re-admit the **same** run id from persisted state)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (resume returns a paused run to active execution, same run id; the two-point classification's resume backstop and the run-bound replacement drain — 2026-08-16 amendment, round-2 fold); Spec-004 §Interfaces And Contracts (pause/resume route through the same intervene adjudication)
  - **Verifies invariant:** I-004-4, I-004-6, I-004-7, I-004-19, I-004-20, I-004-21, I-004-22, I-004-23
  - **Consumes:** T1.7 `getRun` accessor (reads current run version); T3.16's boundary classifier (the resume-time revalidation — reused, never re-derived); the `queue_items.target_run_id` run-bound rows and their `admitting_intervention_id` linkage (T1.4); the CP-006-1 `PiiEncryptor` seam (decrypting the drained row's `pii_payload` body — consumed, never authored)
  - **Behavior (authored):** adjudicate the caller first — the identical `Action::"intervene"` evaluation the intervention path runs (role axis + the non-local node-owner approval leg, I-004-19; `Spec-012 §Required Behavior`'s governed-identically rule), rejecting the request untouched on deny — then apply the **same** stale-replay version guard as T2.8 before composing the resume — reject the request untouched if `RunResumeRequest.expectedRunVersion !== current` (`Spec-004 §Interfaces And Contracts`, mandatory per D-004-2 as extended to pause/resume); otherwise re-admit the **same** run id from persisted state and acknowledge via `RunControlAck { runId, currentState, runVersion }`. **Two rewind-hardening legs (2026-08-16 amendment, round-2 fold):** (a) **boundary backstop** — before re-admission, revalidate position-vs-boundary via T3.16's classifier: a run whose current position sits strictly below its newest **current** `usage.context_compacted` boundary refuses resume fail-closed with the typed `run.compaction_boundary_diverged` (409, run state unchanged; registered beside `run.execution_root_released` in `error-contracts.md §Run` — the same standing-refusal family), the backstop for a boundary row whose content-asynchronous delivery lagged past T3.13's settlement reclassification (I-004-20) — a boundary-diverged run is non-resumable in V1, per `Spec-004 §Required Behavior`'s two-point classification paragraph — and the classification executes inside the same session-append-lock critical section that commits the resume transition: boundary rows are session events appending under that same lock, so none can interleave between classification and re-admission, and one appending after it is post-resume by total append order; (b) **run-bound drain** — after re-admitting the run, deliver its run-bound queued items (`queue_items.target_run_id` = this run, state `queued`, `created_at ASC`) into the run as its next provider sends, never converting them into new runs — in V1 at most one such item exists (the composite's replacement; guard (ii) refused while any older send was pending), so the drain is exactly the resend-next delivery I-004-21 promises. **Principal and body resolution at drain (2026-08-18 amendment):** the drained turn executes under the **admitting intervention caller's** effective principal, which the drain reads by following the row's own `admitting_intervention_id` to that intervention's `admitting_principal_id` — a two-hop row read, never an inference from run history (a run accumulates interventions across its lifetime, and "the most recent one" is not a correct answer to "which one admitted this item"). A run-bound item whose linkage does not resolve, or whose admitting intervention carries no principal, **fails the drain closed** — the item stays `queued` and the resume refuses — rather than falling back to the run's initiator, which is precisely the substitution I-004-22 exists to forbid. The body is read back through the participant-keyed envelope (`queue_items.pii_payload` under `pii_participant_id`'s key), so a drained row whose participant key has been shredded likewise fails closed rather than delivering an empty send. The drained item leaves queue-state `queued` for the run-bound arm of `admitted` only when its send's delivery confirmation folds in (the provider `user_text` echo window T3.17's guard (ii) names — the same per-send confirmation discipline every T2.9 send rides); a crash short of that confirmation leaves the row `queued`, and the next `run.resume` drain re-delivers under that existing at-least-once machinery rather than any new protocol. **Tests:** a resume against a run whose current position sits below a late-delivered current boundary refuses `run.compaction_boundary_diverged` and mutates nothing, while the same run with that boundary marked superseded by an accepted rewind resumes normally; a resume of a rewound run holding a run-bound replacement delivers it as the run's next provider send (asserted on the provider-bound send order), the raw row transitioning `queued → admitted` only at delivery confirmation, and never mints a new run for it; and an unbound queued item on the same session still converts through ordinary admission, unaffected by the drain. The 2026-08-18 amendment adds: a drained run-bound item executes under the principal stamped on the intervention its `admitting_intervention_id` names — asserted against a run whose history holds a **later** intervention by a different participant, so that resolving by recency rather than by linkage would pick the wrong principal and fail the test; a run-bound item whose linkage resolves to no intervention, and one whose admitting intervention carries a NULL principal, each leave the item `queued` and refuse the resume rather than falling back to the run initiator; and a drained item whose participant key has been shredded fails closed rather than delivering an empty send.
- **T3.6 (RECOVER) — Deterministic restart recovery** (distinguish `failed` vs `interrupted` on daemon restart)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Fallback Behavior (deterministic restart recovery — distinguish failed vs interrupted); Spec-006 §Replay Interaction with Compacted Regions (replay on daemon restart)
  - **Verifies invariant:** I-004-2, I-004-6
- **T3.7 (EMIT) — Emit `run.*` state-change events + terminal exactly-once emitter** (Spec-006 `run_lifecycle` taxonomy; campaign B9 P1-2 detail)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` state-change event payload + the at-most-once terminal emission per `(runId, runVersion)` — the emitter contract)
  - **Verifies invariant:** I-004-2, I-004-13
  - **Behavior (authored, campaign B9 P1-2):** the terminal `run_lifecycle` emit (`run.completed` / `run.failed` / `run.interrupted`) is the **primary** at-most-once-per-`(runId, runVersion)` guard. It runs a **guard → swap → append** sequence wrapped in `withSessionAppendLock(sessionId, critical)` — the per-session append lock [Plan-006](./006-session-event-taxonomy-and-audit-log.md) T3.1 publishes from its dependency-leaf `session-append-lock.ts` (re-homed off `event-log-service.ts` by the PR #281 ingest-halt amendment, Codex round 3, for eager-ESM-cycle freedom), owner-scoped reentrant via `AsyncLocalStorage` so the `append()` issued inside the critical section reuses the held token and never self-deadlocks. Under the lock: read-your-writes current run state via `getRun(runId)` (T1.7), verify no terminal row already exists for this `(runId, runVersion)` epoch, then append the terminal event — so the check-and-append is atomic against concurrent same-session appends. The terminal payload MUST carry non-null `runId` / `runVersion` (the emitter precondition). **Backstop-index interplay:** Plan-006's partial-unique `idx_session_events_run_terminal_once` + the `trg_run_terminal_key_insert` / `trg_run_terminal_key_update` / `trg_run_terminal_key_promote` trigger trio are the **schema-level backstop** behind this guard (a duplicate terminal, a NULL / storage-class-drifted key, or a non-terminal row re-typed into the terminal set fails loud at the DB layer) — never a replacement for the emitter, which is the primary enforcement; the schema work is defense-in-depth (`Spec-006 §Run Lifecycle (run_lifecycle)`).
  - **Rejected alternatives (carried from campaign B1 / design §9 BP-7):** a generic silent-no-op `appendIfAbsent(...)` API stays **rejected** — the corpus enforces terminal exactly-once with a fail-loud emitter guard + fail-loud schema backstop, not a silent absorb-if-present primitive. A projection-level deterministic identity `run-terminal:<runId>:<runVersion>` stays **SECONDARY** — the emitter guard + schema backstop are primary; a deterministic-id dedupe would at best restate that guarantee at the projection layer and is not authored as the enforcement mechanism.
- **T3.8 (MAP) — Map intervention outcomes → run-state transitions**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (intervention outcome → run-state mapping)
  - **Verifies invariant:** I-004-6
- **T3.9 (STEER-ROUTE) — Route steer to driver (or degrade per T2.7)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Driver-Level Steer Mechanics (route steer to driver, or degrade per T2.7; Plan-005 capability)
  - **Verifies invariant:** I-004-2
- **T3.10 (SETUP-GATE) — Run-setup gate seam: ordered pre-start gates + terminal release hooks (Tier-6 audit)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-010 §Fallback Behavior (blocked-in-setup parks the run in `starting`, interruptible) + run-state-machine.md `starting` rows (`starting -> running` fires only after gates pass; `starting -> interrupted` on user stop while parked)
  - **Verifies invariant:** I-004-12
  - Note: `RunSetupGate` interface `{ assertRunReady(context): Promise<void>; onRunTerminal?(context): Promise<void> }` with an ordered gate array on the run-engine; context carries `{ runId, sessionId, queueItem }` (incl. the T1.1 `workspaceId?`). `queued -> starting` admits the run, then gates execute in registration order BEFORE `driver.startRun`; a gate throw parks the run in `starting` with the typed error surfaced (no `running` transition fires; interrupt/cancel per T3.1/T3.2 applies to parked runs). `onRunTerminal` hooks fire in reverse registration order when the run reaches any terminal state. Plan-004 ships the SEAM with zero registered gates — generic, no Plan-010 knowledge; Plan-010's execution-root gate registers at Tier 6 (Plan-010 D-010-16, reciprocal CP-010-9 — CP-004-8). Tests: gate registration order; gate-throw parks in `starting` with the typed error; zero-gate pass-through; reverse-order terminal hooks fire exactly once (per `runVersion` epoch — campaign B2); interrupt-while-parked transitions to `interrupted`.
- **T3.11 (LATE-ABSORB) — Post-interrupt / post-rollback late-event absorption** (campaign B9, P1-8)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (post-interrupt late-event absorption; execution-epoch keying); Spec-006 §Run Lifecycle (run_lifecycle) (terminal-class straggler routes into the at-most-once terminal emitter)
  - **Verifies invariant:** I-004-14
  - **Behavior (authored):** at the run-engine inbound dispatch, a provider **run-lifecycle** event for a run already in an absorbing terminal state — or one the per-binding epoch cursor attributes to a **pre-rollback execution epoch** — is **absorbed, never appended** — and the two absorption conditions route differently (Codex round 6): an already-terminal run's terminal-class straggler routes into the T3.7 terminal emitter and no-ops there (the guard finds the `(runId, runVersion)` terminal already present), but a **pre-rollback-epoch straggler — terminal-class included — is absorbed at the epoch check itself, before the terminal emitter**, because post-rollback the reopened run's current `runVersion` carries no terminal row, so the emitter's duplicate guard alone would append the stale terminal against the new epoch and re-terminalize the rewound run; a non-terminal lifecycle straggler is dropped from the lifecycle path. Every absorbed straggler is recorded only as a `run.late_event.absorbed` OTel diagnostic **non-event** (never a `session_events` row), with no state transition and no `runVersion` advance. The absorb decision runs inside `withSessionAppendLock` so the terminal-check and the absorb see one consistent state. Non-lifecycle families (usage telemetry foremost) append normally under their own rules — a pre-rollback-epoch non-lifecycle row appends **carrying the durable source-epoch stamp** (the typed epoch-attribution pair on the append surface — **Plan-006 T1.9's `sourceEpoch` + `sourcePosition` carrier per CP-004-12**, stamped together with the row's `runId` per the pairing rule, a **named merge prerequisite** gating this late-append leg; consumed by T3.14's supersede projection, which needs epoch, position, and run identity to rank the late row against its epoch's surviving prefix), so billable usage is never dropped. **Surface-opening exception (Codex round 5):** a pre-rollback-epoch `driver_ask.requested` (either kind) is not a passive row — appended open it would mint a pending interactive surface and could drive the rewound run toward `waiting_for_*` — so the late-append path records it as a **closed pair, atomic**: the epoch-stamped `driver_ask.requested` row and its `driver_ask.canceled` terminal (superseded pre-rollback-epoch cause; `Spec-006 §Driver Ask Events` terminal, no new event type) land under the same `withSessionAppendLock` append, no pending ask surface registers, no run-state transition fires, and a permission-kind ask is never handed to the approval-pipeline normalizer — the epoch check runs at inbound dispatch, before normalization, so Plan-012's normalizer is never invoked for a stale-epoch ask (no Plan-012 symbol authored). The same rule governs any future non-lifecycle family whose append opens a pending interactive surface. Epoch attribution is per-binding and needs no wire token: the per-binding cursor is an **(epoch, position) delivery pair** — the position component advances at each turn boundary observed in the binding's ordered delivery; on an in-place binding the driver's `rollbackTo` confirmation is the in-stream fence. The stamp's value source is the straggler's **operation association** (Codex rounds 4-5): the run engine records each provider-side operation — tool call, message item, artifact production — against the **(epoch, turn) current when it opened**, at open time in the ordered stream (pre-fence by construction), and the straggler's correlation key resolves that recorded pair — the epoch must ride the association because re-executed turns reuse ordinals (epoch-0's turn 7 and epoch-1's turn 7 are distinct operations distinguishable only by their recorded epochs). Delivery order bounds only the _emission_ turn, so a content-asynchronous straggler (a turn-4 tool's progress delivered at turn 10) must rank by its operation's opening pair — the closing delivery position is superseded-by-construction (a generation closes at the run's then-current position, which admission requires strictly above `targetPosition`), so stamping it on every straggler would vacate the surviving-prefix ranking `sourcePosition` exists for. Binding lineage (Codex round 5; **V1 instance re-derived 2026-08-26**): a fork-superseded binding freezes wholesale — every delivery pre-epoch at its retained pair — and after the Codex rewind rebinding to `thread/fork` (`Spec-004 §Driver-Level Rollback Mechanics`) **both V1 drivers are fork-superseded**, so this is the only arm V1 reaches. The generic rule's other arm — an **in-place** rollback **rotating the delivery generation at the fence**, the confirmation closing the pre-rollback generation (pair retained for straggler attribution) while post-fence deliveries open the new epoch at the confirmed floor, one provider binding spanning epochs with per-generation attribution and a post-fence delivery current unless its association resolves to a closed generation's operation — **has no V1 driver and is not built or tested here**. An association-less straggler takes the retained closed-generation pair, which in V1 is always the superseded fork binding (a hypothetical in-place binding's post-fence association-less delivery is current by the in-stream fence's total order): always superseded with its epoch, never resurfacing as current, its append and billability untouched. Never a read-time guess and never the post-rollback run's current position. For the **pause-first composite** the absorption window opens **earlier than the fence** — from the moment the composite issues its internal interrupt (T3.12), run-lifecycle events sourced from the interrupted execution are the pause's **expected echo**: consumed by the pause application exactly as the client pause path consumes its own interrupt's echo, or absorbed as above, never routed as spontaneous transitions — so a late `run.interrupted` arriving after the internal pause but before the `rollbackTo` confirmation cannot re-terminalize the mid-composite run or release its root (`Spec-004 §Required Behavior`). **Tests:** a late `run.interrupted` in the pause-first composite window (after the internal interrupt, before the `rollbackTo` confirmation) is absorbed as the pause's expected echo — the mid-composite run is neither re-terminalized nor has its execution root released; a pre-rollback-epoch `driver_ask.requested` late-appends as the closed pair (epoch-stamped `requested` + `canceled`), registers no pending ask surface, fires no `waiting_for_*` transition, and its permission-kind variant never reaches the approval-pipeline normalizer; a stale `run.completed` / `run.failed` attributed to the superseded epoch, arriving after the run reopened `paused`, is absorbed at the epoch check — it never reaches the terminal emitter and the run stays `paused` (live + replay); a stamped late row ranks by its association's opening turn on both sides of the cutoff — a straggler whose operation opened at-or-below the confirmed target stays **current** (the epoch's surviving prefix, never superseded) while one whose operation opened above it is marked superseded (via T3.14); **no in-place-binding generation-rotation assertions are authored** — after the 2026-08-26 Codex rebinding both V1 drivers fork-and-supersede, so a rotation test would exercise machinery no V1 driver instantiates; the reused-ordinal disambiguation it used to carry is covered on the fork path instead, where a re-executed same-ordinal operation's event appends current unstamped while its superseded-epoch twin stays stamped (the association's epoch, not the ordinal, is the discriminator); an association-less straggler on a superseded fork binding takes the retained pair and is superseded (the conservative fallback's negative control).
- **T3.15 (ASK-EXPIRY-PARK) — Driver input-ask expiry park leg** (campaign B9; Spec-012 Part-B fail-closed follow-up, 2026-07-17)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-012 §Resolved Questions and V1 Scope Decisions (input-kind ask expiry is park-resumable via `waiting_for_input → paused`; never auto-approve); Spec-006 §Driver Ask Events (input-kind asks route to the run's interactive surface, not the approval pipeline)
  - **Verifies invariant:** I-004-17
  - **Behavior (authored):** the run-engine handles `driver_ask.expired` for `kind: 'input'` asks by parking the run **`waiting_for_input → paused`** — the park edge Spec-012's Part-B amendment adds to `run-state-machine.md` (previously legal only for rollback) — driven by the daemon's ask-expiry path and **deliberately NOT** the CP-004-9 / CP-012-5 approval-outcome seam (that seam's expired outcomes continue-with-refusal back to `running`, correct only for approval-pipeline categories; a driver input ask never traverses the approval pipeline). No input is fabricated (fail-closed); the ask closes as expired and the run parks in its ordinary resumable `paused` state (resume re-engages the provider, which may re-raise the ask). **Replay-atomic:** the park transition commits as one unit with `driver_ask.expired` under `withSessionAppendLock`; daemon startup reconciliation derives a missing sibling effect from the persisted `(ask_id, expiry_at)` pair; a crash-after-first-effect test rides this task. This is the reciprocal of Plan-012's B13-authored T2.8 driver-ask-normalizer emission; the CP-004-9 expired→continue-with-refusal mapping stays approval-pipeline-only.

- **T3.19 (EFFECTIVE-PRINCIPAL) — Turn-scoped effective-principal resolution + publication** (2026-08-26 turn-scoped effective-principal amendment)
  - **Files:** `run-engine.ts` (EXTEND); `run-execution-context.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (the intervention-identity rule's 2026-08-26 publication clause); Spec-012 §Required Behavior (the turn-scoped resolution rule this task consumes and does not restate); Spec-006 §Usage Telemetry (usage_telemetry) (the effectivePrincipal member on usage.cost_update); Spec-016 §Session Cost Receipt (the per-caused-by partition the value keys)
  - **Verifies invariant:** I-004-25
  - **Behavior (authored):** at every turn boundary the run engine opens, it resolves the turn's effective principal and publishes it as an **immutable per-turn scope** on the run's execution context — the context accumulates scopes and never mutates a published one, and the turn's frame consumers are handed the scope bound when that turn's stream opened, so a late-arriving frame cannot read a newer turn's value: the admitting participant of the `interventions` row that opened the turn (a steer, or a rollback carrying `replacementSend`) — read from `admitting_principal_id` on the `participant` arm of that row's `origin` discriminator — otherwise the participant who started the run, and the `system` arm where neither exists because no participant caused the turn (a scheduled sweep, an idle-timeout settlement, a daemon-initiated recovery turn). Resolution is daemon-side and reads no wire field, so the standing informational-body-fields rule is unbroken and a body-supplied actor that disagrees refuses as the existing `auth.principal_mismatch` rather than being trusted. The value is **published, not emitted**: this task writes no event and mints no column — [Plan-005](./005-provider-driver-contract-and-capabilities.md)'s usage normalizer reads the scope **of the turn whose stream produced the frame** and stamps the metered row under CP-004-16 ⇄ CP-005-10, exactly as the account identity on `usage.rate_limit_update` is daemon-supplied onto a driver-sourced event. Bind-at-dispatch rather than read-at-emission is the contract, not an optimization: a `usage.cost_update` for turn A routinely lands after turn B has opened, and a normalizer reading a mutable current-turn accessor would bill A's spend to B's principal — silently, and in exactly the rows the per-caused-by partition is built from. Plan-012's T2.2 in-memory resolution and this publication read the same rows and must not diverge: this task takes Plan-012's resolver as its source rather than re-deriving the rule, so there is one implementation of "who is the effective principal" and not two.
  - **Tests:** a run started by A and steered by B publishes A for the turns before the steer and B for the turn the steer opened, asserted at the context rather than through the receipt — the unit under test is the resolution, not the fold; **a cost frame belonging to turn A that is delivered after turn B has opened stamps A's principal**, which is the assertion that distinguishes a bound scope from a mutable read and fails against the read-at-emission shape; a daemon-initiated recovery turn publishes the `system` arm and carries no participant reference; a turn whose opening intervention row carries the `system` origin never yields a participant, so the storage CHECK and the published value agree by construction.
  - **Estimate:** 1 PR

> **Tier-6 reciprocal (CP-004-9, doc-only):** when Phase 3 executes, the run-engine also registers Plan-012's approval outcome listener (`registerApprovalOutcomeListener` — blocking/unblocking transitions per the CP-004-9 row) and calls `cancelPendingForRun(runId, cause)` from the same terminal path that fires `onRunTerminal` hooks, so pending approval requests settle as `approval.canceled` instead of dangling (Plan-012 A-6/A-10). (Campaign B13, Plan-012 CP-012-11:) the same Phase-3 execution also has the T3.7 emitter read Plan-012's `resolvedPostureFor(runId)` accessor — registered beside its CP-004-8 setup gate, whose `assertRunReady` returns void — at `run.running` emission, so the stamp carries the complete resolved posture; no resolved posture ⇒ fail closed, never an unstamped sandboxed run. (Campaign B13, Codex round 3:) the same execution also consumes Plan-012 T2.8's input-ask lifecycle signals through the transition applicator the CP-004-9 listener drives: a live input-kind `driver_ask.requested` applies `running → waiting_for_input` atomically with its append — making T3.15's park edge reachable and the pending-ask blocking-state exemption real for input asks — and a delivered answer applies `waiting_for_input → running` atomically with `driver_ask.responded` (run-state-machine.md's annotated causes on both edges); a live-leg provider retraction releases the blocking state the same way for either kind — `waiting_for_* → running` atomically with `driver_ask.canceled`, no outcome delivered (Codex round 4).

> **Resolved at the Plan-016 walk (same Tier-6 audit):** child-run behavior on parent interrupt/cancel is **non-cascade** — children are independent intervention targets (Spec-016 §Run Hierarchy; run-state-machine.md §Child-Run Behavior as rewritten by Plan-016 A-016-17). No cascade logic is authored here or in Plan-016; `run_links` is owned by Plan-016 (Tier 6). (F-004-3-08 → closed by Plan-016 A-016-17.)

### Phase 3B — Rollback Time-Travel Cluster (explicit-label supplement)

**Goal:** The `rollback` time-travel intervention as one dispatch unit — admission guards, state-gating, and forward emission (T3.12), the conversation leg + turn-snapshot file-restore caller (T3.13), the supersede projection (T3.14), the compaction-boundary classifier (T3.16), the atomic edit-and-resend composite (T3.17), and the `run rollback` CLI surface (T4.7) — together with the T4.2 rollback-affordance and T4.8 edit-entry-affordance **activation arms**, two EXTENDs on the Phase-4 dormant shipments that this supplement's PRs carry as declared obligations (the Wired-at-3B seams below; Codex PR #346 round 2, and the 2026-08-18 placement amendment for T4.8).

**Precondition:** Re-homed from Phases 3 and 4 at the NS-69 PR's round-1 fold (Codex PR #346, 2026-08-17): preflight Gate 5 resolves `precondition_box_checked` entries per phase, so the checkout-root box's Phase-3 entry held all of Phase 3 — T3.10's `RunSetupGate` seam included — while [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) Phase 3, that box's own producer, gates on the seam's merge (CP-010-9): a mechanical two-plan deadlock no dispatch order could satisfy. This supplement holds exactly the tasks that read the cluster's gated surfaces (the Plan-024 Phase-3B / Plan-017 Phase-2B explicit-label shape), making the ship-as-one-unit direction the wire-arm box's comment already recorded literal. Dispatch is by explicit label only (`preflight.mjs docs/plans/004-queue-steer-pause-resume.md 3B`) — the numeric walk never selects a supplement. The un-deadlocked order: Phase 3 (the seam) → Plan-010 Phase 3 (the `checkout_root` column + gate registration) → Plan-009 Phase 2B (the seven-input callee growth) → this supplement. **Interim safety (why the split is fail-safe, not fail-open):** until this supplement ships, no rollback handler exists and no client surface exposes one — T4.2's rollback affordance ships dormant in Phase 4, hidden regardless of declared driver capability, its activation arm riding this supplement (Codex PR #346 round 2) — so no run can hold a pre-rollback execution epoch and no run's position can sit below a compaction boundary (positions only move backward through T3.12) — T3.11's rollback-conditional absorption arms and T3.5's boundary backstop are vacuously satisfied in the interim, never bypassed, and Plan-006's compactor runs its vacuous default attribution against a corpus that cannot contain a rewound tail. **Wired-at-3B seams:** T3.16's task carries its T3.5 resume-backstop call-site (the classifier's third call-site — T3.5's leg (a) activates here), and the cross-phase integration test arms that drive the cluster from Phase-3 stayers — T3.11's pause-first-composite absorption-window arm and T3.5's rewound-run resume arms (the boundary-diverged refusal pair and the run-bound replacement drain) — ride this supplement's PRs; their tasks' Phase-3 shipments carry the seeded-state arms only. T4.2's rollback-affordance activation arm — the flip from Phase-4 dormancy to the capability-keyed gate — rides the same PRs, its Phase-4 shipment carrying the dormant affordance only (Codex PR #346 round 2). T4.8's edit-entry activation arm rides them identically (2026-08-18 placement amendment): its eligibility predicate's first conjunct is false at Phase-4 merge, so the affordance is hidden there regardless of capability, and this supplement's PRs flip it live in the same landing as the T3.12 handler and the T3.17 composite it dispatches into — an entry point can never precede the intervention it invokes.

```yaml
preconditions:
  # The numbered-phase chain: T4.7 registers on the Phase-4 SDK client (T4.1),
  # and phases 1-2 are covered transitively through these two entries' own
  # blocks (a merged phase proves its gates held at its dispatch). Phase 4 is
  # SDK substrate only for this supplement: T4.2's rollback affordance ships
  # dormant there (hidden regardless of driver capability) and activates via
  # this supplement's T4.2 arm, so the affordance never precedes the T3.12
  # handler (Codex PR #346 round 2).
  - { type: plan_phase, plan: 004, phase: 3, status: merged }
  - { type: plan_phase, plan: 004, phase: 4, status: merged }
  # T4.7 only: Plan-007's Phase-R3 CLI scaffold (the apps/cli workspace + the
  # main.ts Cli instance, T-007r-3-1 / T-007r-3-2) — the command file cannot
  # compile before the workspace exists. R-series phases resolve through the
  # external_plan_phase_merged form (the Plan-022 Phase-4 precedent).
  - { type: external_plan_phase_merged, plan: 007, phase: R3 }
  # Plan-010's B22 turn-snapshot service is its Phase 5 (the discrete named
  # snapshot Phase); B23 ships it as code and it must merge before this
  # supplement dispatches the rollback file-restore leg (T3.13, CP-010-12).
  - { type: plan_phase, plan: 010, phase: 5, status: merged }
  # The mechanical half of the checkout-root box below: the
  # run_execution_contexts.checkout_root column lands with Plan-010 Phase 3
  # (T3.2, its only writer). The box's other producer — Plan-009 Phase 2B's
  # seven-input callee growth — is a supplement-labeled phase the plan_phase
  # grammar cannot name, so that half stays box-prose-carried.
  - { type: plan_phase, plan: 010, phase: 3, status: merged }
  # Codex re-audit round 4: the files-partially-restored wire arm REQUIREs the
  # two restore-warning enumerations, but Plan-010 T5.2's partial_restore
  # variant named only the failed command — the producer-side amendment is a
  # named merge prerequisite (the CP-004-12 pattern): this box fails until the
  # lead-owned Plan-010/Spec-010 amendment lands and checks it.
  - { type: precondition_box_checked, box: "Plan-010 partial-restore enumerations registered" }
  # 2026-08-16 rewind-hardening amendment: T3.17's composite consumes T1.2's
  # `replacementSend` payload field and T1.3's widened result members
  # (`resend-unapplied`, `boundary-diverged`, `resendDisposition` — round-2
  # fold), all of which mirror the api-payload §Plan-004 registration this box
  # gates (the Phase-1 entry mirrors), and T4.7's --replace-message option has
  # no member to populate until it lands. Phase-scoped like every sibling box
  # entry, so it holds T3.16 too — the conservative direction the cluster's
  # ship-as-one-unit label makes literal.
  - { type: precondition_box_checked, box: "Rollback replacement-send wire arm registered" }
  # 2026-08-17 normalized-root transport closure (NS-69): T3.13 supplies
  # CP-010-12's seventh caller-resolved input (checkoutRoot, from the new
  # run_execution_contexts.checkout_root column), but the shipped callee
  # signatures stay five-field until Plan-009 Phase 2B ships the CP-010-15
  # consented EXTEND — this plan is Tier 5, both producers Tier 6, so tier
  # order cannot close the window. The box's prose names both producers and
  # checks when both have merged; the plan_phase entry above carries the
  # Plan-010 half mechanically, and this entry carries the Phase-2B half.
  - { type: precondition_box_checked, box: "Snapshot-callee checkout-root parameter shipped" }
  # Round-2 fold: T3.17's drained-turn principal stamp consumes the Plan-012
  # admitting-principal carrier (the lead-owned Plan-004 <-> Plan-012 amendment
  # registers it, checking both plans' same-named boxes). Phase-scoped like the
  # sibling entries.
  - { type: precondition_box_checked, box: "Admitting-principal carrier registered" }
```

#### Tasks

- **T3.12 (ROLLBACK) — `rollback` intervention handler: guard, state-gating, transitions, forward emission** (campaign B9, R8/RA-2)
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND — the `rollback` dispatch arm) + `run-engine.ts` (EXTEND — the transitions + forward emission)
  - **Spec coverage:** Spec-004 §Required Behavior (rollback — version guard, state-gating, pause-first, forward `run.rolled_back`, failure semantics); Spec-004 §Interfaces And Contracts (rollback capability gate + 6-state result); ADR-011 (generic `applyIntervention` dispatch — new types need no interface change)
  - **Verifies invariant:** I-004-3, I-004-6, I-004-10, I-004-12, I-004-15
  - **Consumes:** T1.2 (`rollback` payload arm), T1.7 `getRun`; the `Run State Machine §Rollback Transitions` edges (campaign B2)
  - **Behavior (authored):** `applyIntervention('rollback', {targetPosition})` rides the existing generic dispatch (ADR-011, zero interface change). **All fail-closed pre-dispatch validation completes before any effect:** the mandatory `expectedRunVersion` guard (evaluated once at admission against the pre-pause version, per D-004-1 semantics), the static capability gate (`driver.capability_unsupported`, no orchestration fallback, when the driver does not declare `rollback` — I-004-10; the refusal settles `requested → rejected` with the machine-readable cause persisted to `interventions.rejection_reason` (T1.4, Codex round 4) — the durable source an idempotent replay reconstructs the response's `rejectionReason` from, since the stored `result` is forbidden on `rejected`; from the 2026-09-06 typed-composite-guard amendment the same reconstruction reads `interventions.rejection_guard` beside it and replays the recorded literal on the `rejectionGuard` member — equal to the recorded one across a daemon restart, absent exactly where the column is NULL, and never re-derived by re-evaluating T3.17's guards against a run that has since moved on), the `targetPosition` boundary check (a recorded turn boundary strictly below the current position — with the sole carve-out that a target **equal to the current position** is admissible when the run's recorded rollback outcome at that position left the file leg incomplete: `files-unrestored`, `files-partially-restored`, or the position-mismatch skip — the **file-leg recovery path**, whose conversation leg no-ops by absolute-position convergence and whose restore re-runs to the fixpoint), the **uncompacted-rewind-span check** (every row of the target run strictly above `targetPosition` — the rows the supersede cutoff would mark — is uncompacted; a span intersecting already-compacted history refuses whole per `Spec-004 §Required Behavior`, because compaction destroyed the per-row attribution the T3.14 projection needs — detection reading the stubs' preserved keys (Codex round 5): every run-scoped stub preserves `runId`, so the check counts the target run's unstamped stubs with preserved `originPosition` above `targetPosition`, or with none (a pre-rollback-era stub, unknowable ⇒ the same standing refusal), while stamped stubs and other runs' stubs never block; the check is atomic with the write-ahead intervention row's durability under `withSessionAppendLock`, and from then until the intervention concludes the compactor defers the span's rows — T3.14's `attributeAtCompaction` answers `'defer'` for rows covered by a dispatched-but-unconcluded rollback row (Plan-006 T3.2's third disposition, Codex round 4) — closing the check-then-compact race in the admission-to-boundary window), and execution-root availability. **Every path that settles a rollback `requested → rejected` persists its machine-readable cause to `interventions.rejection_reason`** (Codex round 5, generalizing the round-4 capability-gate rule): the boundary-check, uncompacted-rewind-span, root-`busy`, and T2.5 authorization refusal families of `Queue And Intervention Model §Intervention State Transition Table` persist their causes exactly as the capability gate does, because the wire contract REQUIRES `rejectionReason` on a rollback `rejected` response (T1.3) — a cause-less rejected settle would be unreconstructable on idempotent replay. **State-gating** per `Run State Machine §Rollback Transitions`: legal from `paused` / `waiting_for_approval` / `waiting_for_input` / the three terminal states; a `running` target is **paused-first** (the existing pause path: interrupt + persist) as an unguarded internal sub-step that queues **no** resume event and cancels a pending resume as moot. A `waiting_for_approval` / `waiting_for_input` source **voids the pending block durably on the confirmed conversation leg — atomic with the `waiting_for_* → paused` transition and the forward `run.rolled_back` emission, never at admission or pre-dispatch** (Codex round 6; `run-state-machine.md §Rollback Transitions` gates the void on a conversation-leg-confirmed rollback — a failed or no-rewind conversation leg leaves the pending block and its originating native ask intact, the run stays in its `waiting_for_*` state, and the outcome records `nothing-applied` per T3.13): the awaited approval settles `approval.canceled` via Plan-012's `cancelPendingForRun(runId, cause)` seam (CP-004-9 — consumed, never authored here; `Spec-006 §Approval Flow (approval_flow)` terminal) — and when that pending approval was normalized from a **permission-kind driver ask** (Plan-012's T2.8 normalizer records the originating-ask provenance on the row it mints), the void **also settles the originating native ask** `driver_ask.canceled` through its owning surface within the same atomic void, so the provider-side ask can neither stay pending nor later expire/respond against the rewound turn (Codex round 5; provenance consumed, never authored — no Plan-012 symbol) — and a driver input-ask settles `driver_ask.canceled` through its owning surface (`Spec-006 §Driver Ask Events` terminal — no new event type minted); a queued pending resume against the target run is voided the same way (canceled as moot — the position it would resume from is rewound). The void is **progression-serialized**: a concurrent resolution that applies first advances `runVersion` so the in-flight rollback `expired`s; a rollback that applies first cancels the block and the late resolution finds a canceled request and no-ops. A **terminal-source** rollback re-acquires the execution root (reactivating `run_execution_contexts`, clearing `released_at` atomically with the re-open — I-004-12) — the re-acquisition serving the bound file leg's exclusive tenancy (`Spec-004 §Required Behavior` scopes it to the file leg), so a terminal source classified onto T3.13's conversation-only short-circuit **never re-acquires**: its already-released context keeps its recorded release, the vanished-checkout cause stamped only when the confirmed re-open itself releases a live context, and the run re-opens `paused` holdless under the rootless resume refusal (Codex PR #346 round 2); a root `busy` under another run refuses the whole intervention pre-dispatch; a run with no restorable root rolls back conversation-only through T3.13's short-circuit arms (the released-context rootless class, or a recorded checkout root itself gone — the confirmed re-open then releasing that context with the vanished-checkout cause recorded, its convergence into the rootless class per T3.13; never a mere nested-directory deletion under a live checkout, which restores). A terminal-source composite **releases its re-acquired root on every path that does not confirm the conversation rewind** (a preflight refusal, a conversation-leg `degraded`, or the no-rewind reconciliation): the run stays terminal, the workspace returns to `ready`, and the execution context's `released_at` marker is restored — the re-acquired hold is retained **exactly** when the run re-opens `paused`, and released on every non-confirming exit so the terminal-source re-acquisition never leaks the root (a rule quantifying over re-acquired holds alone — a conversation-only terminal source never re-acquires, so its confirmed re-open retains nothing and the run stays released per T3.13). On the **confirmed conversation leg** (T3.13), append the **forward** `run.rolled_back` event (`{sessionId, runId, runVersion, channelId?, targetPosition}`) under `withSessionAppendLock`, advance `runVersion`, and land the run in `paused` — a state-changing rollback (terminal-exit or waiting-void) also emits `run.paused`; an in-place rollback from `paused` emits only `run.rolled_back`. The authoritative log never truncates (`ADR-017 Decision Log`). 1:1:1 cardinality holds with the sole dispatch-axis exception of the pause-first composite's two driver-facing calls (I-004-3). **Tests:** a waiting-source rollback settles the pending approval `approval.canceled` (via `cancelPendingForRun`) / a driver input-ask `driver_ask.canceled` and voids a queued resume; a permission-backed approval void settles BOTH surfaces (`approval.canceled` + the originating `driver_ask.canceled`); each pre-dispatch refusal family persists its machine-readable cause to `interventions.rejection_reason` (boundary-check, uncompacted-rewind-span — a rollback whose rewound tail includes a compacted row refuses whole — and root-`busy` included, not only the capability gate); the progression-serialized race — resolution-first ⇒ the rollback is `expired`; rollback-first ⇒ the late resolution no-ops (a provider answering the native ask after a permission-backed void included); a waiting-source rollback whose conversation leg fails leaves the approval/input block pending and the run in its `waiting_for_*` state, recording `nothing-applied`; a compaction tick racing an admitted-but-unconcluded rollback defers every rewind-span row (none is stubbed unstamped in the admission-to-boundary window), and a concluded-refused rollback releases the deferral (the span compacts current on the next pass); a rollback whose span contains an unstamped compacted stub of the target run refuses whole — a position-less legacy stub of that run included — while another run's stubs and `sourceEpoch`-stamped stubs never block admission.
- **T3.13 (ROLLBACK-RESTORE) — Conversation leg + turn-snapshot file-restore caller** (campaign B9; CP-004-11 / CP-010-12 caller side)
  - **Files:** `run-engine.ts` (EXTEND) + rollback integration tests (`packages/runtime-daemon/**/__tests__/`)
  - **Spec coverage:** Spec-004 §Required Behavior (conversation-then-file sequencing, confirmed-floor validation, the settlement-time boundary reclassification — 2026-08-16 amendment, round-2 fold — partial-restore disposition, conversation-only fallback); Spec-004 §Driver-Level Rollback Mechanics; Spec-010 §Turn-Boundary Snapshots (the consumed capture / restore ops)
  - **Verifies invariant:** I-004-3, I-004-16, I-004-20
  - **Consumes:** CP-004-11 — [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s `captureTurnSnapshot` / `resolveRestoreTarget` / `restoreToTurn` (Phase-5 callees, never authored here) + [Plan-005](./005-provider-driver-contract-and-capabilities.md)'s `rollbackTo` op / `DriverRollbackResult`; the `run_execution_contexts` row (Plan-010 D-010-5 — its `checkout_root` column, the 2026-08-17 normalized-root transport closure, arriving with Plan-010 Phase 3 / T3.2)
  - **Behavior (authored):** the caller side of CP-010-12. **(a) Turn-boundary capture call-site:** at each turn boundary of a writable-mode run, call `captureTurnSnapshot({executionRoot, checkoutRoot, runId, epoch, turnOrdinal, mode})` — resolving `executionRoot` + `checkoutRoot` + `mode` from the run's `run_execution_contexts` row (D-010-5; `checkoutRoot` from its `checkout_root` column, the normalized enclosing working-tree top level the Plan-010 T3.2 gate captured at context creation — CP-010-12's seventh caller-resolved input, 2026-08-17: the caller gains one field on a row it already reads, never a `workspaces` query; the parameter joins the shipped five-field callee signatures only at Plan-009 Phase 2B's CP-010-15 consented EXTEND and the column lands with Plan-010 Phase 3, a **named merge prerequisite** — the §Preconditions box "Snapshot-callee checkout-root parameter shipped" + its Phase-3B Gate-5 entry hold this leg's dispatch until both phases merge, so this caller is never authored against the five-field form for the Phase-2B signature growth to break), supplying the **execution epoch** (0 before any rollback; **advances with each accepted `run.rolled_back` — a confirmed conversation rewind, including a position-mismatch rewind confirmed at the recorded floor and the same-position recovery's convergence no-op (Codex round 6) — regardless of the file-leg disposition**: a `files-unrestored` / `files-partially-restored` outcome still advances it, and only an outcome with no confirmed rewind — whole-rejection, `pause-only`, `nothing-applied` — leaves it unchanged, because re-executed turns reuse position ordinals and `Spec-004 §Required Behavior` requires the post-rollback execution's `turn-<N>` capture never collide with, nor silently resolve to, the superseded epoch's `turn-<N>` tree); the callee's `read-only` self-guard and its capture-never-blocks-the-turn contract are Plan-010's. **(b) Rollback restore sequencing under exclusive execution-root tenancy — preflight → conversation → bound file-restore:** **conversation-only short-circuit first** — a run whose mode snapshots nothing (`read-only`), or whose execution context is **released** with no existing root (a disposed ephemeral clone / a worktree retired by the cleanup sweep — `Spec-004 §Required Behavior`'s rootless class, keyed on disposal provenance, never a bare disk-existence probe of the execution root), or whose recorded `checkout_root` itself no longer exists on disk (the tree the restore operates in — nothing left to restore into) takes the **conversation-only** branch here, before `resolveRestoreTarget`: it skips snapshot resolution and the file leg entirely (the resolver would otherwise `no_snapshot`-refuse a read-only run that correctly captured nothing, wrongly failing the whole rollback), records the `conversation-only` disposition **only after the conversation leg's driver-confirmed rewind** (Codex round 4 — a conversation leg that fails or confirms no rewind on this branch records the no-rewind outcome per T3.12: `pause-only` when the running-source internal pause applied, else `nothing-applied`; never a fabricated `conversation-only` success), and the re-opened rootless run is **non-resumable** — `run.resume` against a released-context run with no existing root is refused with the typed `run.execution_root_released` (409, run state unchanged; distinct from the setup-time `workspace.execution_root_unresolved` — `error-contracts.md §Run`), nothing recreating a disposed clone / retired worktree — and the checkout-root-absent arm **converges into that same class rather than forming a third** (Codex PR #346 round 2 — a live context whose recorded tree is gone has nothing to resume into either, and `Spec-004 §Required Behavior` now names the arm): the confirmed conversation leg **releases a live execution context atomically with the re-open**, stamping `released_at` with the vanished-checkout disposal cause — the release IS the recorded disposal provenance — while a terminal-source instance arrives with its context **already released** and keeps its original release record (never re-acquired on this branch — T3.12's file-leg scoping), with the preflight's one-time existence check of the recorded `checkout_root` as the classifier either way, still never an execution-root disk probe — so the re-opened run is structurally the released rootless class, the same typed `run.execution_root_released` refusal governs its resume, and the composite's guard (iv) refuses it pre-dispatch; a non-confirming exit mutates nothing (no release without confirmation, the T3.12 discipline). **A live context whose nested execution root was deleted while its recorded `checkout_root` survives is not this branch at all** (Codex PR #346 round 1 — a disk-existence short-circuit on the execution root would skip restore exactly where the checkout-root transport enables it): that run proceeds through `resolveRestoreTarget` / `restoreToTurn`, which re-materialize the deleted directory's captured content through the checkout root — the caller-side mirror of Plan-009 T-009-2B-4's deleted-bound-directory arm and the case the seven-input contract exists for. For a writable-mode run with a live root, derive the **`epochLineage`** (ordered `{epoch, rewindBase}` list) from the run engine's durable epoch / intervention records and call the non-mutating `resolveRestoreTarget({executionRoot, checkoutRoot, runId, targetPosition, epochLineage})`, which returns a `TurnSnapshotResolution` (Plan-010 T5.2) — its typed `head_moved` (HEAD advanced past the snapshot) / `no_snapshot` (no ref in the owning epoch) refusal variants reject the whole rollback **before** the conversation leg. Conversation leg: dispatch `rollbackTo` (the daemon resolves the run's live binding at dispatch; a fork-composed leg repoints it via `DriverRollbackResult.applied.bindingId`) and domain-validate the driver-confirmed floor (`sessionPosition`) equal to the accepted `targetPosition` before the file leg — a mismatch degrade records the confirmed position. The confirmed floor is additionally **reclassified at settlement** against the then-current boundary set via T3.16's classifier (the two-point classification, `Spec-004 §Required Behavior` — the in-stream fence guarantees every pre-rewind boundary emission is delivered by confirmation, so this recheck sees the moved set): a crossing conclusion settles the intervention `degraded` naming **`boundary-diverged`** — file leg skipped fail-closed, any staged `replacementSend` suppressed (`resendDisposition: 'unapplied'`), run `paused` at the confirmed floor, forward event at the confirmed position, divergence cause persisted on the durable row — evaluated **before** the ordinary mismatch handling per the disposition-precedence order (conversation-leg divergence, then position-mismatch, then the file-leg arms). **Same-position file-leg recovery (the T3.12 carve-out):** when the accepted target equals the current position, the conversation leg's **convergence no-op — the driver confirming `sessionPosition == targetPosition` with no movement — counts as the confirmed floor for this branch** (Codex round 6): the composite proceeds to the bound file leg and `restoreToTurn` re-runs to the fixpoint, the forward `run.rolled_back` emits at the confirmed position, and the epoch advances under the uniform trigger above — the no-rewind outcomes (`pause-only` / `nothing-applied`) apply only when the driver fails to confirm the floor, never to the recovery convergence. Bound file-restore: call `restoreToTurn(resolution)` (it re-verifies the HEAD precondition under the hold — TOCTOU guard) and map the returned `TurnSnapshotRestoreResult` (Plan-010 T5.2) onto the result's file-leg `disposition` (`RollbackInterventionResult`) — `restored → files-restored` carrying the callee result's collision-overwrite + divergent-gitlink enumerations through as the arm's REQUIRED `overwrittenIgnoredPaths` / `divergentGitlinks` (empty when none, never dropped — Codex post-merge round), `partial_restore → files-partially-restored` (with the failed step named plus the same REQUIRED enumerations covering every effect applied before the failure, the failing command's partial writes included (PR #230 round 1) — the identical never-silent mapping, Codex re-audit round 2; the producer-side naming on `partial_restore` is a **named merge prerequisite** — the §Preconditions box "Plan-010 partial-restore enumerations registered" + its Phase-3B Gate-5 entry hold this leg's dispatch until the lead-owned Plan-010/Spec-010 amendment lands (Codex re-audit round 4, the CP-004-12 pattern; cross-plan §2 one-writer) — never a fabricated empty; **never collapsed into `files-unrestored`**), and the execution-time `head_moved` re-verify refusal → `files-unrestored` (the conversation leg has already applied; the worktree is untouched by the refusal). Plan-004 owns the pre-dispatch validation, the exclusive execution-root tenancy, and the conversation leg; Plan-010 owns the callee (`git/turn-snapshot-service.ts`) — B9 **calls** it, never writes it (cross-plan §2 one-writer). **Provider-rewind currency (Codex half discharged 2026-08-26):** the Codex leg was re-verified against `codex-cli 0.149.1` — `thread/rollback` is deprecated in the generated type and the mechanism is rebound to `thread/fork` at an inclusive `lastTurnId` (`Spec-004 §Driver-Level Rollback Mechanics`; `Spec-005 §Parity Capability Mechanism Grades`; `ADR-017 Decision Log`), so this leg is authored against the successor and no deprecated handler is registered. The **Claude** half — `--resume-session-at` composed with `--fork-session` — remains unconfirmed against a live binary (`Plan-005 §Risks And Blockers`): re-verify that composition against the then-installed provider binary before this leg's code lands. **Tests:** the turn-boundary call-site invokes the callee with the resolved root / checkout root / mode / epoch — the checkout root sourced from the run's `run_execution_contexts.checkout_root` exactly as the production read does, never a hand-passed literal; a full rollback round-trip as integration tests — preflight refusal (no worktree mutation), conversation-then-file success, confirmed-floor-mismatch degrade, released-context conversation-only, a live-context run whose recorded `checkout_root` is gone rolling back conversation-only with the context released at the confirmed re-open (the vanished-checkout cause stamped) and `run.resume` then refused `run.execution_root_released` — its non-confirming twin leaving the context live and unreleased, its terminal-source sibling re-opening `paused` with the context still released — never re-acquired, the original `released_at` record unchanged — and resume refused identically, a rootless run whose conversation leg fails recording `nothing-applied` (never `conversation-only`), a branch-mode run whose nested execution root the turn deleted outright while its recorded `checkout_root` survives proceeding through the file leg — `restoreToTurn` re-materializing the deleted directory's captured content, never the conversation-only branch — while the same run with the recorded `checkout_root` itself deleted takes conversation-only without the rootless resume refusal, the same-position file-leg recovery reaching `restoreToTurn` after the convergence no-op (never recorded `nothing-applied`), the `files-partially-restored` disposition (its enumerations carrying the overwrites applied before the failure — a completed read-tree leg's or the failing command's own partial writes — never empty-washed), a `run.resume` against the rootless re-opened run refused `run.execution_root_released`, a `files-restored` outcome surfacing the callee's enumerations (an overwritten colliding ignored path and a divergent gitlink both appear in the recorded result), a degraded-confirmed rollback (`files-unrestored`) still advancing the execution epoch — the next turn-boundary capture mints refs under the new epoch namespace, never colliding with the superseded epoch's refs — a daemon restart with the same-position recovery dispatched-but-unconcluded reconciling as at-target (the crash window between dispatch intent and restore completion): the restore re-runs to the fixpoint and the durable outcome lands, never a no-rewind conclusion stranding the partial tree; a `usage.context_compacted` row landing between admission classification and the driver's confirmation settles the intervention `degraded` naming `boundary-diverged` with the file leg skipped and the run `paused` at the confirmed floor (the admission-to-settlement race); and a boundary row whose delivery lags past settlement leaves the settled outcome unchanged while the next `run.resume` refuses `run.compaction_boundary_diverged` (T3.5's backstop — asserted across the pair).
- **T3.14 (ROLLBACK-SUPERSEDE) — Projection supersede-marking of rolled-back turns** (campaign B9)
  - **Files:** `run-engine.ts` (EXTEND) + the daemon composition root (the CP-006-1 `PiiEncryptor` injection site — Plan-001 / Plan-003 bootstrap; EXTEND: construct Plan-006's `Compactor` with this task's `RollbackAttributionSource` implementation, replacing T3.2's vacuous default)
  - **Spec coverage:** Spec-004 §Required Behavior (rolled-back turns stay in the timeline marked superseded by projection); Spec-004 §Driver-Level Rollback Mechanics (timeline presentation — supersede cutoff: turn > post-rollback position of the accepted `run.rolled_back` event); ADR-017 Decision Log (the authoritative log never truncates or rewrites)
  - **Verifies invariant:** I-004-15
  - **Behavior (authored):** rolled-back turns are never truncated from the authoritative log; the run / intervention projection marks them **superseded** by the **post-rollback position cutoff** carried on the accepted `run.rolled_back` event — a turn is superseded when its position is **strictly greater than that event's post-rollback position** (`turn > targetPosition`), so the epoch-0 prefix **at or below** the target stays current while the rewound tail above it is marked (`Spec-004 §Driver-Level Rollback Mechanics`). Cross-epoch rows are keyed additionally on the **pre-rollback execution epoch**: the projection consumes the durable source-epoch stamp T3.11 records on pre-rollback rows (the Plan-006-owned `sourceEpoch` + `sourcePosition` pair per CP-004-12, Plan-006 T1.9 — the named merge prerequisite above; the position companion — the straggler's per-event originating turn (T3.11) — is what lets the cutoff rank a late row at or below `targetPosition` into the epoch's surviving prefix rather than blanket-superseding it), so a replayed projection marks stale-epoch rows superseded exactly like the in-time rows of the epoch they belong to. Row-to-turn assignment is **uniform across delivery timing** (Codex round 4 ripple): a row ranks by its operation's originating turn — resolved from the stamped `sourcePosition` on late rows, and from the row's own operation linkage (the payload's correlation to the operation's opening (epoch, turn) association) on in-time rows of the content-asynchronous families — falling back to its timeline slot when no linkage resolves; identical content ranks identically whether it arrived in time or late, so an in-time turn-4 usage row delivered during turn 8 survives a rollback to 5 exactly like its late-delivered sibling (a synchronous family's originating turn and timeline slot coincide, so its assignment is unchanged). Marks are **scoped to the epoch each accepted `run.rolled_back` rewound** (Codex round 5): a mark from epoch _N_'s rollback applies to **no row of any later epoch** — after rollback 10→5, a re-execution, then rollback 7→6, the second epoch's re-executed turn 6 stays **current** (the Spec-004 cutoff: `6 > 6` is false) while the first epoch's turn 6 remains superseded — a re-executed turn **never inherits** the superseded mark its reused ordinal carries in an earlier epoch, and pre-rollback assistant output / tool activity can never resurface as current timeline state; the scoping is downward-inclusive across the run's lineage (Codex round 1 on the CP-004-13 consumer PR #232): an accepted rollback rewinds the run's **surviving history**, inherited earlier-epoch prefix included, so a row of epoch _e_ at position _p_ is superseded exactly when _p_ exceeds the minimum cutoff among accepted rollbacks at epoch ≥ _e_ — after 10→5, re-execution to 7, then 7→3, the inherited epoch-0 turn 4 (current after the first rollback) is superseded by the second, attributed to source epoch 0 with the second rollback's `targetPosition` 3. The projection **implements Plan-006 T3.2's `RollbackAttributionSource`** (`attributeAtCompaction` — the compaction-time attribution the Plan-006 compactor injects to stamp a rolled-back run's rewound-tail stubs: `'superseded'` + the triple for a rolled-back epoch's rewound tail, `'defer'` for a row a dispatched-but-unconcluded rollback intervention's span covers — read from the durable `interventions` write-ahead row, so the deferral survives restart (Codex round 4) — `'current'` otherwise, the `'current'` answer carrying the row's resolved originating position (its association turn, else its timeline slot) for the stub's preserved `originPosition` key (Codex round 5); the body-registered reciprocal of that Plan-006-owned interface seam, no Plan-006 symbol authored here) **and ships its composition-root wiring** (Codex round 4): the daemon composition (the CP-006-1 `PiiEncryptor` composition root — Plan-001 / Plan-003 bootstrap) constructs Plan-006's `Compactor` with this projection in place of the vacuous default from this task on, with a runtime assertion outside tests that production construction never carries the default (the CP-006-1 assertion pattern) — an unwired seam would silently compact every rewound tail unstamped while the integration suite passes on a manually-injected instance and **exports the superseded-turn read** (`supersededTurns(runId)` — the per-turn supersede attribution the position-cutoff + epoch keying produce: each superseded (epoch, turn) carrying its source epoch and the superseding rollback's `targetPosition` — lineage-resolved: the first accepted rollback at the row's epoch or later that rewound the surviving history containing it — values the cutoff comparison already holds — never a bare membership predicate, which could not populate the consumer's marker) as the seam **[Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md)'s `timeline-projector.ts` + `TimelineRead` consume** to render rewound turns distinctly (CP-004-13 — Plan-004 provides-forward; the Plan-013-side consumer registration landed 2026-07-20, no Plan-013 symbol authored here — without this seam the marking never reaches the rendered timeline). **Tests:** a rolled-back run's turns above `targetPosition` are marked superseded while the at-or-below prefix stays current; the two-rollback sequence (10→5, re-execute, 7→6) keeps the second epoch's re-executed turn 6 current while the first epoch's turn 6 stays superseded — a reused ordinal never inherits an earlier epoch's mark; a projection rebuild preserves both markings; the exported `supersededTurns(runId)` returns the attributed marked set for a given run — each entry carrying its source epoch and the superseding rollback's `targetPosition`; the lineage sequence (10→5, re-execute to 7, 7→3) supersedes the inherited epoch-0 turn 4 with source epoch 0 + `targetPosition` 3 while the epoch-2 re-execution at turn 4 stays current; an in-time content-asynchronous row (a turn-4 usage row delivered during turn 8) survives a 10→5 rollback current, matching its late-delivered sibling's ranking (uniform row-to-turn assignment); a rebuild after the boundary `run.rolled_back` row and its stale-epoch rows are compacted to audit stubs still produces both markings — the stub-preserved `runId`/`runVersion`/`targetPosition` cutoff plus each stamped stub's `sourceEpoch` + `sourcePosition` + `runId` triple — stamped at ingestion for late arrivals, at compaction (via the injected `RollbackAttributionSource`) for the ordinary rewound tail — suffice (`Spec-006 §Compacted Event Format`, campaign B9 CP-004-12); the real daemon composition path constructs the `Compactor` with this projection — asserted through the composition root, never a manually-injected instance — and the runtime assertion rejects a production construction still carrying the vacuous default (Codex round 4); `attributeAtCompaction` answers `'defer'` for a row covered by a dispatched-but-unconcluded rollback row and flips to `'superseded'` / `'current'` once the intervention concludes.
- **T3.16 (ROLLBACK-COMPACTION-GATE) — Compaction-boundary classification of rewind targets** (2026-08-16 rewind-hardening amendment)
  - **Files:** `packages/runtime-daemon/src/session/run-engine.ts` (EXTEND — the classification, alongside T3.12's other pre-dispatch validations) + rollback admission tests (`packages/runtime-daemon/**/__tests__/`)
  - **Spec coverage:** Spec-004 §Required Behavior (compaction-boundary-aware rewind targeting — the three-valued fail-closed classification); Spec-004 §Driver-Level Rollback Mechanics (compaction-boundary anchoring — the per-driver translation obligation the classification's liftability depends on); Spec-006 §Usage Telemetry (usage_telemetry) (the `usage.context_compacted` boundary row this reads)
  - **Verifies invariant:** I-004-20
  - **Consumes:** T1.7 `getRun`; T3.14's row-to-turn assignment (the operation-association ranking, reused — never re-derived); `interventions.rejection_reason` (T1.4) for the persisted cause — deliberately **no capability input**: V1 registers no boundary-crossing-rewind capability member (`Spec-004 §Required Behavior`), so the classification reads no driver declaration and the refusal is unconditional on every leg
  - **Behavior (authored):** a **fourth** fail-closed pre-dispatch validation on T3.12's admission path, ordered with the others and before the internal pause. Resolve **B** — the greatest originating position among the target run's `usage.context_compacted` rows that are **current**, i.e. not marked superseded by T3.14's projection — ranking each row by the **same operation-association rule T3.14 already implements** (the stamped `sourcePosition` on late rows, the payload's operation linkage on in-time content-asynchronous rows, the timeline slot only as the linkage-less fallback), never off a bare arrival-order read taken in preference to association: a usage aggregate is exactly the content-asynchronous family whose delivery order does not bound its originating turn, so an arrival-ranked B floats **above** the true boundary. That direction is conservative, not unsafe — nothing is appended before the operation it reports, so a slot-ranked B over-refuses and never admits a crossing target — which is why association is required for **precision**: skipping it would deny every legitimate rewind across the delivery gap, widest on exactly the long-running turns this gate must not punish (`Spec-004 §Required Behavior`). Then classify: **no current row** ⇒ admit (the V1 default — no V1 driver ships a compaction operation, so a boundary arises only from provider-native auto-compaction); **`targetPosition >= B`** ⇒ admit unchanged (the rewound tail is strictly above the target, so the boundary row is not in it and the provider legitimately keeps its compacted context — a target **equal** to B included, since rewinding to the compaction's own boundary keeps the compaction rather than reversing it); **`targetPosition < B`, or a current compaction row whose originating position does not resolve** ⇒ **refuse the whole intervention pre-dispatch**, no leg applied, persisting the machine-readable cause to `interventions.rejection_reason` exactly as T3.12's other refusal families do (the wire contract REQUIREs `rejectionReason` on a rollback `rejected` response — T1.3). The position-less arm is the genuinely unrankable row — no stamp, no linkage, **and** no recoverable slot, the shape a retention audit stub leaves behind — not merely a row that fell through to the slot fallback, which ranks and classifies normally. It refuses **every** target of that run: evidence of a compaction that cannot be proven at-or-below the target is treated as above it, the same conservative direction the uncompacted-rewind-span check takes for a position-less audit stub. The refusal is **provider-agnostic** — it binds both driver legs identically, because it follows from compaction being a context rewrite, not from either wire's rewind mechanism — and **liftable**: a driver declaring re-verified boundary-crossing rewind semantics (verified against the then-installed binary under the ADR-017 row that already governs both rewind mechanisms) admits crossing targets on its own legs — a **future** lift: no capability member exists in V1 for a driver to declare, so this task ships the refusal unconditional and branch-free, and the lift arrives with the Spec-005 amendment that registers the member, never as a latent runtime branch here. The classifier is **exported for reuse**: T3.13's settlement-time reclassification of the confirmed floor and T3.5's resume-time position-vs-boundary backstop both call it (the two-point classification, `Spec-004 §Required Behavior`) — one classification implementation, three call-sites, never re-derived. **Do not conflate** this with the uncompacted-rewind-span check in T3.12: that one reads the daemon retention pass's `event.compacted` audit stubs, this one reads the provider's `usage.context_compacted` rows; a rewind cleared by one is not thereby cleared by the other, and both must pass. **Tests:** a run with no compaction row admits every otherwise-valid target; a target above B admits; a target exactly at B admits (the off-by-one that would make "undo the compaction" reachable); a target one below B refuses whole with the cause persisted and no leg applied — on both driver legs; a run whose current compaction row has no resolvable originating position refuses every target, including one far above the row's arrival slot; a compaction row already superseded by an earlier accepted rewind does not gate a later one (unreachable through the V1 wire — no accepted rewind can supersede a compaction row under the unconditional refusal — so the test seeds the superseded projection state directly on the row, never driving a capability declaration no V1 surface accepts); an in-time content-asynchronous compaction row delivered many turns after it occurred ranks by its operation association, so a target between its true originating position and its arrival slot **admits** — the arrival-slot regression: slot-ranking would refuse exactly this legitimate target, the over-refusal the association rule exists to eliminate — while a target below the true originating position still refuses whole; and a boundary-crossing refusal is a `rejected` settle, never a `degraded` one — nothing dispatched.
- **T3.17 (ROLLBACK-RESEND) — Atomic edit-and-resend composite** (2026-08-16 rewind-hardening amendment)
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND — the `replacementSend` admission guards) + `run-engine.ts` (EXTEND — the post-rewind admission leg) + `packages/runtime-daemon/src/migrations/00NN-intervention-rejection-guard.ts` (CREATE — the additive nullable `interventions.rejection_guard` and its column-attached `CHECK`, transcribed from the canonical `local-sqlite-schema.md` `interventions` block; ordinal deliberately unpinned here and resolved at this task's own PR, the Plan-006 `content_payload` placeholder idiom, since T1.4's `0015` has shipped and is never amended) + `packages/runtime-daemon/src/session/migration-runner.ts` (**EXTEND — the same-commit registration edit**, which the dep-map [§2 Package Path Ownership Map](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map)'s `packages/runtime-daemon/src/migrations/` row makes a registered class of edits rather than ownership of the runner: the `import { INTERVENTION_REJECTION_GUARD_MIGRATION_SQL } from "../migrations/00NN-intervention-rejection-guard.js"`, a further `if (!hasMigrationApplied(db, N))` → `db.transaction(...).immediate()` → inner re-check → `.exec()` block appended to `applyMigrations` in the identical shape versions 1–17 already carry — argued once in that function's own preamble and deliberately **not** re-derived per version — plus the module header's version→plan enumeration extended by its own `version N by Plan-004` line. The block is authored explicitly rather than looped, because that header pins the `.immediate()` concurrency seam against the `worker_threads` concurrent-boot race test and re-expressing it through a registry would re-open that control flow for re-validation. **The pair is one commit, not two:** `openDatabase` is the canonical handle factory for daemon code _and_ tests, so an orphan migration file leaves every fresh and every upgraded database without the column and this task's own settle write fails `no such column` — the orphan-file failure that dep-map row names) + `packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts` (EXTEND — a per-ordinal `describe("00NN-intervention-rejection-guard migration shape")` block on the `0017-command-receipt-mcp-task-handle` precedent, the nearest sibling because it is likewise an additive nullable `ADD COLUMN` carrying a column-attached `CHECK` onto an already-shipped table: the column present and typed after a bare `.exec()` over a `0001`+`0015` base; the `CHECK`'s three planted refusals surviving a reopen (an unknown literal, a guard on a `steer` row, a guard on a non-`rejected` rollback row); a second `.exec()` throwing, since SQLite has no `ADD COLUMN IF NOT EXISTS` and the `schema_version` guard is what makes re-application a no-op; `it("anchors its own schema_version row at N")` asserting version **and** description; and the suite-level `it("anchors schema_version rows at [1..17]")` version-walk extended to `[1..N]`, which is the assertion an unregistered migration actually fails) + rollback integration tests (`packages/runtime-daemon/**/__tests__/`). **Landed-fact annotation owed at this task's PR** (the `0017` convention — that migration's own `command_receipts.mcp_task_id` comment in [Local SQLite Schema §Queue and Intervention Tables (Plan-004)](../architecture/schemas/local-sqlite-schema.md#queue-and-intervention-tables-plan-004) names its module and exported SQL constant as a `file#CONST` pair): the `rejection_guard` column comment's unpinned `00NN` placeholder is replaced by the resolved migration path joined to its exported SQL constant (`INTERVENTION_REJECTION_GUARD_MIGRATION_SQL`) in that same `file#CONST` shape — a form this plan states rather than writes, since the file does not exist until that PR and a written cite would fail the corpus cite-target check here. The corpus keeps no separate migration ledger, so that annotation plus the runner header's version→plan line **are** the registration record
  - **Spec coverage:** Spec-004 §Required Behavior (atomic edit-and-resend — the run-bound admit-not-dispatch leg, the four structural refusal guards, the admission condition with `resendDisposition`, the three-valued failure taxonomy, and the boundary-only supersede rule); Spec-004 §Interfaces And Contracts (the optional `replacementSend` payload member; the PII-safe decrypt-and-compare idempotency rule)
  - **Verifies invariant:** I-004-21, I-004-3, I-004-18, I-004-22, I-004-23
  - **Consumes:** T1.2 (`replacementSend` payload arm) + T1.3 (`resend-unapplied` / `boundary-diverged` dispositions + the `resendDisposition` member) — all gated on the §Preconditions replacement-send registration box, **registered 2026-08-25** and now checked, so this leg carries no hold; T2.9's `user.message` emitter + queue-admission path (reused verbatim — the composite mints no second send surface); the `queue_items.target_run_id` column (T1.4); T3.12's guard/state-gating/emission machinery; T3.13's conversation leg + settlement reclassification (the suppression input); T3.14's supersede projection; the CP-006-1 `PiiEncryptor` seam (injected at the composition root — envelope encryption at staging **and** decrypt-and-compare at idempotency-reuse adjudication; consumed, never authored); the admitting-principal carrier registered 2026-08-18 (T2.4's acceptance-time stamp on this intervention's own row plus the `queue_items.admitting_intervention_id` linkage — the §Preconditions admitting-principal box, now checked)
  - **Behavior (authored):** the composite is the **existing** `rollback` intervention with `replacementSend` present — one durable `interventions` row, one `clientIdempotencyKey`, one `expectedRunVersion` guard evaluated once at admission, one atomic application under the session append lock. Every T3.12 / T3.13 rule holds unchanged (target-position domain check, T3.16's compaction-boundary classification, the Spec-010 restore precondition, the uncompacted-rewind-span check, execution-root tenancy, the capability gate, confirmed-floor validation, epoch advance, supersede-by-projection, write-ahead reconciliation). **Persist first, through the PII envelope:** the `replacementSend` body is written onto the write-ahead intervention row **before** driver dispatch — encrypted into `interventions.pii_payload` under the requesting participant's key via the CP-006-1 `PiiEncryptor` (the same `splitPii` discipline T2.9's `user.message` path applies), `pii_participant_id` stamped, the plaintext `payload` carrying only the non-PII rollback members and the leg's presence — so the caller's text survives every arm including a crash mid-composite, recoverable under the requester's live key, and the Plan-022 Path-1 key deletion that shreds the event copy shreds this copy identically. V1 has no daemon-side composer-draft surface to stage it in (`Spec-017 §State And Data Implications` defers draft persistence past V1), which is why the reconciliation row carries it. **Four structural refusal guards**, fail-closed at admission, pre-dispatch, whole-intervention, each persisting its cause to `interventions.rejection_reason` **and naming itself on the response's typed `rejectionGuard`** (2026-09-06 typed-composite-guard amendment — T1.3's closed union; this task is the member's sole producer and carries its obligation: a refusal raised by one of these four ALWAYS populates it with that guard's literal, and every other refusal family this handler reaches — the **eight** the Tests row below enumerates and controls over — leaves it absent, so presence reads as "a composite guard refused" and never as "some rollback refused"; `interventions.rejection_reason` and its wire `rejectionReason` are unchanged and stay the **machine-readable cause** they have been since campaign B9 (never prose — the shipped console renders the wire member verbatim in a refusal `code` slot that is "never prose, never localized, never reworded"), what the typed member adds being a **closed** vocabulary where that one is open: `error-contracts.md §Intervention` deliberately registers no code for an intervention outcome, so no contract enumerates the causes a rollback `rejected` may carry and no client can switch on it exhaustively — **and the literal gets a durable column of its own**, the round-2 fold correcting round 1's claim that it was "derivable from the persisted cause on replay", which is false for that same reason: deriving the literal from an unenumerated cause vocabulary is exactly the unpublished-value-set match the member abolishes, and the wire forbids `result` on `rejected`, so an idempotent replay has nothing but the row to answer from. This task writes the refusing guard to the additive nullable `interventions.rejection_guard` **in the same write that settles `state = 'rejected'`** — the four guards are pre-dispatch admission refusals, so the settlement is one write, and a guard written ahead of that state refuses at the database rather than in review. The column's `CHECK` is **column-attached rather than a table constraint**, which is what keeps this off a table rebuild: `ALTER TABLE … ADD COLUMN` accepts a column-attached `CHECK` whose expression reads sibling columns, so the closed vocabulary and the `type = 'rollback' AND state = 'rejected'` binding land together in one appended column on a table that has already shipped (T1.4, PR #402) — the canonical DDL is the `local-sqlite-schema.md` `interventions` block, and the migration is this task's own **next-ordinal** file, never an edit of the shipped `0015`): (i) a `running` target refuses outright — the composite deliberately does **not** take T3.12's pause-first path, because the message being replaced is still being answered (bare rollback keeps pause-first unchanged — I-004-15); (ii) a run holding a **pending send** refuses, in either form — an accepted send not yet delivery-confirmed (the window between T2.9's admitted `user.message` row and the provider's `user_text` echo folding into it as delivery confirmation, where no turn boundary is yet acknowledged to validate against) or an **undrained queued send** admitted before the composite (FIFO would deliver the older item ahead of the replacement, breaking resend-next; front-insertion is a V1-deferred queue-priority override, so the remedy is `QueueItemCancel`-then-reissue, and the queued-send arm lifts if V1.1 priority overrides land) — because the pending-send order is not the composite's to reorder; (iii) a `targetPosition` whose boundary was not opened by a **participant-authored** `user.message` of the target run refuses — an orchestration-authored boundary (a workflow phase input, an orchestrated child run) has no participant send to replace, and rewriting one would rewrite a phase input outside the workflow's own authoring surface; a **bare** rollback of that same boundary stays admissible; (iv) a **non-resumable rootless** target — execution context released with no existing root, or a live context whose recorded `checkout_root` is gone (T3.13's confirmed leg releases it into that class, so admission would immediately dead-letter — Codex PR #346 round 2), the class T3.13 marks `run.execution_root_released` — refuses whole: the leg's promise is delivery on the bound run's next `run.resume`, which such a run can never perform, so an admitted replacement would be a dead letter; bare rollback of it stays admissible conversation-only. **Admit run-bound, do not dispatch — and only on a fully-successful rewind:** admission requires the confirmed floor **equal** to `targetPosition`, T3.13's settlement reclassification concluding boundary-clear, and the file leg complete or legitimately conversation-only — any degraded arm suppresses the resend (`resendDisposition: 'unapplied'`; the staged text stays recoverable on the row, and the caller's recovery is the file-leg recovery path re-issued with the leg re-attached, guard (ii) being clean since suppression admitted nothing). On admission, admit the corrected message as a queue item through T2.9's ordinary participant-send path (`user.message` row + queue admission, `correlation_id` stamped as usual), stamping `queue_items.target_run_id` with the rewound run's id in the same write — the row persisting in queue-state `queued` (exactly T3.5's drain predicate), reaching the run-bound arm of `admitted` only at that drain's delivery — the admission, the stamp, and the intervention's settlement committing in **one durable transaction** — the run still lands `paused` at the confirmed position, **no auto-resume**, and **no second driver-facing call**, so I-004-3's sole dispatch-axis exception is unchanged; the run-bound item delivers into its bound run as that run's next provider send on `run.resume` (T3.5's drain — necessarily next, guard (ii) having refused while any older send was pending), never converting into a new run. The drained turn executes under the **admitting intervention caller's** effective principal (`Spec-012 §Required Behavior`'s extended rule), which the same write anchors by stamping `queue_items.admitting_intervention_id` with **this** intervention's id — the row-anchored linkage T3.5's drain follows to reach the principal T2.4 already stamped on this row at acceptance (2026-08-18 amendment; the carrier the §Preconditions admitting-principal box named is registered, so this leg carries no hold). The composite therefore mints no principal-resolution path of its own: acceptance stamped the principal universally, this task records which intervention admitted the item, and the drain reads the two. The replacement body additionally rests in `queue_items.pii_payload` under the requester's key through T2.9's ordinary split (I-004-23) — so the one send now rests encrypted in both durable places, the intervention row and the queue row, under the same participant DEK and shredded by the same single key deletion. **Failure taxonomy, three-valued, text recoverable in each:** _refused_ (any guard fails ⇒ `rejected`, or `expired` on a guard mismatch — nothing staged, nothing dispatched); _committed-then-failed_ (rewind confirmed but the replacement not admitted — an admission-condition suppression naming the earlier-failing leg's disposition (`boundary-diverged` / position-mismatch / the file-leg arms, in that precedence), or an admission failure after a fully-successful rewind naming `resend-unapplied` — either way `degraded` with `resendDisposition: 'unapplied'`, run `paused` at the confirmed position with `run.rolled_back` appended per T3.12's state-follows-the-conversation-leg rule, text recoverable from `pii_payload` under the requester's key — and on the `resend-unapplied` arm alone the settlement additionally carries `overwrittenIgnoredPaths` / `divergentGitlinks` (REQUIRED, present-and-empty when nothing was overwritten or diverged), sourced by branch: **where the file leg ran**, from the same T3.13 restore result that would have populated the `files-restored` outcome this disposition displaces, so I-004-21's never-silent carry survives the displacement rather than being asserted and then dropped by the arm that stands in; **on the conversation-only branch**, as two empty arrays, there being no restore result to read and nothing displaced — the reading those two empty arrays already carry on `files-restored` — 2026-08-25 wire registration); _transport-lost_ (driver throw / timeout / restart ⇒ the T3.13 write-ahead reconciliation resolves it, and its **completion branch drives the staged `replacementSend` forward among the remaining legs** — concluding a confirmed rewind without re-admitting it would strand the very text this leg preserves — admitting and settling in the **same single transaction** the live path uses, so an unconcluded intervention proves no admission occurred and recovery cannot double-admit, while an idempotent replay returns the recorded outcome rather than admitting twice). **Supersede marking is boundary-only:** T3.14's projection is driven by the accepted `run.rolled_back` cutoff and by nothing else — not the composite's admission, not its dispatch, not the replacement send; the replacement appends **after** the boundary in session append order, so a consumer orders rewind-then-resend without any linkage field on either row (the CP-004-13 seam is unchanged and needs no widening). **Idempotency-reuse adjudication is decrypt-and-compare** (`Spec-004 §Interfaces And Contracts`): a reused `clientIdempotencyKey` is a replay only when the incoming `replacementSend` plaintext equals the stored body decrypted under the requester's live participant key via the injected `PiiEncryptor` — never a ciphertext comparison (the envelope is nonce-randomized, so equal plaintexts encrypt unequal) and never a persisted plaintext digest (a shred-surviving guessing oracle); an undecryptable stored body — the key shredded — settles the reuse fail-closed as `intervention.idempotency_conflict`. **Retention sweep:** the daemon retention pass NULLs `interventions.pii_payload` past the `Spec-022 §PII Data Map` 90-day full-retention bound (this task authors the sweep — a plain NULL-out, safe because no digest or anchor binding attaches to the row's ciphertext, unlike `session_events`' CP-006-12-bound bytes; the non-PII audit record is retained for the row's lifetime), and the participant data-subject export includes the decrypted replacement-send bodies of rows stamped with the participant (that map's row for this path). **Tests:** the composite settles as one intervention with one durable row and one idempotency key (no second record, no second driver call); each of the four guards refuses whole with its cause persisted and no leg applied — the `running` guard asserted against a bare rollback of the same run, which still pauses-first and succeeds, and the rootless guard asserted against a bare rollback of the same rootless run, which stays admissible conversation-only; each of those four refusals additionally carries its own `rejectionGuard` literal on the response (a table-driven assertion over all four, so a guard added later without its literal fails), with the **negative control** that the non-composite refusal families reaching the same handler — all **eight**, enumerated by T3.17's total negative control below, never a four-family sample — settle `rejected` with `rejectionGuard` ABSENT, which is what makes presence readable; a fully-successful rewind admits the corrected message as a queue item **bound to the rewound run** (`target_run_id` and persisted queue-state `queued` asserted on the raw row) and leaves the run `paused` with no resume queued, the item delivering only on an explicit `run.resume`; a `files-partially-restored` settlement suppresses the resend — no queue item, `resendDisposition: 'unapplied'`, disposition naming the file leg — and the recovery composite (same target, leg re-attached) admits after the fixpoint restore; a `boundary-diverged` settlement suppresses the resend the same way; an identical-body key reuse replays the recorded outcome via decrypt-and-compare (no second admission), a differing-body reuse settles `intervention.idempotency_conflict`, and a reuse after participant-key shred settles `intervention.idempotency_conflict` fail-closed (never a replay, never a crash); the retention sweep NULLs `pii_payload` past the 90-day bound while the plaintext `payload` and the row survive; the replacement body lands in `interventions.pii_payload` under the requester's key with `pii_participant_id` stamped and the plaintext `payload` carrying no body (asserted on the raw row, both directions); a run holding an undrained queued send refuses the composite whole (the queued-send arm of the pending-send guard) and admits after that item is cancelled; an admission failure after a confirmed rewind settles `degraded` naming `resend-unapplied` with `run.rolled_back` still appended and the text recoverable from `pii_payload` under the requester's key, that same settlement carrying the confirmed rewind's `overwrittenIgnoredPaths` / `divergentGitlinks` — asserted non-empty against a rewind that overwrote an ignored path, and present-and-empty against one that overwrote nothing and against the conversation-only branch; a daemon restart between dispatch and settle reconciles at-target and admits the staged send exactly once (never zero, never twice) — and no interleaved durable state exists in which the queue item is committed while the intervention is unconcluded, the admit-and-settle transaction being atomic on both the live and reconciliation paths; a replay of the same `clientIdempotencyKey` returns the recorded outcome without a second admission; the timeline supersede set is identical whether the composite is admitted, dispatched-but-unconfirmed, or fully settled up to the moment the boundary appends — the seeded refutation being the plausible-but-wrong rule that the timeline truncates when the resend is admitted; and an orchestration-authored boundary refuses the composite while accepting a bare rollback. The 2026-08-18 amendment adds: the admitted queue row carries `admitting_intervention_id` equal to this composite's intervention id (asserted on the raw row), and its body decrypts under the requester's key to the same plaintext as the intervention row's copy — one send, two envelopes, one key.
  - **Tests:** each of the four guards tripped in isolation asserts its own literal on both surfaces at once — the `rejectionGuard` of the response and the `interventions.rejection_guard` of the raw row — so the wire and the column can never drift apart unnoticed; the **non-composite negative control** runs the same four refusal conditions against a bare `rollback` and, against a composite, **every** non-structural refusal family — table-driven and **total over the set**, not a sample of it, so a family added later without a row fails the control's own cardinality assertion. The set is the **eight** `requested → rejected` families a rollback reaches, enumerated by counting `Queue And Intervention Model §Intervention State Transition Table`'s condition cell against T3.12's own refusal enumeration (which is what caught the four-family sample this control previously ran): (1) the capability gate — `driver.capability_unsupported`, I-004-10; (2) T2.5's authorization refusal; (3) the target-position domain check — non-integer, negative, current-or-future, or boundary-less; (4) T3.16's compaction-boundary classification; (5) an **incompatible target run state** — T3.12's state gate, a `queued` or `starting` source, the only two outside the legal set (`running` by way of the bare rollback's pause-first path — a composite from `running` is guard (i), not this family — `paused`, the two waiting states, and the three terminals); (6) the **Spec-010 restore-precondition** refusal — `HEAD` unequal to the snapshot's recorded first parent; (7) the **uncompacted-rewind-span** intersection — an unstamped or position-less audit stub of the target run above `targetPosition`; and (8) the **execution-root `busy`** refusal — a terminal-source rollback whose root is held by another run. Each asserts the member absent on the response **and** the column NULL on the raw row, so presence is proven to mean "a composite guard refused" rather than merely to correlate with it — the schema admits the optional member on any rollback `rejected` response, so these producer assertions are its only enforcement. The **restart/replay equality** case is the durability assertion and is run against a genuinely reopened database, never a warm in-process handle: refuse a composite on a named guard, close the daemon's connection and reopen it so nothing survives in memory, replay the identical request under the same `clientIdempotencyKey`, and assert the replayed response carries a `rejectionGuard` **equal to the recorded one** together with the unchanged `rejectionReason` — the assertion being equality to the recorded literal and not merely presence, since a re-evaluated guard could coincide with the recorded one and still be the wrong answer. Its adversarial twin mutates the run between refusal and replay so a re-evaluation would land on a _different_ guard, and asserts the replay still returns the recorded one, which is what separates reading the row from re-deciding. The DDL's own guarantees carry planted negative controls at the database boundary — a guard literal outside the four, a guard on a `steer` row, and a guard on a non-`rejected` rollback row each refuse as a constraint violation rather than persisting.
- **T4.7 — `run rollback` CLI command** (campaign B9; RA-7 reachability)
  - **Files:** `apps/cli/src/commands/run-rollback.ts` (CREATE) + `apps/cli/src/main.ts` (EXTEND — register the command on Plan-007's clipanion `Cli` instance; clipanion has no auto-discovery) + `apps/cli/src/commands/__tests__/run-rollback.test.ts`
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (the `rollback` intervention client surface + 6-state `InterventionResult` + `RollbackInterventionResult` disposition-union rendering + result-state→exit mapping); Spec-004 §Required Behavior (the 2026-08-16 amendment's atomic edit-and-resend composite — this command is its V1 client surface); Spec-007 §Required Behavior (the CLI is the first delivery track; commands consume the daemon through the typed client, never daemon internals)
  - **Verifies invariant:** I-004-10
  - **Command (authored):** `sidekicks run rollback <runId> --to <targetPosition> --expected-run-version <n> [--wait-timeout <seconds>] [--resume-key <clientIdempotencyKey>] [--replace-message <text>]` — a clipanion command file (kebab filename → nested `run rollback` path per the Plan-007 `daemon-status.ts` command-file precedent, `Plan-007 §Client SDK + CLI (packages/client-sdk/, apps/cli/)`). The command is **registered on Plan-007's `apps/cli/src/main.ts` `Cli` instance** (T-007r-3-2 CREATE) — clipanion has no auto-discovery, so an unregistered command file is unreachable; Plan-007 owns `main.ts` and Plan-004 EXTENDs it with this one `.register()` call (dep-map §2 `apps/cli/src/main.ts` row; the CP-004-4 `run.*` reciprocal with Plan-007 already covers the namespace). It dispatches the `run.intervene` `rollback` intervention (CP-004-4 namespace; T1.2 payload arm) via the `@ai-sidekicks/client-sdk` typed daemon client — the client seam generates the **mandatory** requester-side `clientIdempotencyKey` (a fresh UUID per invocation — unless `--resume-key` supplies the key a prior invocation printed, reused verbatim so the T1.2 replay contract takes over from the recorded row (Codex post-merge round) — `Spec-004 §Interfaces And Contracts`; the T1.2 arm carries it) so an at-least-once retry replays the recorded outcome rather than re-applying — and renders the 6-state `InterventionResult` — including the `RollbackInterventionResult` disposition on terminal `applied` / `degraded` rollback outcomes (T1.3; the state-split contract carries no disposition on any other state, and each terminal state admits only its own disposition class — `applied` ⇒ `RollbackAppliedResult`, `degraded` ⇒ `RollbackDegradedResult` — so a response whose `state` contradicts its disposition fails parse before exit-mapping, never exiting 0 on a failed restore; Codex round 5) — surfacing on a `files-restored` render, on a `files-partially-restored` degraded render (Codex re-audit round 2), and on a `resend-unapplied` degraded render (2026-08-25 wire registration — that arm displaces a completed restore and carries the same two enumerations), the arm's `overwrittenIgnoredPaths` / `divergentGitlinks` warnings (informational, exit unaffected on every one of them: `applied` still exits 0 and the warnings never alter `degraded`'s code; never collapsed) — and maps the **terminal result state to a POSIX exit code**: `applied` → 0, and `rejected` / `expired` / `degraded` → distinct nonzero codes per the `exit-codes.ts` scheme. A response still **`requested` / `accepted`** (admission acknowledged, application in flight — e.g. an idempotent replay observed mid-application) never exits the process (Codex round 4): the command **waits for the terminal state by idempotent replay** — it re-dispatches the identical `run.intervene` payload at a bounded interval, reusing the SAME `clientIdempotencyKey` minted at invocation start (the T1.2 replay contract returns the recorded row's current state, never re-applying), until the intervention reaches `applied` / `rejected` / `degraded` / `expired`, then renders and exit-maps that terminal state. The wait is **client-side bounded** (Codex round 5 — supersedes round 4's "expiry backstop" termination claim): intervention `expired` fires only on a version advance or a target-state change (`Queue And Intervention Model §State Model` — no wall-clock expiry exists), so a stalled daemon would otherwise hold the poll open forever; `--wait-timeout <seconds>` (default 600; `0` explicitly opts into an unbounded wait) stops polling and exits with its own distinct `exit-codes.ts` code — a client-side outcome, not a result state: the intervention stays live daemon-side, and the CLI prints the `interventionId` + `clientIdempotencyKey` so a later re-invocation passing that key via `--resume-key` resumes the idempotent replay, never re-applying (without the option every invocation mints fresh, so the printed key would be unusable — Codex post-merge round). A genuine transport failure mid-wait exits through the transport mapper below. The static `driver.capability_unsupported` refusal is a **normal `run.intervene` response** with lifecycle state `rejected` carrying `rejectionReason` (`Queue And Intervention Model §Driver Result To Lifecycle Mapping` maps it `requested → rejected`), so it rides the **result**, not the `JsonRpcError` channel — the CLI renders the `rejectionReason` with **no disposition** (a `rejected` response carries none — `result?: never` on the state-split arm; Codex round 4) and adds **no** local gate (it renders the daemon's authoritative decision, I-004-10). The `exit-codes.ts` `JsonRpcErrorCode → PosixExitCode` mapping still covers genuine **transport** errors (an unreachable daemon, a malformed request). `--expected-run-version` is **required** (the fail-closed guard is mandatory, D-004-1 / T3.12 — the CLI never omits it). `--replace-message <text>` (2026-08-16 rewind-hardening amendment) populates the `rollback` arm's optional `replacementSend` member, making the atomic edit-and-resend composite reachable — this command is its V1 client surface, and without the flag the spec'd composite would ship with no invocation path at all (the RA-7 reachability rule this task already exists for). Omitting the flag dispatches a **bare** rollback with the member **absent**, never present-and-empty (T1.2's arm rejects an empty body, so the CLI never synthesizes one from a missing option). The flag adds no client-side gate and no second request: the composite is one intervention under one `clientIdempotencyKey`, so the wait-by-idempotent-replay loop, the `--resume-key` resume path, and the exit mapping above are unchanged — its four structural refusals (a `running` source; a pending send — accepted-but-undelivered or undrained-queued; a `targetPosition` whose boundary is not participant-authored; a non-resumable rootless target — while a compaction-boundary-crossing target also refuses, that is T3.16's general rollback-domain gate every rollback shares, not a composite-specific guard) arrive as ordinary `rejected` results carrying `rejectionReason` on the same no-disposition path as `driver.capability_unsupported`, and its suppressed-or-failed resend arrives on the ordinary `degraded` disposition (T1.3) carrying `resendDisposition: 'unapplied'`, rendered as the confirmed rewind **plus** the unapplied resend — the member rendered alongside the disposition — under `degraded`'s single exit code — the CLI never reports partial success as success, the same never-exit-0-on-a-failed-restore rule the disposition-union parse above already enforces. Dispatch is held by the §Preconditions replacement-send wire-arm box (this phase's `precondition_box_checked` entry): until the union member is registered the flag has no field to populate — **registered 2026-08-25**, so the box is checked and the flag has its field; the Phase-3B gate set otherwise governs this task's dispatch unchanged. Honors Plan-007's `apps/cli` import isolation (I-007-13 — imports only `@ai-sidekicks/client-sdk`, `@ai-sidekicks/contracts`, `clipanion`, Node built-ins; never `@ai-sidekicks/runtime-daemon`), enforced by `apps/cli/eslint.config.mjs`. **Consumes:** the [Plan-007](./007-local-ipc-and-daemon-control.md) `apps/cli` scaffold (Phase R3, T-007r-3-1 — the precondition prose above) + `@ai-sidekicks/client-sdk` daemon client + the `run.*` namespace (CP-004-4, ratified D-004-3). Tests: dispatches `run.intervene` with the `rollback` arm carrying `targetPosition` + `expectedRunVersion` + a client-generated `clientIdempotencyKey` (UUID); renders each of the 6 result states and maps each terminal state to its POSIX exit code (`applied` → 0; `rejected` / `expired` / `degraded` → their distinct nonzero codes); an `accepted` first response followed by an `applied` replay renders the terminal state and exits 0 — the wait-by-idempotent-replay path, same key across polls — and an intervention expiring mid-wait exits with `expired`'s code; a wait exceeding `--wait-timeout` exits with the pending-timeout code and prints the `interventionId` + key (the intervention stays live daemon-side); a `driver.capability_unsupported` refusal renders as a `rejected` result carrying `rejectionReason` and no disposition (not a transport error); the command is reachable through the real `Cli` (an end-to-end `sidekicks run rollback` invocation resolves to this command, proving registration); a `--resume-key` re-invocation reuses the supplied key verbatim (no fresh mint), enters the wait-by-replay loop, and renders the recorded terminal state without re-applying; a `files-restored` result renders its `overwrittenIgnoredPaths` / `divergentGitlinks` warnings and still exits 0, and a `files-partially-restored` result renders the same warnings alongside `failedStep` while exiting with `degraded`'s code; a `--expected-run-version`-omitted invocation is a usage error before any dispatch; a `--replace-message` invocation dispatches the `rollback` arm carrying `replacementSend` with the supplied body while an invocation omitting the flag carries **no** `replacementSend` key at all (asserted on the serialized payload, not on a falsy read — the present-but-empty shape is never synthesized); a `resend-unapplied` degraded result renders both legs (the confirmed rewind and the unapplied resend) and exits with `degraded`'s code, never 0; and each of the composite's four structural refusals — the `running` source, the pending send (exercised in both arms: an accepted-but-undelivered send and an undrained queued send), the non-participant-authored boundary, and the non-resumable rootless target — renders as a `rejected` result carrying its `rejectionReason` with no disposition, exiting with `rejected`'s code.

### Phase 4 — Desktop Run-Controls

**Goal:** The client SDK + renderer run-controls subtree — optimistic projection reconciled to daemon truth, capability-gated steer (with `rollback` the second identically-gated control per campaign B2 — desktop control-gate at T4.2; the `run rollback` CLI command, T4.7, ships with the Phase-3B cluster it fronts), renderer bridge-only, and the edit-and-resend entry affordance (T4.8, 2026-08-18 placement amendment) shipping dormant beside T4.2's control gate.

**Precondition:** Phase 1–3 daemon surface landed; **D-004-3 (`run.*` method strings) ratified** before T4.1 / T4.4 wire calls land. Plan-023 renderer substrate + `window.sidekicks` bridge present (prose-only — no Gate-5 grammar names it). T4.7 and its two task-scoped gates (the Plan-007 Phase-R3 scaffold entry and the replacement-send wire-arm box entry) moved to Phase 3B with the rollback cluster (Codex PR #346 round 1), so this phase's entries carry no rollback-cluster hold. T4.8 carries a **task-scoped** hold on the same replacement-send wire-arm box (stated in its own task body) rather than a phase-level entry, which would needlessly hold T4.1–T4.6; like T4.2's control gate it ships dormant here and activates from Phase 3B, so the phase's Gate-5 block stays as written. That sentence states where the hold is recorded, not whether it is outstanding: the box was checked 2026-08-25 (§6 node NS-75), so the hold stands satisfied while T4.8's Phase-4 dormancy is unchanged — conjunct (1) is still false at Phase-4 merge.

```yaml
preconditions:
  # Machine-enforced form of the phase-1–3 prose (preflight Gate 5). The
  # Part-B box entry is carried onto this phase too: the auto-walk soft-skips
  # an ineligible phase and tries later ones, so gating Phase 3 alone would
  # let the walk select Phase 4 past the unmet park leg.
  - { type: plan_phase, plan: 004, phase: 1, status: merged }
  - { type: plan_phase, plan: 004, phase: 2, status: merged }
  - { type: plan_phase, plan: 004, phase: 3, status: merged }
  - { type: precondition_box_checked, box: "Driver input-ask park leg authored" }
```

#### Tasks

- **T4.1 — `runControlClient.ts` SDK (single daemon-transport factory)**
  - **Files:** `packages/client-sdk/src/runControlClient.ts` (CREATE)
  - **Spec coverage:** Spec-023 §Signature Feature Composition Sketches (Runs View — daemon run-state / queue subscriptions); Spec-004 §Interfaces And Contracts (run-control client surface)
  - **Verifies invariant:** I-004-9
  - **Transport (authored):** a single daemon-transport factory `createDaemonRunControlClient(transport)` riding `window.sidekicks.daemon.call` / `daemon.subscribe`. **Rationale:** run-control authority is daemon-only — `ADR-003 §Decision` ("the daemon will be the authority that applies and records their outcomes"), Assumption #3 (`ADR-003 §Assumptions Audit`), and no control-plane run-control surface exists in the corpus (verified). This **excludes** the `sessionClient.ts` dual-transport pattern; it structurally resembles `membershipClient.ts`'s daemon half (`docs/architecture/cross-plan-dependencies.md §NS-28: Plan-002 Phase 5 — Client SDK Membership Surface`), but the rationale is the source fact, not the precedent.
  - **Consumes:** `window.sidekicks.daemon` bridge (Spec-023); the `run.*` method strings (CP-004-4 — ratified, D-004-3)
- **T4.2 — Capability-gated steer + rollback; pause/resume/interrupt/cancel NEVER gated**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (CREATE)
  - **Spec coverage:** Spec-004 §Default Behavior (capability-gated controls; pause/resume orchestration-layer, never driver-gated); Spec-004 §Interfaces And Contracts (the `rollback` capability gate — the second gated control); ADR-011 (pause is orchestration, not a driver capability)
  - **Verifies invariant:** I-004-10
  - **Note:** the §Rollback "disable any false flag" line is a **trap** — of this plan's controls exactly two are gated on driver capability: `steer` and, per I-004-10 as amended, the campaign-B2 `rollback` intervention. The desktop `rollback` control-gate is **authored here** (campaign B9): the run-controls subtree disables the rollback affordance when the driver does not declare the `rollback` capability, the renderer mirror of T3.12's daemon-side static gate — no orchestration fallback, identical to the `steer` gate. **Phase-4 dormancy (Codex PR #346 round 2):** this phase merges before the Phase-3B rollback supplement ships T3.12's handler, so the Phase-4 shipment authors the rollback affordance **dormant** — hidden regardless of declared capability — and the supplement's T4.2 activation arm (a Wired-at-3B seam) flips it to the capability-keyed rule in the same PRs that land the handler; the capability gate alone would expose a control whose request has no daemon handler in the interim. `pause` / `resume` / `interrupt` / `cancel` are orchestration-layer and are never capability-gated.
- **T4.3 — Optimistic-vs-runtime-truth reconciliation**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND)
  - **Spec coverage:** Spec-023 §Signature Feature Composition Sketches (Runs View — daemon run-state subscription)
  - **Verifies invariant:** I-004-9
  - **Consumes:** daemon run-state subscription — subscribe request shape follows the shipped `subscribePresence → { sessionId }` precedent (`docs/architecture/cross-plan-dependencies.md §NS-29: Plan-002 Phase 6 — Desktop session-members Renderer`)
- **T4.4 — Surface 9 run-states + 6 intervention-states**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (surface 9 run-states + 6 intervention-states; canonical terminal is failed, not errored)
  - **Verifies invariant:** I-004-9
  - **Consumes:** `RunState` / `InterventionState` (T1.3); `run.*` subscription channel names (CP-004-4 — ratified, D-004-3)
- **T4.5 — Bridge-bypass import-restriction**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND) + import-restriction assertion test
  - **Spec coverage:** Spec-023 §Preload Bridge Contract (renderer composes via the preload bridge, never bypasses)
  - **Verifies invariant:** I-004-11 (= CP-004-5)
  - **Note:** enforces the renderer bridge-discipline rule (`docs/architecture/cross-plan-dependencies.md §2. Package Path Ownership Map`, `apps/desktop/src/renderer/` row) for the `run-controls/` subtree.
- **T4.6 — Test suite (SDK + orchestration + single-client renderer component tests)**
  - **Files:** `packages/client-sdk/**/*.test.ts` + daemon orchestration integration tests + `apps/desktop/src/renderer/src/run-controls/__tests__/*.test.tsx`
  - **Spec coverage:** Spec-004 §Acceptance Criteria (SDK + orchestration + renderer-component test coverage)
  - **Verifies invariant:** I-004-9, I-004-10, I-004-11 (SDK + orchestration + renderer-component layer)
  - **Note:** single-client RTL renderer component tests ship **now**, mirroring the merged NS-29 `session-members/participant-roster.test.tsx` precedent (`@testing-library/react` `render`/`screen` + an `installMockBridge` `{ daemon: { call, subscribe } }` mock + the bridge-projection / no-direct-daemon-import assertion, per the `SessionBootstrap.test.tsx` idiom). Only the **multi-client live-run-state E2E** (cross-client daemon-stream reconciliation) defers — to the **Plan-023 Tier-8 IPC dispatcher**, _not_ BL-131 (which is Plan-003-scoped and whose "harness unavailable until Tier 8" premise NS-29 has already overtaken). SDK + orchestration tests are in-scope now.
- **T4.8 — Edit-and-resend entry affordance (row-footer pencil + eligibility predicate)**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND) + `apps/desktop/src/renderer/src/run-controls/__tests__/*.test.tsx`
  - **Spec coverage:** Spec-023 §Signature Feature Composition Sketches (Timeline View — the edit-and-resend placement bullet); Spec-023 §Acceptance Criteria (the row-footer entry point dispatches the existing rollback control and renders the owning plan's projection); Spec-004 §Required Behavior (the composite admits run-bound and does not dispatch; the dim-not-remove client rule)
  - **Verifies invariant:** I-004-24
  - **Authors:** the entry component — a hover-revealed pencil affordance for a participant `user.message` row footer, opening an inline editor with a Cancel action and a confirm action that dispatches `applyIntervention('rollback', {targetPosition, replacementSend})` through the T4.1 client — plus the eligibility predicate that drives its three visibility states. Both live in this plan's own `run-controls/` subtree; the component is exported as a mountable unit and [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md) T4.2 mounts it at the row-footer composition point (CP-004-15). This task authors **no** Plan-013 symbol and edits no file outside `run-controls/`.
  - **Eligibility predicate (eight conjuncts, all mirroring daemon rules — the affordance is a projection, never a second source of truth):** (1) the Phase-3B activation arm has shipped; (2) the driver declares the `rollback` capability (I-004-10); (3) the row is a participant-authored `user.message` (I-004-21 guard (iii)); (4) that boundary is not already marked superseded (the CP-004-13 supersede seam as Plan-013 renders it); (5) the run is not `running` (guard (i) — this leg takes no pause-first path); (6) the run holds no pending send, accepted-but-undelivered or undrained-queued (guard (ii)); (7) the run is not a non-resumable rootless target (guard (iv)); (8) the viewer holds a session write-access role, `owner` or `collaborator` (I-004-8 / I-004-19). Conjunct (8) is **authorship-blind by design** — a deliberate divergence from consumer chat clients, where only the author may edit: guard (iii) constrains the **target row's** author and the role check constrains the **caller**, so any owner or collaborator may correct another participant's boundary, exactly as the cross-user run-control rule already admits cross-user interventions.
  - **Visibility mapping (fail-closed in all three states):** conjunct (1) false ⇒ **hidden** (no handler exists, so an affordance would dispatch into nothing); conjunct (2) false ⇒ **disabled**, the capability-absent presentation T4.2 already owns; conjuncts (5)–(7) false ⇒ **disabled with the stated reason** (run busy, send pending, run not resumable), never a silently inert control; conjuncts (3)–(4) false ⇒ the row is structurally not a candidate and renders **no affordance at all**; conjunct (8) false ⇒ no affordance (the non-disclosure direction — a read-only viewer is not told what they could otherwise have edited).
  - **Copy (load-bearing):** the confirm action and its settlement feedback state that the replacement is **queued and sends on the next Resume**. The word "sent" (or "re-sent") is wrong on every path: the composite **admits** the replacement run-bound and does not dispatch it, and the run lands `paused` with no auto-resume (I-004-21). A degraded arm reports the replacement **not applied** (`resendDisposition: 'unapplied'`), never silently dropping it — and a `resend-unapplied` settlement additionally surfaces the displaced restore's `overwrittenIgnoredPaths` / `divergentGitlinks` warnings beside that line (2026-08-25 wire registration): that arm stands in for a **completed** file restore, so suppressing them here would hide destructive restore effects at the affordance exactly as it would at T4.7's CLI render, which `Spec-010 §Turn-Boundary Snapshots`' never-silent mandate forbids; both empty on the conversation-only branch, where no file leg ran, and the warnings are informational — they never change what the feedback says about the replacement itself. A **`rejected`** settlement — one of I-004-21's four structural guards refusing at admission — renders that guard's **remedy** off the typed `rejectionGuard` literal by an exhaustive switch, and never off a match against `rejectionReason`, whose cause vocabulary no contract enumerates (2026-09-06 typed-composite-guard amendment; T1.3's closed union, produced by T3.17): a `'no-pending-send'` refusal says to cancel or let the queued sends drain and retry, `'no-active-turn'` to let the turn finish or interrupt it, `'participant-authored-target'` that only a message the participant side authored can be corrected, `'resumable-target'` that the run can no longer resume and the correction belongs in a fresh run. The literal is **absent** on every non-composite refusal family, and the render degrades to `rejectionReason` alone there rather than guessing — a missing literal is never mapped to a default remedy, which would attach the wrong advice to a refusal the composite did not raise.
  - **Precondition (task-scoped):** this task lands **behind** the §Preconditions box "Rollback replacement-send wire arm registered" — until that registration lands there is no `replacementSend` member for the confirm action to populate. The hold is deliberately carried as **task-scoped reviewer-checkable prose rather than a Phase-4 `precondition_box_checked` entry**, because a phase-level Gate-5 entry would hold T4.1–T4.6 as well, none of which touches the composite (the Plan-013 T4.5 / Plan-014 Tasks-7–10 task-scoped-hold shape). Like T4.2's control gate, the affordance **ships dormant** here — conjunct (1) is false at Phase-4 merge, so it is hidden regardless of every other conjunct — and its activation arm rides the Phase-3B supplement's PRs beside T4.2's (a Wired-at-3B seam). The dormant direction is fail-safe: an affordance can never precede the T3.12 handler or the T3.17 composite. **Registered 2026-08-25** (§6 node NS-75): the wire arm is in `api-payload-contracts.md §Plan-004`, the §Preconditions box is checked, and this task-scoped hold is met — the confirm action now has a `replacementSend` member to populate. The dormancy above is untouched by that discharge: conjunct (1) is still false at Phase-4 merge, so the affordance still ships hidden and still activates from the Phase-3B supplement.
  - **Tests:** the eight-conjunct predicate exercised one conjunct-false at a time against a mocked bridge, asserting the mapped visibility state for each (hidden / disabled / disabled-with-reason / absent) rather than merely asserting non-render; the dormancy arm (activation false ⇒ hidden even with every other conjunct true and the capability declared); the authorship-blind arm (a collaborator viewer editing another participant's row is eligible); the in-flight dim arm (rows the rewind would cut are dimmed and still present at their original ordinals until `run.rolled_back` appends); a copy assertion pinning the queued-sends-on-next-Resume wording so a later edit cannot regress it to "sent"; and a `resend-unapplied` settlement render asserting the displaced restore's `overwrittenIgnoredPaths` / `divergentGitlinks` warnings appear beside the not-applied line — non-empty against a rewind that overwrote an ignored path, absent-as-warnings but non-failing against one that overwrote nothing and against the conversation-only branch; a per-guard remedy render driven off `rejectionGuard` across all four literals, with the negative control that a `rejected` settlement carrying no literal renders `rejectionReason` alone and no remedy. RTL + `installMockBridge`, the T4.6 idiom.
  - **Consumes:** the T4.1 client's intervention call; the row facts Plan-013 passes at the mount (CP-004-15)

## Parallelization Notes

- Queue store and run-engine integration can proceed in parallel once the contracts are fixed.
- UI controls should wait for intervention result semantics to stabilize.

## Test And Verification Plan

- Queue durability tests across daemon restart
- State-machine tests for pause, resume, interrupt, and blocked states
- Manual verification of queue then steer then interrupt flows

## Rollout Order

1. Ship queue persistence and read-only queue visibility
2. Enable queue mutation and the orchestration-layer run-controls — interrupt, cancel, pause, resume (none capability-gated, I-004-10)
3. Enable steer where driver capabilities allow — `steer` and the campaign-B2 `rollback` intervention are this plan's two driver-capability-gated controls (rollback sequenced by campaign B9: daemon handler T3.12, provider-leg + file-restore T3.13, desktop gate T4.2, CLI command T4.7; the 2026-08-16 rewind-hardening amendment adds T3.16's compaction-boundary classification, T3.17's edit-and-resend composite, and T4.7's `--replace-message` option that reaches it to the same step, and the 2026-08-18 placement amendment adds T4.8's desktop entry affordance beside T4.7 — none is a separate control, the first narrowing which rollback targets are admissible and the rest riding the rollback payload or merely entering it)

## Rollback Or Fallback

- Disable `steer` and `rollback` (this plan's two capability-gated controls — I-004-10 as amended; `rollback` added by campaign B9 via T3.12–T3.14 / T4.2 / T4.7, extended by the 2026-08-16 rewind-hardening amendment via T3.16 / T3.17 and T4.7's `--replace-message` option) if driver-capability handling regresses; queue + the orchestration-layer controls (interrupt, cancel, pause, resume) stay enabled — they are not capability-gated (I-004-10). A rollback whose file-restore leg cannot proceed is handled **fail-closed within the intervention itself**, so no plan-level fallback is needed: for a writable live root an unavailable snapshot or a failed HEAD precondition **rejects the whole intervention pre-dispatch** (no leg applied), and an execution-time restore failure after the confirmed conversation leg records `files-unrestored` / `files-partially-restored` (T3.13); `conversation-only` is confined to the Spec-004 cases — `read-only` mode and a genuinely nonexistent restore root (the released-context rootless class, or the recorded checkout root itself gone — a live context released into that class at confirmation, an already-released terminal source arriving in it) — never a general file-leg-unavailable degradation: a live root whose nested execution directory was deleted restores through the recorded checkout root (T3.13).

## Risks And Blockers

- Provider capability mismatch can produce misleading controls
- Queue concurrency across multiple clients needs strict daemon serialization
- **Fork-across-compaction semantics are unverified on both provider legs (2026-08-16 rewind-hardening amendment).** What a provider rewind reconstructs across its own context-compaction boundary — pre-compaction context, or the compacted summary alone — is the open question the `Spec-004 §Driver-Level Rollback Mechanics` ADR-017 re-verification obligation records, and it is unanswerable from the checked-in probe records alone. I-004-20 makes this **fail closed rather than blocking**: every V1 driver refuses boundary-crossing targets, so T3.16 ships without waiting on the re-verification, and the refusal lifts per-driver only when that verification lands against a then-installed binary. The risk is therefore scope, not correctness — some rewind targets a user might want are refused until the question is answered.
- **The `resend-unapplied` arm depends on queue admission being able to fail after a confirmed rewind (2026-08-16 rewind-hardening amendment).** If T2.9's admission path is in practice infallible once the run is `paused`, the committed-then-failed arm becomes unreachable and its test would assert nothing; T3.17's test plan therefore drives the arm through an injected admission failure rather than hoping to observe one, and the arm stays specified because the caller's text must be recoverable even from an arm that is rare.
- **The composite's desktop affordance is placed and assigned (2026-08-18 placement amendment — the 2026-08-16 rewind-hardening amendment's one named residual, now retired).** That amendment left _where_ an edit-message control belongs in the turn surface open, because the question spans the Spec-013 timeline surface (whose rows the composite supersedes) and the renderer shell's composer area, neither of which this plan owns; T4.7's `--replace-message` kept the composite reachable from the CLI in the interim, so the leg was never stranded. The placement is now settled: a hover-revealed pencil in the participant `user.message` row's footer opening an inline editor — authored by **T4.8** in this plan's `run-controls/` subtree, mounted by Plan-013 at the row-footer composition point (**CP-004-15**), and sequenced behind the replacement-send wire-arm registration by **BL-153** — a gate that is now **met**: the registration landed 2026-08-25 (§6 node NS-75), BL-153 closed `completed` with it, and the §Preconditions box is checked, so T4.8's task-scoped hold no longer stands. The residual risk that remains is ordinary sequencing, not an open design question: the affordance ships dormant in Phase 4 and activates from Phase 3B, so the desktop surface stays unreachable-by-omission (never fail-open) until the handler, the composite, and the wire arm are all in place — the wire arm being the one of the three already in place since 2026-08-25.

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped:
  - phase: 1
    task: [T1.1, T1.2, T1.3, T1.4, T1.5, T1.6, T1.7]
    pr: 402
    sha: 5cbe1614
    merged_at: 2026-09-01
    files:
      - packages/contracts/src/__tests__/runControl.test.ts
      - packages/contracts/src/index.ts
      - packages/contracts/src/runControl.ts
      - packages/runtime-daemon/src/migrations/0015-queue-and-interventions.ts
      - packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts
      - packages/runtime-daemon/src/session/__tests__/session-service.test.ts
      - packages/runtime-daemon/src/session/migration-runner.ts
    verifies_invariant: [I-004-1, I-004-2, I-004-4, I-004-22, I-004-23]
    spec_coverage:
      [
        "Spec-004 §Interfaces And Contracts (QueueItemCreate / QueueItemList / QueueItemCancel; InterventionRequest; InterventionResult 6-state + RunStateChange event)",
        "Spec-004 §State And Data Implications (queue items durable storage + intervention audit records)",
        "Spec-004 §Required Behavior (pause/resume as orchestration-layer triggers)",
        "Spec-006 §Run Lifecycle (run_lifecycle)",
      ]
    notes: |
      Phase-1 tasks T1.1-T1.7 shipped as one contracts+migration PR. runControl.ts lands the full Phase-1 contract surface: the QueueItem trio with the Tier-6 workspaceId run-binding field; the InterventionRequestPayload discriminated union including the rollback arm's replacementSend member (registered 2026-08-25, NS-75, so the arm carries no hold); InterventionRequestResponse with the state-split RollbackInterventionResult union (RollbackAppliedResult / RollbackDegradedResult, the class-scoped resendDisposition fragments, the required-and-nullable newestBoundaryPosition on boundary-diverged, rejectionReason required on the rollback rejected arm); RunPauseRequest / RunResumeRequest + RunControlAck; and the RunStateChangeEvent wire projection. Migration 0015 lands queue_items + interventions plus the T1.5 command_receipts forward-declared shell, registered in migration-runner as a guarded per-version block (no contiguity claim - 0016/0017 landed in parallel branches). One codex P1 declined with evidence and pinned in a doc-comment: RunStateChangeEvent is the run.subscribeState WIRE projection (previousState/currentState, no sessionId - the subscription scope carries it), a deliberately distinct shape from the durable Spec-006 run-lifecycle payload owned by Plan-006's SessionEventSchema; the subscription server projects durable to wire. A second round-1 ruling: filesystemPathSchema stays unbounded in length (min(1) + NUL-reject only) because Windows extended-length paths have no 260/4096 bound - byte bounds belong to the framework body-size limit.
      verifies_invariant = the parse/DDL-level set canonically enforced by this shipment: I-004-1 (queue contracts parse + queue_items durable table), I-004-2 (the response union parses strictly per interventionType - a malformed rollback result fails validation), I-004-4 (the payload union's fail-closed parse), and the database halves of I-004-22 / I-004-23 (the interventions origin CHECK requiring the admitting principal exactly on the participant arm with no DEFAULT, so an unstamped insert fails closed; the queue_items participant-keyed PII envelope columns), all asserted by the migration-shape suite. The declared T1.4 / T1.6 / T1.7 contributions toward I-004-3 / I-004-6 / I-004-7 are behavioral and their canonical suites land with the Phase 2-3 daemon services, so they are recorded against those phases per the Plan-005 Phase-1 precedent. T1.5 declares Verifies: none (structural shell - CP-004-2). Review: codex code review completed with the round-1 folds taken in-diff; CI green on the squash head.
```

### Notes

- 2026-09-06 — **Typed composite refusal guard (§6 node NS-101).** Status flipped `approved → review` and restored `approved` in the same diff by the typed-composite-guard targeted readiness-audit delta riding this PR, jointly with Spec-004; both §Preconditions records carry the scoped Re-opened/Delivered pair. I-004-21's four structural refusal guards had reached the wire untyped — the composite settled `rejected` carrying `rejectionReason` alone, which is a **machine-readable cause and never prose** but whose **vocabulary is open**: `error-contracts.md §Intervention` deliberately registers no code for an intervention outcome, so no contract enumerates the causes a rollback `rejected` may carry and a client rendering a per-guard remedy could show that cause but never switch on it exhaustively — a family added later carries a new identifier every reader falls through, at no compile-time cost (PR #449 round 3, correcting the "free-form human sentence" premise rounds 1–2 carried against this registry's own definition and the shipped console's never-prose `code` slot). The `rejected` arm now carries the closed-union `rejectionGuard`, arm-scoped and producer-obligated: **arm**-scoped rather than base-scoped because only a `rollback` request can be a composite, so a strict parse refuses the member everywhere it could never legitimately appear; **producer**-obligated for the reason `resendDisposition` already is, since no member of a `rejected` response identifies its request as composite. The four literals are Spec-004's own guard names kebab-cased, so the wire vocabulary is derived from the normative paragraph rather than invented beside it, and a fifth guard amends both together. **No invariant is minted** — I-004-21 is widened in place on the NS-100 precedent, so the preflight Gate-4 invariant-reference census pin does not move. **One column is minted** at the round-2 fold — the additive nullable `interventions.rejection_guard`, the replay carrier — and **no census moves**, a column not being a table: counted directly over the final files, local SQLite stays **59** `CREATE TABLE` statements, and no intervention type, wire method, event type, error code, Cedar action, or table is added, these guards settling as `rejected` **results** rather than `JsonRpcError` envelopes. This plan's own closed sets are likewise unmoved, each re-derived by count: invariants **25**, cross-plan obligations **16**, `Spec-004 §Acceptance Criteria` **10** (the composite criterion extended in place), and the task set unchanged — T1.3, T3.12, T3.17, and T4.8 are amended, none added. Producer T3.17, contract T1.3, reader T4.8 (its per-guard remedy render, with the absent-literal degrade as its negative control), each already gated — the member is not minted ahead of a reader. `Spec-004 §Acceptance Criteria` is extended **in place** on its composite criterion, so the criteria set stays ten.
- 2026-08-26 — **Codex rewind rebound to `thread/fork` (§6 node NS-85).** Status flipped `approved → review` and restored `approved` in the same diff by the NS-85 targeted readiness-audit delta riding this PR; the paired-spec and plan-readiness §Preconditions boxes were Re-opened and Delivered in-diff. **Classification: FLIP** per the runbook [§Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row 4 — this plan's authored task bodies change, so it is **not** the citation/narrowing class it was first recorded as. Three sites are corrected. **(1)** The §Risks And Blockers provider-rewind-currency MUST is **narrowed, not deleted**: the Codex half is discharged — re-verified against `codex-cli 0.149.1`, where `thread/rollback` is deprecated in the generated type and the mechanism binds to `thread/fork` at an inclusive `lastTurnId` — while the Claude `--resume-session-at` + `--fork-session` half stays outstanding and still gates this leg's code. **(2)** The binding-lineage paragraph's **V1 instance** is re-derived: both V1 drivers now fork-and-supersede, so the generic rule's in-place generation-rotation arm is retained as forward-looking contract with no V1 driver. **(3)** T3.5's two rotation-keyed test assertions are **withdrawn** — a phase must not ship a test that cannot be instantiated — with the reused-ordinal disambiguation they carried re-homed onto the fork path. That withdrawal is the task-body change that makes this a flip rather than an erratum. This plan still gains no task, invariant, obligation, or phase. **Census: no move** — the flip is restored inside this diff, so the README plan-status census is unchanged.
- 2026-08-26 — **Turn-scoped effective-principal publication (§6 node NS-84).** Status flipped `approved → review` and restored `approved` in the same diff, jointly with Spec-004; both §Preconditions audit-relevant boxes re-opened and re-checked in-diff. Adds **T3.19**, **I-004-25**, and **CP-004-16** ⇄ Plan-005 CP-005-10: the run engine resolves each turn's effective principal from the `interventions` admitting-principal rows and **publishes** it on the run execution context, where Plan-005's usage normalizer stamps it onto `usage.cost_update`. It is published rather than emitted here — this plan mints no event, no column, and no wire member, so no census moves. The resolution rule is unchanged and stays Spec-012's, so Spec-012 and Plan-012 do **not** flip; T3.19 consumes Plan-012's T2.2 resolver rather than re-deriving it, so "who is the effective principal" has one implementation. Checks the two Phase-4B Preconditions boxes on Plan-016 and Plan-029 that this carrier had held since 2026-08-18.

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

**Round 2 (Codex on 8e6583a) — 10 findings, all fixed in one commit.**

- **F1 (contracts):** `RollbackInterventionResult` restructured from the four-literal `fileRestore` into a `disposition` discriminated union spanning the full `Spec-004 §Required Behavior` vocabulary — file-leg (`files-restored` / `files-partially-restored` (+`failedStep`) / `files-unrestored` / `conversation-only`) plus the pre-file arms (`pause-only`, `nothing-applied`, `position-mismatch` (+`requestedPosition`/`confirmedPosition`)); never-collapse preserved; T1.3 mirror + T4.7 rendering updated.
- **F2 (contracts):** root-`busy` reclassified as pre-dispatch whole-rejection, not `files-unrestored`; the `files-unrestored` comment now names execution-time refusal causes only (the HEAD re-verify) and drops "root busy".
- **F3 (contracts):** `InterventionRequestResponse` discriminated per intervention type — a `rollback` outcome that fails `RollbackInterventionResult` parse fails validation, never falling through to the generic `Record<string, unknown>` arm; test added.
- **F4 (T4.7):** explicit result-state→POSIX-exit contract added (`applied`→0; `rejected`/`expired`/`degraded`→distinct nonzero) alongside the transport `JsonRpcErrorCode` mapper; the static `driver.capability_unsupported` refusal rides the **result** as `rejected` carrying the new `rejectionReason` field, not the transport channel; per-state test.
- **F5 (T3.11):** the "typed stamp shape Plan-006-owned" phantom replaced by CP-004-12 — a named merge prerequisite for the owed Plan-006 epoch-attribution carrier + a dep-map Plan-004→Plan-006 edge; no Plan-006 symbol authored. **Lead-owned follow-up:** register the typed epoch-attribution field in Plan-006 / Spec-006 (the B11 Plan-006 bundle shipped without it).
- **F6 (T3.12):** the waiting-source void now names the durable actions — `approval.canceled` via Plan-012 `cancelPendingForRun` and `driver_ask.canceled` via its owning surface (both consumed, not authored), atomic with the rollback transition — plus the queued-resume void and the progression-serialized race; test added.
- **F7 (T3.13):** new `run.execution_root_released` (409, run state unchanged) added to `error-contracts.md §Run` as a Plan-004 extension (the Plan-027 error-contracts-extension precedent) and named in T3.13's non-resumable rule; test added.
- **F8 (T4.7):** `apps/cli/src/main.ts` added to T4.7 Files (EXTEND) with the Plan-007 ownership note (T-007r-3-2 CREATE; clipanion has no auto-discovery) + a dep-map §2 row + an invocation-through-real-`Cli` registration test.
- **F9 (fallback bullet):** reworded fail-closed — an unavailable snapshot / failed HEAD precondition rejects pre-dispatch and an execution-time failure records `files-unrestored` / `files-partially-restored`; conversation-only is confined to read-only mode + a released root.
- **F10 (T3.14):** the exported `supersededTurns(runId)` read seam named + CP-004-13 + a dep-map Plan-013→Plan-004 consumer edge; no Plan-013 symbol authored. **Lead-owned follow-up — landed 2026-07-20:** the Plan-013 consumer registration (`timeline-projector.ts` + `TimelineRead` calling the seam; Plan-013 §Implementation Steps / Spec-013 §Required Behavior).

**Round 3 (Codex on abc44be) — 3 findings, all fixed in one commit.**

- **F1 (contracts):** the rollback response arm is state-split — a terminal rollback outcome (`applied` / `degraded`) REQUIRES `result: RollbackInterventionResult` (a disposition-less terminal response fails parse; test), while `requested` / `accepted` / `rejected` / `expired` rollback responses carry no `result` (`rejectionReason` covers the rejected cause); T1.3 mirror updated.
- **F2 (T3.13):** the execution-epoch trigger corrected — the epoch advances with **each accepted `run.rolled_back`** (a confirmed conversation rewind, including a position-mismatch rewind confirmed at the recorded floor), regardless of the file-leg disposition, per `Spec-004 §Required Behavior`'s collision requirement (re-executed turns reuse position ordinals, so a degraded-confirmed rollback keying the epoch on the final `applied` state would collide with the superseded epoch's `turn-<N>` refs); test added. The same stale "per applied rollback" shorthand in Plan-010's ratified `<E>` epoch-source box (§Phase 5 — Turn-snapshot service) and Spec-010 §Turn-Boundary Snapshots is a **lead-owned erratum** riding the queued Spec-010 errata batch — out of this PR's scope, not re-imported here.
- **F3 (preconditions):** Phase 3's `preconditions:` block gains a Plan-005 `plan_phase` entry (the campaign B10 parity bundle's placement of the four-op `ProviderDriver` widening + the `InterventionType` union widening per CP-005-6), and Phase 1 gains a new `preconditions:` block with the same entry gating T1.2's `'rollback'` arm — both machine-enforced via preflight Gate 5's declared-⊆-shipped resolution. _Placement corrected pre-merge (lead verification against the B10 branch): the widening is Plan-005 **T1.8 — Phase 1's** contract catch-up task (re-opening the shipped phase), not a Phase-3 task; both entries retargeted `phase: 3` → `phase: 1`, which also avoids transitively dragging Plan-005 Phase 3's own Plan-006 gate into this plan's chain._

**Round 4 (Codex on 2249792) — 6 findings, all fixed in one commit.**

- **F1 (T4.7):** a `rejected` rollback response renders `rejectionReason` only — the state-split contract carries no disposition outside `applied` / `degraded`, so the prior "rejectionReason + disposition" rendering would dereference a nonexistent field or force the schema loose; rendering + tests corrected.
- **F2 (T4.7):** pending states defined — a `requested` / `accepted` response never exits the process: the CLI waits for the terminal state by idempotent replay (re-dispatching with the SAME `clientIdempotencyKey` minted at invocation start; the T1.2 replay contract returns the recorded row, never re-applies); accepted→applied and mid-wait-expiry tests added. _The "bounded by the intervention's own expiry backstop" termination claim this round recorded was wrong — corrected in round 5 F6: `expired` has no wall-clock trigger, so the bound is the client-side `--wait-timeout`._
- **F3 (Phase 4):** the Plan-007 R3 CLI-scaffold dependency is machine-gated — `{ type: external_plan_phase_merged, plan: 007, phase: R3 }` (the Plan-022 Phase-4 R-series precedent) joins the YAML block; the prose-only form was Gate-5-invisible, letting Phase 4 dispatch before `apps/cli` exists. The 2026-08-16 rewind-hardening amendment adds a second T4.7-scoped entry on the same reasoning — `precondition_box_checked` on the replacement-send wire-arm box, since `--replace-message` has no `replacementSend` member to populate until that registration lands. Both entries are phase-scoped by construction (Gate 5 resolves per phase, never per task); the over-capture is accepted here exactly as it already was for the R3 entry, because the alternative — prose-only scoping — is invisible to the gate.
- **F4 (contracts):** `position-mismatch` regrouped from the no-rewind group into the confirmed-rewind group (supersedes the round-2 grouping): its conversation leg DID rewind — to the confirmed floor — so `run.rolled_back` emits at the confirmed position and the execution epoch advances, exactly the T3.13 trigger; grouping it beside `pause-only` / `nothing-applied` invited implementations to skip both.
- **F5 (T3.13):** the rootless / read-only branch selects conversation-only pre-resolution but records the disposition **only after** the driver-confirmed rewind — a failed or unconfirmed conversation leg records `pause-only` / `nothing-applied`, never a fabricated `conversation-only` success; test added.
- **F6 (T1.4 / T3.12 / contracts / schema):** `rejectionReason` is replay-durable — the new nullable `interventions.rejection_reason` column (Plan-004-owned DDL) is written at T3.12's refusal path and read back on idempotent replay; the wire arm forbids `result` on `rejected`, so the cause could not ride the stored outcome JSON.

**Round 5 (Codex on b503b34) — 7 findings, all fixed in one commit.**

- **F1 (contracts / T1.3 / T4.7):** the terminal rollback arms are state-scoped per disposition class — `applied` admits exactly `RollbackAppliedResult` (`files-restored` / `conversation-only`) and `degraded` exactly `RollbackDegradedResult` (the other five) — so a malformed response pairing `state: "applied"` with a degraded disposition fails parse instead of exit-mapping to 0 while rendering a failed restore; `RollbackInterventionResult` stays the union; the `Queue And Intervention Model §Driver Result To Lifecycle Mapping` degraded enumeration gains the missing `nothing-applied` (a b9-introduced literal, set-quantifier re-derive).
- **F2 (Preconditions / Phase 3):** the CP-004-12 Plan-006 epoch-attribution carrier is machine-gated — a new unchecked `## Preconditions` box ("Plan-006 epoch-attribution carrier registered", checked by the lead-owned Plan-006/Spec-006 amendment PR per the Part-B precedent) rides a Phase-3 `precondition_box_checked` entry, paired with `{ plan: 006, phase: 1, status: merged }` for the shipped-carrier ordering; prose alone was Gate-5-invisible, letting Phase 3 dispatch before the carrier exists.
- **F3 (contracts / T1.3 / T3.12):** `rejectionReason` is REQUIRED on the rollback `rejected` arm — every refusal family of `Queue And Intervention Model §Intervention State Transition Table` (boundary-check, root-`busy`, T2.5 authorization, capability) persists its machine-readable cause to `interventions.rejection_reason`, not only the capability gate; a cause-less rejected settle would be unreconstructable on idempotent replay; tests added.
- **F4 (T3.11):** a pre-rollback-epoch `driver_ask.requested` is a surface-opening event, not a passive row — the late-append path records it as an atomic closed pair (epoch-stamped `requested` + `canceled`), registers no pending ask surface, fires no `waiting_for_*` transition, and never hands a permission-kind stale ask to the approval-pipeline normalizer; tests added.
- **F5 (T3.12):** a `waiting_for_approval` void whose pending approval was normalized from a permission-kind driver ask settles BOTH surfaces — `approval.canceled` plus the originating native `driver_ask.canceled` — within the same atomic void (provenance consumed from Plan-012's T2.8 normalizer, no Plan-012 symbol authored), so the provider-side ask cannot stay pending or later respond against the rewound turn; race test extended.
- **F6 (T4.7):** the round-4 "expiry backstop" termination claim was wrong — `expired` fires only on a version advance or target-state change (`Queue And Intervention Model §State Model`), never a deadline — so the pending wait is client-side bounded: `--wait-timeout <seconds>` (default 600; `0` opts into unbounded) exits with its own distinct code, printing `interventionId` + `clientIdempotencyKey` so a later same-key re-invocation resumes the idempotent replay; test added.
- **F7 (T3.14):** supersede marks are scoped to the epoch each accepted `run.rolled_back` rewound — a re-executed turn never inherits the mark its reused ordinal carries in an earlier epoch (rollback 10→5, re-execute, rollback 7→6 keeps the second epoch's turn 6 current per the `6 > 6` cutoff while the first epoch's turn 6 stays superseded); the prior "still marked by its source epoch" wording invited exactly that inheritance; behavior + test rewritten.

**Round 6 (Codex on 404239b) — 3 findings, all fixed in one commit.**

- **F1 (T3.12):** the waiting-source void is explicitly deferred to the **confirmed conversation leg** — atomic with the `waiting_for_* → paused` transition and the forward emission, never at admission — matching `run-state-machine.md §Rollback Transitions`' conversation-leg-confirmed gate; a failed or no-rewind leg leaves the approval/input block and its native ask intact, the run stays `waiting_for_*`, and the outcome records `nothing-applied`; the prior "atomic with the rollback transition" wording admitted a void-before-rewind reading that would drop a still-valid block; test added.
- **F2 (T3.11):** pre-rollback-epoch **terminal-class** stragglers are absorbed at the epoch check itself, before the T3.7 terminal emitter — the reopened run's current `runVersion` carries no terminal row, so the emitter's `(runId, runVersion)` duplicate guard alone would append the stale terminal against the new epoch and re-terminalize the rewound run; the route-into-emitter path is confined to the already-terminal condition it is safe for; live + replay test added.
- **F3 (T3.13):** the same-position file-leg recovery is defined as its own confirmed branch — the conversation leg's convergence no-op (driver confirms `sessionPosition == targetPosition`, no movement) counts as the confirmed floor, so the composite proceeds to `restoreToTurn` and re-runs to the fixpoint with the forward event + uniform epoch advance, instead of falling into the ordinary no-rewind handling (`nothing-applied`) that would have made the T3.12 recovery carve-out unreachable; test added.

**2026-07-20 — W2.5 targeted re-audit (campaign Task 23, B9 delta — the rollback / late-absorption / ask-expiry-park task set with its contracts, schema, and dep-map edits): zero critical / zero major findings (5 minor / 2 nit dispositioned in this closing PR); Status promoted `review` → `approved`.** First-time promotion of this standing-`review` plan (the Tier-5 / NS-17 readiness-audit box is checked; the D-004-1..3 ratifications executed at the Tier-5 swap) — the closing re-audit PR (#228) is cited in lieu of a tier git-tag (runbook R7 durable-citation refinement). The seven dispositions, all fixed here: (1) T4.7's promised same-key resume was unreachable — the command gains `[--resume-key <clientIdempotencyKey>]`, reusing the printed key verbatim instead of minting fresh (Codex post-merge round on PR #225, as are 2–4); (2) I-004-14 routed every terminal-class straggler into the T3.7 emitter — rescoped to T3.11's round-6 behavior (already-terminal → emitter no-op; pre-rollback-epoch, terminal-class included, absorbed at the epoch check before the emitter); (3) I-004-15's "always lands the run in `paused`" overclaimed — landing is conversation-leg-confirmed-only, a non-confirming outcome leaves the source state in place; (4) the bare `files-restored` applied arm gains the two REQUIRED Spec-010-mandated enumerations `overwrittenIgnoredPaths` / `divergentGitlinks` (empty-when-none) — Plan-004-owned wire field names onto which T3.13 maps the enumerations the Plan-010 T5.2 `restored` variant carries (the callee-side shape stays Plan-010-owned), surfaced by T4.7's success render; (5) I-004-13 + T3.7 still said trigger "pair" — Plan-006's round-3 promote leg made it a trio (`trg_run_terminal_key_promote`; repo-swept: these were the only two stale sites); (6) T3.13's Verifies line gains I-004-3 (the invariant's own text names T3.13 the provider-leg author of the dispatch-axis exception); (7) the sweep's full invariant-matrix pass found one sibling — I-004-4's column claimed T1.7, which no task or spec text supports (T1.7 claims only I-004-7); the column entry is dropped. Audit dimensions: file-authority closure; spec-coverage integrity (§-anchors verified against spec content); invariant claims (I-004-13..17 re-walked against task behaviors — the two overclaims above were the only mismatches); machine-gate shape + acyclicity (the Phase-1/3/4 `preconditions:` chains resolve forward-only); cross-plan ownership (every consumed symbol named to its owner; no Plan-005 / 006 / 010 / 012 / 013 symbol authored); census; consumer readiness (the CP-004-7/8/10/13 provide-forward contracts are concrete for their consumers); internal coherence. Dispatch layering is tightened, never loosened, by this PR: the CP-004-12 and partial-restore-enumeration boxes stay unchecked and Phases 3–4 stay Gate-5-held until the Plan-006 carrier, the Plan-010 partial-restore amendment, Plan-010 Phase 5, and Plan-005 Phase 1 land — promotion clears the doc-status gate only. Codex round 1 on this closing PR added two dispositions, both fixed here: the Spec-004 confirmed-floor no-op parenthetical gains the recovery-convergence exception at its three sites (§Required Behavior, the §Driver-Level confirmed-floor bullet, the acceptance criterion) — an erratum-class narrowing under the runbook's spec-amendment rule (typo / citation / narrowing does not flip the spec), reconciling the sentence with the same bullet's carve-out whose "restore re-runs to the fixpoint" text the plan's round-6 branch implements; and the contracts comment's "mapped verbatim" claim is reframed as the explicit Plan-004-owned wire mapping (the callee-side result shape stays Plan-010's to pin — cross-plan one-writer). Codex round 2 added two more, both fixed here: the landing-overclaim class the audit fixed on I-004-15 also lived in Spec-004's own acceptance criteria and the `run-state-machine.md §Rollback Transitions` intro — the ACs now scope the `paused` landing and the waiting-source block void to the conversation-leg-confirmed rewind with the no-rewind source-state preservation stated on the failure-path criterion, and the domain doc's intro sentence + waiting bullet gain the confirmed qualifier their own transition-table rows and confirmed-leg paragraph already carried (the same erratum-class narrowing — the semantics were derivable from §Required Behavior's nothing-for-every-other-source path); and the `files-partially-restored` wire arm carried only `failedStep` while Spec-010's rationale for the distinct partial disposition is precisely that earlier steps' effects are already on disk — the arm gains the same two REQUIRED never-silent enumerations (those the steps completed before the failure produced; empty-when-none), mapped by T3.13, rendered by T4.7's degraded render, I-004-16 extended; with the partial outcome now carrying the first pass's overwrites durably, the recovery pass's empty enumeration is honest (it overwrote nothing) — the callee-side naming on `partial_restore` stays the Plan-010-owned erratum in the queued Spec-010 errata batch. Codex round 3 added one: the `DriverRollbackResult` floor-validation mirror in the contracts doc's driver section still carried the unexcepted no-op-is-no-rewind rule the round-1 narrowing excepted at Spec-004's three sites — a fourth mirror, now carrying the same recovery-convergence exception (a comment-only coherence fix of Spec-004-owned validation semantics sitting in the driver-contract region, per the dep-map housekeeping exception; the driver result shape is untouched); the repo-wide floor-rule sweep confirms no fifth mirror — the wire `targetPosition` comment already carried the carve-out, and the `RunRolledBackEvent` comment is convergence-accurate as written. Codex round 4 added three, all fixed here: the narrowed landing AC overclaimed `targetPosition` for the confirmed-mismatch branch — the AC and the `run-state-machine.md` transition rows' position claims now land at the confirmed position (`targetPosition` bar a position-mismatch degrade); the partial-arm enumerations' producer side is now a **named merge prerequisite** — a new unchecked §Preconditions box ("Plan-010 partial-restore enumerations registered") + Phase-3 `precondition_box_checked` entry hold T3.13's dispatch until the lead-owned Plan-010/Spec-010 amendment lands (the CP-004-12 pattern; authoring Plan-010's T5.2 here would breach cross-plan §2 one-writer and falsify this audit's no-Plan-010-symbol attestation); and the restart-reconciliation rule was ambiguous for a dispatched-but-unconcluded same-position recovery, whose at-target and unchanged observations coincide by construction — reconciled as **at-target** at both Spec-004 sites (completion branch, never the no-rewind conclusion that would strand the partial tree), with the T3.13 crash-window test added.

**2026-08-03 — Cross-user run-control authorization (paired Spec-004 / Spec-012 amendment): Status flipped `approved` → `review`; the §Preconditions paired-spec box re-opened.** [vision.md §The Collaboration Model](../vision.md)'s ratified statement — a participant steers agents in the session, "steering an agent owned by another participant is granted by that owner's approval policy, never by membership alone," and "membership alone never authorizes execution on someone else's machine" — had no contracted authorization shape on the intervention path. [Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts) now names it: the Cedar action for a run-control intervention is `Action::"intervene"` on the target `Run` resource with the run's hosting node in the request context, authorization evaluates against session membership role and never against run authorship, and a non-local hosting node **adds** the target-node-owner approval requirement rather than substituting for the role check. **Adds** invariant **I-004-19** (cross-node intervention never bypasses node-owner authority; settles `rejected` when that approval is absent or denied) on **T2.5**, whose Spec-coverage and Consumes markers gain the hosting-node context input and the [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) approval leg (consumed, never authored — cross-plan §2 one-writer holds). **Mints no** approval category, intervention type, wire field, table, or CP row: the cross-node leg reuses `tool_execution`, and the four canonical intervention types are unchanged. **The rule's reach is all six run-control verbs (Codex round 2 on the reconciliation PR):** the orchestration-layer `run.pause` / `run.resume` — daemon-handled, never `applyIntervention`-dispatched — route through the identical `Action::"intervene"` adjudication before composing any run-state mutation, extending **I-004-19 to T3.4 / T3.5** the same deliberate cross-cutting way the Tier-5 audit extended D-004-2's mandatory comparand to them: both tasks' Behavior lines adjudicate-then-guard, their Verifies-invariant markers carry the claim, and the local + remote acceptance arms land with the restoring delta (reciprocal: Plan-012 T2.7's matrix names the pause/resume arms, consumed never authored). New normative spec behavior on an `approved` plan is the same shape that flipped Plan-006 (2026-07-29, 2026-08-02), Plan-024 (2026-08-02), and Plan-008 (2026-08-03), so the paired-spec box re-opens and Status flips `review`; the restoring targeted readiness-audit delta audits the amendment, re-checks the box, and restores `approved`. **The re-opened gate is plan-wide:** preflight Gate 7 scans the plan-template governance trio plus the header Status row, so it halts every phase, Phase 3 included — it is **not** the phase-scoped Gate 5 `precondition_box_checked` mechanism, which covers only the three named scoped boxes (driver input-ask park leg, Plan-006 epoch carrier, Plan-010 partial-restore enumerations), none of which this amendment touches. Dispatch layering is tightened, never loosened: every pre-existing Gate-5 hold stands unchanged beneath the new plan-wide hold.

**2026-08-08 — Cross-user run-control authorization targeted readiness-audit delta (PR #299; §6 node NS-49): Status restored `review → approved` on this plan and Spec-004; the §Preconditions paired-spec box re-checked.** The delta audited the 2026-08-03 amendment against primary sources plus an adversarial cross-surface coherence pass — one minor finding (amended in this swap) beside the scheduled acceptance-arm delivery; zero critical / zero major. **The delivery:** [Spec-004 §Acceptance Criteria](../specs/004-queue-steer-pause-resume.md#acceptance-criteria) gains the local + remote authorization arms the 2026-08-03 bullet above scheduled — the local arm binds authorship-blind write-access admission (`owner` / `collaborator` admitted on another participant's locally-hosted run with `initiatorId`, creating participant, and agent owner never read as authorization inputs — the role grant bounded by Spec-012's node-trust rule, another participant's agent hosted on its owner's node under the credentials-never-travel invariant and so always taking the remote arm; `runtime contributor` / `viewer` denied, a denied intervention settling `rejected` while a denied `run.pause` / `run.resume` request is rejected untouched, composing no run-state mutation — the orchestration verbs settle no `InterventionResult`, exactly I-004-19's T3.4 / T3.5 extension), and the remote arm binds the additive node-owner `tool_execution` leg confined to Spec-024's dispatch lifecycle — `dispatch.denied` on an approver's refusal, `dispatch.expired` on unresolved caller-token expiry, the post-receipt `dispatch.rejected` on the target's role-check failure, the intervention capability handler never executing and no `InterventionResult` created on any deny — under the unobtainable-approval-is-absent fail-closed reading (reciprocal: Plan-012 T2.7's cross-user intervention matrix names the test-side arms, consumed never authored). **The finding:** [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) positively enumerates Cedar's controlling inputs but carried no registration for the amendment's hosting-node request-context input — the authorization model's only controlling context input absent from the doc that owns the model; a run-control resource-context bullet now registers it (daemon-resolved, never client-supplied; Spec-004 contract text, Spec-012 rule owner). **Consumer-readiness disposition (recorded, no gate change):** the remote arm's producer chain post-dates this plan's tier — Plan-012's approval pipeline is Tier 6 and Plan-027's cross-node dispatch surface is Tier 9 — so at this plan's Tier 5 no run can be non-locally hosted and no node-owner approval can exist; the arm ships fail-closed by construction (with no dispatch surface the intervention capability handler is unreachable — an unobtainable approval is an absent approval), and its live cross-node exercise rides Plan-012 T2.7's matrix at that plan's tier — the disposition's gate shape is corrected by Codex round 2 below. **Structure re-verifications, all clean:** the amendment changes the adjudication's inputs, not its mechanism — I-004-8's pre-existing Tier-5-audited structure (Cedar per ADR-012 as the engine selection; verified PASETO `sub` as principal) stands, and the CP-012-5 / CP-012-11 walk confirms no Plan-012-owned evaluator symbol is consumed on this path; the Spec-004 Cedar shape restates [vision.md §The Collaboration Model](../vision.md)'s two ratified sentences faithfully (byte-checked); rule ownership resolves to [Spec-012 §Required Behavior](../specs/012-approvals-permissions-and-trust-boundaries.md#required-behavior) (amended in the same 2026-08-03 change, I-012-23 — Spec-012's own restoring delta is a separate vehicle); the [Security Architecture §Permission Matrix](../architecture/security-architecture.md#permission-matrix-task-54) run-control row, [§Inter-Node Trust Boundaries](../architecture/security-architecture.md#inter-node-trust-boundaries), [Spec-024](../specs/024-cross-node-dispatch-and-approval.md)'s every-cross-node-hop `tool_execution` rule, and the [Queue And Intervention Model](../domain/queue-and-intervention-model.md) all mirror the rule or stay coherently silent (no stale authorship-scoped authorization claim anywhere in the walked set); and zero inbound `:NNN` line cites target Spec-004 or this plan, so the acceptance-arm insertion and this bullet move no citer. Promotion ran the runbook's [§Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate) four criteria — Depends On closure (Spec-004's row names three canonical domain/architecture docs plus Spec-001 `approved`; Spec-012 is not among them, so no deadlock with its separate delta), no blocking open questions (§Resolved Questions records none), doc-first attestation (this plan's Shipment Manifest is empty and no code cites the amended rows), and this gate citation — and the [§Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#status-promotion-gate) with the closing PR cited in lieu of a tier git-tag (the R7 durable-citation refinement, the PR #228 precedent above). Codex round 1 on this closing PR added four dispositions, all fixed there: (1) the local-arm acceptance criterion, read in isolation, contradicted [vision.md §The Collaboration Model](../vision.md#the-collaboration-model)'s steering rule ("granted by that owner's approval policy, never by membership alone") — the criterion gains the node-trust binding that makes the agent-owner exclusion vision-consistent (every agent executes on its owner's machine under the credentials-never-travel invariant, so a run targeting another participant's agent is non-locally hosted by construction and the owner's-approval-policy authority is enforced as the hosting-node leg — the exclusion itself is the rule owner's own I-012-23 doctrine, unchanged); (2) the remote-arm criterion's deny settlement wrongly reached into the intervention lifecycle — corrected to Spec-024's dispatch terminals (`dispatch.denied` / `dispatch.expired` / post-receipt `dispatch.rejected`; the capability handler executes only after a signed `allow` ApprovalRecord, so no `InterventionResult` exists on any deny — the own-node `rejected` settlement stays this spec's per Spec-024's own-node out-of-scope line); (3) the api-payload run-control bullet registered the hosting-node context but left the local-transport principal unresolved (local IPC carries no PASETO token) — it gains the transport-verified principal registration (local socket ⇒ the node-owner participant identity bound at [Spec-003](../specs/003-runtime-node-attach.md) attach, daemon-resolved; cross-user callers ⇒ PASETO + DPoP via Spec-024 dispatch per the existing forwarding rule); and (4) the restoration's clearing scope gains the named Spec-012 doc-first hold recorded in §Preconditions above — status restoration is not dispatch eligibility for I-004-19's carriers while Spec-012 sits `review`. Codex round 2 added four more, all fixed there — correcting this audit's consumer-readiness disposition from no-gate-change to **three named unchecked §Preconditions prerequisites** (the CP-004-12 two-layer pattern: each box paired with Phase-2 + Phase-3 `precondition_box_checked` entries holding T2.5 / T3.4 / T3.5): (1) the **local node-owner principal binding** — `runtimenode.attach` is client-driven and hands the daemon no principal, so the local arm's Cedar principal rides the Plan-018-gated credential/identity provider seam (the CP-006-13 / `resolveCurrentParticipantId` precedent), failing closed while unwired; (2) the **per-run hosting-node carrier** — no shipped surface durably names a run's hosting node (CP-004-7's `getRun` triple, `run_execution_contexts`, and Spec-006's orchestration-optional `producingNodeId` all lack it, and an agent's mutable `defaultNodeId` would misclassify existing runs after rebinding), so the two arms are indistinguishable until the lead-owned amendment registers the carrier; (3) the **cross-node run-control dispatch capability** — no plan authors the handler contract, the caller-side dispatch branch, or the Plan-004 ↔ Plan-027 reciprocal registration, so the remote arm is fail-closed-unreachable (an undeclared capability rejects at intake per Spec-024's declared-capability rule) until the lead-owned amendment lands; and (4) the remote-arm criterion now pins the Cedar composition — the `Action::"intervene"` adjudication joins the target's pre-approval step-5 evaluation, terminating post-receipt `dispatch.rejected` before any owner prompt surfaces, never deferred to post-approval handler execution whose faults are `dispatch.failed`. Codex round 3 added three coherence ripples of those fixes, all aligned there: I-004-19's remote-arm settlement and Plan-012 T2.7's cross-node matrix arm (the latter under the cross-plan housekeeping exception — Plan-012 sat `review` with its own delta queued, which landed 2026-08-10 and restored it `approved`: PR #317, §6 node NS-54) now assert the dispatch terminals with no `InterventionResult` created, matching the corrected acceptance criterion; the Spec-004 Cedar-principal contract sentence, I-004-8, and T2.5's Consumes row now split the principal by transport — verified PASETO `sub` on identity-carrying transports, the daemon-resolved node-owner binding on the PASETO-less local socket (the provider-seam prerequisite) — instead of requiring a token the local transport never carries; and the api-payload cross-node sentences stop claiming DPoP verification on the dispatch path — Spec-024's envelope carries a request-hash-bound signed `caller_token` and no DPoP proof, so the token's `req_hash` binding is named as that transport's sender-constraining mechanism (the pre-existing Local-daemon forwarding sentence corrected in the same swap).

**2026-08-17 — Rewind hardening (compaction-boundary-aware targeting + atomic edit-and-resend; both legs user-ratified 2026-08-16): Status flipped `approved → review` and restored `approved` in the same diff, the §Preconditions paired-spec box re-opened and re-checked in the same diff.** The amendment composes with the campaign-B2 rollback intervention rather than forking it — same confirmed-floor, epoch-gate, write-ahead, and reconciliation machinery — and mints no event type, wire method, table, or intervention type (the round-2 fold later mints one typed error code, `run.compaction_boundary_diverged`, on the compaction leg). **Leg 1** adds I-004-20 and T3.16: rewind-target validation classifies `targetPosition` against the target run's newest **current** provider-side compaction (`usage.context_compacted` — deliberately not the daemon retention pass's `event.compacted`, which the pre-existing uncompacted-rewind-span check governs; the two are separately gated and a rewind must clear both), ranking the boundary by T3.14's operation-association rule rather than arrival slot, and refusing a boundary-crossing target whole and fail-closed pre-dispatch — the direction Spec-016's V1 finding forces, since neither provider wire exposes a compaction-restore operation. The refusal is liftable per-driver by the ADR-017 re-verification this plan's §Risks And Blockers now tracks, and `Spec-004 §Driver-Level Rollback Mechanics` records the pre-compaction-leaf anchoring obligation that re-verification is verifying against. **Leg 2** adds I-004-21 and T3.17: edit-and-resend rides the existing `rollback` intervention as an optional `replacementSend` payload leg. Two V1 mapping decisions are load-bearing and were taken deliberately against the surveyed reference shape. First, the replacement leg **admits** a queue item rather than dispatching a send: V1's rollback contract mandates that a confirmed rollback lands `paused` and never auto-resumes, so a dispatching resend would contradict two standing rules for one leg — "resend" here guarantees durable re-submission and in-order delivery, not that the message is already on the wire, and I-004-3's sole dispatch-axis exception is left intact. Second, the reference's crash-copy composer draft has **no V1 equivalent** (`Spec-017 §State And Data Implications` defers daemon-side draft persistence past V1), so the corrected text is persisted on the write-ahead intervention row instead — which is strictly better for the failure taxonomy, since that row is already the reconciliation source and therefore recoverable in all three arms (hardened to the participant-keyed PII-envelope split by the PR #344 round-1 record below — the claim here stands as the record of this day's diff). Three structural refusal guards map onto real V1 surfaces (an active turn; the T2.9 admitted-row-to-`user_text`-echo delivery window — widened to the full pending-send family by the PR #344 round-1 record below; orchestration-authored boundaries from Spec-016 / Spec-017), and the provider/mode constraint the reference expresses as a workflow-mode deadlock guard maps to guard (iii) rather than to a new mode check. **Client reachability** is authored in the same diff rather than deferred: T4.7's `run rollback` command gains `[--replace-message <text>]`, the option that populates `replacementSend`. The RA-7 reachability rule that minted T4.7 for the bare rollback applies unchanged to the composite — a spec'd, invariant-bearing, task-bearing leg with no invocation path would be unshipped by this plan's own standard — and the option is deliberately thin: one intervention under one `clientIdempotencyKey`, so the wait-by-idempotent-replay loop, the `--resume-key` resume path, and the terminal-state exit mapping are all unchanged, with the four structural refusals (the fourth joining at the round-2 fold) arriving as ordinary `rejected` results and the suppressed-or-failed resend on the ordinary `degraded` disposition. The **desktop** affordance — where an edit-message control belongs in the renderer's turn surface — is not authored here and is the amendment's one named residual (§Risks And Blockers). _(Closed 2026-08-18 by the desktop edit-affordance placement amendment — T4.8 / I-004-24 / CP-004-15, the dated bullet below; this record states what that amendment left open, and is preserved as written.)_ **In-swap targeted readiness-audit delta.** Claim-vs-content was run over this amendment's own diff, not over recollection: every added normative sentence was checked against the surface it cites, and the three anchors the amendment leans on were byte-verified against their real headings (`Spec-006 §Usage Telemetry (usage_telemetry)`, `Spec-016 §Resolved Questions and V1 Scope Decisions`, `Spec-017 §State And Data Implications`). The pass caught one of the amendment's own claims: a drafted sentence had the ADR-017 re-verification obligation recording the fork-across-compaction question open "on both legs", where the landed obligation in `Spec-004 §Driver-Level Rollback Mechanics` names it on the Claude bullet only — the sentence now points at the section-scoped anchoring rule this amendment adds (both-legs by construction) instead of at a per-leg record that does not exist, reconciling with the landed text rather than duplicating or widening it. Census arithmetic: **net-unchanged** — no plan or spec ends this diff at a different Status than it started (Spec-004 and Plan-004 both `approved → review → approved` within the diff; Spec-013's two edits are erratum-class narrowings and move no Status), and no SQLite or Postgres table is added or removed, so the README / CLAUDE.md table censuses are untouched. Set-quantifier sweep across every enumeration the two legs touch: Spec-004's pre-dispatch validation list and its rewind-domain rejection families, the `Queue And Intervention Model` `requested → rejected` refusal row, its `degraded` disposition row (five → six, and the neighbouring "completes this enumeration" claim disambiguated to the no-rewind-source enumeration it actually scoped), its canonical `rollback` payload shape and Field-Level Consistency table, its §State Model `degraded`-meaning row and its §Boundary enumeration of the states a `rollback` may target (both of which stated the pause-first rule or the degrade causes without the composite's narrowing), the `Run State Machine` pause-first rule for a `running` source and its §Implementation Note guard enumeration restating that rule, Spec-013's §Pitfalls To Avoid list, this plan's `RollbackDegradedResult` count and `T3.2–T3.17` task ranges, the replacement-send box's own consumer list (T1.2 / T1.3 / T3.17 / T4.7, swept when the CLI option joined it), and Spec-004's "rollback controls only" V1 scope line (unchanged in extension — the composite adds no sixth control, and `--replace-message` is an option on an existing command, not a new one). **Seeded-refutation negative control:** the plausible-but-wrong twin "the timeline truncates when the resend is admitted" was posed against the authored rule and refuted by it — supersede marking is driven **solely** by the accepted `run.rolled_back` cutoff, so a composite whose conversation leg never confirms marks nothing; T3.17 carries that twin as a named test, and Spec-004 §Pitfalls To Avoid records it. **Marker-line integrity:** T3.16 and T3.17 carry the audit-grade rows (`Files:`, `Spec coverage:`, `Verifies invariant:`) with every `Spec coverage:` cite — T4.7's added second Spec-004 cite included — in the unbackticked `Spec-NNN §Heading` grammar Gate 4 parses, and neither new task's `Verifies invariant:` row cites a foreign plan's invariant (T3.16 → I-004-20; T3.17 → I-004-21 / I-004-3 / I-004-18; T4.7's row is unchanged at I-004-10, since carrying a field is not verifying the composite's guards). The matrix symmetry pass ran **both** directions and found one pre-existing asymmetry outside this amendment's growth: I-004-10's Verified-by column omitted T4.7, whose row has claimed the invariant's no-local-gate arm since campaign B9 — the same class as the W2.5 audit's disposition (7), corrected in this swap (a column entry, not a marker line, so the Gate-4 census basis is unaffected). The Gate-4 corpus-census pin moves with the diff that changes its basis — the two new tasks contribute four bold resolved invariant references, so the pinned count is re-derived from a live run rather than by arithmetic, and the ledger comment records the arithmetic beside it. One born-unchecked box is minted — "Rollback replacement-send wire arm registered" — holding T1.2 / T1.3 / T3.17 / T4.7 until the `api-payload-contracts.md §Plan-004` registration lands; its prefix is deliberately backtick-free because preflight Gate 5 matches box titles by raw `startsWith`.

**2026-08-17 — PR #344 Codex round 1: five contract hardenings folded into the amendment (no Status move).** The rewind-hardening amendment's review round surfaced five defects in the composite's own contract and two record-level drifts; each is folded into the normative surfaces rather than catalogued. **(1) At-rest PII split.** The `replacementSend` body no longer persists plaintext in `interventions.payload`: it encrypts into the new `interventions.pii_payload` under the requesting participant's key via the CP-006-1 `PiiEncryptor` (`pii_participant_id` stamped), the same `splitPii` discipline the `user.message` path applies — erasure parity is structural, since the Plan-022 Path-1 key deletion shreds both copies identically. `local-sqlite-schema.md`'s Plan-004-owned `interventions` DDL carries the column pair (columns on an existing table — the 55-table census is unmoved), and `Spec-004 §Required Behavior`, `§Interfaces And Contracts`, I-004-21, T3.17, and the `Queue And Intervention Model §Field-Level Consistency` row all state the split. **(2) Atomic admit-and-settle.** Replacement admission (queue item + `user.message` row) and intervention settlement commit in one durable transaction, on the live path and the reconciliation completion branch alike, so an intervention observed unconcluded at restart proves no admission occurred and recovery cannot double-admit — closing the crash window between a committed admission and an unsettled intervention that the `clientIdempotencyKey` (which dedupes intervention applications, not queue admissions) never covered. **(3) Pending-send guard widened.** Guard (ii) is now **No pending send**, refusing on an undrained queued send as well as the accepted-but-undelivered window: FIFO would drain the older item ahead of the replacement, breaking resend-next, and front-insertion is the V1-deferred queue-priority override — so the ordering promise is enforced by refusal at the gate (`QueueItemCancel`-then-reissue is the remedy; the queued-send arm lifts if V1.1 priority overrides land). The guard count stays three; every enumeration site was swept (spec guards block and acceptance criterion, I-004-21, T3.17, T4.7's matrix, the domain refusal row). **(4) Cross-node scoping.** The durable-row recoverability guarantee is scoped to arms that reach intervention creation: a cross-node composite refused by the target owner's approval or the pre-approval role check creates no intervention state, and the caller-side surface that authored the text remains its holder. **(5) T3.16's arrival-slot regression test corrected.** The authored case expected refusal for a target between a compaction's true originating position and its arrival slot — the exact over-refusal the operation-association ranking eliminates; the case now expects admission, with the refusal case kept below the true originating position. **Record-level:** the anchoring rule in `Spec-004 §Driver-Level Rollback Mechanics` now states its own authority — the ordering hazard plus per-driver ADR-017 re-verification as the first-party evidence path, the 2026-08-12 reference-runtime parity survey demoted to corroboration — and T4.7's structural-refusal parenthetical names the true guard set (compaction-crossing being T3.16's general rollback-domain gate, not a composite guard), with the CLI matrix exercising the pending-send refusal in both arms. No event type, wire method, error code, intervention type, or table is minted; the Gate-4 marker basis is untouched (no `Verifies invariant:` row changed); Statuses stay `approved`.

**2026-08-17 — PR #344 Codex round 2: the replacement-delivery mechanism redesigned run-bound; nine findings folded into the amendment (Spec-012/Plan-012 flip-and-restored in the same swap; no Plan-004 Status move).** Round 2 converged on one under-specified mechanism — the replacement leg's delivery path — plus five independent defects; all are folded into normative surfaces. **(1) Run-bound delivery.** "Admitted as a queue item, drains on the next `run.resume`" was unsatisfiable as written: `queue_items` carried no run association, the domain model defines `admitted` as conversion into a **new** run, and T3.5 had no drain behavior. The redesign adds the nullable `queue_items.target_run_id` column (T1.4's unshipped CREATE — a column on an existing table, the 55-table census unmoved; stamped solely by T3.17's composite admission in V1, `QueueItemCreate` mints no run-bound items), the run-bound admitted-state arm in the domain model, and T3.5's resume-time drain — a run-bound item delivers into its bound run as its next provider send, never converting into a new run. **(2) Four structural refusal guards.** Guard (iv) — a non-resumable rootless target refuses the composite whole (the promised next-resume delivery can never occur; bare rollback stays admissible conversation-only) — joins the set; every count site was swept (spec guards block, banner, Interfaces selector, acceptance criterion, I-004-21, T3.17, T4.7, the domain refusal row, the §6 ledger). **(3) Admission condition + `resendDisposition`.** The replacement admits only on a fully-successful rewind — confirmed floor equal to the target, settlement reclassification boundary-clear, file leg complete or legitimately conversation-only; every degraded arm suppresses it, and composite terminal results carry the `resendDisposition: 'admitted' | 'unapplied'` member — schema-optional at parse, since nothing on a rollback result identifies its request as composite, with population a tested producer obligation (T1.3; rides the wire-arm registration box with the other widenings). **(4) Two-point boundary classification.** The admission-to-confirmation TOCTOU window closes at settlement: T3.13 reclassifies the confirmed floor against the then-current boundary set via T3.16's exported classifier (the in-stream fence delivers every pre-rewind boundary emission by confirmation), a crossing conclusion settling `degraded` naming the new `boundary-diverged` disposition; T3.5's resume gate (`run.compaction_boundary_diverged`, registered in `error-contracts.md §Run`) backstops post-settlement stragglers, making a boundary-diverged run non-resumable in V1 like the rootless class. I-004-20 states both points; its Verified-by column and the tasks' Verifies rows grew symmetrically (T3.13, T3.5 — the Gate-4 marker basis moves this round). **(5) V1 reads no capability input.** T3.16's Consumes dropped the driver capability declaration and its declaration-driving test: no boundary-crossing-rewind capability member exists in V1, so the refusal is unconditional and branch-free, the lift arriving with the future Spec-005 amendment that registers the member. **(6) Admitting-principal rebinding.** A replacement turn executed under the run initiator's principal was a confused-deputy: `Spec-012 §Required Behavior`'s turn-scoped effective-principal rule is extended in place (a same-line rewrite; the spec grows only by its tail recording blockquote, below every inbound `Spec-012 line NNN` cite) to the run-bound replacement send as its intervention-opened arm, Spec-012/Plan-012 flip-and-restore `approved` in the same swap, and Plan-004 gains the producer-side `Admitting-principal carrier registered` box (same-named as Plan-012's consumer box, both checked by the same lead-owned carrier amendment; T3.17's principal-stamp leg holds until then, fail-closed to deny). **(7) PII lifecycle registration.** `interventions.pii_payload` joins `Spec-022 §PII Data Map` (90-day full-retention bound — T3.17 authors the NULL-out sweep, safe absent any digest binding — Path-1 crypto-shred disposition), the participant export, Plan-022's shred inventory, and the erasure runbook's enumerations; `data-architecture.md`'s PII sentence widens. **(8) Decrypt-and-compare idempotency.** Replay-or-conflict on a reused key is adjudicated by decrypting the stored envelope under the requester's live key and comparing plaintexts — never ciphertext comparison (nonce-randomized) and never a persisted plaintext digest (a shred-surviving guessing oracle); an undecryptable body settles `intervention.idempotency_conflict` fail-closed. **(9) T1.4 attribution.** The Note now attributes the round-1 PII pair and the round-2 `target_run_id` to T1.4's CREATE on the `rejection_reason` precedent. One typed error code (`run.compaction_boundary_diverged`) is minted on the compaction leg; the composite leg still mints no intervention type, wire method, event type, error code, or table; Plan-004/Spec-004 Statuses stay `approved`.

**2026-08-18 — Admitting-principal carrier + queue-PII envelope (§6 node NS-71): Status flipped `approved → review` and restored `approved` in the same diff, with the §Preconditions admitting-principal box checked, the paired-spec box re-opened and re-checked, and the audit box's in-swap targeted readiness-audit delta recorded.** The amendment discharges the born-unchecked carrier box the PR #344 round-2 fold minted, in the shape that box named. **(1) Verified-principal stamp.** `interventions` gains `admitting_principal_id`, daemon-resolved from the transport at acceptance and stamped by T2.4 on **every** intervention record — not a private path the composite carries — so no code path can produce an intervention whose admitting identity is unrecorded. The column follows the shipped `approval_resolutions.approver_id` model (D-012-12) for both naming and resolution semantics rather than inventing a second convention, treats any wire-supplied principal as informational-and-cross-checked, and refuses a mismatch with the existing `auth.principal_mismatch` — no duplicate error code minted. **(2) Origin discriminator (D-004-4).** `origin TEXT NOT NULL CHECK(origin IN ('participant', 'system'))` with a table-level CHECK making the principal required **iff** the origin is `participant`. The discriminator is explicit rather than inferred from `initiator_id IS NULL`, which would conflate a system-originated intervention with a participant one whose initiator was merely unsupplied; and `origin` carries no DEFAULT, so an unstamped insert fails at the database instead of defaulting into the principal-free arm. **(3) Row-anchored linkage.** `queue_items.admitting_intervention_id`, stamped by T3.17 and followed by T3.5, so a run-bound item resolves its admitting intervention off its own row — a run accumulates interventions across its lifetime, so recency is not a correct answer — and an unresolvable linkage fails the drain closed rather than falling back to the run initiator. **(4) Queue-PII envelope.** `queue_items` gains the participant-keyed pair `pii_payload` / `pii_participant_id`, mirroring the `interventions` machinery: participant-authored bodies rest encrypted under the sender's DEK, orchestration-authored items keep both columns NULL with content in plaintext `payload` (split-not-replace — an in-process orchestration admission has no participant key), and the split costs no queryability because every drain and ordering selector is already its own column. The stamp closes a real gap rather than adding a column: `queue_items` previously named no participant at all, so the Spec-022 Path-1 fan-out could not address the table even in principle, and queue rows are durable forever and never deleted on drain, so a participant's send bodies would otherwise outlive every run that consumed them in plaintext. The row's posture is untouched — durable, never deleted on drain, never evented. **(5) Steer-text home settled.** Participant-authored steer directive text takes the same at-rest split as `replacementSend`, and the `interventions.payload` schema comment is corrected so its non-PII claim is true under that routing; Spec-006 event shapes are deliberately **not** widened — the content stays off-timeline by design. **Adds** invariants **I-004-22** and **I-004-23**, design decision **D-004-4**, and obligation **CP-004-14**; **adds no table** — five columns and one table-level CHECK join unshipped `CREATE`s on this plan's own T1.4, so the SQLite census is unmoved at 55 and the Postgres census at 26. The `queue_items` participant stamp is deliberately left **unindexed**, matching its `interventions` sibling: the Path-1 selector is a V1.1 maintenance scan, never a hot path, and an asymmetry between the two tables would need a reason neither has.

**2026-08-18 — Desktop edit-affordance placement (user-ratified): Status flipped `approved → review` and restored `approved` in the same diff; the §Preconditions plan-readiness-audit box re-opened and re-checked; §6 node NS-73.** The 2026-08-16 rewind-hardening amendment shipped the edit-and-resend composite with one named residual — _where_ the renderer's edit-message entry point belongs — and this amendment settles it. **The placement.** A hover-revealed pencil affordance in the participant `user.message` row's footer opens an inline editor with a Cancel action and a confirm action; the confirm action dispatches the existing `rollback` intervention carrying `replacementSend`. The alternatives were weighed and rejected: an overflow/kebab menu on the row buries a first-class correction behind a second click and reads as an administrative action rather than an edit, and a composer-level recall gesture (press-Up to pull the last message back) both hides the affordance from every row but one and puts a run-mutating rewind behind a keystroke with no visible target — the row-anchored pencil names its target row unambiguously, which is the whole safety property of a rewind entry point. **It is an entry point, not a control.** The V1 control set is unwidened, exactly as `Spec-004 §Resolved Questions and V1 Scope Decisions` already restates for the composite itself; the affordance dispatches a `rollback` that T4.2's gate already governs. Spec-004 accordingly takes nothing normative here. **The predicate is a mirror, not a second authority.** I-004-24 binds the affordance to eight conjuncts, each traced in T4.8's body to the daemon rule it projects (the activation arm, I-004-10's capability gate, I-004-21's four structural refusal guards, the CP-004-13 supersede seam, and the I-004-8 / I-004-19 role axis) — the I-004-9 projection discipline applied to an affordance rather than to run state. Conjunct (8) is **authorship-blind**, a deliberate divergence from consumer chat clients: guard (iii) constrains the target row's author and the role check constrains the caller, so any owner or collaborator may correct another participant's boundary — the same cross-user posture the 2026-08-03 amendment already ratified for interventions generally, and re-litigating it at the renderer would have re-introduced an authorship axis the daemon does not have. **Fail-closed in every state.** Hidden while dormant (no handler exists), disabled without the declared capability, disabled-with-a-stated-reason on transient guard failure, and absent entirely on structurally ineligible rows — the one shape ruled out is a visible control that silently does nothing. **Copy is load-bearing.** The replacement **admits** run-bound and does not dispatch, so every surfaced string says queued-and-sends-on-next-Resume; "sent" would be false on the success path itself, not merely in an error arm, and T4.8's tests pin the wording so a later edit cannot regress it. **Sequencing.** T4.8 lands in Phase 4 and ships **dormant** exactly as T4.2's control gate does, its activation arm riding the Phase-3B supplement beside T4.2's — an entry point can never precede the T3.12 handler or the T3.17 composite. Its hold on the replacement-send wire-arm box is carried as task-scoped prose, not a Phase-4 Gate-5 entry that would also hold T4.1–T4.6; BL-153 tracks that sequencing as a named gate. **Ownership.** CP-004-15 splits the work host-mounts/owner-authors: this plan authors the component and predicate in `run-controls/`, Plan-013 exposes the row-footer composition point and mounts it at its T4.2 — the discipline Plan-013 T4.4 already follows against Plan-023's composer-area point — so no renderer symbol crosses ownership in either direction. **In-swap targeted readiness-audit delta.** The re-open's named scope was audited against the surfaces it cites rather than against recollection: each of the eight conjuncts was resolved to a live daemon rule (no conjunct is renderer-invented); the dim-not-remove treatment was checked against `Spec-004 §Required Behavior`'s MAY-dim / MUST-NOT-remove-or-renumber pair, and the invariant states the elected MAY without weakening the MUST NOT; the settlement copy was checked against the composite's admits-not-dispatches mapping; the invariant matrix was swept in both directions (I-004-24's Verified-by names T4.8 and T4.8's marker line names I-004-24 — the symmetry class the 2026-08-17 delta caught on I-004-10); and the set-quantifier sweep covered every enumeration the placement touches — the wire-arm box's consumer list (T4.8 added with its task-scoped mechanism named), §Rollout Order step 3, the Phase-3B goal and Wired-at-3B seam list, and, in `Spec-023 §Signature Feature Composition Sketches`, both the Timeline View and Runs View interaction enumerations, re-derived rather than appended to. Two pre-existing Spec-023 defects surfaced in that sweep and are repaired in the same diff: the Runs View enumeration had never listed `rollback` (absent since campaign B2 made it a first-class intervention type, its desktop control gate authored by campaign B9 at T4.2), and it listed a queue **reorder** interaction that contradicts this plan's own I-004-5 and `Spec-004 §Resolved Questions and V1 Scope Decisions` — V1 defers queue priority overrides and no reorder wire surface exists — so the gloss is struck rather than left standing. **The swap is three documents, not two.** `Spec-023` flips and restores `approved` in the same diff beside this plan and [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md) — new normative sketch text plus two materially re-derived interaction enumerations is the spec-amendment class — with the scoped Re-opened/Delivered record riding [Plan-023](./023-desktop-shell-and-renderer.md)'s paired-spec Preconditions box; Plan-023 itself neither flips nor is otherwise touched, since this amendment authors nothing in its owned subtrees. `Spec-023 §Acceptance Criteria` gains the matching criterion, **appended last** so no existing bullet's position moves, and T4.8's `Spec coverage:` claims it in the heading-plus-behavior-name form those unnumbered criteria take. **One self-inflicted defect was caught by the same sweep and repaired here.** Adding text to Spec-023 shifts that file's line numbering, which invalidated the four `Spec-023 line NNN` anchors [Plan-016](./016-multi-agent-channels-and-orchestration.md) Phase 4 aims at it: two landed on blank lines and failed the corpus cite gate outright, and two kept resolving against neighbouring content — the more dangerous half, since nothing would have flagged them. All four are converted to the drift-proof §-anchor-plus-descriptor form rather than renumbered, per the standing rule that a moved line cite is converted and never renumbered. The parent `§Signature Feature Composition Sketches` anchor carries the two sketch cites because that section's sub-sketch headings take parenthetical owner suffixes that make `§Multi-Agent Channels View` an invalid anchor — the anchor caution Plan-023 already records — and the descriptors carry the precision the line numbers used to. The conversion is cite-form only, with no normative edit to Plan-016, and the corpus-wide cite survey returns zero anomalies after it. No intervention type, wire method, event type, error code, table, or column is minted; the SQLite and Postgres censuses do not move; the Gate-4 pin moves by exactly one resolved bold reference (T4.8's `Verifies invariant: I-004-24`), re-derived from a live run rather than by arithmetic.

**2026-08-25 — Rollback replacement-send wire-arm registration (§6 node NS-75): the §Preconditions wire-arm box checked; Status flipped `approved → review` and restored `approved` in the same diff on this plan and Spec-004.** The 2026-08-16 rewind-hardening amendment deliberately left the `api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` registration to a follow-up PR under the same write-ordering discipline that made CP-004-12's carrier a named merge prerequisite, and [BL-153](../archive/backlog-archive.md) tracked it as the one edit no plan task owned; that registration landed in this diff and BL-153 closes `completed` with it. **Registered:** the `replacementSend?: { content: string }` payload member on the `rollback` arm (request-side only, presence-discriminating, non-empty at parse, no attachment member in V1); the `boundary-diverged` member of `RollbackDegradedResult` carrying `confirmedPosition` + `newestBoundaryPosition`; the `resend-unapplied` member; and `resendDisposition` as a pair of class-scoped fragments intersected at the two terminal state-split arms so the round-5 state-scoping is **preserved rather than widened** — verified by compiling that section's declarations with six valid shapes and seven seeded negative controls, each proven bound to the contract by a perturbation that turns it slack. Three surrounding statements in the same section were re-derived against the moved set rather than left stale: the degraded-class count (**five → seven**), the state-scoping sentence (each terminal arm now names its intersection), and the orthogonality note (`resendDisposition` is neither a disposition nor a rewind-group member). **The PR's Codex rounds closed three representational gaps in the registration itself, all adjudicated into the contract rather than deferred.** (a) `newestBoundaryPosition` was registered REQUIRED and non-nullable, which left the settlement outcome of a **position-less compaction row** unconstructible — `Spec-004 §Required Behavior` classifies such a row as crossing for every target of its run, and while admission refuses that run outright, the row can arrive between admission and the `rollbackTo` confirmation, where settlement cannot un-rewind and must degrade; the member is now `number | null`, required-and-nullable so an absent member never passes for an unstated cause. (b) The single shared `resendDisposition` fragment admitted the two shapes Spec-004 declares invalid (`applied` + `'unapplied'`, `degraded` + `'admitted'`) and let the composite-only `resend-unapplied` arm omit the member entirely; the fragment is now split by terminal state and REQUIRED on that one arm — the only place a rollback result identifies its own request as composite — so the state-determined V1 mapping is encoded in the arm types on the round-5 precedent instead of resting on prose the strict-parse boundary could not enforce. (c) That same arm was registered payload-free, which silently dropped `files-restored`'s two never-silent enumerations in the one case where the restore had already run to the fixpoint: its reachability condition is a **fully-successful** rewind, so it **displaces** the `files-restored` outcome rather than following a skipped or pre-mutation-refused file leg the way every other degraded arm does (`files-unrestored` refuses at execution time pre-mutation; `position-mismatch` and `boundary-diverged` skip the file leg fail-closed). It now carries `overwrittenIgnoredPaths` + `divergentGitlinks` on the same REQUIRED + empty-when-none contract, so `Spec-010 §Turn-Boundary Snapshots`' never-silent mandate survives the displacement; on the conversation-only branch both are empty, the same reading two empty arrays already carry on `files-restored`. **Error contracts take nothing.** The counterexample hunt found every code this arm can reach already registered — `run.compaction_boundary_diverged` and `run.execution_root_released` (`Error Contracts §Run`), `intervention.idempotency_conflict` (§Intervention), `auth.principal_mismatch` (§Auth) — and the composite's four structural refusal guards settle as `rejected` lifecycle results carrying `rejectionReason`, never on the `JsonRpcError` channel, so no row was added. **Both Status rows flip and restore in this same diff** under the audit runbook's §Status Flip Rule. The registration alone would not have moved them — it mirrors content both docs already carry as `approved` into a canonical architecture doc that has no Status lifecycle, the three otherwise-uncoined identifiers were user-ratified in advance, and this plan's body gains no task, invariant, cross-plan obligation, phase, or Required ADR. The three narrowings above do move them: each changes what the wire shape admits, T1.3 states that shape, and `Spec-004 §Required Behavior` is where two of them contradicted the prose as written — the schema-optional-on-every-result rule and its premise that no result member identifies its request as composite, both falsified by the `resend-unapplied` disposition the same amendment registered. That is the runbook's plan behavior-change row, which the spec-amendment rule follows, so this plan and Spec-004 flip `approved → review` and are restored `approved` by the in-swap targeted readiness-audit delta recorded at the §Preconditions audit box — the PR #278 shape, this plan's sixth flip cycle (third at NS-69, fourth at NS-71, fifth at NS-73) and Spec-004's fifth. What this plan gains beyond the flip is the box check, five erratum-class narrowings in T1.3 (the `resend-unapplied` arm's durable locator is the response's own `interventionId`, never a wire member that arm does not carry; `newestBoundaryPosition` named and typed nullable; the composite-only arm's required `resendDisposition`; that arm's displaced `files-restored` enumerations; the class-scoped fragment split), and discharge notes on T1.2 / T4.7 / T4.8. Spec-004 gains the three narrowings, a dated amendment blockquote in its own header convention, and the **landed-fact annotation** on the `§Interfaces And Contracts` sentence that named this registration a merge prerequisite — now recording it discharged, in the parenthetical shape that section already uses for a cross-bundle registration landed elsewhere. Nothing widens: no intervention type, wire method, event type, error code, table, column, disposition, or terminal state is minted, the V1 control set stays closed exactly as `Spec-004 §Resolved Questions and V1 Scope Decisions` states it, both Status rows read `approved` at merge so no census moves, and T4.8 still ships dormant.

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated
