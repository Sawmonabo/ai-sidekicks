# Plan-004: Queue Steer Pause Resume

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `004` |
| **Slug** | `queue-steer-pause-resume` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-004: Queue Steer Pause Resume](../specs/004-queue-steer-pause-resume.md) |
| **Required ADRs** | [ADR-003](../decisions/003-daemon-backed-queue-and-interventions.md), [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-011](../decisions/011-generic-intervention-dispatch.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md) |
| **Dependencies** | [Plan-001](./001-shared-session-core.md) (session core + `session/` run-engine dir + Phase 5 renderer bootstrap), [Plan-005](./005-provider-driver-contract-and-capabilities.md) (driver capability checks + `DriverInterventionResult`), [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (T1.9's `sourceEpoch`/`sourcePosition` epoch-attribution carrier per CP-004-12, and T3.1's `session-append-lock.ts` dependency leaf whose `withSessionAppendLock` T3.7's terminal emitter wraps guard-swap-append in — Phase-3 gated per this plan's Phase-3 `plan_phase` entry; F-006-HALT-06, PR #282, 2026-08-02), [Plan-007](./007-local-ipc-and-daemon-control.md) (`api-payload-contracts.md` method-name registry), [Plan-023 Tier 1 Partial](./023-desktop-shell-and-renderer.md#tier-1-partial-pr-sequence) (`window.sidekicks` preload bridge + renderer substrate — the Tier-1-shipped substrate slice, not the Tier-8 remainder; this plan's `run-controls/` renderer subtree consumes only the bridge, so the substrate is the sole dependency and no Tier-5 → Tier-8 back-edge is created) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |
| **References** | [Updated Spec-004](../specs/004-queue-steer-pause-resume.md) (6 intervention states, pause as orchestration-layer), [Run State Machine](../domain/run-state-machine.md) (9 states) |

## Goal

Implement daemon-backed queue state and authoritative intervention handling for active runs.

## Scope

This plan covers queue persistence, intervention records, run-state transitions, and client control surfaces for queue, steer, pause, resume, and interrupt.

## Non-Goals

- Workflow scheduling
- Provider-specific pause emulation beyond declared capabilities
- Notification routing

## Preconditions

- [x] Paired spec is approved — **re-opened 2026-08-03, scoped to the cross-user run-control authorization amendment** (supersedes the 2026-07-18 campaign Task-28 / W1.5 re-check): Spec-004 §Interfaces And Contracts gained normative Cedar text — the `Action::"intervene"` action on the target `Run` resource, the hosting-node context input, and the authorize-on-session-role-never-run-authorship rule — so the spec flipped `approved → review` per the audit runbook's spec-amendment rule, and this plan flips with it under the runbook's plan behavior-change row, carrying the new **I-004-19**. A targeted readiness-audit delta audits the amendment, re-checks this box, and restores `approved` — that delta PR is the delivery-and-promotion vehicle (the Plan-006 PR #282 flip-and-restore-in-one-swap shape). The gate this re-opens is **plan-wide, not phase-scoped**: preflight Gate 7 checks the plan-template governance trio together with the header Status row, so no lane-1 code PR dispatches for **any** phase — Phase 3 included — until the delta lands; the three named scoped boxes below carry Phase 3's Gate-5 `precondition_box_checked` entries and are untouched by this amendment. **Delivered 2026-08-08:** the cross-user run-control authorization targeted readiness-audit delta (PR #299; §6 node NS-49) ran the re-open's named scope — the Spec-004 §Interfaces And Contracts Cedar shape, I-004-19's T2.5 / T3.4 / T3.5 reach, and the owed local + remote acceptance arms — plus an adversarial cross-surface coherence pass: one minor finding (the api-payload §Authenticated Principal registration gap, amended in the same swap) beside the scheduled acceptance-arm delivery; zero critical / zero major; the consumer-readiness and structure re-verifications are recorded in the dated Notes bullet. Status restored `review → approved` on this plan and Spec-004 (closing PR #299, the promotion vehicle this row named).
- [x] **Driver input-ask park leg authored (Part-B fail-closed follow-up, 2026-07-17; authored by campaign B9, 2026-07-20):** [Spec-012 §Resolved Questions and V1 Scope Decisions](../specs/012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions) and [run-state-machine.md](../domain/run-state-machine.md) require the run-engine to handle `driver_ask.expired` for `kind: 'input'` asks via the `waiting_for_input → paused` park edge — **authored as T3.15** (reciprocal of Plan-012's B13-authored T2.8 normalizer; the CP-004-9 expired→continue mapping stays approval-pipeline-only). Phase-3 run-engine code does not dispatch while this box is unchecked — enforced mechanically by Phase 3's `preconditions:` block (`precondition_box_checked` entry, preflight Gate 5; Gate 7's governance scan deliberately covers only the plan-template trio, so a scoped box needs the phase-level entry); the B9 bundle PR authored T3.15 and checks this box.
- [x] **Plan-006 epoch-attribution carrier registered (CP-004-12; Codex round 5, 2026-07-20; amendment landed 2026-07-20):** the durable typed epoch-attribution field on the event-append surface — `Spec-004 §Required Behavior` mandates the stamp; T3.11 writes it, T3.14's supersede projection consumes it — registered by the lead-owned 2026-07-20 amendment PR (#231) as [Plan-006](./006-session-event-taxonomy-and-audit-log.md) **T1.9's cross-cutting `sourceEpoch` + `sourcePosition` payload carrier pair**: `SourceEpochSchema` / `SourcePositionSchema` + their payload-key consts and the `withEpochStamp` pairing helper in `packages/contracts/src/event.ts`, with the Spec-006 registrations at §Event Type Enumeration (the cross-cutting-pair paragraph — run-scoped-variant admission) and §Compacted Event Format (audit-stub preservation of the stamped-row attribution triple and the `run.rolled_back` boundary cutoff, so the stamp survives compaction for T3.14). That PR checks this box (the Part-B precedent above — a one-line cross-plan edit riding the amendment). Phase-3 run-engine code still does not dispatch until the paired `{ plan: 006, phase: 1 }` entry resolves: the re-opened Plan-006 Phase 1 — T1.9 included — must have fully shipped (declared-⊆-shipped).
- [x] **Plan-010 partial-restore enumerations registered (Codex re-audit round 4, 2026-07-20; amendment landed 2026-07-20):** the `files-partially-restored` wire arm REQUIREs `overwrittenIgnoredPaths` / `divergentGitlinks` (empty-when-none, covering every effect applied before the failure), but [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) T5.2's `partial_restore` variant named only the failed command — the producer-side enumerations were **not yet in the Plan-010 corpus**, so T3.13 could not map the promised arrays without fabricating empties. The lead-owned 2026-07-20 amendment PR (#230) amended Plan-010 T5.2 (+ the `Spec-010 §Turn-Boundary Snapshots` restore bullet) to carry them on the partial result — pinned name-identical to this plan's wire fields (`overwrittenIgnoredPaths` / `divergentGitlinks` / `failedStep`), so the T3.13 mapping is an identity — and checks this box (the CP-004-12 pattern — cross-plan §2 one-writer kept the amendment out of this plan's PRs). Phase-3 run-engine code does not dispatch while this box is unchecked (Phase 3's `precondition_box_checked` entry).
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [x] Plan-implementation-readiness audit passed (Tier 5 / NS-17)

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/runControl.ts`
- `packages/runtime-daemon/src/queue/queue-store.ts`
- `packages/runtime-daemon/src/interventions/intervention-service.ts`
- `packages/runtime-daemon/src/session/run-engine.ts`
- `packages/client-sdk/src/runControlClient.ts`
- `apps/desktop/src/renderer/src/run-controls/`

## Invariants

| ID | Invariant | Verified by |
| --- | --- | --- |
| **I-004-1** | Queue items are persisted by the runtime before admission; queue creation fails closed (rejects) when persistence is unavailable rather than admitting un-durable work. | T1.4, T2.1 |
| **I-004-2** | Every intervention outcome (`applied` / `degraded` / `rejected` / `expired`) is durably recorded with a daemon-owned receipt on every path. | T1.4, T2.6, T2.7 |
| **I-004-3** | Request:dispatch:record cardinality is 1:1:1 — one `applyIntervention` call produces exactly one driver dispatch and exactly one durable record. **Amended (campaign B2):** the `rollback` composite from `running` is the sole dispatch-axis exception — one intervention, one durable record, but the atomic application makes two driver-facing calls (the internal pause's interrupt, then the `rollbackTo` conversation leg — [Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior)); authored by T3.12 (handler) + T3.13 (provider-leg), campaign B9. | T2.4, T3.12, T3.13 |
| **I-004-4** | `target_run_id` is stable across `waiting` / `blocked` transitions — an intervention never silently retargets to a different run. | T1.4, T3.3 |
| **I-004-5** | Queue admission is FIFO by `created_at ASC`; queue priority is excluded from admission ordering (`Spec-004 §Resolved Questions and V1 Scope Decisions`). | T2.2 |
| **I-004-6** | Run-state and queue-item transitions are monotonic — no backward transitions; terminal states are absorbing — **absent rollback** (campaign B2: the `rollback` intervention is the sole backward/terminal-exit path, [Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); authored by T3.12, campaign B9). | T2.3, T3.4, T3.5, T1.6, T3.12 |
| **I-004-7** | Stale-replay version guard: a run-mutating client command — an intervention, `pause`, or `resume` — whose `expectedRunVersion` does **not equal** the current run version is rejected and applies nothing (an intervention transitions `requested → expired`; `pause` / `resume` reject the request with the run left untouched). **Any** mismatch fails closed — stale (older) _or_ anomalous-future — per `Spec-004 §Interfaces And Contracts` ("a version guard mismatch produces `expired`"); a `<`-only guard would silently admit a future comparand no honest client can hold. **The version comparand is the any-run-progression counter (D-004-1); the guard authors the comparison _behavior_ and the version _derivation_ is fixed by the ratified D-004-1 semantics. The comparand is mandatory (D-004-2, covering interventions + pause/resume) — an absent `expectedRunVersion` is rejected, not skipped.** | T2.8, T3.4, T3.5 (read T1.7) |
| **I-004-8** | Intervention authorization uses the verified PASETO `sub` as the Cedar principal; any client-supplied `initiatorId` is informational only and never authoritative. | T2.5 |
| **I-004-9** | Runtime-truth: the daemon run-state stream is canonical; desktop run-controls render an optimistic projection that is reconciled to the daemon truth on every update. | T4.1, T4.3, T4.4 |
| **I-004-10** | Capability-gate: only `steer` is gated on the driver steer capability. `pause` / `resume` / `interrupt` / `cancel` are orchestration-layer (daemon-composed) and are **never** capability-gated. **Amended (campaign B2):** `rollback` is the second capability-gated intervention — statically refused (`driver.capability_unsupported`, no orchestration fallback) when the driver does not declare the `rollback` flag ([Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts)); the daemon-side static gate is authored by T3.12, the desktop control gate by T4.2, campaign B9. | T4.2, T3.12 |
| **I-004-11** | Bridge-only: the `run-controls/` renderer subtree reaches daemon / control-plane state exclusively via `window.sidekicks`; it never imports `packages/runtime-daemon` or `packages/control-plane` directly. | T4.5 (= CP-004-5) |
| **I-004-12** | Setup-gate: a run transitions `starting -> running` only after every registered `RunSetupGate.assertRunReady` resolves; a gate refusal parks the run in `starting` (never a silent skip, never `running`), and registered `onRunTerminal` hooks fire exactly once per terminal transition — once per `runVersion` epoch: a campaign-B2 rollback out of a terminal state re-opens the run, and a later terminal fires them again ([Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); the terminal-source re-open + execution-root re-acquisition is authored by T3.12, campaign B9). (Tier-6 audit.) | T3.10, T3.12 |
| **I-004-13** | Terminal exactly-once (campaign B9, P1-2): the terminal `run_lifecycle` emitter is the **primary** at-most-once-per-`(runId, runVersion)` guard — a guard→swap→append sequence run inside `withSessionAppendLock` ([Plan-006](./006-session-event-taxonomy-and-audit-log.md) T3.1) with read-your-writes state from `getRun`, so the check-and-append is atomic against concurrent same-session appends. Plan-006's partial-unique backstop index + terminal-key trigger trio are defense-in-depth behind this emitter guard, never a replacement for it ([Spec-006 §Run Lifecycle](../specs/006-session-event-taxonomy-and-audit-log.md#run-lifecycle-run_lifecycle)). | T3.7 |
| **I-004-14** | Late-event absorption (campaign B9, P1-8): a provider run-lifecycle event for a run already in an absorbing terminal state — or one attributed to a pre-rollback execution epoch — is **absorbed, never appended**: an already-terminal run's terminal-class straggler routes into the T3.7 emitter and no-ops there; a pre-rollback-epoch straggler — terminal-class included — is absorbed at the epoch check itself, before the emitter (the reopened run's current `runVersion` carries no terminal row, so the emitter guard alone would re-terminalize the rewound run); a non-terminal lifecycle straggler is dropped with only a `run.late_event.absorbed` OTel diagnostic **non-event** — no `session_events` row, no state transition, no `runVersion` advance ([Spec-004 §Required Behavior](../specs/004-queue-steer-pause-resume.md#required-behavior)). Non-lifecycle families (usage telemetry foremost) append normally, carrying a durable source-epoch stamp when pre-rollback. | T3.11 |
| **I-004-15** | Rollback intervention (campaign B9, R8/RA-2): `rollback` is version-guarded (mandatory `expectedRunVersion`; every fail-closed pre-dispatch validation completes before any effect), state-gated (legal from `paused` / `waiting_for_approval` / `waiting_for_input` / the three terminal states; a `running` target is paused-first via the existing pause path), and lands the run in `paused` exactly on conversation-leg-confirmed application — a non-confirming outcome (whole-rejection, or a failed / no-rewind conversation leg) leaves the source state in place: a `waiting_for_*` source keeps its pending block, a terminal source stays terminal with its re-acquired root released, and only a `running` source whose internal pause applied rests `paused` recording `pause-only`; a conversation-leg-confirmed rollback appends a **forward** `run.rolled_back` under the append lock and advances `runVersion` — the authoritative log never truncates or rewrites, and rolled-back turns stay queryable marked superseded by projection ([Run State Machine §Rollback Transitions](../domain/run-state-machine.md#rollback-transitions-campaign-b2); [ADR-017 Decision Log](../decisions/017-shared-event-sourcing-scope.md#decision-log)). | T3.12, T3.14 |
| **I-004-16** | Rollback file-restore caller contract (campaign B9, CP-004-11 / CP-010-12): the file-restore leg resolves the execution root + `execution_mode` + epoch from the `run_execution_contexts` row, derives the `epochLineage` (`{epoch, rewindBase}` list) from durable epoch / intervention records, validates the driver-confirmed floor equal to the accepted `targetPosition`, and invokes [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s turn-snapshot restore under exclusive execution-root tenancy — never authoring Plan-010's callee. A root-less run (disposed ephemeral clone / retired worktree) rolls back **conversation-only** and is non-resumable, and the restore result's per-path disposition (`files-restored` / `files-partially-restored` / `files-unrestored` / `conversation-only`) is recorded on the intervention outcome, never collapsed — the `files-restored` and `files-partially-restored` outcomes carrying the restore result's overwrite/divergence enumerations (`overwrittenIgnoredPaths` / `divergentGitlinks`; on the partial arm every effect applied before the failure, the failing command's partial writes included), never dropped. | T3.13 |
| **I-004-17** | Driver input-ask expiry park (campaign B9, Spec-012 Part-B fail-closed follow-up): an input-kind `driver_ask.expired` parks the run `waiting_for_input → paused` **replay-atomically** with the ask closure (one `withSessionAppendLock` unit); no input is ever fabricated (fail-closed), the park deliberately does NOT traverse the CP-004-9 approval-outcome seam (that seam's expired outcomes continue-with-refusal, correct only for approval-pipeline categories), and daemon startup reconciliation derives a missing sibling effect from the persisted `(ask_id, expiry_at)` pair ([Spec-012 §Resolved Questions and V1 Scope Decisions](../specs/012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions)). | T3.15 |
| **I-004-18** | `user.message` durable row (campaign B18): the queue-submit surface appends exactly one `user.message` row per accepted participant send — atomically with admission, `message` routed through the Plan-022 call-site splitter (`pii_payload`), `correlation_id` stamped so the provider `user_text` wire echo folds into the row as delivery confirmation and never appends a second row ([Spec-006 §User Message Events](../specs/006-session-event-taxonomy-and-audit-log.md#user-message-events); the row-31 correlate disposition stays Plan-006-owned). | T2.9 |
| **I-004-19** | Cross-node intervention never bypasses node-owner authority (2026-08-03 cross-user run-control amendment): an intervention whose target run's hosting node is not the caller's local node MUST route through target-node-owner approval — an approval in the `tool_execution` category per [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) — and MUST settle `rejected`, never `applied` or `degraded`, when that approval is absent or denied. Session membership role admits the intervention on the role axis only and never substitutes for the node-owner leg ([Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts); [Security Architecture §Inter-Node Trust Boundaries](../architecture/security-architecture.md#inter-node-trust-boundaries)). The obligation covers the orchestration-layer `run.pause` / `run.resume` verbs identically even though they never ride `applyIntervention`: T3.4 / T3.5 evaluate the same `Action::"intervene"` adjudication — role axis plus the non-local node-owner approval leg — before composing any run-state mutation, rejecting the request untouched on deny (pause/resume hold no `InterventionType` membership and settle no `InterventionResult`), the same deliberate cross-cutting extension shape D-004-2's mandatory comparand took ([Spec-012 §Required Behavior](../specs/012-approvals-permissions-and-trust-boundaries.md#required-behavior)'s governed-identically rule). | T2.5, T3.4, T3.5 |

## Cross-Plan Obligations

| ID | Obligation | Direction | Tasks |
| --- | --- | --- | --- |
| **CP-004-1** | Driver capability flags + `DriverInterventionResult` (`applied` / `degraded`) | **Consume** from [Plan-005](./005-provider-driver-contract-and-capabilities.md) | T2.6, T2.7 |
| **CP-004-2** | `command_receipts` table — Plan-004 CREATEs the forward-declared table shell; Plan-015 OWNS its column semantics + read model | **Provide shell** to [Plan-015](./015-persistence-recovery-and-replay.md) | T1.5 |
| **CP-004-3** | `packages/contracts/src/runControl.ts` — new contract surface, no prior owner (verified against the ownership map) | **Create** | T1.1–T1.3, T1.6, T1.7 |
| **CP-004-4** | `run.*` method-name namespace (`run.queueList` / `run.queueCreate` / `run.queueCancel` / `run.intervene` / `run.pause` / `run.resume` / `run.subscribeState` / `run.subscribeQueue`) — wire contract registered in `api-payload-contracts.md §Method-Name Registry`. **The eight concrete strings are ratified (D-004-3).** | **Register** with [Plan-007](./007-local-ipc-and-daemon-control.md) | T4.1, T4.4 |
| **CP-004-5** | `run-controls/` renderer subtree consumes daemon / control-plane state only via `window.sidekicks` (Spec-023 bridge discipline) | **Honor** [Plan-023](./023-desktop-shell-and-renderer.md) | T4.5 (= I-004-11) |
| **CP-004-6** | `packages/runtime-daemon/src/session/run-engine.ts` — **Plan-004 CREATEs this file** (its unexecuted Phase 3 is the file's first author; corrects the prior EXTEND-of-Plan-001 attribution — P-1, campaign B9). The `session/` **directory** is Plan-001-owned, but this specific file crosses per the CP-001-1 per-file-content-ownership precedent ([cross-plan-dependencies.md §2 Package Path Ownership Map](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map)); Plan-001's completed Shipment Manifest is never touched. | **Create** (in Plan-001's `session/` dir) | Phase 3 (T3.1 CREATE; T3.2–T3.15 EXTEND) |
| **CP-004-7** | `getRun(runId): { version, sessionId, state }` run-state read accessor — Plan-004 is the run-state-read **origin**; per [cross-plan-dependencies.md §3 Inter-Plan Dependency Graph](../architecture/cross-plan-dependencies.md#3-inter-plan-dependency-graph), the Plan-016 row names this `getRun` accessor (per CP-004-7), while the Plan-015 (queue-state recovery) and Plan-017 (queue/steer) rows depend on Plan-004 without naming this accessor | **Provide (forward)** to [Plan-016](./016-multi-agent-channels-and-orchestration.md) (the §3-named `getRun` consumer) | T1.7 |
| **CP-004-8** | `RunSetupGate` registration seam (`{ assertRunReady, onRunTerminal? }`, ordered array on `run-engine.ts`) + the `QueueItemCreateRequest.workspaceId?` run-binding carrier — Plan-004 ships the seam with zero gates; Plan-010's execution-root gate registers at Tier 6 (Tier-6 audit, 2026-06-10) | **Provide (forward)** to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-9) | T3.10, T1.1 |
| **CP-004-9** | Approval run-blocking seam — Plan-012's approval-service provides `registerApprovalOutcomeListener({ onBlocking(runId, approvalRequestId, blockingState), onResolved(runId, approvalRequestId, outcome) })` + `cancelPendingForRun(runId, cause)`; the run-engine registers the listener at engine construction (same registration directionality as the CP-004-8 seam). `onBlocking` drives `running -> waiting_for_input` for `user_input` / `mcp_elicitation` categories and `running -> waiting_for_approval` for the other 7; `onResolved` returns the run to `running` — rejected/expired outcomes continue-with-refusal, never terminate (run-state-machine.md `waiting_for_approval -> running` row), except a denied — or expired, silence-is-denial (campaign B15) — pre-turn moderation gate (`category: 'gate'`), which Plan-016's moderation gate system-cancels with `trigger: 'moderation_denied'` (Spec-016 D-016-10; the expired-gate arm records Plan-016/Plan-012-owned behavior on this doc-only reciprocal — non-normative here, its normative homes riding those plans' standing `review` windows, and this plan's own first-PR gate requires their completed promotions per AGENTS.md §Doc-First Discipline — the B13 one-clause reciprocal precedent); every terminal transition calls `cancelPendingForRun` so still-pending requests settle as `approval.canceled` (`Spec-006 §Approval Flow (approval_flow)`; Tier-6 audit 2026-06-10, Plan-012 A-6/A-10) | **Consume** from [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) (reciprocal CP-012-5) | Phase 3 (doc-only at this audit — Phase 3 unexecuted; listener registration + terminal-hook call land with the run-engine implementation) |
| **CP-004-10** | Orchestration queue-admission seam — Plan-016's orchestration-run-service composes **in-process** with the daemon queue-admission service after its own admission pipeline passes, passing the typed `OrchestrationRunLinkCarrier` ([api-payload-contracts §Plan-016](../architecture/contracts/api-payload-contracts.md#plan-016--multi-agent-channels-and-orchestration)); the admission API returns the minted `RunId` alongside `queueItemId`; `run.queued` threads the carrier fields into its payload as optional additive fields (`{agentId?, parentRunId?, linkType?, internalHelper?, producingNodeId?}` plus the admission-resolved `effectiveRunConfig?`); independent of the carrier, the queue write stamps `admittedUnpricedCapCents?` (native-cap unpriced admissions only) + `admittedModelFamily?` (every admitted run) on EVERY provider run's `run.queued` — the ordinary `run.queueCreate` path stamps directly via **Plan-016's admission-stamp resolver** (the T2.3 family/pricing/`cost_cap` accessor set), which Plan-016 T2.3 wires into this queue-write seam as an EXTEND (Plan-004's own Phase-2 queue tasks carry no pricing/budget machinery — the ordinary-path stamp code lands with Plan-016 T2.3 and is gated on it); the orchestration path supplies the same resolver's values via the carrier — the durable replay sources for cost reservations, worst-case terminal debits, and as-of-admission family keying, never client-suppliable (campaign B6, `Spec-016 §Cost Derivation And Absent-Cost Semantics`) — `Spec-006 §Run Lifecycle (run_lifecycle)`, Plan-016 D-016-3); the wire `run.queueCreate` never accepts the carrier (child-run creation goes through `orchestration.runCreate` only); the `run.completed` `trigger` value set gains `'turn_limit'` for Plan-016's turn-limit completion (D-016-8); Plan-016's system-initiated interventions (budget/idle/moderation) enter through an in-process entrypoint below the wire authz boundary with NULL-for-system actor + the standard `expectedRunVersion` reject-re-read-retry loop (D-016-7) | **Provide (forward)** to [Plan-016](./016-multi-agent-channels-and-orchestration.md) (reciprocal CP-016-7; Tier-6 audit, Plan-016 walk) | Phase 2 (doc-only at this audit — carrier threading lands with the queue-create implementation) |
| **CP-004-11** | Rollback leg consumes (campaign B9): (a) the file-restore leg calls [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s Phase-5 turn-snapshot ops — `captureTurnSnapshot` at the turn boundary and `resolveRestoreTarget` / `restoreToTurn` for the rewind (whose `TurnSnapshotResolution` / `TurnSnapshotRestoreResult` result unions T3.13 maps onto the file-leg arms of the `RollbackInterventionResult` `disposition`) — supplying the caller-resolved execution root, `execution_mode`, epoch, turn ordinal, and `epochLineage`; Plan-004 is the caller side (CP-010-12), never authors `git/turn-snapshot-service.ts` (cross-plan §2 one-writer). (b) the conversation leg calls Plan-005's `rollbackTo` driver op + `DriverRollbackResult` (campaign B3) — the daemon resolves the run's live binding at dispatch; the driver-confirmed floor is domain-validated equal to the accepted `targetPosition` before the file leg. | **Consume** from [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-12) + [Plan-005](./005-provider-driver-contract-and-capabilities.md) | T3.13 |
| **CP-004-12** | Durable source-epoch attribution carrier (campaign B9, round-2 F5): a **typed epoch-attribution field on the event-append surface**, stamped on pre-rollback-epoch non-lifecycle rows at late-append (T3.11) and read by the supersede projection's cross-epoch keying (T3.14). `Spec-004 §Required Behavior` mandates the durable stamp and delegates its typed shape to the campaign's Plan-006 bundle — registered 2026-07-20 as **Plan-006 T1.9's cross-cutting `sourceEpoch` + `sourcePosition` payload carrier pair** (`SourceEpochSchema` / `SourcePositionSchema` + payload-key consts + the `withEpochStamp` pairing helper; Spec-006 §Event Type Enumeration + §Compacted Event Format), the lead-owned follow-up recorded in §Notes — the position companion carries the late row's turn-boundary ordinal, since no run-scoped family payload has a native position key for the `turn > targetPosition` cutoff to read. It stays a **named merge prerequisite** gating T3.11's late-append leg and T3.14's epoch consumption (that code cannot merge until the carrier _ships_ — machine-enforced since Codex round 5 by Phase 3's paired `precondition_box_checked` + `{ plan: 006, phase: 1 }` Gate-5 entries, the latter holding until the re-opened Plan-006 Phase 1 lands on `develop`) and is not authorable here (cross-plan §2 one-writer — Plan-004 writes no Plan-006 symbol). | **Consume** from [Plan-006](./006-session-event-taxonomy-and-audit-log.md) | T3.11, T3.14 |
| **CP-004-13** | Superseded-turn read seam (campaign B9, round-2 F10): T3.14's projection **exports `supersededTurns(runId)`** — the per-turn supersede attribution the position cutoff + epoch keying produce: each superseded (epoch, turn) carries its source epoch and the superseding rollback's `targetPosition`, lineage-resolved across every accepted rollback at the row's epoch or later — as the seam [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md)'s `timeline-projector.ts` + `TimelineRead` consume to render rewound turns distinctly (without it the marking never reaches the rendered timeline). Plan-004 provides-forward the export; the **Plan-013-side consumer registration landed 2026-07-20** (Plan-013 §Implementation Steps + Spec-013 §Required Behavior superseded-turn rendering), no Plan-013 symbol authored here (cross-plan §2 one-writer). | **Provide (forward)** to [Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md) | T3.14 |

## Ratified Design Decisions (Tier 5 audit, 2026-05-30)

The Tier-5 plan-readiness audit (NS-17) surfaced three open decisions; all three were ratified at the user-review pause and are folded here as the plan's design of record. The cross-cutting corpus amendments they entail (Spec-004 / Spec-006 version-derivation + emission; `api-payload-contracts.md §Method-Name Registry`; `error-contracts.md`) are executed at swap alongside this `approved → review` flip.

- **D-004-1 (was F-004-2-10) — `version` = any-run-progression counter.** The `expectedRunVersion` stale-replay guard (I-004-7 / T2.8) compares against a run `version` derived as an **any-run-progression counter** (reading (b)): it increments on every run progression — applied interventions included — not only on `run.*` state-machine transitions (reading (a)). **Rationale (the hardened call):** reading (a) fails to catch two steers racing on a still-`running` run (zero new `run.*` events → the second stale steer passes undetected); reading (b) closes that replay window. Plan-004 authors T1.7's accessor _shape_ and T2.8's guard _behavior_; the `version` _derivation_ follows this ratified semantics. Entails a Spec-004 / Spec-006 amendment defining the `version` field on the `run.*` payload + its increment/emission point (executed at swap).
- **D-004-2 (was F-004-2-06) — `expectedRunVersion` is mandatory (fail-closed).** The comparand is **required** on every intervention request (`Spec-004 §Interfaces And Contracts` mandatory reading); the absent-comparand case is **rejected**, not applied. An optional comparand would let a caller bypass the stale-replay guard by omitting the field — the hardened reading fails closed. T2.8 authors the present-comparand guard; the absent case rejects. Entails a Spec-004 clarification (executed at swap).
  - **Scope extension (Tier-5 audit) — the mandatory comparand binds `run.pause` / `run.resume`, not only interventions.** The ratified `RunPauseRequest` / `RunResumeRequest` contracts (`docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume`) carry the **same mandatory** `expectedRunVersion` guard, with `RunControlAck` echoing the advanced `runVersion` so the caller threads the fresh comparand forward. This **extends D-004-2 beyond its original intervention-only scope**: a stale `pause` replayed against a run that has since progressed is the identical replay hazard the guard exists to close, so the comparand fails closed the same way (T3.4 / T3.5 apply the T2.8 guard before composing). `pause` / `resume` remain **orchestration-layer run-control verbs, not interventions** — they hold no `InterventionType` (`steer | interrupt | cancel | rollback`, campaign B2) membership and are not serialized through the intervention queue (ADR-011) — so this is a deliberate cross-cutting extension of the mandatory-comparand obligation, recorded here and reflected in the `api-payload-contracts.md` run-control comment at swap.
- **D-004-3 (was F-004-4-04) — the eight `run.*` method strings are ratified.** `run.queueList` / `run.queueCreate` / `run.queueCancel` / `run.intervene` / `run.pause` / `run.resume` / `run.subscribeState` / `run.subscribeQueue` (CP-004-4) are the wire contract, registered in `api-payload-contracts.md §Method-Name Registry` at swap. Reciprocal `provides` is recorded on [Plan-007](./007-local-ipc-and-daemon-control.md) (the `run.*` namespace owner) in the cross-plan dependency map.

## Data And Storage Changes

- Add local `queue_items`, `interventions`, and `command_receipts` tables.
- Project queue and intervention read models from the durable `queue_items` / `interventions` tables — run state is event-sourced (per ADR-001 / ADR-017), so the `session_events` log is **not** extended with new queue/intervention columns.
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions.

## API And Transport Changes

- Add queue list/create/cancel endpoints and intervention request or result events to the client SDK.

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

1. Define run-control contracts and migrations.
2. Implement queue store and serialized intervention application in the daemon.
3. Integrate run-engine state transitions and capability checks.
4. Add desktop queue and intervention controls plus status rendering.

## Implementation Phase Sequence

### Phase 1 — Contracts, Migrations, and the Run-Read Accessor

**Goal:** Land the `runControl.ts` contract surface, the `queue_items` / `interventions` / `command_receipts` migrations, the client-facing pause/resume trigger, and the run-state read accessor that Phase 2's guard and Phase 4's surfacing both consume.

**Precondition:** D-004-1 (`version` = any-run-progression counter) and D-004-2 (`expectedRunVersion` mandatory, fail-closed) ratified — T1.7's accessor `version` field and T1.6's request shape implement these outcomes.

```yaml
preconditions:
  # T1.2's `'rollback'` InterventionType arm imports the Plan-005-owned union
  # member that the campaign B10 bundle widens together with its consumers
  # (CP-005-6; the widening is Plan-005 T1.8 — the Phase-1 contract catch-up
  # task, which re-opens the shipped phase). Tier order (4 < 5) makes this
  # near-automatic — the entry makes it machine-enforced (preflight Gate 5):
  # declared-⊆-shipped resolves false until the re-opened Plan-005 Phase 1
  # (T1.7/T1.8 included) has fully shipped.
  - { type: plan_phase, plan: 005, phase: 1, status: merged }
```

#### Tasks

- **T1.1 — QueueItem contracts** (`QueueItemCreate` / `QueueItemList` / `QueueItemCancel` Zod schemas, incl. the Tier-6-amended `QueueItemCreateRequest.workspaceId?` run-binding field per api-payload-contracts.md §Plan-004 — CP-004-8)
  - **Files:** `packages/contracts/src/runControl.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (QueueItemCreate / QueueItemList / QueueItemCancel against runtime-owned durable state)
  - **Verifies invariant:** I-004-1
  - **Consumes:** `QueueItemState` enum (queued / admitted / superseded / canceled / expired) + branded `QueueItemId` / `RunId` / `SessionId` (in-package)
- **T1.2 — `InterventionRequestPayload` discriminated union** (`steer` | `interrupt` | `cancel` | **`rollback`**) — the `rollback` arm `{ type: 'rollback'; targetPosition: number }` is authored by this bundle (campaign B9), mirroring `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` byte-for-byte (`targetPosition`: normalized session position, Zod int + nonnegative at parse; the daemon boundary-existence + strictly-below-current-position check is the fail-closed admission validation, not a parse concern — Spec-004 §Required Behavior)
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionRequest — target run id, type, mandatory expectedRunVersion guard)
  - **Verifies invariant:** I-004-4
  - **Consumes:** `RunId`, `InterventionType` (`'steer' | 'interrupt' | 'cancel' | 'rollback'` — the `'rollback'` member is Plan-005-owned (`provider-driver.ts` §Shared Enums per CP-005-6) and imported upward; the shipped `provider-driver.ts` union stays three-membered until the campaign's Plan-005 bundle (B10) widens union + consumers together, so this arm is gated on that widening — the api-payload-contracts code-mirror gate)
- **T1.3 — `InterventionRequestResponse` (6 states) + `RunStateChangeEvent` (9 states)** (campaign B9; Codex round 2 restructures the rollback outcome to the discriminated `RollbackInterventionResult` `disposition` union — `files-restored` / `files-partially-restored` (with `failedStep` + the same two REQUIRED enumerations covering every effect applied before the failure — Codex re-audit round 2, widened by PR #230 round 1) / `files-unrestored` / `conversation-only` / `pause-only` / `nothing-applied` / `position-mismatch` (with `requestedPosition` + `confirmedPosition`) — mirroring `Spec-004 §Required Behavior`'s full vocabulary, carried only on a `rollback` result; the response is discriminated on `interventionType` so the seam parses `result` STRICTLY per type — a malformed rollback result fails validation, never a generic fall-through (test); the rollback arm is additionally state-split (Codex round 3) and state-scoped per disposition class (Codex round 5): `applied` REQUIRES `result: RollbackAppliedResult` (`files-restored` / `conversation-only`; the `files-restored` arm carries the two REQUIRED restore-warning enumerations `overwrittenIgnoredPaths` / `divergentGitlinks` — empty when none, Codex post-merge round) and `degraded` REQUIRES `result: RollbackDegradedResult` (the five degraded-only dispositions) — `RollbackInterventionResult` is their union; a disposition-less terminal rollback response fails parse (test), and so does a state/disposition mismatch (`applied` carrying `files-unrestored`; test) — while `requested` / `accepted` / `rejected` / `expired` rollback responses carry no `result`; a `rejected` outcome REQUIRES `rejectionReason` (Codex round 5 — the machine-readable cause is mandatory on the rollback `rejected` arm, `driver.capability_unsupported` foremost with every `Queue And Intervention Model §Intervention State Transition Table` refusal family included, T2.5 authorization refusals too; the field stays optional on every other state) so the static refusal rides the lifecycle response, not the `JsonRpcError` channel; the 6 `InterventionState` values are unchanged, and non-rollback results omit the disposition)
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionResult 6-state + RunStateChange event)
  - **Verifies invariant:** I-004-2
  - **Consumes:** `InterventionId` / `RunId`, `InterventionState` (requested / accepted / applied / rejected / degraded / expired), `RunState` (9-state), `RunFailureCategory` (**import** from the Plan-001 / Spec-006 taxonomy — do not redefine). The `RollbackInterventionResult` `disposition` vocabulary is Plan-004-owned rollback-outcome content (semantics in T3.13; `files-partially-restored` is never collapsed into `files-unrestored`, per [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) T5.2).
- **T1.4 — Migration: `queue_items` + `interventions` tables**
  - **Files:** `packages/runtime-daemon/src/migrations/00NN-queue-and-interventions.ts` (CREATE)
  - **Spec coverage:** Spec-004 §State And Data Implications (queue items durable storage + intervention audit records)
  - **Verifies invariant:** I-004-1, I-004-2, I-004-3, I-004-4
  - **Consumes:** local SQLite migration runner (Plan-001 substrate)
  - **Note:** the `interventions` DDL matches the canonical `local-sqlite-schema.md` block — including the nullable `rejection_reason` column (campaign B9, Codex round 4): the durable machine-readable cause of a `rejected` outcome (`driver.capability_unsupported` foremost), written at T3.12's refusal path and read back on idempotent replay so the reconstructed response carries the same `rejectionReason` — the wire contract forbids `result` on `rejected` (T1.3 state-split), so the cause needs its own column, never a value fabricated at replay time.
- **T1.5 — Migration: `command_receipts` table (forward-declared shell)**
  - **Files:** `packages/runtime-daemon/src/migrations/00NN-queue-and-interventions.ts` (EXTEND)
  - **Spec coverage:** none (forward-declared shell — column semantics + read model owned by Plan-015, no fabricated spec anchor per anti-fabrication rule)
  - **Verifies invariant:** none (structural shell only — see CP-004-2)
  - **Consumes:** local SQLite migration runner
- **T1.6 — `RunPauseRequest` / `RunResumeRequest` + `RunControlAck` (client-facing pause/resume trigger)**
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (pause/resume as orchestration-layer triggers)
  - **Verifies invariant:** I-004-6, I-004-7
  - **Consumes:** `RunId`, `RunState`. Authors the two request types `RunPauseRequest { targetRunId: RunId; expectedRunVersion: number }` and `RunResumeRequest { targetRunId: RunId; expectedRunVersion: number }` plus the shared `RunControlAck { runId: RunId; currentState: RunState; runVersion: number }`, matching `docs/architecture/contracts/api-payload-contracts.md §Plan-004 — Queue Steer Pause Resume` byte-for-byte. Authored as **separate request types** (not an `InterventionType` member) because `pause` / `resume` are absent from `InterventionType` (`steer | interrupt | cancel | rollback`, campaign B2) by design (orchestration-layer, ADR-011) — the client needs a typed trigger distinct from `applyIntervention`. The **mandatory** `expectedRunVersion` carries the stale-replay guard (I-004-7; D-004-2 as extended to pause/resume).
- **T1.7 — Run-read accessor `getRun(runId): { version, sessionId, state }`**
  - **Files:** `packages/contracts/src/runControl.ts` (accessor contract, CREATE) + `packages/runtime-daemon/src/session/run-engine.ts` (forward-ref only — the `getRun` engine-side read lands in Phase 3 when Plan-004 CREATEs the file at T3.1, CP-004-6; Phase 1 authors only the accessor contract)
  - **Spec coverage:** Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` run-state payload — {sessionId, runId, previousState, newState}, event-sourced run state, no standalone runs table); ADR-001; ADR-017
  - **Verifies invariant:** I-004-7 (provides the version comparand for the stale-replay guard) — = CP-004-7 forward-provide
  - **Consumes:** `RunId`, `RunState`. `sessionId` + `state` are derivable from the `Spec-006 §Run Lifecycle (run_lifecycle)` projection. **`version` is the any-run-progression counter ratified as D-004-1** — this task authors the accessor _shape_; the `version` _derivation_ follows the ratified D-004-1 semantics.

### Phase 2 — Queue Admission and Serialized Intervention Application

**Goal:** Daemon-owned, serialized queue admission and the generic `applyIntervention` dispatcher with durable per-outcome records.

**Precondition:** Phase 1 contracts + migrations landed; D-004-1 ratified (T2.8 guard implements the any-run-progression version semantics); Plan-006 Phase 1 merged (the `user.message` literal + union-registration seam T2.9's payload variant registers through — campaign B18).

```yaml
preconditions:
  # Machine-enforced form of the prose line above (preflight Gate 5).
  - { type: plan_phase, plan: 004, phase: 1, status: merged }
  # Campaign B18: T2.9 registers the user.message SessionEventSchema payload
  # variant against the Plan-006 union-registration seam and stamps rows with
  # the registered literal — both exist only once Plan-006 Phase 1 (T1.10's
  # census closure) has fully shipped. Tier order already sequences this; the
  # entry makes it machine-checked rather than assumed (the CP-004-12 pattern).
  - { type: plan_phase, plan: 006, phase: 1, status: merged }
```

#### Tasks

- **T2.1 (Q1) — Queue admission gate (fail-closed when persistence unavailable)**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Fallback Behavior (reject new queue creation when persistence unavailable); ADR-003 §Failure Mode Analysis (block new queued work when persistence unavailable)
  - **Verifies invariant:** I-004-1
  - **Consumes:** QueueItem contracts (T1.1); error code `queue.persistence_unavailable` (F-004-2-07 — register in `error-contracts.md`)
- **T2.2 (Q2) — FIFO admission (`created_at ASC`; priority excluded)**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Default Behavior (FIFO admission, created_at ASC); Spec-004 §Resolved Questions and V1 Scope Decisions (queue priority deferred — excluded from ordering)
  - **Verifies invariant:** I-004-5
  - **Consumes:** QueueItem store (T2.1)
- **T2.3 (Q3) — Pre-admission cancellation + supersede / expire transitions**
  - **Files:** `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (queue items support cancellation before admission; supersede / expire transitions)
  - **Verifies invariant:** I-004-6
  - **Consumes:** `QueueItemState` (T1.1)
- **T2.4 (I1) — `applyIntervention` generic dispatch (ADR-011)**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (CREATE)
  - **Spec coverage:** Spec-004 §Required Behavior (generic intervention dispatch); ADR-011 (applyIntervention(type, payload) generic dispatcher)
  - **Verifies invariant:** I-004-3 (1:1:1 request:dispatch:record)
  - **Consumes:** `InterventionRequestPayload` (T1.2)
- **T2.5 (I2) — Authorization (Cedar principal = verified PASETO `sub`)**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (Cedar principal = verified PASETO sub; initiatorId informational only; the Cedar action is intervene on the target Run resource with the run's hosting node carried in the request context; authorization evaluates against session membership role, never run authorship; a non-local hosting node adds the target-node-owner approval requirement rather than substituting for the role check); Spec-012 §Required Behavior (run-control interventions authorize on session write access, bounded by the node-trust rule); ADR-012 (Cedar); ADR-010 (PASETO sub)
  - **Verifies invariant:** I-004-8, I-004-19
  - **Consumes:** verified PASETO claims (from the Plan-001 / Plan-008 auth context); client `initiatorId` is informational only; the target run's hosting-node identity as Cedar request context; and — for a non-local hosting node — the [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) target-node-owner approval leg in the `tool_execution` category (consumed, never authored here). Neither `initiatorId`, the run's creating participant, nor the target agent's owner is read as an authorization input on this path.
- **T2.6 (I3) — Map `DriverInterventionResult` {`applied` | `degraded`} → 6-state**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (InterventionResult 6-state mapping from DriverInterventionResult)
  - **Verifies invariant:** I-004-2
  - **Consumes:** `DriverInterventionResult` (Plan-005 driver contract — CP-004-1), `InterventionState` (T1.3)
- **T2.7 (I4) — Steer-no-capability degrades to queue + interrupt; durable audit every path**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Driver-Level Steer Mechanics (steer-no-capability degrades to queue + interrupt, durable audit every path; Plan-005 capability checks)
  - **Verifies invariant:** I-004-2
  - **Consumes:** driver capability flags (Plan-005 — CP-004-1)
- **T2.8 (I5) — Stale-replay version guard + per-outcome event**
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (expectedRunVersion stale-replay guard, mandatory fail-closed)
  - **Verifies invariant:** I-004-7
  - **Consumes:** T1.7 `getRun` accessor (reads current run version)
  - **Behavior (authored):** read current run version via `getRun(targetRunId)`; if `expectedRunVersion !== current` → transition `requested → expired`, apply nothing, and emit an `intervention.*` event `{ sessionId, interventionId, targetRunId, type, state, actor }`. The guard rejects **any** mismatch — stale (older) _or_ anomalous-future — per `Spec-004 §Interfaces And Contracts` ("a version guard mismatch produces `expired`"); a `<`-only comparison would silently admit a future comparand no honest client can hold. **Comparand evaluated against the any-run-progression version semantics (D-004-1).** The absent-`expectedRunVersion` case is rejected (fail-closed) per D-004-2 (mandatory).
- **T2.9 (Q4) — `user.message` durable row: payload variant + admission-time emitter** (campaign B18 — closes the Spec-006 ownership assignment: emission belongs to this plan's queue-submit surface, and no task carried it)
  - **Files:** `packages/contracts/src/runControl.ts` (EXTEND — `UserMessageEventPayloadSchema`) + `packages/runtime-daemon/src/queue/queue-store.ts` (EXTEND — the admission-time append)
  - **Spec coverage:** Spec-006 §User Message Events (the durable user-message row; the accepting daemon appends it when it accepts the send; the `user_text` wire echo correlates as delivery confirmation and never appends a second row)
  - **Verifies invariant:** I-004-18
  - **Consumes:** the `user.message` `SessionEventType` literal + the Plan-006 union-registration seam (`packages/contracts/src/event.ts` — Plan-006-owned; this task registers the variant through the seam, never edits Plan-006's registry symbols; gated on Plan-006 Phase 1 by this phase's `preconditions:` entry); the Plan-022 call-site splitter (`splitPii` → `pii_payload`) for `message`; the T2.1 queue store's admission transaction.
  - **Behavior (authored, campaign B18):** (a) contracts — author `UserMessageEventPayloadSchema`, the [Spec-006 §User Message Events](../specs/006-session-event-taxonomy-and-audit-log.md#user-message-events) shape `{sessionId, queueItemId?, runId?, channelId?, actor, message}`, and register it as the `user.message` `SessionEventSchema` payload variant through the Plan-006 union-registration seam (the emitter-authors-payload precedent — the Plan-012 `ApprovalFlowEventPayloadSchema` shape; Plan-006 T1.10 registers literal/category/disposition only, and its flip-≠-emission rule holds `user.message` emission until this variant lands). (b) emitter — the queue-admission accept path appends **exactly one** `user.message` row atomically with the accepted queue item's persistence (one durable unit: an acknowledged send is never admitted without its timeline row; a rejected admission appends nothing), with `message` routed through the Plan-022 call-site splitter (`pii_payload`; the plaintext payload keeps only the non-PII identifiers) and the envelope's `correlation_id` stamped so the provider-side `user_text` echo folds into this row as delivery confirmation (the row-31 correlate disposition — Plan-006-owned registry data; the echo-side handling is the Plan-005 normalizer's disposition consumption, not this task). Tests: an accepted send appends exactly one `user.message` row with pii-routed `message` and a stamped `correlation_id`; a failed admission (persistence unavailable, I-004-1) appends nothing; the payload variant round-trips append/replay validation.

### Phase 3 — Run-Engine Orchestration (Interrupt / Pause / Resume / Recover)

**Goal:** Wire intervention outcomes into the 9-state run machine; compose pause/resume as orchestration (not a driver capability); deterministic restart recovery. **Campaign B9 adds:** the P1-2 terminal-emitter enforcement detail (T3.7), P1-8 late-event absorption (T3.11), the `rollback` time-travel intervention (T3.12–T3.14), and the driver-input-ask expiry park leg (T3.15).

**Precondition:** Phase 2 intervention service landed. **Plan-004 CREATEs `packages/runtime-daemon/src/session/run-engine.ts` at T3.1 (CP-004-6 — Plan-004 is the file's first author; the `session/` directory is Plan-001-owned, this file crosses per the §2 per-file precedent); T3.2–T3.15 EXTEND it.**

```yaml
preconditions:
  # Machine-enforced form of the prose line above (preflight Gate 5).
  - { type: plan_phase, plan: 004, phase: 2, status: merged }
  # Plan-010's B22 turn-snapshot service is its Phase 5 (the discrete named
  # snapshot Phase); B23 ships it as code and it must merge before this phase
  # dispatches the rollback file-restore leg (T3.13, CP-010-12). A free-form
  # prose precondition emits zero Gate-5 entries and silently passes — only
  # this YAML shape makes the snapshot gate real (preflight-contract §Gate 5).
  - { type: plan_phase, plan: 010, phase: 5, status: merged }
  # The campaign B10 parity bundle lands the four-op ProviderDriver CONTRACT
  # widening — `rollbackTo` / `DriverRollbackResult` plus the `InterventionType`
  # union widening (CP-005-6: the bundle "widens union + consumers together") —
  # as Plan-005 T1.8, the Phase-1 contract catch-up task (Phase 3 holds only
  # the driver implementation legs, which the T3.12 static capability gate
  # covers at runtime); T3.12/T3.13 consume the T1.8 contract symbols, so this
  # phase cannot dispatch until the re-opened Plan-005 Phase 1 has fully
  # shipped (declared-⊆-shipped makes this entry resolve false until then).
  - { type: plan_phase, plan: 005, phase: 1, status: merged }
  # CP-004-12: the Plan-006-owned typed epoch-attribution field (T3.11 writes
  # it, T3.14 consumes it) is a named merge prerequisite — registered 2026-07-20
  # as Plan-006 T1.9's sourceEpoch+sourcePosition carrier pair; prose alone was Gate-5-invisible
  # (Codex round 5). Two-layer
  # gate: the box fails until the lead-owned Plan-006/Spec-006 amendment lands
  # (that PR checks it — the Part-B precedent), and the plan_phase entry then
  # holds dispatch until the re-opened Plan-006 Phase 1 (the carrier's
  # contracts phase) has fully shipped — declared-⊆-shipped closes the window
  # where an already-shipped Phase 1 would satisfy the entry pre-amendment.
  - { type: precondition_box_checked, box: "Plan-006 epoch-attribution carrier registered" }
  - { type: plan_phase, plan: 006, phase: 1, status: merged }
  # T3.7's terminal emitter imports `withSessionAppendLock` from Plan-006 T3.1's
  # dependency-leaf `session-append-lock.ts` — a Phase-3 deliverable, so the
  # Phase-1 entry above alone leaves the lock module absent at dispatch.
  # Registered by the Plan-006 T3.1-seam targeted readiness-audit delta
  # (F-006-HALT-06, PR #282, 2026-08-02) — the Plan-024 Phase-3B CP-024-5
  # sibling shape.
  - { type: plan_phase, plan: 006, phase: 3, status: merged }
  - { type: precondition_box_checked, box: "Driver input-ask park leg authored" }
  # Codex re-audit round 4: the files-partially-restored wire arm REQUIREs the
  # two restore-warning enumerations, but Plan-010 T5.2's partial_restore
  # variant names only the failed command — the producer-side amendment is a
  # named merge prerequisite (the CP-004-12 pattern): this box fails until the
  # lead-owned Plan-010/Spec-010 amendment lands and checks it.
  - { type: precondition_box_checked, box: "Plan-010 partial-restore enumerations registered" }
```

#### Tasks

- **T3.1 (INTERRUPT) — Interrupt application** (`running → interrupted`)
  - **Files:** `packages/runtime-daemon/src/session/run-engine.ts` (**CREATE** — Plan-004 is this file's first author, CP-004-6; T3.2–T3.15 EXTEND it)
  - **Spec coverage:** Spec-004 §Required Behavior (interrupt transitions run directly to interrupted); Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` taxonomy)
  - **Verifies invariant:** I-004-6
- **T3.2 (CANCEL) — Cancel application** (`running → interrupted`)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (cancel intervention → interrupted run-terminal)
  - **Verifies invariant:** I-004-6
  - **Note:** the cancel intervention maps the active run to the canonical `interrupted` run-terminal — cancel is a user-initiated interruption, distinct from T3.1's `interrupt` in actor/intent but **not** in run-terminal (per [run-state-machine.md](../domain/run-state-machine.md): "the cancel intervention maps to the `interrupted` terminal state … distinct from queue-level `QueueItemCancel`"; the run terminals are `completed` / `interrupted` / `failed` — there is no `canceled` run-state). `canceled` is a **`QueueItemState`** (queued / admitted / superseded / canceled / expired; T1.1), applied to queued items by the `QueueItemCancel` path (T2.3), never a run-terminal — this task transitions only the run.
- **T3.3 (WAIT-GUARD) — `waiting` / `blocked` guard (target_run_id stable)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §State And Data Implications (waiting / blocked stays on the same run id — target_run_id stable)
  - **Verifies invariant:** I-004-4
- **T3.4 (PAUSE) — Pause orchestration** (compose: interrupt + persist resumable state + enqueue resume marker — **not** driver-dependent)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (pause composes interrupt + persist + queue resume, orchestration-layer); Spec-004 §Interfaces And Contracts (pause/resume route through the same intervene adjudication); ADR-011 (orchestration dispatch, not a driver capability)
  - **Verifies invariant:** I-004-6, I-004-7, I-004-19
  - **Consumes:** T1.7 `getRun` accessor (reads current run version)
  - **Behavior (authored):** adjudicate the caller first — the identical `Action::"intervene"` evaluation the intervention path runs (role axis + the non-local node-owner approval leg, I-004-19; `Spec-012 §Required Behavior`'s governed-identically rule), rejecting the request untouched on deny — then apply the **same** stale-replay version guard as T2.8 before composing the pause — read current run version via `getRun(RunPauseRequest.targetRunId)`; reject the request untouched if `RunPauseRequest.expectedRunVersion !== current` (`Spec-004 §Interfaces And Contracts`, mandatory per D-004-2 as extended to pause/resume); otherwise interrupt the active run, persist resumable state, enqueue the resume marker, and acknowledge via `RunControlAck { runId, currentState, runVersion }`.
- **T3.5 (RESUME) — Resume orchestration** (re-admit the **same** run id from persisted state)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (resume returns a paused run to active execution, same run id); Spec-004 §Interfaces And Contracts (pause/resume route through the same intervene adjudication)
  - **Verifies invariant:** I-004-4, I-004-6, I-004-7, I-004-19
  - **Consumes:** T1.7 `getRun` accessor (reads current run version)
  - **Behavior (authored):** adjudicate the caller first — the identical `Action::"intervene"` evaluation the intervention path runs (role axis + the non-local node-owner approval leg, I-004-19; `Spec-012 §Required Behavior`'s governed-identically rule), rejecting the request untouched on deny — then apply the **same** stale-replay version guard as T2.8 before composing the resume — reject the request untouched if `RunResumeRequest.expectedRunVersion !== current` (`Spec-004 §Interfaces And Contracts`, mandatory per D-004-2 as extended to pause/resume); otherwise re-admit the **same** run id from persisted state and acknowledge via `RunControlAck { runId, currentState, runVersion }`.
- **T3.6 (RECOVER) — Deterministic restart recovery** (distinguish `failed` vs `interrupted` on daemon restart)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Fallback Behavior (deterministic restart recovery — distinguish failed vs interrupted); Spec-006 §Replay Interaction with Compacted Regions (replay on daemon restart)
  - **Verifies invariant:** I-004-2, I-004-6
- **T3.7 (EMIT) — Emit `run.*` state-change events + terminal exactly-once emitter** (Spec-006 `run_lifecycle` taxonomy; campaign B9 P1-2 detail)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-006 §Run Lifecycle (run_lifecycle) (`run.*` state-change event payload + the at-most-once terminal emission per `(runId, runVersion)` — the emitter contract)
  - **Verifies invariant:** I-004-2, I-004-13
  - **Behavior (authored, campaign B9 P1-2):** the terminal `run_lifecycle` emit (`run.completed` / `run.failed` / `run.interrupted`) is the **primary** at-most-once-per-`(runId, runVersion)` guard. It runs a **guard → swap → append** sequence wrapped in `withSessionAppendLock(sessionId, critical)` — the per-session append lock [Plan-006](./006-session-event-taxonomy-and-audit-log.md) T3.1 publishes from its dependency-leaf `session-append-lock.ts` (re-homed off `event-log-service.ts` by the PR #281 ingest-halt amendment, Codex round 3, for eager-ESM-cycle freedom), owner-scoped reentrant via `AsyncLocalStorage` so the `append()` issued inside the critical section reuses the held token and never self-deadlocks. Under the lock: read-your-writes current run state via `getRun(runId)` (T1.7), verify no terminal row already exists for this `(runId, runVersion)` epoch, then append the terminal event — so the check-and-append is atomic against concurrent same-session appends. The terminal payload MUST carry non-null `runId` / `runVersion` (the emitter precondition). **Backstop-index interplay:** Plan-006's partial-unique `idx_session_events_run_terminal_once` + the `trg_run_terminal_key_insert` / `trg_run_terminal_key_update` / `trg_run_terminal_key_promote` trigger trio are the **schema-level backstop** behind this guard (a duplicate terminal, a NULL / storage-class-drifted key, or a non-terminal row re-typed into the terminal set fails loud at the DB layer) — never a replacement for the emitter, which is the primary enforcement; the schema work is defense-in-depth (`Spec-006 §Run Lifecycle (run_lifecycle)`).
  - **Rejected alternatives (carried from campaign B1 / design §9 BP-7):** a generic silent-no-op `appendIfAbsent(...)` API stays **rejected** — the corpus enforces terminal exactly-once with a fail-loud emitter guard + fail-loud schema backstop, not a silent absorb-if-present primitive. A projection-level deterministic identity `run-terminal:<runId>:<runVersion>` stays **SECONDARY** — the emitter guard + schema backstop are primary; a deterministic-id dedupe would at best restate that guarantee at the projection layer and is not authored as the enforcement mechanism.
- **T3.8 (MAP) — Map intervention outcomes → run-state transitions**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (intervention outcome → run-state mapping)
  - **Verifies invariant:** I-004-6
- **T3.9 (STEER-ROUTE) — Route steer to driver (or degrade per T2.7)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Driver-Level Steer Mechanics (route steer to driver, or degrade per T2.7; Plan-005 capability)
  - **Verifies invariant:** I-004-2
- **T3.10 (SETUP-GATE) — Run-setup gate seam: ordered pre-start gates + terminal release hooks (Tier-6 audit)**
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-010 §Fallback Behavior (blocked-in-setup parks the run in `starting`, interruptible) + run-state-machine.md `starting` rows (`starting -> running` fires only after gates pass; `starting -> interrupted` on user stop while parked)
  - **Verifies invariant:** I-004-12
  - Note: `RunSetupGate` interface `{ assertRunReady(context): Promise<void>; onRunTerminal?(context): Promise<void> }` with an ordered gate array on the run-engine; context carries `{ runId, sessionId, queueItem }` (incl. the T1.1 `workspaceId?`). `queued -> starting` admits the run, then gates execute in registration order BEFORE `driver.startRun`; a gate throw parks the run in `starting` with the typed error surfaced (no `running` transition fires; interrupt/cancel per T3.1/T3.2 applies to parked runs). `onRunTerminal` hooks fire in reverse registration order when the run reaches any terminal state. Plan-004 ships the SEAM with zero registered gates — generic, no Plan-010 knowledge; Plan-010's execution-root gate registers at Tier 6 (Plan-010 D-010-16, reciprocal CP-010-9 — CP-004-8). Tests: gate registration order; gate-throw parks in `starting` with the typed error; zero-gate pass-through; reverse-order terminal hooks fire exactly once (per `runVersion` epoch — campaign B2); interrupt-while-parked transitions to `interrupted`.
- **T3.11 (LATE-ABSORB) — Post-interrupt / post-rollback late-event absorption** (campaign B9, P1-8)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-004 §Required Behavior (post-interrupt late-event absorption; execution-epoch keying); Spec-006 §Run Lifecycle (run_lifecycle) (terminal-class straggler routes into the at-most-once terminal emitter)
  - **Verifies invariant:** I-004-14
  - **Behavior (authored):** at the run-engine inbound dispatch, a provider **run-lifecycle** event for a run already in an absorbing terminal state — or one the per-binding epoch cursor attributes to a **pre-rollback execution epoch** — is **absorbed, never appended** — and the two absorption conditions route differently (Codex round 6): an already-terminal run's terminal-class straggler routes into the T3.7 terminal emitter and no-ops there (the guard finds the `(runId, runVersion)` terminal already present), but a **pre-rollback-epoch straggler — terminal-class included — is absorbed at the epoch check itself, before the terminal emitter**, because post-rollback the reopened run's current `runVersion` carries no terminal row, so the emitter's duplicate guard alone would append the stale terminal against the new epoch and re-terminalize the rewound run; a non-terminal lifecycle straggler is dropped from the lifecycle path. Every absorbed straggler is recorded only as a `run.late_event.absorbed` OTel diagnostic **non-event** (never a `session_events` row), with no state transition and no `runVersion` advance. The absorb decision runs inside `withSessionAppendLock` so the terminal-check and the absorb see one consistent state. Non-lifecycle families (usage telemetry foremost) append normally under their own rules — a pre-rollback-epoch non-lifecycle row appends **carrying the durable source-epoch stamp** (the typed epoch-attribution pair on the append surface — **Plan-006 T1.9's `sourceEpoch` + `sourcePosition` carrier per CP-004-12**, stamped together with the row's `runId` per the pairing rule, a **named merge prerequisite** gating this late-append leg; consumed by T3.14's supersede projection, which needs epoch, position, and run identity to rank the late row against its epoch's surviving prefix), so billable usage is never dropped. **Surface-opening exception (Codex round 5):** a pre-rollback-epoch `driver_ask.requested` (either kind) is not a passive row — appended open it would mint a pending interactive surface and could drive the rewound run toward `waiting_for_*` — so the late-append path records it as a **closed pair, atomic**: the epoch-stamped `driver_ask.requested` row and its `driver_ask.canceled` terminal (superseded pre-rollback-epoch cause; `Spec-006 §Driver Ask Events` terminal, no new event type) land under the same `withSessionAppendLock` append, no pending ask surface registers, no run-state transition fires, and a permission-kind ask is never handed to the approval-pipeline normalizer — the epoch check runs at inbound dispatch, before normalization, so Plan-012's normalizer is never invoked for a stale-epoch ask (no Plan-012 symbol authored). The same rule governs any future non-lifecycle family whose append opens a pending interactive surface. Epoch attribution is per-binding and needs no wire token: the per-binding cursor is an **(epoch, position) delivery pair** — the position component advances at each turn boundary observed in the binding's ordered delivery; on an in-place binding the driver's `rollbackTo` confirmation is the in-stream fence. The stamp's value source is the straggler's **operation association** (Codex rounds 4-5): the run engine records each provider-side operation — tool call, message item, artifact production — against the **(epoch, turn) current when it opened**, at open time in the ordered stream (pre-fence by construction), and the straggler's correlation key resolves that recorded pair — the epoch must ride the association because re-executed turns reuse ordinals (epoch-0's turn 7 and epoch-1's turn 7 are distinct operations distinguishable only by their recorded epochs). Delivery order bounds only the _emission_ turn, so a content-asynchronous straggler (a turn-4 tool's progress delivered at turn 10) must rank by its operation's opening pair — the closing delivery position is superseded-by-construction (a generation closes at the run's then-current position, which admission requires strictly above `targetPosition`), so stamping it on every straggler would vacate the surviving-prefix ranking `sourcePosition` exists for. Binding lineage (Codex round 5): a fork-superseded binding (Claude) freezes wholesale — every delivery pre-epoch at its retained pair — while an in-place rollback (Codex) **rotates the delivery generation at the fence**: the confirmation closes the pre-rollback generation (pair retained for straggler attribution) and post-fence deliveries open the new epoch at the confirmed floor — one provider binding spans epochs with per-generation attribution, and a post-fence delivery is current unless its association resolves to a closed generation's operation. An association-less straggler takes the retained closed-generation pair — reachable only on the superseded fork binding (an in-place binding's post-fence association-less delivery is current by the in-stream fence's total order): always superseded with its epoch, never resurfacing as current, its append and billability untouched. Never a read-time guess and never the post-rollback run's current position. For the **pause-first composite** the absorption window opens **earlier than the fence** — from the moment the composite issues its internal interrupt (T3.12), run-lifecycle events sourced from the interrupted execution are the pause's **expected echo**: consumed by the pause application exactly as the client pause path consumes its own interrupt's echo, or absorbed as above, never routed as spontaneous transitions — so a late `run.interrupted` arriving after the internal pause but before the `rollbackTo` confirmation cannot re-terminalize the mid-composite run or release its root (`Spec-004 §Required Behavior`). **Tests:** a late `run.interrupted` in the pause-first composite window (after the internal interrupt, before the `rollbackTo` confirmation) is absorbed as the pause's expected echo — the mid-composite run is neither re-terminalized nor has its execution root released; a pre-rollback-epoch `driver_ask.requested` late-appends as the closed pair (epoch-stamped `requested` + `canceled`), registers no pending ask surface, fires no `waiting_for_*` transition, and its permission-kind variant never reaches the approval-pipeline normalizer; a stale `run.completed` / `run.failed` attributed to the superseded epoch, arriving after the run reopened `paused`, is absorbed at the epoch check — it never reaches the terminal emitter and the run stays `paused` (live + replay); a stamped late row ranks by its association's opening turn on both sides of the cutoff — a straggler whose operation opened at-or-below the confirmed target stays **current** (the epoch's surviving prefix, never superseded) while one whose operation opened above it is marked superseded (via T3.14); on an in-place binding after generation rotation, a post-fence event whose association resolves to a closed generation's operation stamps that generation's (epoch, turn) while a re-executed same-ordinal operation's event appends current unstamped (reused ordinals disambiguated by the association's epoch) and a post-fence association-less delivery appends current (the rotation control); an association-less straggler on a superseded fork binding takes the retained pair and is superseded (the conservative fallback's negative control).
- **T3.12 (ROLLBACK) — `rollback` intervention handler: guard, state-gating, transitions, forward emission** (campaign B9, R8/RA-2)
  - **Files:** `packages/runtime-daemon/src/interventions/intervention-service.ts` (EXTEND — the `rollback` dispatch arm) + `run-engine.ts` (EXTEND — the transitions + forward emission)
  - **Spec coverage:** Spec-004 §Required Behavior (rollback — version guard, state-gating, pause-first, forward `run.rolled_back`, failure semantics); Spec-004 §Interfaces And Contracts (rollback capability gate + 6-state result); ADR-011 (generic `applyIntervention` dispatch — new types need no interface change)
  - **Verifies invariant:** I-004-3, I-004-6, I-004-10, I-004-12, I-004-15
  - **Consumes:** T1.2 (`rollback` payload arm), T1.7 `getRun`; the `Run State Machine §Rollback Transitions` edges (campaign B2)
  - **Behavior (authored):** `applyIntervention('rollback', {targetPosition})` rides the existing generic dispatch (ADR-011, zero interface change). **All fail-closed pre-dispatch validation completes before any effect:** the mandatory `expectedRunVersion` guard (evaluated once at admission against the pre-pause version, per D-004-1 semantics), the static capability gate (`driver.capability_unsupported`, no orchestration fallback, when the driver does not declare `rollback` — I-004-10; the refusal settles `requested → rejected` with the machine-readable cause persisted to `interventions.rejection_reason` (T1.4, Codex round 4) — the durable source an idempotent replay reconstructs the response's `rejectionReason` from, since the stored `result` is forbidden on `rejected`), the `targetPosition` boundary check (a recorded turn boundary strictly below the current position — with the sole carve-out that a target **equal to the current position** is admissible when the run's recorded rollback outcome at that position left the file leg incomplete: `files-unrestored`, `files-partially-restored`, or the position-mismatch skip — the **file-leg recovery path**, whose conversation leg no-ops by absolute-position convergence and whose restore re-runs to the fixpoint), the **uncompacted-rewind-span check** (every row of the target run strictly above `targetPosition` — the rows the supersede cutoff would mark — is uncompacted; a span intersecting already-compacted history refuses whole per `Spec-004 §Required Behavior`, because compaction destroyed the per-row attribution the T3.14 projection needs — detection reading the stubs' preserved keys (Codex round 5): every run-scoped stub preserves `runId`, so the check counts the target run's unstamped stubs with preserved `originPosition` above `targetPosition`, or with none (a pre-rollback-era stub, unknowable ⇒ the same standing refusal), while stamped stubs and other runs' stubs never block; the check is atomic with the write-ahead intervention row's durability under `withSessionAppendLock`, and from then until the intervention concludes the compactor defers the span's rows — T3.14's `attributeAtCompaction` answers `'defer'` for rows covered by a dispatched-but-unconcluded rollback row (Plan-006 T3.2's third disposition, Codex round 4) — closing the check-then-compact race in the admission-to-boundary window), and execution-root availability. **Every path that settles a rollback `requested → rejected` persists its machine-readable cause to `interventions.rejection_reason`** (Codex round 5, generalizing the round-4 capability-gate rule): the boundary-check, uncompacted-rewind-span, root-`busy`, and T2.5 authorization refusal families of `Queue And Intervention Model §Intervention State Transition Table` persist their causes exactly as the capability gate does, because the wire contract REQUIRES `rejectionReason` on a rollback `rejected` response (T1.3) — a cause-less rejected settle would be unreconstructable on idempotent replay. **State-gating** per `Run State Machine §Rollback Transitions`: legal from `paused` / `waiting_for_approval` / `waiting_for_input` / the three terminal states; a `running` target is **paused-first** (the existing pause path: interrupt + persist) as an unguarded internal sub-step that queues **no** resume event and cancels a pending resume as moot. A `waiting_for_approval` / `waiting_for_input` source **voids the pending block durably on the confirmed conversation leg — atomic with the `waiting_for_* → paused` transition and the forward `run.rolled_back` emission, never at admission or pre-dispatch** (Codex round 6; `run-state-machine.md §Rollback Transitions` gates the void on a conversation-leg-confirmed rollback — a failed or no-rewind conversation leg leaves the pending block and its originating native ask intact, the run stays in its `waiting_for_*` state, and the outcome records `nothing-applied` per T3.13): the awaited approval settles `approval.canceled` via Plan-012's `cancelPendingForRun(runId, cause)` seam (CP-004-9 — consumed, never authored here; `Spec-006 §Approval Flow (approval_flow)` terminal) — and when that pending approval was normalized from a **permission-kind driver ask** (Plan-012's T2.8 normalizer records the originating-ask provenance on the row it mints), the void **also settles the originating native ask** `driver_ask.canceled` through its owning surface within the same atomic void, so the provider-side ask can neither stay pending nor later expire/respond against the rewound turn (Codex round 5; provenance consumed, never authored — no Plan-012 symbol) — and a driver input-ask settles `driver_ask.canceled` through its owning surface (`Spec-006 §Driver Ask Events` terminal — no new event type minted); a queued pending resume against the target run is voided the same way (canceled as moot — the position it would resume from is rewound). The void is **progression-serialized**: a concurrent resolution that applies first advances `runVersion` so the in-flight rollback `expired`s; a rollback that applies first cancels the block and the late resolution finds a canceled request and no-ops. A **terminal-source** rollback re-acquires the execution root (reactivating `run_execution_contexts`, clearing `released_at` atomically with the re-open — I-004-12); a root `busy` under another run refuses the whole intervention pre-dispatch; a root-less run rolls back conversation-only (T3.13). A terminal-source composite **releases its re-acquired root on every path that does not confirm the conversation rewind** (a preflight refusal, a conversation-leg `degraded`, or the no-rewind reconciliation): the run stays terminal, the workspace returns to `ready`, and the execution context's `released_at` marker is restored — the re-acquired hold is retained **exactly** when the run re-opens `paused`, and released on every non-confirming exit so the terminal-source re-acquisition never leaks the root. On the **confirmed conversation leg** (T3.13), append the **forward** `run.rolled_back` event (`{sessionId, runId, runVersion, channelId?, targetPosition}`) under `withSessionAppendLock`, advance `runVersion`, and land the run in `paused` — a state-changing rollback (terminal-exit or waiting-void) also emits `run.paused`; an in-place rollback from `paused` emits only `run.rolled_back`. The authoritative log never truncates (`ADR-017 Decision Log`). 1:1:1 cardinality holds with the sole dispatch-axis exception of the pause-first composite's two driver-facing calls (I-004-3). **Tests:** a waiting-source rollback settles the pending approval `approval.canceled` (via `cancelPendingForRun`) / a driver input-ask `driver_ask.canceled` and voids a queued resume; a permission-backed approval void settles BOTH surfaces (`approval.canceled` + the originating `driver_ask.canceled`); each pre-dispatch refusal family persists its machine-readable cause to `interventions.rejection_reason` (boundary-check, uncompacted-rewind-span — a rollback whose rewound tail includes a compacted row refuses whole — and root-`busy` included, not only the capability gate); the progression-serialized race — resolution-first ⇒ the rollback is `expired`; rollback-first ⇒ the late resolution no-ops (a provider answering the native ask after a permission-backed void included); a waiting-source rollback whose conversation leg fails leaves the approval/input block pending and the run in its `waiting_for_*` state, recording `nothing-applied`; a compaction tick racing an admitted-but-unconcluded rollback defers every rewind-span row (none is stubbed unstamped in the admission-to-boundary window), and a concluded-refused rollback releases the deferral (the span compacts current on the next pass); a rollback whose span contains an unstamped compacted stub of the target run refuses whole — a position-less legacy stub of that run included — while another run's stubs and `sourceEpoch`-stamped stubs never block admission.
- **T3.13 (ROLLBACK-RESTORE) — Conversation leg + turn-snapshot file-restore caller** (campaign B9; CP-004-11 / CP-010-12 caller side)
  - **Files:** `run-engine.ts` (EXTEND) + rollback integration tests (`packages/runtime-daemon/**/__tests__/`)
  - **Spec coverage:** Spec-004 §Required Behavior (conversation-then-file sequencing, confirmed-floor validation, partial-restore disposition, conversation-only fallback); Spec-004 §Driver-Level Rollback Mechanics; Spec-010 §Turn-Boundary Snapshots (the consumed capture / restore ops)
  - **Verifies invariant:** I-004-3, I-004-16
  - **Consumes:** CP-004-11 — [Plan-010](./010-worktree-lifecycle-and-execution-modes.md)'s `captureTurnSnapshot` / `resolveRestoreTarget` / `restoreToTurn` (Phase-5 callees, never authored here) + [Plan-005](./005-provider-driver-contract-and-capabilities.md)'s `rollbackTo` op / `DriverRollbackResult`; the `run_execution_contexts` row (Plan-010 D-010-5)
  - **Behavior (authored):** the caller side of CP-010-12. **(a) Turn-boundary capture call-site:** at each turn boundary of a writable-mode run, call `captureTurnSnapshot({executionRoot, runId, epoch, turnOrdinal, mode})` — resolving `executionRoot` + `mode` from the run's `run_execution_contexts` row (D-010-5), supplying the **execution epoch** (0 before any rollback; **advances with each accepted `run.rolled_back` — a confirmed conversation rewind, including a position-mismatch rewind confirmed at the recorded floor and the same-position recovery's convergence no-op (Codex round 6) — regardless of the file-leg disposition**: a `files-unrestored` / `files-partially-restored` outcome still advances it, and only an outcome with no confirmed rewind — whole-rejection, `pause-only`, `nothing-applied` — leaves it unchanged, because re-executed turns reuse position ordinals and `Spec-004 §Required Behavior` requires the post-rollback execution's `turn-<N>` capture never collide with, nor silently resolve to, the superseded epoch's `turn-<N>` tree); the callee's `read-only` self-guard and its capture-never-blocks-the-turn contract are Plan-010's. **(b) Rollback restore sequencing under exclusive execution-root tenancy — preflight → conversation → bound file-restore:** **conversation-only short-circuit first** — a run whose mode snapshots nothing (`read-only`) or whose execution root no longer exists (a disposed ephemeral clone / a worktree retired by the cleanup sweep) takes the **conversation-only** branch here, before `resolveRestoreTarget`: it skips snapshot resolution and the file leg entirely (the resolver would otherwise `no_snapshot`-refuse a read-only run that correctly captured nothing, wrongly failing the whole rollback), records the `conversation-only` disposition **only after the conversation leg's driver-confirmed rewind** (Codex round 4 — a conversation leg that fails or confirms no rewind on this branch records the no-rewind outcome per T3.12: `pause-only` when the running-source internal pause applied, else `nothing-applied`; never a fabricated `conversation-only` success), and the re-opened rootless run is **non-resumable** — `run.resume` against a released-context run with no existing root is refused with the typed `run.execution_root_released` (409, run state unchanged; distinct from the setup-time `workspace.execution_root_unresolved` — `error-contracts.md §Run`), nothing recreating a disposed clone / retired worktree. For a writable-mode run with a live root, derive the **`epochLineage`** (ordered `{epoch, rewindBase}` list) from the run engine's durable epoch / intervention records and call the non-mutating `resolveRestoreTarget({executionRoot, runId, targetPosition, epochLineage})`, which returns a `TurnSnapshotResolution` (Plan-010 T5.2) — its typed `head_moved` (HEAD advanced past the snapshot) / `no_snapshot` (no ref in the owning epoch) refusal variants reject the whole rollback **before** the conversation leg. Conversation leg: dispatch `rollbackTo` (the daemon resolves the run's live binding at dispatch; a fork-composed leg repoints it via `DriverRollbackResult.applied.bindingId`) and domain-validate the driver-confirmed floor (`sessionPosition`) equal to the accepted `targetPosition` before the file leg — a mismatch degrade records the confirmed position. **Same-position file-leg recovery (the T3.12 carve-out):** when the accepted target equals the current position, the conversation leg's **convergence no-op — the driver confirming `sessionPosition == targetPosition` with no movement — counts as the confirmed floor for this branch** (Codex round 6): the composite proceeds to the bound file leg and `restoreToTurn` re-runs to the fixpoint, the forward `run.rolled_back` emits at the confirmed position, and the epoch advances under the uniform trigger above — the no-rewind outcomes (`pause-only` / `nothing-applied`) apply only when the driver fails to confirm the floor, never to the recovery convergence. Bound file-restore: call `restoreToTurn(resolution)` (it re-verifies the HEAD precondition under the hold — TOCTOU guard) and map the returned `TurnSnapshotRestoreResult` (Plan-010 T5.2) onto the result's file-leg `disposition` (`RollbackInterventionResult`) — `restored → files-restored` carrying the callee result's collision-overwrite + divergent-gitlink enumerations through as the arm's REQUIRED `overwrittenIgnoredPaths` / `divergentGitlinks` (empty when none, never dropped — Codex post-merge round), `partial_restore → files-partially-restored` (with the failed step named plus the same REQUIRED enumerations covering every effect applied before the failure, the failing command's partial writes included (PR #230 round 1) — the identical never-silent mapping, Codex re-audit round 2; the producer-side naming on `partial_restore` is a **named merge prerequisite** — the §Preconditions box "Plan-010 partial-restore enumerations registered" + its Phase-3 Gate-5 entry hold this leg's dispatch until the lead-owned Plan-010/Spec-010 amendment lands (Codex re-audit round 4, the CP-004-12 pattern; cross-plan §2 one-writer) — never a fabricated empty; **never collapsed into `files-unrestored`**), and the execution-time `head_moved` re-verify refusal → `files-unrestored` (the conversation leg has already applied; the worktree is untouched by the refusal). Plan-004 owns the pre-dispatch validation, the exclusive execution-root tenancy, and the conversation leg; Plan-010 owns the callee (`git/turn-snapshot-service.ts`) — B9 **calls** it, never writes it (cross-plan §2 one-writer). **Provider-rewind currency:** Codex `thread/rollback` / Claude `--resume-session-at` are unconfirmed against a live binary (`Plan-005 §Risks And Blockers`; `ADR-017 Decision Log`) — re-verify the method against the then-installed provider binary before this leg's code lands. **Tests:** the turn-boundary call-site invokes the callee with the resolved root / mode / epoch; a full rollback round-trip as integration tests — preflight refusal (no worktree mutation), conversation-then-file success, confirmed-floor-mismatch degrade, root-less conversation-only, a rootless run whose conversation leg fails recording `nothing-applied` (never `conversation-only`), the same-position file-leg recovery reaching `restoreToTurn` after the convergence no-op (never recorded `nothing-applied`), the `files-partially-restored` disposition (its enumerations carrying the overwrites applied before the failure — a completed read-tree leg's or the failing command's own partial writes — never empty-washed), a `run.resume` against the rootless re-opened run refused `run.execution_root_released`, a `files-restored` outcome surfacing the callee's enumerations (an overwritten colliding ignored path and a divergent gitlink both appear in the recorded result), a degraded-confirmed rollback (`files-unrestored`) still advancing the execution epoch — the next turn-boundary capture mints refs under the new epoch namespace, never colliding with the superseded epoch's refs — and a daemon restart with the same-position recovery dispatched-but-unconcluded reconciling as at-target (the crash window between dispatch intent and restore completion): the restore re-runs to the fixpoint and the durable outcome lands, never a no-rewind conclusion stranding the partial tree.
- **T3.14 (ROLLBACK-SUPERSEDE) — Projection supersede-marking of rolled-back turns** (campaign B9)
  - **Files:** `run-engine.ts` (EXTEND) + the daemon composition root (the CP-006-1 `PiiEncryptor` injection site — Plan-001 / Plan-003 bootstrap; EXTEND: construct Plan-006's `Compactor` with this task's `RollbackAttributionSource` implementation, replacing T3.2's vacuous default)
  - **Spec coverage:** Spec-004 §Required Behavior (rolled-back turns stay in the timeline marked superseded by projection); Spec-004 §Driver-Level Rollback Mechanics (timeline presentation — supersede cutoff: turn > post-rollback position of the accepted `run.rolled_back` event); ADR-017 Decision Log (the authoritative log never truncates or rewrites)
  - **Verifies invariant:** I-004-15
  - **Behavior (authored):** rolled-back turns are never truncated from the authoritative log; the run / intervention projection marks them **superseded** by the **post-rollback position cutoff** carried on the accepted `run.rolled_back` event — a turn is superseded when its position is **strictly greater than that event's post-rollback position** (`turn > targetPosition`), so the epoch-0 prefix **at or below** the target stays current while the rewound tail above it is marked (`Spec-004 §Driver-Level Rollback Mechanics`). Cross-epoch rows are keyed additionally on the **pre-rollback execution epoch**: the projection consumes the durable source-epoch stamp T3.11 records on pre-rollback rows (the Plan-006-owned `sourceEpoch` + `sourcePosition` pair per CP-004-12, Plan-006 T1.9 — the named merge prerequisite above; the position companion — the straggler's per-event originating turn (T3.11) — is what lets the cutoff rank a late row at or below `targetPosition` into the epoch's surviving prefix rather than blanket-superseding it), so a replayed projection marks stale-epoch rows superseded exactly like the in-time rows of the epoch they belong to. Row-to-turn assignment is **uniform across delivery timing** (Codex round 4 ripple): a row ranks by its operation's originating turn — resolved from the stamped `sourcePosition` on late rows, and from the row's own operation linkage (the payload's correlation to the operation's opening (epoch, turn) association) on in-time rows of the content-asynchronous families — falling back to its timeline slot when no linkage resolves; identical content ranks identically whether it arrived in time or late, so an in-time turn-4 usage row delivered during turn 8 survives a rollback to 5 exactly like its late-delivered sibling (a synchronous family's originating turn and timeline slot coincide, so its assignment is unchanged). Marks are **scoped to the epoch each accepted `run.rolled_back` rewound** (Codex round 5): a mark from epoch _N_'s rollback applies to **no row of any later epoch** — after rollback 10→5, a re-execution, then rollback 7→6, the second epoch's re-executed turn 6 stays **current** (the Spec-004 cutoff: `6 > 6` is false) while the first epoch's turn 6 remains superseded — a re-executed turn **never inherits** the superseded mark its reused ordinal carries in an earlier epoch, and pre-rollback assistant output / tool activity can never resurface as current timeline state; the scoping is downward-inclusive across the run's lineage (Codex round 1 on the CP-004-13 consumer PR #232): an accepted rollback rewinds the run's **surviving history**, inherited earlier-epoch prefix included, so a row of epoch _e_ at position _p_ is superseded exactly when _p_ exceeds the minimum cutoff among accepted rollbacks at epoch ≥ _e_ — after 10→5, re-execution to 7, then 7→3, the inherited epoch-0 turn 4 (current after the first rollback) is superseded by the second, attributed to source epoch 0 with the second rollback's `targetPosition` 3. The projection **implements Plan-006 T3.2's `RollbackAttributionSource`** (`attributeAtCompaction` — the compaction-time attribution the Plan-006 compactor injects to stamp a rolled-back run's rewound-tail stubs: `'superseded'` + the triple for a rolled-back epoch's rewound tail, `'defer'` for a row a dispatched-but-unconcluded rollback intervention's span covers — read from the durable `interventions` write-ahead row, so the deferral survives restart (Codex round 4) — `'current'` otherwise, the `'current'` answer carrying the row's resolved originating position (its association turn, else its timeline slot) for the stub's preserved `originPosition` key (Codex round 5); the body-registered reciprocal of that Plan-006-owned interface seam, no Plan-006 symbol authored here) **and ships its composition-root wiring** (Codex round 4): the daemon composition (the CP-006-1 `PiiEncryptor` composition root — Plan-001 / Plan-003 bootstrap) constructs Plan-006's `Compactor` with this projection in place of the vacuous default from this task on, with a runtime assertion outside tests that production construction never carries the default (the CP-006-1 assertion pattern) — an unwired seam would silently compact every rewound tail unstamped while the integration suite passes on a manually-injected instance and **exports the superseded-turn read** (`supersededTurns(runId)` — the per-turn supersede attribution the position-cutoff + epoch keying produce: each superseded (epoch, turn) carrying its source epoch and the superseding rollback's `targetPosition` — lineage-resolved: the first accepted rollback at the row's epoch or later that rewound the surviving history containing it — values the cutoff comparison already holds — never a bare membership predicate, which could not populate the consumer's marker) as the seam **[Plan-013](./013-live-timeline-visibility-and-reasoning-surfaces.md)'s `timeline-projector.ts` + `TimelineRead` consume** to render rewound turns distinctly (CP-004-13 — Plan-004 provides-forward; the Plan-013-side consumer registration landed 2026-07-20, no Plan-013 symbol authored here — without this seam the marking never reaches the rendered timeline). **Tests:** a rolled-back run's turns above `targetPosition` are marked superseded while the at-or-below prefix stays current; the two-rollback sequence (10→5, re-execute, 7→6) keeps the second epoch's re-executed turn 6 current while the first epoch's turn 6 stays superseded — a reused ordinal never inherits an earlier epoch's mark; a projection rebuild preserves both markings; the exported `supersededTurns(runId)` returns the attributed marked set for a given run — each entry carrying its source epoch and the superseding rollback's `targetPosition`; the lineage sequence (10→5, re-execute to 7, 7→3) supersedes the inherited epoch-0 turn 4 with source epoch 0 + `targetPosition` 3 while the epoch-2 re-execution at turn 4 stays current; an in-time content-asynchronous row (a turn-4 usage row delivered during turn 8) survives a 10→5 rollback current, matching its late-delivered sibling's ranking (uniform row-to-turn assignment); a rebuild after the boundary `run.rolled_back` row and its stale-epoch rows are compacted to audit stubs still produces both markings — the stub-preserved `runId`/`runVersion`/`targetPosition` cutoff plus each stamped stub's `sourceEpoch` + `sourcePosition` + `runId` triple — stamped at ingestion for late arrivals, at compaction (via the injected `RollbackAttributionSource`) for the ordinary rewound tail — suffice (`Spec-006 §Compacted Event Format`, campaign B9 CP-004-12); the real daemon composition path constructs the `Compactor` with this projection — asserted through the composition root, never a manually-injected instance — and the runtime assertion rejects a production construction still carrying the vacuous default (Codex round 4); `attributeAtCompaction` answers `'defer'` for a row covered by a dispatched-but-unconcluded rollback row and flips to `'superseded'` / `'current'` once the intervention concludes.
- **T3.15 (ASK-EXPIRY-PARK) — Driver input-ask expiry park leg** (campaign B9; Spec-012 Part-B fail-closed follow-up, 2026-07-17)
  - **Files:** `run-engine.ts` (EXTEND)
  - **Spec coverage:** Spec-012 §Resolved Questions and V1 Scope Decisions (input-kind ask expiry is park-resumable via `waiting_for_input → paused`; never auto-approve); Spec-006 §Driver Ask Events (input-kind asks route to the run's interactive surface, not the approval pipeline)
  - **Verifies invariant:** I-004-17
  - **Behavior (authored):** the run-engine handles `driver_ask.expired` for `kind: 'input'` asks by parking the run **`waiting_for_input → paused`** — the park edge Spec-012's Part-B amendment adds to `run-state-machine.md` (previously legal only for rollback) — driven by the daemon's ask-expiry path and **deliberately NOT** the CP-004-9 / CP-012-5 approval-outcome seam (that seam's expired outcomes continue-with-refusal back to `running`, correct only for approval-pipeline categories; a driver input ask never traverses the approval pipeline). No input is fabricated (fail-closed); the ask closes as expired and the run parks in its ordinary resumable `paused` state (resume re-engages the provider, which may re-raise the ask). **Replay-atomic:** the park transition commits as one unit with `driver_ask.expired` under `withSessionAppendLock`; daemon startup reconciliation derives a missing sibling effect from the persisted `(ask_id, expiry_at)` pair; a crash-after-first-effect test rides this task. This is the reciprocal of Plan-012's B13-authored T2.8 driver-ask-normalizer emission; the CP-004-9 expired→continue-with-refusal mapping stays approval-pipeline-only.

> **Tier-6 reciprocal (CP-004-9, doc-only):** when Phase 3 executes, the run-engine also registers Plan-012's approval outcome listener (`registerApprovalOutcomeListener` — blocking/unblocking transitions per the CP-004-9 row) and calls `cancelPendingForRun(runId, cause)` from the same terminal path that fires `onRunTerminal` hooks, so pending approval requests settle as `approval.canceled` instead of dangling (Plan-012 A-6/A-10). (Campaign B13, Plan-012 CP-012-11:) the same Phase-3 execution also has the T3.7 emitter read Plan-012's `resolvedPostureFor(runId)` accessor — registered beside its CP-004-8 setup gate, whose `assertRunReady` returns void — at `run.running` emission, so the stamp carries the complete resolved posture; no resolved posture ⇒ fail closed, never an unstamped sandboxed run. (Campaign B13, Codex round 3:) the same execution also consumes Plan-012 T2.8's input-ask lifecycle signals through the transition applicator the CP-004-9 listener drives: a live input-kind `driver_ask.requested` applies `running → waiting_for_input` atomically with its append — making T3.15's park edge reachable and the pending-ask blocking-state exemption real for input asks — and a delivered answer applies `waiting_for_input → running` atomically with `driver_ask.responded` (run-state-machine.md's annotated causes on both edges); a live-leg provider retraction releases the blocking state the same way for either kind — `waiting_for_* → running` atomically with `driver_ask.canceled`, no outcome delivered (Codex round 4).

> **Resolved at the Plan-016 walk (same Tier-6 audit):** child-run behavior on parent interrupt/cancel is **non-cascade** — children are independent intervention targets (Spec-016 §Run Hierarchy; run-state-machine.md §Child-Run Behavior as rewritten by Plan-016 A-016-17). No cascade logic is authored here or in Plan-016; `run_links` is owned by Plan-016 (Tier 6). (F-004-3-08 → closed by Plan-016 A-016-17.)

### Phase 4 — Desktop Run-Controls

**Goal:** The client SDK + renderer run-controls subtree + the `run rollback` CLI command — optimistic projection reconciled to daemon truth, capability-gated steer (with `rollback` the second identically-gated control per campaign B2 — desktop control-gate at T4.2, CLI command at T4.7, both campaign B9), renderer bridge-only.

**Precondition:** Phase 1–3 daemon surface landed; **D-004-3 (`run.*` method strings) ratified** before T4.1 / T4.4 wire calls land. Plan-023 renderer substrate + `window.sidekicks` bridge present. For T4.7 only, [Plan-007](./007-local-ipc-and-daemon-control.md)'s `apps/cli` scaffold (Phase R3, T-007r-3-1) is present — the CLI command file cannot compile before the workspace exists (YAML-gated below via the `external_plan_phase_merged` R-series form, Codex round 4 — prose alone is Gate-5-invisible; the Plan-023 renderer substrate above stays prose-only, and the Plan-010 snapshot dependency is enforced transitively by the `phase: 3` entry, whose own block carries it).

```yaml
preconditions:
  # Machine-enforced form of the phase-1–3 prose (preflight Gate 5). The
  # Part-B box entry is carried onto this phase too: the auto-walk soft-skips
  # an ineligible phase and tries later ones, so gating Phase 3 alone would
  # let the walk select Phase 4 past the unmet park leg.
  - { type: plan_phase, plan: 004, phase: 1, status: merged }
  - { type: plan_phase, plan: 004, phase: 2, status: merged }
  - { type: plan_phase, plan: 004, phase: 3, status: merged }
  # T4.7 only: Plan-007's Phase-R3 CLI scaffold (the apps/cli workspace + the
  # main.ts Cli instance, T-007r-3-1 / T-007r-3-2) — the command file cannot
  # compile before the workspace exists. R-series phases resolve through the
  # external_plan_phase_merged form (the Plan-022 Phase-4 precedent).
  - { type: external_plan_phase_merged, plan: 007, phase: R3 }
  - { type: precondition_box_checked, box: "Driver input-ask park leg authored" }
```

#### Tasks

- **T4.1 — `runControlClient.ts` SDK (single daemon-transport factory)**
  - **Files:** `packages/client-sdk/src/runControlClient.ts` (CREATE)
  - **Spec coverage:** Spec-023 §Signature Feature Composition Sketches (Runs View — daemon run-state / queue subscriptions); Spec-004 §Interfaces And Contracts (run-control client surface)
  - **Verifies invariant:** I-004-9
  - **Transport (authored):** a single daemon-transport factory `createDaemonRunControlClient(transport)` riding `window.sidekicks.daemon.call` / `daemon.subscribe`. **Rationale:** run-control authority is daemon-only — `ADR-003 §Decision` ("the daemon will be the authority that applies and records their outcomes"), Assumption #3 (`ADR-003 §Assumptions Audit`), and no control-plane run-control surface exists in the corpus (verified). This **excludes** the `sessionClient.ts` dual-transport pattern; it structurally resembles `membershipClient.ts`'s daemon half (`docs/architecture/cross-plan-dependencies.md §NS-28: Plan-002 Phase 5 — Client SDK Membership Surface`), but the rationale is the source fact, not the precedent.
  - **Consumes:** `window.sidekicks.daemon` bridge (Spec-023); the `run.*` method strings (CP-004-4 — ratified, D-004-3)
- **T4.2 — Capability-gated steer + rollback; pause/resume/interrupt/cancel NEVER gated**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (CREATE)
  - **Spec coverage:** Spec-004 §Default Behavior (capability-gated controls; pause/resume orchestration-layer, never driver-gated); Spec-004 §Interfaces And Contracts (the `rollback` capability gate — the second gated control); ADR-011 (pause is orchestration, not a driver capability)
  - **Verifies invariant:** I-004-10
  - **Note:** the §Rollback "disable any false flag" line is a **trap** — of this plan's controls exactly two are gated on driver capability: `steer` and, per I-004-10 as amended, the campaign-B2 `rollback` intervention. The desktop `rollback` control-gate is **authored here** (campaign B9): the run-controls subtree disables the rollback affordance when the driver does not declare the `rollback` capability, the renderer mirror of T3.12's daemon-side static gate — no orchestration fallback, identical to the `steer` gate. `pause` / `resume` / `interrupt` / `cancel` are orchestration-layer and are never capability-gated.
- **T4.3 — Optimistic-vs-runtime-truth reconciliation**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND)
  - **Spec coverage:** Spec-023 §Signature Feature Composition Sketches (Runs View — daemon run-state subscription)
  - **Verifies invariant:** I-004-9
  - **Consumes:** daemon run-state subscription — subscribe request shape follows the shipped `subscribePresence → { sessionId }` precedent (`docs/architecture/cross-plan-dependencies.md §NS-29: Plan-002 Phase 6 — Desktop session-members Renderer`)
- **T4.4 — Surface 9 run-states + 6 intervention-states**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND)
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (surface 9 run-states + 6 intervention-states; canonical terminal is failed, not errored)
  - **Verifies invariant:** I-004-9
  - **Consumes:** `RunState` / `InterventionState` (T1.3); `run.*` subscription channel names (CP-004-4 — ratified, D-004-3)
- **T4.5 — Bridge-bypass import-restriction**
  - **Files:** `apps/desktop/src/renderer/src/run-controls/` (EXTEND) + import-restriction assertion test
  - **Spec coverage:** Spec-023 §Preload Bridge Contract (renderer composes via the preload bridge, never bypasses)
  - **Verifies invariant:** I-004-11 (= CP-004-5)
  - **Note:** enforces the renderer bridge-discipline rule (`docs/architecture/cross-plan-dependencies.md §2. Package Path Ownership Map`, `apps/desktop/src/renderer/` row) for the `run-controls/` subtree.
- **T4.6 — Test suite (SDK + orchestration + single-client renderer component tests)**
  - **Files:** `packages/client-sdk/**/*.test.ts` + daemon orchestration integration tests + `apps/desktop/src/renderer/src/run-controls/__tests__/*.test.tsx`
  - **Spec coverage:** Spec-004 §Acceptance Criteria (SDK + orchestration + renderer-component test coverage)
  - **Verifies invariant:** I-004-9, I-004-10, I-004-11 (SDK + orchestration + renderer-component layer)
  - **Note:** single-client RTL renderer component tests ship **now**, mirroring the merged NS-29 `session-members/participant-roster.test.tsx` precedent (`@testing-library/react` `render`/`screen` + an `installMockBridge` `{ daemon: { call, subscribe } }` mock + the bridge-projection / no-direct-daemon-import assertion, per the `SessionBootstrap.test.tsx` idiom). Only the **multi-client live-run-state E2E** (cross-client daemon-stream reconciliation) defers — to the **Plan-023 Tier-8 IPC dispatcher**, _not_ BL-131 (which is Plan-003-scoped and whose "harness unavailable until Tier 8" premise NS-29 has already overtaken). SDK + orchestration tests are in-scope now.
- **T4.7 — `run rollback` CLI command** (campaign B9; RA-7 reachability)
  - **Files:** `apps/cli/src/commands/run-rollback.ts` (CREATE) + `apps/cli/src/main.ts` (EXTEND — register the command on Plan-007's clipanion `Cli` instance; clipanion has no auto-discovery) + `apps/cli/src/commands/__tests__/run-rollback.test.ts`
  - **Spec coverage:** Spec-004 §Interfaces And Contracts (the `rollback` intervention client surface + 6-state `InterventionResult` + `RollbackInterventionResult` disposition-union rendering + result-state→exit mapping); Spec-007 §Required Behavior (the CLI is the first delivery track; commands consume the daemon through the typed client, never daemon internals)
  - **Verifies invariant:** I-004-10
  - **Command (authored):** `ai-sidekicks run rollback <runId> --to <targetPosition> --expected-run-version <n> [--wait-timeout <seconds>] [--resume-key <clientIdempotencyKey>]` — a clipanion command file (kebab filename → nested `run rollback` path per the Plan-007 `daemon-status.ts` command-file precedent, `Plan-007 §Client SDK + CLI (packages/client-sdk/, apps/cli/)`). The command is **registered on Plan-007's `apps/cli/src/main.ts` `Cli` instance** (T-007r-3-2 CREATE) — clipanion has no auto-discovery, so an unregistered command file is unreachable; Plan-007 owns `main.ts` and Plan-004 EXTENDs it with this one `.register()` call (dep-map §2 `apps/cli/src/main.ts` row; the CP-004-4 `run.*` reciprocal with Plan-007 already covers the namespace). It dispatches the `run.intervene` `rollback` intervention (CP-004-4 namespace; T1.2 payload arm) via the `@ai-sidekicks/client-sdk` typed daemon client — the client seam generates the **mandatory** requester-side `clientIdempotencyKey` (a fresh UUID per invocation — unless `--resume-key` supplies the key a prior invocation printed, reused verbatim so the T1.2 replay contract takes over from the recorded row (Codex post-merge round) — `Spec-004 §Interfaces And Contracts`; the T1.2 arm carries it) so an at-least-once retry replays the recorded outcome rather than re-applying — and renders the 6-state `InterventionResult` — including the `RollbackInterventionResult` disposition on terminal `applied` / `degraded` rollback outcomes (T1.3; the state-split contract carries no disposition on any other state, and each terminal state admits only its own disposition class — `applied` ⇒ `RollbackAppliedResult`, `degraded` ⇒ `RollbackDegradedResult` — so a response whose `state` contradicts its disposition fails parse before exit-mapping, never exiting 0 on a failed restore; Codex round 5) — surfacing on a `files-restored` render, and on a `files-partially-restored` degraded render (Codex re-audit round 2), the arm's `overwrittenIgnoredPaths` / `divergentGitlinks` warnings (informational, exit unaffected in both directions: `applied` still exits 0 and the warnings never alter `degraded`'s code; never collapsed) — and maps the **terminal result state to a POSIX exit code**: `applied` → 0, and `rejected` / `expired` / `degraded` → distinct nonzero codes per the `exit-codes.ts` scheme. A response still **`requested` / `accepted`** (admission acknowledged, application in flight — e.g. an idempotent replay observed mid-application) never exits the process (Codex round 4): the command **waits for the terminal state by idempotent replay** — it re-dispatches the identical `run.intervene` payload at a bounded interval, reusing the SAME `clientIdempotencyKey` minted at invocation start (the T1.2 replay contract returns the recorded row's current state, never re-applying), until the intervention reaches `applied` / `rejected` / `degraded` / `expired`, then renders and exit-maps that terminal state. The wait is **client-side bounded** (Codex round 5 — supersedes round 4's "expiry backstop" termination claim): intervention `expired` fires only on a version advance or a target-state change (`Queue And Intervention Model §State Model` — no wall-clock expiry exists), so a stalled daemon would otherwise hold the poll open forever; `--wait-timeout <seconds>` (default 600; `0` explicitly opts into an unbounded wait) stops polling and exits with its own distinct `exit-codes.ts` code — a client-side outcome, not a result state: the intervention stays live daemon-side, and the CLI prints the `interventionId` + `clientIdempotencyKey` so a later re-invocation passing that key via `--resume-key` resumes the idempotent replay, never re-applying (without the option every invocation mints fresh, so the printed key would be unusable — Codex post-merge round). A genuine transport failure mid-wait exits through the transport mapper below. The static `driver.capability_unsupported` refusal is a **normal `run.intervene` response** with lifecycle state `rejected` carrying `rejectionReason` (`Queue And Intervention Model §Driver Result To Lifecycle Mapping` maps it `requested → rejected`), so it rides the **result**, not the `JsonRpcError` channel — the CLI renders the `rejectionReason` with **no disposition** (a `rejected` response carries none — `result?: never` on the state-split arm; Codex round 4) and adds **no** local gate (it renders the daemon's authoritative decision, I-004-10). The `exit-codes.ts` `JsonRpcErrorCode → PosixExitCode` mapping still covers genuine **transport** errors (an unreachable daemon, a malformed request). `--expected-run-version` is **required** (the fail-closed guard is mandatory, D-004-1 / T3.12 — the CLI never omits it). Honors Plan-007's `apps/cli` import isolation (I-007-13 — imports only `@ai-sidekicks/client-sdk`, `@ai-sidekicks/contracts`, `clipanion`, Node built-ins; never `@ai-sidekicks/runtime-daemon`), enforced by `apps/cli/eslint.config.mjs`. **Consumes:** the [Plan-007](./007-local-ipc-and-daemon-control.md) `apps/cli` scaffold (Phase R3, T-007r-3-1 — the precondition prose above) + `@ai-sidekicks/client-sdk` daemon client + the `run.*` namespace (CP-004-4, ratified D-004-3). Tests: dispatches `run.intervene` with the `rollback` arm carrying `targetPosition` + `expectedRunVersion` + a client-generated `clientIdempotencyKey` (UUID); renders each of the 6 result states and maps each terminal state to its POSIX exit code (`applied` → 0; `rejected` / `expired` / `degraded` → their distinct nonzero codes); an `accepted` first response followed by an `applied` replay renders the terminal state and exits 0 — the wait-by-idempotent-replay path, same key across polls — and an intervention expiring mid-wait exits with `expired`'s code; a wait exceeding `--wait-timeout` exits with the pending-timeout code and prints the `interventionId` + key (the intervention stays live daemon-side); a `driver.capability_unsupported` refusal renders as a `rejected` result carrying `rejectionReason` and no disposition (not a transport error); the command is reachable through the real `Cli` (an end-to-end `ai-sidekicks run rollback` invocation resolves to this command, proving registration); a `--resume-key` re-invocation reuses the supplied key verbatim (no fresh mint), enters the wait-by-replay loop, and renders the recorded terminal state without re-applying; a `files-restored` result renders its `overwrittenIgnoredPaths` / `divergentGitlinks` warnings and still exits 0, and a `files-partially-restored` result renders the same warnings alongside `failedStep` while exiting with `degraded`'s code; a `--expected-run-version`-omitted invocation is a usage error before any dispatch.

## Parallelization Notes

- Queue store and run-engine integration can proceed in parallel once the contracts are fixed.
- UI controls should wait for intervention result semantics to stabilize.

## Test And Verification Plan

- Queue durability tests across daemon restart
- State-machine tests for pause, resume, interrupt, and blocked states
- Manual verification of queue then steer then interrupt flows

## Rollout Order

1. Ship queue persistence and read-only queue visibility
2. Enable queue mutation and the orchestration-layer run-controls — interrupt, cancel, pause, resume (none capability-gated, I-004-10)
3. Enable steer where driver capabilities allow — `steer` and the campaign-B2 `rollback` intervention are this plan's two driver-capability-gated controls (rollback sequenced by campaign B9: daemon handler T3.12, provider-leg + file-restore T3.13, desktop gate T4.2, CLI command T4.7)

## Rollback Or Fallback

- Disable `steer` and `rollback` (this plan's two capability-gated controls — I-004-10 as amended; `rollback` added by campaign B9 via T3.12–T3.14 / T4.2 / T4.7) if driver-capability handling regresses; queue + the orchestration-layer controls (interrupt, cancel, pause, resume) stay enabled — they are not capability-gated (I-004-10). A rollback whose file-restore leg cannot proceed is handled **fail-closed within the intervention itself**, so no plan-level fallback is needed: for a writable live root an unavailable snapshot or a failed HEAD precondition **rejects the whole intervention pre-dispatch** (no leg applied), and an execution-time restore failure after the confirmed conversation leg records `files-unrestored` / `files-partially-restored` (T3.13); `conversation-only` is confined to the two Spec-004 cases — `read-only` mode and a released execution root — never a general file-leg-unavailable degradation.

## Risks And Blockers

- Provider capability mismatch can produce misleading controls
- Queue concurrency across multiple clients needs strict daemon serialization

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped: []
```

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

**Round 2 (Codex on 8e6583a) — 10 findings, all fixed in one commit.**

- **F1 (contracts):** `RollbackInterventionResult` restructured from the four-literal `fileRestore` into a `disposition` discriminated union spanning the full `Spec-004 §Required Behavior` vocabulary — file-leg (`files-restored` / `files-partially-restored` (+`failedStep`) / `files-unrestored` / `conversation-only`) plus the pre-file arms (`pause-only`, `nothing-applied`, `position-mismatch` (+`requestedPosition`/`confirmedPosition`)); never-collapse preserved; T1.3 mirror + T4.7 rendering updated.
- **F2 (contracts):** root-`busy` reclassified as pre-dispatch whole-rejection, not `files-unrestored`; the `files-unrestored` comment now names execution-time refusal causes only (the HEAD re-verify) and drops "root busy".
- **F3 (contracts):** `InterventionRequestResponse` discriminated per intervention type — a `rollback` outcome that fails `RollbackInterventionResult` parse fails validation, never falling through to the generic `Record<string, unknown>` arm; test added.
- **F4 (T4.7):** explicit result-state→POSIX-exit contract added (`applied`→0; `rejected`/`expired`/`degraded`→distinct nonzero) alongside the transport `JsonRpcErrorCode` mapper; the static `driver.capability_unsupported` refusal rides the **result** as `rejected` carrying the new `rejectionReason` field, not the transport channel; per-state test.
- **F5 (T3.11):** the "typed stamp shape Plan-006-owned" phantom replaced by CP-004-12 — a named merge prerequisite for the owed Plan-006 epoch-attribution carrier + a dep-map Plan-004→Plan-006 edge; no Plan-006 symbol authored. **Lead-owned follow-up:** register the typed epoch-attribution field in Plan-006 / Spec-006 (the B11 Plan-006 bundle shipped without it).
- **F6 (T3.12):** the waiting-source void now names the durable actions — `approval.canceled` via Plan-012 `cancelPendingForRun` and `driver_ask.canceled` via its owning surface (both consumed, not authored), atomic with the rollback transition — plus the queued-resume void and the progression-serialized race; test added.
- **F7 (T3.13):** new `run.execution_root_released` (409, run state unchanged) added to `error-contracts.md §Run` as a Plan-004 extension (the Plan-027 error-contracts-extension precedent) and named in T3.13's non-resumable rule; test added.
- **F8 (T4.7):** `apps/cli/src/main.ts` added to T4.7 Files (EXTEND) with the Plan-007 ownership note (T-007r-3-2 CREATE; clipanion has no auto-discovery) + a dep-map §2 row + an invocation-through-real-`Cli` registration test.
- **F9 (fallback bullet):** reworded fail-closed — an unavailable snapshot / failed HEAD precondition rejects pre-dispatch and an execution-time failure records `files-unrestored` / `files-partially-restored`; conversation-only is confined to read-only mode + a released root.
- **F10 (T3.14):** the exported `supersededTurns(runId)` read seam named + CP-004-13 + a dep-map Plan-013→Plan-004 consumer edge; no Plan-013 symbol authored. **Lead-owned follow-up — landed 2026-07-20:** the Plan-013 consumer registration (`timeline-projector.ts` + `TimelineRead` calling the seam; Plan-013 §Implementation Steps / Spec-013 §Required Behavior).

**Round 3 (Codex on abc44be) — 3 findings, all fixed in one commit.**

- **F1 (contracts):** the rollback response arm is state-split — a terminal rollback outcome (`applied` / `degraded`) REQUIRES `result: RollbackInterventionResult` (a disposition-less terminal response fails parse; test), while `requested` / `accepted` / `rejected` / `expired` rollback responses carry no `result` (`rejectionReason` covers the rejected cause); T1.3 mirror updated.
- **F2 (T3.13):** the execution-epoch trigger corrected — the epoch advances with **each accepted `run.rolled_back`** (a confirmed conversation rewind, including a position-mismatch rewind confirmed at the recorded floor), regardless of the file-leg disposition, per `Spec-004 §Required Behavior`'s collision requirement (re-executed turns reuse position ordinals, so a degraded-confirmed rollback keying the epoch on the final `applied` state would collide with the superseded epoch's `turn-<N>` refs); test added. The same stale "per applied rollback" shorthand in Plan-010's ratified `<E>` epoch-source box (§Phase 5 — Turn-snapshot service) and Spec-010 §Turn-Boundary Snapshots is a **lead-owned erratum** riding the queued Spec-010 errata batch — out of this PR's scope, not re-imported here.
- **F3 (preconditions):** Phase 3's `preconditions:` block gains a Plan-005 `plan_phase` entry (the campaign B10 parity bundle's placement of the four-op `ProviderDriver` widening + the `InterventionType` union widening per CP-005-6), and Phase 1 gains a new `preconditions:` block with the same entry gating T1.2's `'rollback'` arm — both machine-enforced via preflight Gate 5's declared-⊆-shipped resolution. _Placement corrected pre-merge (lead verification against the B10 branch): the widening is Plan-005 **T1.8 — Phase 1's** contract catch-up task (re-opening the shipped phase), not a Phase-3 task; both entries retargeted `phase: 3` → `phase: 1`, which also avoids transitively dragging Plan-005 Phase 3's own Plan-006 gate into this plan's chain._

**Round 4 (Codex on 2249792) — 6 findings, all fixed in one commit.**

- **F1 (T4.7):** a `rejected` rollback response renders `rejectionReason` only — the state-split contract carries no disposition outside `applied` / `degraded`, so the prior "rejectionReason + disposition" rendering would dereference a nonexistent field or force the schema loose; rendering + tests corrected.
- **F2 (T4.7):** pending states defined — a `requested` / `accepted` response never exits the process: the CLI waits for the terminal state by idempotent replay (re-dispatching with the SAME `clientIdempotencyKey` minted at invocation start; the T1.2 replay contract returns the recorded row, never re-applies); accepted→applied and mid-wait-expiry tests added. _The "bounded by the intervention's own expiry backstop" termination claim this round recorded was wrong — corrected in round 5 F6: `expired` has no wall-clock trigger, so the bound is the client-side `--wait-timeout`._
- **F3 (Phase 4):** the Plan-007 R3 CLI-scaffold dependency is machine-gated — `{ type: external_plan_phase_merged, plan: 007, phase: R3 }` (the Plan-022 Phase-4 R-series precedent) joins the YAML block; the prose-only form was Gate-5-invisible, letting Phase 4 dispatch before `apps/cli` exists.
- **F4 (contracts):** `position-mismatch` regrouped from the no-rewind group into the confirmed-rewind group (supersedes the round-2 grouping): its conversation leg DID rewind — to the confirmed floor — so `run.rolled_back` emits at the confirmed position and the execution epoch advances, exactly the T3.13 trigger; grouping it beside `pause-only` / `nothing-applied` invited implementations to skip both.
- **F5 (T3.13):** the rootless / read-only branch selects conversation-only pre-resolution but records the disposition **only after** the driver-confirmed rewind — a failed or unconfirmed conversation leg records `pause-only` / `nothing-applied`, never a fabricated `conversation-only` success; test added.
- **F6 (T1.4 / T3.12 / contracts / schema):** `rejectionReason` is replay-durable — the new nullable `interventions.rejection_reason` column (Plan-004-owned DDL) is written at T3.12's refusal path and read back on idempotent replay; the wire arm forbids `result` on `rejected`, so the cause could not ride the stored outcome JSON.

**Round 5 (Codex on b503b34) — 7 findings, all fixed in one commit.**

- **F1 (contracts / T1.3 / T4.7):** the terminal rollback arms are state-scoped per disposition class — `applied` admits exactly `RollbackAppliedResult` (`files-restored` / `conversation-only`) and `degraded` exactly `RollbackDegradedResult` (the other five) — so a malformed response pairing `state: "applied"` with a degraded disposition fails parse instead of exit-mapping to 0 while rendering a failed restore; `RollbackInterventionResult` stays the union; the `Queue And Intervention Model §Driver Result To Lifecycle Mapping` degraded enumeration gains the missing `nothing-applied` (a b9-introduced literal, set-quantifier re-derive).
- **F2 (Preconditions / Phase 3):** the CP-004-12 Plan-006 epoch-attribution carrier is machine-gated — a new unchecked `## Preconditions` box ("Plan-006 epoch-attribution carrier registered", checked by the lead-owned Plan-006/Spec-006 amendment PR per the Part-B precedent) rides a Phase-3 `precondition_box_checked` entry, paired with `{ plan: 006, phase: 1, status: merged }` for the shipped-carrier ordering; prose alone was Gate-5-invisible, letting Phase 3 dispatch before the carrier exists.
- **F3 (contracts / T1.3 / T3.12):** `rejectionReason` is REQUIRED on the rollback `rejected` arm — every refusal family of `Queue And Intervention Model §Intervention State Transition Table` (boundary-check, root-`busy`, T2.5 authorization, capability) persists its machine-readable cause to `interventions.rejection_reason`, not only the capability gate; a cause-less rejected settle would be unreconstructable on idempotent replay; tests added.
- **F4 (T3.11):** a pre-rollback-epoch `driver_ask.requested` is a surface-opening event, not a passive row — the late-append path records it as an atomic closed pair (epoch-stamped `requested` + `canceled`), registers no pending ask surface, fires no `waiting_for_*` transition, and never hands a permission-kind stale ask to the approval-pipeline normalizer; tests added.
- **F5 (T3.12):** a `waiting_for_approval` void whose pending approval was normalized from a permission-kind driver ask settles BOTH surfaces — `approval.canceled` plus the originating native `driver_ask.canceled` — within the same atomic void (provenance consumed from Plan-012's T2.8 normalizer, no Plan-012 symbol authored), so the provider-side ask cannot stay pending or later respond against the rewound turn; race test extended.
- **F6 (T4.7):** the round-4 "expiry backstop" termination claim was wrong — `expired` fires only on a version advance or target-state change (`Queue And Intervention Model §State Model`), never a deadline — so the pending wait is client-side bounded: `--wait-timeout <seconds>` (default 600; `0` opts into unbounded) exits with its own distinct code, printing `interventionId` + `clientIdempotencyKey` so a later same-key re-invocation resumes the idempotent replay; test added.
- **F7 (T3.14):** supersede marks are scoped to the epoch each accepted `run.rolled_back` rewound — a re-executed turn never inherits the mark its reused ordinal carries in an earlier epoch (rollback 10→5, re-execute, rollback 7→6 keeps the second epoch's turn 6 current per the `6 > 6` cutoff while the first epoch's turn 6 stays superseded); the prior "still marked by its source epoch" wording invited exactly that inheritance; behavior + test rewritten.

**Round 6 (Codex on 404239b) — 3 findings, all fixed in one commit.**

- **F1 (T3.12):** the waiting-source void is explicitly deferred to the **confirmed conversation leg** — atomic with the `waiting_for_* → paused` transition and the forward emission, never at admission — matching `run-state-machine.md §Rollback Transitions`' conversation-leg-confirmed gate; a failed or no-rewind leg leaves the approval/input block and its native ask intact, the run stays `waiting_for_*`, and the outcome records `nothing-applied`; the prior "atomic with the rollback transition" wording admitted a void-before-rewind reading that would drop a still-valid block; test added.
- **F2 (T3.11):** pre-rollback-epoch **terminal-class** stragglers are absorbed at the epoch check itself, before the T3.7 terminal emitter — the reopened run's current `runVersion` carries no terminal row, so the emitter's `(runId, runVersion)` duplicate guard alone would append the stale terminal against the new epoch and re-terminalize the rewound run; the route-into-emitter path is confined to the already-terminal condition it is safe for; live + replay test added.
- **F3 (T3.13):** the same-position file-leg recovery is defined as its own confirmed branch — the conversation leg's convergence no-op (driver confirms `sessionPosition == targetPosition`, no movement) counts as the confirmed floor, so the composite proceeds to `restoreToTurn` and re-runs to the fixpoint with the forward event + uniform epoch advance, instead of falling into the ordinary no-rewind handling (`nothing-applied`) that would have made the T3.12 recovery carve-out unreachable; test added.

**2026-07-20 — W2.5 targeted re-audit (campaign Task 23, B9 delta — the rollback / late-absorption / ask-expiry-park task set with its contracts, schema, and dep-map edits): zero critical / zero major findings (5 minor / 2 nit dispositioned in this closing PR); Status promoted `review` → `approved`.** First-time promotion of this standing-`review` plan (the Tier-5 / NS-17 readiness-audit box is checked; the D-004-1..3 ratifications executed at the Tier-5 swap) — the closing re-audit PR (#228) is cited in lieu of a tier git-tag (runbook R7 durable-citation refinement). The seven dispositions, all fixed here: (1) T4.7's promised same-key resume was unreachable — the command gains `[--resume-key <clientIdempotencyKey>]`, reusing the printed key verbatim instead of minting fresh (Codex post-merge round on PR #225, as are 2–4); (2) I-004-14 routed every terminal-class straggler into the T3.7 emitter — rescoped to T3.11's round-6 behavior (already-terminal → emitter no-op; pre-rollback-epoch, terminal-class included, absorbed at the epoch check before the emitter); (3) I-004-15's "always lands the run in `paused`" overclaimed — landing is conversation-leg-confirmed-only, a non-confirming outcome leaves the source state in place; (4) the bare `files-restored` applied arm gains the two REQUIRED Spec-010-mandated enumerations `overwrittenIgnoredPaths` / `divergentGitlinks` (empty-when-none) — Plan-004-owned wire field names onto which T3.13 maps the enumerations the Plan-010 T5.2 `restored` variant carries (the callee-side shape stays Plan-010-owned), surfaced by T4.7's success render; (5) I-004-13 + T3.7 still said trigger "pair" — Plan-006's round-3 promote leg made it a trio (`trg_run_terminal_key_promote`; repo-swept: these were the only two stale sites); (6) T3.13's Verifies line gains I-004-3 (the invariant's own text names T3.13 the provider-leg author of the dispatch-axis exception); (7) the sweep's full invariant-matrix pass found one sibling — I-004-4's column claimed T1.7, which no task or spec text supports (T1.7 claims only I-004-7); the column entry is dropped. Audit dimensions: file-authority closure; spec-coverage integrity (§-anchors verified against spec content); invariant claims (I-004-13..17 re-walked against task behaviors — the two overclaims above were the only mismatches); machine-gate shape + acyclicity (the Phase-1/3/4 `preconditions:` chains resolve forward-only); cross-plan ownership (every consumed symbol named to its owner; no Plan-005 / 006 / 010 / 012 / 013 symbol authored); census; consumer readiness (the CP-004-7/8/10/13 provide-forward contracts are concrete for their consumers); internal coherence. Dispatch layering is tightened, never loosened, by this PR: the CP-004-12 and partial-restore-enumeration boxes stay unchecked and Phases 3–4 stay Gate-5-held until the Plan-006 carrier, the Plan-010 partial-restore amendment, Plan-010 Phase 5, and Plan-005 Phase 1 land — promotion clears the doc-status gate only. Codex round 1 on this closing PR added two dispositions, both fixed here: the Spec-004 confirmed-floor no-op parenthetical gains the recovery-convergence exception at its three sites (§Required Behavior, the §Driver-Level confirmed-floor bullet, the acceptance criterion) — an erratum-class narrowing under the runbook's spec-amendment rule (typo / citation / narrowing does not flip the spec), reconciling the sentence with the same bullet's carve-out whose "restore re-runs to the fixpoint" text the plan's round-6 branch implements; and the contracts comment's "mapped verbatim" claim is reframed as the explicit Plan-004-owned wire mapping (the callee-side result shape stays Plan-010's to pin — cross-plan one-writer). Codex round 2 added two more, both fixed here: the landing-overclaim class the audit fixed on I-004-15 also lived in Spec-004's own acceptance criteria and the `run-state-machine.md §Rollback Transitions` intro — the ACs now scope the `paused` landing and the waiting-source block void to the conversation-leg-confirmed rewind with the no-rewind source-state preservation stated on the failure-path criterion, and the domain doc's intro sentence + waiting bullet gain the confirmed qualifier their own transition-table rows and confirmed-leg paragraph already carried (the same erratum-class narrowing — the semantics were derivable from §Required Behavior's nothing-for-every-other-source path); and the `files-partially-restored` wire arm carried only `failedStep` while Spec-010's rationale for the distinct partial disposition is precisely that earlier steps' effects are already on disk — the arm gains the same two REQUIRED never-silent enumerations (those the steps completed before the failure produced; empty-when-none), mapped by T3.13, rendered by T4.7's degraded render, I-004-16 extended; with the partial outcome now carrying the first pass's overwrites durably, the recovery pass's empty enumeration is honest (it overwrote nothing) — the callee-side naming on `partial_restore` stays the Plan-010-owned erratum in the queued Spec-010 errata batch. Codex round 3 added one: the `DriverRollbackResult` floor-validation mirror in the contracts doc's driver section still carried the unexcepted no-op-is-no-rewind rule the round-1 narrowing excepted at Spec-004's three sites — a fourth mirror, now carrying the same recovery-convergence exception (a comment-only coherence fix of Spec-004-owned validation semantics sitting in the driver-contract region, per the dep-map housekeeping exception; the driver result shape is untouched); the repo-wide floor-rule sweep confirms no fifth mirror — the wire `targetPosition` comment already carried the carve-out, and the `RunRolledBackEvent` comment is convergence-accurate as written. Codex round 4 added three, all fixed here: the narrowed landing AC overclaimed `targetPosition` for the confirmed-mismatch branch — the AC and the `run-state-machine.md` transition rows' position claims now land at the confirmed position (`targetPosition` bar a position-mismatch degrade); the partial-arm enumerations' producer side is now a **named merge prerequisite** — a new unchecked §Preconditions box ("Plan-010 partial-restore enumerations registered") + Phase-3 `precondition_box_checked` entry hold T3.13's dispatch until the lead-owned Plan-010/Spec-010 amendment lands (the CP-004-12 pattern; authoring Plan-010's T5.2 here would breach cross-plan §2 one-writer and falsify this audit's no-Plan-010-symbol attestation); and the restart-reconciliation rule was ambiguous for a dispatched-but-unconcluded same-position recovery, whose at-target and unchanged observations coincide by construction — reconciled as **at-target** at both Spec-004 sites (completion branch, never the no-rewind conclusion that would strand the partial tree), with the T3.13 crash-window test added.

**2026-08-03 — Cross-user run-control authorization (paired Spec-004 / Spec-012 amendment): Status flipped `approved` → `review`; the §Preconditions paired-spec box re-opened.** [vision.md §The Collaboration Model](../vision.md)'s ratified statement — a participant steers agents in the session, "steering an agent owned by another participant is granted by that owner's approval policy, never by membership alone," and "membership alone never authorizes execution on someone else's machine" — had no contracted authorization shape on the intervention path. [Spec-004 §Interfaces And Contracts](../specs/004-queue-steer-pause-resume.md#interfaces-and-contracts) now names it: the Cedar action for a run-control intervention is `Action::"intervene"` on the target `Run` resource with the run's hosting node in the request context, authorization evaluates against session membership role and never against run authorship, and a non-local hosting node **adds** the target-node-owner approval requirement rather than substituting for the role check. **Adds** invariant **I-004-19** (cross-node intervention never bypasses node-owner authority; settles `rejected` when that approval is absent or denied) on **T2.5**, whose Spec-coverage and Consumes markers gain the hosting-node context input and the [Spec-024](../specs/024-cross-node-dispatch-and-approval.md) approval leg (consumed, never authored — cross-plan §2 one-writer holds). **Mints no** approval category, intervention type, wire field, table, or CP row: the cross-node leg reuses `tool_execution`, and the four canonical intervention types are unchanged. **The rule's reach is all six run-control verbs (Codex round 2 on the reconciliation PR):** the orchestration-layer `run.pause` / `run.resume` — daemon-handled, never `applyIntervention`-dispatched — route through the identical `Action::"intervene"` adjudication before composing any run-state mutation, extending **I-004-19 to T3.4 / T3.5** the same deliberate cross-cutting way the Tier-5 audit extended D-004-2's mandatory comparand to them: both tasks' Behavior lines adjudicate-then-guard, their Verifies-invariant markers carry the claim, and the local + remote acceptance arms land with the restoring delta (reciprocal: Plan-012 T2.7's matrix names the pause/resume arms, consumed never authored). New normative spec behavior on an `approved` plan is the same shape that flipped Plan-006 (2026-07-29, 2026-08-02), Plan-024 (2026-08-02), and Plan-008 (2026-08-03), so the paired-spec box re-opens and Status flips `review`; the restoring targeted readiness-audit delta audits the amendment, re-checks the box, and restores `approved`. **The re-opened gate is plan-wide:** preflight Gate 7 scans the plan-template governance trio plus the header Status row, so it halts every phase, Phase 3 included — it is **not** the phase-scoped Gate 5 `precondition_box_checked` mechanism, which covers only the three named scoped boxes (driver input-ask park leg, Plan-006 epoch carrier, Plan-010 partial-restore enumerations), none of which this amendment touches. Dispatch layering is tightened, never loosened: every pre-existing Gate-5 hold stands unchanged beneath the new plan-wide hold.

**2026-08-08 — Cross-user run-control authorization targeted readiness-audit delta (PR #299; §6 node NS-49): Status restored `review → approved` on this plan and Spec-004; the §Preconditions paired-spec box re-checked.** The delta audited the 2026-08-03 amendment against primary sources plus an adversarial cross-surface coherence pass — one minor finding (amended in this swap) beside the scheduled acceptance-arm delivery; zero critical / zero major. **The delivery:** [Spec-004 §Acceptance Criteria](../specs/004-queue-steer-pause-resume.md#acceptance-criteria) gains the local + remote authorization arms the 2026-08-03 bullet above scheduled — the local arm binds authorship-blind write-access admission (`owner` / `collaborator` admitted on another participant's locally-hosted run with `initiatorId`, creating participant, and agent owner never read as authorization inputs; `runtime contributor` / `viewer` denied, a denied intervention settling `rejected` while a denied `run.pause` / `run.resume` request is rejected untouched, composing no run-state mutation — the orchestration verbs settle no `InterventionResult`, exactly I-004-19's T3.4 / T3.5 extension), and the remote arm binds the additive node-owner `tool_execution` leg with `rejected`-never-`applied`/`degraded` on absent-or-denied approval and the unobtainable-approval-is-absent fail-closed reading (reciprocal: Plan-012 T2.7's cross-user intervention matrix names the test-side arms, consumed never authored). **The finding:** [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) positively enumerates Cedar's controlling inputs but carried no registration for the amendment's hosting-node request-context input — the authorization model's only controlling context input absent from the doc that owns the model; a run-control resource-context bullet now registers it (daemon-resolved, never client-supplied; Spec-004 contract text, Spec-012 rule owner). **Consumer-readiness disposition (recorded, no gate change):** the remote arm's producer chain post-dates this plan's tier — Plan-012's approval pipeline is Tier 6 and Plan-027's cross-node dispatch surface is Tier 9 — so at this plan's Tier 5 no run can be non-locally hosted and no node-owner approval can exist; the arm ships fail-closed by construction (absent approval ⇒ `rejected`), needs no §Preconditions box and no Phase-2 `preconditions:` entry — the machine gate that would hold T2.5, the arm's earliest carrier (unlike the partial-restore-enumerations prerequisite above, whose box plus Phase-3 Gate-5 entry hold T3.13 because a wire shape there depends on the later producer; here the leg consumes an approval outcome, not a schema) — and its live cross-node exercise rides Plan-012 T2.7's matrix at that plan's tier. **Structure re-verifications, all clean:** the amendment changes the adjudication's inputs, not its mechanism — I-004-8's pre-existing Tier-5-audited structure (Cedar per ADR-012 as the engine selection; verified PASETO `sub` as principal) stands, and the CP-012-5 / CP-012-11 walk confirms no Plan-012-owned evaluator symbol is consumed on this path; the Spec-004 Cedar shape restates [vision.md §The Collaboration Model](../vision.md)'s two ratified sentences faithfully (byte-checked); rule ownership resolves to [Spec-012 §Required Behavior](../specs/012-approvals-permissions-and-trust-boundaries.md#required-behavior) (amended in the same 2026-08-03 change, I-012-23 — Spec-012's own restoring delta is a separate vehicle); the [Security Architecture §Permission Matrix](../architecture/security-architecture.md#permission-matrix-task-54) run-control row, [§Inter-Node Trust Boundaries](../architecture/security-architecture.md#inter-node-trust-boundaries), [Spec-024](../specs/024-cross-node-dispatch-and-approval.md)'s every-cross-node-hop `tool_execution` rule, and the [Queue And Intervention Model](../domain/queue-and-intervention-model.md) all mirror the rule or stay coherently silent (no stale authorship-scoped authorization claim anywhere in the walked set); and zero inbound `:NNN` line cites target Spec-004 or this plan, so the acceptance-arm insertion and this bullet move no citer. Promotion ran the runbook's [§Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate) four criteria — Depends On closure (Spec-004's row names three canonical domain/architecture docs plus Spec-001 `approved`; Spec-012 is not among them, so no deadlock with its separate delta), no blocking open questions (§Resolved Questions records none), doc-first attestation (this plan's Shipment Manifest is empty and no code cites the amended rows), and this gate citation — and the [§Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#status-promotion-gate) with the closing PR cited in lieu of a tier git-tag (the R7 durable-citation refinement, the PR #228 precedent above).

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated
