# Plan-009: Repo Attachment And Workspace Binding

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `009` |
| **Slug** | `repo-attachment-and-workspace-binding` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-009: Repo Attachment And Workspace Binding](../specs/009-repo-attachment-and-workspace-binding.md) |
| **Required ADRs** | [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-006](../decisions/006-worktree-first-execution-mode.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md) |
| **Dependencies** | [Plan-001](./001-shared-session-core.md) (daemon SQLite substrate — migration-runner guarded-block extension seam, `openDatabase` handle factory, `SessionService` session-existence reads; `SessionIdSchema` contract import; shipped Tier 1), [Plan-003](./003-runtime-node-attach.md) (`NodeIdSchema` contract import; renderer/SDK precedents; shipped Tier 3), [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (Tier 4 — `repo.*`/`workspace.*` `session_lifecycle` taxonomy; Plan-009 is the emitter of the 6 repo/workspace events; consumes `EventLogService.append` + the T1.2 type registry, build-order precondition for Phase 2), [Plan-007](./007-local-ipc-and-daemon-control.md)-partial (Tier 1 — JSON-RPC registry substrate; the `repo.*` namespace registers at Tier 6 per CP-009-5), [Plan-023](./023-desktop-shell-and-renderer.md)-partial (Tier 1 — renderer substrate + `window.sidekicks` bridge stub; live IPC dispatch lands at Tier 8 per CP-009-6) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |

## Goal

Implement durable RepoMount attachment and explicit workspace binding for session execution.

## Scope

This plan covers repo root resolution, RepoMount persistence, workspace creation, execution-mode capability exposure, and workspace health projection.

## Non-Goals

- Worktree lifecycle
- PR preparation
- Rich diff UI

## Preconditions

- [x] Paired spec is approved
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [x] **Plan-readiness audit complete per [runbook](../operations/plan-implementation-readiness-audit-runbook.md)** — Tier-6 audit (2026-06-10): 52 findings resolved; `## Implementation Phase Sequence` + per-Phase `#### Tasks` authored; §Invariants, §Cross-Plan Obligations, and §Ratified Design Decisions added; Spec-009 gains §Local Trust Envelope / §Repo Mount Health / §Detach Semantics (V1 definitions); companion amendments in `api-payload-contracts.md` §Plan-009 + §Repo Method-Name Registry, `error-contracts.md` §Repo, `local-sqlite-schema.md` §Workspace and Git Tables, and `cross-plan-dependencies.md` §2/§3. **Re-opened 2026-08-16, scoped to the repo-identity amendment** (Spec-009's new `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`, the `Spec-009 §Local Trust Envelope (V1 Definition)` registered-working-tree admission rule, the two new acceptance criteria, and the I-009-15 / I-009-16 growth this plan takes on): the spec flipped `approved → review` for the amendment's review window per the audit runbook's spec-amendment rule, and this plan flips with it under the runbook's plan behavior-change row. **Delivered 2026-08-16** by that amendment's targeted readiness-audit delta, landing in the SAME diff (the Plan-006 PR #278 flip-and-restore-in-one-swap shape): the delta audits the named scope against the readiness dimensions, authors `### Phase 2B — Repo Identity Keying And Resolution Hardening` with per-task `Files:` / `Spec coverage:` / `Verifies invariant:` rows, mints I-009-15 + I-009-16 and amends I-009-1 + I-009-3 onto the new contract, mints the scoped carrier box below, and converts the 19 inbound `Spec-009 line NNN` cites whose targets shifted into `§`-anchor + content form — no surface of the growth is left unaudited. The PR's Codex review round (2026-08-17) then hardened four surfaces of the same growth inside the same review window — the two-form `WorkspaceBind.directory` wire rule (T-009-2B-3), the derive-then-verify main-checkout rule with its `--separate-git-dir` refusal arm and AC5's fifth refusal shape (T-009-2B-1), the descriptor-anchored pointer-file screen (T-009-2B-2), and run-boundary re-verification of user-supplied roots (T-009-2B-4) — and this Delivered record covers those repairs too. A second review round (2026-08-17, same window) then inverted the resolution arm order to derivation-first — closing the planted direct-to-common pointer-file spoof the own-toplevel-first order accepted, with the submodule `core.worktree` candidate added — added the bounded at-path-and-strictly-between unusable-metadata screen, persisted the derived identity (`repo_mounts.metadata.commonDir`) with the bind/run match, reconciled `Spec-009 §Default Behavior`'s default-workspace rooting sentence, and registered the CP-009-9 branch-mode re-rooting with its `workspaces.metadata.boundRoot` carrier (Spec-010/Plan-010 flip-and-restored `approved` in the same swap); this Delivered record covers those repairs too. Status restored `review → approved` on this plan and Spec-009 in the same swap. Restoration clears the audit and Status gates only: lane-1 dispatch still rides tier order (Tier 6), this plan's §Preconditions, and each phase's Gate-5 `preconditions:` chain.
- [ ] **Repo-identity resolver amendment landed (repo-identity targeted readiness-audit delta, 2026-08-16):** the shipped resolver keys repo identity on the enclosing working tree's own top level, so N linked worktrees of one repository resolve as N repositories — contradicting `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`, the amended `Spec-009 §Required Behavior` canonical-root bullet, and acceptance criteria 4 and 5. `### Phase 2B — Repo Identity Keying And Resolution Hardening` is the owning code lane; this box checks when Phase 2B merges. Born unchecked per the NS-54 carrier-box class. **Phases 3 and 4 do not dispatch while it is unchecked** — each declares the phase-scoped `precondition_box_checked` entry (preflight Gate 5; Gate 7's governance scan deliberately covers only the plan-template trio, so a scoped box needs the phase-level entry), because Phase 3 is the wire surface and Phase 4 the renderer surface over an identity the daemon would otherwise still be computing wrong, and the Rollout-step-2 SDK/CLI verification of acceptance criterion 4 is unrunnable until Phase 2B lands. Phase 2B itself is reachable only by explicit supplement-label dispatch (`preflight.mjs docs/plans/009-repo-attachment-and-workspace-binding.md 2B`) — the no-argument walk is numeric-only and never selects a supplement. The box's contradiction list includes the CP-009-9 branch-mode leg: until Phase 2B lands, `ExecutionRootService.#bindBranchMode` returns `mount.canonical_root` — lawful under the shipped per-directory identity, the I-009-15 relocation bug under the amended one — so the identity swap and the branch-arm re-rooting land atomically in Phase 2B or not at all.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Invariants

- **I-009-1 — Canonical-root fidelity.** Every persisted RepoMount records the symlink-resolved absolute canonical repository root — the MAIN checkout's working-tree root, identified by the repository's git common directory (I-009-15) — and a user-entered path is never stored as the canonical root without resolution. **Why load-bearing.** `Spec-009 §Required Behavior` + line 121 (pitfall); path-canonicalization bugs leak outside the trust envelope (§Risks And Blockers) and corrupt every downstream workspace/worktree root derivation (Plan-010/Plan-011). **Verification.** T1.5 unit tests (subdirectory input resolves to repo root; symlinked input resolves to realpath); T-009-2B-1 unit tests (a linked worktree's input resolves to the main-checkout root, not to the worktree's own top level); T2.3/T2.6 attach integration tests assert the persisted `canonical_root` carries the resolver output, not the request input.
- **I-009-2 — Explicit resolution failure.** Canonical-root resolution failure aborts attach with typed `repo.root_resolution_failed`; the daemon never guesses a root or silently substitutes the user-entered path. **Why load-bearing.** `Spec-009 §Fallback Behavior`; silent substitution converts a user typo into a durable wrong-root mount that every later run executes against. **Verification.** T1.5 failure-path tests; T1.4 typed-error test; T2.3 attach integration test (failed resolution persists no `repo_mounts` row).
- **I-009-3 — Trust-envelope containment.** A workspace execution root always resolves — post-symlink, platform-normalized — within the canonical root of a repo mount attached to the same session, OR within one of that repository's own git-registered working trees admitted under the same bidirectional-link and physical-containment screens, OR within a daemon-provisioned execution root admitted by provenance (an arm the registration screens never narrow); traversal or symlink escape is rejected with typed `repo.outside_trust_envelope`, and membership of a user-supplied root is re-verified at the run boundary — not only at bind — with loss of membership blocking the run and transitioning the workspace to `stale`. **Why load-bearing.** `Spec-009 §Required Behavior` with the §Local Trust Envelope (V1 Definition) at `Spec-009 §Local Trust Envelope (V1 Definition)`; this is the security boundary between the session and the rest of the operator's filesystem, and once identity keys on the common directory (I-009-15) a linked worktree is no longer path-contained in its own repository's canonical root, so containment alone would reject binds to working trees the repository demonstrably owns. **Verification.** T1.6 adversarial unit tests (`..` traversal, symlink escape, path-prefix collision, absolute-path escape); T-009-2B-3 admission tests (a registered working tree admitted; an unregistered sibling directory, a one-way pointer, and a registration whose git directory is not physically contained by the claimed common directory each rejected); T-009-2B-4 run-boundary tests (a worktree unregistered after bind refuses at the next `assertWritable` and lands `stale`; a daemon-provisioned root under the same loss stays writable); T2.4 bind integration test.
- **I-009-4 — Honest non-git classification.** A non-git path is classified `vcsType: "none"` at resolution time and is never presented as a git mount; conversely `"none"` requires git's positive not-a-repository verdict UNCONTRADICTED by a visible `.git` entry at the supplied path — damaged or unreadable repository metadata refuses `"vcs_error"`, never a plain-directory mount (PR #250 round 4). **Why load-bearing.** `Spec-009 §Required Behavior` + line 56 + AC line 129; misclassification makes the capability projection lie about git-backed modes (`Spec-009 §Fallback Behavior`). **Verification.** T1.5 classification tests; T2.5 capability-read tests inherit.
- **I-009-5 — Repo-mount rows persist resolved identity plus provenance.** Every `repo_mounts` row stores the user-entered attach path (`local_path`, provenance), the resolver-produced canonical root (`canonical_root`), the owning `node_id`, and a lifecycle state from the closed set; trust-envelope enforcement and node-ownership routing key off `canonical_root`, never `local_path`. **Why load-bearing.** `Spec-009 §Required Behavior` ("not only the user-entered path" — both values are meaningful), line 73 (canonical root + owner node + lifecycle state), line 117 (node ownership). **Verification.** T2.1 migration test (NOT NULL `node_id`/`canonical_root`, state CHECK); T2.3 service test (attach with a subdirectory input stores the resolved root in `canonical_root` alongside the entered subpath in `local_path`).
- **I-009-6 — Workspace identity is stable across execution-mode switches.** No code path creates a new `workspaces` row for a mode switch; re-provisioning updates `execution_mode`/`fs_root` in place on the same `id`. **Why load-bearing.** `Spec-009 §Execution Mode Transitions` ("This is NOT a new workspace"); downstream run/diff attribution keys off the stable workspace id. **Verification.** T2.4 test — full beginReprovision/completeReprovision cycle retains `id` and row count.
- **I-009-7 — An unavailable execution root is observable as `stale` on every daemon read surface, and the write gate refuses stale workspaces.** **Why load-bearing.** `Spec-009 §Fallback Behavior`: stale transition + "new write runs must be blocked until repair." **Verification.** T2.5 test (probe-fail → health projection reports stale and reports the transition owed); T2.4 test (`markStale` persists the transition the projection derives; `assertWritable` throws `workspace.stale`).
- **I-009-8 — Capability projection never silently substitutes a mode.** Every mode absent from `availableModes` appears in `restrictions` with a reason string. **Why load-bearing.** `Spec-009 §Fallback Behavior` explicit-gap mandate. **Verification.** T2.5 test — `vcs_type 'none'` mount yields `availableModes ['read-only']` + 3 populated restriction entries.
- **I-009-9 — Every repo-mount/workspace state transition appends its matching Spec-006 `session_lifecycle` event exactly once.** **Why load-bearing.** The durable timeline is the product's audit surface; emission at the transition site (single seam, T2.2) prevents drift between rows and events. **Verification.** T2.2/T2.3/T2.4 tests assert the emitted event type sequence per transition (attach → `repo.attached` + `workspace.ready`; stale → `workspace.stale`; detach → `repo.detached` + one `workspace.archived` per dependent — mount event first: every cascade row write rides the `repo.detached` append's transaction, and an archive event may only follow the commit that made it true (`Spec-009 §Detach Semantics (V1 Definition)`)).
- **I-009-10 — Every `repo.*` method validates daemon-side before dispatch.** Each registered method carries its canonical contracts Zod request AND response schema, so the substrate validates inbound params before the handler runs and outbound results before they hit the wire; client-only workspace binding is structurally impossible. **Why load-bearing.** `Spec-009 §Pitfalls To Avoid` names "Allowing client-only workspace binding with no daemon validation" as the failure mode; the substrate validates only what is registered with it — a handler registered with permissive schemas silently reopens the pitfall. **Verification.** T3.4 assertions (3) and (5): schema-invalid params are rejected with the deps spy at zero calls; registration asserted against the canonical contracts schema objects.
- **I-009-11 — The SDK marshals; it never derives daemon-owned repo/workspace state.** `repoClient` caches nothing and computes no health, git metadata, canonical root, or capability values client-side; daemon-owned projections ride through verbatim. **Why load-bearing.** `Spec-009 §State And Data Implications` places repo health and git metadata in daemon-owned projection state "rather than client cache"; a deriving or caching SDK forks the source of truth and breaks the stale-workspace blocking contract downstream. Mirrors the `runtimeNodeClient` read-side never-mask stance. **Verification.** T3.5 pass-through assertions: every response field equals the daemon-supplied payload; no transformation, no memoization between calls.
- **I-009-12 — Renderer mutations are explicit actions.** The `repo-attach/` renderer views MUST NOT invoke `repo.attach` or `repo.workspaceBind` as a mount/render side effect; both fire only from an explicit user click. The only mount-triggered wire calls in the subtree are reads (`repo.workspaceList`, `repo.executionModeCapabilitiesRead`). **Why load-bearing.** `Spec-009 §Required Behavior` (attach is an explicit action) + line 43 (binding is explicit) + line 122 (no auto-writing before a mode is explicitly chosen): an auto-fired attach/bind couples a trust-bearing action to navigation — the exact wrong-UX shape the shipped AttachFlow idle-state precedent rejects. **Verification.** T4.4 component tests: the mock-bridge `daemon.call` spy is NOT invoked on render of `RepoAttachView` / `WorkspaceBindView` mutation paths, and is invoked exactly once per mutation after the explicit click.
- **I-009-13 — Stale workspaces are rendered, labeled, never hidden.** The workspace list renders every workspace the read returns; a `stale` workspace stays in the rendered set with its `stale` state and a write-runs-blocked-until-repair label. No `.filter(...)` drops a row by `state`, and the view computes no client-side health scalar (health is daemon-owned projection state). **Why load-bearing.** `Spec-009 §Fallback Behavior` (stale transition + write-block until repair) + line 67 (WorkspaceList exposes health) + line 75 (daemon-owned projection, not client cache) + §Risks And Blockers (stale handling "needs clear UX and blocking behavior"). The render-side never-mask posture mirrors I-003-1 admit-not-eject on the shipped NodeRoster. **Verification.** T4.4 workspace-list test: fixture containing a `stale` workspace asserts the row renders with `data-workspace-state="stale"` and the write-blocked label, alongside `ready` peers.
- **I-009-14 — Execution-mode capabilities render verbatim; the renderer never substitutes a mode.** The bind/capabilities surface offers exactly the daemon-returned `availableModes` (with `restrictions` reasons rendered verbatim) and MUST NOT offer, default to, or substitute any execution mode the daemon did not return. **Why load-bearing.** `Spec-009 §Fallback Behavior` (expose the capability gap explicitly "rather than silently substituting a different mode") + line 44 (canonical four-mode taxonomy, ADR-006) + line 123 (no client-only binding decisions): the daemon is the capability authority; a renderer-synthesized mode option is precisely the silent substitution the spec forbids. **Verification.** T4.4 bind-view test: fixture `availableModes: ["read-only", "branch"]` with a `worktree` restriction reason asserts exactly two mode options render, the restriction reason text renders verbatim, and no `worktree` / `ephemeral clone` option exists in the DOM.
- **I-009-15 — Repo identity keys on the git common directory; execution roots stay per-working-tree.** Resolution identifies a repository by its git COMMON directory and returns the MAIN checkout's working-tree root as the canonical root, so every working tree of one repository — the main checkout and each linked worktree — resolves to one identical `repo_mounts.canonical_root`, while `workspaces.fs_root` continues to name the per-working-tree path actually bound or provisioned. **Why load-bearing.** `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`; identity is the key for the active-mount uniqueness index and for every daemon cache, filesystem watch, remote fetch, and throttle window scoped to a repository — keying those on the enclosing working tree instead silently multiplies them per worktree, and the plausible over-correction (returning the common directory itself as the workspace path) would root every worktree's execution at the main checkout, which is a different and worse bug. **Verification.** T-009-2B-1 unit tests over real multi-worktree git fixtures: attach from the main checkout and from each of two linked worktrees yields one identical canonical root, and each resolution simultaneously reports that working tree's own distinct root; T-009-2B-4 acceptance test asserts the second worktree attach is refused `repo.already_attached` against the first mount while a bind rooted at that worktree still succeeds with its own `fs_root`.
- **I-009-16 — Resolution refuses rather than misattributes.** A supplied path that does not exist, git metadata that is present but unusable, a linked-worktree link that is not bidirectional or whose registration is not physically contained by the common directory it claims, and a git pointer file that is not a regular file are each refused with a typed `RepoRootResolutionError`; none of them continues the ancestor scan, and none is reclassified as the plain-directory fallback. **Why load-bearing.** `Spec-009 §Fallback Behavior` + `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`; walking up lexically from a path that does not exist attributes it to whatever repository contains its parent (a dotfiles repository at the operator's home directory makes that wrong for nearly every path beneath it), continuing past unusable metadata attributes a damaged repository to its parent, a one-way pointer file is attacker-writable and would let a planted `.git` claim any repository, and an unscreened non-regular pointer (a FIFO named `commondir`) wedges the scan indefinitely. **Verification.** T-009-2B-2 unit tests, one refusal arm per clause plus a negative control per arm proving the honest shape still resolves; the FIFO arm is POSIX-gated with a seam-driven twin that runs on every platform.

## Cross-Plan Obligations

- **CP-009-1 — Plan-010 imports Plan-009's canonical repo/workspace contract types.** `packages/contracts/src/repo.ts` (Phase 1 T1.1) owns the canonical Zod schemas for `ExecutionMode`, `WorkspaceState`, `RepoMountState`, `VcsType`, `RepoMountHealth`, the branded `RepoMountId` / `WorkspaceId`, and the state-parameterized lifecycle-payload factory `buildRepoWorkspaceLifecyclePayloadSchema` + its `RepoWorkspaceLifecyclePayloadOf` return alias (PR #250 round 4 — Plan-010 instantiates it over `WorktreeStateSchema` in `worktree.ts` for the `worktree.*` payload per CP-010-5, keeping payload schemas in the emitter's domain file with no third `state` arm in `repo.ts`). Plan-010's `packages/contracts/src/worktree.ts` (renamed from `execution-mode.ts` per Plan-010 D-010-1) (`Plan-010 §Target Areas`) and worktree contracts MUST import these — never redefine — because Plan-009 ships first within Tier 6 ([cross-plan dependencies §5](../architecture/cross-plan-dependencies.md#5-canonical-build-order): "Plan-010 needs Plan-009"). **Direction:** Provide to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md); reciprocal return-cite owed on Plan-010 at its Tier-6 audit. **Tasks:** T1.1.
- **CP-009-2 — Plan-010 mode-switch writes route through Plan-009 workspace primitives.** Plan-009's `WorkspaceService` provides `beginReprovision(workspaceId, targetMode)` (state → `provisioning`; emits `workspace.provisioning`), `completeReprovision(workspaceId, fsRoot)` (state → `ready`; updates `execution_mode`/`fs_root` in place; emits `workspace.ready`), and `failReprovision(workspaceId, errorDetail)` (state → `stale`; writes `metadata.lastError`; emits `workspace.stale`). Plan-010's `execution-mode-service.ts` MUST drive the Spec-009 `ready -> provisioning -> ready` progression (`Spec-009 §Execution Mode Transitions`) through these primitives, never via raw UPDATEs on the Plan-009-owned table. **Why load-bearing.** Preserves one-writer state-machine discipline on `workspaces` (workspace id stable across switches; only the execution root changes) and keeps event emission exactly-once at the transition site (I-009-9). **Direction:** Provide to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md); reciprocal return-cite owed on Plan-010. **Tasks:** T2.4.
- **CP-009-3 — Stale workspaces block write-run execution-root handoff.** Plan-009 provides `WorkspaceService.assertWritable(workspaceId)` throwing typed `workspace.stale` (error-contracts.md §Workspace) when `state = 'stale'`. Plan-010's execution-root-prepare path MUST call it before preparing an execution root for any writable mode that finds the workspace outside the open reprovision bracket — a prepare that finds `provisioning` is the bracket's own provisioner and lawfully skips the gate, which raises its invariant error for `provisioning` by design (the same scoping as Plan-010 I-010-12 / CP-010-3; PR #297); the same gate guards Plan-009's own writable-bind path. **Why load-bearing.** `Spec-009 §Fallback Behavior`: "new write runs must be blocked until repair" — the enforcement consumer is otherwise unnamed in any downstream doc. **Direction:** Provide to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md); reciprocal return-cite owed on Plan-010 (its spec/plan currently never mention stale). **Tasks:** T2.4.
- **CP-009-4 — Repo/workspace event payload schema + union registration with Plan-006.** Plan-009 Phase 1 authors `RepoWorkspaceLifecyclePayloadSchema` (`{sessionId, repoMountId?, workspaceId?, worktreeId?, state, actor?}` per `Spec-006 §Repo, Workspace, and Worktree Lifecycle (session_lifecycle)`) in `packages/contracts/src/repo.ts` (emitter-authors-payload per the Plan-003 precedent recorded in Plan-006 CP-006-5 carry-forward), and registers the 6 Plan-009-emitted types — `repo.attached`, `repo.detached`, `workspace.provisioning`, `workspace.ready`, `workspace.stale`, `workspace.archived` — into the Plan-006-owned `SessionEventSchema` union in `event.ts` as an additive-MINOR EXTEND. The 6 type names + `session_lifecycle` category mapping already ship in Plan-006 T1.2's `SESSION_EVENT_CATEGORY_BY_TYPE`. Phase 2 consumes the schema at the emission boundary (`.parse()` seam). **Why load-bearing.** Without it the emitter persists unvalidated payloads or Plan-009 re-authors Plan-006-owned union surface without a contract. **Direction:** Register with [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (reciprocal already exists — Plan-006 names Plan-009 as emitter). **Tasks:** T1.1, T2.2.
- **CP-009-5 — `repo.*` JSON-RPC method-name namespace registered under Plan-007's `MethodRegistry`.** Six methods — `repo.attach`, `repo.mountRead`, `repo.workspaceBind`, `repo.executionModeCapabilitiesRead`, `repo.workspaceList`, `repo.detach` — registered under the Plan-007-partial registry per the §5 substrate-vs-namespace carve-out (Plan-002 `presence.*` precedent NS-26); wire contract ratified in `api-payload-contracts.md` §Repo Method-Name Registry (Tier 6) (D-009-1). Plan-007 owns the registry surface, not this namespace's handlers. Registration is gated on BL-142 (deployed registry-regex conformance to the Tier-1 canonical). **Direction:** Register with [Plan-007](./007-local-ipc-and-daemon-control.md). **Tasks:** T3.1, T3.2, T3.4, T3.7.
- **CP-009-6 — Plan-023 owns the `window.sidekicks` preload bridge the Phase 4 renderer projects over.** Source: [cross-plan-dependencies.md §2](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map) renderer-ownership row (Plan-023 creates the substrate; Plan-009 extends with the `repo-attach/` views as thin bridge projections that must not bypass the bridge) and the §5 Tier 8 row (Plan-023-remainder ships the live IPC dispatch). Obligation: Plan-023's Tier 8 remainder wires the `repo.*` daemon JSON-RPC methods and the daemon event-subscription channel through the preload bridge and composes the `repo-attach/` views into the shell (App-shell composition is Plan-023-owned; Phase 4 ships exported views + barrel only, matching the shipped `session-members/` / `runtime-node-attach/` posture). Until then every bridge method throws `NotImplementedAtTier1Error` and the Phase 4 views' rejected/error branches are the production-observable paths.
- **CP-009-7 — Busy-marking primitives for run execution-root holds (Tier-6 Plan-010 walk).** Plan-009's `WorkspaceService` provides `markBusy(workspaceId, runId)` (state `ready -> busy`) and `releaseBusy(workspaceId)` (state `busy -> ready`). The transitions write the state column without a dedicated event type — the Spec-006 taxonomy registers no busy/release workspace events, and the run's own `run.*` lifecycle events carry the hold's timeline visibility (closed-registry posture, same as Plan-010 D-010-11). Plan-010's run-setup gate MUST bracket every repo-bound run's execution-root hold with these primitives (one holding run at a time in V1; `markBusy` on an already-busy workspace throws typed `workspace.busy` per error-contracts.md §Workspace), and Spec-009's detach-conflict refusal (`repo.detach_conflict` while any dependent workspace is `busy`) keys off exactly this state. **Direction:** Provide to [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-4). **Tasks:** T2.4. **Direction:** Consume from [Plan-023](./023-desktop-shell-and-renderer.md) (tracked-not-satisfied until Tier 8; reciprocal `provides` row routed to the Plan-023 Tier-8 audit, NS-21). **Tasks:** T4.1, T4.2, T4.3, T4.5.
- **CP-009-8 — Workspace envelope root for approval path-scope matching (Tier-6 Plan-012 walk).** `workspaces.fs_root` is the canonical workspace envelope root Plan-012's remembered-scope matcher resolves for path-category approvals: a remembered path scope matches by prefix containment against the run's resolved execution root (read via Plan-010's `run_execution_contexts` binding — see Plan-010 CP-010-11), and a path of unknown provenance falls back to strict per-request approval (`Spec-012 §Fallback Behavior`). Plan-012 reads `fs_root` + workspace state only — never writes Plan-009-owned rows. Within-tier ordering: Plan-012 builds after Plan-009 (this surface is a precondition). **Direction:** Provide to [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) (reciprocal CP-012-9). **Tasks:** T2.1 (the `workspaces` DDL ships `fs_root`).
- **CP-009-9 — Branch-mode execution root re-based onto the workspace's bound working tree (Plan-010-consented EXTEND).** Under common-directory keying, `repo_mounts.canonical_root` stops meaning "the tree the participant bound", so Plan-010's shipped `ExecutionRootService.#bindBranchMode` — which returns it as the branch-mode root, correct under the pre-amendment identity — would relocate every linked-worktree workspace's branch-mode runs to the main checkout (the I-009-15 relocation bug class). T-009-2B-4 re-roots the branch arm at `workspaces.metadata.boundRoot` — the admitted bind origin, recorded at workspace creation and never cleared by `beginReprovision` (which releases `fs_root` on the way into `provisioning`, exactly why the origin needs its own durable carrier) — falling back to `canonical_root` when the key is absent so pre-amendment rows keep byte-identical behavior, inside `packages/runtime-daemon/src/workspace/execution-root-service.ts`, a Plan-010-extended file per the [§2 ownership row](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map); the EXTEND is consented by Plan-010's reciprocal CP-010-14, and `Spec-010 §Turn-Boundary Snapshots`' branch-mode sentence is corrected in the same swap (Spec-010/Plan-010 flip-and-restored `approved`, PR #340). The leg ships INSIDE Phase 2B — atomic with the identity change, because landing either half alone re-opens the relocation — so the Preconditions carrier box holds it exactly as it holds the rest of Phase 2B. **Direction:** Consume consent from [Plan-010](./010-worktree-lifecycle-and-execution-modes.md) (reciprocal CP-010-14). **Tasks:** T-009-2B-4.

## Ratified Design Decisions

Decisions below were adjudicated by the Tier-6 plan-readiness audit (2026-06-10) and ratified at its swap, per the Plan-004 D-004-3 precedent. Each is binding contract surface for the Tasks blocks.

- **D-009-1 — `repo.*` method-name strings.** Six wire strings ratified in `api-payload-contracts.md` §Repo Method-Name Registry (Tier 6): `repo.attach` (mutation), `repo.mountRead` (query), `repo.workspaceBind` (mutation), `repo.executionModeCapabilitiesRead` (query), `repo.workspaceList` (query), `repo.detach` (mutation). Tail style is dotted-camelCase per the Tier-1 `METHOD_NAME_FORMAT`; `repo.executionModeCapabilitiesRead` keeps interface-name symmetry with `WorkspaceExecutionModeCapabilitiesRead*` (the shorter `repo.workspaceCapabilities` variant was rejected — it minted a third name diverging from both interface and schema names). Method strings (imperative) are disjoint-by-form from the Spec-006 durable event names (past participle: `repo.attached`).
- **D-009-2 — `RepoMountHealth` is a derived projection object, never persisted.** Shape: `{ status: "healthy" | "unreachable"; checkedAt: string }` (ISO-8601). Merges the audit's competing proposals: a render-ready discriminant (`status`) plus probe provenance (`checkedAt`). No `unknown` member — the on-read probe floor (D-009-5) means every read carries a fresh verdict. `unreachable` (not `stale`) avoids overloading the workspace-state vocabulary across axes. No `health` column lands in `repo_mounts` — `Spec-009 §State And Data Implications` pins health to projection state, not row state. Semantics in `Spec-009 §Repo Mount Health (V1 Definition)`.
- **D-009-3 — `repo.*` error vocabulary.** error-contracts.md gains §Repo: `repo.not_found` (404), `repo.root_resolution_failed` (422), `repo.outside_trust_envelope` (403), `repo.already_attached` (409), `repo.detach_conflict` (409); §Workspace gains `workspace.stale` (409, the write-gate code). Adjudication: `repo.root_resolution_failed` over the competing `repo.canonical_root_unresolvable` (two-of-three subagent convergence + status 422); `repo.outside_trust_envelope` over `repo.path_outside_trust_envelope` (two-of-three convergence); `repo.not_found` over `repo.mount_not_found` (matches the `session.not_found` / `workspace.not_found` registry precedent).
- **D-009-4 — Mount-first single funnel; `VcsType = "git" | "none"`.** Non-git directories ride the SAME funnel as git repositories: `repo.attach` accepts any local path; a non-git path yields a RepoMount with `vcsType: "none"` and reduced capabilities. `WorkspaceBindRequest` always binds by `repoMountId` — `Spec-009 §Interfaces And Contracts`'s "or directory root" arm is satisfied by the plain-directory mount, not by a mount-less bind path (there is no mount-less workspace; `workspaces.repo_mount_id` is NOT NULL).
- **D-009-5 — V1 capability matrix is static by `vcs_type`; health detection is two-layer.** Matrix: `'git'` → `availableModes` = all four modes, `defaultMode 'worktree'` (ADR-006 — the default WRITABLE coding mode; the fresh-workspace initial posture is always `read-only` per `Spec-009 §Default Behavior` and is not what `defaultMode` reports); `'none'` → `['read-only']`, `defaultMode 'read-only'`, `restrictions` populated with a reason per excluded mode. Dynamic per-repo worktree-unavailability (ADR-006 accepted trade-off) is NOT probed at capability-read time in V1: it surfaces at provisioning time through the `Spec-009 §Execution Mode Transitions` failure path (Plan-010's surface); a later static probe extends `restrictions` additively without contract change. Health detection: (1) on-read probe floor — every health-reporting read surface and every write gate synchronously probes filesystem availability before answering, persisting the `stale` workspace transition at probe time (zero scheduler dependency); (2) background refresh (`Spec-009 §Default Behavior`) via a daemon idle-scheduler tick re-probing attached mounts, default interval 60s (configurable), mirroring the Plan-006 compactor's idle-scheduler precedent — the scheduler wiring may land with Phase 3 if the seam is not exposed at Phase-2 time; the on-read floor keeps `Spec-009 §Fallback Behavior` satisfied either way.
- **D-009-6 — Detach semantics (V1).** Ratified in `Spec-009 §Detach Semantics (V1 Definition)`: refuse with `repo.detach_conflict` while any dependent workspace is `busy` (no force-detach in V1); otherwise transition the mount to `detached` and archive all dependent workspaces, emitting `repo.detached` + one `workspace.archived` each; `detached` is terminal for the row — re-attach creates a new mount row; V1 detach is SDK/CLI-surfaced only (no renderer detach view). Contract shapes: `RepoDetachRequest { repoMountId }` / `RepoDetachResponse { repoMountId, state, archivedWorkspaceIds }` in api-payload-contracts.md §Plan-009.
- **D-009-7 — `repo_mounts` persistence model: provenance + canonical columns; active-root uniqueness; required default workspace.** `local_path` keeps the user-entered attach path (provenance; `Spec-009 §Required Behavior` "not only the user-entered path" implies both are known); a NEW `canonical_root` column carries the resolver output and is the key for trust-envelope checks and dedupe. Partial unique index `idx_repo_mounts_active_root ON repo_mounts(session_id, node_id, canonical_root) WHERE state = 'attached'` — uniqueness binds the CANONICAL root per owning node (two entered aliases resolving to one root on one node are one mount; the same absolute path on two different nodes is two distinct node-local filesystems — `Spec-009 §State And Data Implications` — and both attach), detached rows stay re-attachable as new rows. Additional columns: `node_id` (owner; `Spec-009 §State And Data Implications`), `updated_at` (lifecycle-mutation timestamp, symmetric with `workspaces`). `workspaces` gains `metadata TEXT NOT NULL DEFAULT '{}'` (`lastError` key per `Spec-009 §Execution Mode Transitions`) and its `execution_mode` DDL default flips to `'read-only'` (`Spec-009 §Default Behavior` initial posture; `'worktree'` is the default writable RUN mode per ADR-006, not the row default). `RepoAttachResponse.defaultWorkspaceId` is REQUIRED — attach unconditionally creates the default read-only workspace for git and non-git mounts alike (`Spec-009 §Default Behavior` + line 81).

## Target Areas

- `packages/contracts/src/repo.ts`
- `packages/runtime-daemon/src/workspace/repo-root-resolver.ts`
- `packages/runtime-daemon/src/workspace/git-repository-layout.ts` (Phase 2B — the single routed reader for git's on-disk layout: pointer-file stat screen + byte ceiling, git-dir absent/unusable classification, bidirectional worktree-registration verification, registered-working-tree enumeration)
- `packages/runtime-daemon/src/workspace/trust-envelope.ts`
- `packages/runtime-daemon/src/workspace/repo-errors.ts`
- `packages/runtime-daemon/src/workspace/repo-mount-service.ts`
- `packages/runtime-daemon/src/workspace/workspace-service.ts`
- `packages/runtime-daemon/src/workspace/workspace-projector.ts`
- `packages/runtime-daemon/src/workspace/workspace-event-emitter.ts`
- `packages/runtime-daemon/src/migrations/0010-repo-workspaces.ts` (version resolved from the `0NNN` next-free-at-PR-open placeholder by the 2026-08-04 §Notes true-up)
- `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — guarded block per the runner's documented extension contract)
- `packages/runtime-daemon/src/ipc/handlers/repo-attach.ts`, `repo-mount-read.ts`, `repo-workspace-bind.ts`, `repo-execution-mode-capabilities-read.ts`, `repo-workspace-list.ts`, `repo-detach.ts` (+ `handlers/index.ts` EXTEND)
- `packages/client-sdk/src/repoClient.ts` (+ `packages/client-sdk/src/index.ts` EXTEND)
- `apps/desktop/src/renderer/src/repo-attach/`

## Data And Storage Changes

- Add local `repo_mounts` and `workspaces` tables with health, ownership, and execution-mode capability fields.
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions — as amended by the Tier-6 audit: `repo_mounts` carries `node_id` + `canonical_root` + `updated_at`, a `vcs_type` CHECK (`'git' | 'none'`), and the `idx_repo_mounts_active_root` partial unique index; `workspaces` carries `metadata` and an `execution_mode` default of `'read-only'` (D-009-7). Mount health is a derived projection, never a column (D-009-2).

## API And Transport Changes

- Add repo attach or detach, workspace bind or list, and execution-mode capability read APIs to the client SDK.
- Wire method names are ratified in [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Repo Method-Name Registry (Tier 6) (D-009-1); error codes in [Error Contracts](../architecture/contracts/error-contracts.md) §Repo (D-009-3). All six methods ride the daemon JSON-RPC transport only — repo mounts and workspaces are node-local filesystem state (ADR-004), so there is no control-plane tRPC sibling.

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

1. Implement canonical repo root resolution and trust-boundary validation.
2. Add RepoMount and workspace persistence plus execution-mode capability projections.
3. Register the `repo.*` JSON-RPC namespace handlers under the Plan-007-partial method registry (one binder file per method under `packages/runtime-daemon/src/ipc/handlers/`, per the `presence.*` NS-26 precedent) and add the typed client SDK surface (`repoClient.ts`) covering attach, detach, mount read, workspace bind, execution-mode capability read, and workspace listing. _(Step text amended by the Tier-6 audit — the original named only "client SDK methods for attach and workspace listing", omitting the daemon half of the wire surface.)_
4. Add desktop repo attach and workspace-binding UI.

## Parallelization Notes

- RepoMount persistence and workspace projection work can proceed in parallel after contracts land.
- Desktop repo attach UI should wait on stable workspace health payloads.

## Test And Verification Plan

- Repo attach validation tests _(superseded by the per-task Tests fields in §Implementation Phase Sequence — Phase 1 T1.4-T1.6, Phase 2 T2.3)_
- Workspace stale-state tests _(Phase 2 — workspace projection; T2.4/T2.5)_
- Manual verification of attaching multiple repositories to one session _(maps Spec-009 AC line 128; automated multi-mount coverage lands in T2.6; the manual check splits per leg — the SDK/CLI leg runs at Tier 6 via Phase 3 + Rollout step 2; the desktop-renderer leg is Tier-8-gated (live bridge required) with the Plan-003 T5.4 disposition — recorded on the Phase 4 PR, owner = user at Tier 8)_
- Renderer component tests (Phase 4, T4.4): RTL + vitest renderer project against a per-suite mock `window.sidekicks` bridge — assertions map to Spec-009 AC line 127 (canonical root rendered on the resolved attach branch), AC line 128 (multi-mount workspace set renders every row), AC line 129 + line 57 (capability gap rendered verbatim; no synthesized mode), line 59 (stale row visible with write-blocked labeling), line 40 / line 43 (mutations fire only on explicit click, never on mount), plus the Tier-1 stub-throw rejected/error branch on every view.
- Phase-attributable automated coverage is specified per-task in §Implementation Phase Sequence (the canonical test inventory).

## Rollout Order

1. Ship RepoMount and workspace persistence
2. Enable attach via internal tooling or CLI
3. Enable desktop repo attach UX

Phase 2B (repo identity keying + resolution hardening, 2026-08-16 amendment) sits between steps 1 and 2 and adds no rollout step of its own: identity is corrected while the only consumer is the daemon's own service layer, before step 2 makes attach an observable SDK/CLI surface.

## Rollback Or Fallback

- Disable multi-repo attach and keep one RepoMount per session if the first rollout shows projection instability.

## Risks And Blockers

- Path canonicalization bugs can leak outside the intended trust envelope
- Stale workspace handling needs clear UX and blocking behavior
- Repo identity and execution root are two different values that both look like "the repo path", and collapsing them in either direction is a silent, plausible-looking bug: keying identity on the enclosing working tree multiplies every repository-scoped cache, watch, fetch, and throttle per worktree, while rooting execution at the identity relocates a worktree's work to the main checkout. I-009-15 keeps them separate and Phase 2B's negative controls pin both directions (2026-08-16 amendment)

## Implementation Phase Sequence

The four NUMBERED phases map 1:1 onto the four Implementation Steps. Phase 1 has no unsatisfied upstream code dependency; Phases 2-4 serialize behind their predecessors (and Phase 2 behind Plan-006 Phase 3, Tier 4). Phase 2B is a SUPPLEMENT phase added by the 2026-08-16 repo-identity amendment and maps onto no Implementation Step: it corrects identity keying in the Phase-1/Phase-2 surfaces that already shipped, sits between §Rollout Order steps 1 and 2, and gates Phases 3 and 4 through the `Repo-identity resolver amendment landed` §Preconditions box. All ratification-gated preconditions marked `[x]` below were satisfied by the Tier-6 audit swap that landed this section, except the two boxes the 2026-08-16 amendment minted unchecked on Phases 3 and 4, which the amendment's own §Preconditions entry names.

### Phase 1 — Repo contracts & canonical root resolution

**Goal.** Ship the canonical typed contracts for the six wire surfaces plus the daemon-side canonical-root resolver and trust-envelope validator that every later phase consumes.

**Preconditions.**

- [x] Trust-envelope V1 definition ratified — `Spec-009 §Local Trust Envelope (V1 Definition)` (this audit)
- [x] `RepoMountHealth` + `VcsType` shapes ratified — api-payload-contracts.md §Plan-009 (D-009-2, D-009-4)
- [x] `repo.*` error codes ratified — error-contracts.md §Repo (D-009-3)
- [x] `RepoDetach` contract ratified — `Spec-009 §Detach Semantics (V1 Definition)` + api-payload-contracts.md §Plan-009 (D-009-6)

#### Tasks

- **T1.1 — `repo.ts` contract core: branded IDs + canonical enums + lifecycle payload schema + barrel export.**
  - **Files:** `packages/contracts/src/repo.ts` (CREATE), `packages/contracts/src/index.ts` (EXTEND — add `export * from "./repo.js";` per the existing barrel convention), `packages/contracts/src/event.ts` (EXTEND — additive registration of the 6 repo/workspace types into the `SessionEventSchema` union per CP-009-4)
  - Define and export: `RepoMountId` / `WorkspaceId` branded UUID types + schemas via `brandedUuidIdSchema`; `ExecutionModeSchema` over `"read-only" | "branch" | "worktree" | "ephemeral clone"` (ADR-006 four-mode taxonomy); `WorkspaceStateSchema` over the 5 states; `RepoMountStateSchema` over the 3 states; `VcsTypeSchema` over `"git" | "none"` (D-009-4); `RepoMountHealthSchema` as the ratified `{status: "healthy" | "unreachable", checkedAt: string}` projection object (D-009-2); `RepoWorkspaceLifecyclePayloadSchema` (`{sessionId, repoMountId?, workspaceId?, worktreeId?, state, actor?}` per `Spec-006 §Repo, Workspace, and Worktree Lifecycle (session_lifecycle)`, CP-009-4); `buildRepoWorkspaceLifecyclePayloadSchema` — the state-parameterized family factory the canonical payload schema is the two-vocabulary instantiation of (`z.union([RepoMountStateSchema, WorkspaceStateSchema])`), with its return-surface generic alias `RepoWorkspaceLifecyclePayloadOf<TState>` (the factory's isolatedDeclarations-explicit `z.ZodType<…>` return names it; `RepoWorkspaceLifecyclePayload` is its two-vocabulary instantiation), both exported so Plan-010 instantiates the same shape over its own `WorktreeStateSchema` in `worktree.ts` per CP-010-5 — never a third `state` arm here, which would recreate the eager `repo.ts` → `worktree.ts` module-cycle class the §Cross-Plan Amendments relocation broke and loosen every family's accept set (PR #250 round 4). Do NOT define `WorktreeId`/`WorktreeState` (Plan-010-owned). This file is the canonical origin Plan-010 imports per CP-009-1.
  - **Tests:** `packages/contracts/src/__tests__/repo.test.ts` (CREATE) — parse-accept each enum value incl. the space-containing `"ephemeral clone"`; parse-reject a 5th execution mode, a 6th workspace state, a 4th mount state, a non-UUID id; parse-reject a `health.status` outside the 2-value union and a `health` missing `checkedAt`; assert `index.ts` re-exports (import from package root resolves `ExecutionModeSchema` — barrel-gap regression per Plan-001 [GitHub PR-#30](https://github.com/Sawmonabo/ai-sidekicks/pull/30) round-1).
  - **Acceptance:** all six Spec-009 wire surfaces can type their fields from this module alone; no enum value diverges from api-payload-contracts.md §Shared Enums or the §Plan-009 shared shapes (`VcsType`, `RepoMountHealth`).
  - **Spec coverage:** Spec-009 line 44 (canonical execution-mode taxonomy)
  - **Verifies invariant:** I-009-4 (the `VcsType` discriminator is the contract carrier of honest non-git classification)
  - **Consumes:** `brandedUuidIdSchema` ← `packages/contracts/src/internal/branded.ts` (package-internal helper; Plan-002 [GitHub PR-#102](https://github.com/Sawmonabo/ai-sidekicks/pull/102) Amendment 1, shipped Tier 2); `SessionEventSchema` union extension seam ← Plan-006 Phase 1 `packages/contracts/src/event.ts` (Tier 4 build-order; additive-MINOR EXTEND per CP-009-4)

- **T1.2 — `RepoAttach` + `RepoMountRead` + `RepoDetach` request/response schemas.**
  - **Files:** `packages/contracts/src/repo.ts` (EXTEND, same file as T1.1); `packages/contracts/src/node-id.ts` (CREATE — Plan-003-owned declaration, structural relocation per §Cross-Plan Amendments); `packages/contracts/src/runtime-node.ts` (EXTEND — re-export seam, same amendment)
  - Zod schemas + inferred types for `RepoAttachRequest` `{sessionId, localPath, nodeId}`, `RepoAttachResponse` `{repoMountId, state, vcsType, canonicalRoot, defaultWorkspaceId}` (D-009-7 — `defaultWorkspaceId` REQUIRED), `RepoMountReadRequest` `{repoMountId}`, `RepoMountReadResponse` `{id, sessionId, nodeId, localPath, canonicalRoot, vcsType, state, health, attachedAt}` (amended shape per D-009-2/D-009-7), `RepoDetachRequest` `{repoMountId}` / `RepoDetachResponse` `{repoMountId, state, archivedWorkspaceIds}` (D-009-6). `localPath` documents the user-entered-path provenance semantics; `canonicalRoot` documents that it is the resolver output, never echoed input.
  - **Tests:** parse-accept a valid fixture per shape; parse-reject `RepoAttachRequest` missing any of the three Spec-009-required fields (`localPath` / `sessionId` / `nodeId`); parse-reject a `health.status` value outside the 2-value union; assert `RepoAttachResponse.canonicalRoot` and `.defaultWorkspaceId` are required (an attach response without a resolved root or its default workspace is unrepresentable).
  - **Acceptance:** shapes match the canonical wire doc as amended; every `Spec-009 §Interfaces And Contracts` / line 64 / §Detach Semantics field requirement is representable.
  - **Spec coverage:** Spec-009 line 63 (RepoAttach accepts local path, session id, owning runtime node), Spec-009 line 64 (RepoMountRead exposes canonical root, VCS metadata, current health), Spec-009 §Detach Semantics (V1 Definition) (RepoDetach accepts a repo mount id and transitions the mount to detached)
  - **Verifies invariant:** none (contract-shape task; enforcement invariants bind on T1.5/T1.6 and Phase 2)
  - **Consumes:** `SessionIdSchema` ← `packages/contracts/src/session.ts` (Plan-001 Phase 2, shipped Tier 1); `NodeIdSchema` ← `packages/contracts/src/node-id.ts` (Plan-003, shipped Tier 3 — declaration hoisted verbatim out of `runtime-node.ts`, which still re-exports it, by Plan-009 T1.2 to break the `repo.ts` → `runtime-node.ts` → `event.ts` → `repo.ts` import cycle; Plan-003 ownership unchanged); `RepoMountHealth` / `VcsType` ← T1.1

- **T1.3 — `WorkspaceBind` + `WorkspaceExecutionModeCapabilitiesRead` + `WorkspaceList` schemas.**
  - **Files:** `packages/contracts/src/repo.ts` (EXTEND)
  - Zod schemas + inferred types for `WorkspaceBindRequest` `{repoMountId, executionMode, directory?}` (`directory` is a mount-root-relative subdirectory — its containment validation is T1.6's surface; mount-first model per D-009-4), `WorkspaceBindResponse` `{workspaceId, fsRoot?, executionMode, state}` (`fsRoot` absent while `state = 'provisioning'` — writable binds defer the root to Plan-010 provisioning completion; present for read-only binds), `WorkspaceExecutionModeCapabilitiesReadRequest` as the exactly-one union `{repoMountId?, workspaceId?}` via Zod refinement (mount-scoped reads answer "what could a workspace on this mount do"; workspace-scoped reads answer "what may THIS workspace do now" — per-workspace gap exposure, stale workspaces restrict writable modes), `WorkspaceExecutionModeCapabilitiesReadResponse` `{availableModes, defaultMode, restrictions?}` (`defaultMode` reports the daemon's default mode for the next WRITABLE coding run per D-009-5, never the fresh-workspace posture; `restrictions` is a sparse map — reason per restricted mode, unrestricted modes omitted), `WorkspaceListRequest` `{sessionId, repoMountId?}`, `WorkspaceListResponse` workspace array `{id, repoMountId, executionMode, state, fsRoot?, lastError?}` (`lastError` present iff `state = 'stale'` from a recorded failure — the `metadata.lastError` surface per D-009-7, `Spec-009 §Execution Mode Transitions`; Tier-6 Plan-010 walk). **Superseded in part, 2026-08-17 (PR #340 Codex round 1; owning lane Phase 2B / T-009-2B-3) — this row is a shipped-manifest record and is NOT rewritten:** the mount-root-relative reading of `directory` remains the shipped relative form, and the amended `Spec-009 §Local Trust Envelope (V1 Definition)` additionally reads the same string's ABSOLUTE form as naming a registered working tree of the mount's repository — mandatory where no relative spelling exists, as for a worktree on another Windows drive — with both forms admitted solely by post-resolution containment within an admitted root. No schema change; the containment surface stays T1.6's, extended by T-009-2B-3.
  - **Tests:** parse-accept one fixture per shape; parse-reject a capabilities-read request supplying both `repoMountId` and `workspaceId`, and one supplying neither (exactly-one refinement); parse-reject `WorkspaceBindRequest` with an out-of-taxonomy `executionMode`; assert `restrictions` keys are constrained to `ExecutionMode` values and the map accepts a strict subset (sparse); assert `WorkspaceListResponse` items expose `state`, optional `fsRoot`, and optional `lastError`.
  - **Acceptance:** binding is representable only with an explicit `executionMode` from the canonical set; the capabilities read is expressible for both a mount and a bound workspace; list items expose health (`state`) + binding state (`executionMode`/`fsRoot`).
  - **Spec coverage:** Spec-009 line 65 (WorkspaceBind accepts repo mount or directory root plus intended execution mode), Spec-009 line 66 (capabilities read exposes currently-valid modes for the bound repo mount or workspace), Spec-009 line 67 (WorkspaceList exposes workspace health and current binding state)
  - **Verifies invariant:** none (contract-shape task; I-009-3 enforcement binds on T1.6)
  - **Consumes:** `SessionIdSchema` ← `packages/contracts/src/session.ts` (Plan-001 Phase 2, shipped Tier 1); `ExecutionModeSchema` / `WorkspaceStateSchema` ← T1.1

- **T1.4 — Typed repo error classes carrying the canonical `repo.*` code strings.**
  - **Files:** `packages/runtime-daemon/src/workspace/repo-errors.ts` (CREATE — first file in the Plan-009-owned `workspace/` directory)
  - Error classes (OOP per repo convention; mirrors `packages/runtime-daemon/src/ipc/session-errors.ts` precedent): `RepoRootResolutionError` (`code: "repo.root_resolution_failed"`, carries a non-sensitive `reason` discriminant — `"path_not_found" | "not_readable" | "vcs_error" | "not_absolute" | "root_mismatch"`, the last two added by PR #250 — the T1.5 absoluteness gate and its root verification respectively — per the 2026-07-25 §Notes entry), `TrustEnvelopeViolationError` (`code: "repo.outside_trust_envelope"`, carries NO raw attempted path in `message`/`fields` per the error-contracts sanitization discipline), `RepoMountNotFoundError` (`code: "repo.not_found"`), `RepoAlreadyAttachedError` (`code: "repo.already_attached"`, carries the conflicting active mount's id — the D-009-7 node-scoped active-uniqueness refusal), `RepoDetachConflictError` (`code: "repo.detach_conflict"`, carries the busy dependent workspace ids — the `Spec-009 §Detach Semantics (V1 Definition)` refusal); all five error-contracts.md §Repo rows have carriers. Code strings type-bound to a local `as const` union matching the error-contracts.md §Repo rows (hoist-to-contracts test: the strings become cross-surface at Phase 3 SDK consumption — Phase 3 decides the hoist; Phase 1 keeps them daemon-local with a type-binding comment).
  - **Tests:** `packages/runtime-daemon/src/workspace/__tests__/repo-errors.test.ts` (CREATE) — each class exposes its exact canonical code string; `TrustEnvelopeViolationError` constructed with an attempted path does not leak it into `message` (assert absence of the path substring); `instanceof` discrimination works across the five classes.
  - **Acceptance:** all five §Repo registry codes have typed, registry-matching carriers — Phase 1's two failure behaviors (resolution failure, envelope rejection) plus Phase 2's duplicate-attach and busy-detach refusals — that Phase 2/3 map onto the JSON-RPC envelope without re-keying.
  - **Spec coverage:** Spec-009 line 58 (canonical-root resolution failure fails explicitly), Spec-009 line 45 (rejection of traversal / outside-envelope binding), Spec-009 §Detach Semantics (V1 Definition) (detach refused with `repo.detach_conflict` while a dependent workspace is busy)
  - **Verifies invariant:** I-009-2 (typed explicit failure), I-009-3 (typed envelope rejection carrier)
  - **Consumes:** `repo.*` code strings ← error-contracts.md §Repo (D-009-3, ratified by this audit)

- **T1.5 — Canonical repo-root resolver.**
  - **Files:** `packages/runtime-daemon/src/workspace/repo-root-resolver.ts` (CREATE); `packages/runtime-daemon/src/workspace/repo-errors.ts` (EXTEND — the `"not_absolute"` and `"root_mismatch"` reason members and their message entries; T1.4-owned, same plan / phase / PR); `packages/runtime-daemon/src/workspace/trust-envelope.ts` (EXTEND — exports the three component-comparison helpers the resolver's root verification consumes, plus the twin-surface note true-up that import forces; T1.6-owned, same plan / phase / PR); `packages/contracts/src/repo.ts` (EXTEND — comment-only true-up of the `localPath` admission-posture bullet, recording that the resolver refuses what the wire schema admits)
  - Implement `RepoRootResolver` (class, constructor-injected exec/fs seams for testability): given a user-entered local path, produce `{canonicalRoot: string, vcsType: VcsType}` or throw `RepoRootResolutionError`. Behavior: (a) the input path is never assumed to be the repo root — resolution walks to the actual VCS toplevel (`Spec-009 §Implementation Notes`); (b) the result is absolute and symlink-resolved (realpath) so the persisted root is canonical, not an alias (`Spec-009 §Required Behavior`); (c) a non-git directory that exists, is readable, and visibly carries NO `.git` entry resolves to `{canonicalRoot: realpath(input), vcsType: "none"}` — the plain-directory fallback (`Spec-009 §Fallback Behavior`), while a directory whose own `.git` entry is visible while git reports not-a-repository refuses `"vcs_error"` instead, because a verdict contradicted by visible metadata is not a positive verdict (I-009-4, PR #250 round 4); (d) a nonexistent or unreadable path throws `RepoRootResolutionError` — never a guessed root (`Spec-009 §Fallback Behavior`); (e) input that does not name one COMPLETE location — bare-relative, `~`-prefixed, empty, or a driveless Windows root such as `\repos\foo` (which `path.win32.isAbsolute` reports absolute while it names no volume) — is refused with reason `"not_absolute"` before any filesystem probe, because completing it from the daemon's own cwd, home, or current drive manufactures a plausible wrong root from daemon-side state (I-009-2's never-guess clause; added by PR #250 per the 2026-07-25 §Notes entry); (f) a git-reported toplevel is VERIFIED, not trusted — the supplied path must be path-contained within it AND the root must report itself as its own toplevel when re-queried, or the attach is refused with reason `"root_mismatch"`, because a repository's own config file can carry `core.worktree` and make `rev-parse --show-toplevel` answer with a sibling or an ancestor tree the operator never supplied (I-009-1's fidelity clause; added by PR #250 per the 2026-07-25 §Notes entry).
  - Note: recommended mechanism is `git -C <path> rev-parse --show-toplevel` via `node:child_process` `execFile` (argv-only, no shell — per the project's argv-only execution stance) with `node:fs.promises.realpath` composition; a naive `.git`-directory walk misclassifies linked worktrees and submodules, where `.git` is a file. System `git` binary is an environment dependency: `git` absent/non-executable routes to the `RepoRootResolutionError` `"vcs_error"` reason (the `Spec-009 §Fallback Behavior` explicit-failure path), never to a silent `"none"` classification — absence of git on the host must not reclassify a real repository as a plain directory. **Superseded in part, 2026-08-16 (repo-identity amendment; owning lane Phase 2B / T-009-2B-1) — this row is a shipped-manifest record and is NOT rewritten:** asking git rather than walking `.git` directories remains ratified and is what the amendment builds on, but `--show-toplevel` ALONE answers with the enclosing working tree's own top level, which is the identity of a working tree and not of a repository. Discovery gains the companion `--git-common-dir` query and derives the canonical root from it per `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`; the toplevel answer keeps its role as the enclosing working tree's root. The clause immediately above — that a naive `.git`-directory walk misclassifies linked worktrees and submodules, where `.git` is a file — is exactly the hazard the amendment's bidirectional-registration verification (T-009-2B-2) generalizes rather than a claim it retracts.
  - **Tests:** `packages/runtime-daemon/src/workspace/__tests__/repo-root-resolver.test.ts` (CREATE; temp-dir git fixtures) — attach from a nested subdirectory resolves to the repository toplevel; a symlink to a repo subdirectory resolves to the symlink-resolved canonical toplevel; a plain directory (no repo) classifies `vcsType: "none"` with `canonicalRoot` = its realpath; a nonexistent path throws `RepoRootResolutionError` with `reason: "path_not_found"`; git-invocation failure surfaces `reason: "vcs_error"` (not a `"none"` fallback); input that names no complete location (bare-relative, `~`-prefixed, empty) throws `reason: "not_absolute"` and never a root resolved against the daemon's cwd, with git never spawned for such an input; the Windows branch of that gate is driven from POSIX CI by injecting `path.win32` as the resolver's `platformPath` seam — driveless roots (`\repos\foo`, `/repos/foo`) and drive-relative `C:foo` refused, `C:\repos` / `C:/repos` / UNC `\\server\share\repo` admitted, and a POSIX control proving the root-length rule stays win32-scoped; the returned root is always absolute; and the root verification is pinned against REAL git redirect fixtures built with `git init --separate-git-dir` plus a `core.worktree` override — a sibling redirect pointed at another real repository (which self-reports, so only containment can catch it) and an ancestor redirect pointed at the input's own parent (which contains the input, so only the re-query can catch it), each refusing with `reason: "root_mismatch"` and never with a `"none"` classification, alongside a negative control proving raw git really is redirected on the host and the honest `--separate-git-dir` shape that must still resolve; a mode-0111 plain directory refuses `reason: "not_readable"`, and so does a repository whose ROOT carries that mode when attached from a READABLE nested subdirectory (the case proving the readability probe reads the OUTGOING root, not the input) — both gated on a non-root POSIX euid, with seam-driven twins that run on every platform and uid (probe `EACCES` ⇒ `not_readable`, probe `ENOENT` ⇒ `path_not_found`); and a command-scope mirror control proving `git -c core.worktree` and the `GIT_CONFIG_COUNT` pairs do NOT move `--show-toplevel` on the same host where the repo-own-config negative control proves the redirect real. Round-4 additions: the plain-directory arm is verdict-consistency-gated — an unreadable `.git` directory (mode `000`, non-root euid), an empty `.git` directory, and a dangling gitfile each refuse `"vcs_error"` (the last two ungated — no chmod involved), the gitfile fixture proving inline that raw git's stderr matches the anchored marker so the shape genuinely reaches the plain-directory arm; seam twins drive the probe's absence path (`ENOENT` on the `.git` entry ⇒ still `"none"`) and presence path (probe success ⇒ `"vcs_error"`) on every platform and uid; and the 0111 plain-directory refusal doubles as the ordering pin — no `.git` entry there, so the consistency gate passes and `finish`'s enumerability probe still refuses `"not_readable"`.
  - **Acceptance:** resolver output is the only value Phase 2 persists as `canonical_root`; no code path returns an unresolved or guessed root.
  - **Spec coverage:** Spec-009 line 41 (canonical root resolution and persistence basis), Spec-009 line 115 (never assume the user-selected path is the repo root), Spec-009 line 58 (explicit failure on resolution failure), Spec-009 line 56 (non-git plain-directory fallback classification)
  - **Verifies invariant:** I-009-1, I-009-2, I-009-4
  - **Consumes:** `VcsType` ← T1.1 (same phase — listed for clarity, not a cross-phase dep); `toComparableComponents` / `isContainedWithin` / `componentsEqual` ← T1.6 (same phase, same PR — the component-comparison primitives the root verification calls, exported from the T1.6-owned `trust-envelope.ts` rather than hand-duplicated a sixth time); system `git` binary (environment dependency, documented above; not a contract symbol)

- **T1.6 — Trust-envelope containment validator.**
  - **Files:** `packages/runtime-daemon/src/workspace/trust-envelope.ts` (CREATE)
  - Implement `TrustEnvelopeValidator` (class): given a candidate execution root (mount canonical root + optional `directory` from `WorkspaceBindRequest`) and the session's attached-mount canonical roots, return the validated resolved root or throw `TrustEnvelopeViolationError`. Behavior per `Spec-009 §Local Trust Envelope (V1 Definition)`: resolve the candidate (absolute + realpath) THEN check containment — symlink resolution precedes the boundary check so a symlink inside the mount pointing outside is caught; containment is path-component-boundary-aware (`/repo-evil` is NOT inside `/repo`); comparison is case-folded on case-insensitive filesystems (win32 per ADR-019); `..` traversal and absolute-path `directory` values that escape the mount root are rejected.
  - **Tests:** `packages/runtime-daemon/src/workspace/__tests__/trust-envelope.test.ts` (CREATE; temp-dir + symlink fixtures) — adversarial set: `directory: "../sibling"` rejected; `directory` chain resolving through a symlink to outside the mount root rejected; prefix-collision (`/repo-evil` candidate vs `/repo` envelope) rejected; absolute `directory` outside the root rejected; happy path: nested subdirectory inside the mount root accepted and returned symlink-resolved; mount-root itself accepted; thrown errors are `TrustEnvelopeViolationError` (typed, path-redacted per T1.4).
  - **Acceptance:** no input — traversal, symlink, prefix-collision, absolute escape — yields a validated root outside an attached mount's canonical root.
  - **Spec coverage:** Spec-009 line 45 (reject path traversal or workspace binding outside the declared local trust envelope), Spec-009 §Local Trust Envelope (V1 Definition) (containment is symlink-resolved, component-boundary-aware, case-folded on case-insensitive filesystems; directory resolved against the mount canonical root and re-checked after symlink resolution)
  - **Verifies invariant:** I-009-3
  - **Consumes:** trust-envelope V1 definition ← `Spec-009 §Local Trust Envelope (V1 Definition)` (ratified by this audit)
  - Note: the validator also proves the resolved root is a READABLE directory, through a third injectable seam (the `DirectoryReadabilityProbe`, an `opendir`-based probe declared in this module and shared by construction with T1.5's resolver, which imports it). The returned value's only destination is `workspaces.fs_root` — a root a process is later asked to run inside and the daemon to enumerate — and T2.4's bind flow persists it without asking what it is, so a contained regular file would otherwise become a workspace that can never spawn, and a contained mode-0111 directory one that can never be browsed (the same class the resolver refuses at attach; PR #250 review round 3 unified both modules onto the one probe). Refusal is the same argless `TrustEnvelopeViolationError`, and the check runs LAST so every earlier refusal keeps its own cause. The seam is injectable because the win32 suite drives resolved paths no POSIX host has; the file-refusal rule is pinned on the real filesystem so it runs on CI, and the unreadable-directory rule likewise, gated on a non-root euid (root bypasses permission checks; CI's runner is non-root, so it runs there too).

### Phase 2 — RepoMount & workspace persistence + projections

**Goal.** Ship the `repo_mounts` + `workspaces` migration, the attach/read/detach and bind/list/transition service layer, the health + capability projections, and the Spec-006 lifecycle-event emission seam.

**Preconditions.**

- [x] Plan-009 Phase 1 merged (contracts in `packages/contracts/src/repo.ts` — branded IDs, unions, request/response Zod schemas, `RepoWorkspaceLifecyclePayloadSchema` + its state-parameterized factory, canonical-root resolver + trust-envelope validator) — merged 2026-07-26 via PR #250 (squash 0ed3c7f)
- [x] Plan-006 Phase 3 merged (Tier 4 — `EventLogService.append` sole append path; Plan-006 Phase 1 T1.2 registry already carries the 6 Plan-009 event names) — merged 2026-08-04, T3.1–T3.5 via PR #287 (squash `1ed7e1b`), with the Plan-006 Shipment Manifest phase-3 entry landing in the PR #289 housekeeping true-up
- [x] Schema amendments ratified — local-sqlite-schema.md §Workspace and Git Tables (D-009-7, this audit)
- [x] Detach cascade semantics ratified — `Spec-009 §Detach Semantics (V1 Definition)` (D-009-6) — gates T2.3's detach surface only

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the unchecked boxes above (preflight Gate 5;
  # PR #251 round 2 — the plural prose header is not a recognized gate form,
  # so the Plan-006 Phase 3 build-order dependency previously failed open).
  - { type: plan_phase, plan: 009, phase: 1, status: merged }
  - { type: plan_phase, plan: 006, phase: 3, status: merged }
```

#### Tasks

- **T2.1 — `repo_mounts` + `workspaces` migration + runner registration.**
  - **Files:** `packages/runtime-daemon/src/migrations/0010-repo-workspaces.ts` (CREATE — version 10 resolved from the placeholder rule "next free version per migration-runner append order at PR-open time": the chain stood at 0001–0009 when PR #292 opened), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — add the version-10 guarded `if (!hasMigrationApplied(db, 10))` block with `db.transaction(...).immediate()` + in-transaction re-check, per the runner's documented extension contract), `packages/runtime-daemon/src/workspace/__tests__/repo-workspace-migration.test.ts` (CREATE), `packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts` (EXTEND — Plan-001-owned; collects the suite's planted Plan-009-landing handoff at four assertion sites — repair contract in the Note below; the §Cross-Plan Amendments PR #292 session-substrate-suites block is the criterion-(e) disclosure), `packages/runtime-daemon/src/session/__tests__/session-service.test.ts` (EXTEND — Plan-001-owned; the four applied-version-set pins — three exact-equality `schema_version` arrays plus the concurrent-boot IMMEDIATE verifier with its assertion message and per-version atomicity walk — red under any new migration by design; repair contract site (v) in the Note; same disclosure block)
  - **Spec coverage:** Spec-009 line 73 (repo mount record persists canonical root, owner node, lifecycle state), Spec-009 line 74 (workspace record persists execution root, repo association, health)
  - **Verifies invariant:** I-009-5
  - **Consumes:** migration-runner guarded-block extension seam ← Plan-001 Phase 3 (shipped Tier 1; extension contract in the runner docstring); `openDatabase` canonical handle factory ← Plan-001 Phase 3 (shipped); amended DDL ← local-sqlite-schema.md §Workspace and Git Tables (D-009-7)
  - Note: SQL is an inlined TypeScript string constant copied **verbatim** from the amended schema doc, including `-- Owner: Plan-009` headers, plus its own `INSERT INTO schema_version (version, applied_at, description)` row — the exact `0002-runtime-node.ts` convention. Migration creates ONLY `repo_mounts` + `workspaces` + their indexes (including the partial-unique `idx_repo_mounts_active_root`); `worktrees`/`ephemeral_clones`/`branch_contexts` are Plan-010's migration. Tests assert: exact column sets (incl. `node_id`, `canonical_root`, `updated_at`, `metadata`); CHECK rejection for invalid `state`/`execution_mode`/`vcs_type` values; FK enforcement on `workspaces.repo_mount_id`; duplicate active attach of one canonical root on the same node blocked by the partial unique index while the same canonical root on a different node attaches cleanly and a detached row permits re-attach; idempotent re-run via `openDatabase` reopen. Migration-shape repair (the Plan-001-owned suite scheduled these edits for exactly this landing — dispatch with the code work above, one task): (i) `ALL_EXPECTED_TABLES` grows `repo_mounts` + `workspaces` (16 → 18 exact-equality census) and its version-walk derivation comment is re-derived through version 10; (ii) the FK-DML-enforcement test's (`packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#FORWARD_REFERENCES_DML_ENFORCEMENT_TEST`) two plain-`CREATE TABLE` parent stubs in its version-4 fixture are replaced with fixture rows against the real DDL per the stub block's own instruction — the stubs' deliberate plain CREATE is the loud "table already exists" tripwire that fires the moment 0010 applies, the real tables' NOT NULL column sets must be satisfied, and the test's `.not.toThrow()` arm requires real parent rows, which is why this site runs on the fully-migrated handle; (iii) the fresh-db absence test (`not.toContain` on the two Plan-009 table names) re-scopes onto a handle that has applied only versions ≤ 4 and then asserts against `packages/runtime-daemon/src/migrations/0004-worktree-lifecycle.ts#WORKTREE_LIFECYCLE_MIGRATION_SQL` in isolation — its premise (0004 itself must NOT create the Plan-009 parent tables) survives 0010 unchanged, but a fully-migrated handle no longer demonstrates it; (iv) the parentless-db test (`packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#WRITE_INERT_ON_PARENTLESS_DB_TEST`) re-scopes the same way, with the `SQLITE_ERROR`-class-vs-FK-class distinction now jointly pinned across sites (ii) and (iv) — each test's comment naming the other half — and both exported anchors staying valid (SQLite's forward-reference resolution behavior does not stop being true when the parents exist on other handles); (v) `session-service.test.ts`'s four applied-version-set pins re-derive to `[1..10]`: the three exact-equality `schema_version` arrays, and the concurrent-boot IMMEDIATE verifier — its array, its assertion message (nine → ten anchor rows; v10 replaces v9 as the newest migration), and a NEW v10 bullet in the per-version atomicity walk, RE-DERIVED per the walk's own closing instruction rather than incremented (the v10 script's two CREATE TABLEs, its four CREATE INDEXes — the partial-unique `idx_repo_mounts_active_root` among them — and the version-10 INSERT ride one script / one `.exec()`, so a losing racer can land neither a duplicate anchor nor an anchor without its tables); fixture constraint for (iii)/(iv): the 0004 SQL inserts its own `schema_version` row, so the isolated handle needs the earlier chain applied first — mechanism is the implementer's choice. The touch rides this PR as a dedicated `test(daemon)` commit carrying a `Refs: Plan-001` footer (lane 2 riding the surfacing lane-1 branch per the PR #250 migration-race precedent in §Cross-Plan Amendments — the census and tripwire gate this PR's own suite verification).

- **T2.2 — `workspace-event-emitter.ts` — single emission seam for the 6 `session_lifecycle` repo/workspace events.**
  - **Files:** `packages/runtime-daemon/src/workspace/workspace-event-emitter.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/workspace-event-emitter.test.ts` (CREATE)
  - **Spec coverage:** Spec-006 §Repo Workspace and Worktree Lifecycle (session_lifecycle) (the repo.attached, repo.detached, workspace.provisioning, workspace.ready, workspace.stale, and workspace.archived rows)
  - **Verifies invariant:** I-009-9
  - **Consumes:** `EventLogService.append(envelope)` ← Plan-006 Phase 3 T3.1 (upstream Tier 4; sole append path — allocates sequence + integrity columns internally; build-order precondition); 6 type names + `session_lifecycle` category mapping in `SESSION_EVENT_CATEGORY_BY_TYPE` ← Plan-006 Phase 1 T1.2 (upstream Tier 4); `RepoWorkspaceLifecyclePayloadSchema` ← Plan-009 Phase 1 T1.1 (CP-009-4); `SessionId` ← Plan-001 Phase 2 `packages/contracts/src/session.ts` (shipped)
  - Note: Mirrors the shipped `RuntimeNodeEventEmitter` single-seam pattern (`packages/runtime-daemon/src/node/node-event-emitter.ts`): standalone module imported by both services so event construction cannot drift; payload validated at the emission boundary with the Phase-1 schema's `.parse()` (the parsed output is what gets persisted); envelope/payload duplicate fields (`sessionId`, `actor`) reconciled from one input. Payload shape is the single shared `{sessionId, repoMountId?, workspaceId?, worktreeId?, state, actor?}` (`Spec-006 §Repo, Workspace, and Worktree Lifecycle (session_lifecycle)`) across all 6 types. This module never computes integrity bytes — the Plan-006 append path owns them. Tests: each constructor emits exactly the right `type`/`category`/payload; malformed payload rejected at the `.parse()` seam.

- **T2.3 — `repo-mount-service.ts` — attach / read / detach persistence.**
  - **Files:** `packages/runtime-daemon/src/workspace/repo-mount-service.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/repo-mount-service.test.ts` (CREATE), `packages/runtime-daemon/src/workspace/workspace-service.ts` (EXTEND — class-docstring ownership carve-out naming the detach cascade's in-transaction `workspaces` statements)
  - **Spec coverage:** Spec-009 line 40 (explicit attach creates a durable RepoMount), Spec-009 line 41 (canonical root persisted, not only the user-entered path), Spec-009 line 50 (attach defaults: one repo mount + one default workspace at the main checkout), Spec-009 line 56 (non-git path binds as plain-directory mount with git features disabled), Spec-009 line 58 (root-resolution failure fails explicitly), Spec-009 line 63 (RepoAttach accepts local path, session id, owning runtime node), Spec-009 line 64 (RepoMountRead exposes canonical root, VCS metadata, current health), Spec-009 line 127 (AC1 — durable repo mount with canonical root metadata), Spec-009 §Detach Semantics (V1 Definition) (detach refused with repo.detach_conflict while a dependent workspace is busy; detach archives dependent workspaces and emits the lifecycle events)
  - **Verifies invariant:** I-009-5, I-009-9
  - **Consumes:** canonical-root resolver ← Plan-009 Phase 1 T1.5 (the T1.6 trust-envelope validator is bind-side only — T2.4 consumes it; attach performs no containment check per `Spec-009 §Local Trust Envelope (V1 Definition)`); `RepoAttachRequest`/`RepoAttachResponse`/`RepoMountRead*`/`RepoDetach*` shapes ← Plan-009 Phase 1 T1.2; session-existence read ← Plan-001 Phase 3 `SessionService.replay(sessionId)` (shipped — returns `null` for a session with no events, the daemon's session-existence predicate; attach refuses on `null` before persisting anything, guarding the domain invariant that every mount belongs to exactly one session); event emission ← T2.2; rows ← T2.1; typed `repo.*` error classes ← Plan-009 Phase 1 T1.4 (the `repo.already_attached` duplicate-attach and `repo.detach_conflict` busy-detach carriers)
  - Note: `RepoMountService` class (constructor-injected `Database` handle + emitter + resolver — matches the shipped service-class pattern). `attach({sessionId, localPath, nodeId})`: resolve canonical root (failure → `repo.root_resolution_failed`, never a guess; on Windows construct `RepoRootResolver` with an ABSOLUTE `gitExecutablePath` — the bare-name default is searched in the daemon's own current directory before `PATH`, per the 2026-07-25 §Notes entry, and Phase 2 exposes the injectable constructor seam for it — no daemon-config key exists yet, that wiring rides the Plan-007 bootstrap-config surface, so a Windows deployment supplies the absolute path at construction); no containment check fires at attach — attach IS envelope admission (`Spec-009 §Local Trust Envelope (V1 Definition)`: the resolved root joins the envelope; it is never validated against one, which would reject every first or unrelated-path attach); probe git-ness → `vcs_type 'git' | 'none'`; INSERT mount (`state 'attached'`, `local_path` = entered path, `canonical_root` = resolver output, `node_id`); create the default workspace via T2.4 `createDefaultWorkspace` in the same transaction; emit `repo.attached`; duplicate active canonical root on the same node → `repo.already_attached` (D-009-7; the same path on a different node is a distinct repo and attaches). `read(repoMountId)`: row + T2.5 health enrichment (`status`/`checkedAt`). `detach(repoMountId)`: per `Spec-009 §Detach Semantics (V1 Definition)` — refuse with `repo.detach_conflict` while any dependent workspace is `busy`; otherwise state → `detached`, archive dependents via T2.4, emit `repo.detached` (+ one `workspace.archived` each). Tests: AC1 durability (attach → close → `openDatabase` reopen → row present with `canonical_root` ≠ entered subpath); non-git dir attach yields `vcs_type 'none'` mount that still works; resolution failure produces `repo.root_resolution_failed` and persists nothing; an attach to a fresh unrelated path on an envelope-bearing session succeeds (admission, not containment — `Spec-009 §Local Trust Envelope (V1 Definition)`); detach-while-busy refused; detach cascade archives + emits.

- **T2.4 — `workspace-service.ts` — bind / list / state-transition primitives + write gate.**
  - **Files:** `packages/runtime-daemon/src/workspace/workspace-service.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/workspace-service.test.ts` (CREATE)
  - **Spec coverage:** Spec-009 line 43 (binding is explicit and resolves to one concrete execution root before a run), Spec-009 line 45 (reject path traversal / outside trust envelope), Spec-009 line 51 (read-only default until a writable mode is explicitly selected), Spec-009 line 59 (unavailable path → stale; new write runs blocked until repair), Spec-009 line 65 (WorkspaceBind accepts repo mount plus intended execution mode), Spec-009 line 67 (WorkspaceList exposes workspace health and binding state), Spec-009 line 85 (mode switch re-provisions in place; workspace ID stable), Spec-009 line 87 (state progression ready → provisioning → ready), Spec-009 line 90 (row keeps id; execution_mode/fs_root updated; state cycles through provisioning), Spec-009 line 91 (failed switch → stale with error detail in workspace metadata), Spec-009 line 128 (AC2 — multiple repo mounts and multiple bound workspaces per session)
  - **Verifies invariant:** I-009-6, I-009-7
  - **Consumes:** trust-envelope validator ← Plan-009 Phase 1 T1.6; `WorkspaceBind*`/`WorkspaceList*` shapes ← Plan-009 Phase 1 T1.3; `workspace.stale` + `workspace.mode_unsupported` error codes ← error-contracts.md §Workspace (D-009-3); event emission ← T2.2; rows ← T2.1
  - Note: `WorkspaceService` class. `createDefaultWorkspace(mount)` (called by T2.3 attach for git AND non-git mounts per D-009-7): `execution_mode 'read-only'`, `fs_root` = canonical root, `state 'ready'` (no provisioning step exists for read-only-at-main-checkout), emit `workspace.ready`. `bind({repoMountId, executionMode, directory?})`: derive `session_id` from the mount row; resolve `directory` against the mount canonical root and reject escapes (`repo.outside_trust_envelope`) — the T1.6 validator this calls also guarantees the root it returns is a READABLE directory, so this flow inherits that and needs no file-type or readability check of its own; reject modes outside the mount's capability set (`workspace.mode_unsupported`, per the D-009-5 matrix); read-only → row `state 'ready'` with resolved `fs_root`; writable modes → row `state 'provisioning'`, `fs_root` NULL, emit `workspace.provisioning` — provisioning completion is Plan-010's execution-root surface via the CP-009-2 primitives. `list({sessionId, repoMountId?})` returns rows + health enrichment (`Spec-009 §Interfaces And Contracts`). Transition primitives `beginReprovision` / `completeReprovision` / `failReprovision` (CP-009-2) drive the line-87 progression in place, with `failReprovision` writing `metadata.lastError` (`Spec-009 §Execution Mode Transitions`) — credential-scrubbed FIRST and then truncated to `WORKSPACE_LAST_ERROR_MAX_LEN` at persist time, in that order because truncating after the scrub cannot bisect a secret token out of the scrubber's pattern match, and bounded at persist because the same constant is the wire cap the I-009-10-validated `repo.workspaceList` response enforces, so one uncapped verbose failure would otherwise make every later list response unrepresentable (PR #250 round 4); `markStale(workspaceId)` is the line-59 transition (no detail parameter — a probe-derived stale is not a "recorded failure" under T1.3's `lastError` present-iff contract, whose detail-bearing path is `failReprovision`; signature trued up per the 2026-08-05 T2.4 §Notes entry); `assertWritable(workspaceId)` throws `workspace.stale` (CP-009-3); busy-marking primitives `markBusy(workspaceId, runId)` / `releaseBusy(workspaceId)` drive `ready -> busy -> ready` around run execution-root holds with `markBusy` on an already-busy workspace throwing typed `workspace.busy` (CP-009-7 — consumed by Plan-010's run-setup gate; no dedicated event type, per the closed-registry posture). Tests: AC2 (two mounts, several workspaces, list returns all with states); traversal-rejection on `directory`; id-stability + row-count invariance across a full reprovision cycle; `failReprovision` persists `stale` + `lastError`; `assertWritable` passes `ready`, throws on `stale`.

- **T2.5 — `workspace-projector.ts` — health + execution-mode-capability projection.**
  - **Files:** `packages/runtime-daemon/src/workspace/workspace-projector.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/workspace-projector.test.ts` (CREATE)
  - **Spec coverage:** Spec-009 line 57 (capability gap exposed explicitly, never silent substitution), Spec-009 line 66 (capabilities read exposes which modes are currently valid), Spec-009 line 75 (repo health and git metadata are daemon-owned projection state), Spec-009 line 129 (AC3 — non-git workspaces usable without pretending git-only features), Spec-009 §Repo Mount Health (V1 Definition) (repo mount health is the daemon-probed reachability projection)
  - **Verifies invariant:** I-009-7, I-009-8
  - **Consumes:** `ExecutionMode`/`WorkspaceState` unions + `WorkspaceExecutionModeCapabilitiesReadResponse` shape ← Plan-009 Phase 1 T1.1/T1.3; `RepoMountHealth` shape ← Plan-009 Phase 1 T1.1 (D-009-2); rows ← T2.1; probe inputs ← service layer (T2.3/T2.4)
  - Note: Pure projection module per the shipped `session-projector.ts` precedent — no I/O inside the fold; the service layer performs the filesystem probe and feeds `{row, probe}` in. `computeRepoMountHealth(mountRow, probe)` → `{status, checkedAt}` (D-009-2); `computeWorkspaceHealth(workspaceRow, probe)` → health view deriving the stale verdict when the probe reports the path unavailable (the service persists the `markStale` transition the projector derives — on-read floor per D-009-5). `computeExecutionModeCapabilities(mountRow)` → `{availableModes, defaultMode, restrictions}` per the D-009-5 V1 static matrix: `'git'` → all four modes, `defaultMode 'worktree'` (ADR-006); `'none'` → `['read-only']`, `defaultMode 'read-only'`, `restrictions` populated with a reason per excluded mode — never silent substitution. Background-refresh wiring per D-009-5 layer 2 (daemon idle scheduler; lands here if the scheduler seam is available at Phase-2 time, else with Phase 3 — the on-read floor satisfies line 59 either way). Tests: AC3 (non-git mount: read-only available + three reasoned restrictions); probe-fail → stale health on every read surface; git mount → four modes + worktree default; restriction map is exhaustive over excluded modes (I-009-8).

- **T2.6 — Phase-2 acceptance suite — AC-mapped persistence + projection integration.**
  - **Files:** `packages/runtime-daemon/src/workspace/__tests__/repo-workspace-acceptance.test.ts` (CREATE)
  - **Spec coverage:** Spec-009 line 127 (AC1 — attach yields a durable repo mount with canonical root metadata), Spec-009 line 128 (AC2 — a session contains multiple repo mounts and multiple bound workspaces), Spec-009 line 129 (AC3 — non-git directory workspaces usable without pretending git-only features), Spec-009 line 42 (multiple repo mounts in one session)
  - **Verifies invariant:** I-009-5, I-009-6, I-009-7, I-009-8, I-009-9 (integration)
  - **Consumes:** `openDatabase` ← Plan-001 Phase 3 (shipped); full Phase-2 surface ← T2.1–T2.5; `EventLogService` test double or real instance ← Plan-006 Phase 3 T3.1 (upstream Tier 4 — real instance preferred once shipped; the emitter seam is injectable either way)
  - Note: End-to-end over a real temp-file SQLite handle via `openDatabase` (the canonical factory — pragma/migration order never re-derived in tests): attach two real git fixtures + one plain directory to one session; assert AC1 durability across reopen; AC2 multi-mount/multi-workspace listing; AC3 reduced capability surface; assert the emitted durable-event sequence for the full lifecycle (`repo.attached`, `workspace.ready`, `workspace.provisioning`/`workspace.ready` reprovision cycle, `workspace.stale` on probe-fail, `repo.detached` + one `workspace.archived` per archived dependent on detach) — exactly one event per transition (I-009-9).

### Phase 2B — Repo Identity Keying And Resolution Hardening

**Precondition:** Phase 2 merged — this supplement amends the Phase-1 resolver and trust-envelope modules and the Phase-2 attach/bind services, all of which have shipped (PR #250, PR #292). It re-opens neither manifest entry: Phase 2B EXTENDs those files and adds one new module, and it rewrites no shipped task row. Every surface it consumes already exists — the system `git` binary, the `GitFileExecutor` / `PathRealpathResolver` / `DirectoryReadabilityProbe` injectable seams, the `RepoRootResolutionError` reason union, and the `toComparableComponents` / `isContainedWithin` / `componentsEqual` comparison primitives — so **no born-unchecked carrier box is minted for Phase 2B itself**: it authors the one surface that was missing (a repository-registration reader) rather than waiting on another plan for it. The gate is the self-referential entry below. Dispatch is by explicit supplement label only (`preflight.mjs docs/plans/009-repo-attachment-and-workspace-binding.md 2B`); the no-argument walk is numeric-only and resolves PAST this supplement to Phase 3, which is precisely why Phase 3 and Phase 4 carry the `Repo-identity resolver amendment landed` §Preconditions box as a Gate-5 `precondition_box_checked` entry — without it the walk would select Phase 3 across the unmet dependency.

<!-- prettier-ignore -->
```yaml
preconditions:
  # Self-referential gate: Phase 2B amends the Phase-1 resolver/trust-envelope
  # modules and the Phase-2 attach/bind services, so it dispatches only after
  # this plan's own Phase 2 manifest has fully shipped (preflight Gate 5).
  - { type: plan_phase, plan: 009, phase: 1, status: merged }
  - { type: plan_phase, plan: 009, phase: 2, status: merged }
```

**Goal.** Move repo identity off the enclosing working tree's top level and onto the repository's git common directory, so N working trees of one repository resolve as ONE repo mount, and harden resolution so a nonexistent path, unusable git metadata, a non-bidirectional worktree link, or a non-regular pointer file is refused rather than misattributed to an ancestor repository. Execution roots stay per-working-tree throughout — the amendment changes the identity key, never where work happens. Sits between §Rollout Order steps 1 and 2: identity is corrected while the only consumer is the daemon's own service layer, before step 2 exposes attach through the SDK/CLI and bakes the wrong semantics into an observable surface.

- Satisfies §Invariants I-009-15 and I-009-16; re-verifies the amended I-009-1 and I-009-3
- Sourced from `Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`, the amended `Spec-009 §Local Trust Envelope (V1 Definition)` registered-working-tree admission rule, and acceptance criteria 4 and 5 — all landed by the 2026-08-16 repo-identity amendment (AC5's fifth refusal shape, the failed bidirectional root verification, added in the same PR's Codex round; the derivation-first arm order, the at-path-and-strictly-between unusable-metadata screen, the persisted-identity match, and the CP-009-9 branch-mode leg added in its round 2)
- Mints **no** wire contract change: `RepoAttachRequest` / `RepoAttachResponse` / `WorkspaceBind*` / `WorkspaceList*` are untouched, `RepoRootResolutionError`'s five-member reason union is untouched (the new refusals map onto `path_not_found`, `vcs_error`, and `root_mismatch`), no error code is added to error-contracts.md §Repo, and no migration lands — `repo_mounts.canonical_root` and `workspaces.fs_root` keep their DDL and change only in which value the daemon computes for them, while the round-2 persistence legs ride the existing metadata JSON columns (`repo_mounts.metadata.commonDir`, `workspaces.metadata.boundRoot` — schema-doc comment rows updated, no DDL, and absent keys degrade to the pre-amendment reading so no backfill is needed)
- **Carried-forward review obligations:** the eleven PR #340 Codex round-3/4 findings enumerated in this plan's §Progress Log entry of 2026-08-17 are triaged inside this phase's implementation PR — each implemented, refuted with recorded reasoning, or re-routed to the plan that owns its surface — before this phase's manifest entry lands

#### Tasks

- **T-009-2B-1 — Common-directory identity in the resolver.**
  - **Files:** `packages/runtime-daemon/src/workspace/repo-root-resolver.ts` (EXTEND), `packages/runtime-daemon/src/workspace/__tests__/repo-root-resolver.test.ts` (EXTEND)
  - Discovery asks git for BOTH answers in place of the single `--show-toplevel` query: the enclosing working tree's own root and the repository's git common directory (`rev-parse --show-toplevel` and `rev-parse --path-format=absolute --git-common-dir`, each through the existing `queryCanonicalToplevel` invocation shape — same argv-only `executeFile` seam, same stripped and locale-pinned environment, same timeout and buffer cap, so the two queries cannot drift into different trust postures). The MAIN checkout root is then DERIVED AND VERIFIED, never assumed (`Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition)`), and the derivation runs FIRST — before any own-toplevel fallback — because the fallback is the spoofable arm: a planted `.git` pointer file naming a repository's common directory directly makes git report gitDir == commonDir with the planted holder as toplevel (empirically reproduced on git 2.50.1 during the PR #340 round-2 triage), so a gitDir==commonDir⇒own-toplevel FIRST rule persists the planted directory as that repository's root. Candidate derivation, in order: common-directory basename `.git` ⇒ the candidate is its parent (the ordinary layout); otherwise a `core.worktree` recorded in the common directory's own `config` ⇒ the candidate is that value resolved against the common directory (the submodule layout — the module directory `<superproject>/.git/modules/<name>` has a non-`.git` basename yet genuinely records its working tree; read file-scoped via `git config --file <commonDir>/config --get core.worktree` through the same executor seam, never via discovery). The candidate is accepted only when its own `.git` entry classifies (T-009-2B-2) as resolving back to that same common directory — a DIRECTORY canonically equal to it or a POINTER naming it — and an accepted candidate IS `RepoRootResolution.canonicalRoot`. Only when no candidate derives, or the back-check fails, does the resolver consult the supplied tree: canonicalized `--absolute-git-dir` equal to the common directory ⇒ `canonicalRoot` is that tree's own toplevel — the first-attach-defines-root arm every `--separate-git-dir` MAIN checkout takes, because that layout's common directory sits outside the working tree entirely and git records no main-working-tree pointer for it (no `core.worktree` is written, and git's own `git worktree list` there reports the git directory itself as the main working tree) — otherwise refuse `reason: "root_mismatch"` naming attach-from-the-main-checkout as the recovery: the linked-worktree-of-a-`--separate-git-dir`-repository shape lands on this refusal arm by design, and the repository is attached via its main checkout instead. The named residual: for that layout only, a direct-to-common pointer-file holder is indistinguishable from the true main checkout at first attach (git records nothing to check against); the step-4 membership screen closes the spoof for every layout git does record, and the attach-persisted `metadata.commonDir` match (T-009-2B-3/T-009-2B-4) bounds the residual after attach. The working tree's own root is returned alongside as a new `workingTreeRoot` member so the caller can root its default workspace where the participant actually pointed. `RepoRootResolution` is a daemon-internal interface — growing it changes no wire schema. Deliberately NO ancestor DISCOVERY walk is introduced — git is still the authority on where the repository is, which is what keeps the "refuse a path that does not exist rather than walking up from it" rule structural here rather than a check that can be forgotten, and step 2's `realpathOrThrow` still refuses a nonexistent input with `path_not_found` before any git spawn, pinned by a regression test in this task rather than left incidental — but resolution gains the bounded POST-HOC screen the spec's corrected absent-vs-unusable rule requires: git's own upward discovery hard-stops only on an unusable pointer-FILE shape and silently SKIPS an unusable `.git` DIRECTORY at an ancestor (both empirically probed, git 2.50.1), so after git answers, the supplied path's own `.git` entry and every directory strictly between it and the resolved toplevel are screened via `classifyGitDirEntry`, and an UNUSABLE verdict refuses with `reason: "vcs_error"` — the at-path arm covers the empty strictly-between set (a supplied path directly under its repository's toplevel) and applies on the no-repository arm too, where it is the only entry screened (an unusable own entry refuses rather than classifying `vcsType: "none"`) — finitely many components below a root git already resolved, never a walk above it, so the screen can refuse a wrong answer but never manufacture a root.
  - Step 4's CONTAINMENT check is re-scoped in the same edit, and this is the non-obvious half: today it asserts the supplied path is contained within the value about to be persisted, which under common-directory keying would refuse EVERY linked-worktree attach (a worktree beside the main checkout is not path-contained within it). The check becomes: the supplied path must be contained within the enclosing `workingTreeRoot`, and `workingTreeRoot` must be a verified member of the repository named by the common directory — canonically EQUAL to the derived-and-verified `canonicalRoot` (the main-checkout leg; equality, not a pointer-resolution check, so a planted pointer-file holder that merely RESOLVES to the common directory never passes as the main checkout) or a working tree the repository registers (T-009-2B-2's bidirectional verification). The `core.worktree` redirect vector the shipped `root_mismatch` refusal exists to catch stays closed by that membership test, which is strictly stronger than the containment it replaces: a redirected toplevel that contains the input no longer passes merely by containing it. Step 5's fixpoint re-query is retained unchanged against `workingTreeRoot`.
  - **Tests:** real multi-worktree temp-dir git fixtures (main checkout plus two `git worktree add` linked worktrees) — attach from the main checkout, from each linked worktree, and from a nested subdirectory of a linked worktree all yield one identical `canonicalRoot`, while `workingTreeRoot` differs per tree and equals that tree's own root; a bare-repository fixture refuses through the existing `vcs_error` arm (no working tree to attach); a `--separate-git-dir` MAIN-checkout fixture resolves `canonicalRoot` to the checkout's own top level — never to the metadata directory's parent — and a linked worktree added to that fixture refuses `root_mismatch` with the recovery naming the main checkout; the shipped `root_mismatch` redirect fixtures (sibling and ancestor `core.worktree` redirects) still refuse with `reason: "root_mismatch"`; a nonexistent path still refuses `reason: "path_not_found"` with git never spawned; the plain-directory arm still classifies `vcsType: "none"` at the input's own realpath; a planted direct-to-common `.git` pointer file (a directory whose `.git` file names a real repository's common directory) refuses `reason: "root_mismatch"` with the real repository's root derived — the arm-order regression fixture: under an own-toplevel-first order it attaches as a root; a SUBMODULE fixture (superproject with one initialized submodule) resolves the submodule checkout's own root as `canonicalRoot` via the `core.worktree` candidate — never the superproject's root, never the module directory under `<superproject>/.git/modules/` — and a linked worktree added from the submodule checkout resolves to that same submodule root; an ancestor-unusable fixture (an emptied `.git` directory planted between a nested supplied path and its repository's toplevel) refuses `reason: "vcs_error"` even though git's own discovery silently skips it, with its negative-control twin proving the same nested path resolves once the planted entry is removed; an at-path twin — the supplied path's OWN `.git` emptied (the empty strictly-between set), once under an enclosing repository and once with no repository above — refuses `reason: "vcs_error"` on both arms rather than resolving to the enclosing repository or classifying `vcsType: "none"`. **Negative control:** a fixture asserting `canonicalRoot` is NOT the common directory itself — the plausible-but-wrong twin of this change is returning `<main>/.git` as the root, which would make every workspace root at a git directory; the assertion pins `canonicalRoot` to the main checkout's WORKING-TREE root and `basename(canonicalRoot) !== ".git"`.
  - **Acceptance:** every working tree of one repository resolves to one identical canonical root; no resolution returns a git directory, a bare repository's object store, or a root the supplied path does not sit inside; and no directory that merely points at a repository's common directory resolves as a working tree of that repository unless git itself records it as one.
  - **Spec coverage:** Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition) (identity keys on the git common directory; canonical root is the main checkout's working-tree root; resolution surfaces both the repository identity and the enclosing working tree's own root), Spec-009 line 41 (canonical root resolution and persistence basis), Spec-009 line 115 (never assume the user-selected path is the repo root)
  - **Verifies invariant:** I-009-15, I-009-1
  - **Consumes:** `GitFileExecutor` / `PathRealpathResolver` / `PlatformPathModule` seams + `queryCanonicalToplevel` + `RepoRootResolutionError` ← Plan-009 Phase 1 T1.5 (shipped); `toComparableComponents` / `isContainedWithin` / `componentsEqual` ← Plan-009 Phase 1 T1.6 (shipped); working-tree membership verification + `classifyGitDirEntry` (the candidate back-check and the at-path-and-strictly-between unusable screen) ← T-009-2B-2 (same phase, same PR); file-scoped `core.worktree` read (`git config --file <commonDir>/config --get core.worktree`, per git-config(1)) through the same executor seam; system `git` binary supporting `--git-common-dir` (git ≥ 2.5) and `--path-format=absolute` (git ≥ 2.31 — the governing floor, per the git 2.31 release notes; environment dependency, not a contract symbol)

- **T-009-2B-2 — Git pointer-file reader + repository-registration verification.**
  - **Files:** `packages/runtime-daemon/src/workspace/git-repository-layout.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/git-repository-layout.test.ts` (CREATE)
  - The module every direct filesystem read of git's on-disk layout routes through, so the screens cannot be applied in one call site and forgotten in another. `readPointerFile(path)` opens the entry non-blocking (`O_RDONLY | O_NONBLOCK`), validates the OPENED descriptor with `fstat` — refusing anything that is not a REGULAR file — and reads through that same descriptor under a fixed byte ceiling, so the regular-file and bounded-read guarantees hold for the object actually read: a pre-open `stat` alone leaves the TOCTOU window where the checked path is swapped for a FIFO or device node between check and open (a FIFO named `commondir` or `gitdir` planted in a scanned directory is the concrete shape — the open blocks indefinitely and wedges the scan), and trusting pre-open size metadata for the bound has the same race; the non-blocking flag makes even a FIFO's open return immediately so the `fstat` verdict, not the open, decides. `classifyGitDirEntry(workingTreeRoot)` returns the three-way verdict the amended contract requires — ABSENT (no `.git` entry), a DIRECTORY (a main checkout), or a POINTER (`gitdir:` file) — and, separately, UNUSABLE for an entry that is present but unreadable, malformed, or naming something that is not a usable git directory; an absent entry is a continue-condition for any caller that scans, an unusable one is a STOP that refuses — a rule the daemon must enforce ITSELF, because git does not: git's own upward discovery hard-stops only on the unusable pointer-file shape and silently skips an unusable `.git` DIRECTORY at an ancestor (empirically probed, git 2.50.1), which is precisely how a damaged repository gets attributed to its parent; T-009-2B-1's at-path-and-strictly-between screen is this classifier's consumer for that arm. `verifyWorkTreeRegistration(commonDir, workingTreeRoot)` requires git's link to be BIDIRECTIONAL — the working tree's `.git` file names a git directory, AND that git directory's registration under `<commonDir>/worktrees/<name>/gitdir` names this working tree's own `.git` back — and additionally requires the registration to be physically contained by the common directory it claims (the registered git directory's grandparent canonically equals that common directory), because a pointer file is attacker-writable and must never establish repository membership by itself. `listRegisteredWorkTrees(commonDir)` enumerates `<commonDir>/worktrees/*/gitdir`, resolving git ≥ 2.48 relative registrations against the registration file's own directory, and returns only entries that pass the same verification. Every path comparison in the module canonicalizes BOTH sides (symlink-resolved and absolutized) before comparing: on macOS `/tmp` is `/private/tmp`, so an uncanonicalized registration value never equals its canonicalized peer and a legitimate working tree is silently refused.
  - **Tests:** real temp-dir fixtures — a main checkout classifies DIRECTORY, a linked worktree classifies POINTER and verifies bidirectionally, a plain directory classifies ABSENT; UNUSABLE arms for a mode-`000` `.git` directory (POSIX, non-root euid), an empty `.git` directory, and a dangling `gitdir:` pointer, each with a seam-driven twin that runs on every platform and uid; a hand-planted `.git` file pointing at a real foreign repository's git directory is REFUSED (the one-way-pointer arm — the registration there names a different working tree back); a registration whose git directory is not physically contained by the claimed common directory is refused; a FIFO named `commondir` is refused by the descriptor screen — non-blocking open, `fstat` on the opened descriptor — without hanging, POSIX-gated with a seam-driven twin; a registration written through a symlinked temp root (`/tmp` on macOS) verifies, proving both-side canonicalization. **Negative control per arm:** the honest shape of each refused fixture still verifies, so no arm can pass by refusing everything.
  - **Acceptance:** no pointer file establishes repository membership on its own; every accepted linked worktree is one git itself registers, and every rejected shape is rejected for a named reason rather than by a blanket refusal.
  - **Spec coverage:** Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition) (git-dir absent versus unusable; the bidirectional worktree link plus physical containment of the registration; the regular-file stat screen and byte ceiling; canonicalization of both sides of every path comparison), Spec-009 line 58 (explicit failure on resolution failure)
  - **Verifies invariant:** I-009-16
  - **Consumes:** `PathRealpathResolver` + `DirectoryReadabilityProbe` seam shapes ← Plan-009 Phase 1 T1.5/T1.6 (shipped — reused verbatim so the module is testable without a real filesystem); `RepoRootResolutionError` reason union ← Plan-009 Phase 1 T1.4 (shipped; unchanged — refusals map onto `vcs_error` and `root_mismatch`)

- **T-009-2B-3 — Registered-working-tree trust-envelope admission.**
  - **Files:** `packages/runtime-daemon/src/workspace/trust-envelope.ts` (EXTEND), `packages/runtime-daemon/src/workspace/__tests__/trust-envelope.test.ts` (EXTEND)
  - `TrustEnvelopeValidator` gains the registered-working-tree arm the identity change makes necessary: once the canonical root is the main checkout's root, a linked worktree of that repository is generally NOT path-contained within it, so containment alone would reject binds to working trees the repository demonstrably owns. A git-backed mount's envelope becomes its canonical root PLUS the working-tree paths `listRegisteredWorkTrees` returns for that repository, each admitted only through T-009-2B-2's bidirectional-link and physical-containment verification. Registration is read from the repository at bind time and never cached, so a working tree git no longer registers is no longer in the envelope. This arm governs USER-SUPPLIED bind paths only and never narrows the provenance admission of daemon-provisioned execution roots: a provisioned worktree whose registration an external `git worktree prune` has removed stays admitted by provenance and is retired by the Plan-010 sweep, so the two admission rules cannot disagree in that window. Containment WITHIN an admitted registered working tree is then checked by exactly the shipped rule — resolve first, then compare component-boundary-aware and case-folded — so `..` traversal, symlink escape, prefix collision, and absolute-path escape are refused inside a registered worktree exactly as they are inside the canonical root. Plain-directory (`vcs_type 'none'`) mounts gain no arm: they have no repository and therefore no registrations, and their envelope stays the canonical root alone. The WIRE form is fixed by the amended `Spec-009 §Local Trust Envelope (V1 Definition)` containment bullet and changes no schema: a RELATIVE `WorkspaceBind.directory` resolves against the canonical root exactly as shipped, and an ABSOLUTE `directory` is how a bind names a registered working tree (or any admitted root) — mandatory where no relative spelling exists, as for a worktree on another Windows drive — with admission for both forms decided by post-resolution containment within the union of admitted roots. The admission check is exposed for reuse at the RUN boundary (T-009-2B-4 wires it into run handoff), so bind-time and run-time membership verify through one code path rather than two drifting ones. Bind-time derivation additionally MATCHES the attach-persisted identity: attach records the canonicalized common directory as `repo_mounts.metadata.commonDir` (inside the existing metadata JSON column — no DDL; T-009-2B-4's attach leg writes it), and the common directory this task re-derives from the mount's canonical root at bind time must equal it; a mismatch — the root's `.git` entry moved or retargeted since attach — refuses with `reason: "root_mismatch"` and marks the mount per `Spec-009 §Repo Mount Health (V1 Definition)` rather than silently following the new target, with re-attach as the named recovery, and an ABSENT key (a pre-amendment row) degrades to the pre-amendment no-match reading rather than refusing. The run-boundary reuse inherits the same match, so a post-attach retarget cannot walk binds or runs onto a different repository.
  - **Tests:** multi-worktree fixture — a bind naming a registered linked worktree by ABSOLUTE `directory` is admitted and returns the symlink-resolved root; a nested subdirectory of that worktree is admitted; `directory: "../sibling"` from inside the worktree is refused; an absolute `directory` resolving outside every admitted root is refused; a symlink inside the worktree pointing outside every admitted root is refused; a sibling directory that is NOT a registered working tree of the mount's repository is refused; a working tree registered by a DIFFERENT repository is refused (admission is per-repository, not per-machine); the read-at-bind-time-never-cached rule is pinned DISCRIMINATINGLY — between two binds of the same worktree path, only the ADMINISTRATIVE registration (`<commonDir>/worktrees/<name>`) is deleted while the working-tree directory itself stays present and readable, so the second bind can fail only because membership is re-read and no longer verifies (a `git worktree remove` fixture would delete the directory too, letting a registration-caching implementation pass by failing on path resolution instead); and — the provenance-scoping arm — a daemon-provisioned execution root whose registration has been pruned out from under it stays admitted, proving the registration screens never narrow provenance admission. All refusals remain the argless, path-redacted `TrustEnvelopeViolationError`; a retarget fixture — after a successful attach, the mount root's `.git` is repointed at a second real repository's common directory — refuses the next bind through the persisted-identity match (`metadata.commonDir` no longer equals the re-derivation) while an untouched sibling mount still binds, and a row with the key absent binds under the pre-amendment reading.
  - **Acceptance:** every path a repository itself registers as one of its working trees is bindable, daemon-provisioned roots stay admitted by provenance irrespective of registration state, and no other path outside the canonical root is bindable.
  - **Spec coverage:** Spec-009 §Local Trust Envelope (V1 Definition) (a git-backed mount's envelope is its canonical root plus the working-tree paths the repository registers, each admitted through the same bidirectional-link and physical-containment screens; registration read at bind time and never cached; the two wire forms of `WorkspaceBind.directory` — relative against the canonical root, absolute to name a registered working tree — admitted solely by post-resolution containment within an admitted root; the registration arm governs user-supplied bind paths only and never narrows daemon-provisioned provenance admission; containment within an admitted working tree checked exactly as within the canonical root), Spec-009 line 45 (reject path traversal or workspace binding outside the declared local trust envelope), Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition) (the attach-persisted identity match at bind time — a re-derivation that no longer equals `repo_mounts.metadata.commonDir` refuses rather than following the moved `.git` entry)
  - **Verifies invariant:** I-009-3
  - **Consumes:** `TrustEnvelopeValidator` + `toComparableComponents` / `isContainedWithin` / `componentsEqual` + `DirectoryReadabilityProbe` ← Plan-009 Phase 1 T1.6 (shipped); `listRegisteredWorkTrees` + `verifyWorkTreeRegistration` ← T-009-2B-2 (same phase, same PR); repository common directory for the bound mount ← T-009-2B-1's resolution output, re-derived at bind time from the mount's canonical root and matched against the attach-persisted `repo_mounts.metadata.commonDir` (T-009-2B-4's attach leg writes it)

- **T-009-2B-4 — Attach/bind reconciliation + Phase-2B acceptance suite.**
  - **Files:** `packages/runtime-daemon/src/workspace/repo-mount-service.ts` (EXTEND), `packages/runtime-daemon/src/workspace/workspace-service.ts` (EXTEND — `createDefaultWorkspace` roots at the resolution's `workingTreeRoot`), `packages/runtime-daemon/src/workspace/execution-root-service.ts` (EXTEND — Plan-010-extended file per the §2 ownership row, consented via CP-009-9 / Plan-010 CP-010-14: the branch-mode arm re-roots at the workspace's bound working tree), `packages/runtime-daemon/src/workspace/__tests__/execution-root-service.test.ts` (EXTEND — the linked-worktree branch-mode arm), `packages/runtime-daemon/src/workspace/__tests__/repo-workspace-acceptance.test.ts` (EXTEND)
  - `RepoMountService.attach` persists the resolution's `canonicalRoot` as `repo_mounts.canonical_root` unchanged, and hands `workingTreeRoot` to `createDefaultWorkspace`, which roots the default workspace at the working tree the participant actually named rather than at the repository's main checkout. This is the seam where the amendment's plausible-but-wrong twin would land: rooting the default workspace at the canonical root would silently relocate every linked-worktree attach's execution to the main checkout — a data-loss-adjacent behavior change wearing the costume of a one-line simplification. Attaching a second working tree of an already-attached repository now collides on the `idx_repo_mounts_active_root` partial unique index and is refused `repo.already_attached` carrying the existing mount's id (the D-009-7 refusal, unchanged in mechanism — only the value being compared changed); the refusal message and the SDK-level guidance name binding a workspace on the existing mount as the way to work in that second tree, because "already attached" is now a per-REPOSITORY verdict rather than a per-directory one. Detach is unchanged in mechanism and its per-repository reading is recorded in `Spec-009 §Detach Semantics (V1 Definition)`. The RUN-boundary seam closes the last gap between the amended envelope and the shipped services: `WorkspaceService.assertWritable` — the existing pre-run write gate (CP-009-3) — additionally re-verifies a USER-SUPPLIED execution root's envelope membership through T-009-2B-3's validator (canonical-root containment or currently-registered working tree), so a worktree unregistered AFTER a successful bind stops being runnable at the next run rather than persisting indefinitely as a trusted root the spec now calls outside the envelope; a root that no longer verifies blocks the run and transitions the workspace to `stale` with the typed error recorded (the `Spec-009 §Fallback Behavior` blocked-until-repair shape, the same transition the execution-mode-switch failure path already uses), and daemon-provisioned roots are exempt by provenance — their lifecycle belongs to the Plan-010 retirement sweep, never to this check. Two round-2 persistence legs land at the same seams: `RepoMountService.attach` persists the resolution's canonicalized common directory as `repo_mounts.metadata.commonDir` (the identity anchor T-009-2B-3's bind-time match reads), and workspace creation records the admitted bind origin as `workspaces.metadata.boundRoot` — `workingTreeRoot` for the default workspace, the admitted resolved root for an explicit bind — a value `beginReprovision` never clears (it releases `fs_root` on the way into `provisioning`, which is exactly why the bound origin needs its own durable carrier). The BRANCH-MODE leg then closes the last per-working-tree gap: the shipped `ExecutionRootService.#bindBranchMode` returns `mount.canonical_root` — correct while the canonical root meant the bound tree, but under common-directory keying it would silently relocate every linked-worktree workspace's branch-mode runs to the main checkout, the exact relocation I-009-15 names as a different and worse bug. Under CP-009-9 (Plan-010's consent: CP-010-14) the branch arm re-roots at the workspace's `metadata.boundRoot`, falling back to `mount.canonical_root` when the key is absent (pre-amendment rows keep byte-identical behavior — no backfill migration), and `#verifyBranchModeBind` verifies against the same value; `Spec-010 §Turn-Boundary Snapshots`' branch-mode sentence is corrected in the same swap to name the workspace's bound checkout.
  - **Tests:** end-to-end over a real temp-file SQLite handle via `openDatabase` and a real multi-worktree git fixture — attach the main checkout, then attach a linked worktree of the same repository: the second attach is refused `repo.already_attached` naming the first mount, and exactly one `repo_mounts` row exists for the repository; bind a workspace rooted at that linked worktree on the existing mount and assert its `fs_root` is the WORKTREE's root, not the canonical root, with the mount's `canonical_root` unchanged; attach two DIFFERENT repositories and assert two mounts (acceptance criterion 2 is not regressed by the collapse); attach the same repository on a different `node_id` and assert it still attaches (D-009-7's node-scoping is unchanged); detach the mount and assert every dependent workspace across both working trees archives; delete a bound worktree's ADMINISTRATIVE registration after a successful bind (directory left present and readable) and assert the next `assertWritable` refuses, the workspace transitions to `stale` with the error recorded, and — the provenance-exemption arm — a daemon-provisioned root under the same registration loss stays writable; the branch-mode arm — a workspace bound to a linked worktree and switched to `branch` mode prepares its execution root at the WORKTREE's own root, never the canonical root, with `#verifyBranchModeBind` verifying against that root, while a pre-amendment workspace row without `metadata.boundRoot` still roots at the canonical root (the compatibility fallback pinned discriminatingly, not by accident of both values coinciding — the fixture's bound tree differs from the canonical root).
  - **Acceptance:** acceptance criteria 4 and 5 hold end-to-end, and acceptance criteria 1-3 are re-asserted unchanged against the new identity value.
  - **Spec coverage:** Spec-009 line 130 (AC4 — attaching from a linked worktree and from the main checkout resolve to one canonical root; N working trees yield ONE repo mount while each workspace keeps its own execution root), Spec-009 line 131 (AC5 — nonexistent path, unusable git metadata, missing or one-way registration, and non-regular pointer file each fail attach explicitly), Spec-009 line 50 (attach defaults: one repo mount + one default workspace), Spec-009 §Detach Semantics (V1 Definition) (re-attach is per-repository; detach archives dependent workspaces), Spec-009 §Local Trust Envelope (V1 Definition) (run handoff re-verifies user-supplied roots — membership loss blocks the run and transitions the workspace to `stale`; daemon-provisioned roots exempt by provenance), Spec-009 §Repo Identity And Common-Directory Keying (V1 Definition) (attach persists the derived identity as the metadata anchor), Spec-010 §Turn-Boundary Snapshots (branch mode's execution root is the workspace's bound checkout — the sentence corrected by this PR's same-swap Spec-010 amendment)
  - **Verifies invariant:** I-009-15, I-009-16, I-009-5, I-009-3
  - **Consumes:** `openDatabase` ← Plan-001 Phase 3 (shipped); `RepoMountService` / `WorkspaceService` / `workspace-projector` ← Plan-009 Phase 2 T2.3/T2.4/T2.5 (shipped); resolver output shape ← T-009-2B-1 (same phase); envelope admission ← T-009-2B-3 (same phase); `repo.already_attached` carrier ← Plan-009 Phase 1 T1.4 (shipped); `ExecutionRootService` branch-mode arm ← Plan-010 Phase 2 (shipped; EXTEND consented via CP-009-9 / Plan-010 CP-010-14)

### Phase 3 — Client SDK + `repo.*` IPC namespace

**Goal.** Ship both halves of the wire surface: the daemon-side `repo.*` JSON-RPC namespace handlers under `packages/runtime-daemon/src/ipc/handlers/` (per the Plan-002 `presence.*` precedent NS-26 and the §5 substrate-vs-namespace carve-out) and the typed SDK surface at `packages/client-sdk/src/repoClient.ts`. Phase 3 also owes the D-009-5 layer-2 background refresh — the daemon-owned periodic re-probe of attached mounts (`Spec-009 §Default Behavior`: "Repo metadata defaults to background refresh through daemon-owned git services"; `Spec-009 §Repo Mount Health (V1 Definition)` names the same refresher for mount health) — deferred out of Phase 2 because no idle scheduler exists in the package at Phase-2 time (T2.5, PR #292). None of T3.1–T3.7 as written covers it, so the Phase-3 dispatch DAG must add the owning task row before dispatch; the on-read probe floor keeps `Spec-009 §Fallback Behavior` satisfied until it lands. Phase 3's composition root additionally owes the handle-identity constraint recorded on the Phase-2 service deps: `RepoMountService`, `WorkspaceService`, and the `EventLogService` behind their shared emitter must be constructed over ONE `openDatabase` handle — `transactionalPrelude` atomicity (I-009-9) holds only within a single connection, and a two-handle composition breaks it silently — and the wiring task adds a structural identity guard when it lands (the guard needs the append path's cooperation, an EXTEND of the Plan-006-owned `event-log-service.ts` that Phase 2 was not authorized to make).

**Preconditions.**

- [x] Plan-009 Phase 1 merged (contracts — Zod schemas for the six interface pairs) — merged 2026-07-26 via PR #250 (squash 0ed3c7f)
- [x] Plan-009 Phase 2 merged (RepoMount/workspace services + execution-mode capability projections) — merged 2026-08-05 via PR #292 (squash 7531bd5)
- [x] `repo.*` method strings ratified — api-payload-contracts.md §Repo Method-Name Registry (Tier 6) (D-009-1)
- [x] BL-142 landed — deployed registry method-name regex conformed to the Tier-1 ratified canonical (the deployed tail class rejects camelCase; without the fix, `repo.mountRead`-class registrations throw at daemon boot)
- [x] BL-143 landed — `DaemonDomainError` projection branch in `mapJsonRpcError` (without it, T3.6's wire assertions observe anonymous `-32603` errors instead of the ratified `repo.*` codes; surfaced by the Tier-6 Plan-010 walk)
- [x] `RepoMountReadResponse` amended shape ratified — `canonicalRoot` + `health` (D-009-2, D-009-7)
- [x] `repo.*` error codes ratified — error-contracts.md §Repo (D-009-3) — gates T3.6
- [x] `RepoDetach` contract + cascade semantics ratified — `Spec-009 §Detach Semantics (V1 Definition)` (D-009-6) — gates T3.7
- [ ] Phase 2B merged — the §Preconditions box `Repo-identity resolver amendment landed` (2026-08-16). Phase 3 is the first surface that exposes attach and bind beyond the daemon's own service layer, so shipping it over the pre-amendment identity would publish per-working-tree repo identity as observable SDK/CLI behavior and make the Rollout-step-2 verification of acceptance criterion 4 unrunnable.

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the boxes above (preflight Gate 5; PR #251
  # round 2 — the plural prose header is not a recognized gate form).
  - { type: plan_phase, plan: 009, phase: 1, status: merged }
  - { type: plan_phase, plan: 009, phase: 2, status: merged }
  - { type: bl_closed, ref: 142 }
  - { type: bl_closed, ref: 143 }
  # 2026-08-16 repo-identity amendment: the named scoped box is the enforced
  # form of the Phase 2B dependency. The no-argument walk is numeric-only and
  # resolves PAST the 2B supplement to this phase, so without this entry the
  # walk would select Phase 3 across the unmet dependency.
  - { type: precondition_box_checked, box: "Repo-identity resolver amendment landed" }
```

#### Tasks

- **T3.1 — Daemon mutation handlers: `repo.attach` + `repo.workspaceBind` binder files.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/repo-attach.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/repo-workspace-bind.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND — barrel re-exports `registerRepoAttach`/`RepoAttachDeps`, `registerRepoWorkspaceBind`/`RepoWorkspaceBindDeps`), `packages/runtime-daemon/src/workspace/workspace-errors.ts` (CREATE — the mechanical carrier hoist below) + `packages/runtime-daemon/src/workspace/workspace-service.ts` (EXTEND — replace the carrier definitions with imports)
  - **Spec coverage:** Spec-009 line 40 (explicit attach creates a durable RepoMount), Spec-009 line 63 (RepoAttach accepts local path + session id + owning node), Spec-009 line 43 (workspace binding is explicit and resolves one concrete execution root), Spec-009 line 65 (WorkspaceBind accepts mount + intended execution mode), Spec-009 line 44 (canonical execution-mode taxonomy — enforced by the contracts `ExecutionMode` Zod enum at the registry validation boundary)
  - **Verifies invariant:** I-009-10
  - **Consumes:** `MethodRegistry` + `registry.register(method, paramsSchema, resultSchema, handler, {mutating})` ← Plan-007-partial substrate (shipped Tier 1; `packages/runtime-daemon/src/ipc/registry.ts`, `packages/contracts/src/jsonrpc-registry.ts`); `RepoAttachRequestSchema`/`RepoAttachResponseSchema` + `WorkspaceBindRequestSchema`/`WorkspaceBindResponseSchema` ← this plan Phase 1 (`packages/contracts/src/repo.ts`); attach/bind domain callbacks (deps-closure pattern) ← this plan Phase 2 (`repo-mount-service.ts`, `workspace-service.ts`); `repo.attach` + `repo.workspaceBind` strings ← §Repo Method-Name Registry (D-009-1); camelCase registration ← BL-142 (§Precondition)
  - Note: one binder file per method, `register{Method}(registry, deps)` + exported `{Method}Deps` interface, per the shipped `presence-read.ts` exemplar (NS-26). Both register `mutating: true` (state-changing; gated by the substrate's pre-handshake mutating-op gate). Handlers are thin delegators: domain validation (canonical-root resolution, trust-envelope rejection) lives in the Phase 2 services; thrown domain errors surface through `mapJsonRpcError` per the substrate's sanitized-error invariant. This task also owns the mechanical hoist of the four `workspace.*` error carriers out of `workspace-service.ts` into `workspace-errors.ts` (definitions relocated verbatim, service imports them; T2.4 shipped them in-module because its Files row carried no carrier file — 2026-08-05 T2.4 §Notes entry), because this task's binders are the first consumers that surface those carriers through `mapJsonRpcError` and the relocation belongs with its first out-of-module consumer.

- **T3.2 — Daemon query handlers: `repo.mountRead` + `repo.executionModeCapabilitiesRead` + `repo.workspaceList` binder files.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/repo-mount-read.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/repo-execution-mode-capabilities-read.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/repo-workspace-list.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND), `packages/runtime-daemon/src/workspace/workspace-projector.ts` (EXTEND — the workspace-scoped capability narrowing below)
  - **Spec coverage:** Spec-009 line 64 (RepoMountRead exposes canonical root + VCS metadata + current health), Spec-009 line 66 (capabilities read exposes currently-valid execution modes), Spec-009 line 57 (capability gap exposed explicitly rather than silently substituting a mode), Spec-009 line 67 (WorkspaceList exposes workspace health and current binding state)
  - **Verifies invariant:** I-009-10
  - **Consumes:** `MethodRegistry` ← Plan-007-partial substrate (shipped Tier 1); `RepoMountReadRequestSchema`/`ResponseSchema`, `WorkspaceExecutionModeCapabilitiesReadRequestSchema`/`ResponseSchema`, `WorkspaceListRequestSchema`/`ResponseSchema` ← this plan Phase 1; mount-read/capability/list projections ← this plan Phase 2 (`workspace-projector.ts` + services); `repo.mountRead`/`repo.executionModeCapabilitiesRead`/`repo.workspaceList` strings ← D-009-1
  - Note: all three register `mutating: false` (pure projection reads continue working for `pre`/`done-incompatible` connections, matching the `presence.read` read-only-continues posture). The capabilities response is the wire surface for ADR-006's four-mode taxonomy gaps (e.g. non-git mounts restrict to `read-only`). This task also owns the WORKSPACE-scoped arm of the capabilities read (`workspaceId` in the exactly-one request union — "what may THIS workspace do now", per the T1.3 contract row): it EXTENDS `workspace-projector.ts` to narrow writable modes for a `stale` workspace (`Spec-009 §Fallback Behavior`), routing the narrowing through the projector's per-mode verdict shape — a reason per restricted mode, never a filtered `availableModes` — so I-009-8 holds in every scope; T2.5's module header records the same binding rule, and the projector's mount-scoped internals stay unexported until this task consumes them (PR #292 T2.5 round).

- **T3.3 — `repoClient.ts` typed SDK surface (daemon transport).**
  - **Files:** `packages/client-sdk/src/repoClient.ts` (CREATE — `RepoClient` interface + `createDaemonRepoClient(client: JsonRpcClient): RepoClient` with `attach`, `mountRead`, `workspaceBind`, `executionModeCapabilitiesRead`, `workspaceList`, `detach`), `packages/client-sdk/src/index.ts` (EXTEND — barrel `export * from "./repoClient.js"`)
  - **Spec coverage:** Spec-009 line 63 (attach), Spec-009 line 64 (mountRead), Spec-009 line 65 (workspaceBind), Spec-009 line 66 (executionModeCapabilitiesRead), Spec-009 line 67 (workspaceList), Spec-009 §Detach Semantics (V1 Definition) (detach), Spec-009 line 75 (health/git metadata are daemon-owned projection state, not client cache — the client derives nothing)
  - **Verifies invariant:** I-009-11
  - **Consumes:** `JsonRpcClient.call<P, R>` transport ← Plan-007-partial SDK substrate (shipped Tier 1 via [GitHub PR-#19](https://github.com/Sawmonabo/ai-sidekicks/pull/19); `packages/client-sdk/src/transport/jsonRpcClient.ts`); request/response Zod schemas ← this plan Phase 1 (`@ai-sidekicks/contracts`); `repo.*` method strings ← D-009-1; daemon methods ← this plan Phase 3 T3.1/T3.2/T3.7
  - Note: factory naming per the shipped convention (`createDaemonSessionClient` / `createDaemonMembershipClient` / `createDaemonRuntimeNodeClient`). Deliberately **no** `createControlPlaneRepoClient` sibling — repo mounts/workspaces are node-local filesystem state with no control-plane tRPC procedures; the file header documents this as the SDK's first daemon-only surface. Method-name strings authored locally as consts per the `runtimeNodeClient.ts` "wire strings authored locally" pattern. No subscribe path (none of the six methods streams), so no `LocalSubscriptionConsumer` import.

- **T3.4 — Daemon handler registration + dispatch round-trip tests.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/repo-handlers.test.ts` (CREATE — combined file per the `session-handlers.test.ts` precedent)
  - **Spec coverage:** Spec-009 line 123 (no client-only binding without daemon validation — assert invalid `WorkspaceBind` params are rejected by the registry BEFORE the deps callback runs, i.e. the callback is never invoked on schema-invalid input), Spec-009 line 127 (AC1 — attach round-trip response carries `repoMountId` + `canonicalRoot` from the deps result, asserting the durable-mount-with-canonical-root contract at the wire), Spec-009 AC2 (line 128 — `repo.workspaceList` round-trip returns entries spanning two mounts of one session supplied by the deps fixture)
  - **Verifies invariant:** I-009-10
  - **Consumes:** `MethodRegistryImpl` test harness pattern ← Plan-007-partial (shipped Tier 1; `registry.test.ts` + `handlers/__tests__/` precedents); registered binders ← T3.1/T3.2
  - Note: concrete assertions per method — (1) `register` is called with the exact ratified string (literal equality against the D-009-1 table); (2) duplicate registration throws (substrate inheritance); (3) schema-validates-before-dispatch: malformed params (missing `sessionId`, out-of-enum `executionMode`) produce the substrate's invalid-params error and the deps spy records zero calls; (4) `mutating` flags: `true` for attach/workspaceBind/detach, `false` for the three reads; (5) deps round-trip: parsed params reach the callback typed, callback result is schema-validated outbound.

- **T3.5 — SDK integration tests.**
  - **Files:** `packages/client-sdk/test/repoClient.integration.test.ts` (CREATE — per the `membershipClient.integration.test.ts` / `runtimeNodeClient.integration.test.ts` location convention)
  - **Spec coverage:** Spec-009 AC2 (line 128 — workspaceList surfaces multiple mounts + multiple bound workspaces of one session through the SDK unmodified), Spec-009 AC3 (line 129 — for a non-git mount fixture, executionModeCapabilitiesRead returns the reduced `availableModes` set with the gap explicit in `restrictions`; the SDK does not pretend git-only modes are available), Spec-009 line 75 (pass-through never-mask — every response field equals the daemon-supplied payload byte-for-byte; the client computes no health, no capability, no canonical root)
  - **Verifies invariant:** I-009-11
  - **Consumes:** `JsonRpcClient` + in-process transport test double ← Plan-007-partial SDK substrate (shipped Tier 1); `createDaemonRepoClient` ← T3.3; contracts schemas ← this plan Phase 1
  - Note: assert outbound frames carry the ratified method strings + schema-valid params; assert schema-invalid daemon responses throw the SDK's validation error (the Zod layer is load-bearing in both directions, per the substrate convention).

- **T3.6 — Typed error-mapping round-trip tests for the spec-named failure modes.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/repo-handlers.test.ts` (EXTEND), `packages/client-sdk/test/repoClient.integration.test.ts` (EXTEND)
  - **Spec coverage:** Spec-009 line 45 (trust-envelope / path-traversal rejection — deps callback throwing the trust-envelope domain error surfaces on the wire as the ratified `repo.outside_trust_envelope` code, sanitized per the substrate error-mapping invariant), Spec-009 line 58 (canonical-root-resolution failure — attach fails explicitly with `repo.root_resolution_failed`; assert no mount-shaped success payload is fabricated)
  - **Verifies invariant:** I-009-10
  - **Consumes:** `mapJsonRpcError` ← Plan-007-partial substrate (shipped Tier 1; `packages/runtime-daemon/src/ipc/jsonrpc-error-mapping.ts`); `DaemonDomainError` projection branch ← BL-143 (§Preconditions — tracked-not-satisfied until the Plan-007-owned conformance PR lands); `repo.*` error codes ← error-contracts.md §Repo (D-009-3); thrown domain-error classes ← this plan Phase 1 T1.4 / Phase 2
  - Note: the non-git path is asserted as NOT an error (attach succeeds with the fallback-capability mount per `Spec-009 §Fallback Behavior`) — a negative-control test pinning the deliberate no-code decision.

- **T3.7 — `repo.detach` handler + SDK method + cascade round-trip tests.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/repo-detach.ts` (CREATE), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND), `packages/client-sdk/src/repoClient.ts` (EXTEND — `detach` method), `packages/runtime-daemon/src/ipc/handlers/__tests__/repo-handlers.test.ts` (EXTEND), `packages/client-sdk/test/repoClient.integration.test.ts` (EXTEND)
  - **Spec coverage:** Spec-009 §Detach Semantics (V1 Definition) (RepoDetach accepts a repo mount id and transitions the mount to detached without deleting the durable record; detach refused with repo.detach_conflict while any dependent workspace is busy; cascade archives dependent workspaces and the response carries archivedWorkspaceIds; detached is terminal and re-attach creates a new mount row)
  - **Verifies invariant:** I-009-10
  - **Consumes:** `RepoDetachRequestSchema`/`RepoDetachResponseSchema` ← this plan Phase 1 T1.2 (D-009-6); detach domain callback ← this plan Phase 2 T2.3; `repo.detach` string ← D-009-1; `repo.detach_conflict` code ← D-009-3
  - Note: binder registers `mutating: true`. Round-trip tests: detach happy path returns `state 'detached'` + the `archivedWorkspaceIds` the deps fixture archived; busy-workspace fixture surfaces `repo.detach_conflict` on the wire; SDK pass-through asserts the response verbatim. No renderer surface exists for detach (`Spec-009 §Detach Semantics (V1 Definition)`) — deliberate, not an omission.

### Phase 4 — Desktop repo attach & workspace-binding UI

**Goal.** Ship the `repo-attach/` renderer views as thin projections over the `window.sidekicks` preload bridge: explicit attach flow, workspace list with stale visibility, bind + capabilities surface, with mock-bridge component tests.

**Preconditions.**

- [ ] Plan-009 Phase 3 merged (`repo.*` handlers + SDK; the views' wire strings and shapes are final)
- [ ] Phase 2B merged — the §Preconditions box `Repo-identity resolver amendment landed` (2026-08-16), carried onto this phase because the Gate-5 entry is phase-scoped and a box declared on Phase 3 alone would let the auto-walk select Phase 4 past it
- [x] `repo.*` method strings ratified — D-009-1
- [x] `Spec-023 §Preload Bridge Contract` path-display amendment ratified (this audit; co-recorded for the Plan-023 Tier-8 audit NS-20) — display-only path VALUES inside daemon payload data are data, not capabilities; gates the `canonicalRoot`/`fsRoot` display slices of T4.1/T4.2/T4.3

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the phase-dependency box above (preflight Gate 5;
  # PR #251 round 2 — the plural prose header is not a recognized gate form).
  - { type: plan_phase, plan: 009, phase: 3, status: merged }
  # 2026-08-16 repo-identity amendment: carried onto every phase the scoped box
  # gates, per the Gate-5 rule that a box declared on one phase alone lets the
  # auto-walk select its successor past the unmet dependency.
  - { type: precondition_box_checked, box: "Repo-identity resolver amendment landed" }
```

#### Tasks

- **T4.1 — Repo attach view (click-triggered explicit attach).**
  - **Files:** `apps/desktop/src/renderer/src/repo-attach/RepoAttachView.tsx` (CREATE)
  - **Spec coverage:** Spec-009 line 40 (attach is an explicit action creating a durable RepoMount), Spec-009 line 63 (request carries local path + session id + owning runtime node), Spec-009 line 41 (canonical root rendered as the resolved value, distinct from the user-entered path), Spec-009 line 58 (failed canonical-root resolution renders as an explicit failure), Spec-009 line 127 (AC1 — resolved branch renders the repo mount's canonical-root metadata)
  - **Verifies invariant:** I-009-12
  - **Consumes:** `repo.attach` ← this plan Phase 3 (daemon `repo.*` namespace handler + SDK; wire string per D-009-1); `RepoAttachRequest` / `RepoAttachResponse` / `RepoMountId` / `RepoMountState` ← this plan Phase 1 (`packages/contracts/src/repo.ts`, type-only imports); `SessionId` ← Plan-001 Phase 2 (shipped Tier 1); `NodeId` ← Plan-003 (shipped — `packages/contracts/src/node-id.ts`, the declaring leaf `runtime-node.ts` re-exports); `window.sidekicks.daemon.call` ← Plan-023-partial Tier 1 (shipped stub — throws `NotImplementedAtTier1Error` until Plan-023 Tier 8 per CP-009-6); ambient `window.sidekicks` type ← Plan-002 Phase 6 T6.0 (shipped — `apps/desktop/src/renderer/src/sidekicks-bridge.d.ts`)
  - Note: click-triggered `idle | pending | resolved | rejected` register (shipped AttachFlow/InviteAcceptView precedent — mutation views start `idle`, never fire on mount). `sessionId` and `nodeId` arrive as branded props (prop-contract posture of NodeRoster/AttachFlow — the renderer is untrusted and does not discover the local node; a future Plan-023 router supplies them). The local path is a controlled text input — Spec-009's own "user-entered path" framing (`Spec-009 §Required Behavior`); the `native.showOpenDialog` picker upgrade is Tier-8 territory (`OpenDialogOptions`/`OpenDialogResult` are empty Tier-1 stub interfaces and the FilePathRef token shape is a Plan-023 Tier-8 decision — do NOT bind the dialog at this tier). Resolved branch renders `RepoAttachResponse` verbatim — `repoMountId`, `state`, `vcsType` (verbatim string; `"none"` literal per D-009-4), `canonicalRoot` (display-only path value per the ratified Spec-023 amendment), `defaultWorkspaceId` — with `data-mount-state` / `data-vcs-type` attributes for smoke selectors. Rejected branch renders the normalized wire-error envelope (`code` as `Error.name`, AttachFlow `normalizeAttachError` precedent); the Tier-1 sync-throw is funneled through the async-IIFE `try/catch` (SessionBootstrap precedent). DaemonMethod brand cast documented once per call site (participant-roster precedent). Type-only `@ai-sidekicks/contracts` imports; no `electron` / `node:*` / `@ai-sidekicks/client-sdk` (renderer-untrusted boundary, `apps/desktop/eslint.config.mjs`).

- **T4.2 — Workspace list view (mount-triggered health/binding projection).**
  - **Files:** `apps/desktop/src/renderer/src/repo-attach/WorkspaceListView.tsx` (CREATE)
  - **Spec coverage:** Spec-009 line 67 (WorkspaceList exposes workspace health and current binding state), Spec-009 line 42 (multiple repo mounts in one session — set render across distinct repoMountIds), Spec-009 line 128 (AC2 — multiple mounts and multiple bound workspaces all render), Spec-009 line 59 (a stale workspace renders visibly with write-runs-blocked-until-repair labeling), Spec-009 line 75 (health is daemon-owned projection state — the view re-reads, never caches or derives)
  - **Verifies invariant:** I-009-13
  - **Consumes:** `repo.workspaceList` ← this plan Phase 3 (D-009-1); `WorkspaceListRequest` / `WorkspaceListResponse` / `WorkspaceId` / `WorkspaceState` / `ExecutionMode` ← this plan Phase 1; `workspace.stale` + `workspace.ready` daemon event channel ← `Spec-006 §Repo, Workspace, and Worktree Lifecycle (session_lifecycle)` (`workspace.stale` / line 275 `workspace.ready`) with daemon-side emitters from this plan Phase 2 — subscribed as OPAQUE change-signals that re-invoke the snapshot read (payload never decoded; the param-less `daemon.subscribe` placeholder is a pinned Spec-023 gap, so scoping rides the re-read's `{ sessionId }` request); `window.sidekicks.daemon.call` + `daemon.subscribe` + `Unsubscribe` ← Plan-023-partial Tier 1 (shipped stub; live backend ← Plan-023 Tier 8 per CP-009-6)
  - Note: mount-triggered `loading | loaded | error` register; subscribe-BEFORE-initial-read ordering + effect-scoped `cancelled` flag + monotonic request-sequence out-of-order guard + same-session no-flicker contract + `sessionId`-change render-phase reset — all verbatim NodeRoster/ParticipantRoster postures. One `<li>` per workspace rendering `id`, `repoMountId`, `executionMode`, `state` verbatim (and `fsRoot` as a display-only path value per the ratified Spec-023 amendment), with `data-workspace-state` / `data-execution-mode` / `data-repo-mount-id` attributes; a `stale` row additionally renders the write-blocked label (I-009-13). NO `.filter(...)` by state. Grouping by `repoMountId` is visual only — the view derives no mount metadata.

- **T4.3 — Workspace bind view + execution-mode capabilities surface (+ subtree barrel).**
  - **Files:** `apps/desktop/src/renderer/src/repo-attach/WorkspaceBindView.tsx` (CREATE), `apps/desktop/src/renderer/src/repo-attach/ExecutionModeCapabilities.tsx` (CREATE), `apps/desktop/src/renderer/src/repo-attach/index.ts` (CREATE)
  - **Spec coverage:** Spec-009 line 43 (binding is explicit and resolves to one concrete execution root), Spec-009 line 65 (bind carries the intended execution mode from the canonical set), Spec-009 line 66 (capabilities read exposes currently-valid modes for the mount), Spec-009 line 57 (capability gap rendered explicitly — restrictions verbatim, never silently substituted), Spec-009 line 44 (canonical four-mode taxonomy is the only mode vocabulary rendered), Spec-009 line 46 (non-git workspaces render with reduced git-aware capabilities), Spec-009 line 129 (AC3 — non-git workspaces usable without pretending to support git-only features), Spec-009 line 51 (newly attached default read-only context rendered until a writable mode is explicitly selected), Spec-009 line 116 (binding stays explicit even when the session has a single repo mount)
  - **Verifies invariant:** I-009-14
  - **Consumes:** `repo.executionModeCapabilitiesRead` + `repo.workspaceBind` ← this plan Phase 3 (D-009-1); `WorkspaceExecutionModeCapabilitiesReadRequest` / `WorkspaceExecutionModeCapabilitiesReadResponse` / `WorkspaceBindRequest` / `WorkspaceBindResponse` / `ExecutionMode` / `WorkspaceState` ← this plan Phase 1; `window.sidekicks.daemon.call` ← Plan-023-partial Tier 1 (shipped stub; live ← Plan-023 Tier 8 per CP-009-6)
  - Note: two components. `ExecutionModeCapabilities` is presentational and bridge-free (the shipped `CapabilityDeclaration` decomposition precedent): renders `availableModes`, `defaultMode`, and per-mode `restrictions` reasons verbatim — including the space-containing `"ephemeral clone"` token exactly as the wire carries it. `WorkspaceBindView` composes it: mount-triggered capabilities read (`loading | loaded | error`) feeds a mode `<select>` constrained to EXACTLY `availableModes` (I-009-14 — no synthesized options), then a click-triggered explicit Bind action (`idle | pending | resolved | rejected` for the mutation leg) calls `repo.workspaceBind` with `{ repoMountId, executionMode }` and renders `WorkspaceBindResponse` verbatim (`workspaceId`, `executionMode`, `state`; `fsRoot` as display-only path value) — the daemon's verdict is the rendered truth; the view computes no execution root client-side (`Spec-009 §Pitfalls To Avoid` reflection). `repoMountId` arrives as a branded prop. Boundary note: bind-time INTENDED-mode selection lives here; run-time mode SWITCHING of an existing workspace is Plan-010's `execution-mode-picker/` surface (cross-plan-dependencies.md §2 renderer row) — do not extend this view there. `index.ts` barrels the three views (runtime-node-attach barrel precedent); App-shell composition is deliberately NOT performed (Plan-023 owns the shell — shipped views are exported, not mounted, per the `session-members/` / `runtime-node-attach/` precedent and CP-009-6).

- **T4.4 — Renderer component tests (mock-bridge suite).**
  - **Files:** `apps/desktop/src/renderer/src/repo-attach/__tests__/RepoAttachView.test.tsx` (CREATE), `apps/desktop/src/renderer/src/repo-attach/__tests__/WorkspaceListView.test.tsx` (CREATE), `apps/desktop/src/renderer/src/repo-attach/__tests__/WorkspaceBindView.test.tsx` (CREATE)
  - **Spec coverage:** Spec-009 AC1 (line 127 — resolved attach branch renders canonicalRoot from the mocked RepoAttachResponse), Spec-009 line 128 (AC2 — fixture of 2 mounts / 3 workspaces renders every row; set-render, no filter), Spec-009 line 129 (AC3 — non-git fixture renders verbatim vcsType + restricted modes without any git-only mode option), Spec-009 line 59 (stale fixture row stays rendered with data-workspace-state="stale" + write-blocked label), Spec-009 line 57 (restriction reasons render verbatim; the rendered markup contains no mode absent from availableModes), Spec-009 line 40 (mock daemon-call spy not invoked on mount of the attach view; invoked exactly once after the explicit click), Spec-009 line 43 (same explicit-click assertion for the bind mutation)
  - **Verifies invariant:** I-009-12, I-009-13, I-009-14
  - **Consumes:** vitest `renderer` project ← shipped (apps/desktop/vitest.config.ts — happy-dom, `globals: true`, include glob auto-discovers these files); per-suite mock-bridge install/teardown pattern ← Plan-002 Phase 6 T6.3 (shipped — duplicated per suite per the standing directive); `@testing-library/react` ← shipped renderer-test devDep; `NotImplementedAtTier1Error` (value import) ← Plan-023-partial (shipped; resolves to TS source via the `@ai-sidekicks/source` condition); repo contract types ← this plan Phase 1
  - Note: each suite also carries (a) the bridge-projection source-text assertion via `import.meta.glob(..., { query: "?raw" })` — the view sources never import `electron`, `node:*`, runtime-daemon, control-plane, or `@ai-sidekicks/client-sdk` (CP-002-5 idiom; the client-sdk ban is by-convention until the Tier-8 lint hoist) — and (b) the Tier-1 stub-throw path: a mock bridge whose method throws `NotImplementedAtTier1Error` synchronously must land every view in its rendered error/rejected branch (the production-observable path at this tier), never an unhandled rejection. The component-test posture deliberately follows the Plan-002 shipped precedent and the Plan-007 R3 audit precedent (tests now, against the per-suite mock) rather than a BL-131-style deferral (that entry's two-client E2E leg remains Tier-8-gated and is NOT claimed here).

- **T4.5 — Manual multi-repo desktop smoke — Tier-8-gated disposition record.**
  - **Files:** none (verification-disposition task; recorded in `Plan-009 §Progress Log` Notes on the Phase 4 PR)
  - **Spec coverage:** Spec-009 AC2 (line 128 — attaching multiple repositories to one session, exercised through the desktop UX)
  - **Verifies invariant:** none
  - **Consumes:** live `window.sidekicks` bridge backend ← Plan-023 Tier 8 remainder (tracked-not-satisfied — CP-009-6; the views render but every wire call throws until then)
  - Note: the plan-level "Manual verification of attaching multiple repositories to one session" splits per §Test And Verification Plan — its SDK/CLI leg runs at Tier 6 against Phase 3 (Rollout step 2) and belongs to the Phase 3 verification surface; THIS task is the desktop-renderer leg, impossible until the live bridge ships. Record the Plan-003 T5.4 disposition verbatim-pattern (BL-131 shipped-note precedent): "the live desktop leg is impossible pre-Tier-8; owner = user at Tier 8," on the Phase 4 PR rather than silently dropping the row.

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped:
  - phase: 1
    task: [T1.1, T1.2, T1.3, T1.4, T1.5, T1.6]
    pr: 250
    sha: 0ed3c7f
    merged_at: 2026-07-26
    files:
      - docs/architecture/cross-plan-dependencies.md
      - docs/plans/003-runtime-node-attach.md
      - docs/plans/009-repo-attachment-and-workspace-binding.md
      - docs/plans/010-worktree-lifecycle-and-execution-modes.md
      - docs/superpowers/specs/2026-07-01-capability-enhancements-design.md
      - packages/contracts/src/__tests__/repo.test.ts
      - packages/contracts/src/__tests__/session-event.test.ts
      - packages/contracts/src/event.ts
      - packages/contracts/src/index.ts
      - packages/contracts/src/node-id.ts
      - packages/contracts/src/repo.ts
      - packages/contracts/src/runtime-node.ts
      - packages/runtime-daemon/src/ipc/domain-error.ts
      - packages/runtime-daemon/src/node/__tests__/node-event-emitter.test.ts
      - packages/runtime-daemon/src/session/__tests__/migration-race-worker.mjs
      - packages/runtime-daemon/src/session/__tests__/session-service.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/repo-errors.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/repo-root-resolver.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/trust-envelope.test.ts
      - packages/runtime-daemon/src/workspace/repo-errors.ts
      - packages/runtime-daemon/src/workspace/repo-root-resolver.ts
      - packages/runtime-daemon/src/workspace/trust-envelope.ts
    verifies_invariant: [I-009-1, I-009-2, I-009-3, I-009-4]
    spec_coverage:
      [
        "Spec-009 line 44 (canonical execution-mode taxonomy)",
        "Spec-009 line 63 (RepoAttach accepts local path, session id, owning runtime node)",
        "Spec-009 line 64 (RepoMountRead exposes canonical root, VCS metadata, current health)",
        "Spec-009 §Detach Semantics (V1 Definition) (RepoDetach accepts a repo mount id and transitions the mount to detached)",
        "Spec-009 line 65 (WorkspaceBind accepts repo mount or directory root plus intended execution mode)",
        "Spec-009 line 66 (capabilities read exposes currently-valid modes for the bound repo mount or workspace)",
        "Spec-009 line 67 (WorkspaceList exposes workspace health and current binding state)",
        "Spec-009 line 58 (canonical-root resolution failure fails explicitly)",
        "Spec-009 line 45 (rejection of traversal / outside-envelope binding)",
        "Spec-009 §Detach Semantics (V1 Definition) (detach refused with `repo.detach_conflict` while a dependent workspace is busy)",
        "Spec-009 line 41 (canonical root resolution and persistence basis)",
        "Spec-009 line 115 (never assume the user-selected path is the repo root)",
        "Spec-009 line 56 (non-git plain-directory fallback classification)",
        "Spec-009 line 58 (explicit failure on resolution failure)",
        "Spec-009 line 45 (reject path traversal or workspace binding outside the declared local trust envelope)",
        "Spec-009 §Local Trust Envelope (V1 Definition) (containment is symlink-resolved, component-boundary-aware, case-folded on case-insensitive filesystems)",
        "Spec-009 §Local Trust Envelope (V1 Definition) (directory resolved against the mount canonical root and re-checked after symlink resolution)",
      ]
    notes: |
      Full-phase single-PR ship (T1.1-T1.6). Codex rounds 1-4 closed reply-and-resolve (the
      round-1.5 casing claim was refuted by probing, leaving seam-default pins behind); round 5
      timed out with no review under the five-round cap. Four disclosed cross-plan touches per
      this plan's Cross-Plan Amendments section: the node-id.ts extraction, the lane-2 Plan-001
      race-control hardening, comment-only true-ups, and the Plan-010 payload-mechanism
      amendment. NS-38 auto-created in the housekeeping PR.
  - phase: 2
    task: [T2.1, T2.2, T2.3, T2.4, T2.5, T2.6]
    pr: 292
    sha: 7531bd5
    merged_at: 2026-08-05
    files:
      - docs/architecture/schemas/local-sqlite-schema.md
      - docs/plans/009-repo-attachment-and-workspace-binding.md
      - docs/plans/010-worktree-lifecycle-and-execution-modes.md
      - packages/runtime-daemon/src/migrations/0010-repo-workspaces.ts
      - packages/runtime-daemon/src/node/node-event-emitter.ts
      - packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts
      - packages/runtime-daemon/src/session/__tests__/session-service.test.ts
      - packages/runtime-daemon/src/session/migration-runner.ts
      - packages/runtime-daemon/src/workspace/__tests__/repo-mount-service.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/repo-workspace-acceptance.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/repo-workspace-migration.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/workspace-event-emitter.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/workspace-projector.test.ts
      - packages/runtime-daemon/src/workspace/__tests__/workspace-service.test.ts
      - packages/runtime-daemon/src/workspace/repo-mount-service.ts
      - packages/runtime-daemon/src/workspace/workspace-event-emitter.ts
      - packages/runtime-daemon/src/workspace/workspace-projector.ts
      - packages/runtime-daemon/src/workspace/workspace-service.ts
    verifies_invariant: [I-009-5, I-009-6, I-009-7, I-009-8, I-009-9]
    spec_coverage:
      [
        "Spec-009 line 73 (repo mount record persists canonical root, owner node, lifecycle state)",
        "Spec-009 line 74 (workspace record persists execution root, repo association, health)",
        "Spec-006 line 288 (repo.attached)",
        "Spec-006 line 289 (repo.detached)",
        "Spec-006 line 290 (workspace.provisioning)",
        "Spec-006 line 291 (workspace.ready)",
        "Spec-006 line 292 (workspace.stale)",
        "Spec-006 line 293 (workspace.archived)",
        "Spec-009 line 57 (capability gap exposed explicitly, never silent substitution)",
        "Spec-009 line 66 (capabilities read exposes which modes are currently valid)",
        "Spec-009 line 75 (repo health and git metadata are daemon-owned projection state)",
        "Spec-009 line 129 (AC3 — non-git workspaces usable without pretending git-only features)",
        "Spec-009 §Repo Mount Health (V1 Definition) (repo mount health is the daemon-probed reachability projection)",
        "Spec-009 line 43 (binding is explicit and resolves to one concrete execution root before a run)",
        "Spec-009 line 45 (reject path traversal / outside trust envelope)",
        "Spec-009 line 51 (read-only default until a writable mode is explicitly selected)",
        "Spec-009 line 59 (unavailable path -> stale; new write runs blocked until repair)",
        "Spec-009 line 65 (WorkspaceBind accepts repo mount plus intended execution mode)",
        "Spec-009 line 67 (WorkspaceList exposes workspace health and binding state)",
        "Spec-009 line 85 (mode switch re-provisions in place; workspace ID stable)",
        "Spec-009 line 87 (state progression ready -> provisioning -> ready)",
        "Spec-009 line 90 (row keeps id; execution_mode/fs_root updated; state cycles through provisioning)",
        "Spec-009 line 91 (failed switch -> stale with error detail in workspace metadata)",
        "Spec-009 line 128 (AC2 — multiple repo mounts and multiple bound workspaces per session)",
        "Spec-009 line 40 (explicit attach creates a durable RepoMount)",
        "Spec-009 line 41 (canonical root persisted, not only the user-entered path)",
        "Spec-009 line 50 (attach defaults: one repo mount + one default workspace at the main checkout)",
        "Spec-009 line 56 (non-git path binds as plain-directory mount with git features disabled)",
        "Spec-009 line 58 (root-resolution failure fails explicitly)",
        "Spec-009 line 63 (RepoAttach accepts local path, session id, owning runtime node)",
        "Spec-009 line 64 (RepoMountRead exposes canonical root, VCS metadata, current health)",
        "Spec-009 line 127 (AC1 — durable repo mount with canonical root metadata)",
        "Spec-009 §Detach Semantics (V1 Definition) (detach refused with repo.detach_conflict while a dependent workspace is busy)",
        "Spec-009 §Detach Semantics (V1 Definition) (detach archives dependent workspaces and emits the lifecycle events)",
        "Spec-009 line 127 (AC1 — attach yields a durable repo mount with canonical root metadata)",
        "Spec-009 line 128 (AC2 — a session contains multiple repo mounts and multiple bound workspaces)",
        "Spec-009 line 129 (AC3 — non-git directory workspaces usable without pretending git-only features)",
        "Spec-009 line 42 (multiple repo mounts in one session)",
      ]
    notes: |
      Whole-phase PR (T2.1–T2.6 in DAG dispatch order T2.1/T2.2 → T2.5 → T2.4 → T2.3 → T2.6). Codex advisory review unavailable at merge (provider usage limits); required checks ci-gate + docs-corpus-gate green.
```

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

- 2026-06-10 — Tier-6 plan-readiness audit (NS-18): plan rewritten to the audited four-phase Tasks structure; 52 findings adjudicated (13 critical / 27 major / 12 minor); D-009-1..7 ratified, I-009 invariants and CP-009-1..8 minted; the false `Dependencies: None` header replaced with the five-edge substrate enumeration (Plan-001, Plan-003, Plan-006, Plan-007-partial, Plan-023-partial) and the §3 dep-map row replaced to match; api-payload §Repo Method-Name Registry (six `repo.*` strings, D-009-1) ratified; BL-142 escalated (deployed registry camelCase-tail regex drift — Plan-007-owned conformance fix) in the same audit PR.
- 2026-07-25 — PR #250 (T1.5 implementation): `RepoRootResolutionReason` gains a fourth member, `"not_absolute"`, and the resolver gains a matching pre-realpath refusal of non-absolute input. Surfaced by a T1.5 implementation probe: a bare-relative path (`src/workspace`) resolved silently against the DAEMON process's cwd to a plausible wrong root — a guessed root under I-009-2's "never guesses a root" clause, by the same cross-node argument that keeps `~` unexpanded daemon-side (the daemon's cwd/home are not the author's; expansion belongs to the client/CLI layer). Phase C round 1 then found that gate incomplete on Windows: `path.win32.isAbsolute` short-circuits on any leading separator before it looks for a drive, so `\repos\foo` and `/repos/foo` cleared it and would have been completed against the daemon process's CURRENT DRIVE — the same guessed-root defect, reachable with ordinary Windows CLI input on an ADR-019 V1 tier. The gate now requires a COMPLETE parsed root (a win32-scoped root-length rule; POSIX `/` is unaffected), and `node:path` becomes an injected `platformPath` seam with win32-ness derived from the injected module's `sep` rather than `process.platform`, so the Windows branch is exercised on POSIX CI instead of only on a Windows runner. The reason member is unchanged and reads as "does not name one complete location". The T1.4 reason-union enumeration and the T1.5 files/behavior/tests rows above are trued up in the same PR; the wire code stays `repo.root_resolution_failed` (error-contracts.md registers codes, not reason discriminants — no doc change), and the wire schema stays permissive per the `RepoAttachRequest.localPath` admission-posture comment. Phase C round 3 additionally recorded a forward commitment no other governance surface carries: the resolver spawns git by the separator-less name `git`, and on Windows a separator-less name is searched in the SPAWNING process's current directory before `PATH` (libuv `search_path`, `src/win/process.c` — both primary sources, with confirmation dates, are now in this plan's §References, added by the Codex round-1 pass below because a bare file-and-function name is not followable), so a `git.exe` dropped in whatever directory the daemon runs from would execute with daemon privileges — environment scrubbing cannot close it because the search is not environment-driven. The closing seam is the resolver's injectable `gitExecutablePath`, which exists and is settable today; making an ABSOLUTE path its default is gated on Phase 2's daemon-config surface (Phase 1 has none to read one from), so T2.3 carries the dispatch-visible note and a Windows deployment (ADR-019 V1 tier) sets it explicitly until then. The Microsoft source additionally names an operator-side mitigation the plan had not recorded: libuv gates its cwd-first probe on `NeedCurrentDirectoryForExePathW(L"")`, whose return value is determined by the `NoDefaultCurrentDirectoryInExePath` environment variable, so setting that variable closes the cwd-first search for every bare-name spawn in the daemon process — including the `taskkill` one — without waiting on the `gitExecutablePath` default. Codex round 1 then found the resolver TRUSTING what git reported: `RepoRootResolutionReason` gains a FIFTH member, `"root_mismatch"`, and the resolver gains a two-leg verification of the toplevel. The vector is a repository's OWN config file, which no environment strip can reach — `core.worktree` supplied through `GIT_CONFIG_PARAMETERS`, the `GIT_CONFIG_COUNT` family, or `git -c` does NOT move `--show-toplevel`, but the same key in the repository's own config DOES — an asymmetry observed on git 2.50.1, notable because it is the OPPOSITE of what documented config precedence predicts (git-config(1) states the env pairs "will override values in configuration files"; the redirect semantics of a repo-own `core.worktree` and the gitfile shape are themselves documented, all three cited in §References below) — so the setup-timing explanation (command-scope config landing only after repository setup has decided the work tree) is recorded as inference from that observation, not as documented behavior, and `git init --separate-git-dir` leaves a `.git` FILE whose config is reachable without owning the tree it will name. Both shapes were reproduced on git 2.50.1: a SIBLING redirect, which would persist a `canonical_root` the operator never supplied and hand T1.6's `TrustEnvelopeValidator` an envelope over an unrelated tree; and an ANCESTOR redirect, which widens the mount to a tree that merely contains what was attached (`core.worktree=/` being the limit case). Neither leg of the fix subsumes the other — containment refuses the sibling shape (which self-reports honestly when the redirect target is itself a repository) and the re-query refuses the ancestor shape (which the input genuinely sits inside) — and each was verified by neutering the other. The accepted limitation, recorded because it is a real refusal and not an oversight: a repository whose config points `core.worktree` anywhere other than the directory holding its gitfile cannot be attached at all, the dotfiles-style layout being the familiar example; nothing at this layer distinguishes a deliberate redirect from a hostile one, and I-009-1 makes refusing the safe direction. The refusal is always an explicit typed error (I-009-2) and never a `vcsType: "none"` misclassification (I-009-4) — which matters because the verification query in both observed shapes fails with git's not-a-repository verdict, the very verdict that classifies a plain directory on the discovery query. The wire code stays `repo.root_resolution_failed` for the new reason too, on the same error-contracts.md precedent recorded above. Two smaller round-1 fixes ride it: the child-environment strip is rebuilt by case-insensitive omission rather than copy-then-delete, because a Windows environment BLOCK is case-insensitive while the spread copy preserves each inherited key's original spelling, so an inherited `Git_Dir` reached git as `GIT_DIR` past a `delete environment["GIT_DIR"]`; and the line-terminator strip drops the bare LF everywhere but the preceding CR only on win32, because git terminates plumbing output with a bare LF on every platform, so on POSIX — where `\r` is a legal filename character and NTFS forbids it — a `\r` before that LF is the last character of the directory's NAME, and eating it can land on an existing sibling directory rather than merely failing to resolve. A round-1.5 review then reported a macOS casing defect that turned out NOT to exist, recorded here because the investigation left durable pins behind. The claim was that the resolver would refuse a mis-cased attach with `root_mismatch` on default APFS, because the containment comparison is case-sensitive off win32 while `realpath` was believed to preserve the caller's spelling. Probing refuted the premise: `node:fs/promises.realpath`, which both modules already default to, is documented as sharing `fs.realpath.native`'s semantics and returns each component's ON-DISK spelling, so a mis-cased input is normalized before git is asked and both containment operands agree (verified end to end against a real git fixture under a mis-cased parent component; the divergence is real only for `node:fs`'s CALLBACK `realpath` and `realpathSync`, which are a JavaScript walk that preserves the caller's spelling). No code change was warranted, but the seam DEFAULT turned out to be load-bearing and unpinned, so both suites gained a test that attaches (T1.5) and binds (T1.6) through a mis-cased spelling, each gated on a runtime case-insensitivity PROBE rather than on `process.platform` — APFS can be formatted case-sensitive and Linux can mount case-insensitive volumes, so a platform gate would skip the test on one host and fail it on the other. Perturbing both seams to the JavaScript walk fails those two tests and also two pre-existing trust-envelope escape tests, because the walk applies `..` lexically to a symlink's target rather than as the kernel does. The Phase-2 consequence is recorded here because it is not visible from the resolver alone: persisted `canonical_root` values carry the filesystem's own casing, which is what lets D-009-7's `canonical_root`-keyed active-uniqueness index recognize two differently-spelled attaches of one repository as the duplicate they are.
- 2026-07-25 — PR #250 (T1.3 implementation): `WORKSPACE_LAST_ERROR_MAX_LEN` is set at 8192 (the package's error-message class), deliberately generous enough to carry captured git/provisioning output VERBATIM — nothing in the model truncates `metadata.lastError` before it reaches the wire (`workspaces.metadata` carries no length CHECK) and I-009-10 validates responses too, so a tighter cap would make a lawful daemon `repo.workspaceList` response unrepresentable. That choice hands T2.4 a redaction obligation the contract layer cannot discharge: a failing `ephemeral clone` against an authenticated remote can echo a token-bearing remote URL into stderr, which would then persist verbatim into unencrypted SQLite metadata and re-broadcast on every list read. T2.4's CP-009-2 `failReprovision` MUST credential-scrub captured provisioning output before persisting `metadata.lastError` (`Spec-009 §Execution Mode Transitions`). Recorded here because neither the `repo.ts` cap comment nor the commit message survives into T2.4's dispatch brief.
- 2026-07-25 — PR #250 (T1.6 implementation): the trust-envelope validator refuses uniformly — every rejection is the argument-free `TrustEnvelopeViolationError` (`repo.outside_trust_envelope`, HTTP 403), because containment cannot be PROVEN for a path the filesystem declines to resolve and T1.4 gives the module no channel through which a path could reach the wire. That uniformity hands T2.4 two ORDERING obligations on the bind path, neither of which the contract layer can discharge. (i) T2.4 MUST probe execution-root reachability through T2.5's health projection BEFORE calling `validateExecutionRoot`: a root that has vanished — an unplugged volume, a deleted mount root — is an availability condition (`Spec-009 §Fallback Behavior`'s `stale` transition, I-009-7), but `realpath` cannot resolve it, so without the probe an operational outage reaches the client as a security violation carrying no repair affordance. (ii) T2.4 MUST scope its envelope query to `state = 'attached'` and refuse a non-attached or unknown mount id with its own typed error FIRST: admission is component EQUALITY against the attached set, so a `detached` or foreign mount id yields an anchor no envelope member matches and lands on the same 403 — shadowing a 404-class client mistake (`repo.not_found`) under a security code. Both obligations are co-located in the `trust-envelope.ts` module header, at the code they constrain; this entry is the dispatch-durable copy, recorded for the same reason as the T1.3 entry above — the module header does not survive into T2.4's dispatch brief.
- 2026-07-25 — PR #250 (review round 2): the trust-envelope validator now also proves the resolved execution root is a DIRECTORY, through a third injectable seam (`stat`). A regular file inside the mount passes containment, and the returned value's only destination is `workspaces.fs_root` — a root a process is later asked to run inside — so admitting one persists a workspace that can never spawn. Nothing downstream would have caught it: T2.4's bind flow resolves, refuses escapes, and writes, without asking what the path is. The check runs LAST, after containment and the absoluteness backstop, so every pre-existing refusal keeps its own cause; refusal is the same argless carrier, which WIDENS the uniformity residual recorded above rather than adding a new one — a root that vanishes between the `realpath` and the `stat` is reported identically to one that was a regular file all along. Two documentation true-ups ride the same round: this plan's §References gains SQLite and MDN entries covering the snapshot-barrier paragraph's four externally-defined claims (DEFERRED snapshot pinning, `SQLITE_BUSY_SNAPSHOT` on the read→write upgrade, `BEGIN IMMEDIATE` locking at BEGIN, and `Atomics.wait` parking until notify or timeout), and the §3 ownership-map row for Plan-009 now names `packages/contracts/src/node-id.ts` — the declaring leaf — rather than the `runtime-node.ts` re-export, since `repo.ts` must import the leaf directly or the module cycle the relocation broke reforms.
- 2026-07-25 — PR #250 (review round 3): the resolver now proves the outgoing canonical root is OPENABLE FOR ENUMERATION, through a fourth injectable seam (`DirectoryReadabilityProbe`, defaulting to an opendir-and-close probe declared in `trust-envelope.ts` and shared by construction with the validator). The gap was an ordinary POSIX mode: `0111` grants search without read, `realpath` needs only search (step 2 is a TRAVERSABILITY gate, not the readability gate its comment claimed), and git's discovery stats and reads `.git` entries without ever listing the toplevel — so BOTH resolver arms returned a root the daemon cannot enumerate, on which D-009-7's attach flow would build a default read-only workspace that is born unusable while recorded healthy. The probe gates `finish`, the one chokepoint every arm exits through — the git arm matters because a readable nested directory of an unreadable repository root resolves fine and still persists the unreadable ROOT — and probe rejections classify through the existing errno map (`EACCES` ⇒ `not_readable`, a vanished root ⇒ `path_not_found`; no reason-union change, which is what made this a conformance gap against T1.5's own rows (c)/(d) rather than a contract change). The trust-envelope validator had the same class open on the bind side — its round-2 `stat` seam proved the resolved root a DIRECTORY but not readable, so a `0111` descendant of a readable mount root would persist the same broken workspace at bind — and the round unified both modules on the one probe: `stat` is replaced (opendir subsumes the file refusal via `ENOTDIR` and adds the enumerability refusal via `EACCES`), the probe is declared beside the shared comparison helpers and imported by the resolver, and the recorded `resolveDeps` divergence between the two modules shrinks to the resolver's git-execution members. Admission-time only, in both modules: readability drift after attach/bind is T2.5's health projection (D-009-2), not a resolution failure. In the same round the plan's git-mechanism claims were split on citability per the Codex finding: the repo-own-config redirect and the gitfile shape ARE documented (git-config(1), git-init(1) — version-pinned §References entries below), while the setup-timing explanation is NOT — git-config(1) documents the OPPOSITE precedence for the env-borne pairs — so every claim site (this entry's round-1 text above, the resolver header, the strip-list JSDoc, and the suite's redirect-fixture comment) now separates the observation, pinned by a new command-scope mirror control beside the existing redirect negative control, from the inference; the resolver header additionally records why the undocumented asymmetry is not load-bearing — the two-leg verification refuses the redirect even on a future git that honors the documented precedence.
- 2026-07-25 — PR #250 (review round 4): four findings, four fixed. (1) The plain-directory classification now carries a VERDICT-CONSISTENCY gate: git emits its anchored not-a-repository marker not only for genuinely plain directories but for permission-damaged and corrupt repositories — reproduced on git 2.50.1 as a five-shape matrix: a `.git` directory with mode `000` and an EMPTY `.git` directory both emit the exact marker line, and a DANGLING gitfile emits `fatal: not a git repository: <target>`, which the prefix-anchored marker also matches, so all three classified `"none"` and would persist a permission-damaged checkout as a plain mount (an invalid-format gitfile and an unreadable gitfile FILE already routed to `"vcs_error"` via the marker mismatch). The resolver now probes the supplied directory's own `.git` entry through the round-3 readability seam before accepting the fallback — `ENOENT` proves absence and proceeds; success or any other rejection refuses `"vcs_error"`, because a not-a-repository verdict contradicted by visible metadata is not the positive verdict I-009-4's fallback requires. Reason space unchanged, and `"vcs_error"` rather than `"not_readable"` — in the empty-directory and dangling-gitfile shapes nothing is unreadable; the QUERY's answer is what is unusable. Documented residual, same posture as the round-1 dotfiles refusal: a damaged repository attached from a NESTED subdirectory still classifies `"none"` rooted at that subdirectory — git walks up to the damaged metadata while the gate examines only the supplied path; ancestor crawling is refused as unbounded and racy, the subdirectory genuinely enumerates with no visible metadata, and D-009-7's read-only default workspace bounds the harm. (2) The `repo.ts` FORWARD PATH comment promised Plan-010 a third `state` arm this corpus never authorized — Plan-010's T1.1 Files list omits `repo.ts`, the ownership map's five seam classes do not cover it, and the promised `repo.ts` → `worktree.ts` import would re-close the exact eager-cycle class the node-id.ts relocation broke (`worktree.ts` must import `repo.ts` per I-010-1). Resolved by construction: T1.1 now exports the state-parameterized `buildRepoWorkspaceLifecyclePayloadSchema` factory (the canonical schema is its two-vocabulary instantiation; runtime behavior byte-identical), and Plan-010's Phase-1 rows are amended in the same PR to instantiate it over `WorktreeStateSchema` in `worktree.ts` — payload schemas stay in the emitter's domain file per the event-union seam, each family keeps an EXACT state vocabulary (a three-arm union would have let a `workspace.*` payload claim `"merged"`), and no cross-plan file edit ever happens (§Cross-Plan Amendments block below). (3) The T1.3 entry above recorded that nothing in the model truncates `metadata.lastError` — while the wire schema caps it at `WORKSPACE_LAST_ERROR_MAX_LEN` and I-009-10 validates RESPONSES, so one verbose provisioning failure would make every later `repo.workspaceList` response unrepresentable. T2.4's note now binds `failReprovision` to scrub-then-truncate at persist time — scrub FIRST, because truncating first can bisect a secret token so the scrubber's pattern no longer matches it — and the `repo.ts` cap comment records the producer-side bound. (4) The round-3 readability claims split on citability exactly as the git-mechanism claims did: the read-vs-search asymmetry IS documented — POSIX.1-2024 `realpath()` fails `[EACCES]` only for search denial on "a component of the path prefix", so a `0111` FINAL component resolves, while `opendir()` fails `[EACCES]` when "read permission is denied for dirname" and `[ENOTDIR]` when the name is "neither a directory nor a symbolic link to a directory" (both §References entries below) — and the code claim sites now carry the attribution; the remaining leg, that git's own DISCOVERY succeeds on a `0111` root, is not POSIX-specified and stays a version-pinned observation on git 2.50.1, pinned by the suite's git-arm premise controls.
- 2026-08-04 — PR #292 (Phase 2 dispatch true-up): three Tasks-row repairs surfaced by the dispatch-time analysis, applied before any Phase-2 code lands. T2.1's `0NNN` placeholder resolves to `0010` per its own rule (the migration chain stood at 0001–0009 at PR-open), its Files row gains the Plan-001-owned `migration-shape.test.ts` EXTEND with the four-site repair contract in its Note (the §Cross-Plan Amendments PR #292 session-substrate-suites block below is the criterion-(e) disclosure), and T2.3's Consumes names the concrete session-existence read — `SessionService.replay(sessionId)` returning `null` for an event-less session — instead of the bare class name. Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — file-assignment/narrowing true-up with no new invariant, CP row, phase, or Required ADR and no plan-body behavior change; the Tier-6 audit checkbox stands. Census adjudication, recorded because the dispatch analysis initially flagged a README/CLAUDE "56 tables" bump: no bump rides this phase — the figure counts the schema-doc design corpus (`local-sqlite-schema.md` already carries `repo_mounts` + `workspaces` among its 56 `CREATE TABLE` statements, and the control-plane census reading 23 tables against four applied migrations proves the population is the doc, not the applied set), so shipping migration 0010 moves nothing.
- 2026-08-04 — PR #292 (T2.1 implementation): two true-ups surfaced by the T2.1 build, applied before its code commits land. (1) The migration-shape repair contract's site (iv) as first written anchored the parentless-db test at `FORWARD_REFERENCES_DML_ENFORCEMENT_TEST`; that constant titles the FK-DML-enforcement test — site (ii)'s stub-backed fixture, whose `.not.toThrow()` arm requires real parent rows, so re-scoping it onto a version-≤4 handle is unsatisfiable — while the parentless-db test's anchor is `WRITE_INERT_ON_PARENTLESS_DB_TEST`. Sites (ii) and (iv) now carry the correct anchors, with the `SQLITE_ERROR`-class-vs-FK-class distinction jointly pinned across the two tests (each test's comment naming the other half); both constants stay exported and live. (2) The 0010 landing trips a SECOND Plan-001-owned suite the contract had not enumerated: `session-service.test.ts` pins the applied version set by exact equality at four sites — three `schema_version` arrays plus the concurrent-boot IMMEDIATE verifier, whose assertion message and re-derive-rather-than-increment per-version atomicity walk are part of the pin. Same planted-census class, same lane-2 routing, same dedicated `test(daemon)` commit; T2.1's Files row gains the file as repair-contract site (v) and the §Cross-Plan Amendments PR #292 session-substrate-suites block now names both suites. Status: unchanged (`approved`) — same file-assignment/narrowing class as the dispatch true-up above.
- 2026-08-05 — PR #292 (T2.5 review round): the following plan-record repairs from the per-task review, none moving the shipped diff. (1) `Spec-009 §Default Behavior`'s background-refresh mandate ("Repo metadata defaults to background refresh through daemon-owned git services") was cited by NO task row plan-wide, and Phase 3 — where D-009-5 sends the layer-2 wiring when the scheduler seam is absent at Phase-2 time, which it is — contained no task that re-probes anything (T3.1–T3.7 are all IPC/SDK/error-mapping surface). The Phase 3 Goal block now records the obligation and requires the Phase-3 dispatch DAG to add the owning task row before dispatch; no task row is added here, so the record stays ahead of the task graph rather than growing it. (2) The WORKSPACE-scoped arm of `repo.executionModeCapabilitiesRead` ("what may THIS workspace do now", the T1.3 exactly-one union) had no named owner between T2.4's service and the Phase-3 handlers; T3.2 now owns it, EXTENDING `workspace-projector.ts` through the same per-mode verdict shape so I-009-8's no-silent-substitution holds in every scope — T2.5's module header carries the matching binding rule, and the projector's mount-scoped internals stay deliberately unexported until T3.2 consumes them. (3) I-009-7's Verification clause attributed "transition persisted" to the T2.5 projection test, which T2.5's own Note contradicts (the service persists the `markStale` transition the projector derives); the clause now reads derivation-half at T2.5, persistence at T2.4. A fourth touch rides the same round: the PR #292 (2026-08-05) §Cross-Plan Amendments block below, the criterion-(e) disclosure for the comment-only `node-event-emitter.ts` rider the review's claim-class sweep surfaced — with the consequential wording repairs it forced, disambiguating the three pre-existing in-plan references to "the §Cross-Plan Amendments PR #292 block" (unique when written, ambiguous once this round added a second) to name the session-substrate-suites block they mean. Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — record-keeping/narrowing true-ups plus a disclosure block, with no new task, invariant, CP row, phase, or Required ADR and no plan-body behavior change; the Tier-6 audit checkbox stands. Census note: the Phase-1 shipment manifest's "four disclosed cross-plan touches" count is PR-250-scoped by its own enumeration and unmoved by this round.
- 2026-08-05 — PR #292 (T2.4 review round): two plan-record true-ups from the per-task review, neither moving the shipped diff. (1) T2.4's Note wrote `markStale(workspaceId, errorDetail)`; the shipped signature carries no detail parameter, and the review adjudicated the plan record as the drifted side: `Spec-009 §Fallback Behavior` ("the workspace transitions to `stale` and new write runs must be blocked until repair") requires only the transition plus the write block, the detail-bearing path exists as `failReprovision` per CP-009-2, and a detail on `markStale` would CONFLICT with T1.3's "present iff `state = 'stale'` from a recorded failure" contract, because a probe-derived stale is not a recorded failure. The Note row now names the delivered signature with that rationale in place. (2) The four `workspace.*` error carriers (`error-contracts.md` §Workspace rows) ship inside `workspace-service.ts` because T2.4's Files row does not include a carrier file, mirroring T1.4's `repo-errors.ts` pattern in-module; the hoist into a dedicated `workspace-errors.ts` is owned by the first Phase-3 handler task that consumes the carriers through `mapJsonRpcError` (T3.1's binder files), which EXTENDs its Files row with the mechanical relocation at dispatch — recorded here so the module header's placement note has a plan-side owner instead of an unowned forward promise (the class the 2026-08-05 T2.5 entry repaired for the background refresher). Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — narrowing/record-keeping true-ups with no new task, invariant, CP row, phase, or Required ADR and no plan-body behavior change; the Tier-6 audit checkbox stands.
- 2026-08-05 — PR #292 (T2.3 review round): three plan-record repairs from the per-task review, none moving the shipped diff. (1) Two surfaces stated the detach event order backwards — I-009-9's Verification clause and T2.6's Note both read `workspace.archived` + `repo.detached`, while `Spec-009 §Detach Semantics (V1 Definition)` ("emitting `repo.detached` plus one `workspace.archived` per dependent workspace") and the shipped cascade emit the mount event first, because every cascade row write rides the `repo.detached` append's transaction and an archive event may only follow the commit that made it true (each archive append needs its own transaction, and an event for an uncommitted archive is exactly what I-009-9 forbids). Both surfaces now carry the spec's order — repaired before T2.6 dispatch, whose Note is its dispatch instruction and would otherwise have encoded the reverse of shipped behavior as an acceptance assertion, presenting as a T2.3 regression. (2) T2.3's Files row gains `workspace-service.ts` (EXTEND) for the class-docstring ownership carve-out the cascade forces — the dependent read and archive write must share the mount flip's transaction, so they live in `repo-mount-service.ts` and the sibling's every-statement ownership claim is narrowed to name the exception (the same missing-attribution class the 2026-08-04 dispatch true-up repaired for T2.1's `migration-shape.test.ts`). (3) T2.3's Note claimed "Phase 2 is the first phase with a config surface that can supply" the Windows `gitExecutablePath`; Phase 2 ships no daemon-config key — it exposes the injectable constructor seam only, and config wiring rides the Plan-007 bootstrap-config surface — so the Note now instructs a Windows deployment to supply the absolute path at construction. Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — record-correction/attribution true-ups with no new task, invariant, CP row, phase, or Required ADR and no plan-body behavior change; the Tier-6 audit checkbox stands.
- 2026-08-05 — PR #292 (Phase D final PR-scope review): three plan-record true-ups, none moving the shipped diff. (1) T2.2's Consumes over-listed `NodeId`: the shipped emitter imports none and its Spec-006 payload shape carries no node id — the branded `NodeId` is enforced at the attach boundary by T2.3's `AttachRepoMountInput` extending `RepoAttachRequest`, where the plan's other `NodeId` cites (T1.2, T4.1) already sit — so the row now ends at `SessionId`, routed here rather than to post-merge housekeeping to match the four sibling record repairs this PR already carries. (2) The 2026-08-05 T2.5 entry's reference-disambiguation count is scoped to the three IN-PLAN references: a fourth lives in Plan-010 §References (the FK row's disclosure pointer), now disambiguated to name the session-substrate-suites block — a described-content repair to an intact anchor, the class that row's correction history already records for this PR. (3) The Phase 3 Goal now records the composition-root handle-identity obligation for the Phase-2 services (one `openDatabase` handle across both services and the event log — I-009-9's `transactionalPrelude` atomicity holds only within a single connection; the structural guard lands with the Phase-3 wiring because it needs the Plan-006 append path's cooperation), matching the constraint now pinned on both service deps docstrings. Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — record-correction/scoping true-ups with no new task, invariant, CP row, phase, or Required ADR and no plan-body behavior change; the Tier-6 audit checkbox stands.

- 2026-08-07 — PR #297 (Plan-010 Phase 2, T2.4 implementation): CP-009-3 scoped to prepares that find the workspace outside the open reprovision bracket — the bracket's own provisioner lawfully skips the gate, which raises its invariant error for `provisioning` by design; the same scoping as Plan-010 I-010-12 / CP-010-3. Amending-side record at [Plan-010 §Cross-Plan Amendments](./010-worktree-lifecycle-and-execution-modes.md#cross-plan-amendments); status unmoved (narrowing — the gate's ratified behavior already enforced this).

- 2026-08-16 — **Repo-identity amendment (common-directory keying + resolution hardening): Spec-009 and this plan flipped `approved → review` and restored `approved` in the SAME diff; the §Preconditions audit box re-opened and re-checked; `### Phase 2B` authored.** User-ratified 2026-08-16.

  **What changed and why.** `Spec-009 §Required Behavior` said only "resolve and persist the canonical repository root" and never stated the working-tree-versus-repository distinction, so the shipped resolver read it as the enclosing working tree's own top level (`rev-parse --show-toplevel`) and every linked worktree of one repository acquired its own repo identity. The amendment states the contract the daemon needs: identity keys on the git COMMON directory, the canonical root is the MAIN checkout's working-tree root, and execution roots stay per-working-tree. Downstream, every daemon cache, filesystem watch, remote fetch, and throttle window keyed on repo identity becomes shared across a repository's working trees instead of duplicated per tree. Spec-009 gains `## Repo Identity And Common-Directory Keying (V1 Definition)` and two acceptance criteria; `Spec-009 §Required Behavior`, `§Fallback Behavior`, `§Implementation Notes`, and `§Pitfalls To Avoid` are amended IN PLACE at zero net line change, and the two new acceptance criteria plus the two new sections are appended after every previously-cited line, so no surviving inbound line cite moves.

  **Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): FLIP, not narrowing** — new normative spec behavior, two new invariants (I-009-15, I-009-16), a new phase, and a plan-body behavior change on an `approved` plan is the same shape that flipped Plan-006 (2026-07-29, 2026-08-02), Plan-024 (2026-08-02), Plan-008 (2026-08-03), and Plan-010 (2026-08-09, 2026-08-10). The restoring targeted readiness-audit delta lands in this same diff (the Plan-006 PR #278 flip-and-restore-in-one-swap precedent), so the review window opens and closes within one PR and no downstream consumer ever observes a `review` Spec-009 in `develop`.

  **Audit findings, six.** (1) [critical, repaired here] Common-directory keying BREAKS `Spec-009 §Local Trust Envelope (V1 Definition)` as written. The envelope admits a workspace root only when it is path-contained within an attached mount's canonical root, and the provenance bullet of `Spec-009 §Local Trust Envelope (V1 Definition)` admits daemon-PROVISIONED roots — it never admits a USER-supplied linked worktree. Once the canonical root becomes the main-checkout root, a worktree beside it is no longer contained, so every bind to a hand-created worktree would fail `repo.outside_trust_envelope` — the amendment would have shipped an availability regression wearing a security code. Repaired in the same swap: the envelope of a git-backed mount becomes its canonical root PLUS the working-tree paths the repository itself registers, each admitted through the bidirectional-link and physical-containment screens, read at bind time and never cached; I-009-3 is amended to match and T-009-2B-3 owns the code. (2) [critical, repaired here] Step 4 of the shipped `resolveCanonicalRoot` asserts the supplied path is contained within the value about to be persisted — so a one-line swap of `--show-toplevel` for `--git-common-dir` would refuse EVERY linked-worktree attach with `root_mismatch`. T-009-2B-1 re-scopes containment onto the enclosing working tree and adds a repository-membership test, which is strictly stronger than the containment it replaces: the `core.worktree` redirect vector the shipped refusal exists to catch no longer passes merely by containing the input. (3) [major, repaired here] `WorkspaceService.createDefaultWorkspace` roots the default workspace at the canonical root, which under the new identity would relocate every linked-worktree attach's execution to the main checkout — a data-loss-adjacent behavior change wearing the costume of a one-line simplification, and the exact plausible-but-wrong twin this delta's negative control targets. `RepoRootResolution` gains a `workingTreeRoot` member (daemon-internal; no wire schema moves) and T-009-2B-4 owns the reconciliation. (4) [major, recorded] `idx_repo_mounts_active_root` changes USER-VISIBLE behavior: attaching worktree A then worktree B of one repository now collides and refuses `repo.already_attached`. That is what "N worktrees resolve as ONE repo" means, so it is stated in the spec rather than discovered at runtime, reconciled against `Spec-009 §Detach Semantics (V1 Definition)`'s re-attach rule (now per-repository), and pinned by a T-009-2B-4 test. No DDL, migration, error code, or wire field changes. (5) [verification] The "refuse a path that does not exist rather than walking up from it" requirement is ALREADY satisfied by the shipped step-2 `realpathOrThrow`, which refuses with `path_not_found` before any git spawn — because the resolver delegates to git rather than walking ancestors. T-009-2B-1 therefore introduces NO ancestor walk (the reference mechanism's parent walk exists because it has no git binary to ask) and pins the existing refusal with a regression test, so the property stays structural rather than becoming a check that can be forgotten. (6) [major, repaired here] Finding (1)'s repair introduced a SECOND admission rule, and the two disagree on a real state. Daemon-provisioned worktrees are created by `git worktree add`, so they are BOTH provenance-admitted (`Spec-009 §Local Trust Envelope (V1 Definition)`, the pre-existing rule) and registration-admitted (the new rule) — harmless while both admit, but the new rule's "read at bind time, never cached; a working tree git no longer registers is no longer in the envelope" clause EJECTS a provisioned root the moment an external `git worktree prune` removes its registration, which is precisely the window between that prune and the Plan-010 retirement sweep. A running turn would lose its execution root to a rule written for user-supplied paths. Repaired by scoping: the registration arm governs USER-SUPPLIED bind paths only and never narrows provenance admission, stated in the same spec bullet, carried onto I-009-3's third arm, and pinned by a T-009-2B-3 test that prunes a provisioned root's registration and asserts it stays admitted. The two rules can no longer disagree, and the sweep — not the envelope — stays the thing that retires a provisioned root.

  **Claim-vs-content over this diff.** The "19 inbound cites converted" claim was counted, not recalled: `Spec-009 line NNN` cites with `NNN >= 130` numbered exactly 19 occurrences across 7 distinct targets (144×2, 145×2, 151×2, 155×4, 157×5, 158×3, 159×1) at `HEAD`, and zero remain repo-wide after conversion. Each converted citer keeps its descriptor bytes and gains the section anchor with the `(V1 Definition)` suffix group FIRST — the shape `findSectionHeading` reads as the suffix claim; a content descriptor in that position is undecidable and fails closed. The seven Shipment-Manifest `spec_coverage` strings were converted too rather than left to rot: re-anchoring preserves each merged PR's claim more faithfully than a stale line number, and every descriptor is byte-identical to what its PR recorded. The one inbound volatile Spec-009 line cite outside this plan — the `lastError` field comment under `api-payload-contracts.md §Plan-009 — Repo Attachment And Workspace Binding`, citing the spec's execution-mode-switch failure rule (the workspace transitions to `stale` with the error detail recorded) — targets a line above every appended section and is verified unmoved. The two new acceptance criteria are appended AFTER the existing three, so AC1/AC2/AC3 keep lines 127/128/129 and every ordinal cite to them stands.

  **Marker-line integrity, mechanically verified.** `preflight.mjs docs/plans/009-repo-attachment-and-workspace-binding.md 2B` exits 0 (`2B` / `size-class: L`) with no demoted Gate-4 grammar findings on stderr, and the no-argument numeric walk halts on Phases 3 and 4 with both gate legs firing — the Gate-5 `precondition_box_checked` entry and the prose-box scan — confirming the scoped carrier box holds the wire and renderer phases while leaving Phase 2B dispatchable by explicit label. **Seeded-refutation negative control:** perturbing one marker-line suffix claim from `(V1 Definition)` to `(V2 Definition)` on a scratch copy run through the identical harness flips the same invocation to exit 1 with `[section-not-found] T-009-2B-1 (Spec coverage)`, and the unperturbed copy through that same harness exits 0 — so the clean pass is discriminating evidence, not a vacuous one. Prettier `--check` is clean on both files.

  **What this delta deliberately does NOT do.** It does not rewrite the shipped T1.5 row: that row is a manifest record of what PR #250 claimed, so it gains a marked "Superseded in part" clause naming Phase 2B as the owning lane rather than being edited into agreement with today's contract. It mints no born-unchecked carrier for Phase 2B itself — Phase 2B authors the one missing surface (a repository-registration reader) rather than waiting on another plan — and the one box it does mint (`Repo-identity resolver amendment landed`) gates Phases 3 and 4 only, on the reasoning that publishing per-working-tree identity through the SDK would make it observable contract before it is correct.

- 2026-08-17 — **PR #340 Codex review round 1: six findings, all repaired in the same PR; no schema, error code, or wire method moves.** (1) [P1] The registered-working-tree admission arm had no WIRE form — `WorkspaceBind.directory` was specified mount-root-relative with absolute paths rejected, so a linked worktree (generally outside the canonical root, and on Windows possibly on another drive with no relative spelling at all) was admitted by the envelope but inexpressible by the request. Repaired in `Spec-009 §Local Trust Envelope (V1 Definition)`: the one existing string field carries two forms — relative against the canonical root as shipped, ABSOLUTE to name a registered working tree — both admitted solely by post-resolution containment within the union of admitted roots; T-009-2B-3 carries the rule and its test arms. No schema change. (2) [P1] The spec's identity contract cited no upstream git primary source, leaving its non-trivial git claims unverifiable under the AGENTS.md citation standard; Spec-009 §References gains git-worktree(1), gitrepository-layout(5), git-rev-parse(1), and git-init(1). (3) [P2] Deriving the main-checkout root as the common directory's parent breaks `--separate-git-dir` layouts, where the common directory sits outside every working tree (its parent is unrelated). Empirically verified during repair: git writes no `core.worktree` for that layout and its own `git worktree list` misreports the git directory as the main working tree, so the main root is genuinely unrecorded there. T-009-2B-1 and the spec now state derive-then-VERIFY: gitDir==commonDir ⇒ own toplevel (the arm a `--separate-git-dir` main checkout takes); otherwise parent-of-commonDir accepted only with basename `.git` and a bidirectional `.git`-entry check back to the same common directory, refusing `root_mismatch` with attach-from-the-main-checkout as the named recovery — the linked-worktree-of-`--separate-git-dir` shape lands on that refusal by design. The `--path-format=absolute` floor (git ≥ 2.31) replaces the stale 2.5 floor as governing. (4) [P1] The stat-then-open pointer-file screen had a TOCTOU window — the checked path swapped for a FIFO between `stat` and `open` still wedges the scan. Spec bullet and T-009-2B-2 now require non-blocking open, `fstat` on the OPENED descriptor, and the bounded read through that descriptor, so the guarantees bind the object actually read. (5) [P2] The never-cached registration test was non-discriminating: `git worktree remove` deletes the working directory too, so a registration-caching implementation would still fail the second bind on path resolution and pass. The arm now deletes only the ADMINISTRATIVE registration with the directory left present and readable, so membership re-read is the only possible failure cause. (6) [P1] Envelope exit was enforced at bind only — a workspace bound to a later-unregistered worktree kept running on a root the amended contract calls outside the envelope, because the shipped `assertWritable` checks state and reachability, not membership. T-009-2B-4 wires run-handoff re-verification through T-009-2B-3's validator for USER-SUPPLIED roots (one code path for bind-time and run-time), failure transitioning the workspace to `stale` with the typed error recorded per `Spec-009 §Fallback Behavior`; daemon-provisioned roots stay provenance-exempt. I-009-3 carries the run-boundary clause and T-009-2B-4's Verifies row gains I-009-3 (the Gate-4 census — 831 on `develop` — lands at 839 across this PR: the amendment's seven references plus this one, 838 being the pre-round intermediate). Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — every repair lands within the same review window the 2026-08-16 flip-and-restore opened and closed in this PR, tightening the amendment's own surfaces (wire-form rule, derivation verification, descriptor screen, run-boundary enforcement) with — at this round — no new task, phase, CP row, or Required ADR (the PR's round 2 then registered CP-009-9; its own entry below records that adjudication); the re-checked audit box's Delivered scope covers exactly these surfaces. A post-round coherence sweep in the same PR then discharged the round's ripple: `Spec-009 §Acceptance Criteria`'s AC5 refusal enumeration gained the fifth shape the derive-then-verify arm introduced (a derived main-checkout root failing its bidirectional verification), the audit box's Delivered record was extended to name the round's four surfaces, the shipped T1.3 row and the `WorkspaceBindRequest.directory` comment in `api-payload-contracts.md` §Plan-009 were reconciled with the two-form wire rule (the T1.3 row by the T1.5 superseded-in-part convention — shipped-manifest records are marked, not rewritten), and the §6 NS-63 record was aligned with the round's hardening.
- 2026-08-17 — **PR #340 Codex review round 2: seven findings, all repaired in the same PR; still no DDL, error code, or wire method moves — the two persistence legs ride existing metadata JSON columns.** Every premise was verified empirically before repair (git 2.50.1, scratch fixtures; `execution-root-service.ts` read at its shipped source). (1) [P1] `Spec-009 §Default Behavior` still said the default workspace roots "at the main checkout", contradicting the amendment's own both-values rule and T-009-2B-4; the sentence now roots at the enclosing working tree of the supplied path, in place. (2) [P1] The absent-vs-unusable rule's rationale — "git itself stops there" — was empirically half-false: git hard-stops only on the unusable pointer-FILE shape and silently SKIPS an unusable `.git` DIRECTORY at an ancestor, discovering the outer repository. The spec's rule now states what git actually does and requires the daemon's own bounded post-hoc screen of the supplied path's own `.git` entry and every directory strictly between it and the resolved toplevel — the at-path arm covering the empty strictly-between set and the no-repository arm, so AC5's "at the path itself" clause is mechanism-backed on both resolution arms (T-009-2B-1 carries it via `classifyGitDirEntry`; refuses `vcs_error`) — a screen that can refuse a wrong answer but never manufacture a root, so the no-upward-walk rule stands. (3) [P1] The gitDir==commonDir⇒own-toplevel arm ran FIRST and was spoofable: a planted `.git` pointer file naming a repository's common directory directly reports gitDir==commonDir with the planted holder as toplevel, and would have been persisted as that repository's root. The arms are inverted to derivation-first — parent-of-common-directory, or the submodule module directory's `core.worktree` back-pointer (probe-confirmed: a submodule's module config records `core.worktree` resolving to its checkout), each verified by the candidate's own `.git` entry resolving back — with own-toplevel surviving only for the `--separate-git-dir` main checkout where git records nothing, that arm's residual named in place, and the step-4 membership main-checkout leg tightened to canonical EQUALITY with the derived root so a pointer-resolution match never impersonates the main checkout. (4) [P1] Submodule checkouts (common-directory basename ≠ `.git`) had no lawful derivation and would all have refused; the `core.worktree` candidate is that derivation, closing AC4 for the submodule layout without narrowing it. (5) [P1] The derived identity was never persisted, so a post-attach retarget of the root's `.git` entry would walk later binds and runs onto a different repository undetected; attach now persists `repo_mounts.metadata.commonDir` and T-009-2B-3's bind-time (and T-009-2B-4's run-time) re-derivation must match it, refusing `root_mismatch` with mount health marked and re-attach as the recovery. (6) [P1] Branch mode still rooted at `mount.canonical_root` (`execution-root-service.ts` `#bindBranchMode`, verified at source), which under the new keying relocates a linked-worktree workspace's branch-mode runs to the main checkout — the I-009-15 relocation bug — and `beginReprovision` clears `fs_root`, so no shipped column carried the bound origin durably; workspace creation now records `workspaces.metadata.boundRoot`, the branch arm re-roots at it (fallback `canonical_root` for pre-amendment rows) under the new CP-009-9 with Plan-010's reciprocal CP-010-14 consent, and `Spec-010 §Turn-Boundary Snapshots`' "shared main checkout" sentence is corrected in the same swap — Spec-010 and Plan-010 flip `approved → review` and restore `approved` in this same diff (the PR #321/#323 neighbor-swap shape). (7) [P2] The plan's own §References carried no git primary source for the worktrees-layout and git ≥ 2.48 relative-registration claims its tasks make; git-worktree(1) and gitrepository-layout(5) are added with claim mapping. Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): this plan and Spec-009 stay within the same flip-and-restore window (the round-1 adjudication's basis is re-scoped in its entry above — round 2 adds the CP-009-9 row and grows T-009-2B-1/2B-3/2B-4 in place, still no new task, phase, or Required ADR); the audit box's Delivered record is extended to cover this round's surfaces. The Gate-4 census stays 839 — no Verifies row moved. Verification basis: the planted-gitfile, submodule, ancestor-skip, and `--separate-git-dir` behaviors were each reproduced on git 2.50.1 scratch fixtures before any edit landed.

### Cross-Plan Amendments

**PR #250 (2026-07-25) — `packages/contracts/src/node-id.ts` extraction + `packages/contracts/src/runtime-node.ts` re-export seam.**

T1.2's implementation surfaced a latent import-graph defect in the Plan-003-owned `NodeId` declaration home: composing `NodeIdSchema` into `repo.ts` — the exact consumption the ownership map promises ([cross-plan-dependencies.md §2 `packages/contracts/src/`](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map): "with `NodeIdSchema` imported upward by Plan-009 for mount node-ownership") — would have closed the eager three-hop module cycle `repo.ts → runtime-node.ts → event.ts → repo.ts`. Every edge is a module-scope Zod initializer value read (`runtime-node.ts → event.ts` since Plan-003 Phase 1 — `EventEnvelopeVersionSchema` composed on `RuntimeNodeAttachRequestSchema`, joined later by T2.0's `EVENT_FIELD_MAX_LEN` payload caps, the Phase-5 roster projection's `clientVersion` reuse (PR #150), and Plan-006 T1.4's `CapabilityDetailsSchema` rebind; `event.ts → repo.ts` since T1.1's CP-009-4 union registration), so no evaluation order avoids a temporal-dead-zone `ReferenceError` at import time, and `tsc` compiles module cycles silently — a total package failure discoverable only at runtime. Plan-009 T1.2 is the first composer that completes the third edge, so the hazard was not discoverable before this plan's work.

To keep the promised import possible without weakening the Plan-003 contract, PR #250:

1. **Adds `packages/contracts/src/node-id.ts`** — a dependency-free leaf (zod-only imports, structurally cycle-proof) holding the `NODE_ID_MAX_LEN` / `NodeId` / `NodeIdSchema` declaration block relocated verbatim from `runtime-node.ts` (7 LOC of moved declarations; the rest of the file is header documentation). Follows the `channel-id.ts` standalone-leaf precedent.
2. **Re-seams `packages/contracts/src/runtime-node.ts`** — the declaration block is replaced by a two-statement re-export, so the file's public API is unchanged; the untouched `__tests__/runtime-node.test.ts` (158 tests) is the held API control, and the barrel resolves to the leaf's single instance (identity-pinned in `__tests__/repo.test.ts`).
3. **`repo.ts` imports the leaf directly** rather than through `runtime-node.ts`, keeping the repo domain out of the runtime-node module subtree.

**Cross-plan touch rationale (Housekeeping Exception, [cross-plan-dependencies.md §Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule)):** (a) structural-only — a declaration relocation plus re-export with zero runtime or behavioral change (identical schema instance, identical public API, held test control); (b) first discoverable via the extending plan's composition — the cycle's third edge exists only when Plan-009 composes `NodeIdSchema`, and T1.2 was the first composer; (c) sub-30-LOC in changed code — 7 relocated declaration LOC + 2 re-export statements + 1 direct-leaf import (the new file's remaining lines are documentation; sub-day — one dispatch), disclosed rather than implied since `node-id.ts` totals 92 lines; (d) one-time correction — Plan-003 retains ownership of BOTH files (`node-id.ts` is Plan-003-owned from birth; any change to `NodeId`'s shape or cap remains a Plan-003 edit governed by Spec-003), and Plan-009 claims no ongoing edit rights — this is not a sixth registered seam class. Owner-side pointer recorded at Plan-003 §Phase 1 file list; extender-side rows at this plan's T1.2 Files/Consumes lines.

**Refs:** Plan-003 Phase 1 (`runtime-node.ts` original ship, NS-30 / PR #135), [ADR-014](../decisions/014-trpc-control-plane-api.md) (the double-T Standard-Schema-V1 rationale the relocated block preserves).

**PR #250 (2026-07-25) — deterministic snapshot barrier in the Plan-001 DEFERRED-race negative control (`packages/runtime-daemon/src/session/__tests__/migration-race-worker.mjs` + `session-service.test.ts`).**

T1.5's suite additions surfaced a latent reliability defect in the Plan-001-owned concurrent-boot race tests (original ship PR #9): the DEFERRED negative control — the test proving `applyMigrations`' `.immediate()` is load-bearing by demonstrating ≥1 `SQLITE_BUSY`-class failure across 5 trials × 8 workers — rested on a probabilistic contention model (per-trial ~0.2 "all racers serialize by luck", 3.2e-4 five-trial false-negative bound) calibrated on an unloaded machine. Once T1.5's real-process fixtures joined the daemon suite, host saturation serialized the worker spawns, the DEFERRED snapshots stopped overlapping, and the control false-negatived: 2 of ~5 full-suite runs failed the ≥1 assertion while the file alone stayed 6/6 green. The flake gated this PR's own suite verification, so the fix could not be deferred to a Plan-001 follow-up without shipping the chain on a red-flaky gate.

The fix makes the collision structural instead of statistical: a `SharedArrayBuffer` + `Atomics.wait` rendezvous parks every DEFERRED worker inside its open transaction — after the read that pins its WAL snapshot, before the write that upgrades it — until all siblings arrive, so on release one racer wins the write lock and the other seven hold stale snapshots (`SQLITE_BUSY_SNAPSHOT` by construction). A 3 s deadline degrades a straggler trial to the prior probabilistic behavior rather than hanging the suite; a fresh buffer per trial prevents terminated-trial stragglers releasing the next trial's barrier; supplying the barrier on the IMMEDIATE path is refused as fixture misuse (`BEGIN IMMEDIATE` serializes at BEGIN, so an in-transaction rendezvous is structurally unreachable). Validated with a negative control: the barrier neutered reproduces the flake's per-trial contention spread on demand, while the barrier yields the full 7-loser count on every trial.

**Cross-plan touch rationale (post-completion enhancement lane, NOT the Housekeeping Exception):** the [§Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule) expressly routes behavioral changes to shipped owned code past its Housekeeping Exception to [CONTRIBUTING.md §How Code Lands](../../CONTRIBUTING.md#how-code-lands-work-classification) lane 2, and this fix exceeds the exception's sub-30-LOC bound in any case (+118/−3 worker, +68/−15 test — the barrier mechanism, its misuse validation, and the rewritten reliability rationale the retired probabilistic model no longer supports). Lane 2 applies cleanly: Plan-001 Phase 3's manifest entry exists (PR #9), and the change stays inside the approved spec envelope — the negative control's purpose and its ≥1-BUSY assertion are unchanged; only the contention mechanism becomes structural, and the production `.immediate()` detector path is byte-untouched. It rides the surfacing PR as a dedicated `test(daemon)` commit carrying lane 2's `Refs: Plan-001` traceability footer, rather than serializing through a separate precursor PR, because the defect gates this PR's own verification — the flake fires on this branch's full-suite runs — matching the PR #30 precedent of prerequisite lighter-lane commits landing on a lane-1 branch. Plan-001 retains ownership of both files; Plan-009 claims no ongoing edit rights.

**Refs:** Plan-001 Phase 3 (migration + race-test original ship, PR #9).

**PR #250 (2026-07-25) — comment-only true-ups riding the Housekeeping Exception (`packages/runtime-daemon/src/ipc/domain-error.ts` JSDoc; `packages/runtime-daemon/src/node/__tests__/node-event-emitter.test.ts` cite).**

Two documentation-only riders corrected files this plan does not own. Neither changes a byte of emitted JavaScript:

1. **`packages/runtime-daemon/src/ipc/domain-error.ts`** (Plan-007-partial — the `ipc/` substrate whose `DaemonDomainError` T1.4's five `repo.*` carriers extend). Its optional-fields docstring said an unset optional field "stays genuinely absent". That was accurate under legacy `[[Set]]` emit and went stale once the config set `useDefineForClassFields: true`: the declarations are emitted, so the own property exists holding `undefined` either way, and it is the mapper's VALUE checks (`thrown.detail !== undefined`, `thrown.jsonRpcCode ?? …`) rather than key presence that read absence. The guarded `if (x !== undefined) this.x = x` assignment is a type-level device for `exactOptionalPropertyTypes`, not a runtime one — the docstring now says so, and the class body is untouched.
2. **`packages/runtime-daemon/src/node/__tests__/node-event-emitter.test.ts`** (Plan-003). One comment line naming the `NodeId` declaration home read "(runtime-node.ts header)" and now reads "(contracts `node-id.ts` header)". Transparently consequent on the relocation the first block above discloses — but that block names only the contracts files the relocation moved, not this daemon-side consumer's stale pointer, so the file is disclosed here.

**Cross-plan touch rationale (Housekeeping Exception, [cross-plan-dependencies.md §Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule)):** (a) comment-only, which sits inside the exception's "type-level or structural-only with zero runtime or behavioral change" bound and strictly under it — no declaration, signature, or structure moves in either file; (b) first discoverable via this plan's composition — T1.4's carriers are the first subclasses whose author audited the base-class contract against the current tsconfig, and the stale `NodeId` pointer only became wrong when T1.2 relocated the declaration; (c) 11 changed lines in the first file and one in the second, each a single sub-day dispatch; (d) one-time correction — Plan-007-partial retains ownership of `ipc/` and Plan-003 of its emitter suite, and Plan-009 claims no ongoing edit rights on either file.

**Why the disclosure lands in this block:** criterion (e) requires the exception be documented in the EXTENDING plan's §Cross-Plan Amendments, and the T1.4 commit message argued (a)–(d) at the change while leaving (e) unsatisfied — a gap the PR-scope review surfaced. This block is the (e) leg for both riders.

**Refs:** Plan-007-partial (`ipc/` substrate ownership, the `DaemonDomainError` base class), Plan-003 (`node/__tests__/` test-file ownership; the `NodeId` declaration the pointer names).

**PR #250 (2026-07-25) — Plan-010 Phase-1 payload-mechanism amendment (`docs/plans/010-worktree-lifecycle-and-execution-modes.md`, nine net-zero line rewrites + a dated §Notes adjudication bullet + one ownership-map clause).**

Codex round 4 surfaced a three-surface contradiction this plan's T1.1 created: the `repo.ts` FORWARD PATH comment promised Plan-010 a third `WorktreeStateSchema` arm on `RepoWorkspaceLifecyclePayloadSchema.state`; Plan-010's ratified rows said "reuse that payload schema" as-is (I-010-1 / CP-010-1 / CP-010-5 / D-010-12 / T1.1 / T2.1) — while the two-arm union rejects five of Plan-010's six worktree states; and the ownership map's Plan-010 entry already located the worktree-event payload schemas in `worktree.ts`. The promised edit was unbuildable besides: it is sanctioned by no map seam class, and its `repo.ts` → `worktree.ts` import would re-close the eager module-cycle class the node-id.ts relocation above broke. Resolution: T1.1 exports the state-parameterized factory (§Notes round-4 entry), and Plan-010's rows are amended IN THIS PR so its T1.1 derives `WorktreeLifecyclePayloadSchema` in `worktree.ts` as the factory's `WorktreeStateSchema` instantiation — the map's existing language becomes literally true, and the never-redefines contract is preserved (an instantiation is not a redefinition).

**Cross-plan touch rationale:** governance-text amendment to an `approved` plan's mechanism rows, not code — the amended rows bind Plan-010's UNEXECUTED Phase 1 (its Shipment Manifest is empty, so no shipped artifact moves), the contradiction is first discoverable from this plan's T1.1 composition (the FORWARD PATH promise was authored here), and the alternative — leaving three surfaces contradictory until Plan-010's dispatch — recreates the audit-gap class the readiness audits exist to close. All nine Plan-010 line rewrites are net-zero in line count (an inbound `:233` line-cite on that file from the campaign design corpus pinned its numbering at edit time; the sweep then found that cite ALREADY drifted — line 233 is blank and the described run-engine attribution sits in the Phase-3 merged-precondition row — so the same PR converts it to content-anchored form per the corpus line-cite discipline, retiring the pin — the appended §Notes bullet postdates that retirement, with no surviving inbound pin constraining the file); the map's Plan-009 §2 entry gains the factory clause in place. Plan-010 retains ownership of its plan file and of `worktree.ts`; Plan-009 claims no ongoing edit rights — this block is the §Ownership Rule criterion-(e) disclosure for the governance-doc touch. The owner-side record is Plan-010's dated §Notes entry (2026-07-25), which adjudicates Status under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — narrowing-of-ambiguity row plus default rule, with no new invariant, CP row, phase, or Required ADR and no plan-body behavior change, because the as-audited mechanism admitted no build at all; the Tier-6 audit checkbox stands, and the amended file passes the preflight gates, G4 traceability included.

**Refs:** Plan-010 Phase 1 (unexecuted; Tier-6 audit 2026-06-10), [ADR-018](../decisions/018-cross-version-compatibility.md) §Decision #8 (the additive-MINOR classification both the factory export and Plan-010's future variant registration ride).

**PR #292 (2026-08-04) — Plan-001 session-substrate suites: scheduled Plan-009-landing repair (`packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts` + `packages/runtime-daemon/src/session/__tests__/session-service.test.ts`).**

T2.1's migration 0010 trips two Plan-001-owned suites — and every affected site is a handoff those suites planted for exactly this landing, not a defect. In the migration-shape suite: the applied-table census (`ALL_EXPECTED_TABLES`) is an exact-equality pin over the full migrated set, so any new migration moves it by design; the FK-DML-enforcement test (`packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#FORWARD_REFERENCES_DML_ENFORCEMENT_TEST`) stubs the two Plan-009 parent tables in its version-4 fixture with deliberate plain `CREATE TABLE` statements whose own comment instructs their replacement "with fixture rows against the real DDL" when Plan-009's migration lands — the plain CREATE (not `IF NOT EXISTS`) is the constructed loud tripwire that fires on this branch the moment 0010 applies; and the fresh-db absence test and the parentless-db test (`packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#WRITE_INERT_ON_PARENTLESS_DB_TEST`) each pin a premise their own text scopes to the state the daemon ships in _until Plan-009 Phase 2 lands_. In the session-service suite: four applied-version-set pins (three exact-equality `schema_version` arrays and the concurrent-boot IMMEDIATE verifier — whose assertion message and per-version atomicity walk are part of the pin, and whose walk closes with an explicit re-derive-rather-than-increment instruction addressed to every future migration) red under version 10 by the same design. The repair contract — census growth 16 → 18 with a re-derived derivation comment, stub replacement against the real DDL, the two isolation re-scopes onto `packages/runtime-daemon/src/migrations/0004-worktree-lifecycle.ts#WORKTREE_LIFECYCLE_MIGRATION_SQL` preserving both behavioral pins with both exported anchors intact, and the `[1..10]` version-set re-derivation with a reasoned v10 atomicity bullet — is specified at T2.1's Note sites (i)–(v) and executes inside the T2.1 dispatch.

**Cross-plan touch rationale (lane 2 riding the surfacing lane-1 branch, per the PR #250 migration-race precedent above):** (a) collecting a pre-authorized handoff, not repairing the owner's logic — every assertion's intent survives (the censuses stay exact-equality, the FK test still proves enforcement against real parents, both re-scoped tests keep pinning exactly what they pinned, and the atomicity walk's structural argument is re-derived for v10 as its own text demands); only the fixtures and the pinned sets move, as the suites' own comments schedule. It still routes past the Housekeeping Exception because the assertions' observable behavior changes and the edit exceeds the sub-30-LOC bound, so the [§Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule) sends it to [CONTRIBUTING.md §How Code Lands](../../CONTRIBUTING.md#how-code-lands-work-classification) lane 2 — which applies cleanly: Plan-001 Phase 3's manifest entry exists (PR #9) and the change stays inside the approved spec envelope. (b) First discoverable at this migration's landing — the tripwire is constructed to be un-triggerable before a migration creates the real tables, and 0010 is that migration; the session-service pins joined the enumeration at T2.1 implementation, when the full-suite run first reddened them. (c) The five Note-contract sites across two owned test files, one dispatch, riding this PR as a dedicated `test(daemon)` commit carrying lane 2's `Refs: Plan-001` traceability footer rather than serializing through a precursor PR, because the defect gates this PR's own verification — the censuses and tripwire fire red on this branch's full daemon suite the moment 0010 applies, the same escape-hatch reasoning the migration-race block records. (d) One-time collection — Plan-001 retains ownership of both suites, and Plan-009 claims no ongoing edit rights. This block is the criterion-(e) disclosure. One governance-prose rider joins it: the Plan-010 §References row anchored at the two migration-shape test constants described the stub-parent fixture this repair deletes, so its fixture clauses are trued up to the real version-10 parents in the same PR — a described-content repair to an intact anchor, with Plan-010 retaining ownership of its plan file and the row's own correction history recording the change.

**Refs:** Plan-001 Phase 3 (migration substrate + both session-substrate suites' ownership, original ship PR #9).

**PR #292 (2026-08-05) — comment-only true-up riding the Housekeeping Exception (`packages/runtime-daemon/src/node/node-event-emitter.ts` correlation-pair comment).**

One documentation-only rider corrected a file this plan does not own. The Plan-003-owned emitter's correlation-pair comment claimed that under `exactOptionalPropertyTypes` an explicit `undefined` "is not assignable either" to the optional correlation fields — false for fields declared `?: string | undefined`, where the declared `| undefined` arm makes an explicit `undefined` key type-check; the omission is enforced by the conditional spread, not the compiler. The comment now says so. The neighboring `transactionalPrelude` comment in the same file makes the same-shaped claim TRUTHFULLY (the option is a bare optional with no `| undefined` arm) and is untouched — the repair was adjudicated instance-by-instance, not as a bulk rewrite.

**Cross-plan touch rationale (Housekeeping Exception, [cross-plan-dependencies.md §Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule)):** (a) comment-only — zero runtime or behavioral change, strictly inside the exception's structural-only bound; (b) first discoverable via this plan's composition — T2.2's `workspace-event-emitter.ts` mirrors the shipped emitter pattern per its T2.2 Note, and the T2.5 review round's audit of the mirrored comment against the actual field declarations is what falsified the claim, in the copy and its origin alike; (c) 3 changed comment lines, one sub-day dispatch; (d) one-time correction — Plan-003 retains ownership of its emitter, and Plan-009 claims no ongoing edit rights. This block is the criterion-(e) disclosure.

**Refs:** Plan-003 Phase 2 (`node-event-emitter.ts` original ship, NS-31 / PR #137).

**PR #340 follow-up (2026-08-17) — durable carrier for the eleven Codex findings carried forward from review rounds 3+4.**

PR #340's Codex review rounds 3 and 4 raised eleven findings that were answered on the PR's threads but deliberately not folded into the amendment diff — each reply records the docs-round scoping and promises implementation-time triage. A reply on a resolved thread of a merged PR is not a durable carrier: nothing on the dependency DAG or in this plan's body would otherwise surface those findings to the Phase 2B implementer. This entry is the carrier. The Phase 2B implementation PR MUST triage every item below — implement it, refute it with recorded reasoning, or re-route it to the plan that owns the surface — before Phase 2B's manifest entry lands; the Phase 2B head carries the matching pointer bullet. Thread anchors resolve against the PR page as `https://github.com/Sawmonabo/ai-sidekicks/pull/340#<anchor>`.

The eleven, by review title, priority badge, and thread anchor:

1. Revalidate the target root before branch reprovision (P1, `discussion_r3793963408`)
2. Define a health outcome for identity mismatches (P2, `discussion_r3793963415`)
3. Reconcile AC4 with the separate-git-dir refusal (P2, `discussion_r3793963421`)
4. Preserve repository-wide snapshots for nested branch binds (P1, `discussion_r3793963426`)
5. Key active-mount uniqueness on the common directory (P1, `discussion_r3794009994`)
6. Expand the envelope definition to include registered roots (P1, `discussion_r3794009995`)
7. Cite the Git discovery behavior that drives the scanner (P1, `discussion_r3794009996`)
8. Recheck nested worktrees before canonical-root admission (P1, `discussion_r3794009997`)
9. Case-fold the common-directory basename check (P2, `discussion_r3794010000`)
10. Describe the resolved read-only root in the response (P2, `discussion_r3794010002`)
11. Extend branch-mode immutability to the bound checkout (P2, `discussion_r3794010005`)

Status adjudication under the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): stays `approved` — this entry and the Phase 2B pointer bullet mint no task, invariant, CP row, phase, or Required ADR; they record review obligations already publicly answered on the merged PR, the same class as PR #297's practice of writing phase-facing obligations into the phase Goal rather than silently deferring them. No census moves.

**Refs:** PR #340 (the Spec-009/Plan-009 repo-identity amendment, §6 node NS-63), Phase 2B — Repo Identity Keying And Resolution Hardening (this plan's §Implementation Phase Sequence).

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated

## References

- [libuv `src/win/process.c` (v1.51.0)](https://github.com/libuv/libuv/blob/v1.51.0/src/win/process.c) — the Windows executable search T1.5's bare-`git` spawn inherits. `search_path`, for a name containing no directory separator: "The file is really only a name; look in cwd first, then scan path", reached only when `NeedCurrentDirectoryForExePathW(L"")` returns TRUE; the `PATH` slices are walked afterward. Tag-pinned rather than branch-pinned so the quoted comment stays at the cited location (confirmed 2026-07-25).
- [`NeedCurrentDirectoryForExePathW` function (processenv.h)](https://learn.microsoft.com/en-us/windows/win32/api/processenv/nf-processenv-needcurrentdirectoryforexepathw) — the gate libuv consults. "The value of the `NoDefaultCurrentDirectoryInExePath` environment variable determines the value this function returns", and a name that CONTAINS a backslash always returns TRUE. libuv passes the empty string, which carries no backslash, so that variable is decisive: setting it closes the cwd-first search for every bare-name spawn in the process (confirmed 2026-07-25).
- [Node.js `fs` — `fs.realpath.native()` / `fsPromises.realpath()`](https://nodejs.org/api/fs.html#fsrealpathnativepath-options-callback) — the realpath seam both T1.5 and T1.6 default to. `fsPromises.realpath()` "Determines the actual location of `path` using the same semantics as the `fs.realpath.native()` function", while the callback `fs.realpath()` is a separate implementation whose first documented difference is "No case conversion is performed on case-insensitive file systems" — the divergence the seam pin exists to catch. `fs.realpath.native()` additionally notes that on musl-linked Linux "the procfs file system must be mounted on `/proc` in order for this function to work. Glibc does not have this restriction" (confirmed 2026-07-25).
- [libuv `uv_fs_realpath`](https://docs.libuv.org/en/v1.x/fs.html) — what Node's native realpath calls. "Equivalent to `realpath(3)` on Unix. Windows uses `GetFinalPathNameByHandleW`", with platform caveats "discovered when used in Node": macOS and the BSDs "will fail with UV_ELOOP if more than 32 symlinks are found while resolving the given path. This limit is hardcoded and cannot be sidestepped", and on Windows "Inconsistent casing when using drive letters", unresolvable ImDisk-style ramdisk volumes, and resolution that "bypasses subst'd drives" (confirmed 2026-07-25).
- [SQLite — `BEGIN TRANSACTION`](https://sqlite.org/lang_transaction.html) — the transaction semantics the DEFERRED negative control's barrier is placed against, and the reason the fixture refuses a barrier on the IMMEDIATE path. Three sentences compose the snapshot-pinning claim: "DEFERRED means that the transaction does not actually start until the database is first accessed", then "If the first statement after BEGIN DEFERRED is a SELECT, then a read transaction is started. Subsequent write statements will upgrade the transaction to a write transaction if possible, or return SQLITE_BUSY", and for the duration of that read transaction the connection's view is fixed — "While its read transaction is active, X will continue to see an historic snapshot of the database prior to the changes implemented by Y". So parking a worker after its first SELECT and before its first write parks it holding a pinned snapshot, which is exactly where the barrier releases it. The IMMEDIATE path instead takes its write lock at BEGIN — "IMMEDIATE causes the database connection to start a new write immediately, without waiting for a write statement", and "The BEGIN IMMEDIATE might fail with SQLITE_BUSY if another write transaction is already active on another database connection" — so contention there is resolved before any statement the barrier could sit between, making an in-transaction rendezvous structurally unreachable rather than merely unhelpful (confirmed 2026-07-25).
- [SQLite — Result and Error Codes (`SQLITE_BUSY_SNAPSHOT`)](https://sqlite.org/rescode.html#busy_snapshot) — the specific failure the seven parked losers take on release, which is what turns the control's ≥1-`SQLITE_BUSY` assertion from probabilistic into structural. "The SQLITE_BUSY_SNAPSHOT error code is an extended error code for SQLITE_BUSY that occurs on WAL mode databases when a database connection tries to promote a read transaction into a write transaction but finds that another database connection has already written to the database and thus invalidated prior reads." Cited separately from the `BEGIN TRANSACTION` page above because that page documents the upgrade-or-`SQLITE_BUSY` rule without ever naming the extended code — this is its primary home (confirmed 2026-07-25).
- [`Atomics.wait()` (MDN)](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Atomics/wait) — the rendezvous primitive, and the source of the 3 s deadline's degrade-rather-than-hang property. The method "verifies that a shared memory location contains a given value and if so sleeps, awaiting a wake-up notification or a time out", and the same page enumerates its three returns: "not-equal" if the memory location does not match the given value, "ok" if woken by `Atomics.notify()`, or "timed-out" if the timeout expires. The timeout return is what lets a straggler trial fall back to the prior probabilistic behavior instead of blocking the suite, and the `not-equal` return is why the barrier re-checks the arrival count rather than trusting a single wake (confirmed 2026-07-25).
- [git-config(1) — `core.worktree` (git 2.50.0 documentation)](https://git-scm.com/docs/git-config/2.50.0) — the primary source for the repo-own-config redirect the T1.5 root verification exists to refuse. The variable is "honored even when set in a configuration file in a '.git' subdirectory of a directory and its value differs from the latter directory", and "Running Git commands in the '/path/to' directory will still use '/different/path' as the root of the work tree" — documented for the ordinary `.git/config` case, so the redirect needs no `--separate-git-dir` to exist; the gitfile shape (next entry) is only what makes the config file reachable without owning the tree it names. Version-pinned to the git-scm 2.50.0 documentation page, which is where this page last changed and where the `/docs/git-config/2.50.1` URL canonicalizes — matching the git 2.50.1 the §Notes observations were reproduced on (confirmed 2026-07-25).
- [git-init(1) — `--separate-git-dir` (git 2.48.0 documentation)](https://git-scm.com/docs/git-init/2.48.0) — the primary source for the gitfile shape: "Instead of initializing the repository as a directory to either $GIT_DIR or ./.git/, create a text file there containing the path to the actual repository. This file acts as a filesystem-agnostic Git symbolic link to the repository." That text file sitting in the attached directory is what lets a hostile preparer point a work tree at a config they control. Version-pinned to the git-scm 2.48.0 documentation page — the version where this page last changed, and the canonical target of the `/docs/git-init/2.50.1` URL (confirmed 2026-07-25).
- [git-config(1) — ENVIRONMENT, `GIT_CONFIG_COUNT` family (git 2.50.0 documentation)](https://git-scm.com/docs/git-config/2.50.0) — the primary source for what the stripped env channels ARE: pairs that "will be added to the process's runtime configuration", and which "will override values in configuration files, but will be overridden by any explicit options passed via git -c". That documented precedence is why the observed `--show-toplevel` asymmetry (the env channel does NOT move it; the repository's own file DOES) is recorded in §Notes as a version-pinned observation with a suite mirror control rather than as documented behavior — plain precedence reasoning predicts the opposite, so the resolver trusts neither direction: it strips the injection channels AND verifies the reported root (confirmed 2026-07-25).
- [POSIX.1-2024 — `opendir()` (The Open Group Base Specifications Issue 8, IEEE Std 1003.1-2024)](https://pubs.opengroup.org/onlinepubs/9799919799/functions/opendir.html) — the primary source for the readability probe's error discrimination. `opendir()` shall fail with `[EACCES]` when "Search permission is denied for the component of the path prefix of dirname or read permission is denied for dirname", and with `[ENOTDIR]` when "A component of dirname names an existing file that is neither a directory nor a symbolic link to a directory" — the read-permission clause is what makes an opendir-class probe the enumerability check a mode-`0111` directory fails, and the `[ENOTDIR]` clause is what lets the one probe subsume the round-2 file refusal. Quoted from the `opendir()` error list, not `fdopendir()`'s — the page's FIRST `[ENOTDIR]` entry belongs to `fdopendir()` and reads differently (confirmed 2026-07-25).
- [POSIX.1-2024 — `realpath()` (The Open Group Base Specifications Issue 8, IEEE Std 1003.1-2024)](https://pubs.opengroup.org/onlinepubs/9799919799/functions/realpath.html) — the primary source for the traversability-not-readability contrast the round-3 probe exists to close. `realpath()`'s sole shall-fail `[EACCES]` is "Search permission was denied for a component of the path prefix of file_name" — search on the PREFIX, never read on the final component; the page's one other `[EACCES]` is a may-fail confined to relative inputs, which the resolver's step-1 absoluteness gate refuses before `realpath` runs — which is why a mode-`0111` directory resolves through the resolver's step 2 yet fails the opendir probe: the two functions demand different permissions of the same directory (confirmed 2026-07-25).
- [git-worktree(1) (git 2.48.0 documentation)](https://git-scm.com/docs/git-worktree/2.48.0) (accessed 2026-08-17) — the `worktrees/<id>/` registration layout T-009-2B-2's bidirectional verification and `listRegisteredWorkTrees` enumerate; the `git worktree list` main-working-tree reporting the `--separate-git-dir` refusal arm leans on; and `--relative-paths` / `worktree.useRelativePaths` — the relative registration forms `listRegisteredWorkTrees` resolves against the registration file's own directory. Version-pinned to the git-scm 2.48.0 documentation page because the entry carries a version-specific claim: 2.48.0 is the release that introduced the relative-registration forms (the "git ≥ 2.48 relative registrations" claim in T-009-2B-2), so the pinned page is the earliest one documenting them
- [gitrepository-layout(5)](https://git-scm.com/docs/gitrepository-layout) (accessed 2026-08-17) — the `commondir` and `gitdir` layout files `readPointerFile` and the bidirectional worktree-link verification read, the `worktrees/` directory the registration enumeration walks, and the `modules/` submodule git-directory location behind the `core.worktree` candidate derivation (T-009-2B-1)
