# Plan-015: Workflow Authoring And Execution

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `015` |
| **Slug** | `workflow-authoring-and-execution` |
| **Date** | `2026-04-14` |
| **Amended** | `2026-04-22` (full engine V1 per [BL-097](../archive/backlog-archive.md) / [ADR-015](../decisions/015-v1-feature-scope-definition.md) amendment — was V1.1-deferred-subset at original approval; absorbs SA-24/29/30/31 per [Spec-015](../specs/015-workflow-authoring-and-execution.md) + [ADR-015 §Amendment History](../decisions/015-v1-feature-scope-definition.md#amendment-history)); 2026-08-10 (Tier-7 plan-readiness audit — promoted `review → approved`; adds `## Implementation Phase Sequence`, `## Invariants`, `## Cross-Plan Obligations`, `## Upstream-Tier Amendments Required`, and the audit-gate Precondition; adds ADR-012 + ADR-017 to Required ADRs; ratifies the three-value workflow-definition scope domain `session` / `project` / `shared` across the spec, the contract doc, and the DDL, with the `scope_ref` companion column and the `(scope, scope_ref, content_hash)` dedupe key that make the tier storable; mints `workflow.definitionList`, the enumeration the declared operation set lacked; adds the visual workflow builder — [Spec-015 §Visual Workflow Builder](../specs/015-workflow-authoring-and-execution.md#visual-workflow-builder), the graph ↔ definition mapping and its refusal set, reference-only tool bindings, canvas layout held outside the hashed definition bytes, copy-on-write editing of `shared` definitions, the canonical definition file form, and the `sidekicks workflow` command set — paired with ADR-026); 2026-08-11 (chat-start amendment, paired with ADR-027 — adds the three chat-adjacent callers of `workflow.runStart` ([Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization), SA-38/SA-39/C-18): tasks T5.8–T5.10, invariants I-015-17/I-015-18, obligations CP-015-7 (Plan-004 callback-tool host) and CP-015-8 (Plan-010 named action + chat-borne principal carrier), the Tier-5 Plan-010 upstream delta, the `workflow.start_denied` registration, and the additive-optional `WorkflowRunStartRequest.channelId`; flips the plan `approved → review` scoped to that growth; restored `approved` the same day by the paired targeted readiness-audit delta, PR #320); 2026-08-16 (workflow-hardening amendment, paired with the Spec-015 amendment of the same date — adds the park-and-resume surface: tasks T1.9, T2.5, T2.6, T5.11, T5.12, T5.13; invariants I-015-19…I-015-24; obligations CP-015-9 (Plan-018 diagnostic tier — a same-tier consumption, not an upstream-tier amendment) and CP-015-10 (Plan-004 typed usage-limit signal — a sealed Tier-3 amendment, so it joins `## Upstream-Tier Amendments Required` as its fourth row); four projection columns on existing Plan-015-owned tables with **no** new table (two tables at this segment's writing; consolidated onto `workflow_phase_states` alone at the 2026-08-17 review-round segment below); the `workflow.phase_suspended` and `workflow.resumed` additive-MINOR payload widenings; Implementation Step 11; and two born-unchecked §Preconditions boxes — the Plan-004 signal registration and the Plan-018 bucket registration, gating Phase 2 and Phase 5 at this segment's writing and re-homed to the Phase 2B and 5C supplements at the review round. Two censuses move and were re-derived rather than carried: the A-015-15 refusal-point count fifteen → eighteen and the non-security invariant count eleven → seventeen. **The refusal-point half of that reading is superseded in part:** the amendment's 2026-08-17 reciprocal pass carried it to **nineteen** when SA-41's fresh-entry refusal was added; `## Risks And Blockers` holds the current count and this clause records what the 2026-08-16 diff itself moved. Flips the plan `approved → review` scoped to that growth and **restores `approved` in the same swap** under the paired Spec-015/Plan-015 targeted readiness-audit delta's coverage — the 2026-08-10 flip-and-restore-in-one-swap shape rather than the 2026-08-11 two-PR shape — so the README plan-status census does not move. The nine-table schema, the eleven-method wire registry, and the 23-type / five-category event taxonomy are all unchanged); 2026-08-17 (workflow-hardening review round, same vehicle — seven Codex findings folded into the amendment: the durable schedule and fold key re-homed per-phase onto `workflow_phase_states` with the stronger per-row phase-exit CHECK (SA-32 parallel branches park concurrently, and a run-level value could hold only one); the suspension payload's optional `parkAttentionKey` member; the three SA-41 repair codes `workflow.repair_not_parked` / `workflow.repair_attempt_in_flight` / `workflow.repair_version_unaccountable` registered in `error-contracts.md §Workflow`, moving the A-015-15 surface to four-of-nineteen landed; the carrier-held tasks re-homed into explicit-label supplement Phases 2B, 5B, and 5C so each born-unchecked box holds exactly the tasks that read its surface; the engine-record cost fallback re-bounded to class-shedding; [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals) criterion 2 re-grounded; and I-015-24 gaining SA-41's fresh-entry boundary. No census moves; every disposition is recorded in the `## Progress Log` Codex-round entry); 2026-08-17 (workflow-hardening review round 2, same vehicle — ten Codex findings adjudicated, nine folded and one answered on the thread: the SA-42 operator-surface residual converted to the named gate [BL-151](../archive/backlog-archive.md) with the operation-and-event-together SA-25 constraint (T5.11 and the §Risks residual bullet carry it); SA-41's fresh-entry boundary widened **run-wide** across I-015-24, T5.12, the `error-contracts.md` row, the contract-doc enumeration, and the spec's three prose sites — the same refusal point widened, so the A-015-15 census holds at nineteen; T2.5 gaining the detach-interval crash-injection arm with the reproducibility closure recorded in §Risks; T3.2 gaining the `waiting-human` `parkCause` producer obligation; T5.11 gaining the `waiting_on_pool` cancellation fixture; CP-015-9 stating the bucket registration is a closed-set Spec-018/Spec-020/Plan-018 amendment whose storage shape is decided there (the schema doc's file-backed prejudgment corrected); and `api-payload-contracts.md` moved to the seven-code census. No census moves; dispositions in the `## Progress Log` round-2 entry); 2026-08-18 (park-surface and operator-controls amendment, paired with the Spec-015 amendment of the same date — closes the operator-reachability residual the workflow-hardening amendment recorded and the park-legibility gap its per-phase columns left on the read model. Adds tasks T5.14 and T5.15 to the Phase 5B supplement (renamed for the wider scope), invariants I-015-25 and I-015-26, obligation CP-015-11 (Plan-010 — the two named Cedar actions, registered by this amendment's own Spec-010 leg in the same diff, so it opens **no** upstream row and mints no box), Implementation Step 12, the two wire operations `workflow.runCancel` / `workflow.runResume` with their four payload types, the four additive-optional `PhaseState` park members, the `workflow.cancelled` lifecycle event, and the three SA-45 refusal codes in `error-contracts.md §Workflow`. Two censuses move and were re-derived rather than carried: the wire registry **eleven → thirteen** and the event taxonomy **23 → 24** (categories unmoved at five; `workflow_lifecycle` 6 → 7), with the A-015-15 surface moving to twenty-two points / ten codes / seven carrying and the owed extension **unmoved at fifteen**, because all three new points ship with their codes in the same diff. The nine-table schema is unchanged — no table, no column, no index: the four park columns and the `cancelled` run status this surface reads and writes landed with the workflow-hardening amendment. Flips the plan `approved → review` scoped to that growth and **restores `approved` in the same swap** under the paired Spec-015/Plan-015 targeted readiness-audit delta's coverage — the 2026-08-16 flip-and-restore-in-one-swap shape — so the README plan-status census does not move. Promotes [ADR-026](../decisions/026-visual-node-graph-workflow-authoring.md) and [ADR-027](../decisions/027-chat-invoked-workflow-start.md) `proposed → accepted` in the same swap, checking the two born-unchecked ADR boxes in `## Preconditions` and releasing the full-phase Gate-5 holds on Phases 1 and 5; completes [BL-151](../archive/backlog-archive.md) and archives it; and repairs the three coherence defects item 5 of the amendment brief names — the Phase-1 precondition's narrow "T1.7 and T1.8 only" reading, the two ADR boxes' false `## Rollout Order` sequencing pointer, and the node's omitted ADR-narrative half.) |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-015: Workflow Authoring And Execution](../specs/015-workflow-authoring-and-execution.md) |
| **Required ADRs** | [ADR-001](../decisions/001-session-is-the-primary-domain-object.md), [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-012](../decisions/012-cedar-approval-policy-engine.md) (added by the Tier-7 audit — `human-approval` gates and the per-turn moderation gate are Cedar-evaluated), [ADR-015](../decisions/015-v1-feature-scope-definition.md), [ADR-017](../decisions/017-shared-event-sourcing-scope.md) (added by the Tier-7 audit — the 34 `workflow.*` event types and the SA-26 dual anchor ride the shared event-sourcing scope), [ADR-018](../decisions/018-cross-version-compatibility.md), [ADR-026](../decisions/026-visual-node-graph-workflow-authoring.md) (added by the visual-builder amendment — the node-graph authoring surface, the React Flow library adoption, and the canonical-bytes / layout separation; landed `proposed` with that PR, **promoted `accepted` 2026-08-18** by the park-surface and operator-controls amendment, which checked the scoped box below), [ADR-027](../decisions/027-chat-invoked-workflow-start.md) (added by the chat-start amendment — the reserved-`/` interception rule, the `@`-vs-`/` namespace split, and the named-action start authorization; landed `proposed` with that PR, **promoted `accepted` 2026-08-18** in the same swap, which checked its own scoped box below) |
| **Dependencies** | [Plan-005](./005-session-event-taxonomy-and-audit-log.md) (event taxonomy, integrity protocol), [Plan-010](./010-approvals-permissions-and-trust-boundaries.md) (approval records, Cedar policy), [Plan-012](./012-artifacts-files-and-attachments.md) (artifact manifests, OWASP upload), [Plan-013](./013-persistence-recovery-and-replay.md) (recovery, writer worker, replay), [Plan-014](./014-multi-agent-channels-and-orchestration.md) (channel lifecycle), [Plan-003](./003-queue-steer-pause-resume.md) (queue/steer), [Plan-004](./004-provider-driver-contract-and-capabilities.md) (tool-metadata resolution, CP-015-6; callback-tool host, CP-015-7 — added by the chat-start amendment) |
| **Cross-Plan Deps** | Cross-Plan Dependency Graph |
| **References** | [Spec-015](../specs/015-workflow-authoring-and-execution.md) (canonical contract surface; SA-24/29/30/31 narrative); [ADR-015 §Research Conducted](../decisions/015-v1-feature-scope-definition.md#research-conducted) (BL-097 primary-source corpus); see also `## References` at end of file |

## Goal

Implement versioned workflow definitions and durable workflow execution for the full V1 engine — four phase types, four gate types, parallel phases with named pools, and append-only hash-chained approval history — reusing existing session, run, approval, artifact, and recovery primitives.

## Scope

Workflow definition persistence (content-hashed, immutable versions), workflow run state, phase execution for all four V1 phase types (`single-agent`, `multi-agent`, `automated`, `human`), all four gate types, parallel phase execution with `ParallelJoinPolicy`, memory-gated admission, workflow-level gate resolution with per-run hash chain, OWN-only channel linkage, replay-deterministic projection rebuild, and the chat-invoked start surfaces with per-start authorization ([Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization), ADR-027 — added by the 2026-08-11 chat-start amendment).

## Non-Goals

- Marketplace or cross-machine workflow distribution. The `shared` scope tier is node-local reuse breadth, not distribution — a `shared` definition never leaves the daemon that stores it. Cross-machine portability at V1 is the operator-moved file form of [Spec-015 §Definition file form — export and import (C-17)](../specs/015-workflow-authoring-and-execution.md#definition-file-form--export-and-import-c-17), not a service.
- External workflow-engine integration
- Workflow editor polish beyond V1 ambition
- BIND channel ownership (V1.1 criterion-gated per ADR-015)
- Cross-node workflow dispatch (Spec-022 V1.1)
- Cross-run campaign memory and the durable wake-coalescing delivery substrate (V1.1 criterion-gated, deferred 2026-08-16 — the three criteria are stated once in [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals) and not restated here). This plan builds only the in-run fold: the park's provider-account attention key of T2.5 and its `park_attention_key` projection column, which crosses no aggregate boundary. No task here opens a per-watcher generation record, a claim / queue / deliver lifecycle, or any cross-run durable row.

## Preconditions

- [x] Paired spec is approved (amended 2026-04-22 for full-engine V1; the 2026-08-10 Tier-7 audit flipped Spec-015 `approved → review` for the ratified three-value scope-domain growth and the visual-builder amendment ([Spec-015 §Visual Workflow Builder](../specs/015-workflow-authoring-and-execution.md#visual-workflow-builder), its scope-ref widening, and the ADR-026 pairing) and restored it `approved` in the same swap under its own targeted coverage — the flip-and-restore-in-one-swap shape, so this box never re-opened. The 2026-08-11 chat-start amendment (ADR-027; [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization) — SA-38, SA-39, C-18) re-flipped Spec-015 `review`, re-opening this box scoped to that growth. **Restored 2026-08-11 (PR #320):** the paired Spec-015/Plan-015 targeted readiness-audit delta covered the chat-start growth and Spec-015 is `approved` again. The 2026-08-16 workflow-hardening amendment ([Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40), `§Frozen-definition repair (SA-41)`, `§Park integrity and cancellability (SA-42)`, `§Engine event record (SA-43)`, C-19) re-flipped Spec-015 `review` and **restored it `approved` in the same swap** under the paired targeted readiness-audit delta's coverage — the 2026-08-10 flip-and-restore-in-one-swap shape — so this box never re-opened for that growth and the README spec-status census does not move.)
- [x] Required ADRs are accepted (ADR-015 amended 2026-04-22; ADR-012 + ADR-017 added by the Tier-7 audit, both `accepted`). **Scope clause (2026-08-10, visual-builder amendment):** this box covers the Required-ADRs row's `accepted` entries. ADR-026 lands `proposed` in this same PR and its ratification is **not** carried here — it is carried by the named scoped box below, so this box does not silently assert an acceptance that has not happened. **Resolved 2026-08-18:** ADR-026 and ADR-027 are both promoted `accepted` (park-surface and operator-controls amendment), so every entry in the Required-ADRs row is now `accepted` and this box covers the whole row without a carve-out. The two scoped boxes below are checked in the same swap and the clause above is kept as the record of why they existed.
- [x] Blocking open questions are resolved or explicitly deferred (each remaining deferral is named under `## Upstream-Tier Amendments Required` and carries its own scoped box below)
- [x] **Plan-readiness audit complete per [`docs/operations/plan-implementation-readiness-audit-runbook.md`](../operations/plan-implementation-readiness-audit-runbook.md)** — Tier-7 audit (2026-08-10), the first-time `review → approved` promotion: 28 findings adjudicated (`A-015-01`…`A-015-29`, `A-015-26` folded into `A-015-01`); six criticals discharged in this swap (phase sequence, invariants, cross-plan obligations, contract-doc staleness, the `phase_type` CHECK, and this checkbox) plus the ratified scope-domain adjudication; two criticals routed to `## Upstream-Tier Amendments Required` per the runbook `§Cross-Tier Amendment Contingency` and mechanically gated by the two scoped boxes below. **PR #318 review round (2026-08-11): two mechanism-naming additions inside the same swap** — the SA-32 topology's persistence spelling (`dependsOn` / `parallelJoinPolicy` embedded in the canonical body, all-or-none, additive-optional per [ADR-018](../decisions/018-cross-version-compatibility.md)) and the SA-36 operator boundary's enforcement (the `shared`-scope create authorization adjudicated through the consumed Plan-010 `PermissionCheckService`, fail-closed — CP-015-2, T1.5). Each names the mechanism for a boundary the amended spec already ratifies rather than adding a requirement, their two refusals move the A-015-15 census thirteen → fifteen, and both are audited under this same swap's targeted coverage (the Plan-005 PR #278 self-audit shape), so no second flip loop opens. **Re-opened 2026-08-11 scoped to the chat-start amendment's growth only** (T5.8–T5.10, I-015-17, I-015-18, CP-015-7, CP-015-8, the Tier-5 Plan-010 upstream delta, and the two chat-start boxes below); every pre-amendment surface stays covered by the Tier-7 audit and its review round. **Restored 2026-08-11 (PR #320):** the paired Spec-015/Plan-015 targeted readiness-audit delta covered the amendment's growth — G4 traceability over T5.8–T5.10's `Spec coverage:` and `Verifies invariant:` cites with a seeded negative control passed on the corpus oracle, contract coherence across the C-18 grammar's four surfaces and the SA-39 refusal ordering's five, the `workflow.start_denied` / `channelId` legs, the CP-015-7/CP-015-8 one-writer boundaries, and the two chat-start boxes' fail-closed semantics — with zero content findings on the growth; the delta also completed the three README sites the amendment PR left without the flip's record (narrative appends — their counts already stood at the restored values) and moved the §Build Order census back to seven-`review`. **Re-opened 2026-08-16 scoped to the workflow-hardening amendment's growth only** (T1.9, T2.5, T2.6, T5.11–T5.13; I-015-19…I-015-24; CP-015-9, CP-015-10; Implementation Step 11; the four projection columns; the two additive-MINOR payload widenings; the Tier-3 Plan-004 upstream row; and the two carrier boxes below), **and re-checked in this same swap** — the paired Spec-015/Plan-015 targeted readiness-audit delta covering exactly that growth rides this PR rather than a following one (the 2026-08-10 flip-and-restore-in-one-swap shape; the Plan-005 PR #278 self-audit shape for a delta that audits its own vehicle), so no review window opens against a stale census. **Delivered:** claim-versus-content over this PR's own diff — every `Spec coverage:` marker on the six new tasks resolves to a heading that exists in the amended Spec-015 and every `Verifies invariant:` id resolves to a row this plan declares; census arithmetic re-derived from the Status headers rather than recalled (net-unchanged: 27 plans / 28 specs `approved`, the nine-table SQLite schema, the eleven-method wire registry, and the 23-type / five-category taxonomy all unmoved — the growth is columns on existing tables plus additive-MINOR payload members); a seeded-refutation negative control run against the Gate-4 oracle and passed (recorded in the `## Progress Log` delta entry); and marker-line integrity re-verified across the six new tasks (`Spec coverage:` unbackticked in the `Spec-NNN §Heading` form Gate 4 parses, `Verifies invariant:` carrying declared ids only). Every pre-amendment surface stays covered by the Tier-7 audit, its PR #318 review round, and the chat-start delta. **Hardening round (2026-08-17, same swap):** landing the amendment's corpus reciprocals — the schema doc's four columns, their two partial indexes, the structural half of SA-40's clear-on-exit rule (a `workflow_runs` terminal-status CHECK at this record's writing; re-homed with the columns onto `workflow_phase_states` as the stronger per-row phase-exit CHECK at the Codex round below), and the A-015-15 refusal-census re-derivation on the contract doc and `error-contracts.md` — surfaced one internal-coherence defect and four unstated rules, all folded into the amended sections rather than catalogued: SA-41's re-pin, bounded only by _parked_, together with SA-40's same-attempt-continuing resume licensed swapping a definition under an in-flight attempt, now closed by a sixth SA-41 rule carrying its own typed refusal (T5.12's third refusal; the A-015-15 census moves to nineteen); a schedule fire that fails to transition the run is not an exit and leaves the value armed, and an undecodable durable value is quarantined rather than fatal (both T2.6); the attention fold fails open toward the run's own record and never back into a retry, moving SA-40's fold constraints two → three (T2.5); and the engine record pairs dispatch-intent with outcome so silence between them is diagnostic (T5.13). The growth adds no task, no invariant, no obligation, and no census move beyond the refusal count, so this same swap's targeted coverage extends over it and no second flip loop opens. **Codex round (2026-08-17, same swap):** seven review findings adjudicated into the amendment rather than catalogued — the durable schedule and fold key re-homed per-phase (`workflow_runs` → `workflow_phase_states`, with the run-level terminal-status CHECK replaced by the stronger per-row phase-exit CHECK), the suspension payload's optional `parkAttentionKey` member so the projected fold key is a function of the log, the three `workflow.repair_*` codes registered in `error-contracts.md §Workflow` (four of the nineteen A-015-15 refusal points now carry codes), the carrier-held tasks re-homed into the explicit-label supplement Phases 2B / 5B / 5C so each born-unchecked box holds exactly the tasks that read its surface (with the ADR-026 / ADR-027 box narratives corrected to state their full-phase Gate-5 breadth honestly), the engine-record cost fallback re-bounded to class-shedding rather than an unqualified no-op, [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals) criterion 2 re-grounded (machine-derivable scale, operator-attested insufficiency), and I-015-24 gaining SA-41's fresh-entry boundary — all inside the ratified scope, so this swap's targeted coverage extends over the round (full dispositions: the `## Progress Log` 2026-08-17 Codex-round entry). **Codex round 2 (2026-08-17, same swap):** ten further findings adjudicated — nine folded as normative content (the SA-42 residual's named gate [BL-151](../archive/backlog-archive.md) with the SA-25 operation-and-event-together constraint; SA-41's fresh-entry boundary widened run-wide across the invariant, task, contract, and spec sites with the A-015-15 census holding at nineteen; T2.5's detach-interval crash-injection arm and its reproducibility closure; T3.2's `parkCause` producer obligation; T5.11's `waiting_on_pool` fixture; CP-015-9's registration-is-a-closed-set-amendment strengthening with the storage-shape prejudgment corrected in the schema doc; and the `api-payload-contracts.md` seven-code mirror) and one answered on the thread (the Phase-5B `external_plan_phase_merged 17/2B` chain — a structural seam dependency, with BL-151 as the real operator-reachability gate). No new capability and no census move, so this swap's targeted coverage extends over this round as well (full dispositions: the `## Progress Log` round-2 entry). **Re-opened 2026-08-18 scoped to the park-surface and operator-controls amendment's growth only** (T5.14, T5.15; I-015-25, I-015-26; CP-015-11; Implementation Step 12; the two wire operations and their four payload types; the four additive-optional `PhaseState` members; the `workflow.cancelled` event type; the three SA-45 refusal codes; and the Phase-5B supplement's widened scope), **and re-checked in this same swap** — the paired Spec-015/Plan-015 targeted readiness-audit delta covering exactly that growth rides this PR rather than a following one (the 2026-08-16 flip-and-restore-in-one-swap shape). **Delivered:** claim-versus-content over this PR's own diff — every `Spec coverage:` marker on T5.14 and T5.15 resolves byte-exactly to a heading the amended Spec-015 declares (`§Park surfacing on the read model (SA-44)`, `§Operator run control (SA-45)`, `§Truth vs projection vs ephemeral (SA-25)`) and every `Verifies invariant:` id resolves to a row this plan declares; both censuses re-derived from the amended contract doc and taxonomy rather than recalled (registry eleven → thirteen, taxonomy 23 → 24 with categories unmoved at five, A-015-15 nineteen → twenty-two points with seven carrying and the owed extension unmoved at fifteen, non-security invariants seventeen → nineteen, cross-plan obligations ten → eleven with the upstream-row count unmoved at four, and the SQLite nine-table census verified unmoved); the two shapes the growth deliberately does **not** take, each checked against the surface it would have disturbed — the `PhaseState.state` union stays five-valued, so the I-015-12 lockstep pin does not move, and the re-pin stays a resume-request member, so the registry grows by two rather than three; and one hardening applied in-round rather than deferred — the SA-45 projector rule was authored run-scoped and unconditional after the draft's engine-only spelling would have left I-015-25 quantifying over a rebuild the event alone could not produce. Every pre-amendment surface stays covered by the Tier-7 audit, its PR #318 review round, the chat-start delta, and the workflow-hardening amendment's rounds. **Governance leg (same swap):** ADR-026 and ADR-027 were read in full before promotion, their antitheses checked as genuinely steel-manned and their syntheses recorded, their Pre-Implementation Checklists discharged (build-time rows left open and annotated with the owning task rather than checked falsely), and `docs/archive/superpowers/plans/` and `docs/backlog.md` swept for scheduled work against either ADR before the flip — nothing found, so the promotion is not vetoed. **Spec-010 leg (same swap):** CP-015-11's two named Cedar actions are registered here rather than deferred — [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration extended **in place** on its existing line, with a tail recording blockquote placed below every inbound `Spec-010 line NNN` cite (30 distinct lines, the highest 144, re-derived in this diff rather than carried forward, so no cite moves) and Spec-010 flip-and-restored `approved` in the same swap. [Plan-010](./010-approvals-permissions-and-trust-boundaries.md) is deliberately **not** flipped: it gains no task, invariant, design decision, or behavior, holds no named-action enumeration of its own, and the adjudication call-sites are this plan's (T5.14), so the runbook's plan behavior-change row is not reached. The consequence for this plan's own gating is the point — registration and consumer land in one diff, so no born-unchecked box is minted, T5.14 is held by nothing, and the `Chat-borne user carrier + workflow::start action registered` box that discharged earlier the same day stays checked and **unwidened** rather than reopened to cover two actions it never held.
- [x] **OWN-channel turn budget provider registered (A-015-07; Tier-5 Plan-014 upstream delta):** [Spec-015 §Interfaces And Contracts](../specs/015-workflow-authoring-and-execution.md#interfaces-and-contracts) specifies the OWN channel's `turns_per_agent` budget and [Spec-015 §Workflow Timeline Integration](../specs/015-workflow-authoring-and-execution.md#workflow-timeline-integration) emits progress at 25 / 50 / 75% of it, but no shipped surface provides the value. The ratified resolution is that Plan-014 provides it as a `turnsPerAgent` member on the Plan-014-owned `ChannelConfig`, together with a `InterruptReason` member covering workflow-phase-driven interrupts (the SA-9 `REQUEST_CANCEL` / `TERMINATE` cascade, A-015-14). Plan-015 consumes both and authors neither; the Plan-014/Spec-014 restoring readiness-audit delta is the vehicle that lands them and checks this box. Phase 3 carries the Gate-5 `precondition_box_checked` entry, and Phases 4–5 chain behind Phase 3 so the auto-walk cannot hop the gate. `CP-015-5` is blocked on this delta. **Checked 2026-08-11 (PR #321):** that delta landed both legs — `ChannelConfig.turnsPerAgent` (Plan-014 D-014-23, T1.1 contract + T2.6 enforcement; [Spec-014 §Interfaces And Contracts](../specs/014-multi-agent-channels-and-orchestration.md#interfaces-and-contracts)) and the fourth `InterruptReason` member `workflow_phase_cancelled` ([Spec-014 §Stop Conditions](../specs/014-multi-agent-channels-and-orchestration.md#stop-conditions)) — restoring Spec-014 and Plan-014 `approved` in the same swap. Phase 3 dispatch still rides its remaining preconditions (Phase 2 merged; the Tier-3 registration delta; tier order — the contracts land as code at Plan-014's Tier-5 build).
- [x] **ADR-026 ratified accepted (visual-builder amendment, 2026-08-10):** ADR-026 governs the node-graph authoring surface — the graph model, the refusal set, reference-only tool bindings, the canonical-bytes / layout separation, and the React Flow library adoption — and lands `proposed` in this same PR, so the Required-ADRs box above deliberately does not carry it (see its scope clause). Until this box is checked, the builder surface does not dispatch: **T1.7, T1.8, T5.5, T5.6, and T5.7** are the tasks it exists to hold. Phase 1 and Phase 5 each carry a Gate-5 `precondition_box_checked` entry against this box — the two phases that own those tasks — following the admitting-principal carrier-box precedent Plan-010 set for a governance decision that ships alongside the plan it gates. Stated honestly (2026-08-17 review round): a Gate-5 entry gates its **whole declaring phase**, so while this box is open the entries hold every Phase-1 and Phase-5 task, not just the five builder tasks — an accepted, short-lived breadth rather than a defect, because what checks the box is ADR-026's `proposed → accepted` promotion — a same-repo governance PR carrying no code dependency of its own, so the breadth is bounded by that PR's landing rather than by another plan's schedule (unlike the two carrier boxes below, which wait on other plans' deltas and therefore hold task-scoped supplement phases instead). **Pointer corrected 2026-08-18:** this sentence previously credited `## Rollout Order` with sequencing the promotion. It does not — that section's five items map 1:1 onto the Phase-1…Phase-5 **code** sequence and name no governance PR. What mechanically holds the two phases is this box's Gate-5 `precondition_box_checked` entry in the Phase-1 and Phase-5 `preconditions:` blocks, and nothing else. What checks it: ADR-026's `proposed → accepted` promotion. **Checked 2026-08-18 (this PR):** ADR-026 is promoted `accepted` — its antithesis reviewed against its three steel-manned counter-arguments and its synthesis recorded, its Pre-Implementation Checklist discharged, and its Decision Log carrying the promotion row. The full-phase Gate-5 hold on Phases 1 and 5 is released; T1.7, T1.8, T5.5, T5.6, and T5.7 dispatch on their remaining gates and tier order.
- [x] **ADR-027 ratified accepted (chat-start amendment, 2026-08-11):** [ADR-027](../decisions/027-chat-invoked-workflow-start.md) governs the chat-start surface and the start-authorization shape — the reserved-`/` interception rule, the `@`-vs-`/` namespace split, the named-action-over-category authorization, and the daemon-resolved chat-borne principal — and lands `proposed` in the amendment PR, so the Required-ADRs box above does not carry it (the ADR-026 scope-clause precedent). Until this box is checked, the chat-start tasks do not dispatch: **T5.8, T5.9, and T5.10** are the tasks it exists to hold. Phase 5 carries a Gate-5 `precondition_box_checked` entry against this box — and, stated honestly (2026-08-17 review round), that entry holds every Phase-5 task while the box is open, the same accepted short-lived breadth as the ADR-026 box above: the ADR promotion that checks it is a same-repo governance PR carrying no code dependency of its own, and what mechanically holds Phase 5 meanwhile is this box's Gate-5 `precondition_box_checked` entry in that phase's `preconditions:` block (the same 2026-08-18 pointer correction the ADR-026 box records — `## Rollout Order` sequences code items, never a governance promotion). What checks it: ADR-027's `proposed → accepted` promotion. **Checked 2026-08-18 (this PR):** ADR-027 is promoted `accepted` — its three steel-manned counter-arguments each answered in the recorded synthesis, its Pre-Implementation Checklist discharged (`workflow.start_denied` is registered in `error-contracts.md §Workflow`, the reserved-`/` grammar is normative in [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization)), and its Decision Log carrying the promotion entry. The full-phase Gate-5 hold on Phase 5 is released; T5.8, T5.9, and T5.10 dispatch on their remaining gates — the chat-borne user carrier box below still holds chat-borne starts resolved from anything but the node owner, and the fail-closed I-015-18 state is what those three tasks ship until it checks.
- [x] **Chat-borne user carrier + `workflow::start` action registered (Tier-5 Plan-010 upstream delta):** [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39) adjudicates the named Cedar operation action `Action::"workflow::start"`, whose schema registration is Plan-010 surface, and requires the chat-borne authoring user as a daemon-resolved value — the admitting-principal-carrier class Plan-010 already tracks. Neither exists on a shipped surface, and a Tier-7 walk never amends a sealed Tier-5 pair, so both ride a Spec-010/Plan-010 targeted readiness-audit delta (`## Upstream-Tier Amendments Required`). This box does **not** hold T5.8–T5.10: they ship the fail-closed state — a chat-borne start resolves only the node-owner principal and every other chat-borne start refuses `workflow.start_denied` (I-015-18). Checking this box is what activates chat-borne starts resolved from the transport rather than from the node owner alone; `CP-015-8` is blocked on it. **Delivered 2026-08-18** by the Spec-010/Plan-010 admitting-principal carrier amendment and its in-swap targeted readiness-audit delta — precisely the vehicle this box named. Both halves landed: the **action registration**, with `Action::"workflow::start"` joining [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation Cedar action enumeration (extended in place on its existing line, so no inbound `Spec-010 line NNN` cite moved), adjudicated on the target workflow per [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39); and the **carrier class**, registered as a class rule in [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) — every admitting write records the daemon-resolved transport-authenticated principal on its own durable row — naming the shipped `approval_resolutions.approver_id` (D-010-12) as the first instance, `interventions.admitting_principal_id` (D-003-4, landed in the same amendment) as the second, and this plan's chat-borne workflow start as the third. What is deliberately **not** delivered here, and must not be: this plan's own principal column. Under one-writer discipline Plan-015 instantiates it when the chat-start surface lands, following the now-registered class rather than inventing a fourth convention — the class registration is what makes that instantiation a transcription instead of a design decision. Transport-resolved chat-borne starts are activated by this check; the fail-closed state T5.8–T5.10 ship (node-owner principal only, every other chat-borne start refusing `workflow.start_denied`, I-015-18) was never held by this box and is unchanged until those tasks dispatch.
- [x] **Typed provider usage-limit signal registered (Tier-3 Plan-004 upstream delta; workflow-hardening amendment, 2026-08-16; Delivered 2026-08-18 by the session-cost-receipt + provider-account amendment):** [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) recognizes a usage-limit refusal from a typed driver signal **only** (C-19), and no such signal is shipped: `RecoveryCondition` is closed at `recovery-needed` \| `reauth-required` and no registered driver error code names a spent usage allowance. The signal must carry a closed-set cause plus, where the provider reported one, a reset boundary stamped with its own provenance — a **sibling axis beside** `RecoveryCondition`, not a widening of it, following the `RecoverySpanClassification` precedent already recorded in `api-payload-contracts.md` (the two axes answer different questions, and a usage-limit park needs no operator). Its name and declaration site are Spec-004 / Plan-004 surface and a Tier-7 walk never amends a sealed Tier-3 pair, so it rides a combined Spec-004/Plan-004 targeted readiness-audit delta (`## Upstream-Tier Amendments Required`), which is what checks this box. Until then **T2.5 and T2.6 are held** — the two tasks that read the signal, homed in the explicit-label supplement **Phase 2B** so the hold reaches exactly them; every task of the numeric phases stays dispatch-eligible on its own gates, including T1.9, whose contract-and-DDL surface is independent of the driver vocabulary. Phase 2B carries the Gate-5 `precondition_box_checked` entry against this box — a Gate-5 entry gates its whole declaring phase, so carrying it on Phase 2 (the amendment's original placement, corrected at the 2026-08-17 review round) would have held T2.1–T2.4 too — and the numeric chain 2 → 3 → 4 → 5 never waits on it: supplement phases dispatch by explicit label only, the Plan-007 Phase 2B / Plan-022 Phase 3B shape. `CP-015-10` is blocked on it. **Delivered 2026-08-18.** The carrier landed through that combined Spec-004/Plan-004 targeted readiness-audit delta, in the same diff as this flip: [Spec-004 §Fallback Behavior](../specs/004-provider-driver-contract-and-capabilities.md#fallback-behavior) mints the typed provider usage-limit signal as a **sibling axis beside** `RecoveryCondition` — explicitly not a widening of it — carrying a closed-set cause and an optional reset boundary that stamps its own provenance, with recognition **typed-only** and an unrecognized shape yielding no signal rather than a synthesized one; [Plan-004 §Phase 3B — Provider-account seam + typed usage-limit signal](./004-provider-driver-contract-and-capabilities.md#phase-3b--provider-account-seam--typed-usage-limit-signal) builds it at T3.16 under `I-004-6`, and `CP-004-8` registers it. `RecoveryCondition` is unwidened, exactly as this box required. T2.5 and T2.6 are released, and Phase 2B is dispatch-eligible on its remaining gates. The fail-closed posture this box described is now historical rather than current: a quota refusal is recognized from the typed signal, and the prohibition on approximating it by reading message text stands permanently as `I-004-6`.
- [ ] **Engine-event diagnostic bucket registered (Plan-018, same tier; workflow-hardening amendment, 2026-08-16):** [Spec-015 §Engine event record (SA-43)](../specs/015-workflow-authoring-and-execution.md#engine-event-record-sa-43) places the always-on per-run engine record on the **Plan-018-owned** bounded-retention diagnostic tier — the four-bucket tier of [Spec-018 §PII in Diagnostics](../specs/018-observability-and-failure-recovery.md#pii-in-diagnostics) with its ≤ 7-day default TTL, its `retention_policy_override` warning past 30 days, and its mandatory [Spec-020 §Shred Fan-Out](../specs/020-data-retention-and-gdpr.md#shred-fan-out) Path-3 membership. Plan-018 `CREATE`s the bucket and its retention row; Plan-015 writes into it and authors neither. This is **not** an upstream-tier amendment — Plan-018 is a Tier-7 sibling, not a sealed lower tier — so it is an ordinary same-tier consumption held by this box rather than by the runbook `§Cross-Tier Amendment Contingency`; the vehicle is a Plan-018 task in its own build. Until it checks, **T5.13 is held** — homed in the explicit-label supplement **Phase 5C** so the hold reaches exactly it — and no Plan-015 code opens a diagnostic sink of its own. Phase 5C carries the Gate-5 `precondition_box_checked` entry (a Gate-5 entry gates its whole declaring phase, so carrying it on Phase 5 — the amendment's original placement, corrected at the 2026-08-17 review round — would have held every Phase-5 task). `CP-015-9` is blocked on it. What checks it: Plan-018 registering the bucket with its TTL and shred-path membership, plus the reciprocal Spec-018 / Spec-020 enumeration edits.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/workflow/`
- `packages/runtime-daemon/src/workflow/workflow-definition-service.ts`
- `packages/runtime-daemon/src/workflow/workflow-run-service.ts`
- `packages/runtime-daemon/src/workflow/phase-executor.ts`
- `packages/runtime-daemon/src/workflow/parallel-join-resolver.ts`
- `packages/runtime-daemon/src/workflow/resource-pool-admitter.ts`
- `packages/runtime-daemon/src/workflow/gate-chain-writer.ts`
- `packages/runtime-daemon/src/workflow/gate-chain-verifier.ts`
- `packages/runtime-daemon/src/workflow/workflow-projector.ts`
- `packages/runtime-daemon/src/workflow/human-phase-form-service.ts`
- `packages/client-sdk/src/workflowClient.ts`
- `apps/desktop/src/renderer/src/console/workflows/`
- `apps/desktop/src/renderer/src/console/workflows/builder/` — the node-graph canvas, node palette, inspector, and connection rules. Sits **under** the Plan-015-owned renderer target area above, so it adds no ownership boundary.
- `apps/cli/src/commands/workflow-*.ts` — the `sidekicks workflow` command files, registered per the established per-file CLI ownership-crossing convention.
- `apps/desktop/src/renderer/src/console/workflows/chat-start/` — the `/workflow` command registry and its thirteen-verb parser, interception hook consumer, autocomplete source, and composer affordance (chat-start amendment). Sits **under** the Plan-015-owned renderer target area above, so it adds no ownership boundary; the interception hook itself extends the Plan-021-owned composer module, named at T5.8 dispatch.
- `packages/runtime-daemon/src/workflow/workflow-callback-tool.ts` — the sixteen `workflow_*` session callback tool definitions and their daemon-side handlers (chat-start amendment).
- `packages/runtime-daemon/src/workflow/park-scheduler.ts` — the durable auto-resume schedule: jitter derivation, arming, the deadline refusal, the post-rebuild boot sweep, and the clear-on-every-exit rule (workflow-hardening amendment).
- `packages/runtime-daemon/src/workflow/engine-event-log.ts` — the always-on engine event record writer and its correlation-key stamping (workflow-hardening amendment). It writes into the Plan-018-owned diagnostic bucket — whose storage shape Plan-018 decides at its registration amendment — and owns no retention, TTL, or shred behavior of its own.

`packages/contracts/src/workflow/` above is EXTENDed by the visual-builder amendment with the document, node and edge types, the graph refusal set, and the file-form parse / serialize surface; no new package is introduced.

**Dependencies this plan registers**, each landing in the package that runs it and each admitted by the reason [Spec-015 §Implementation Notes](../specs/015-workflow-authoring-and-execution.md#implementation-notes) records:

- `@dagrejs/dagre` 3.1.1 in `apps/desktop` and in `packages/runtime-daemon` alike, laying the canvas out left to right. Both homes are needed rather than one: the builder lays out what the author is editing, and the daemon lays out a document an agent wrote with no layout section, so it opens laid out rather than stacked at the origin.
- `jsonata` 2.2.2 in `packages/runtime-daemon` — the expression language, sandboxed by construction with no host access, with the binding namespace registered on top of it and one evaluation budgeted with an abort.
- `quickjs-emscripten` 0.32.0 in `packages/runtime-daemon`, the release WebAssembly variant — the Code node's quick-tier interpreter, held to a memory limit and an interrupt deadline, with nothing of the host bound into it. The budgets are this plan's, not the library's: it ships none.
- No library for the Code node's full tier: two programs the daemon spawns. `bun` runs JavaScript and TypeScript and installs the package and version the import line names; `uv run` runs Python and installs what the script's own inline metadata block declares. `bun` publishes its binary on npm at 1.4.2, so the daemon can carry it the way it carries any other pinned tool; `uv` does not, and how it reaches a person's machine is the open item recorded in `## Risks And Blockers`. Neither is linked into the daemon, so neither adds a bundle or a native addon.
- No sandbox of this plan's own for the full tier or the shell-command node: the run's provider wrapper is the boundary — `@anthropic-ai/sandbox-runtime` 0.0.77 on the Claude leg, `codex sandbox` under a named permission profile on the Codex leg — driven at the run's execution posture through the Plan-010 posture surface. An OS sandbox we wrote and maintained ourselves is the alternative this rejects: both providers already ship one for all three platforms, in the posture vocabulary the product already speaks, and a wrapper plus a profile is the whole bridge.
- `croner` 10.0.1 in `packages/runtime-daemon` — cron with timezones and a next-run computation for the schedule trigger. The durable next-fire row is the truth and the in-process timer is a cache over it, so a restart re-arms from the row.
- `picomatch` 4.0.7 as a direct `packages/runtime-daemon` dependency rather than the transitive one it is today, filtering the globs of Node's own recursive `fs.watch` for the file-event trigger. No watcher library is added.
- `@cedar-policy/cedar-wasm` on the Cedar line [ADR-012](../decisions/012-cedar-approval-policy-engine.md) pins is already the daemon's through the Plan-010 evaluation surface; every workflow policy action adjudicates through it and this plan adds no second policy engine and no second pin.
- `@xyflow/react` is already pinned in `apps/desktop` by Plan-021, in fully controlled mode; the builder consumes it and adds no second canvas library.

## Data And Storage Changes

- **The document is node-shaped, and its bytes are the hash preimage.** A workflow definition is one JSON document — the `WorkflowDocument` type this plan adds to `packages/contracts/src/workflow/` — whose canonical bytes are the RFC 8785 canonicalization of its hashed field list, BLAKE3'd ([Spec-015 §State And Data Implications](../specs/015-workflow-authoring-and-execution.md#state-and-data-implications)). It carries its schema version, its name and an optional description, exactly one `trigger` node, the rest of the graph as `nodes`, and `edges` each naming a source node and handle and a target node and handle ([Spec-015 §Graph model — nodes, ports, and edges (SA-32)](../specs/015-workflow-authoring-and-execution.md#graph-model--nodes-ports-and-edges-sa-32)). Two sections sit **outside** the hashed body: `layout` — a position per node, an optional viewport and the sticky notes — and `pinData`, an item array per node id. A node carries its id, its kind, the kind version written at insert, its display name, its sibling order, its params, and optionally whether it is disabled, its notes, its error disposition, its retry bounds, whether it executes once over the whole input, and whether it always outputs data. The document holds **no settings block**: what a failure does is set on the node that failed, how long step data is kept and how long a run may work are the two controls on the console's Runtime settings page over the machine-local settings surface ([Spec-006 §Required Behavior](../specs/006-local-ipc-and-daemon-control.md#required-behavior)), and the clock a schedule reads is a param on the schedule trigger itself.
- **A node is the authored unit; a phase is the run-time one, and the run-time vocabulary is unchanged.** The definition/execution separation stands as [Spec-015 §Interfaces And Contracts](../specs/015-workflow-authoring-and-execution.md#interfaces-and-contracts) states it: `WorkflowPhaseId` identifies a phase in the definition and `PhaseRunId` one execution of it, derived from the workflow run, the phase and the attempt number. One step record exists per node attempt, keyed by the run, the node and the attempt, and it is a projection over the events the run, approval and form pipelines already emit ([Spec-015 §Execution semantics](../specs/015-workflow-authoring-and-execution.md#execution-semantics), [Spec-015 §Truth vs projection vs ephemeral (SA-25)](../specs/015-workflow-authoring-and-execution.md#truth-vs-projection-vs-ephemeral-sa-25)). The four phase kinds are nodes like any other and compile down **at run time, not at save time**, their executors calling the existing run-admission, orchestration, approval and form paths when the node runs ([Spec-015 §Implementation Notes](../specs/015-workflow-authoring-and-execution.md#implementation-notes)); those attempts are what the `workflow_phase_states` rows, the `phase_outputs` rows and the `workflow_gate_resolutions` chain record, so `PhaseState`, `WorkflowPhaseId`, the `workflow.phase_*` events and the phase DDL all stay exactly as they are. The trigger node never enters the phase sequence, never receives a phase run id and never produces a phase-state row ([Spec-015 §Graph model — nodes, ports, and edges (SA-32)](../specs/015-workflow-authoring-and-execution.md#graph-model--nodes-ports-and-edges-sa-32)).
- Add the workflow schema per [Local SQLite Schema §Workflow Tables](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-015) (SA-24) — thirteen tables. Nine are the engine's: `workflow_definitions`, `workflow_versions`, `workflow_runs`, `workflow_phase_states`, `phase_outputs`, `workflow_gate_resolutions`, `parallel_join_state`, `workflow_channels`, `human_phase_form_state`. `workflow_definitions` and `workflow_versions` carry the document body as the hash preimage with a `layout_json` column beside it, because layout is outside the hash ([Spec-015 §Canvas layout is not definition bytes (SA-35)](../specs/015-workflow-authoring-and-execution.md#canvas-layout-is-not-definition-bytes-sa-35)). Four are added by the builder, the trigger surface and the runs surface:

| Table | Holds |
| --- | --- |
| `workflow_steps` | One row per step attempt: run id, node id, attempt, execution index, the source array, status, timings, the three payload references, cost, error. |
| `workflow_triggers` | One row per armed trigger: workflow id, node id, kind, config hash, next fire, last fire, whether it is enabled. |
| `workflow_webhook_tokens` | One row per workflow that has a webhook trigger: the token hash, when it was created, when it was last used. |
| `workflow_node_state` | The per-(workflow, node) key-value store holding trigger cursor state, so no mutable field lives on the document. |

- **A step's payloads are inline while they are small.** Its input, output and log sit on the step row under a 64 KiB cap; above the cap each becomes an artifact through the Plan-012 ingest pipeline and the row keeps the reference. One read serves either, and the panel that shows a payload says which of the two it is reading — so a run row stays small and a step's input and output stay addressable from a message.
- Source-of-truth hierarchy (SA-25): `session_events` remains canonical; `workflow_definitions`, `workflow_versions`, `phase_outputs` and `workflow_gate_resolutions` are immutable truth; `workflow_runs`, `workflow_phase_states`, `workflow_steps`, `parallel_join_state` and `workflow_channels` are projections rebuildable via Plan-013 `ProjectionRebuild`. The per-step record is a projection over events the run, approval and form pipelines already emit, which is what keeps a run's step history a function of the log rather than a second account of it. Three of the four added tables are **mutable truth** rather than projections: the armed-trigger rows, the webhook token rows and the per-node key-value store are facts about what this machine is armed to do next, which no event history reconstructs — the durable row is the truth and the in-process timer is a cache re-armed from it after a restart ([Spec-015 §Entry node and the V1 trigger surface (SA-37)](../specs/015-workflow-authoring-and-execution.md#entry-node-and-the-v1-trigger-surface-sa-37)).
- **A form draft is daemon-held.** `human_phase_form_state` carries the drafts of the human form kind and is written through `workflow.humanFormDraftSave`, so a half-filled form survives a reload rather than living in the window; typed unsent text is never put in window storage. Each autosave bumps the row's own draft version, the optimistic-concurrency token the next write checks against, and the row is keyed one draft per phase run and person.
- `workflow_gate_resolutions` carries a per-run BLAKE3 hash chain anchored to `session_events` via a dual-anchor payload (`gate_resolution_id` + `row_hash`) on the `workflow.gate_resolved` event (SA-26). Dual-hash: BLAKE3 for daemon-internal identity, SHA-256 reserved for Plan-012 artifact content (SA-27).
- **Workflow-definition scope is three-valued:** `session` | `project` | `shared`. `session` scopes a definition to its authoring session, `project` spans the sessions of one project, and `shared` is the cross-project tier — authored once and reusable by any project on the same daemon, resolved out of the same local definition store with no additional table and no sync path. Scope identity is carried by a `scope_ref` column alongside `scope`, in the shape [Spec-025 §Unified Inventory](../specs/025-mcp-server-configuration-and-governance.md#unified-inventory) already uses for scope-qualified bindings — the authoring session id at `session`, the canonical repository root (`repo_mounts.canonical_root`) at `project`, the empty string at `shared` — and the dedupe key is `(scope, scope_ref, content_hash)`, so a definition stored once at a scope is one row regardless of which session submitted it. A `scope`-only widening does not work: `project` would name no project, and a `(session_id, content_hash)` key would partition `shared` definitions by authoring session, so two sessions storing the same shared definition would produce two rows resolution cannot reconcile. `shared` widens **visibility and reuse breadth only**: it is not distribution, not a marketplace and not cross-machine sync — a `shared` definition never leaves the daemon that stores it, and [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals) continues to exclude marketplace and global-library distribution. The `workflow_definitions.scope` column carries the domain (`CHECK(scope IN ('session','project','shared'))`) while `session_id` keeps recording the authoring session at every scope, so provenance survives the widening.
- **Copy-on-write provenance column:** `workflow_definitions.parent_content_hash`, nullable, recording the content hash of the `shared` definition a row was branched from when an author edited a shared definition (T1.7, [Spec-015 §Definition scope in the builder (SA-36)](../specs/015-workflow-authoring-and-execution.md#definition-scope-in-the-builder-sa-36)). It is provenance and **not** part of the hashed body, so a branched definition and a from-scratch definition with identical bodies still carry the same `content_hash` and converge on the dedupe key — the intended behavior, since the two are the same definition. Editing a `shared` definition in place is forbidden by construction: definition rows are immutable and the table carries no updated-at column, so an in-place edit would append a version to the shared definition and silently repoint every project on the daemon that resolves it. No table is added by it.
- **`workflow_phase_states.phase_type` admits the four V1 phase types only:** `CHECK(phase_type IN ('single-agent','multi-agent','automated','human'))` per [Spec-015 §Phase-Type and Gate-Type Taxonomy](../specs/015-workflow-authoring-and-execution.md#phase-type-and-gate-type-taxonomy). Gate types live on the gate column, never here.
- **Admission reads the machine, and only its headroom fraction is configuration:** the SA-3 gate restated from [Spec-015 §Execution semantics](../specs/015-workflow-authoring-and-execution.md#execution-semantics) — capacity is `process.availableMemory()` re-read on every tick less a headroom fraction from local daemon configuration, the reservation a phase takes is seeded per phase type and then replaced by the observed resident size of that type's recent launches on this machine per provider, a ramp interval separates grants, and one phase is always admitted. There is no capacity constant, no phase-count ceiling and no slot pool, so nothing here is a number an operator calibrates. The observed sizes and the ramp state live in memory beside the reservations — ephemeral by the same rule (SA-25), re-learned rather than restored after a restart — and this plan's own admitter counts them, consuming no other plan's pool API and adding no dependency row.
- **Park-and-resume projection columns — four columns on `workflow_phase_states`, no new table.** `park_reason` (nullable, `CHECK(park_reason IN ('waiting-human','provider-usage-limited'))` — the closed union of [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42), in the DDL-CHECK-versus-contract lockstep I-015-12 enforces), `park_cause` (nullable text, the engine-authored sentence, bounded at 8 KiB and truncated at a UTF-8 code-point boundary with a visible marker — a durable operator-facing string with no bound is an unbounded write on the projection tier), `auto_resume_at` (nullable UTC instant — the durable schedule of [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40), a durable value rather than an in-memory timeout, so a parked phase resumes itself across a daemon restart) and `park_attention_key` (nullable text — provider plus account identity plus credential generation, the outage-scoped fold key; a correlation for presentation that gates no start, resume, retry or schedule fire). Per-phase rather than per-run, and the placement is load-bearing: under [Spec-015 §Graph model — nodes, ports, and edges (SA-32)](../specs/015-workflow-authoring-and-execution.md#graph-model--nodes-ports-and-edges-sa-32) two parallel branches of one run may park concurrently against different provider accounts with different reset boundaries, and a run-level schedule or fold key could hold only one of them — delaying or stranding the sibling and folding two distinct outages into one record. The table is already a projection, and all four values ride the suspension event that produced them, so the byte-equal `ProjectionRebuild` of I-015-10 holds with no exception and no schedule or fold key survives a rebuild the log does not contain. Two partial indexes back the boot sweep and the fold read: one on `workflow_phase_states(auto_resume_at)` where non-null, one on `workflow_phase_states(park_attention_key)` where non-null — both narrow by construction, and both stay the bare non-null form because the per-row CHECK already confines the columns to `state = 'suspended'`. That CHECK — `state = 'suspended' OR (auto_resume_at IS NULL AND park_attention_key IS NULL)` — makes the **phase-exit half** of SA-40's clear-on-exit rule structural rather than a code convention: SQLite enforces CHECKs per statement, so the same UPDATE that moves a phase out of `suspended` must null both live columns. The run-cancel half cannot be DDL-enforced, since run status lives in `workflow_runs` and a SQLite CHECK cannot cross tables, so SA-42's cancel clears the two live columns in the cancel's own unit of work — code-enforced and failure-injection-tested at T5.11 — while the park **record** columns carry no such coupling by design: `park_reason` and `park_cause` survive resume and cancellation untouched, so liveness is read from run status plus phase `state`, never from the presence of a park reason.
- **The engine event record is not a SQLite table (SA-43).** It is newline-delimited JSON on the Plan-018-owned bounded-retention diagnostic tier, so it adds no table, no write to the Plan-013 writer worker and no write amplification to the canonical event path — the reason the always-on posture is affordable at all. Plan-015 writes records and correlates them by `workflowRunId` / `phaseRunId` / `attemptNumber` / a monotonic per-run sequence; Plan-018 owns the bucket, its TTL and its shred-path membership (CP-015-9).
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions, index rationale, and write-amplification estimates.

## API And Transport Changes

- Add the `workflow` method root to shared contracts and the typed client SDK — **twenty-seven operations**, root plus camelCase tail on the Plan-014 `channel.rosterRead` / `orchestration.runCreate` convention. Thirteen are the engine's own surface; fourteen are added by the builder, the node catalog, the trigger surface and the runs surface. The method string is the wire identity and the PascalCase name is the contract-type name. Shapes live in [api-payload-contracts.md §Plan-015](../architecture/contracts/api-payload-contracts.md#plan-015--workflow-authoring-and-execution).

| Contract type | Method string | Note |
| --- | --- | --- |
| `WorkflowDefinitionCreate` | `workflow.definitionCreate` | Takes a workflow document, content-hashes it, persists version 1, and validates the full refusal set; a `shared`-target create clears the daemon's operator-scope authorization first |
| `WorkflowDefinitionRead` | `workflow.definitionRead` | Definition header plus the current version pointer; the latest version unless one is named |
| `WorkflowDefinitionUpdate` | `workflow.definitionUpdate` | A new immutable version of an existing definition, optimistic on the expected version; an edit to a `shared` definition branches rather than touching the original |
| `WorkflowDefinitionList` | `workflow.definitionList` | Scope-resolved enumeration, most-specific-first, deduped on `(scope, scope_ref, content_hash)`, each entry carrying its last run, its schedule and whether it is enabled |
| `WorkflowDefinitionDelete` | `workflow.definitionDelete` | Soft delete; runs keep their pinned versions, and the reply states how many |
| `WorkflowDefinitionExport` | `workflow.definitionExport` | The canonical file form of one version, layout section included |
| `WorkflowDefinitionImport` | `workflow.definitionImport` | Parse, then submit through the create path, so an import carries no governance state |
| `WorkflowVersionRead` | `workflow.versionRead` | One immutable version body, addressed by definition id and version number; a running instance stays pinned to its own |
| `WorkflowVersionChainRead` | `workflow.versionChainRead` | The chain one run's pinned version belongs to, addressed by that version id |
| `WorkflowEnabledSet` | `workflow.enabledSet` | Arms or disarms every trigger of one workflow; refuses where no trigger can arm |
| `WorkflowRunStart` | `workflow.runStart` | Binds a run to a pinned version, optionally with input items and a mode; adjudicates the named start action and refuses `workflow.start_denied` |
| `WorkflowRunRead` | `workflow.runRead` | Run header, the per-phase state projection with its four live park members, and the step array with each step's three payload references |
| `WorkflowRunList` | `workflow.runList` | The runs enumeration, with the filters the table narrows on and paging |
| `WorkflowRunCancel` | `workflow.runCancel` | Operator run cancellation; appends `workflow.cancelled` in the same unit of work as the status write (SA-45, I-015-25) |
| `WorkflowRunResume` | `workflow.runResume` | Operator resumption of a parked run, carrying the SA-41 re-pin as an optional request member rather than as an operation of its own |
| `WorkflowRunRetry` | `workflow.runRetry` | Re-runs from a named step with the prior run's step data pinned upstream, minting a new run in retry mode |
| `WorkflowRunsPauseSet` | `workflow.runsPauseSet` | The scheduler-wide hold on starting new runs: sets it and answers with how many starts are waiting. It addresses no run id, so it is neither of the two per-run controls |
| `WorkflowStepRead` | `workflow.stepRead` | One step's input, output or log by reference, paged and redacted |
| `WorkflowNodeExecute` | `workflow.nodeExecute` | Executes one node, or it and its ancestors, against pinned or prior input; the daemon computes the filtered run |
| `PhaseOutputRead` | `workflow.phaseOutputRead` | Immutable outputs for one phase run; a retry adds rows and never mutates (SA-16) |
| `WorkflowGateResolve` | `workflow.gateResolve` | Appends one gate-resolution row and its `session_events` anchor; the approval kind rides it |
| `WorkflowGateChainVerify` | `workflow.gateChainVerify` | Operator-triggered chain re-verification, backing the `sidekicks workflow verify-gate-chain <run_id>` subcommand |
| `HumanPhaseFormDraftSave` | `workflow.humanFormDraftSave` | Saves a form draft on the daemon; the human form kind activates it |
| `HumanPhaseFormSubmit` | `workflow.humanFormSubmit` | Optimistic-concurrency submit; writes `phase_outputs` |
| `WorkflowResultsPost` | `workflow.resultsPost` | Posts a run's results into the invoking session as its results row; the session comes from the call's own context and is never a parameter |
| `WorkflowSubscribe` | `workflow.subscribe` | Subscription: run, step and schedule notifications for the runs table and the canvas overlay, and the starting hold with its waiting count |
| `WorkflowKindList` | `workflow.kindList` | The node catalog with its param specs, so the palette, the inspector and an agent read one list |

- Carry workflow version ids, `phase_run_id`s (which double as the channel-owning phase id per SA-6), gate states, step boundaries and parallel-join resolution through timeline events — five categories under `workflow.*`, enumerated with their types in [Spec-015 §Workflow Timeline Integration](../specs/015-workflow-authoring-and-execution.md#workflow-timeline-integration). Their registration in the Plan-005 registry is [Spec-005](../specs/005-session-event-taxonomy-and-audit-log.md)'s, where the five categories and their type literals are enumerated as their own sections. Envelope follows [CloudEvents v1.0.2](https://github.com/cloudevents/spec/blob/v1.0.2/cloudevents/spec.md) additive-extension rules; semantic-convention naming aligns with [OpenTelemetry Semantic Conventions for Events](https://github.com/open-telemetry/semantic-conventions/blob/main/docs/general/events.md). Four ordering rules ride the step and trigger events and are asserted where the engine emits them: a step's started event precedes its finished or failed event; `workflow.run_waiting` precedes every resume of the same run; `workflow.schedule_fired` precedes the `workflow.started` it caused; and `workflow.results_posted` follows the terminal status of the run it names.
- Event payload schemas evolve additive-MINOR per [ADR-018](../decisions/018-cross-version-compatibility.md); the `row_hash` + `gate_resolution_id` fields on `workflow.gate_resolved` are such an addition, per the SA-26 dual-anchor scheme in §Data And Storage Changes above. The per-phase projection `WorkflowRunReadResponse` carries gains the four park members as additive-optional and **live-scoped** — emitted only while the phase is parked, which makes `parkReason` presence the wire's park discriminator and is why the `PhaseState.state` union is deliberately not widened with a sixth arm: widening it would move the I-015-12 lockstep pin and force a DDL, contract and taxonomy edit to express what an optional member already answers.
- Typed refusals per [error-contracts.md](../architecture/contracts/error-contracts.md) §Workflow. Ten codes are registered — `workflow.not_found`, `workflow.invalid_phase`, `workflow.gate_closed`, `workflow.start_denied`, the three SA-41 repair codes `workflow.repair_not_parked` / `workflow.repair_attempt_in_flight` / `workflow.repair_version_unaccountable`, and the three SA-45 operator-control codes `workflow.control_denied` / `workflow.run_not_cancellable` / `workflow.resume_not_parked` — against a surface that refuses in more ways than that: chain-break detection, a sandbox that cannot start while the run's posture is `sandboxed`, `fail-fast` sibling abort, a `max_phase_transitions` / `max_duration` breach, a human-form optimistic-concurrency conflict, a definition content-hash mismatch (I-015-5), an expression that does not parse (I-015-2), an invalid graph shape (any of the refused shapes, I-015-16), a scope-ref violation, a tool binding carrying an inline governance facet (I-015-14), an unknown top-level key in an imported definition file, an inconsistent topology spelling, and the `shared`-scope operator-authorization refusal a `shared`-target create evaluates before any write (CP-015-2). The refusal points the builder and the trigger surface add — a trigger that cannot arm, a webhook token that does not match, a Code node over its memory or time budget, an expression evaluation over its budget, and an import whose schema version is unknown — each land with a registered code in the same change as the refusal, and none ships unregistered. The extension covering the points that still carry no code is owed on that contract document and is **not taken by this plan** (A-015-15; the file is outside this plan's write scope); until it lands, no phase may mint an unregistered code, which is the constraint [Spec-015 §Loud-errors discipline (C-12)](../specs/015-workflow-authoring-and-execution.md#loud-errors-discipline-c-12) already imposes.
- **Chat start and the agent tools add no wire method.** The `/workflow` command root and the composer affordance are client-surface sugar resolving to the operations above (the [Spec-014 §Turn Policies](../specs/014-multi-agent-channels-and-orchestration.md#turn-policies) `@mention` precedent), and the sixteen workflow callback tools are `SessionCallbackTool`s rather than JSON-RPC methods (CP-015-7). `WorkflowRunStartRequest` carries an additive-optional `channelId` per [ADR-018](../decisions/018-cross-version-compatibility.md) — provenance and progress-surface binding, not an input to the role adjudication, but daemon-validated as a channel of the starting session before it binds a surface, so a `channelId` outside that session refuses. Each tool is adjudicated by a **named Cedar action, never by tool name** ([Spec-010 §Interfaces And Contracts](../specs/010-approvals-permissions-and-trust-boundaries.md#interfaces-and-contracts) registers all four and [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization) names them per tool): the read tools are gated on the session itself; authoring, updating, setting a schedule, enabling or disabling a workflow and posting a run's results take `Action::"workflow::author"`; running a workflow or executing one of its nodes takes `Action::"workflow::start"`; cancelling takes `Action::"workflow::cancel"` and resuming takes `Action::"workflow::resume"`. This plan consumes those adjudications through `PermissionCheckService` (CP-015-8, CP-015-11) and authors no Cedar schema entry.

## Invariants

The following invariants are **load-bearing** and MUST be preserved across all Plan-015 PRs and downstream extensions. Any change that would weaken or remove one requires a coordinated cross-plan amendment (see cross-plan-dependencies.md). `I-015-1` … `I-015-7` carry the seven SA-13 security invariants of [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) into first-class plan ids so every `#### Tasks` block can name the one it discharges; that section states each as non-conformant-if-violated and requires at least one failing test per invariant.

| ID | Invariant | Grounds in | Verified by |
| --- | --- | --- | --- |
| **I-015-1** | Argv-list-only execution: no author-controlled field ever reaches a shell-string command form — no `shell: true`, no `/bin/sh -c`. Shell metacharacters arrive at the process unshelled or the launch refuses. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I1) | T2.2, T4.2 |
| **I-015-2** | Typed substitution only: parameter expressions parse to a closed whitelisted AST or throw `ExpressionParseError`. No Jinja2, no `${…}` eval, no Turing-complete expression language over definition strings. A program runs only inside a boundary the interpreter or the operating system enforces at the run's execution posture — the quick tier's WebAssembly interpreter under its budgets, or a full-tier Code node and a `developer.shell` node inside the provider's own sandbox — and at `sandboxed` a sandbox that cannot start refuses the step instead of running it on the host. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I2) | T1.1, T1.5, T2.2 |
| **I-015-3** | Approver capability is a typed identity-bound capability evaluated through the Plan-010 Cedar path, never a submitter string and never "whoever can read"; an admin override is a distinct audited entry kind. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I3); [ADR-012 §Decision](../decisions/012-cedar-approval-policy-engine.md#decision) | T2.3, T3.3 |
| **I-015-4** | Secrets are referenced only (`secret://<scope>/<name>`), resolved at phase launch into env / stdin / named-file fd — never argv, never inline in a definition body, never in an emitted event or artifact. Log redaction is defense-in-depth on top, never the primary control. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I4) | T1.1, T2.2, T5.13 |
| **I-015-5** | External tool references resolve by content address, never by mutable tag; re-resolving a pinned reference whose bytes changed yields a typed `ContentHashMismatch` rather than a silent substitution. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I5) | T1.5, T2.2 |
| **I-015-6** | Human-phase uploads clear the Plan-012 OWASP pipeline unchanged — size cap, extension allowlist, magic-byte sniff, storage rename, path validation, AV hook, quarantine-on-fail. Plan-015 consumes that pipeline and reimplements no part of it. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I6) | T3.2 |
| **I-015-7** | `workflow_gate_resolutions` is append-only: no row is ever updated or deleted, per-run `sequence` is monotonic from 1, and each row's `row_hash` recomputes from `prev_hash \|\| JCS-canonical(row_body)`. Row-level tampering is detected at daemon start; replay evaluates at-approval-time policy, never current policy. | [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) (SA-13 invariant I7); [ADR-017 §Decision](../decisions/017-shared-event-sourcing-scope.md#decision) | T2.3, T5.1, T5.2 |
| **I-015-8** | The SA-20 event-ordering rules hold on every emitted sequence: `workflow.phase_admitted` precedes `workflow.phase_started`; `workflow.phase_cancelling` precedes `workflow.phase_failed` whenever `cancellation_reason` is non-null; a `workflow-phase`-scoped `workflow.gate_resolved` precedes the next `workflow.phase_started`; `workflow.channel_created_for_phase` precedes any event carrying that `channelId`; parallel cancellation emits one coordinator event plus per-sibling chains at a deterministic tick boundary, never mid-callback. | [Spec-015 §Workflow Timeline Integration](../specs/015-workflow-authoring-and-execution.md#workflow-timeline-integration) (SA-20 ordering invariants) | T2.1, T2.3, T3.1, T4.1 |
| **I-015-9** | The SA-26 dual anchor is a pairing, not a convention: no `workflow_gate_resolutions` row exists without its paired `session_events` row carrying `gate_resolution_id` + `row_hash`, and no such event exists without its row. Both are written by the same Plan-013 writer-worker unit of work, so a partial pair is unreachable rather than merely uncommon. | [Spec-015 §State And Data Implications](../specs/015-workflow-authoring-and-execution.md#state-and-data-implications) (SA-26 hash-chain scheme) | T2.3, T5.2 |
| **I-015-10** | The SA-25 truth/projection split holds: `workflow_definitions`, `workflow_versions`, `phase_outputs`, and `workflow_gate_resolutions` are immutable truth; `workflow_runs`, `workflow_phase_states`, `parallel_join_state`, `workflow_channels`, and `human_phase_form_state` rebuild byte-equal from `session_events` plus the truth tables through the Plan-013 `ProjectionRebuild` path. No projection ever consults request state. | [Spec-015 §State And Data Implications](../specs/015-workflow-authoring-and-execution.md#state-and-data-implications) (SA-25 truth vs projection vs ephemeral) | T1.4, T5.1 |
| **I-015-11** | Replay is deterministic: `phaseRunId` is derived from `BLAKE3(workflowRunId \|\| nodeId \|\| attemptNumber)` and never randomly minted, so replaying a run's history reproduces the identical `phaseRunId` sequence and final state without a `DeterminismViolationError`. | [Spec-015 §Deterministic identity (SA-21)](../specs/015-workflow-authoring-and-execution.md#deterministic-identity-sa-21) | T1.1, T5.1 |
| **I-015-12** | The phase-type, gate-type, failure-behavior, definition-scope, run-status, and park-reason domains stay in lockstep across the shared contracts, the DDL CHECK constraints, and the taxonomy — four phase types (`single-agent`, `multi-agent`, `automated`, `human`), four gate types, three failure behaviors, three scope values (`session`, `project`, `shared`), seven run statuses (`new`, `running`, `waiting`, `succeeded`, `failed`, `cancelled`, `crashed`), and — added 2026-08-16 — two park reasons (`waiting-human`, `provider-usage-limited`) — enforced by a conformance suite rather than by comment. Widening the park-reason union is an additive MINOR bump under [ADR-018](../decisions/018-cross-version-compatibility.md) that moves this pin, never a comment. The run-status domain is additionally **mirrored narratively** in [`docs/domain/workflow-model.md` §Workflow Run States](../domain/workflow-model.md#workflow-run-states), which is deliberately **not** a fourth conformance surface — a prose table is not machine-assertable, so the suite cannot reach it — but **is** a required same-diff sweep site: widening or renaming any domain this row pins obligates updating that mirror in the amending diff rather than leaving it to recall. | Plan-owned: the taxonomy is governed by [Spec-015 §Phase-Type and Gate-Type Taxonomy](../specs/015-workflow-authoring-and-execution.md#phase-type-and-gate-type-taxonomy), the scope domain by [Spec-015 §Resolved Questions and V1 Scope Decisions](../specs/015-workflow-authoring-and-execution.md#resolved-questions-and-v1-scope-decisions), and the park-reason union by [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42), but the contract-versus-DDL lockstep **mechanism** is a plan-designed enforcement no governing document requires. | T1.6, T1.9 |
| **I-015-13** | Definition enumeration never discloses a definition outside the caller's resolved scope set: a `session`-scoped definition reaches only its own session, a `project`-scoped one only sessions of that same `scope_ref` project, and `shared` the whole daemon. The filter is a query-level predicate on `(scope, scope_ref)`, never a post-fetch trim of a wider result set, so a caller in one project cannot observe another project's definitions — including by name, id, or count. | [Spec-015 §Core SDK and persistence contracts](../specs/015-workflow-authoring-and-execution.md#core-sdk-and-persistence-contracts) (the `WorkflowDefinitionList` scope-resolution rule added by the same Tier-7 amendment that minted the operation) | T1.5 |
| **I-015-14** | The builder can never author a definition that bypasses tool governance. A tool binding persisted in a workflow definition carries a reference only — the scope-qualified binding identity plus a tool name — and **no** `enabled`, `approvalMode`, or `idempotencyClass` facet. Those three are node-operator surface set exclusively through the Spec-025 override operations under Cedar authorization; a definition carrying one is rejected at parse, not merely ignored at launch, so an imported or hand-edited definition cannot smuggle a weakened posture onto a machine. Effective facets are read-only in the inspector, sourced live from the governance surface. | [Spec-015 §Tool bindings are references, never inline policy (SA-34)](../specs/015-workflow-authoring-and-execution.md#tool-bindings-are-references-never-inline-policy-sa-34); [Spec-025 §Tool-Level Overrides](../specs/025-mcp-server-configuration-and-governance.md#tool-level-overrides); [Spec-025 §Authorization](../specs/025-mcp-server-configuration-and-governance.md#authorization) | T1.8, T5.5, T5.6 |
| **I-015-15** | Canonical definition bytes are layout-independent. Canvas geometry is excluded from the JCS-canonicalized definition body and from the BLAKE3 hash preimage: moving every node in a definition and re-serializing yields a byte-identical body and an identical content hash, and mints no new version. The exclusion is hash-scope only — no field the engine reads may be relocated into the layout channel. | [Spec-015 §Canvas layout is not definition bytes (SA-35)](../specs/015-workflow-authoring-and-execution.md#canvas-layout-is-not-definition-bytes-sa-35); [Spec-015 §Truth vs projection vs ephemeral (SA-25)](../specs/015-workflow-authoring-and-execution.md#truth-vs-projection-vs-ephemeral-sa-25) | T1.8, T5.5, T5.6 |
| **I-015-16** | Edit-time graph validation mirrors the daemon's definition validation and never substitutes for it. Every refusal rule — a self-edge, a cycle among `main` handles save the edge that closes a loop body, a handle-type mismatch, a second edge into an input that accepts one, an edge whose target is the trigger node, an edge out of a terminal kind (a `done`-gated phase being the same case), a fan-out carrying no join policy or converging on more than one join point, and an orphan node or a document with no nodes, no trigger or more than one — is re-evaluated at `workflow.definitionCreate`, whose answer is authoritative. A client that skips or weakens a rule produces a typed refusal, never a persisted definition. No builder-only error code is minted; refusals use the codes registered in `error-contracts.md` §Workflow. | [Spec-015 §Graph shapes that are refused (SA-33)](../specs/015-workflow-authoring-and-execution.md#graph-shapes-that-are-refused-sa-33); [Spec-015 §Loud-errors discipline (C-12)](../specs/015-workflow-authoring-and-execution.md#loud-errors-discipline-c-12) | T1.8, T5.5, T5.7 |
| **I-015-17** | Console-word interception is total: a registered `/workflow` verb executes client-side and its text never composes into a channel message, agent context, or provider turn, on any path — the steer-through-composer surfaces that share the input box included. A word the bound provider's own enumeration carries is forwarded to that provider exactly as typed and only through the binding it was read under; anything else is sent as the user's own prose over the driver's neutralized outbound boundary ([Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization)). The runtime mints no unknown-command refusal and no literal-slash escape; a console verb whose argument its control cannot take answers with one flow row saying what the verb accepts and presses nothing. The command grammar exists at the human composer only — channel text, including agent-authored text, never parses as a command. | [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization) (the reserved-`/` interception rule, C-18); [Spec-014 §Turn Policies](../specs/014-multi-agent-channels-and-orchestration.md#turn-policies) (addressing-gated activation) | T5.8 |
| **I-015-18** | Every workflow-run start adjudicates the named Cedar operation action per start — CLI, desktop, chat command, composer affordance, and the `workflow_run` callback tool alike — with no approval-category traversal and no remembered grant. A matrix-denied principal, a chat-borne principal the transport cannot resolve, and a `channelId` naming a channel outside the starting session each refuse `workflow.start_denied`; the role adjudication precedes the channel validation — the channel read never executes for a matrix-denied principal; the callback tool stays withheld at spawn while the approval seam is unregistered, and a stray invocation answers `denied`, never `completed`. | [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39); [Spec-015 §Chat-start surface (SA-38)](../specs/015-workflow-authoring-and-execution.md#chat-start-surface-sa-38) | T5.9, T5.10 |
| **I-015-19** | A provider usage-limit refusal is recognized from the typed driver signal alone — never from message text, provider prose, an error string, a rate-limit window name, or a model id — and parks the phase immediately: no `max_retries` attempt is consumed, no new `phaseRunId` is minted, no `max_phase_transitions` counter increments, and the park happens even when the refusal reports no reset window. Absent a registered typed carrier the engine parks nothing and the refusal travels the ordinary failure path; it never approximates the signal by reading text. | [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) (C-19 typed-only recognition) | T2.5 |
| **I-015-20** | The durable resume schedule is armed only where the typed refusal's reset boundary is stamped provider-reported; its jitter offset is a **scaled — never modular** — pure function of `workflowRunId` that replay reproduces byte-identically; it is never armed at or past the run's `max_duration` deadline; it is re-armed from its durable row **after** projection rebuild and never fired inline during one; and every transition out of the park clears both the durable value and any armed timer in the same unit of work. The armed instant is carried by the suspension event, so the projection rebuilds from the log with no special case. | [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) (the five schedule rules); [Spec-015 §Truth vs projection vs ephemeral (SA-25)](../specs/015-workflow-authoring-and-execution.md#truth-vs-projection-vs-ephemeral-sa-25) | T2.6 |
| **I-015-21** | A parked run is cancellable from **every** suspended or waiting state without precondition and without waiting on anything; the cancel clears the durable resume schedule in the same unit of work, so no timer can resurrect it; and it preserves the record — the parked phase-state row, its `park_reason`, its bounded `park_cause`, and every completed phase output survive untouched. `park_cause` is bounded at 8 KiB, truncated at a UTF-8 code-point boundary with a visible marker, and is never written into a phase output, an artifact, or an agent-visible context. | [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42) | T5.11 |
| **I-015-22** | The engine event record is on for every run with no flag that turns it on; every record carries `workflowRunId`, `phaseRunId`, `attemptNumber`, and a monotonic per-run sequence; it carries no secret material and no raw provider prompt or completion text; and **no** replay, verification, projection-rebuild, or audit path reads it — its expiry degrades diagnosis only, never correctness. It is newline-delimited JSON on the Plan-018-owned bounded-retention bucket, never a SQLite table and never a canonical event. | [Spec-015 §Engine event record (SA-43)](../specs/015-workflow-authoring-and-execution.md#engine-event-record-sa-43); [Spec-018 §PII in Diagnostics](../specs/018-observability-and-failure-recovery.md#pii-in-diagnostics) (the bounded-retention tier it lands on) | T5.13 |
| **I-015-23** | The provider-account attention fold is a correlation, never a lock. Concurrently parked runs present as one attention record only when every one of them carries the same non-null attention key — a set mixing a usage-limit park with any other failure is never folded — and no start, resume, retry, or schedule fire consults the record or is gated by it. Any transition back to running invalidates a not-yet-surfaced record for that key, so a later refusal surfaces again rather than being suppressed by a stale one. The key is carried by the suspension event's optional `parkAttentionKey` member, so the projected `park_attention_key` column is a function of the log (SA-25) and a rebuild reconstructs the fold with no special case. | [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) (the two fold constraints); [Spec-015 §Loud-errors discipline (C-12)](../specs/015-workflow-authoring-and-execution.md#loud-errors-discipline-c-12) | T2.5 |
| **I-015-24** | A frozen-definition repair is explicit and audited or it does not happen: re-pinning to the definition's current version occurs only when the resuming operator requests it by name, never from a timer, an auto-resume, or an ordinary resume, never while the instance is running, and only where the resume enters its next phase **fresh** with no other non-terminal phase of the run holding an in-flight attempt — a re-pin requested on a park whose resume continues an in-flight attempt, or while a parked parallel sibling holds one (SA-32 branches park independently and the pin is run-level), refuses toward the explicit fresh-entry action that would discard the blocking attempt (SA-41's run-wide fresh-entry boundary), never swapping the definition under units already dispatched from the frozen bytes. It is recorded as an additive-optional member on `workflow.resumed` naming the version left and the version joined; it re-runs and rewrites no completed phase output and resets no `max_retries`, `max_phase_transitions`, or `max_duration` counter; and it is refused, leaving the run parked and unchanged, when the target version cannot account for every phase the run already completed. | [Spec-015 §Frozen-definition repair (SA-41)](../specs/015-workflow-authoring-and-execution.md#frozen-definition-repair-sa-41); [Spec-015 §Fallback Behavior](../specs/015-workflow-authoring-and-execution.md#fallback-behavior) (the frozen-version rule it bounds) | T5.12 |
| **I-015-25** | A run reaches the `cancelled` status durably only by appending `workflow.cancelled` in the **same unit of work** as the status write, and applying that event clears `park_reason`, `park_cause`, `auto_resume_at`, and `park_attention_key` on **every** phase row of the run. Both halves are load-bearing for the same reason: without the pairing, or without the run-scoped clearing rule, a `ProjectionRebuild` replays the run's last `workflow.phase_suspended` payload, restores its armed schedule and attention key, and resurrects a run the operator cancelled — so the byte-equal rebuild of I-015-10 fails on exactly the rows an operator most needs to trust. The clearing is unconditional and run-scoped rather than enumerated in the payload: SA-32 parallel branches park independently, several phases of one run may be parked at once, and every phase of a cancelled run is unparked by construction. A second cancel of an already-`cancelled` run replays the original event's identity rather than appending again. | [Spec-015 §Operator run control (SA-45)](../specs/015-workflow-authoring-and-execution.md#operator-run-control-sa-45); [Spec-015 §Truth vs projection vs ephemeral (SA-25)](../specs/015-workflow-authoring-and-execution.md#truth-vs-projection-vs-ephemeral-sa-25); [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42) | T5.14 |
| **I-015-26** | The four park members of the phase-state projection are **live, not record**: a response emits `parkReason`, `parkCause`, `autoResumeAt`, and `parkAttentionKey` for a phase if and only if that phase is parked at response-build time, so a phase that resumed, completed, failed, or belongs to a cancelled run carries none of them — including where the `park_reason` and `park_cause` **columns** still hold the record SA-42 preserves. Presence of `parkReason` is therefore the wire's park discriminator, and no sixth `PhaseState.state` arm is minted: the five-value run-status-facing union stays in lockstep with I-015-12 and the DDL. Emitting a stale park member on an unparked phase is a conformance failure, not a cosmetic one — it would make the discriminator lie. | [Spec-015 §Park surfacing on the read model (SA-44)](../specs/015-workflow-authoring-and-execution.md#park-surfacing-on-the-read-model-sa-44); [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42) (the record-versus-liveness split) | T5.14 |

## Cross-Plan Obligations

Plan-015 declares the following obligations on adjacent plans (or inherits obligations declared by them). Implementation cannot proceed — or must defer specific surfaces — without these being satisfied or explicitly staged. Census re-derived 2026-08-18: **three of the eleven** name a sealed-tier delta under `## Upstream-Tier Amendments Required` — `CP-015-5` (Tier 5, Plan-014 — discharged 2026-08-11), `CP-015-8` (Tier 5, Plan-010 — discharged 2026-08-18), and `CP-015-10` (Tier 3, Plan-004) — while a fourth, `CP-015-9`, is a **same-tier** consumption held by its own §Preconditions box rather than by an upstream-tier delta, because Plan-018 is a Tier-7 sibling and not a sealed lower tier, so the runbook `§Cross-Tier Amendment Contingency` does not reach it. `CP-015-11` (Tier 5, Plan-010) names **no** upstream row and holds no task: its two named-action registrations land in this same diff through the [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes) enumeration, so nothing is left for a sealed-tier delta to carry.

### CP-015-1 — Event registration (Plan-005)

The five `workflow.*` categories and every event type literal enumerated in [Spec-015 §Event types (SA-19)](../specs/015-workflow-authoring-and-execution.md#event-types-sa-19--34-types-under-workflow) are [Plan-005](./005-session-event-taxonomy-and-audit-log.md) registry surface. Plan-015 emits them and registers none itself, and that section is the enumeration's only home: a literal list added to the registration's shape changes nothing about it, while a count copied out here would drift behind the section that governs. The ratified precedent is the `mcp_governance` row in [Spec-005 §Event Type Summary](../specs/005-session-event-taxonomy-and-audit-log.md#event-type-summary) paired with Plan-025's CP-025-1: the category and its type literals are registered by a named Plan-005 task, and registration moves all three census sites in one PR — the summary table rows, the in-band total, and Plan-005's prose restatement of the registry census.

**Resolution.** Discharged: [Spec-005 §Workflow Lifecycle](../specs/005-session-event-taxonomy-and-audit-log.md#workflow-lifecycle-workflow_lifecycle) through §Workflow Gate Resolution register the five categories and every type literal, the [Plan-005 §Event Taxonomy Coverage](./005-session-event-taxonomy-and-audit-log.md#event-taxonomy-coverage) table names this plan as their emitter, and its census counts them. Consumed by T1.3, which authors the payload schemas and their union arms against those registry entries.

### CP-015-2 — Approvals and moderation (Plan-010)

The `human_phase_contribution` approval category (SA-12) and the per-turn moderation gate a `multi-agent` phase fires are [Plan-010](./010-approvals-permissions-and-trust-boundaries.md) surface. Plan-015 writes no Cedar policy, defines no approval category, and implements no gate evaluation — it consumes `PermissionCheckService` and the approval-blocking flow, and the `human-approval` gate type resolves through the same path (I-015-3, ADR-012). The `shared`-scope create authorization — the operator boundary of [Spec-015 §Core SDK and persistence contracts](../specs/015-workflow-authoring-and-execution.md#core-sdk-and-persistence-contracts) — rides the same consumed surface: T1.5 adjudicates it through `PermissionCheckService` against the daemon-resolved node-owner user identity and mints no policy, no action family, and no approval category of its own.

**Resolution.** Consumed by T1.5 (`shared`-scope create authorization), T2.3 (gate evaluation), T3.2 (human-phase submission), and T3.3 (per-turn moderation). Within-tier ordering: T3.3 lands after Plan-010's approval-service phase; a return-cite is owed from Plan-010 at its restoring readiness-audit delta.

### CP-015-3 — Artifact references and upload validation (Plan-012)

`phase_outputs` rows with `value_kind='artifact_ref'` reference [Plan-012](./012-artifacts-files-and-attachments.md) artifact manifests, and human-phase file uploads run Plan-012's OWASP validation pipeline unchanged (I-015-6). Plan-015 stores references, never bytes, and adds no second upload path.

**Resolution.** Consumed by T3.2. SHA-256 stays Plan-012's manifest-content algorithm while BLAKE3 stays daemon-internal identity (SA-27) — the two hashes are a deliberate split, not a drift to reconcile.

### CP-015-4 — Writer worker and projection rebuild (Plan-013)

Every workflow write routes through [Plan-013](./013-persistence-recovery-and-replay.md)'s single writer worker at its 50-event / 10 ms batch cadence — including the SA-26 dual-anchor pair, which must land as one unit of work for I-015-9 to be structural rather than aspirational. The five projection tables rebuild through Plan-013's `ProjectionRebuild` (I-015-10), and restart-safe resumption reuses Plan-013 runtime-binding restore plus `command_receipts` idempotency.

**Resolution.** Consumed by T1.5 (writer routing), T2.3 (dual-anchor unit of work), and T5.1 (rebuild + resumption). No Plan-015 code opens a write transaction of its own.

### CP-015-5 — Channel lifecycle and turn budget (Plan-014)

`workflow_channels.channel_id` is a foreign key into the [Plan-014](./014-multi-agent-channels-and-orchestration.md)-owned `channels` table, and OWN-channel creation and termination invoke Plan-014 channel operations over wire methods and events, never daemon internals — the consuming side of Plan-014's forward-declared CP-014-6, whose return-cite this audit supplies. Two legs were unprovided at audit time — the `turnsPerAgent` budget the OWN channel runs under, and an `InterruptReason` member for workflow-phase-driven interrupts, without which the SA-9 `REQUEST_CANCEL` and `TERMINATE` policies have no Plan-014 mechanism to invoke (A-015-07, A-015-14) — **both landed 2026-08-11 by the Plan-014/Spec-014 restoring readiness-audit delta (PR #321; Plan-014 D-014-23)**: `ChannelConfig.turnsPerAgent` and the fourth `InterruptReason` member `workflow_phase_cancelled`. `CLOSE_WITH_RECORDS_PRESERVED` maps onto Plan-014's `channel.archive` — non-destructive and terminal, which is exactly "records preserved". The 2026-08-11 chat-start amendment adds a third leg: T5.10's SA-38 `channelId` validation consumes the Plan-014-owned channel read at the run-start path — read-only; Plan-015 never mutates channel state or authors channel semantics.

**Resolution.** The mapping leg is stated in `## Implementation Steps` step 3 and consumed by T3.1. The two formerly-unprovided legs were blocked on the Tier-5 Plan-014 delta named in `## Upstream-Tier Amendments Required` and mechanically held by the `OWN-channel turn budget provider registered` §Preconditions box, whose `precondition_box_checked` entry rides Phase 3 with Phases 4–5 chained behind it — **that delta landed 2026-08-11 (PR #321) and the box is checked**; Phase 3 now rides its remaining gates only.

### CP-015-6 — Tool-binding resolution and governance (Plan-004 / Plan-025)

A workflow phase's tool bindings resolve through the [Spec-004](../specs/004-provider-driver-contract-and-capabilities.md) tool-metadata layer, with per-tool governance facets supplied by the [Plan-025](./025-mcp-server-configuration-and-governance.md) override and trust stores. Plan-015 **consumes** that resolution and authors none of it: it defines no override facet, writes no trust row, evaluates no Cedar policy for tool authorization, and adds no `mcp.*` operation. The definition's persisted binding is an identity reference only (I-015-14); the effective posture is read at launch and refuses there under the existing Spec-025 rules when a binding is untrusted or disabled.

**Resolution.** Not blocked. Plan-025 is a landed Tier-6 leaf and the consumption is read-only, so this obligation is a declared consumption with no upstream delta owed. Consumed by T1.8, T5.5, and T5.6. If a future amendment gives workflow definitions any write into the governance surface, that amendment — not this one — opens the cross-plan write.

### CP-015-7 — `workflow_run` callback tool (Plan-004)

The `workflow_run` session callback tool — the corpus's first concrete `SessionCallbackTool` — is defined by this plan (name, description, input schema, resolution semantics per [Spec-015 §Chat-start surface (SA-38)](../specs/015-workflow-authoring-and-execution.md#chat-start-surface-sa-38)) and registered into the [Plan-004](./004-provider-driver-contract-and-capabilities.md) callback-tool host registry at session spawn. The host owns dispatch: every invocation routes through the CP-004-7 `approval.requestCreate` Cedar seam, is answered (`completed` / `denied` / `failed`) with no invocation left unanswered, and lands as an ordinary `tool_activity` row. While the seam is unregistered the registry is withheld at spawn — the agent leg is born-withheld and Plan-015 neither bypasses nor re-implements any part of the host's fail-closed contract.

**Resolution.** Consumed by T5.9. Plan-015 authors the tool definition and its daemon-side handler (which calls the same start path as `workflow.runStart`); Plan-004 authors the host and the seam; Plan-010 authors the Cedar route. A return-cite is owed from Plan-004 at its next touched delta.

### CP-015-8 — `workflow::start` named action + chat-borne principal carrier (Plan-010)

Registration of the named Cedar operation action `Action::"workflow::start"` in the Cedar schema and the chat-borne daemon-resolved user carrier are [Plan-010](./010-approvals-permissions-and-trust-boundaries.md) surface (the named-operation-action shape and the admitting-principal-carrier class both live there). Plan-015 consumes `PermissionCheckService` adjudication at the `workflow.runStart` handler and authors no Cedar schema entry, no policy, and no carrier — one-writer discipline. The delta's scope is threefold: the Cedar schema registration, the carrier, and the action's attribution joining the [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes) named-action enumeration.

**Resolution.** Blocked on the Tier-5 Plan-010 delta named in `## Upstream-Tier Amendments Required` and mechanically scoped by the `Chat-borne user carrier + workflow::start action registered` §Preconditions box: until it checks, T5.10 ships the fail-closed state (node-owner-only chat-borne resolution; every other chat-borne start refuses). Consumed by T5.9 and T5.10; a return-cite is owed from Plan-010 when the delta lands. **Discharged 2026-08-18** by the Spec-010/Plan-010 admitting-principal carrier amendment and its in-swap targeted readiness-audit delta, which delivers all three legs of the threefold scope above: **(1) the Cedar schema registration** and **(3) the action attribution** land together at [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration — that one site is both the schema-registration home and the attribution enumeration in this spec — extended **in place** on its existing line to carry `Action::"workflow::start"` on the target workflow, cited to [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39), so no inbound `Spec-010 line NNN` cite moved; **(2) the carrier** lands as the admitting-principal-carrier **class rule** in [api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model) — every admitting write records the daemon-resolved transport-authenticated principal on its own durable row — with three named instances: the shipped `approval_resolutions.approver_id` (Plan-010 D-010-12), `interventions.admitting_principal_id` (Plan-003 D-003-4, landed in the same amendment), and this plan's chat-borne workflow start. The **return-cite this row was owed is delivered** as Plan-010's `CP-010-12`, which names this row's discharge in its body rather than minting a further CP number. Unchanged by the discharge: Plan-015 still authors no Cedar schema entry, no policy, and no carrier column — under one-writer discipline it instantiates its own principal column against the now-registered class when the chat-start surface lands, which is a transcription rather than a design decision. T5.9 / T5.10 remain gated on the ADR-027 box, which this discharge does not touch.

### CP-015-9 — Engine-event diagnostic bucket (Plan-018)

The always-on engine event record of [Spec-015 §Engine event record (SA-43)](../specs/015-workflow-authoring-and-execution.md#engine-event-record-sa-43) lands on the bounded-retention diagnostic tier [Plan-018](./018-observability-and-failure-recovery.md) owns — the four-bucket tier [Spec-018 §PII in Diagnostics](../specs/018-observability-and-failure-recovery.md#pii-in-diagnostics) governs, with its ≤ 7-day default TTL, its `retention_policy_override` warning metric past a 30-day override, and its mandatory [Spec-020 §Shred Fan-Out](../specs/020-data-retention-and-gdpr.md#shred-fan-out) Path-3 membership. Plan-018 `CREATE`s the bucket, its retention row, and its shred-path registration; Plan-015 **writes records into it and authors none of that**. Plan-015's side is exactly three things: the record's content, its correlation keys (`workflowRunId`, `phaseRunId`, `attemptNumber`, a monotonic per-run sequence), and the no-secrets / no-raw-prompt-or-completion-text rule it writes under. Per-run is a **correlation, not a storage partition** — one bucket, keyed records — so the tier's retention and shred discipline applies uniformly and no run's records escape the fan-out by living somewhere the sweep does not look.

**Resolution.** Not an upstream-tier amendment: Plan-018 is a Tier-7 sibling, so this is a same-tier consumption held by the `Engine-event diagnostic bucket registered` §Preconditions box, whose Gate-5 `precondition_box_checked` entry rides the explicit-label supplement Phase 5C — the phase that carries exactly T5.13, so the hold reaches no other task. Consumed by T5.13. The reciprocal registration — the bucket's enumeration in [Spec-018 §Required Behavior](../specs/018-observability-and-failure-recovery.md#required-behavior) and [Spec-018 §PII in Diagnostics](../specs/018-observability-and-failure-recovery.md#pii-in-diagnostics), the [Spec-020 §PII Data Map](../specs/020-data-retention-and-gdpr.md#pii-data-map) bounded-retention row, and the Path-3 fan-out entry — is Plan-018 / Spec-018 / Spec-020 surface and is **not taken here**; a return-cite is owed from Plan-018 when it lands. That registration is a Spec-018/Spec-020/Plan-018 **amendment, not a row**: the tier's bucket set is closed at four (`driver_raw_events`, `command_output`, `tool_traces`, `reasoning_detail`), Plan-018's contract tests reject names outside it, and the existing buckets are runtime-local SQLite tables — so the fifth bucket's storage shape, and any table-census move it implies, are decided and recorded at that amendment, in that PR, never prejudged here. Until then no Plan-015 code opens a diagnostic sink of its own, because a PII-carrying sink outside the shred fan-out's reach is a [Spec-018 §PII in Diagnostics](../specs/018-observability-and-failure-recovery.md#pii-in-diagnostics) violation by construction.

### CP-015-10 — Typed provider usage-limit signal (Plan-004)

[Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) recognizes a usage-limit refusal from a typed driver signal **only** and forbids text matching outright (C-19), and no shipped surface carries one: [Spec-004 §Fallback Behavior](../specs/004-provider-driver-contract-and-capabilities.md#fallback-behavior)'s `RecoveryCondition` is closed at `recovery-needed` \| `reauth-required`, and no registered driver error code names a spent usage allowance. The signal is [Plan-004](./004-provider-driver-contract-and-capabilities.md) vocabulary — Plan-015 consumes it and mints neither its name nor its declaration site. What the amendment specifies is its required **content**, not its spelling: a closed-set cause identifying the refusal as a spent usage allowance, and — where the provider reported one — a reset boundary carrying its own provenance stamp, normalized and stamped at the driver boundary in the discipline [Spec-004 §Interfaces And Contracts](../specs/004-provider-driver-contract-and-capabilities.md#interfaces-and-contracts) already binds for `windowSource`. Two shape constraints are load-bearing. **It is a sibling axis, not a widening of `RecoveryCondition`:** [api-payload-contracts.md](../architecture/contracts/api-payload-contracts.md) already refused that widening once, minting `RecoverySpanClassification` beside it because the two axes answer different questions — and a usage-limit park needs no operator, so folding it into a set whose every member means _a human must act_ would misroute all of them. **It is not `usage.rate_limit_update`:** that account-plane snapshot is scoped by [Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry) as a display/quota surface kept off the security/admission plane, its `resetsAt` carries no provenance stamp, and its consumer obligations resolve fail-open to the freshest reading — the engine consumes it for nothing here, deliberately.

**Resolution — discharged 2026-08-18; the carrier is registered.** The Tier-3 Plan-004 delta this obligation waited on landed through the session-cost-receipt + provider-account amendment, which carries its own combined Spec-004/Plan-004 targeted readiness-audit delta — the CP-015-11 shape, registered by that leg rather than deferred to a further delta. [Spec-004 §Fallback Behavior](../specs/004-provider-driver-contract-and-capabilities.md#fallback-behavior) mints the typed provider usage-limit signal as a sibling axis beside `RecoveryCondition` (unwidened), carrying a closed-set cause and an optional provenance-stamped reset boundary, with typed-only recognition and an unrecognized shape yielding no signal; [Plan-004 §Phase 3B — Provider-account seam + typed usage-limit signal](./004-provider-driver-contract-and-capabilities.md#phase-3b--provider-account-seam--typed-usage-limit-signal) builds it at T3.16 under `I-004-6`, and `CP-004-8` is its registration. Consumed by T2.5 and T2.6, both now released — the `Typed provider usage-limit signal registered` §Preconditions box is checked, so the Gate-5 `precondition_box_checked` entry riding Phase 2B is satisfied and that supplement is dispatch-eligible on its remaining gates. The return-cite owed from Plan-004 is paid at [Plan-004 §Cross-Plan Obligations](./004-provider-driver-contract-and-capabilities.md#cross-plan-obligations) CP-004-8. The fail-closed posture recorded above is historical: a quota refusal is now recognized from the typed signal rather than burning the retry ladder, while the prohibition on text matching stands permanently as `I-004-6`.

### CP-015-11 — `workflow::cancel` and `workflow::resume` named actions (Plan-010)

Registration of the two named Cedar operation actions `Action::"workflow::cancel"` and `Action::"workflow::resume"` in the Cedar schema is [Plan-010](./010-approvals-permissions-and-trust-boundaries.md) surface, the same one-writer boundary CP-015-8 draws for `Action::"workflow::start"`. Plan-015 adjudicates both through the consumed `PermissionCheckService` at the `workflow.runCancel` and `workflow.runResume` handlers (T5.14) and authors no schema entry, no policy, and no approval category. They are **three separate actions, not one**, per [Spec-015 §Operator run control (SA-45)](../specs/015-workflow-authoring-and-execution.md#operator-run-control-sa-45): start creates work, cancel destroys it, and resume commits further spend against a provider account, so one action covering all three would make a grant of "may start a workflow" silently mean "may cancel anyone's". The registration's scope mirrors CP-015-8's: the two schema entries plus their attribution in the [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes) named-action enumeration.

**Resolution — registered in this same diff; no upstream row, no box.** The two schema entries and their attribution land through this amendment's own Spec-010 leg, extending [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration **in place** beside `Action::"workflow::start"`, rather than through a further Tier-5 delta. Three consequences, each deliberate. **(1)** This obligation names no `## Upstream-Tier Amendments Required` row and mints no §Preconditions box: there is nothing left for a sealed-tier delta to carry once registration and consumer land together. **(2)** The `Chat-borne user carrier + workflow::start action registered` box — discharged 2026-08-18 by the Spec-010/Plan-010 admitting-principal carrier amendment — stays checked and **unwidened**. Widening a discharged obligation to cover two actions it never held would reopen a closed row on paper for a registration that is already done; the correct record is a second obligation that resolves itself, not a first obligation reopened. **(3)** T5.14 therefore ships adjudicating against a **registered** schema, with no fail-closed interim to specify — unlike `Action::"workflow::start"`, which shipped under I-015-18's node-owner-only posture precisely because its registration trailed its consumer. The chat-borne principal carrier half of CP-015-8 is **not** a dependency here in any case: operator controls are invoked from the CLI and desktop surfaces, whose principals the transport already resolves. No Plan-010 return-cite is owed — that plan gains no task, invariant, or behavior from this leg, the adjudication call-sites being Plan-015's own (T5.14).

## Upstream-Tier Amendments Required

None is amended here, per the runbook `§Cross-Tier Amendment Contingency` — a Tier-N walk never amends a sealed Tier-K, K < N. Each has a named §Preconditions box: the first and third carry Gate-5 `precondition_box_checked` entries that hold dispatch, and the second's box scopes runtime behavior (the fail-closed chat-borne rule) rather than holding a task — so every deferral is machine-enforced or behavior-scoped, never prose-only. `CP-015-9` is deliberately **absent** from this list: Plan-018 is a Tier-7 sibling, not a sealed lower tier, so its obligation is an ordinary same-tier consumption held by its own box.

- **Tier 5 — OWN-channel turn budget and workflow interrupt reason.** [Spec-015 §Interfaces And Contracts](../specs/015-workflow-authoring-and-execution.md#interfaces-and-contracts) specifies `turns_per_agent` for the OWN channel and [Spec-015 §Workflow Timeline Integration](../specs/015-workflow-authoring-and-execution.md#workflow-timeline-integration) emits progress at 25 / 50 / 75% of that budget, but no `ChannelConfig` field, no `workflow_channels` column, and no Spec-014 default supplies it — and Plan-014's `InterruptReason` is a closed set (`budget_exhausted` / `idle_timeout` / `moderation_denied`) with no member a workflow-phase-driven interrupt can use. The ratified resolution keeps ownership with Plan-014: it adds `turnsPerAgent` to `ChannelConfig` and the workflow interrupt member to `InterruptReason`; Plan-015 sets the former at OWN-channel creation and names the latter in the SA-9 cascade, authoring neither. Vehicle: the Plan-014/Spec-014 restoring readiness-audit delta — **landed 2026-08-11 (PR #321; Plan-014 D-014-23, the member named `workflow_phase_cancelled`)**, discharging this leg and checking the `OWN-channel turn budget provider registered` box; `CP-015-5`'s resolution carries the record, and Phases 4–5 stay chained behind Phase 3's remaining gates.
- **Tier 5 — `workflow::start` action + chat-borne principal carrier.** [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39) adjudicates a named Cedar operation action whose schema literal Plan-010 owns, and requires the chat-borne authoring user as a daemon-resolved transport value — the admitting-principal-carrier class. Neither is shipped; a Tier-7 walk never amends a sealed Tier-5 pair. Vehicle: a Spec-010/Plan-010 targeted readiness-audit delta registering both — the Cedar schema entry, the carrier, and the `Action::"workflow::start"` attribution in the [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes) named-action enumeration (the family's per-owning-spec attribution convention). `CP-015-8` is blocked on it; the `Chat-borne user carrier + workflow::start action registered` box scopes the fail-closed behavior (it holds no task — T5.8–T5.10 ship fail-closed under the ADR-027 box). Vehicle **landed 2026-08-18** (the Spec-010/Plan-010 admitting-principal carrier amendment with its in-swap targeted readiness-audit delta; Plan-010 D-010-20 / I-010-25 / CP-010-12, Plan-003 D-003-4), discharging this leg and checking that box: the `Action::"workflow::start"` literal joined [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration in place, and the admitting-principal-carrier class is registered in `api-payload-contracts.md §Authenticated Principal And Authorization Model` with its shipped, new, and forthcoming instances named. `CP-015-8`'s resolution carries the full record; Plan-015 instantiates its own principal column against the registered class when the chat-start surface lands. **`CP-015-11` does not reopen this leg.** The park-surface and operator-controls amendment's two named actions — `Action::"workflow::cancel"` and `Action::"workflow::resume"` per [Spec-015 §Operator run control (SA-45)](../specs/015-workflow-authoring-and-execution.md#operator-run-control-sa-45) — are registered by **this** amendment's own Spec-010 leg rather than by a further upstream delta, extending the same named-operation-action enumeration in place under the same one-writer boundary. The discharged box above therefore stays checked and unwidened, no new upstream row opens, and no box holds T5.14: the registration and its consumer land in one diff.
- **Tier 3 — typed provider usage-limit signal.** [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) recognizes a usage-limit refusal from a typed driver signal only (C-19) and forbids text matching. At the time this row was written no such signal was shipped — `RecoveryCondition` was closed at `recovery-needed` \| `reauth-required` and no registered driver error code named a spent usage allowance. The ratified resolution keeps ownership with Plan-004: it mints the signal as a **sibling axis beside** `RecoveryCondition` — never a widening of it, following the `RecoverySpanClassification` precedent already recorded on `api-payload-contracts.md` — carrying a closed-set cause plus, where the provider reported one, a reset boundary stamped with its own provenance at the normalize boundary; Plan-015 consumes both and authors neither. The account-plane `usage.rate_limit_update` snapshot is explicitly not the carrier ([Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry) scopes it off the security/admission plane and it stamps no provenance), so the delta cannot be discharged by pointing the engine at it. Vehicle: a combined Spec-004/Plan-004 targeted readiness-audit delta. **Landed 2026-08-18** through the session-cost-receipt + provider-account amendment, which carried exactly that vehicle: the signal is minted at [Spec-004 §Fallback Behavior](../specs/004-provider-driver-contract-and-capabilities.md#fallback-behavior) as the sibling axis described above and built at [Plan-004 §Phase 3B — Provider-account seam + typed usage-limit signal](./004-provider-driver-contract-and-capabilities.md#phase-3b--provider-account-seam--typed-usage-limit-signal) T3.16 under `I-004-6`, registered as `CP-004-8`, with `RecoveryCondition` unwidened and the account-plane `usage.rate_limit_update` snapshot still not the carrier — that same amendment scopes the snapshot to `(providerAccountId, windowMins)` for account-scoped display, which is a different question from admission. `CP-015-10` is discharged and the `Typed provider usage-limit signal registered` box is checked, so this row is satisfied rather than outstanding; the fail-closed sentence it carried describes the pre-landing state.

## Implementation Steps

- Contracts: See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed schemas this plan consumes.

1. Define workflow-definition, version, phase-state, gate, and definition-read contracts in shared packages.
   - Full type hierarchy: `WorkflowDefinition`, `WorkflowVersion`, `WorkflowDocument` with its `WorkflowNode`, `WorkflowEdge` and `WorkflowLayout` members, `WorkflowRun`, `WorkflowStep`, `WorkflowPhaseRun`, `PhaseOutput`, `WorkflowGateResolution`, `ParallelJoinState`.
   - All 4 gate types: `auto-continue`, `quality-checks`, `human-approval`, `done`.
   - Entity separation: `WorkflowPhaseId` (the logical phase in the definition) vs `PhaseRunId` (one specific execution instance, which also anchors the OWN channel 1:1 per SA-6). `PhaseRunId` is **derived, never randomly minted**: every bit is a function of `BLAKE3(workflowRunId || nodeId || attemptNumber)` per [Spec-015 §Deterministic identity (SA-21)](../specs/015-workflow-authoring-and-execution.md#deterministic-identity-sa-21). It is therefore **not a ULID** — a ULID's leading 48 bits are a millisecond timestamp, which that preimage cannot produce, so the pre-audit "ULID" wording asserted determinism and a clock at once (A-015-10, Tier-7 audit). Treat it as a derived opaque identifier: the digest's text rendering is not yet ratified and is carried as an entry in [Spec-015 §Open Questions](../specs/015-workflow-authoring-and-execution.md#open-questions), so build the derivation behind a single helper (T1.1) that every call site goes through, and do not encode a rendering assumption anywhere else. The storage column is `TEXT` under every candidate rendering, so no schema work waits on the ruling.
   - Failure behaviors per phase: `retry` (new attempt row, C-9), `go-back-to` (bounded by `max_phase_transitions`), `stop`.
2. Implement durable workflow definition versioning and workflow-run persistence. Definition bodies are content-hashed via BLAKE3 over RFC 8785 JCS canonicalization; schema-version marker enforced by CHECK (C-8). All writes route through the Plan-013 single writer worker (50-event / 10 ms batch cadence).
3. Implement phase execution for all four V1 phase types:
   - `single-agent` — one agent, one driver adapter; inherits Plan-013 runtime-binding restore + `command_receipts` idempotency.
   - `multi-agent` — OWN channel 1:1 via `workflow_channels`; cancel cascade honors `CLOSE_WITH_RECORDS_PRESERVED` with a 30s grace window (SA-9). Per-turn moderation gate fires per Plan-010. **SA-9 termination-policy → Plan-014 mechanism mapping (A-015-14, Tier-7 audit — the enum is Plan-015-owned, the mechanisms are not):** `CLOSE_WITH_RECORDS_PRESERVED` invokes Plan-014's `channel.archive`, whose `active`|`muted` → `archived` transition is terminal and non-destructive — exactly "records preserved". `REQUEST_CANCEL` interrupts each in-flight run in the channel through Plan-014's system-intervention path and then archives once the grace window drains; `TERMINATE` skips the grace window and interrupts immediately before archiving. Both cancel policies invoke the `InterruptReason` member representing a workflow-phase-driven interrupt — `workflow_phase_cancelled`, carried in Plan-014's four-member closed set since 2026-08-11 (D-014-23, PR #321, — the CP-015-5 provider leg, with the `OWN-channel turn budget provider registered` box checked by the same delta), so the two cancel arms carry no upstream provider block and ride Phase 3's remaining gates (stale pre-discharge tail corrected, PR #321 Codex round 2).
   - `automated` — subtype routes `auto-continue` / `quality-checks` / `done`; `quality-checks` writes a gate-resolution row.
   - `human` — form submission writes `phase_outputs` with `value_kind='artifact_ref'` when upload fields present (C-16). `human_phase_contribution` approval category (SA-12) covers non-approval phase submissions. Default timeout semantics per ADR-015 Decision D1: `timeout: "none" | Duration` with no default.
4. Implement `ParallelJoinPolicy` resolver (SA-4): `fail-fast` / `all-settled` / `any-success`. Cancel cascade is tick-synchronous; `cancel_wave_tick` on `parallel_join_state` records the executor tick for audit. Admission is the SA-3 memory gate restated in `## Data And Storage Changes` — available memory re-read each tick less a configured headroom fraction, a per-phase-type reservation learned from recent launches, a ramp interval between grants, and one phase always admitted — counted by this plan's own admitter, not another plan's pool API (A-015-17).
5. Implement workflow-gate resolution with append-only hash chain:
   - Writer-worker-only INSERTs to `workflow_gate_resolutions`; per-run `sequence` monotonic from 1.
   - `row_hash = BLAKE3(prev_hash || JCS-canonical(row_body))`; Ed25519 daemon signature over same bytes; optional approver signature.
   - Dual-anchor: every row paired with a `session_events` row (category `workflow_gate_resolution`) carrying `gate_resolution_id` + `row_hash` in payload (SA-26 per [Spec-015 §State And Data Implications](../specs/015-workflow-authoring-and-execution.md#state-and-data-implications)). Row and event land in one Plan-013 writer-worker unit of work, so a partial pair is structurally unreachable (I-015-9).
6. Implement restart-safe workflow resumption and `ProjectionRebuild` integration: projection tables rebuildable from `session_events` via Plan-013; hash-chain replay re-verifies each row in `sequence` order and halts on `chain_break_detected`. Per-row recompute walks `prev_hash || JCS-canonical(row_body)` and asserts equality with the persisted `row_hash` (flat hash-chain pattern per [Local SQLite Schema §Workflow Tables](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-015)).
7. Add `sidekicks workflow verify-gate-chain <run_id>` CLI subcommand exposing the dual-anchor verification procedure.
8. Add desktop workflow authoring, run-detail, and human-phase form surfaces backed by the shared client SDK. Human-phase drafts are daemon-held through `workflow.humanFormDraftSave`.
9. Ship the visual workflow builder: the graph refusal set and the layout-exclusion split in shared contracts, copy-on-write editing of `shared` definitions over the `scope_ref` column and three-tier resolution landed in steps 1–2, the canonical definition file form, the React Flow canvas in the renderer, and the `sidekicks workflow` command set ([Spec-015 §Visual Workflow Builder](../specs/015-workflow-authoring-and-execution.md#visual-workflow-builder), ADR-026).
10. Ship the chat-invoked start surface: per-start authorization on the shared run-start path, the sixteen `workflow_*` callback tools, and the intercepted `/workflow` command root with the composer affordance ([Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization), ADR-027).
11. Ship the park-and-resume surface: the four projection columns and the two additive-MINOR payload growths in contracts and the migration; typed usage-limit recognition and the immediate park in the executor, with the provider-account attention fold; the durable `auto_resume_at` schedule with scaled run-id-derived jitter, the deadline refusal, the post-rebuild boot sweep, and the clear-on-every-exit rule; always-on cancellability from every waiting state; the explicit, audited frozen-definition re-pin with its three registered refusals; and the always-on engine event record on the Plan-018-owned diagnostic tier ([Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40), [Spec-015 §Frozen-definition repair (SA-41)](../specs/015-workflow-authoring-and-execution.md#frozen-definition-repair-sa-41), [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42), [Spec-015 §Engine event record (SA-43)](../specs/015-workflow-authoring-and-execution.md#engine-event-record-sa-43)). The step mints no table, no event type, and no wire method: the park rides `workflow.phase_suspended`, the repair rides `workflow.resumed`, and the operator control surface stayed the recorded residual SA-42 named — closed by step 12 below.
12. Ship the operator recovery surface: the four additive-optional park members on the phase-state projection so a parked phase is renderable from one `workflow.runRead`; the `workflow.runCancel` and `workflow.runResume` daemon handlers with the SA-41 re-pin carried as an optional member of the resume request; the `workflow.cancelled` event appended in the cancel's own unit of work with the run-scoped projector rule that clears every phase's live park fields on replay; the three registered refusals and the two named Cedar operation actions adjudicated through the consumed `PermissionCheckService`; and the `sidekicks workflow cancel` / `resume` CLI verbs with their SDK methods ([Spec-015 §Park surfacing on the read model (SA-44)](../specs/015-workflow-authoring-and-execution.md#park-surfacing-on-the-read-model-sa-44), [Spec-015 §Operator run control (SA-45)](../specs/015-workflow-authoring-and-execution.md#operator-run-control-sa-45)). This step mints **no table and no column** — the four park columns and the `cancelled` run status it reads and writes landed in step 11.

## Implementation Phase Sequence

Authored by the Tier-7 plan-readiness audit (2026-08-10, A-015-01). The five numeric phases map 1:1 onto `## Rollout Order`, absorbing `## Implementation Steps` and `## Target Areas`; each phase carries a reviewer-checkable `**Precondition:**` line plus the machine-readable `preconditions:` block preflight consumes. Three explicit-label supplement phases — **2B, 5B, and 5C**, the Plan-007 Phase 2B / Plan-022 Phase 3B shape, added at the workflow-hardening amendment's 2026-08-17 review round — carve the amendment's carrier-held and supplement-chained tasks out of their numeric homes, because a Gate-5 `precondition_box_checked` entry gates its **entire declaring phase** (there is no task-level leg), so a born-unchecked carrier box carried on a numeric phase would hold every pre-amendment task in it. Supplements dispatch by explicit label only; the no-argument numeric walk resolves past them, so the numeric chain never waits on a supplement's box.

The `audit_status` entry rides every block, numeric and supplement alike, recording this audit as the phase's readiness evidence. The `OWN-channel turn budget provider registered` box gates Phase 3 specifically, and Phases 2–5 each chain on the preceding phase so the walk cannot hop over it. The `ADR-026 ratified accepted` box, added by the visual-builder amendment, rides Phases 1 and 5 — the two phases that own the five builder tasks — and, as a Gate-5 entry, held those full phases until ADR-026's promotion landed: an accepted, short-lived breadth, because the promotion is a same-repo governance PR carrying no code dependency of its own (the box's own narrative in `## Preconditions` records the same, with the 2026-08-18 correction that `## Rollout Order` sequences code items and never sequenced this promotion). The `ADR-027 ratified accepted` box, added by the chat-start amendment, rides Phase 5 under the same accepted shape. **Both promotions landed 2026-08-18 and both boxes are checked**, so neither entry holds a phase any longer; they stay in the block as satisfied gates, the shape every checked `precondition_box_checked` entry keeps. The workflow-hardening amendment's two boxes instead ride supplements, because their producers are **other plans' deltas with no sequenced landing date** and a full-phase hold of unknown duration is not acceptable breadth: the `Typed provider usage-limit signal registered` box rides **Phase 2B**, the supplement that owns exactly the two tasks (T2.5, T2.6) which read the Plan-004 carrier, and the `Engine-event diagnostic bucket registered` box rides **Phase 5C**, the supplement that owns exactly T5.13 — the amendment's original Phase-2/Phase-5 placement, corrected at its 2026-08-17 review round, would have held T2.1–T2.4 and every Phase-5 task. Neither box holds T1.9, T5.11, or T5.12 — the contract-and-DDL surface is independent of both registrations, and cancellability and the repair (Phase 5B) are engine-internal, gated only on the phases whose seams they extend.

### Phase 1 — Contracts, schema, and writer integration

**Goal:** The shared contract surface, the `workflow.*` event payload schemas of every category Spec-015 lists, the thirteen-table migration, and definition/run persistence routed through the Plan-013 writer worker — everything later phases import.

**Scope:** `packages/contracts/src/workflow/` (NEW), `packages/runtime-daemon/src/migrations/` (EXTEND), `packages/runtime-daemon/src/workflow/workflow-definition-service.ts` + `workflow-run-service.ts` (NEW). Absorbs `## Implementation Steps` 1–2 plus step 9's contract and persistence legs and step 11's contract, payload, and migration leg; Rollout Order item 1.

**Precondition:** Tier-7 plan-readiness audit complete and ADR-026 promoted `accepted` — **phase-wide, not task-scoped**: a Gate-5 `precondition_box_checked` entry gates its whole declaring phase, so an open box holds every Phase-1 task, not only the two builder tasks T1.7 and T1.8. ADR-026 is `accepted` and its box is checked, so this leg is met and Phase-1 dispatch rides the audit alone.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  - { type: precondition_box_checked, box: "ADR-026 ratified accepted" }
```

#### Tasks

- **T1.1 — Workflow contract core.**
  - **Files:** `packages/contracts/src/workflow/` (NEW).
  - **Provides:** `WorkflowDefinition`, `WorkflowVersion`, `WorkflowDocument` with the `WorkflowNode`, `WorkflowEdge` and `WorkflowLayout` members that make up the authored body, `WorkflowRun`, `WorkflowStep`, `WorkflowPhaseRun`, `PhaseOutput`, `WorkflowGateResolution`, `ParallelJoinState` as strict Zod schemas; the four-value phase-type union, the four-value gate-type union, the three-value failure-behavior union, the three-value definition-scope union (`session` / `project` / `shared`), and the three-value parallel-join-policy union (`fail-fast` / `all-settled` / `any-success`); the single `PhaseRunId` derivation helper (BLAKE3 preimage; the digest's text rendering behind one seam pending the [Spec-015 §Open Questions](../specs/015-workflow-authoring-and-execution.md#open-questions) ruling) and the closed whitelisted substitution grammar with its `ExpressionParseError` refusal; the `secret://` reference type with no inline-material arm.
  - **Consumes:** `SessionId` branded-id factory (Plan-001, shipped); `ChannelId` (Plan-001, shipped); Plan-012 artifact-reference type (CP-015-3).
  - **Spec coverage:** Spec-015 §Interfaces And Contracts; Spec-015 §Phase-Type and Gate-Type Taxonomy.
  - **Verifies invariant:** I-015-2, I-015-4, I-015-11.
  - **Tests:** acceptance/rejection rows per union member; an inline secret literal is rejected at parse; a non-whitelisted expression throws `ExpressionParseError` rather than evaluating; `phaseRunId` derivation is stable across processes for a fixed preimage and differs on `attemptNumber`; a scope value outside the three-value domain is rejected.
- **T1.2 — Wire request/response pairs + `workflow` method registry.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.1).
  - **Provides:** the registry's request/response pairs field-for-field against [api-payload-contracts.md §Plan-015](../architecture/contracts/api-payload-contracts.md#plan-015--workflow-authoring-and-execution), bound to the `workflow.*` method strings registered there — `WorkflowRunCancel` and `WorkflowRunResume` ship at T5.14 with the engine rules they invoke; `WorkflowGateResolveResponse` carries the `gateResolutionId` + `rowHash` dual anchor; `WorkflowDefinitionListRequest` / `Response` carry the audit-minted enumeration. `workflow.humanFormDraftSave` ships with its handler, because the human form kind activates it and a form draft is daemon-held.
  - **Consumes:** T1.1 unions and branded ids.
  - **Spec coverage:** Spec-015 §Core SDK and persistence contracts.
  - **Verifies invariant:** I-015-9.
  - **Tests:** request-validation rows per pair; response parity assertions against the contract block; the gate-resolve response rejects a payload missing either anchor field; the list request accepts an omitted `scope` filter and round-trips its `limit` / `cursor` pagination arm while rejecting a cursor of the wrong shape; a `project`-scoped create with no `scopeRef` is a typed schema refusal while `session` and `shared` parse without one; the reserved draft-save method resolves in the registry but has no registered handler.
- **T1.3 — Event payload schemas + union registration.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND), `packages/contracts/src/event.ts` (EXTEND).
  - **Provides:** payload schemas for every `workflow.*` type across the five categories that [Spec-015 §Event types (SA-19)](../specs/015-workflow-authoring-and-execution.md#event-types-sa-19--34-types-under-workflow) enumerates, and their `SessionEventSchema` union registration, matching the Plan-005-owned registry entries (CP-015-1). The `workflow.gate_resolved` payload carries `gate_resolution_id` + `row_hash` as the additive-MINOR extension under ADR-018. A type's **schema** is authored here because contracts are this phase's unit of work, while its **producer** is its own task — `workflow.cancelled`'s is T5.14 — and neither implies the other.
  - **Consumes:** the [Plan-005](./005-session-event-taxonomy-and-audit-log.md) registry entries for the five `workflow_*` categories and their type literals (CP-015-1), against which this task's schemas and union arms are authored.
  - **Spec coverage:** Spec-015 §Workflow Timeline Integration; Spec-015 §Event envelope and category split (SA-18).
  - **Verifies invariant:** I-015-8.
  - **Tests:** the union discriminates every registered type, asserted by enumerating the registry rather than by a literal list, so a type added later fails until covered; each category's members parse and reject strictly; a pre-extension `workflow.gate_resolved` event still parses on replay (ADR-018 back-compat); ordering-rule fixtures reject an admitted-after-started sequence.
- **T1.4 — Migration: the workflow schema.**
  - **Files:** `packages/runtime-daemon/src/migrations/` (CREATE — next free version at PR-open time), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND).
  - **Provides:** the thirteen tables byte-matching [local-sqlite-schema.md §Workflow Tables](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-015) — including the three-value `workflow_definitions.scope` CHECK with its `scope_ref` companion column, the `CHECK((scope = 'shared') = (scope_ref = ''))` ref-presence rule, the `UNIQUE(scope, scope_ref, content_hash)` dedupe key, the per-definition `UNIQUE(definition_id, content_hash)` version key on `workflow_versions` — deliberately not global, so copy-on-write and `project → shared` promotion can reuse a version's bytes under a new definition id — and the four-value `workflow_phase_states.phase_type` CHECK (A-015-08, A-015-11) — with `human_phase_form_state` created empty per SA-28 and `workflow_channels.channel_id` declared as the foreign key into the Plan-014 `channels` table (CP-015-5).
  - **Consumes:** the Plan-001 migration-runner seam (shipped); the Plan-014 `channels` table (CP-015-5).
  - **Spec coverage:** Spec-015 §State And Data Implications; Spec-015 §Human form drafts (SA-28).
  - **Verifies invariant:** I-015-10, I-015-12.
  - **Tests:** migration up plus idempotence; CHECK-rejection rows for a fifth phase type, a gate type supplied as a phase type, and a fourth scope value; a `project`-scoped row with an empty `scope_ref` and a `shared` row with a non-empty one are both rejected; two sessions inserting the same `shared` definition body collide on the dedupe key rather than producing two rows; the same version bytes insert under two different definition ids but collide when re-inserted under one; an `automated` phase inserts successfully (the pre-audit constraint's regression); `human_phase_form_state` exists and is empty after migration; index presence.
- **T1.5 — Definition versioning + run persistence.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-definition-service.ts` (NEW), `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (NEW).
  - **Provides:** content-hashed immutable versions (BLAKE3 over RFC 8785 JCS canonicalization) with the schema-version marker enforced by CHECK (C-8); author-time rejection of every graph shape the refusal set names, evaluated over the document's own `edges` list — the complete declared topology, with no edge living outside it — and re-evaluated here rather than trusted from the client, the daemon's answer being authoritative (I-015-16); content-addressed external tool-reference pinning with a typed `ContentHashMismatch` on re-resolution; run creation binding a run to a pinned version; scope-resolved enumeration behind `workflow.definitionList`, walking `session` then `project` then `shared` most-specific-first and deduping on `(scope, scope_ref, content_hash)`; every write routed through the Plan-013 writer worker at its 50-event / 10 ms batch cadence (CP-015-4); and the `shared`-scope create authorization — every `workflow.definitionCreate` whose target scope is `shared` (direct authoring, file import, and the T1.7 promotion route alike) adjudicated through the Plan-010 Cedar evaluation surface against the daemon-resolved node-owner user identity before any write, failing closed while that identity resolution is unwired (CP-015-2).
  - **Consumes:** Plan-013 single writer worker (CP-015-4); the Plan-010 `PermissionCheckService` and the daemon-resolved node-owner user identity for the `shared`-scope create authorization (CP-015-2); T1.1–T1.4.
  - **Spec coverage:** Spec-015 §Required Behavior; Spec-015 §Execution semantics; Spec-015 §Core SDK and persistence contracts.
  - **Verifies invariant:** I-015-2, I-015-5, I-015-13.
  - **Tests:** identical bodies with differing key order hash identically (JCS); a cyclic document is refused at create, not at launch, while the edge that closes a loop body is accepted; a fan-out whose branches carry no join policy and one whose branches converge on more than one join point are both refused at create; editing a definition mints a new version and leaves a running instance on its pinned version; mutating pinned tool bytes yields `ContentHashMismatch`; a create at `project` scope with no `scopeRef` is a typed refusal while `session` and `shared` derive theirs; enumeration from a session in one project returns that session's, that project's, and the daemon's `shared` definitions and none of a second project's — asserted against the emitted query predicate as well as the result set, so a post-fetch trim cannot pass; no service path opens its own write transaction; a `shared`-target create whose adjudicated principal is not the resolved node owner is refused at the service seam before any row is written; the same create refuses — rather than proceeding — while the identity resolution is unavailable; and neither refusal silently downgrades the request to `project` or `session` scope.
- **T1.6 — Contract ↔ DDL conformance suite.**
  - **Files:** `packages/runtime-daemon/src/workflow/__tests__/workflow-schema-conformance.test.ts` (NEW).
  - **Provides:** mechanical lockstep checks between the T1.1 unions and the T1.4 CHECK lists — phase types, gate types, failure behaviors, definition scopes, parallel-join policies (against the `parallel_join_state.policy` CHECK), run statuses, phase-run statuses, and gate-result statuses — so the domains are an enforced pin rather than a documented one. The run-status row pins `WorkflowRunReadResponse.state` against the `workflow_runs.status` CHECK; a start response legitimately narrows to a subset of that domain, so the check is containment for start and equality for read.
  - **Consumes:** T1.1, T1.4.
  - **Spec coverage:** Spec-015 §Phase-Type and Gate-Type Taxonomy; Spec-015 §Resolved Questions and V1 Scope Decisions.
  - **Verifies invariant:** I-015-12.
  - **Tests:** the suite is the test — one row per pinned pair, each failing when either side drifts.
- **T1.7 — Copy-on-write editing and `project → shared` promotion.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.1), `packages/runtime-daemon/src/migrations/` (EXTEND the same migration as T1.4), `packages/runtime-daemon/src/workflow/workflow-definition-service.ts` (EXTEND from T1.5).
  - **Provides:** the `parent_content_hash` column on `workflow_definitions` and its `parentContentHash` contract member, recording copy-on-write provenance; the copy-on-write path that turns an edit of a `shared` definition into a **new** definition at the editing context's scope (`project` by default, `session` when there is no project context) carrying the shared original's content hash as its parent, leaving the `shared` row untouched; and `project → shared` promotion as an explicit, separately-invoked operator action that creates the definition at `shared` scope from the promoted version's exact bytes — expressed through the existing `workflow.definitionCreate` operation, so no twelfth method string is minted. The scope-ref column, its ref-presence CHECK, the `(scope, scope_ref, content_hash)` dedupe key, and the most-specific-first resolution order are **not** re-delivered here: they land at T1.4 and T1.5 and this task builds on them.
  - **Consumes:** T1.4 schema and T1.5 definition service; the canonical-root resolution already produced by the repo-mount layer (Plan-007, shipped) for the `project` ref value.
  - **Spec coverage:** Spec-015 §Definition scope in the builder (SA-36); Spec-015 §Resolved Questions and V1 Scope Decisions.
  - **Verifies invariant:** I-015-12, I-015-13.
  - **Tests:** editing a `shared` definition leaves the shared row's version count unchanged and produces a project-scope row whose `parent_content_hash` equals the shared original's content hash; the same edit made with no project context lands at `session` scope; promotion creates a `shared` row from byte-identical bytes, so the promoted definition's content hash equals the source version's — and the promoted version row inserts without colliding with the source version's row, because version-hash uniqueness is scoped per definition; a definition created with no parent carries a null parent hash and still hashes identically to the same bytes created with one, because the parent pointer is provenance and not part of the hashed body; and promotion invoked by a principal that does not resolve to the node owner refuses at the same authorization seam every `shared`-target create clears (T1.5), before any row is written.
- **T1.8 — Graph validation and the layout-exclusion boundary.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.1).
  - **Provides:** the eight-rule refusal set evaluated as a pure function over a document ([Spec-015 §Graph shapes that are refused (SA-33)](../specs/015-workflow-authoring-and-execution.md#graph-shapes-that-are-refused-sa-33)) — a self-edge, a cycle among `main` handles with the one exemption for the edge that closes a loop body, a handle-type mismatch, a second edge into an input that accepts one, an edge whose target is the trigger node, an edge out of a terminal kind, a fan-out carrying no join policy or converging on more than one join point, and an orphan node or a document with no nodes, no trigger or more than one; the handle grammar of `(mode, type, index)` parsed tolerantly with a fallback to the first `main` output, so a branch index and a connection type are recoverable from the stored edge alone; a node's params as **references only** — an agent definition, a provider account, a tool binding identity, a secret reference — never a nested node, with a phase node's gate type a property of that node rather than an element of its own; the `go-back-to` back-reference as a node property that is never an edge; and the layout-exclusion boundary as a typed split so the canonicalizer provably cannot see geometry or pinned data.
  - **Consumes:** T1.1's document, node and edge schemas and the phase-type / gate-type domains; the Plan-025-owned `McpServerBindingRef` union that a persisted tool binding composes, and the Spec-004 tool-metadata layer that resolves it at launch (CP-015-6) — neither is re-declared here.
  - **Spec coverage:** Spec-015 §Graph model — nodes, ports, and edges (SA-32); Spec-015 §Graph shapes that are refused (SA-33); Spec-015 §Canvas layout is not definition bytes (SA-35).
  - **Verifies invariant:** I-015-14, I-015-15, I-015-16.
  - **Tests:** each of the eight refusal rules has a minimal rejecting fixture and a minimal accepting neighbor; the loop exemption accepts the edge whose target is a loop node's own input while every other cycle is rejected; a `go-back-to` drawn as an edge is rejected before any daemon call; a handle id naming a mode or type the kind does not declare resolves to the first `main` output rather than throwing; a tool binding carrying an `approvalMode`, an `enabled`, or an `idempotencyClass` value is rejected at parse; a param carrying a nested node rather than a reference is rejected at parse; perturbing every node coordinate and pinning items onto a node both leave the canonicalized body byte-identical.
- **T1.9 — Park-and-resume contract and schema surface.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.1), `packages/contracts/src/event.ts` (EXTEND from T1.3), `packages/runtime-daemon/src/migrations/` (EXTEND the same migration as T1.4), `packages/runtime-daemon/src/workflow/__tests__/workflow-schema-conformance.test.ts` (EXTEND from T1.6).
  - **Provides:** the closed two-value `ParkReason` union (`waiting-human`, `provider-usage-limited`); the four projection columns of `## Data And Storage Changes`, all on `workflow_phase_states` — `park_reason` with its two-value CHECK, `park_cause`, `auto_resume_at`, and `park_attention_key`, with the per-row clear-on-exit CHECK confining the two live columns to `state = 'suspended'` — plus the two partial indexes backing the boot sweep and the fold read; the bounded-cause helper that truncates at 8 KiB on a UTF-8 code-point boundary and appends a visible truncation marker, as the single seam every writer of `park_cause` goes through; the two additive-MINOR payload growths under [ADR-018](../decisions/018-cross-version-compatibility.md) — `workflow.phase_suspended` widening its `reason` to the `ParkReason` union and carrying `parkCause` plus an optional `autoResumeAt` and an optional `parkAttentionKey`, and `workflow.resumed` gaining the optional SA-41 version re-pin member — and the conformance rows pinning the union against the CHECK. Mints **no** table, **no** event type, and **no** method string.
  - **Consumes:** T1.1 unions, T1.3 event payload schemas, T1.4 migration, T1.6 conformance harness. Not held by the `Typed provider usage-limit signal registered` box: the union member is this plan's own vocabulary and the columns land on its own table — only the code that _recognizes_ a provider refusal needs the Plan-004 carrier.
  - **Spec coverage:** Spec-015 §Park integrity and cancellability (SA-42); Spec-015 §Workflow Timeline Integration.
  - **Verifies invariant:** I-015-12.
  - **Tests:** a third `park_reason` value is rejected by both the Zod union and the DDL CHECK, and the conformance row fails when either side is widened alone; a pre-growth `workflow.phase_suspended` event carrying the bare `waiting-human` string still parses on replay and a pre-growth `workflow.resumed` parses without the re-pin member (ADR-018 back-compat, both directions); a `parkCause` exceeding 8 KiB is truncated at a code-point boundary — a multi-byte grapheme straddling the limit is never split into an invalid sequence — and carries the visible marker, while one under the limit is byte-unchanged and unmarked; `auto_resume_at` and `park_attention_key` accept null and round-trip a value on a `suspended` row, and the per-row CHECK rejects either value on a row in any other state — including the single-statement transition out of `suspended` that fails to null them; both partial indexes exist and cover only non-null rows; the migration is idempotent and the thirteen-table count is unchanged after it runs.

### Phase 2 — Sequential execution and the gate chain

**Goal:** `single-agent` and `automated` phases execute end to end under all four gate types, with the append-only per-run hash chain and its dual anchor writing on every resolution.

**Scope:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (NEW), `gate-chain-writer.ts` (NEW). Absorbs `## Implementation Steps` 3 (`single-agent` and `automated` arms) and 5; Rollout Order item 2's open-dispatch head (its behind-the-box tail is the Phase 2B supplement below).

**Precondition:** Phase 1 merged; Plan-010's approval-service phase shipped for the `human-approval` gate arm (CP-015-2). The park tasks T2.5 and T2.6 live in the Phase 2B supplement below — this phase carries no entry against the `Typed provider usage-limit signal registered` box, so T2.1–T2.4 dispatch without waiting on the Plan-004 delta.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  - { type: plan_phase, plan: 015, phase: 1, status: merged }
```

#### Tasks

- **T2.1 — Sequential phase executor + `single-agent` arm.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (NEW).
  - **Provides:** the Kahn-style ready-set tick loop over the phase DAG; the `single-agent` arm (one agent, one driver adapter) inheriting Plan-013 runtime-binding restore and `command_receipts` idempotency; `workflow.phase_*` emission in SA-20 order; `workflow.phase_progressed` at turn-completion and tool-invocation boundaries.
  - **Consumes:** T1.1–T1.5; Plan-013 runtime-binding restore (CP-015-4).
  - **Spec coverage:** Spec-015 §Execution semantics; Spec-015 §Default Behavior.
  - **Verifies invariant:** I-015-8.
  - **Tests:** ready-set admission order is deterministic for a fixed DAG; `phase_admitted` always precedes `phase_started`; a restart mid-phase resumes without duplicating a command receipt; progressed cadence fires on both named boundaries and nowhere else.
- **T2.2 — `automated` phase arm.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.1).
  - **Provides:** the `automated` arm routing `auto-continue` / `quality-checks` / `done` gate subtypes, with `quality-checks` writing a gate-resolution row; argv-list-only process launch for every automated command (no shell string, no `shell: true`); secrets resolved at phase launch into env / stdin / named-file fd and never into argv; content-addressed tool pins resolved before launch. The Code node's two tiers and the shell-command node land here: the quick tier in the WebAssembly interpreter under its memory budget and deadline; the full tier as a child process — `bun` for JavaScript and TypeScript, `uv run` for Python — with the packages the program itself names fetched first, with the network on and none of the author's code running, into a cache directory the run may write, and then the program run. The full tier and the shell-command node are both spawned through the run's own provider sandbox wrapper at the run's execution posture, which the daemon already resolves for a provider command; the engine adds no sandbox of its own, and where the posture is `sandboxed` and the wrapper cannot start, the launch refuses.
  - **Consumes:** T2.1; T1.5 pin resolution; the run's execution posture and its provider sandbox wrapper from the Plan-010 posture surface.
  - **Spec coverage:** Spec-015 §Quality-Check Model; Spec-015 §Output Mode Specification; Spec-015 §Phase-Type and Gate-Type Taxonomy; Spec-015 §Fallback Behavior.
  - **Verifies invariant:** I-015-1, I-015-2, I-015-4, I-015-5.
  - **Tests:** a command carrying shell metacharacters reaches argv unshelled; a launch attempted through a shell string is refused; a canary secret never appears raw, base64-encoded, URL-encoded, or JSON-stringified in argv, logs, or emitted events; a `quality-checks` gate writes exactly one resolution row; a quick-tier program over its memory budget or its deadline refuses with its registered code; a full-tier program's declared packages are fetched before it runs and land in the writable cache directory; a full-tier step and a shell step at a `sandboxed` posture whose wrapper cannot start refuse rather than launching, and neither is retried at a looser posture.
- **T2.3 — Gate-chain writer + dual anchor.**
  - **Files:** `packages/runtime-daemon/src/workflow/gate-chain-writer.ts` (NEW).
  - **Provides:** writer-worker-only INSERTs into `workflow_gate_resolutions` with per-run `sequence` monotonic from 1; `row_hash = BLAKE3(prev_hash || JCS-canonical(row_body))` plus an Ed25519 daemon signature over the same bytes and an optional approver signature; the paired `session_events` row carrying `gate_resolution_id` + `row_hash` written in the same unit of work; `human-approval` gates resolved through the Plan-010 Cedar path with admin override recorded as a distinct entry kind.
  - **Consumes:** Plan-013 writer worker (CP-015-4); Plan-010 `PermissionCheckService` and approval-blocking flow (CP-015-2).
  - **Spec coverage:** Spec-015 §State And Data Implications; Spec-015 §Workflow gate scope vs channel gate scope (SA-7).
  - **Verifies invariant:** I-015-3, I-015-7, I-015-8, I-015-9.
  - **Tests:** an UPDATE or DELETE against a resolution row is refused; a gap or repeat in `sequence` is rejected; a row written without its paired event (and the converse) is unreachable — the failure injection rolls back both; an approver lacking the typed capability is denied and a bare read capability never suffices; an admin override lands as its own audited entry.
- **T2.4 — Bounds, retries, and typed refusals.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.2).
  - **Provides:** `retry` (new attempt row, C-9), `go-back-to` bounded by `max_phase_transitions`, and `stop`; `max_duration` breach handling with a preserved `failure_reason`; every refusal surfaced as a typed error from the registered §Workflow vocabulary — this task mints no code of its own (A-015-15).
  - **Consumes:** T2.1–T2.3.
  - **Spec coverage:** Spec-015 §Fallback Behavior; Spec-015 §Loud-errors discipline (C-12).
  - **Verifies invariant:** I-015-2, I-015-11.
  - **Tests:** each bound hard-fails with `failure_reason` preserved; a retry mints a new attempt row and a new derived `phaseRunId` while leaving the prior attempt's outputs immutable; an unknown phase reference, unknown pool, or unknown approver principal fails loudly at create or launch rather than no-op'ing.

### Phase 2B — Typed usage-limit park and durable pacing (explicit-label supplement)

**Goal:** A provider usage-limit refusal parks the phase immediately from the typed signal alone, and a provider-reported reset boundary arms a durable, restart-surviving resume schedule with its boot sweep.

**Scope:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from Phase 2), `park-scheduler.ts` (NEW). Absorbs `## Implementation Steps` step 11's recognition, park, and scheduler legs; the behind-the-box tail of Rollout Order item 2.

**Precondition:** Phase 2 merged (T2.5 branches the park off the T2.4 refusal path; T2.6 extends the same executor); and the Tier-3 Spec-004/Plan-004 typed usage-limit signal registered (CP-015-10) — the born-unchecked `Typed provider usage-limit signal registered` §Preconditions box, whose Gate-5 entry below is why these two tasks live in a supplement rather than in Phase 2: a `precondition_box_checked` entry gates its whole declaring phase, and on Phase 2 it would have held T2.1–T2.4 too. Dispatch is by explicit supplement label only (`preflight.mjs docs/plans/015-workflow-authoring-and-execution.md 2B`); the no-argument walk is numeric-only and resolves past this supplement, so Phase 3 dispatches on Phase 2 alone and the numeric chain never waits on this box — the Plan-007 Phase 2B / Plan-022 Phase 3B shape. Tasks keep their T2.x ids and ship under the plan's ordinary integer manifest keys (the `external_plan_phase_merged` label form resolves this section by task-set membership).

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  # Carrier hold (CP-015-10): T2.5/T2.6 read the Plan-004 typed usage-limit
  # signal, which no shipped surface carries — the box checks when the Tier-3
  # Spec-004/Plan-004 targeted readiness-audit delta lands.
  - { type: precondition_box_checked, box: "Typed provider usage-limit signal registered" }
  # Self-referential gate: the park branches off the Phase-2 executor's merged
  # refusal path, so this supplement dispatches only after Phase 2 has shipped.
  - { type: plan_phase, plan: 015, phase: 2, status: merged }
```

#### Tasks

- **T2.5 — Typed usage-limit recognition and the immediate park.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.4).
  - **Provides:** recognition of a provider usage-limit refusal from the Plan-004 typed signal **alone**, and the park it triggers — the phase suspends on the refusal itself, before the retry ladder is entered, writing `park_reason: 'provider-usage-limited'` and a bounded engine-authored `park_cause` through the T1.9 seam and emitting `workflow.phase_suspended` with them. The park consumes no `max_retries` attempt, mints no new `phaseRunId`, increments no `max_phase_transitions` counter, and happens even when the refusal reports no reset window. `max_duration` keeps elapsing across it (SA-2 wall-clock), so a parked run reaching its deadline fails there. The park also stamps `park_attention_key` — provider plus account identity plus the credential generation held stable across the refusing dispatch, written into the suspension payload's optional `parkAttentionKey` member so the projected key is a function of the log — and the read side folds runs sharing a non-null key into one attention record, folding a mixed set never, and consulting the record from no start, resume, retry, or schedule-fire path. Recognition is a **typed-signal switch with no text arm at all**: there is no matcher to disable, so the C-19 rule is structural rather than a convention. The `park_cause` is written only to the phase-state projection and its event, never into a phase output, an artifact, or an agent-visible context.
  - **Consumes:** T1.9 contract and columns; T2.1–T2.4; the Plan-004 typed usage-limit signal (CP-015-10) — **this task does not dispatch until that delta lands**, and the ordinary retry-and-fail path stands in the meantime.
  - **Spec coverage:** Spec-015 §Provider-limit pacing and durable resumption (SA-40); Spec-015 §Loud-errors discipline (C-12).
  - **Verifies invariant:** I-015-19, I-015-23.
  - **Tests:** a typed usage-limit refusal parks the phase and the attempt counter, the `phaseRunId`, and the transition counter are all unchanged across it (asserted on the persisted rows, not on a return value); a refusal carrying no reset window parks identically and licenses no extra attempt; a provider message whose **text** names a usage limit while the typed signal says otherwise does **not** park — the negative direction, which is what a text matcher would fail; a grep-level assertion that no recognition path reads message, prose, error-string, window-name, or model-id content; `max_duration` elapsing during a park fails the run at its deadline with `failure_reason` preserved; three runs refused by one account fold to one attention record while a fourth run failing for an unrelated reason is never folded in; a start, a resume, a retry, and a schedule fire each succeed with an attention record present (asserted structurally at the call sites, so the record cannot become a lock); a transition back to running invalidates a not-yet-surfaced record so a later refusal surfaces again; a fold whose correlation cannot be computed surfaces that run's own attention record and still parks — the fail-open direction asserted explicitly, because the dangerous direction (reverting to a retry, or surfacing nothing) is the one that looks like success; a projection rebuild from events alone reconstructs the identical `park_attention_key`, and a key the log does not contain never survives a rebuild; killing the daemon in the interval between the delivery-path detach and the off-path park write leaves no park row, and the post-restart resume of the same attempt receives a fresh typed refusal and parks — the crash-injection arm proving the park's durability rests on the refusal's reproducibility rather than on the in-flight delivery surviving (with the account-reset variant proceeding instead of parking, asserted as the same test's second arm); `park_cause` appears in no phase output, artifact, or agent-visible context on any path.
- **T2.6 — Durable auto-resume schedule and boot sweep.**
  - **Files:** `packages/runtime-daemon/src/workflow/park-scheduler.ts` (NEW), `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.5).
  - **Provides:** the durable resume schedule of SA-40's five rules — armed **only** where the typed refusal's reset boundary is stamped provider-reported, never where it is a driver default, a driver estimate, or absent; the armed instant written into the `workflow.phase_suspended` payload before it is projected, so `workflow_phase_states.auto_resume_at` is a function of the log; the jitter offset computed by **scaling** a fixed-width prefix of the run's derived digest across a bounded 0–120 s span (`offset = span × prefix / 2^width`) as a pure function of `workflowRunId`, so replay reproduces it and a rebuild reconstructs the same row — **a modulus is forbidden**, because when the span exceeds the operand's range it never wraps and the spread silently collapses while every bound assertion still passes; the deadline rule, which refuses to arm at or past the run's `max_duration` and parks unscheduled instead; the boot sweep that re-arms every durable row **after** projection rebuild completes and re-arms a past-due instant at a short bounded delay rather than firing it inline; the clear-on-every-exit rule, which clears both the durable value and any armed timer in the same unit of work as the transition; and the fire path, which re-checks that the phase is still parked and the run still resumable and then makes a real provider attempt, re-parking honestly against whatever boundary is then in force. Two failure-side rules are part of the same seam: a fire that re-checks and then **fails to transition** the run is not an exit, so the durable value stays armed and re-arms at a bounded delay — the clear is keyed on the run leaving the park, never on the timer having fired — and a durable value the sweep **cannot decode** is quarantined rather than fatal, leaving that one run parked and unscheduled with the undecodable value recorded in the engine event record instead of failing the boot sweep for every run.
  - **Consumes:** T2.5 park path; T1.9 columns; the Plan-004 typed usage-limit signal and its provenance-stamped boundary (CP-015-10) — **held with T2.5**; the Plan-013 writer worker for the same-unit-of-work clear (CP-015-4).
  - **Spec coverage:** Spec-015 §Provider-limit pacing and durable resumption (SA-40); Spec-015 §Truth vs projection vs ephemeral (SA-25).
  - **Verifies invariant:** I-015-20.
  - **Tests:** a provider-reported boundary arms and a driver-default, a driver-estimated, and an absent boundary each park with `auto_resume_at` null; the jitter offset is deterministic for a fixed `workflowRunId`, differs across run ids, and its distribution spans the full declared window — with an explicit regression asserting the modular spelling's collapse (a span wider than the operand's range must fail this test, so the scaled form is proven rather than assumed); a schedule that would land at or past `max_duration` is not armed and the row stays null; killing and restarting the daemon re-arms every parked phase's schedule and the re-arm happens strictly after rebuild completes (ordering asserted, not timed); a past-due instant at boot is re-armed at the bounded delay rather than fired during rebuild; resume, cancel, fail, and complete each leave both the durable value null and no timer armed, and the clear commits in the transition's own unit of work (asserted by failure injection: a transition that rolls back leaves the schedule as it was); firing against a still-limited account costs exactly one refused attempt and re-parks rather than failing the run; a projection rebuild from events alone reconstructs the identical `auto_resume_at`, and a schedule the log does not contain never survives a rebuild; a fire that re-checks and then fails to transition the run leaves `auto_resume_at` non-null and re-armed at the bounded delay (asserted on the persisted row, so a clear-on-fire spelling fails the test rather than passing it silently); a row whose `auto_resume_at` cannot be decoded leaves that run parked and unscheduled, leaves every sibling row's schedule armed, and still completes the boot sweep — the negative control being that no exception escapes the sweep.

### Phase 3 — Multi-agent OWN channel and human phase

**Goal:** `multi-agent` phases run in phase-owned channels with the SA-9 termination cascade, and `human` phases accept form submissions as durable phase outputs.

**Scope:** `packages/runtime-daemon/src/workflow/human-phase-form-service.ts` (NEW), the `workflow_channels` linkage, and the `multi-agent` / `human` arms of the executor. Absorbs `## Implementation Steps` 3 (`multi-agent` and `human` arms); Rollout Order item 3.

**Precondition:** Phase 2 merged; the Tier-5 Plan-014/Spec-014 delta merged, supplying the `ChannelConfig.turnsPerAgent` budget and the workflow `InterruptReason` member (CP-015-5 — landed 2026-08-11, PR #321); Plan-010's approval-service phase and Plan-012's artifact pipeline shipped (CP-015-2, CP-015-3).

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  - { type: precondition_box_checked, box: "OWN-channel turn budget provider registered" }
  - { type: plan_phase, plan: 015, phase: 2, status: merged }
```

#### Tasks

- **T3.1 — `multi-agent` arm: OWN channel lifecycle.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.4).
  - **Provides:** 1:1 OWN-channel creation per phase run through Plan-014 wire methods, recorded on `workflow_channels`; the channel's turn budget set from the Plan-014-provided `ChannelConfig.turnsPerAgent`; `workflow.phase_progressed` at channel-turn boundaries and 25 / 50 / 75% budget milestones; the SA-9 termination cascade mapping `CLOSE_WITH_RECORDS_PRESERVED` / `REQUEST_CANCEL` / `TERMINATE` onto Plan-014 archive and interrupt mechanisms with the 30s grace window; transcript hand-off to the next phase via `inheritContext`.
  - **Consumes:** Plan-014 channel creation, archive, and interrupt surfaces plus the `turnsPerAgent` and workflow `InterruptReason` legs (CP-015-5 — landed 2026-08-11, Plan-014 D-014-23; the member is `workflow_phase_cancelled`) — the two legs gated this task through the §Preconditions box above, checked at that landing; no Plan-014 daemon internals are imported.
  - **Spec coverage:** Spec-015 §Interfaces And Contracts; Spec-015 §Discussion Integration Path.
  - **Verifies invariant:** I-015-8.
  - **Tests:** exactly one channel per phase run and none on a retry that reuses the prior attempt's records; `channel_created_for_phase` precedes every event carrying that `channelId`; each termination policy invokes its mapped mechanism and no other; the grace window drains before archive on `REQUEST_CANCEL` and is skipped on `TERMINATE`; the next phase receives the transcript through `inheritContext`.
- **T3.2 — Human-phase form service.**
  - **Files:** `packages/runtime-daemon/src/workflow/human-phase-form-service.ts` (NEW).
  - **Provides:** `workflow.humanFormSubmit` handling with optimistic-concurrency conflict detection against the per-attempt form revision the `workflow.runRead` phase-state projection surfaces (`PhaseState.formRevision`: 0 while an attempt has no accepted submission, 1 after it — so a first submit carries `expectedRevision: 0`, a concurrent second submit is refused as stale, and an abandoned claim leaves 0 for the next claimant); submissions written as `phase_outputs`, with upload fields stored as `value_kind='artifact_ref'` pointing at Plan-012 manifests (C-16); typed `timeout: 'none' | Duration` with no field-level default (ADR-015 Decision D1); claim / re-claim and `workflow.phase_suspended` with `reason: 'waiting-human'` carrying a bounded engine-authored `parkCause` written through the T1.9 helper — the producer-side half of [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42)'s every-park-carries-a-cause rule, which the schema-optional payload member cannot enforce alone (optionality exists for pre-growth replay, never as a producer license). No daemon-side draft persistence — `human_phase_form_state` stays empty at V1 (SA-28).
  - **Consumes:** Plan-012 artifact manifests and the OWASP upload pipeline, unchanged (CP-015-3); Plan-010 approval categories for `human_phase_contribution` (CP-015-2).
  - **Spec coverage:** Spec-015 §Interfaces And Contracts; Spec-015 §Human form drafts (SA-28).
  - **Verifies invariant:** I-015-6, I-015-10.
  - **Tests:** the OWASP battery — zip bomb, polyglot, symlink traversal, oversize, mismatched `Content-Type` — quarantines through Plan-012's pipeline with no second validation path in this service; a fresh attempt projects `formRevision` 0 and an accepted submission moves it to 1; a stale submit is refused rather than silently overwriting; a submission with no explicit `timeout` is refused at definition create; every newly emitted `waiting-human` suspension carries a non-null bounded `parkCause` — the producer obligation, asserted separately from T1.9's legacy-replay acceptance, so a bare pre-growth event still parses while a bare newly-produced one fails this test; `human_phase_form_state` holds zero rows after a full human-phase lifecycle.
- **T3.3 — Per-turn moderation gate wiring.**
  - **Files:** `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T3.1).
  - **Provides:** the per-turn moderation gate fired through Plan-010 for `multi-agent` phase turns, and the `human_phase_contribution` approval category (SA-12) covering non-approval phase submissions; a denial discards the buffered turn output rather than appending it.
  - **Consumes:** Plan-010 `PermissionCheckService` and the approval-blocking flow (CP-015-2).
  - **Spec coverage:** Spec-015 §Interfaces And Contracts; Spec-015 §Loud-errors discipline (C-12).
  - **Verifies invariant:** I-015-3.
  - **Tests:** the gate resolves before the turn's output is appended; a denial leaves no output event; the approval category is Plan-010-defined and never minted here; a moderation refusal is a typed error, never a silent skip.

### Phase 4 — Parallel execution and pool admission

**Goal:** Parallel phase blocks execute under `ParallelJoinPolicy` with a deterministic tick-synchronous cancel wave, admitted by the memory gate and by nothing else.

**Scope:** `packages/runtime-daemon/src/workflow/parallel-join-resolver.ts` (NEW), `resource-pool-admitter.ts` (NEW). Absorbs `## Implementation Steps` 4; Rollout Order item 4.

**Precondition:** Phase 3 merged.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  - { type: plan_phase, plan: 015, phase: 3, status: merged }
```

#### Tasks

- **T4.1 — `ParallelJoinPolicy` resolver.**
  - **Files:** `packages/runtime-daemon/src/workflow/parallel-join-resolver.ts` (NEW).
  - **Provides:** `fail-fast` / `all-settled` / `any-success` resolution (SA-4); a tick-synchronous cancel cascade recorded with `cancel_wave_tick` on `parallel_join_state`; one coordinator `workflow.parallel_join_cancellation` event plus per-sibling chains at the tick boundary; `workflow.phase_failed` extended with `cancellation_reason`.
  - **Consumes:** T2.1 executor tick loop; T1.3 event schemas.
  - **Spec coverage:** Spec-015 §Execution semantics; Spec-015 §Example Flows.
  - **Verifies invariant:** I-015-8, I-015-10.
  - **Tests:** each policy's settle semantics over a five-sibling block; `fail-fast` cancels siblings at one deterministic tick and never from an async callback; the cancel wave records one coordinator event; `cancelling` precedes each sibling's `failed` when `cancellation_reason` is non-null; `parallel_join_state` rebuilds byte-equal from the log.
- **T4.2 — Resource-pool admitter.**
  - **Files:** `packages/runtime-daemon/src/workflow/resource-pool-admitter.ts` (NEW).
  - **Provides:** the SA-3 memory gate — `process.availableMemory()` re-read on every tick less the configured headroom fraction, weighed against a per-phase-type reservation that starts at a pessimistic constant and is then replaced by the observed resident size of that type's recent launches on this machine per provider, a ramp interval between consecutive grants, and at least one phase always admitted so a machine under pressure still moves; a reservation released while a phase waits on a person or parks on a provider limit, and re-taken at resume; `workflow.phase_admitted` on grant and `workflow.phase_waiting_on_pool` on entry-to-blocked plus 30-second intervals while blocked, carrying `waitingSinceSeq`; aggregate `totalPoolWaitMs` on the phase row rather than per-wait rows; reservations, observed sizes and ramp state held in memory, re-requested and re-learned after restart (SA-3 ephemeral tier). Every admitted launch goes out argv-list-only. No phase-count ceiling and no slot pool are built.
  - **Consumes:** daemon configuration for the headroom fraction (no cross-plan pool API at V1, A-015-17); Node's `process.availableMemory()` on the pinned Node 22 line; T4.1 tick boundary.
  - **Spec coverage:** Spec-015 §Execution semantics; Spec-015 §Fallback Behavior.
  - **Verifies invariant:** I-015-1, I-015-8.
  - **Tests:** a machine with room for nothing still admits exactly one phase and emits the waiting event for the rest at entry and on the 30s cadence; admitted always precedes started; a second grant waits out the ramp interval; a phase parked on a person or on a provider limit releases its reservation and takes one again at resume; a phase type's reservation moves off its seeded constant once launches of that type have been observed, per provider; restart re-requests reservations and re-learns the observed sizes rather than restoring stale counts.

### Phase 5 — Resumption, verification CLI, and authoring surfaces

**Goal:** Workflows survive daemon restart with phase state intact, the gate chain is operator-verifiable end to end, and the SDK plus desktop surfaces expose authoring, run detail, and human-phase forms.

**Scope:** `packages/runtime-daemon/src/workflow/gate-chain-verifier.ts` (NEW), `workflow-projector.ts` (NEW), `packages/client-sdk/src/workflowClient.ts` (NEW), `apps/desktop/src/renderer/src/console/workflows/` (NEW), `apps/desktop/src/renderer/src/console/workflows/builder/` (NEW), `apps/cli/src/commands/workflow-*.ts` (NEW), `apps/desktop/src/renderer/src/console/workflows/chat-start/` (NEW), `packages/runtime-daemon/src/workflow/workflow-callback-tool.ts` (NEW). Absorbs `## Implementation Steps` 6–8 (A-015-09 assigns steps 6 and 7 here) plus step 9's renderer, file-form, and CLI legs and step 10's chat-start surfaces; Rollout Order item 5's builder, chat-start, and resumption legs (its cancellability, repair, and engine-record legs are the Phase 5B and 5C supplements below).

**Precondition:** Phase 4 merged; ADR-026 promoted `accepted` (the phase's builder tasks T5.5, T5.6, and T5.7 are what that box exists to hold); ADR-027 promoted `accepted` (likewise the chat-start tasks T5.8, T5.9, and T5.10) — both ADR boxes are Gate-5 entries, so each held the **full** phase rather than only its named tasks until the promotions landed, the accepted short-lived breadth their `## Preconditions` narratives record. ADR-026 and ADR-027 are `accepted` and both boxes are checked, so neither Gate-5 entry holds any longer and Phase-5 dispatch rides Phase 4 and tier order. The park tasks T5.11–T5.13 live in the Phase 5B and 5C supplements below; this phase carries no entry against the `Engine-event diagnostic bucket registered` box.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  - { type: precondition_box_checked, box: "ADR-026 ratified accepted" }
  - { type: precondition_box_checked, box: "ADR-027 ratified accepted" }
  - { type: plan_phase, plan: 015, phase: 4, status: merged }
```

#### Tasks

- **T5.1 — Restart-safe resumption + projection rebuild.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-projector.ts` (NEW).
  - **Provides:** the five projection tables rebuilt from `session_events` plus the immutable truth tables through Plan-013 `ProjectionRebuild`; restart-safe resumption emitting `workflow.resumed` with the structured `resumptionPoint: {activePhaseRunIds, pendingGates}`; hash-chain replay re-verifying each row in `sequence` order and halting on `chain_break_detected`; replay driven only by derived `phaseRunId`s so a re-run reproduces the identical sequence.
  - **Consumes:** Plan-013 `ProjectionRebuild` and recovery surfaces (CP-015-4); T2.3 chain layout.
  - **Spec coverage:** Spec-015 §State And Data Implications; Spec-015 §Deterministic identity (SA-21).
  - **Verifies invariant:** I-015-7, I-015-10, I-015-11.
  - **Tests:** rebuild is byte-equal to the pre-restart projection; a tampered row halts replay at its `sequence` rather than at the end; replay of a full history throws no `DeterminismViolationError` and `deepEqual`s the original final state; no projection path reads request state.
- **T5.2 — Gate-chain verifier + CLI.**
  - **Files:** `packages/runtime-daemon/src/workflow/gate-chain-verifier.ts` (NEW), the `sidekicks workflow verify-gate-chain <run_id>` subcommand.
  - **Provides:** the operator-triggered dual-anchor verification procedure — per-row recompute of `prev_hash || JCS-canonical(row_body)` asserted equal to the persisted `row_hash`, plus the paired `session_events` row's `gate_resolution_id` and `row_hash` cross-check — surfaced through `workflow.gateChainVerify` and the CLI subcommand, reporting the first divergent `sequence` rather than a bare pass/fail.
  - **Consumes:** T2.3 chain layout; T5.1 replay path.
  - **Spec coverage:** Spec-015 §State And Data Implications; Spec-015 §Loud-errors discipline (C-12).
  - **Verifies invariant:** I-015-7, I-015-9.
  - **Tests:** a clean chain verifies; a mutated row body, a re-signed row, and a deleted paired event each fail with the offending `sequence` named; verification cost stays linear in row count and runs outside the hot path.
- **T5.3 — Typed client SDK.**
  - **Files:** `packages/client-sdk/src/workflowClient.ts` (NEW).
  - **Provides:** typed callers for every `workflow.*` method in the registry but the two operator-recovery ones, which ship at T5.15 with the CLI verbs that drive them — marshalling and never deriving — no client-side gate evaluation, no client-side admission outcome, no client-side chain verification.
  - **Consumes:** T1.2 wire pairs.
  - **Spec coverage:** Spec-015 §Core SDK and persistence contracts.
  - **Verifies invariant:** I-015-12.
  - **Tests:** each method round-trips its declared pair; refusals surface verbatim with no client-side retry; the reserved draft-save method is typed but unwired at V1.
- **T5.4 — Desktop authoring, run detail, and human-phase surfaces.**
  - **Files:** `apps/desktop/src/renderer/src/console/workflows/` (NEW).
  - **Provides:** the workflow authoring surface, run-detail rendering with phases as distinct timeline sections and retry iterations as sub-entries, the pending-human count surfaced prominently, and the human-phase form with daemon-held draft persistence through `workflow.humanFormDraftSave` (SA-28). The node-graph builder canvas itself is T5.5; T5.4 establishes the workflows view shell, run detail, and the human-phase form that the builder mounts inside.
  - **Consumes:** T5.3 SDK; the Plan-021 renderer bridge substrate.
  - **Spec coverage:** Spec-015 §Session timeline surfacing; Spec-015 §Output Mode Specification.
  - **Verifies invariant:** I-015-6, I-015-12.
  - **Tests:** phases render as distinct sections with retries nested; a suspended-waiting-human phase surfaces in the pending count; drafts survive a renderer reload without any daemon write; an upload rejected by the daemon pipeline renders its typed refusal rather than a generic failure.
- **T5.5 — Visual workflow builder canvas.**
  - **Files:** `apps/desktop/src/renderer/src/console/workflows/builder/` (NEW), `apps/desktop/package.json` (EXTEND — adds the React Flow renderer dependency whose adoption ADR-026 governs).
  - **Provides:** the node-graph authoring view built on React Flow in fully controlled mode — nodes and edges held in application state, all mutations flowing through the change handlers, connection attempts screened by the validation predicate so a refused connection cannot be completed; a docked node palette listing every kind the runtime offers, grouped by the eight categories and read from `workflow.kindList`, so a kind added in the daemon appears with no renderer change; one generic node renderer driven by each kind's own declared spec, carrying the gate, agent and tool-binding badges of the kinds that have them; an inspector panel writing a node's params and displaying governance facets read-only per I-015-14; canvas pan and zoom with the viewport kept in the document's own layout section; deterministic left-to-right auto-layout for a document that arrives with no layout; and layout written to that section beside the definition body, outside the hashed bytes, so dragging a node mints no version.
  - **Consumes:** T5.3 SDK (`workflow.definitionCreate`, `workflow.definitionRead`, `workflow.versionRead`, `workflow.definitionList`, and `workflow.kindList`); T1.8 refusal set and layout split; T1.7 copy-on-write path; the Plan-021 renderer bridge substrate. The builder reaches the daemon only through the preload bridge and imports no Node, Electron, main-process, or preload module.
  - **Spec coverage:** Spec-015 §Visual Workflow Builder; Spec-015 §Canvas layout is not definition bytes (SA-35).
  - **Verifies invariant:** I-015-14, I-015-15, I-015-16.
  - **Tests:** a connection that would close a cycle, orphan a node, target the trigger node, mismatch handle types, or leave a terminal kind is refused by the connection predicate and never enters edge state; the palette lists every kind `workflow.kindList` returns and no kind it does not; dragging every node and reloading restores the geometry while the content hash and the definition's version count are unchanged; a document read with no layout renders identically across two runs of the auto-layout; the inspector exposes no writable control for `enabled`, `approvalMode`, or `idempotencyClass`; a daemon refusal on save renders its typed code rather than a generic failure; the renderer lint boundary rejects any Node or Electron import added under the builder subtree.
- **T5.6 — Definition file form: export and import.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.8), `packages/client-sdk/src/workflowClient.ts` (EXTEND from T5.3).
  - **Provides:** the canonical YAML file form carrying the schema-version marker — the definition body plus an optional top-level `layout` section outside the hashed bytes — with serialize and parse as the single dialect shared by the desktop builder and the CLI; parse feeds the ordinary definition-create validation path, so an imported definition is subject to every refusal rule and carries no governance state.
  - **Consumes:** T1.8 refusal set and layout split; T1.2 wire pairs.
  - **Spec coverage:** Spec-015 §Definition file form — export and import (C-17); Spec-015 §Visual Workflow Builder.
  - **Verifies invariant:** I-015-14, I-015-15.
  - **Tests:** export → import → export is byte-stable and the content hash is identical at every hop; a file whose `layout` section is stripped imports to the same content hash as the file that carried it; a file with an unknown top-level key is refused rather than silently accepted; a file carrying an inline governance facet is refused at parse; a CLI-produced file with no `layout` opens in the builder through auto-layout and re-exports to the same body bytes.
- **T5.7 — `sidekicks workflow` command set.**
  - **Files:** `apps/cli/src/commands/workflow-list.ts` (NEW), `workflow-show.ts` (NEW), `workflow-start.ts` (NEW), `workflow-status.ts` (NEW), `workflow-export.ts` (NEW), `workflow-import.ts` (NEW), `apps/cli/src/main.ts` (EXTEND — explicit registration, no auto-discovery).
  - **Provides:** CLI parity with the builder over the same typed SDK operations — `workflow list` over `workflow.definitionList`, `workflow show` over `workflow.definitionRead` plus `workflow.versionRead`, `workflow start` over `workflow.runStart`, `workflow status` over `workflow.runRead`, `workflow export` as a client-side serialization of a version read, and `workflow import` as a submission through `workflow.definitionCreate`. Scope is a flag on `list`, `start`, and `import`, defaulting to most-specific-first resolution. Commands marshal and never derive: no client-side validation verdict is treated as authoritative, and every refusal surfaces verbatim. The `verify-gate-chain` subcommand of T5.2 joins the same command root.
  - **Consumes:** T5.3 SDK; T5.6 file form; the Plan-006 CLI scaffold and command-registration seam.
  - **Spec coverage:** Spec-015 §Definition file form — export and import (C-17); Spec-015 §Core SDK and persistence contracts.
  - **Verifies invariant:** I-015-16.
  - **Tests:** each verb round-trips its named operation against a fake transport; `list` groups by scope and reports the tier each definition resolves from; `import` of a file the daemon refuses exits non-zero with the typed code and creates nothing; `export` of a definition authored in the builder and `import` of it on a fresh store yields the identical content hash; no command imports anything outside the CLI's declared import-isolation allowlist.
- **T5.8 — Chat-start surface: command interception + composer affordance.**
  - **Files:** `apps/desktop/src/renderer/src/console/workflows/chat-start/` (NEW — command registry, the thirteen-verb `/workflow` parser, `workflow.definitionList`-backed autocomplete source, workflow-picker affordance, channel progress-card binding); the Plan-021-owned session composer module (EXTEND — the interception hook; the concrete file is named at dispatch from the shipped composer surface, per the established EXTEND convention).
  - **Provides:** the registered `/workflow` command root with its thirteen verbs — `run`, `list`, `runs`, `results`, `open`, `create`, `edit`, `fix`, `schedule`, `cancel`, `resume`, `enable` and `disable` — intercepted client-side and autocompleted over the caller's scope-resolved definition list; ten resolving to the typed operations, `run` carrying the originating `channelId`, and `create` / `edit` / `fix` composing a message to the session's current agent with a structured intent hint; a word the console does not own forwarded or sent as prose rather than refused, and one flow row where a console verb is given an argument its control cannot take; the composer affordance enumerating startable definitions by name over the same list call; the run progress surface bound to the originating channel.
  - **Consumes:** T5.3 SDK; T5.4 workflows view shell; the Plan-021 renderer bridge substrate.
  - **Spec coverage:** Spec-015 §Chat-start surface (SA-38); Spec-015 §Chat-Invoked Start and Start Authorization.
  - **Verifies invariant:** I-015-17.
  - **Tests:** a registered command never reaches channel-message send or any provider-turn composition path, on the steer-through-composer path included (asserted at the composer seam, not by absence); `/workflow run <name>` with an ambiguous name resolves most-specific-first; a slash word the console does not own and the bound provider does not enumerate is sent as the user's prose with no press and no refusal; an unrecognized verb under `/workflow` lands one flow row and composes nothing; autocomplete lists exactly the caller's scope-resolved definitions; a start issued from a channel carries that `channelId` and the progress card renders against it; mid-text slashes are untouched.
- **T5.9 — The sixteen `workflow_*` callback tools.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-callback-tool.ts` (NEW); `packages/contracts/src/workflow/` (EXTEND — each tool's input-schema shape).
  - **Provides:** sixteen `SessionCallbackTool` definitions (name, description, JSON-Schema input) and their daemon-side handlers, registered into the Plan-004 callback-tool host registry at session spawn, each with an approval category and an activity record on every invocation, and each resolving to the typed operations of `## API And Transport Changes` rather than to a path of its own. Five adjudications cover the set: reads gated on the session itself (`workflow_kinds`, `workflow_list`, `workflow_read`, `workflow_runs`, `workflow_run_read`, `workflow_step_read`, `workflow_validate`); `Action::"workflow::author"` (`workflow_create`, `workflow_update`, `workflow_schedule_set`, `workflow_enable` — which takes the enabled flag, so it serves both the enable and the disable verb — and `workflow_results_post`, whose session is derived from the invoking turn and never tool-supplied); `Action::"workflow::start"` (`workflow_run`, `workflow_node_execute`); `Action::"workflow::cancel"` (`workflow_cancel`); and `Action::"workflow::resume"` (`workflow_resume`). `workflow_run` takes a definition name plus an optional scope qualifier, resolves it through the same scope walk, and issues the same start path as `workflow.runStart` under SA-39 adjudication, landing as `tool_activity`. An agent's own tool allowlist decides which of the sixteen that agent holds.
  - **Consumes:** the Plan-004 callback-tool host and its CP-004-7 Cedar seam (CP-015-7); T5.10 adjudication wiring; T1.2 wire pairs.
  - **Spec coverage:** Spec-015 §Chat-start surface (SA-38); Spec-015 §Start authorization (SA-39).
  - **Verifies invariant:** I-015-18.
  - **Tests:** every tool in the set is absent from spawn injection while the approval seam is unregistered and a stray invocation answers `denied` with a diagnostic record, never `completed` and never silence; each tool's registered Cedar action is the one its adjudication group names, asserted by enumerating the set rather than by naming one tool; a permitted `workflow_run` invocation starts the run and lands as `tool_activity` with the run id in the result; a Cedar-denied invocation answers `denied` carrying `workflow.start_denied`; a schema-invalid argument answers `failed` without dispatch; a channel-scoped invocation binds the invoking turn's channel (daemon-derived — the input schema carries no channel field and a smuggled one is schema-refused) and a non-channel-scoped invocation binds none; the handler and `workflow.runStart` produce identical run rows for identical inputs.
- **T5.10 — Start authorization at the run-start path.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (EXTEND — SA-39 adjudication + refusal wiring on the start path all callers share).
  - **Provides:** per-start adjudication of the named Cedar operation action through `PermissionCheckService` on every start path — CLI, desktop, chat command, composer affordance, callback tool — with `workflow.start_denied` refusal for a matrix-denied principal; the fail-closed chat-borne rule: a chat-borne start resolves only the daemon-resolved node-owner principal until the CP-015-8 carrier lands, and every other chat-borne start refuses; `channelId` validation — the named channel must belong to the starting session before it binds a progress surface, refusing `workflow.start_denied` otherwise, with the role adjudication itself never reading the field, the validation running only for a start the matrix admits (role arm first, channel arm second), and the out-of-session and nonexistent-channel outcomes byte-identical.
  - **Consumes:** Plan-010 `PermissionCheckService` (CP-015-2, CP-015-8); the Plan-014-owned channel read for the SA-38 `channelId` validation (CP-015-5 — read-only, never mutating channel state); T1.2 wire pairs.
  - **Spec coverage:** Spec-015 §Start authorization (SA-39); Spec-015 §Chat-start surface (SA-38).
  - **Verifies invariant:** I-015-18.
  - **Tests:** the session's own principal starts; a matrix-denied principal refuses `workflow.start_denied`; the refusal surfaces verbatim through SDK and CLI; a chat-borne start with an unresolvable principal refuses; a `channelId` naming a channel outside the starting session refuses and binds no surface (a forged channel can never render a progress card — asserted), while an in-session `channelId` binds and the role decision is unchanged by the field's presence; a matrix-denied principal with a forged `channelId` refuses on the role arm and the channel read never executes (asserted, not sampled); the channel refusal is byte-identical for an out-of-session channel and a nonexistent channel; no approval row is minted and no grant is remembered on any path; every start path traverses the same adjudication call site (asserted structurally, not by sampling).

### Phase 5B — Park cancellability, frozen-definition repair, and the operator recovery surface (explicit-label supplement)

**Goal:** A parked run cancels from every suspended or waiting state with its record intact, the SA-41 repair ships explicit, audited on `workflow.resumed`, and refused through its three registered codes — and both are **reachable**: an operator sees the park on one read and acts on it through two authorized wire operations, with the cancellation recorded as an event the projection rebuilds from.

**Scope:** `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (EXTEND from Phase 5), `park-scheduler.ts` (EXTEND from Phase 2B), `workflow-projector.ts` (EXTEND from Phase 1), `packages/contracts/src/workflow/` (EXTEND from Phase 1), `packages/client-sdk/src/workflowClient.ts` (EXTEND from Phase 5), `apps/cli/src/commands/workflow-cancel.ts` and `workflow-resume.ts` (NEW). Absorbs `## Implementation Steps` step 11's cancellability and repair legs and the whole of step 12; Rollout Order item 5's T5.11 → T5.12 → T5.14 → T5.15 leg. The wire surface is homed **here rather than in Phase 5** because it invokes the engine rules T5.11 and T5.12 ship, and a phase may not chain on a supplement.

**Precondition:** Phase 5 merged (T5.11 EXTENDs `workflow-run-service.ts` from T5.10, and cancellability's restart assertion needs the T5.1 resumption path); Phase 2B merged (T5.11 extends the T2.6 scheduler's clear path and T5.12 rides the park state T2.5 writes). **No box:** cancellability and the repair are engine-internal — neither reads the Plan-004 carrier nor the Plan-018 bucket — so this supplement is gated only on the phases whose seams it extends, and it is a supplement rather than part of Phase 5 because one of those seams is Phase 2B, which no numeric phase may chain on (the numeric walk must never wait behind a box-held supplement). Dispatch is by explicit supplement label only (`preflight.mjs docs/plans/015-workflow-authoring-and-execution.md 5B`); tasks keep their T5.x ids and ship under the plan's ordinary integer manifest keys.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  # Self-referential gates: T5.11 extends the Phase-5 run service and the
  # Phase-2B scheduler, so both must have shipped. The 2B entry uses the
  # supplement-label form, resolved by task-set membership.
  - { type: plan_phase, plan: 015, phase: 5, status: merged }
  - { type: external_plan_phase_merged, plan: 015, phase: 2B }
```

#### Tasks

- **T5.11 — Park cancellability.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (EXTEND from T5.10), `packages/runtime-daemon/src/workflow/park-scheduler.ts` (EXTEND from T2.6).
  - **Provides:** cancellation as a legal transition into the `cancelled` run status from **every** suspended or waiting state — human-waiting, provider-usage-limited, and any future park reason alike — with no precondition and no wait. The transition is pure bookkeeping and cannot block: a parked run holds no resource-pool reservation (pool state is ephemeral and re-requested under SA-25) and no live phase process, so there is nothing to drain or interrupt. It clears the durable resume schedule — the parked phase's `auto_resume_at` and `park_attention_key` — in the same unit of work as the status write, and it preserves the record: the parked `workflow_phase_states` row's `state`, `park_reason`, and bounded `park_cause`, and every completed `phase_outputs` row, survive untouched (record and live state share the row and part ways at exactly this boundary). **Mints no wire method and no event type — the surface that does is T5.14.** [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42) recorded the operator control surface as a residual that predates this amendment (the `cancelled` status had no named producer from the day it was declared), tracked as [BL-151](../archive/backlog-archive.md) — whose operation set and run-cancelled lifecycle event mint **together**, because a durable cancellation the log cannot reconstruct would let a projection rebuild replay the last suspension payload and resurrect the run (SA-25). **Closed 2026-08-18** by the park-surface and operator-controls amendment: T5.14 ships that surface and BL-151 is `completed`. The split is unchanged by the closure — this task still ships only the engine state-machine rule the surface invokes, exercised in-process by its own fixtures, and mints nothing on the wire or in the taxonomy. Where the censuses moved is T5.14: registry eleven → thirteen, taxonomy by one type (`workflow.cancelled`).
  - **Consumes:** T2.5 park state; T2.6 scheduler clear path; the Plan-013 writer worker for the single unit of work (CP-015-4). Not held by either new box — cancellability is engine-internal and needs neither the Plan-004 carrier nor the Plan-018 bucket.
  - **Spec coverage:** Spec-015 §Park integrity and cancellability (SA-42); Spec-015 §Truth vs projection vs ephemeral (SA-25).
  - **Verifies invariant:** I-015-21.
  - **Tests:** a run parked for each `park_reason` value cancels from that state, and the cancel path is driven by enumerating the union rather than by naming states one at a time, so a widened union fails the test until it is covered; a phase in `waiting_on_pool` — a waiting state that carries no `park_reason` — cancels through the same immediate, dependency-free transition, so an implementation dispatching cancellation by the `park_reason` union alone fails this fixture (SA-42's domain is every suspended or waiting state, not every park); the cancel completes without consulting any pool, provider, or channel surface (asserted structurally at the call site, so no future dependency can make it blockable); a schedule armed before the cancel is cleared and never fires afterward, including across a daemon restart between the cancel and the armed instant; failure injection rolling back the status write leaves the schedule as it was, proving the two commit together; after cancellation the phase-state row's `state` and record columns (`park_reason`, `park_cause`) and every completed phase output are byte-identical to their pre-cancel values, while its live columns (`auto_resume_at`, `park_attention_key`) are null — cleared by the cancel's own unit of work, the code-enforced half of the clear the per-row CHECK cannot reach across tables; and cancelling a run that is running (not parked) still follows the pre-existing path, unchanged by this task.
- **T5.12 — Frozen-definition repair.**
  - **Files:** `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (EXTEND from T5.11).
  - **Provides:** the one governed repair of [Spec-015 §Frozen-definition repair (SA-41)](../specs/015-workflow-authoring-and-execution.md#frozen-definition-repair-sa-41) — an operator resuming a **parked** run may re-pin it to the definition's current version as an explicit, separately-requested action on the resume. Explicit and never implicit: no timer, no armed schedule, and no ordinary resume ever re-pins, and the T2.6 auto-resume path resumes the frozen version and nothing else. Only while parked: a running instance is never re-pinned, which is the [Spec-015 §Fallback Behavior](../specs/015-workflow-authoring-and-execution.md#fallback-behavior) frozen-version rule left unconditional. Only where no attempt of the run is in flight: parked is necessary but not sufficient, so a re-pin requested on a resume that continues an in-flight attempt — the T2.5 / T2.6 usage-limit park by construction, which mints no new `phaseRunId` and continues the same attempt — or while any parked parallel sibling holds one that will continue later, refuses toward the explicit fresh-entry action that would discard the blocking attempt, rather than swapping the run-level definition pointer under units already dispatched from the frozen bytes (the boundary is run-wide because the pin is run-level: SA-32 branches park independently, and a sibling's attempt resumes under whatever version the run then names). Audited: the repair is recorded as the additive-optional member T1.9 added to `workflow.resumed`, naming the version the run left and the version it joined, so the projected run row stays a function of the log and a silent re-pin — which would make the run's own history unreadable — is unreachable rather than merely discouraged. Completed work is preserved: phase outputs are immutable (SA-16), the repair mints no new attempt for a completed phase, and `max_retries`, `max_phase_transitions`, and the `max_duration` deadline all carry over exactly as they stood. And it is refused, loudly and through its registered code — `workflow.repair_not_parked`, `workflow.repair_attempt_in_flight`, and `workflow.repair_version_unaccountable`, registered in `error-contracts.md §Workflow` at the workflow-hardening amendment's 2026-08-17 review round — at each of the three refusal points this path contributes to the A-015-15 census: a re-pin against a run that is not parked, one requested while any of the run's attempts is still in flight, and one whose target version cannot account for the phases the run already completed — either because it omits a phase id whose outputs the run holds or because its declared topology would leave a completed phase unreachable, which SA-41 states is one failure rather than two. Every refusal is **total** — it leaves the run parked and unchanged on its original pinned version, so resolution and validation of the target version complete before any write. Mints no wire method and no event type.
  - **Consumes:** T1.9 payload member; T5.11 park-state handling; T1.5 definition-version store; the Plan-013 writer worker (CP-015-4). Not held by either new box.
  - **Spec coverage:** Spec-015 §Frozen-definition repair (SA-41); Spec-015 §Fallback Behavior.
  - **Verifies invariant:** I-015-24.
  - **Tests:** an ordinary resume of a parked run leaves the pinned version unchanged and emits no re-pin member; a schedule firing on a run whose definition has since advanced resumes the **frozen** version (the auto-resume path is asserted never to reach the re-pin call site, structurally rather than by sampling); a re-pin requested on a running instance refuses; an accepted re-pin emits `workflow.resumed` carrying both version ids and the projection rebuilds the run's version pointer from that event alone; every completed `phase_outputs` row is byte-identical after a repair and no new attempt row exists for a completed phase; `max_retries`, `max_phase_transitions`, and the `max_duration` deadline are unchanged across a repair; a target version omitting a phase id whose outputs the run holds refuses, and so does one whose topology leaves a completed phase unreachable, each leaving the run parked on its original version with its schedule state untouched; a re-pin requested on a park whose resume continues an in-flight attempt refuses and names the fresh-entry action, with the boundary driven by enumerating the `park_reason` union rather than by naming one value, so a widened union fails the test until it is covered; a run with two parallel branches — one parked at a fresh phase boundary, its sibling parked mid-attempt on a usage-limit refusal — refuses the re-pin run-wide with `workflow.repair_attempt_in_flight`, naming the sibling's blocking attempt, so a per-resume spelling of the boundary fails this fixture; each of the three refusals leaves the run's full row set byte-identical to its pre-request state (refusals are total, so a partially-applied repair fails rather than passing silently); and every refusal carries its registered code rather than a minted one.
- **T5.14 — Operator recovery wire surface: park read members, cancel, resume, and `workflow.cancelled`.**
  - **Files:** `packages/contracts/src/workflow/` (EXTEND from T1.1 — the four `PhaseState` members and the four cancel/resume payload types), `packages/runtime-daemon/src/workflow/workflow-run-service.ts` (EXTEND from T5.12 — the two handlers), `packages/runtime-daemon/src/workflow/workflow-projector.ts` (EXTEND from T1.4 — the `workflow.cancelled` apply rule and the park-member read projection).
  - **Provides:** the surface that makes the park legible and the engine rules reachable, in four parts. **(a) Park read members.** `PhaseState` gains `parkReason`, `parkCause`, `autoResumeAt`, and `parkAttentionKey`, all additive-optional under [ADR-018](../decisions/018-cross-version-compatibility.md) and emitted **iff the phase is parked at response-build time** — so a parked run renders from one `workflow.runRead` with no timeline replay, and the presence of `parkReason` is the wire's park discriminator. The five-value `PhaseState.state` union is **not** widened: a sixth arm would move the I-015-12 lockstep pin and force a DDL, contract, and taxonomy edit to express what an optional member already answers, and would leave two encodings of the same fact to drift apart. **(b) Cancel.** `workflow.runCancel` writes the `cancelled` run status and appends `workflow.cancelled` in **one** Plan-013 writer-worker unit of work, invoking the T5.11 engine transition rather than reimplementing it. A second cancel of an already-`cancelled` run replays the original event's identity and writes nothing. A cancel of a run that reached `succeeded` or `failed` refuses `workflow.run_not_cancellable`; a cancel of a **parked** run never refuses, per SA-42's without-precondition rule. **(c) Resume.** `workflow.runResume` invokes the T2.6 resume path, and its optional `versionRepin` member invokes the T5.12 repair with that task's three refusals unchanged. A resume against an unparked run refuses `workflow.resume_not_parked` — distinct from `workflow.repair_not_parked` so an operator can tell whether dropping the re-pin would have worked. Resuming ahead of an armed `autoResumeAt` is permitted with no override flag: the armed instant is advisory pacing, and a re-park is a legal response that emits its own `workflow.phase_suspended`. **(d) Projector rule.** Applying `workflow.cancelled` clears `park_reason`, `park_cause`, `auto_resume_at`, and `park_attention_key` on **every** phase row of the run, unconditionally and from the event alone — run-scoped rather than payload-enumerated, because SA-32 branches park independently and a payload listing the parked phases would have to enumerate them correctly to stay byte-equal. Both operations adjudicate their named Cedar action through the consumed `PermissionCheckService` (CP-015-11) and refuse `workflow.control_denied`, naming which action was denied. **Mints no table and no column** — the four park columns and the `cancelled` status landed at T1.9 and T5.11.
  - **Consumes:** T5.11 cancellation transition; T5.12 repair and its refusals; T2.6 resume path and scheduler clear; T1.9 park columns; T1.3's `workflow.cancelled` payload schema and union registration — authored there with its 33 siblings, so this task produces the event and defines none of it; the Plan-013 writer worker for the single unit of work (CP-015-4); Plan-010 `PermissionCheckService` (CP-015-2, CP-015-11). Fail-closed on the Cedar actions until the CP-015-11 delta lands — the node owner passes and every other principal refuses, exactly as `Action::"workflow::start"` ships under I-015-18.
  - **Spec coverage:** Spec-015 §Park surfacing on the read model (SA-44); Spec-015 §Operator run control (SA-45); Spec-015 §Truth vs projection vs ephemeral (SA-25).
  - **Verifies invariant:** I-015-25, I-015-26.
  - **Tests:** a parked phase's response carries all four members and an unparked phase in the same run carries none, driven by enumerating phase states rather than by naming one, so a state added later fails the test until covered; a phase that resumed past an earlier park carries no park members while its `park_reason` and `park_cause` **columns** still hold the record, proving liveness and record part ways at the wire boundary; a cancelled run's phases likewise carry none; the `PhaseState.state` union is asserted to have exactly its five arms, so a later sixth arm fails here rather than silently at a consumer; a cancel writes status and event in one unit of work, with failure injection rolling back the append proving the status write does not survive alone; a `ProjectionRebuild` over a log ending in `workflow.cancelled` reproduces the cancelled status **and null live park columns on every phase**, including a run whose two parallel branches were both parked against different accounts when the cancel landed — a per-phase or payload-enumerated clearing rule fails this fixture; a second cancel returns the first cancel's event id and appends nothing, asserted by event-count equality rather than by response shape; a cancel of a `succeeded` and of a `failed` run each refuse `workflow.run_not_cancellable` with the run byte-identical afterward; a resume of a running run refuses `workflow.resume_not_parked`, and a resume carrying `versionRepin` against an unparked run refuses that same code rather than `workflow.repair_not_parked`, so the two are not conflated; a resume issued before an armed `autoResumeAt` proceeds and, where the account is still spent, re-parks and emits a fresh `workflow.phase_suspended` rather than refusing; a principal denied either action refuses `workflow.control_denied` naming that action, and a principal granted cancel but not resume is denied resume — so a single-action implementation fails; and every refusal above leaves run status, phase rows, armed schedules, and phase outputs byte-identical to their pre-request state.
- **T5.15 — Operator recovery client surface: SDK methods and CLI verbs.**
  - **Files:** `packages/client-sdk/src/workflowClient.ts` (EXTEND from T5.4), `apps/cli/src/commands/workflow-cancel.ts` (NEW), `apps/cli/src/commands/workflow-resume.ts` (NEW), `apps/cli/src/main.ts` (EXTEND from T5.7 — clipanion has no auto-discovery, so both commands are explicitly registered on the `Cli` instance; the ownership crossing is the one the cross-plan CLI rows already record for Plan-015).
  - **Provides:** the typed SDK methods for the two operations and the `sidekicks workflow cancel <run-id>` / `sidekicks workflow resume <run-id>` verbs, taking the `sidekicks workflow` command set from six verbs to eight. Two new files rather than an edit to T5.7's command module, following the per-file CLI ownership-crossing convention `## Target Areas` already records — so T5.7 ships unchanged and the two surfaces version independently. `resume` carries an explicit `--repin-to-current` flag for the SA-41 repair, never a default and never inferred: SA-41 requires the operator to request the re-pin **by name**, so a flagless resume must be unable to re-pin. Both verbs render the refusal codes as operator-legible messages and neither retries a refusal — a `workflow.control_denied` that silently retried would read as a hang. The park members T5.14 adds surface on `sidekicks workflow status`, so an operator reads the park cause and the armed resume instant without opening a timeline.
  - **Consumes:** T5.14 wire operations and their refusal codes; T5.4 SDK client; the T5.7 command-registration convention (no shared file is edited).
  - **Spec coverage:** Spec-015 §Operator run control (SA-45); Spec-015 §Park surfacing on the read model (SA-44).
  - **Verifies invariant:** I-015-26.
  - **Tests:** `resume` without `--repin-to-current` sends no `versionRepin` member, asserted on the emitted request rather than on the response, so an implementation defaulting the flag on fails; `resume --repin-to-current` sends it; each of the three refusal codes renders a distinct operator message and exits non-zero, enumerated from the registered code set so a newly registered code fails the test until covered; no verb retries a refusal; `workflow status` on a parked run prints the park cause and the armed instant, and on an unparked run prints neither, driven by the same liveness discriminator T5.14 asserts; and the `sidekicks workflow` command set enumerates exactly eight verbs, so a verb added without updating the ownership row fails here.

### Phase 5C — Always-on engine event record (explicit-label supplement)

**Goal:** Every run writes the SA-43 engine record from its first decision, with no enable flag, onto the Plan-018-owned bounded-retention diagnostic bucket.

**Scope:** `packages/runtime-daemon/src/workflow/engine-event-log.ts` (NEW), `phase-executor.ts` (EXTEND from Phase 2B). Absorbs `## Implementation Steps` step 11's engine-record leg; Rollout Order item 5's T5.13 leg.

**Precondition:** Phase 2B merged (T5.13 EXTENDs `phase-executor.ts` at the T2.5/T2.6 park and schedule decision points); the Plan-018 engine-event diagnostic bucket registered (CP-015-9) — the born-unchecked `Engine-event diagnostic bucket registered` §Preconditions box, whose Gate-5 entry below is why T5.13 lives in a supplement: on Phase 5 the entry would have held every Phase-5 task. **Independent of Phase 5:** T5.13 touches no Phase-5 file, so this supplement may dispatch in parallel with Phase 5 once its own gates open. Dispatch is by explicit supplement label only (`preflight.mjs docs/plans/015-workflow-authoring-and-execution.md 5C`); the task keeps its T5.13 id and ships under the plan's ordinary integer manifest keys.

<!-- prettier-ignore -->
```yaml
preconditions:
  - { type: audit_status, status: complete, evidence_pr: 318, baseline_tag: "plan-readiness-audit-tier-8-complete" }
  # Carrier hold (CP-015-9): the record writes into the Plan-018-owned
  # bounded-retention bucket — the box checks when Plan-018 registers the
  # bucket, its TTL, and its Spec-020 shred-path membership.
  - { type: precondition_box_checked, box: "Engine-event diagnostic bucket registered" }
  # Self-referential gate: T5.13 extends the executor at the Phase-2B park and
  # schedule decision points. Supplement-label form, task-set membership.
  - { type: external_plan_phase_merged, plan: 015, phase: 2B }
```

#### Tasks

- **T5.13 — Always-on engine event record.**
  - **Files:** `packages/runtime-daemon/src/workflow/engine-event-log.ts` (NEW), `packages/runtime-daemon/src/workflow/phase-executor.ts` (EXTEND from T2.6).
  - **Provides:** the always-on per-run engine record of [Spec-015 §Engine event record (SA-43)](../specs/015-workflow-authoring-and-execution.md#engine-event-record-sa-43) — newline-delimited JSON written to the Plan-018-owned bounded-retention diagnostic bucket, with **no flag that turns it on**, because a run parks once and there is no second chance to have turned the log on. Every record carries the correlation keys `workflowRunId`, `phaseRunId`, `attemptNumber`, and a monotonic per-run sequence; per-run is a **correlation, not a storage partition**, so one bucket holds keyed records and the tier's retention and shred sweeps reach all of them. Content covers the engine decisions that are otherwise unreconstructable — admission and ready-set ticks, gate evaluations, park and schedule arming with the provenance that gated it, schedule fires and their re-check outcomes, and transition refusals. Work the engine dispatches is recorded as an **intent record paired with an outcome record**, and the intent's wording states that the engine is dispatching, never that the work is running: the pairing is what makes absence diagnostic, so a dispatch with no outcome beside it is itself the finding when the step between them wedges. It is **non-canonical**: no replay, verification, projection-rebuild, or audit path may read it, and its expiry degrades diagnosis only. It carries no secret material and no raw provider prompt or completion text — the `secret://` reference discipline of I-015-4 applies to this sink exactly as it does to events and artifacts, and the record's redaction is structural (references and identifiers are written; resolved material never is) rather than a scrub pass over free text.
  - **Consumes:** the Plan-018-owned diagnostic bucket, its TTL, and its shred-path registration (CP-015-9) — **this task does not dispatch until that registration lands**, and Plan-015 opens no sink of its own in the meantime; T2.5 and T2.6 for the park and schedule decision points.
  - **Spec coverage:** Spec-015 §Engine event record (SA-43); Spec-015 §Truth vs projection vs ephemeral (SA-25).
  - **Verifies invariant:** I-015-4, I-015-22.
  - **Tests:** a run started with no configuration writes records (there is no enable path to exercise, asserted by the absence of any flag in the writer's constructor surface); every emitted record carries all four correlation keys and the sequence is monotonic within a run and independent across runs; a canary secret resolved into a phase launch never appears in any record raw, base64-encoded, URL-encoded, or JSON-stringified, and neither does a canary prompt or completion string; deleting the entire record set leaves replay, chain verification, projection rebuild, and gate-chain audit all passing byte-identically (the non-canonical property, asserted destructively rather than by inspection); no production import path reads the writer's output (asserted structurally); records land in the Plan-018 bucket and inherit its TTL rather than any Plan-015-local retention; a dispatched resume writes an intent record and a distinct outcome record, and a wedged dispatch leaves the intent with no outcome beside it (asserted on the emitted lines, so a single record phrased as a completed fact fails the test); and the writer degrades to a no-op that never fails a run when the bucket is unavailable.

## Parallelization Notes

- Within Phase 1, T1.1 gates everything; T1.2, T1.3, and T1.4 then proceed in parallel, with T1.5 joining after T1.4 and T1.6 last (it asserts T1.1 against T1.4).
- Within Phase 1, T1.7 joins after T1.5 (it extends both T1.4's migration and T1.5's definition service) and T1.8 after T1.1; both land before T1.6, which stays last so it asserts the final contract-versus-DDL state.
- Within Phase 2, T2.3 (gate-chain writer) is independent of T2.2 (the `automated` arm) once T2.1 lands; T2.4 joins both.
- Phase 4's two tasks are independent of the Phase 2 gate-chain path and of each other until the tick boundary is shared — T4.1 owns the tick, so T4.2 lands after it.
- Within Phase 5, T5.1 and T5.2 are sequential (the verifier reuses the replay path); T5.3 may start once Phase 1's wire pairs are merged, but T5.4 waits for T5.3 and for phase-output and restart-resume semantics to stabilize.
- Within Phase 5, T5.6 follows T5.3, T5.5 follows T5.6 and T5.4 (it renders inside the surface T5.4 establishes), and T5.7 follows T5.6 but is independent of T5.5 — the CLI and the canvas are two consumers of one file form and may proceed in parallel.
- Within Phase 5's chat-start additions, T5.10 lands first (the shared start path must refuse before any new caller exists); T5.9 and T5.8 then proceed in parallel — the callback tools and the composer surface reach that one adjudicated path from two sides. All three are additionally held by the ADR-027 box (Gate-5).
- Within Phase 1's workflow-hardening addition, T1.9 joins after T1.4 (it extends that migration) and lands **before** T1.6, which stays last so the conformance suite asserts the final contract-versus-DDL state including the park-reason pin.
- Within the Phase 2B supplement, T2.5 follows T2.4's merged refusal path (the park is a branch off it) and T2.6 follows T2.5 — the scheduler arms off the park the recognition writes, so they are strictly sequential, not parallel. The supplement is held by the `Typed provider usage-limit signal registered` box (Gate-5) and dispatches by explicit label only, so Phase 3 and the rest of the numeric chain never wait on it.
- Across the Phase 5B and 5C supplements, T5.11 follows T5.1 (cancellability asserts across a restart, which needs the resumption path) and T5.12 follows T5.11 — the 5B chain, gated on Phase 5 and Phase 2B with no box; T5.13 (Phase 5C) is independent of both and of every Phase-5 task, so it may proceed once Phase 2B is merged and the `Engine-event diagnostic bucket registered` box (Gate-5) checks — in parallel with Phase 5 itself. None of the three depends on the builder or chat-start tasks, so they neither block nor are blocked by the ADR-026 and ADR-027 holds.
- Across phases the sequence is strictly ordered by the `plan_phase` chain in each phase's `preconditions:` block — this plan's parallelism is intra-phase only (A-015-20 restates the pre-audit work-stream framing in phase terms).

## Test And Verification Plan

Five test categories (SA-29): property-based, fuzz, load, long-running integration, security regression. Each carries a V1 _ambition level_ so the category can be stop-marked independently. Replay-determinism scaffolding follows the Temporal `runReplayHistory` pattern; property + fuzz frameworks pinned to `fast-check` and `@jazzer.js/core`; CVE-reproducer corpus seeds the security-regression battery (SA-30).

| Category | Covers | V1 Ambition | CI Cadence | First runnable at |
| --- | --- | --- | --- | --- |
| Property-based (`fast-check` v4.x) | DAG acyclicity, ready-set determinism, `max_phase_transitions` / `max_duration`, `ParallelJoinPolicy` semantics, retry-re-entry stability, the eight-rule graph refusal set over generated valid and invalid documents (T1.8), layout-perturbation canonical-byte stability under arbitrary geometry (I-015-15), and resume-jitter determinism plus full-window spread over generated `workflowRunId`s with the modular-spelling collapse as an explicit failing arm (T2.6, I-015-20) | **Hardened** — adversarial + concurrency | PR (numRuns=100); nightly (numRuns=10 000) | Phase 1 (definition-level properties, T1.5; graph-refusal and layout-perturbation properties, T1.8); extended at Phase 2 (T2.4, T2.6) and Phase 4 (T4.1) |
| Fuzz (`@jazzer.js/core` v4.x) | Workflow-definition parser, expression grammar (I-015-2), secrets resolver (I-015-4) | **Foundational+** — 15 min/target per PR; 2 h/target nightly | PR (15 min/target) + nightly (2 h/target) | Phase 1 (T1.1 grammar and secret-reference targets; T1.5 definition parser) |
| Load | Parallel executor contention, the memory gate under pressure and its ramp interval, SQLite write-amp | **Foundational** — baseline regression only, no SLO gate | Nightly | Phase 4 (T4.1, T4.2 — the write-amp reading feeds the `## Risks And Blockers` calibration trigger) |
| Long-running integration (`@playwright/test` v1.58.x + real daemon) | Multi-day `human` phase resume, checkpoint/replay determinism, multi-agent channel lifecycle, optimistic-concurrency on human submit, and the park lifecycle end to end — a run parked across a daemon restart re-arms its schedule after rebuild, fires, re-checks, and either resumes or re-parks (T2.6), with the cancel-clears-schedule arm asserted across the same restart boundary (T5.11) | **Hardened** — compressed-time nightly + real-time weekly | Nightly (compressed) + weekly (real-time) | Phase 3 (T3.1, T3.2); replay-determinism arm joins at Phase 5 (T5.1); park-lifecycle arm at Phase 2 (T2.6) and Phase 5 (T5.11) |
| Security regression | Per-invariant I-015-1…I-015-7 battery with CVE-reproducer corpora (SA-30) | **Hardened** — full battery gates merge | PR + merge | Rows land with the task that first makes each invariant reachable — see the phase column below |

Tool pins follow the repo convention of naming the CI-pinned line in the cell (A-015-19); `@playwright/test` v1.58.x is the line [Plan-021](./021-desktop-shell-and-renderer.md) already pins for the desktop E2E harness, so the two suites share one Playwright version.

**Security regression battery (SA-30) — per-invariant CVE corpora.** The plan-side invariant ids are the `## Invariants` entries; the parenthesized `I1`–`I7` are the [Spec-015 §Pitfalls To Avoid](../specs/015-workflow-authoring-and-execution.md#pitfalls-to-avoid) labels they carry. The Phase column discharges A-015-18 — each row lands with the task that first makes the invariant reachable, so no battery row is orphaned from a phase.

| Invariant | Assertion | CVE / source seed | Phase |
| --- | --- | --- | --- |
| I-015-1 (I1) — argv-list-only execution | Semgrep rule bans `exec`/`shell:true`; dynamic test proves shell metachars reach argv unshelled | Generic shell-injection corpus | Phase 2 (T2.2); re-asserted at Phase 4 (T4.2) |
| I-015-2 (I2) — typed substitution, no eval | Every expression payload either parses to whitelisted AST or throws `ExpressionParseError` | n8n [CVE-2025-68613](https://github.com/advisories/GHSA-v98v-ff95-f3cp); Airflow [CVE-2024-39877](https://nvd.nist.gov/vuln/detail/CVE-2024-39877); Airflow [CVE-2024-56373](https://nvd.nist.gov/vuln/detail/CVE-2024-56373); Jenkins `CVE-2024-34144` / `CVE-2024-34145` | Phase 1 (T1.1, T1.5) |
| I-015-3 (I3) — typed approver capability | Cedar capability check; admin override logged as distinct entry kind | Plan-010 policy corpus | Phase 2 (T2.3); Phase 3 (T3.3) |
| I-015-4 (I4) — secrets never in argv/logs/artifacts | Canary secret never appears raw / base64 / URL-encoded / JSON-stringified | Airflow `#54540` masker-bypass reproducer | Phase 1 (T1.1); Phase 2 (T2.2); engine-record arm at Phase 5 (T5.13 — the same invariant applied to the SA-43 diagnostic sink, which is a log and therefore already in this invariant's scope) |
| I-015-5 (I5) — content-addressed external refs | Mutating pinned tool bytes yields `ContentHashMismatch` | GitHub Actions v3→v4 artifact mutability precedent | Phase 1 (T1.5); Phase 2 (T2.2) |
| I-015-6 (I6) — human-phase OWASP uploads | Zip bomb, polyglot, symlink-traversal, oversize, mismatched Content-Type all quarantined | Argo [CVE-2025-66626](https://nvd.nist.gov/vuln/detail/CVE-2025-66626); [OWASP File Upload Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html) | Phase 3 (T3.2); rendering arm at Phase 5 (T5.4) |
| I-015-7 (I7) — append-only hash-chained approval log | Row-level tampering detected at daemon start; replay uses at-approval-time policy | Crosby & Wallach 2009 flat-chain pattern; Spec-005 integrity protocol | Phase 2 (T2.3); detection arms at Phase 5 (T5.1, T5.2) |

The **nineteen** non-security invariants are exercised alongside (census re-derived 2026-08-18: eleven before the workflow-hardening amendment, plus its six = seventeen, plus the park-surface and operator-controls amendment's two): I-015-8 (SA-20 ordering) at T2.1 / T2.3 / T3.1 / T4.1, I-015-9 (SA-26 dual anchor) at T2.3 / T5.2, I-015-10 (SA-25 truth vs projection) at T1.4 / T3.2 / T4.1 / T5.1, I-015-11 (SA-21 replay determinism) at T1.1 / T2.4 / T5.1, I-015-12 (domain lockstep) at T1.6 / T1.7 / T1.9 / T5.3 / T5.4, I-015-13 (enumeration visibility) at T1.5, I-015-14 (tool-governance non-bypass) at T1.8 / T5.5 / T5.6, I-015-15 (layout-independent canonical bytes) at T1.8 / T5.5 / T5.6, I-015-16 (edit-time validation mirrors the daemon) at T1.8 / T5.5 / T5.7, I-015-17 (command-interception totality) at T5.8, I-015-18 (per-start named-action adjudication) at T5.9 / T5.10, I-015-19 (typed-only usage-limit recognition) at T2.5, I-015-20 (durable-schedule discipline) at T2.6, I-015-21 (park cancellability and record preservation) at T5.11, I-015-22 (engine record always-on and non-canonical) at T5.13, I-015-23 (attention fold is never a lock) at T2.5, I-015-24 (explicit audited frozen-definition repair) at T5.12, I-015-25 (cancel-and-event atomicity with the run-scoped park clear) at T5.14, and I-015-26 (park read members are live, never record) at T5.14 / T5.15. Two of the workflow-hardening amendment's six carry a security-flavored arm that rides an existing battery row rather than a new one: I-015-22's canary-secret and canary-prompt assertions are I-015-4 applied to the SA-43 sink, so T5.13 declares **both** ids on its `Verifies invariant:` marker and appears in I-015-4's own Verified-by column rather than being carried by prose alone (the battery table's Phase column above records the same arm), and I-015-21's "`park_cause` never reaches a phase output, artifact, or agent-visible context" is asserted at T2.5 and T5.11 on every write path rather than sampled at one.

**Replay-testing contract (SA-31).** Temporal `runReplayHistory` pattern: replay executor consumes `session_events` history and asserts `DeterminismViolationError` is not thrown; final state is `deepEqual` to original. Every one of the `workflow.*` event types enumerated in [Spec-015 §Workflow Timeline Integration](../specs/015-workflow-authoring-and-execution.md#workflow-timeline-integration) has at least one replay-correctness test ([Temporal TS SDK testing](https://docs.temporal.io/develop/typescript/testing-suite)). The contract is gated at Phase 5 (T5.1), which is why `## Rollout Order` item 5 orders resumption ahead of the UI.

**CI budget.** PR pipeline ≤ 30 min wall-clock; nightly ≤ 8 h; weekly unbounded real-time integration. Fuzz crashers are minimized, checked in under `corpus/<target>/regressions/`, and promoted to named `vitest` regression tests.

### What each layer proves

**Executor unit tests, one per node kind**, over an item fixture covering: the empty input, the single item, the many-item batch, the per-item expression re-evaluation, the item lineage the kind must emit, the failure path under each error disposition, and the redaction of any resolved secret. A branch kind additionally asserts the shape of its output array; the loop kind asserts its done-and-loop ordering and its run-index progression; the merge kind asserts each of its join modes.

**Engine tests**: traversal order under fan-out with explicit sibling order, the fan-in buffer, loop re-entry, item lineage across a branch and a merge, retry under the engine's clamps, the optional run cap, cooperative cancel, the waiting-and-resume cycle, the crash sweep leaving a `waiting` run alone, and a partial run reusing clean step data.

**Expression tests**: each binding, per-item evaluation, the evaluation budget and its abort, redaction of resolved secrets, and the two lineage failure modes with the exact guidance each gives.

**Code node tests**: on the quick tier, the memory limit, the interrupt limit, no host access, and log capture into the step's log. On the full tier, the declared packages fetched before the program runs and cached in a directory the run may write, the step launched at the run's execution posture, and a sandbox that cannot start at a `sandboxed` posture refusing the step rather than running it unprotected.

**Canvas tests on the browser tier**: the controlled round trip, where a change callback produces exactly the document mutation and nothing else; a refusal during the drag for each refused shape; a drop from the palette landing at the pointer; an edge-drop opening the filtered palette; undo and redo across a coalesced drag; copy and paste re-iding nodes; handle re-registration after a param write that changes the port set; and the announcer receiving one message per structural change. These run headless and never reveal a window.

**Builder store tests**: the history cap, the dirty lattice per rule, the validation strip's contents, save-then-run against a dirty draft, and layout writes never touching the hashed body.

**Round-trip tests**: export then import reproducing the same bytes and the same content hash; a document with no layout laying out identically twice; a copy-paste fragment parsing back into the same nodes.

**Proportion**: a build proves the surface scales by stepping the console's text size — the reading matter and every box that carries it grow in one proportion, the icon strokes and the corner radii stay as drawn, and the canvas keeps its own coordinate space, where the node box, the handles, the edge geometry and the grid step are canvas units the zoom scales.

**Surface sweeps**: every screen state below is reachable, every rule with a visible consequence is observable on the screen, and every node kind in the catalog is in the palette.

### Two workflows every scenario is built from

**Morning PR digest** — twelve nodes with one branch, one merge, one loop, one agent node carrying a capability edge, and one approval:

1. `Every weekday 8:30 AM` — a schedule trigger, cron `0 30 8 * * 1-5`, timezone `America/New_York`.
2. `List open PRs` — an HTTP request, one item per pull request.
3. `Needs review?` — an If, true when the review decision is not approved.
4. `For each PR` — a loop over items, batch 1, on the true branch; outputs done and loop.
5. `Read the diff` — a repo diff on the loop branch, set to continue on its error output, so a diff it cannot read leaves by the error branch back into `For each PR` and the loop takes the next pull request.
6. `Review sidekick` — an `agent.run` node, the reviewer agent on a Codex account, its prompt referencing the item's title and diff.
7. `Repo search` — an MCP tool wired by a capability edge into node 6's tool input.
8. `Skipped PRs` — a set-variables node on the false branch.
9. `Merge digest` — a merge in append mode, input 1 from the loop's done output and input 2 from node 8.
10. `Approve the post` — an approval node, outputs approved and rejected.
11. `Write summary` — a write-summary node on the approved branch.
12. `Save as artifact` — a save-as-artifact node on the rejected branch.

**Nightly tests** — five nodes, the failure example: `Every night 2:00 AM` (a schedule trigger) → `Run the suite` (a shell command running the test script) → `Passed?` (an If) → `Write summary` or `Stop with error`.

### The screen states each build must reach

1. **Builder, empty** — the trigger picker asking how the workflow starts, the eight trigger kinds each with one line, the palette docked and live beside it, the toolbar with an untitled name and Save unavailable. Placing a kind from the palette steps the picker aside, and the validation strip then reads "This workflow has no trigger. Every workflow has exactly one." and "Unsaved changes · never saved, 1 node to save."
2. **Builder, Morning PR digest loaded** — all twelve nodes laid out left to right, the If's two labelled branches, the loop's done and loop outputs with the body wiring back, the merge's two inputs, `Read the diff`'s error branch returning to the loop head, the capability edge entering node 6 from below, the approval's two outputs, the schedule pill reading the next fire, the version readout reading `v7`, Save unavailable because the draft is clean.
3. **Builder, a node selected, the inspector on the agent rows and Output** — node 6 selected with its toolbar visible; the inspector open with its chooser reading `reviewer`, its list open on the search field with the cursor on the node's own row, and the four rows under it: Provider `Codex`, Model `gpt-5.6-sol`, Effort `high` marked as the author's own with a reset beside it, and Account reading the identity the provider reports at sign-in with its plan chip, the default chip standing only on the account marked default, which this one is not. Output below shows the last run's items in the table view with the item count, the search box, the pin button and a draggable field.
4. **Builder, during a run** — the run overlay: nodes 1 to 5 succeeded with item counts, node 6 running with its ring, node 7 rendering as an attached capability rather than a step state, nodes 8 to 12 idle, the edge into node 6 animated with its item count, and the toolbar's run control reading `Cancel`.
5. **Workflows tab, eight rows** — a schedule-triggered workflow enabled with its last run succeeded, a second whose last run failed, a manual one, a file-watch one, a webhook one, a disabled schedule, a chat-started one, and one with no runs — with the filter chips above and one row's action menu open.
6. **Runs list, sixteen rows** — one running with a live duration, two waiting on a person at the same approval, one parked on a provider limit with its resume instant, one parked with no instant armed reading `Awaiting resume — no instant is armed.`, one failed, one past the retention bound reading that its step data expired, one marked `Keep`, the rest succeeded; the started-by column showing a schedule, chat, an agent, the user and a webhook; the strip above the list reading `Next waiting (2)` at its left with the starting hold off at its right.
7. **A run's page, failed step** — a Nightly tests run: the run graph with `Run the suite` red, the step panel open on Error showing the exit code, the last log lines and the failing item index, the retry-from-this-step action, and the header carrying the pinned version, the trigger and — because the run has finished — `Open in Review`.
8. **A run's page, waiting on an approval** — a Morning PR digest run: the graph with every step up to `Merge digest` succeeded and `Approve the post` amber; the step panel showing the pending request and its two outcomes as `Reject` and `Approve` with `Open in Review` beside them, all three pressable there and the first two resolving the approval from the step that is blocking; the header saying waiting and carrying no `Open in Review` of its own because the run is still going, with Cancel available. Pressing `Approve` takes all three controls away and leaves one line in their place, `Approved at 2:14 PM`, with no way back, and the run's own Cancel and Resume are what act next.
9. **The results row in the session that asked** — the flat transcript row a run leaves when it ends: workflow name, status, duration, a one-line summary, two attachments as file rows, a link to the run, no cost; above it the live run's progress row ticking its elapsed time in place with nothing pinned; the composer below showing `/workflow ` with the thirteen verbs listed.
10. **Builder, the Versions panel with an older version selected** — the panel open beside the canvas listing `v7`, `v6`, `v5` and `v4` newest first, each with when it was saved, who saved it and its one-line count from the structural diff; with `v5` selected the canvas carries the difference against `v7` — `Review sidekick`, `Skipped PRs` and `Approve the post` marked as changed, the two added edges in the success colour and the removed one dimmed — and node 6's inspector showing its `v5` and `v7` params side by side, with `Restore` and `Show runs` under the list.
11. **Runs list, new runs held** — the starting hold on, a Run now pressed on a workflow row while it is on, so nothing new joins the list and the switch reads `Pause new runs · 1 waiting` with the quiet line beside it saying a run already going finishes; switching it off puts the held run at the top of the list, running.
12. **Walking the runs that need a person** — `Next waiting (2)` pressed from the list opens the first of the two approvals, the one that has waited since 8:30 AM, with its step panel on the pending request, and the control, still in place above the run, now reads `Next waiting (1)`; once that run is dealt with, pressing it again opens the second, and with nothing behind that one the control stands where it was, disabled, reading `Nothing waiting`.

### The done list, walked end to end before the screen is called finished

1. Drag a node from the palette, wire it, click into it, edit every param type, delete it — with no reload and with undo.
2. Save; reopen from the Workflows tab; positions, notes and params intact; export then import round-trips the bytes and the content hash.
3. Run now; watch the nodes light up; open a step's input, output and logs; see the cost on the agent step.
4. Branch with If, fan out with Switch, join with Merge, iterate with the loop, and read upstream values through both the current-item and the by-label bindings.
5. A failing shell step with the error-output disposition routes to the error branch; a retry retries; the runs list shows the failure on the right node.
6. Schedule every five minutes, quit and reopen the app, and the schedule fires; disabling stops it.
7. From a session chat, the run verb then the results verb posts the results row, and the agent reads it and summarizes.
8. In chat, "build me a workflow that …" produces a saved definition through the create tool, listed in the Workflows tab and openable in the builder, laid out.
9. "Fix the nightly workflow, the shell step's working directory is wrong" produces a new version whose difference is visible in the version chain.
10. An approval node parks the run and the approvals surface resolves it; a spent provider account parks and auto-resumes.
11. Frame, memory, CPU and bundle budgets unmoved beyond the builder chunk's declared cost; accessibility checks clean; every affordance keyboard-reachable.

## Rollout Order

The five items map 1:1 onto `## Implementation Phase Sequence` Phases 1–5; the box-held and supplement-chained legs of items 2 and 5 ship as the explicit-label supplement Phases 2B, 5B, and 5C when their gates open, with no rollout position of their own.

1. Land workflow definition + version contracts, the `workflow.*` event schemas, the thirteen-table schema, writer-worker integration, the graph refusal set with its layout-exclusion split, copy-on-write editing of `shared` definitions, and the park-and-resume contract, payload, and column surface. (Phase 1; `## Implementation Steps` 1–2 plus step 9's contract and persistence legs and step 11's contract, payload, and migration leg.)
2. Enable sequential execution for `single-agent` + `automated` phases with the four gate types and gate-chain writer, then — behind the Plan-004 typed-signal box, as the Phase 2B supplement — typed usage-limit recognition with the immediate park and the durable auto-resume schedule with its boot sweep. (Phase 2 with its 2B supplement; step 3's `single-agent` and `automated` arms, plus step 5 and step 11's recognition, park, and scheduler legs.)
3. Enable `multi-agent` phase with OWN channel linkage + `human` phase with local-draft UX. (Phase 3; step 3's `multi-agent` and `human` arms.)
4. Enable parallel phase execution with memory-gated admission and `ParallelJoinPolicy`. (Phase 4; step 4.)
5. Enable restart-safe resumption with `ProjectionRebuild` and hash-chain replay verification, then the `sidekicks workflow verify-gate-chain <run_id>` CLI, then the SDK and the authoring / run-detail / human-phase UI, then the builder canvas, the canonical definition file form, and the `sidekicks workflow` command set, and finally the chat-start surfaces — the start-path authorization, the `workflow_run` callback tool, and the composer command plus affordance, following the T5.4/T5.5 desktop items with no new rollout stage (T5.10 before T5.9/T5.8, ADR-027-gated), and — independently of the builder and chat-start legs — park cancellability, the frozen-definition repair, the operator recovery surface that reaches them, and the always-on engine event record (T5.11 → T5.12 → T5.14 → T5.15 as the Phase 5B supplement; T5.13 in parallel as the Phase 5C supplement, Plan-018-box-gated). (Phase 5 with its 5B and 5C supplements; steps 6, 7, and 8 plus step 9's renderer, file-form, and CLI legs, step 10's chat-start surfaces, step 11's cancellability, repair, and engine-record legs, and the whole of step 12 — the pre-audit rollout list covered only step 8, leaving steps 6 and 7 without a rollout position even though step 6 is the hard prerequisite for the SA-31 replay contract that gates merges. A-015-09 assigns both here and orders them ahead of the UI within the phase.)

## Rollback Or Fallback

- If parallel execution or gate-chain writer regresses, disable parallel blocks and restrict to sequential `single-agent` + `automated` — the engine still satisfies C-1…C-7 for V1 partial rollout.
- If the durable auto-resume schedule misbehaves, the fallback is the **unscheduled park**, which is already a first-class shipped state: stop arming `auto_resume_at` and every parked run stays visible, cancellable, and manually resumable with its stated cause. Recognition, the park itself, and cancellability are independent of the scheduler, so this degrades pacing without losing the park. The four columns stay in place — a nullable column that nothing writes costs nothing and avoids a down-migration.
- If the engine event record proves too costly, the fallback is **bounded recording, never no recording**: shed the high-frequency record classes first — ready-set ticks and gate evaluations, the classes that dominate volume — while retaining parks, schedule arming and fires, refusals, and the dispatch-intent/outcome pairs, so the records that exist precisely for the unreproducible moments survive the cost trim. The no-op writer is reserved for the one case T5.13 already requires it: bucket unavailability, where failing a run over a diagnostic sink would invert the record's non-canonical rank (I-015-22). Neither arm is a disable path — I-015-22 admits no flag that turns the record off, and a cost-motivated off switch would recreate exactly the there-was-no-second-chance blindness the always-on posture exists to avoid.

## Risks And Blockers

- Write amplification under pathological `progressed` heartbeat floods. **Named calibration criterion (A-015-24):** the nightly load category (Phase 4, T4.2) records per-workflow write volume against the [Local SQLite Schema §Workflow Tables](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-015) estimate of ~42 KB / 110 writes per 10-phase workflow; a sustained nightly reading above **2× that estimate** is the trigger to add an executor rate-limit, and until it fires no rate-limit ships. The trigger is a threshold on an already-collected measurement, not a scheduled revisit.
- Per-run gate-chain verification cost scales linearly (~1 ms/row) — acceptable for operator-triggered audit; not in the hot path (T5.2 asserts the linearity and the out-of-hot-path placement).
- **How `uv` reaches a person's machine is not settled, and the Python leg of the full tier waits on it.** `bun` publishes its own binary on npm at 1.4.2, so the daemon carries it the way it carries any other pinned tool; the npm package named `uv` is an unrelated UTF-8 data validator, so npm is not the route for it (both read from the registry 2026-09-21). The candidates are a bundled binary, the platform's own package manager, or a first-run download, and T2.2 cannot ship its Python arm until one is chosen. The JavaScript and TypeScript arm is unblocked either way.
- **The Claude-leg sandbox wrapper is an early research preview.** `@anthropic-ai/sandbox-runtime` 0.0.77 says its interfaces and configuration formats may still change. The exposure is bounded by how it is used — a command wrapper and a profile, resolved through the Plan-010 posture surface rather than linked into the daemon — so a breaking change costs an argv-and-profile fix, and the Codex leg is a separate wrapper that would not break with it.
- **The boundary around the full tier is measured on macOS only.** Measured 2026-09-21 on an Apple-silicon Mac: a TypeScript program naming its package in its import line and a Python program naming its packages in its own inline metadata block each ran with their packages inside `codex sandbox` on the workspace profile, with home-directory writes and the network blocked, and the sandbox cost about 0.02 s per run. Linux and native Windows are unmeasured; on native Windows the Claude leg has no sandbox at all, so a `sandboxed` posture there refuses rather than degrades ([Spec-010 §Required Behavior](../specs/010-approvals-permissions-and-trust-boundaries.md#required-behavior)).
- Non-determinism in replay if driver adapters leak wall-clock or random seed state — Plan-013 runtime-binding resume is the guard, and T5.1's replay-determinism battery (SA-31, asserting no `DeterminismViolationError` and a `deepEqual` final state) is the regression.
- **Error vocabulary gap carried, not closed (A-015-15).** `error-contracts.md §Workflow` defines **ten** codes for a surface with at least **twenty-two** refusal points (nine at the audit, four from the visual-builder amendment, one from the explicit-topology fields, one from the `shared`-scope operator-authorization boundary, one — `workflow.start_denied` — landed by the chat-start amendment as the owed extension's first entry, and three from the workflow-hardening amendment's SA-41 repair path, enumerated identically in `## API And Transport Changes`: a re-pin against a run that is not parked, one requested while any of the run's attempts is still in flight (the resuming phase continuing one rather than entering fresh, or a parked parallel sibling holding one — SA-41's run-wide boundary), and one whose target version cannot account for the phases the run already completed — the omitted phase id and the unreachable topology being one refusal point, since SA-41 states they are the same failure — and three from the 2026-08-18 park-surface and operator-controls amendment's SA-45 path: a Cedar-denied operator control, a cancel of a run that already reached `succeeded` or `failed`, and a resume of a run that is not parked). **The park and pacing surface adds none of its own** — SA-42 makes a parked run cancellable without precondition, so cancellation has no refusal to name — and that claim is scoped to the pacing rules, not to the operator surface that reaches them: the three SA-45 points are the operator surface's, minted when it landed. That file is outside this plan's write scope, so the extension is owed there and named in `## API And Transport Changes`; no Plan-015 task mints a code. Two slices have since landed typed: the SA-41 repair codes at the workflow-hardening amendment's 2026-08-17 review round (`workflow.repair_not_parked`, `workflow.repair_attempt_in_flight`, `workflow.repair_version_unaccountable`) and the SA-45 operator-control codes at the 2026-08-18 amendment (`workflow.control_denied`, `workflow.run_not_cancellable`, `workflow.resume_not_parked`) — **seven of the twenty-two** points now carry codes, `workflow.start_denied` included — so T5.12 and T5.14 both ship typed with no residual dependency on the extension. The remaining **fifteen** refusal points still cannot ship typed until it lands; that number is unmoved by the 2026-08-18 growth because all three new points arrived with their codes in the same diff. Tracked as a blocker on Phases 2, 4, and 5 rather than as a task.
- **The park's typed carrier now exists, and this plan still will not fake one (CP-015-10).** Every rule in [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) hangs off a driver signal that Plan-004 shipped on 2026-08-18 (`I-004-6`, T3.16), and C-19 forbids the stand-in it replaced — matching provider message text. The pitfall survives the carrier's arrival rather than retiring with it, because the temptation changes shape rather than disappearing: the signal is deliberately **silent** on providers and shapes it cannot name, so a driver that observes no usage limit emits nothing, and "nothing" must be read as _not known to be limited_, never as _known not to be limited_. Widening recognition to cover that silence — by message text, exit code, or bare HTTP status — is the same failure the invariant was written against, and it fails the same way: a matcher that silently stops matching converts a park back into a burned ladder with no signal that anything changed.
- **The park's operator control surface — residual 2026-08-16, closed 2026-08-18.** The workflow-hardening amendment shipped [Spec-015 §Park integrity and cancellability (SA-42)](../specs/015-workflow-authoring-and-execution.md#park-integrity-and-cancellability-sa-42)'s cancellability and repair as engine state-machine rules while the wire registry carried no run-cancel, run-resume, or re-pin operation and the taxonomy carried no run-cancelled event type — both predating that amendment, since the `cancelled` run status had had no named producer from the day it was declared. The gap was recorded as a residual rather than left as a build-time discovery, then tracked as [BL-151](../archive/backlog-archive.md), and the park-surface and operator-controls amendment closed it: T5.14 ships the two operations and `workflow.cancelled`, T5.15 the client verbs, and BL-151 is `completed`. **The constraint the residual fixed in advance held exactly as written** — the operation set and the run-cancelled event minted **together**, so a cancelled run's status and its cleared live park columns are a function of the log (SA-25) rather than surviving only in a projection a rebuild would overwrite (I-015-25). What the closure cost, stated rather than elided: the census this plan's 2026-08-16 amendment deliberately held still did move — registry eleven → thirteen, taxonomy 23 → 24 — which is why the closure rode its own dated amendment with its own re-derivations rather than being folded into the one that named the residual.
- **The durable park write must not ride the provider event delivery path (implementation constraint on T2.5 / T2.6).** The refusal that triggers a park arrives on the driver's event stream, and the natural spelling — performing the park's durable write and the schedule's arming inline on that delivery — deadlocks: the writing path ends up waiting on a response that can only arrive through the stream it is itself blocking. The campaign's surveyed reference runtime (2026-08-12 parity survey) hit this shape twice, in independent places, and settled on the same rule both times — detach synchronously on the delivery path, then do the durable park and schedule writes off it. T2.5 and T2.6 build to that rule, and the regression is an ordering test: a park must complete while the driver's stream is still being consumed. The split leaves a crash interval — the daemon dying after the handler detaches but before the off-path park write commits — and its closure is the refusal's **reproducibility**, not a durable inbox: a spent account keeps refusing, so the restart's ordinary mid-phase resume (T2.1) re-dispatches the same attempt and either receives a fresh typed refusal and parks then — no attempt consumed, no ladder burned, per the park path's own rules — or, where the account reset during the downtime, simply proceeds, the park never having been needed. T2.5's crash-injection test pins that convergence, so SA-40's durability never rests on an in-memory signal surviving a crash.

## Progress Log

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

- **2026-08-10 — Tier-7 plan-readiness audit; `review → approved`.** First-time promotion per the runbook `§Status Promotion Gate`, cleared at Tier 7's place in the build order. The audit authored `## Implementation Phase Sequence` (five phases mapped 1:1 onto `## Rollout Order`, 19 tasks, each `#### Tasks` entry carrying `**Files:**` / `**Provides:**` / `**Consumes:**` / `**Spec coverage:**` / `**Verifies invariant:**` / `**Tests:**`), `## Invariants` (`I-015-1` … `I-015-13`, carrying the seven SA-13 security invariants plus the SA-20 ordering, SA-25 truth-vs-projection, SA-26 dual-anchor, and SA-21 replay-determinism legs, with the domain-lockstep entry declared plan-owned and the enumeration-visibility entry grounded in the operation the audit minted), `## Cross-Plan Obligations` (`CP-015-1` … `CP-015-5` against Plans 005 / 010 / 012 / 013 / 014 — CP-015-5 supplies the return-cite Plan-014's CP-014-6 forward-declared), and `## Upstream-Tier Amendments Required`. Required ADRs gained ADR-012 and ADR-017. The contract doc's `§Plan-015` block and the schema doc's Plan-015 tables were corrected in the same swap: the `PhaseDefinition.type` union widened from the pre-amendment two values to the four V1 phase types, five previously shapeless operations gained request/response pairs, `WorkflowGateResolveResponse` gained the SA-26 dual anchor, a `workflow` method registry landed, and the `phase_type` CHECK was rewritten to the four phase types (it had conflated gate types with phase types and omitted `automated` outright, so an `automated` phase could not be inserted).
- **2026-08-10 — scope-domain adjudication (A-015-11), ratified.** The contract doc and the DDL carried `session` \| `channel` while [Spec-015 §Resolved Questions and V1 Scope Decisions](../specs/015-workflow-authoring-and-execution.md#resolved-questions-and-v1-scope-decisions) carried session- and project-scoped only — a two-versus-one split the audit deliberately routed to the owner rather than calling. The ratified resolution takes neither side wholesale: the unsupported `channel` member is struck, and the domain becomes three-valued — `session` \| `project` \| `shared` — with `shared` a daemon-local cross-project reuse tier for workflow templates. It widens visibility breadth only: no new table, no sync path, and no change to [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals), which continues to exclude marketplace and global-library distribution. One column is required and was added in the same swap — `workflow_definitions.scope_ref`, modelled field-for-field on the scope-qualified binding shape [Spec-025 §Unified Inventory](../specs/025-mcp-server-configuration-and-governance.md#unified-inventory) already governs, with the dedupe key moved to `(scope, scope_ref, content_hash)`. Widening `scope` alone would have shipped an unstorable tier: `project` would have named no project, and the old `(session_id, content_hash)` key would have split one `shared` definition into a row per submitting session. Because the growth adds a member to a governed domain, Spec-015 flipped `approved → review` and was restored `approved` in this same swap under this audit's targeted coverage — the flip-and-restore-in-one-swap shape precedented by the Plan-008/Spec-008 deltas — so the README spec-status census does not move.
- **2026-08-10 — two criticals deferred to named upstream vehicles.** `A-015-02` (the Spec-005 `workflow.*` registration of five categories and 23 type literals) and `A-015-07` (the OWN-channel `turnsPerAgent` provider, carrying the A-015-14 `InterruptReason` leg) sit in sealed Tier-3 and Tier-5 respectively, which the runbook `§Cross-Tier Amendment Contingency` forbids a Tier-7 walk to amend. Each is recorded under `## Upstream-Tier Amendments Required`, each has a born-unchecked scoped §Preconditions box, and each box is machine-enforced by `precondition_box_checked` entries — the registration box on all five phases, the turn-budget box on Phase 3 with Phases 4–5 chained behind Phase 3 so the auto-walk cannot soft-skip past it. Promotion therefore clears the audit and status gates only; no phase dispatches until its named delta lands.
- **2026-08-10 — divergences recorded, not forced.** Five findings resolve outside this plan's write scope and are surfaced rather than patched: the scope domain in `docs/domain/workflow-model.md`, which still reads `session` or `channel` and is now stale in both directions — the `channel` member is struck and the domain is three-valued with a `scope_ref` companion — so that domain file owes a correction this audit does not take; `A-015-15` (the `error-contracts.md §Workflow` extension from three codes to the nine-plus refusal points, named in `## API And Transport Changes` and carried as a blocker in `## Risks And Blockers`), the fourth leg of `A-015-10` (`docs/domain/workflow-phase-model.md` still types `PhaseRunId` as a `RunId`, which the not-a-ULID ruling above contradicts — that domain file is another owner's surface, so the correction is owed there), the **encoding half of `A-015-10`** (the audit fixed that a `phaseRunId` is totally derived but deliberately did not choose the digest's text rendering: that is a one-way design decision on a durable primary key, of the same class as the scope domain, and is routed to lead ratification through [Spec-015 §Open Questions](../specs/015-workflow-authoring-and-execution.md#open-questions) — every surface here describes the value as a derived opaque identifier and T1.1 keeps the rendering behind one seam so the ruling lands in a single place), and `A-015-28` (the count embedded in a Spec-015 heading — deliberately not renamed: it is a nit, the rename is pure slug churn, and no citer of that slug exists).

- **2026-08-10 — visual-builder amendment (ADR-026 pairing).** V1 gains a node-graph authoring surface, specified in [Spec-015 §Visual Workflow Builder](../specs/015-workflow-authoring-and-execution.md#visual-workflow-builder) under SA-32…SA-37 and C-17 and governed by ADR-026, which lands `proposed` in this same PR. The plan grew five tasks — T1.7 (copy-on-write off `shared` plus `project → shared` promotion, carried by the new `parent_content_hash` column), T1.8 (the graph ↔ definition mapping and the seven-rule refusal set), T5.5 (the React Flow canvas), T5.6 (the canonical export / import file form), and T5.7 (the `sidekicks workflow` command set) — three invariants (I-015-14 tool-governance non-bypass, I-015-15 layout-independent canonical bytes, I-015-16 edit-time validation mirrors the daemon), and one obligation (CP-015-6, a read-only consumption of the Plan-004 / Plan-025 tool-governance surface with no upstream delta owed). No new wire operation was minted: promotion and import both ride `workflow.definitionCreate`, so the eleven-method registry is unchanged. Because ADR-026 is not yet `accepted`, the `Required ADRs are accepted` box keeps its check under a dated scope clause naming what it covers, and ADR-026's ratification is carried by a separate born-unchecked box whose `precondition_box_checked` entries ride Phases 1 and 5 — the two phases owning the five builder tasks. Every pre-amendment task stays dispatch-eligible on its own gates.

- **2026-08-11 — PR #318 review round (Codex).** Three repairs, audited in the same swap under the audit's targeted coverage (the Plan-005 PR #278 self-audit shape — the growth lands audited rather than opening a second flip loop). (1) **Topology persistence.** SA-32 ratified the graph model while the stored body kept only a linear phase array, leaving a non-linear definition's edges nowhere — the canonical body now embeds each phase's optional `dependsOn` predecessor list and the join-phase `parallelJoinPolicy` (all-or-none per definition; when every phase omits them the array order stays the declared chain and the daemon never materializes the lists), additive-optional per ADR-018 so existing linear definitions keep their bytes and hashes; T1.1/T1.5/T1.6/T1.8, the contract block, and the DDL carry the spelling, and `workflow_versions` now stores the full canonical `definition_body` (was `phase_definitions`, which kept the array alone and left later versions' `name`/`entry` unreconstructable against `content_hash` — the read → export round-trip pinned in the same review round requires the whole preimage). (2) **Version-row uniqueness.** Global `UNIQUE(content_hash)` dedupe would have refused the T1.7 promotion and copy-on-write paths, which legitimately insert a new definition's version 1 from the source version's exact bytes — the key is now `UNIQUE(definition_id, content_hash)`, pinned by T1.4/T1.7 tests. (3) **`shared`-scope authorization.** SA-36's "operator action" had no named enforcement — the C-12 protection-that-isn't class — so every `shared`-target `workflow.definitionCreate` (direct authoring, file import, promotion) now adjudicates operator-scope authorization through the consumed Plan-010 `PermissionCheckService` against the daemon-resolved node-owner identity, fail-closed, minting no policy, action family, or category of its own (CP-015-2, T1.5). The two refusals (1) and (3) add move the A-015-15 error-vocabulary census thirteen → fifteen at its three sites; Spec-015's SA-32 / SA-36 / Core-SDK text carries the paired spelling, recorded in its Amended row.

- **2026-08-11 — chat-start amendment (ADR-027 pairing); `approved → review`.** Workflows become chat-invocable through three callers of the existing `workflow.runStart` — the intercepted `/workflow start <name>` command, the composer affordance, and the `workflow_run` session callback tool (the corpus's first concrete `SessionCallbackTool`) — specified in [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization) under SA-38/SA-39/C-18 and governed by ADR-027, which lands `proposed` in the amendment PR. The plan grew three tasks (T5.8 composer surface, T5.9 callback tool, T5.10 start-path authorization), two invariants (I-015-17 interception totality, I-015-18 per-start named-action adjudication), two obligations (CP-015-7 against the Plan-004 callback-tool host; CP-015-8 against Plan-010 for the `Action::"workflow::start"` schema entry and the chat-borne principal carrier), a Tier-5 Plan-010 entry in `## Upstream-Tier Amendments Required`, Implementation Step 10, and two scoped §Preconditions boxes — the ADR-027 ratification box holding T5.8–T5.10, and the carrier box scoping the fail-closed chat-borne rule while holding no task. No new wire method: the eleven-method registry is unchanged, `WorkflowRunStartRequest` gains additive-optional `channelId` (ADR-018), and `workflow.start_denied` registers in `error-contracts.md §Workflow` as the first landed entry of the A-015-15 owed extension. Unlike the Tier-7 audit's flip-and-restore, this growth's restoring targeted coverage does not ride the same swap: the plan and spec flip `review` here and the paired Spec-015/Plan-015 targeted readiness-audit delta (the PR #307 → #308 two-PR shape) restores both, so the README census moves with each leg.

- **2026-08-11 — chat-start targeted readiness-audit delta (PR #320); `review → approved`.** The restoring re-audit the chat-start amendment (PR #319) queued, run over the amendment's growth only. G4 traceability: every T5.8–T5.10 `Spec coverage:` cite resolves to [Spec-015 §Chat-Invoked Start and Start Authorization](../specs/015-workflow-authoring-and-execution.md#chat-invoked-start-and-start-authorization)'s SA-38/SA-39/C-18 headings and every `Verifies invariant:` cite to I-015-17/I-015-18, each invariant carrying at least one verifying task — negative-controlled by perturbing one cite and watching the corpus oracle name it (`I-015-99 is not declared`, census 712 → 711) before restoring. Contract coherence: the C-18 four-leg grammar consistent across the spec, ADR-027, I-015-17, and T5.8's tests; the SA-39 principal-first-then-channel-visibility ordering and the invisible-channel ≡ nonexistent-channel collapse identical across SA-39, I-015-18, T5.10, ADR-027's success criteria, and the `error-contracts.md` row; `channelId` additive-optional per ADR-018 at every surface; the eleven-method registry unmoved; the callback tool consistent with the `SessionCallbackTool` shape with its channel binding daemon-derived everywhere it is described. Cross-plan: the CP-015-7/CP-015-8 rows present in the dependency map with the owed-delta note; one-writer holds — this plan authors no Cedar schema entry, no host code, no carrier, and no authorization semantics. Fail-closed honesty: the ADR-027 box holds T5.8–T5.10 and the carrier box holds no task, scoping the chat-borne fail-closed rule only. **Zero content findings on the growth. Three README sites the amendment PR left without the flip's record were completed in this delta:** the corpus plan- and spec-census bullets (whose counts — 19 `approved` / 7 `review` and 23 / 5 — never moved with the flip, so they were stale during the one-PR review window and stand correct again at this restore; the flip-and-restore records are appended to their Plan-015/Spec-015 narratives) and the §Phase header's re-opened-delta enumeration; the §Build Order paragraph, the one site the amendment PR did move (to eight `review`), returns to seven here as the restore leg's own census move. Both Statuses restored `approved` and the paired-spec and audit boxes re-checked; the ADR-027 ratification box and the carrier box stay open by design — ratification and the Tier-5 Plan-010 delta are the surviving gates, holding T5.8–T5.10 dispatch and chat-borne starts respectively.

- **2026-08-11 — A-015-07 upstream leg discharged (PR #321).** The Plan-014/Spec-014 restoring targeted readiness-audit delta landed both legs the `OWN-channel turn budget provider registered` box was waiting on — `ChannelConfig.turnsPerAgent` (the OWN-channel budget provider; Plan-014 D-014-23, T1.1 contract + T2.6 enforcement) and the fourth `InterruptReason` member `workflow_phase_cancelled` (the SA-9 `REQUEST_CANCEL`/`TERMINATE` cascade's mechanism; [Spec-014 §Stop Conditions](../specs/014-multi-agent-channels-and-orchestration.md#stop-conditions)) — and that box is checked with its record. CP-015-5's resolution and the `## Upstream-Tier Amendments Required` Tier-5 row carry the landed records; Phase 3 now rides its remaining gates (Phase 2; the Tier-3 registration delta; tier order), with Phases 4–5 still chained behind it. This plan authored neither leg (one-writer): both live on Plan-014's surfaces, named here and consumed at T3.1.
- **2026-08-11 — PR #321 Codex round 2: discharge bookkeeping trued up (no-flip).** Two drift corrections against the A-015-07 discharge recorded above, whose content had already landed: Spec-015's settled `turns_per_agent` entry moved §Open Questions → §Resolved Questions and V1 Scope Decisions (with the §Open Questions intro and §Preconditions censuses re-derived to the two remaining open entries), and the §Implementation Steps step-3 SA-9 cancel-policy tail — still claiming the `InterruptReason` member unlanded — corrected to the landed `workflow_phase_cancelled` member and the checked-box record. Drift-correction only: no contract content moves, the Status stays `approved`, and the README plan census is unchanged.

- **2026-08-16 — workflow-hardening amendment; `approved → review`, restored in the same swap.** A workflow run's park becomes a first-class, durable, explicable state rather than a failure or a retry, specified in [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40), `§Frozen-definition repair (SA-41)`, `§Park integrity and cancellability (SA-42)`, and `§Engine event record (SA-43)` with C-19. The plan grew six tasks — T1.9 (contract, payload, and column surface), T2.5 (typed recognition, the immediate park, and the attention fold), T2.6 (the durable schedule and boot sweep), T5.11 (cancellability), T5.12 (the frozen-definition repair), T5.13 (the always-on engine record) — six invariants (I-015-19 typed-only recognition, I-015-20 durable-schedule discipline, I-015-21 park cancellability and record preservation, I-015-22 engine record always-on and non-canonical, I-015-23 the attention fold is never a lock, I-015-24 explicit audited repair), two obligations (CP-015-9 against Plan-018 for the diagnostic bucket — a **same-tier** consumption, not an upstream-tier amendment, since Plan-018 is a Tier-7 sibling; CP-015-10 against Plan-004 for the typed usage-limit signal, which **is** a sealed Tier-3 amendment and joins `## Upstream-Tier Amendments Required`), Implementation Step 11, and two born-unchecked scoped §Preconditions boxes with Gate-5 entries on Phase 2 and Phase 5 respectively (re-homed to the Phase 2B and 5C supplements at the 2026-08-17 Codex round below). **Censuses deliberately unmoved:** four projection columns land on existing Plan-015-owned tables (two at this entry's writing; consolidated onto `workflow_phase_states` alone at the Codex round below) so the nine-table schema is unchanged; the park rides `workflow.phase_suspended` with a widened `reason` union and the repair rides an additive-optional `workflow.resumed` member, both additive-MINOR under ADR-018, so the 23-type / five-category taxonomy is unchanged; and no wire operation is minted, so the eleven-method registry is unchanged — the operator control surface for cancel, resume, and re-pin stays the residual SA-42 records, carried forward into `## Risks And Blockers` rather than silently assumed. Two censuses **do** move and were re-derived rather than carried: the A-015-15 refusal-point count fifteen → eighteen (correcting the chat-start amendment's unrecorded `workflow.start_denied`, plus the two SA-41 repair refusals), and the non-security invariant count eleven → seventeen. **Deferred rather than scoped:** cross-run campaign memory and the durable wake-coalescing delivery substrate, criterion-gated to V1.1 under the three named criteria of [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals), mirrored as a `## Non-Goals` row here that points at those criteria rather than restating them (one-writer) and states what this plan does build instead — the in-run attention fold and its projection column, which cross no aggregate boundary. Under the runbook's Status Flip Rule the growth flips this plan `approved → review`; the restoring targeted readiness-audit delta rides **this same swap** (below), the 2026-08-10 flip-and-restore-in-one-swap shape rather than the 2026-08-11 two-PR shape, so the README plan-status census does not move.
- **2026-08-16 — workflow-hardening targeted readiness-audit delta (in-swap); `review → approved`.** The restoring re-audit, run over the amendment's growth only and riding its own vehicle (the Plan-005 PR #278 self-audit shape). **G4 traceability:** every new task's `Spec coverage:` marker resolves to a heading that exists in the amended Spec-015 — `§Park integrity and cancellability (SA-42)`, `§Workflow Timeline Integration`, `§Provider-limit pacing and durable resumption (SA-40)`, `§Loud-errors discipline (C-12)`, `§Truth vs projection vs ephemeral (SA-25)`, `§Frozen-definition repair (SA-41)`, `§Fallback Behavior`, `§Engine event record (SA-43)` — every `Verifies invariant:` id resolves to a row this plan declares, and each of I-015-19…I-015-24 carries at least one verifying task (T2.5, T2.6, T5.11, T5.13, T2.5, T5.12 respectively). **Seeded-refutation negative control, executed and passed on the amendment's own content:** T2.6's marker line — a line this amendment authored — was perturbed to `I-015-20, I-015-98` and the plan-execution preflight cite oracle named it exactly, `Phase 2 [invariant-undeclared] T2.6: I-015-98 is not declared by Plan-015 (its ## Invariants block declares 24: …)`, exit 1; the seed was reverted and the same run returned exit 0. The plausible-but-wrong twin the control is aimed at is the rule most likely to be mis-stated on this surface — that the auto-resume timer re-arms by parsing the provider's message text on each retry — and the amendment states the opposite at every site: recognition is typed-signal-only with no text arm to disable (C-19, I-015-19), and the schedule re-arms from its own durable row after projection rebuild (I-015-20). Recorded honestly: the docs-corpus pre-commit runner returned exit 0 on the seeded state — that runner extracts `:NNN` locator cites and does **not** cover this class, so preflight is the oracle for it and a clean runner pass is not evidence here. An earlier control on the pre-amendment surface (T1.6 perturbed to `I-015-99`) fired the same way. **Claim-versus-content over this PR's own diff:** the two payload growths are additive-MINOR at every surface that describes them (the plan's transport section, the spec's event-type list, T1.9's back-compat tests); the four columns sit on tables the SA-25 split already calls projections, and every one is carried by the event that produces it, so I-015-10's byte-equal rebuild needs no exception; the fail-closed state is stated identically in the Plan-004 box, CP-015-10, the upstream-tier row, and the spec's Resolved-Questions decision. **A grounding defect was found and fixed by this delta rather than shipped:** the amendment's first draft armed the schedule on `windowSource: 'provider_reported'`, reusing a Spec-004 field that stamps the provenance of a **context-window headroom** reading — a different fact from a spent usage allowance. The arming gate now requires the new carrier's own provenance stamp, adopting the Spec-004 stamping _discipline_ without borrowing its field, and is independently corroborated by [Spec-005 §Usage Telemetry (usage_telemetry)](../specs/005-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)'s corpus-wide rule that any costly or irreversible action keyed on headroom requires provider-reported provenance. **Marker-line integrity, audited in both directions:** all six new `Spec coverage:` lines are unbackticked in the `Spec-NNN §Heading` form Gate 4 parses, and no `Verifies invariant:` line carries an undeclared id — the direction the preflight oracle checks. The **reverse** direction, which no gate checks, was audited by hand and found two mismatches, both repaired here rather than shipped: I-015-20's Verified-by column and the non-security census sentence both named `T5.1` alongside `T2.6`, but T5.1's marker declares only I-015-7 / I-015-10 / I-015-11 and its Tests assert nothing about the schedule — T5.1 was dropped from both sites, leaving T2.6 (whose own Tests already assert the post-rebuild re-arm) as the single verifier; and the security battery's Phase column claimed an I-015-4 arm at T5.13 while T5.13's marker declared only I-015-22 — `I-015-4` was added to that marker and `T5.13` to I-015-4's Verified-by column, since T5.13's Tests already carry the canary-secret and canary-prompt assertions. A coverage claim with no marker behind it is invisible to Gate 4 by construction, so this direction is a hand-audit obligation, not a tooling one. **Census arithmetic, re-derived from Status headers rather than recalled:** net-unchanged at 27 plans / 28 specs `approved`, because both flips restore in this swap. Zero content findings on the growth beyond the grounding repair recorded above. Both Statuses stay `approved` and the audit and paired-spec boxes are re-checked; the two new carrier boxes stay open by design — the Plan-004 delta and the Plan-018 bucket registration are the surviving gates, holding T2.5/T2.6 and T5.13 respectively.
- **2026-08-17 — amendment reciprocals landed, and the growth hardened in place (same swap).** The reciprocal corpus edits the 2026-08-16 amendment owed landed on its own vehicle rather than being left to the ship pass. `local-sqlite-schema.md §Workflow Tables` now carries the four park-and-resume columns, the two partial indexes, and the structural half of SA-40's clear-on-exit rule (as first landed: `auto_resume_at` / `park_attention_key` on `workflow_runs` with a terminal-status CHECK — re-homed onto `workflow_phase_states` with the stronger per-row phase-exit CHECK at the Codex round below) — **the local SQLite table census does not move: 55 tables before and after**, because all four columns land on tables that already exist and the engine event record is a file-backed diagnostic stream rather than a table (a storage-shape prejudgment corrected at the round-2 entry below: the tier's existing buckets are SQLite tables, and the fifth bucket's shape is Plan-018's decision at its registration amendment). The A-015-15 refusal census was re-derived from its own enumeration rather than carried forward, moving `api-payload-contracts.md` and `error-contracts.md §Workflow` off the pre-chat-start `fifteen` and this plan's `## API And Transport Changes` and `## Risks And Blockers` rows off `eighteen` to a consistent **nineteen** across all four numeric sites, with the two plan rows enumerating the three SA-41 refusals identically. The 2026-08-16 `Amended` row's `fifteen → eighteen` reading stands as the record of that day's diff and is marked in place rather than rewritten. Spec-015 carries the same refusal set in prose at four further sites with no number attached — the §Fallback Behavior bullet, the acceptance criterion, the SA-41 rule itself, and the §Resolved Questions ratification record — and all four were swept to the same three refusals in this pass, closing the two-versus-three split a reader diffing the spec's prose against the plan's census would otherwise have found. **Claim-versus-content on the reciprocals:** the DDL doc's bolded nine-table claim was scoped to the visual-builder amendment alone and would have gone stale silently as this amendment's columns landed beside it — it is now re-derived to cover every amendment since, and states where the always-on record lives instead of leaving a reader to infer a tenth table. **One internal-coherence defect found and fixed rather than shipped:** [Spec-015 §Frozen-definition repair (SA-41)](../specs/015-workflow-authoring-and-execution.md#frozen-definition-repair-sa-41) bounded the re-pin only by _parked_, while [Spec-015 §Provider-limit pacing and durable resumption (SA-40)](../specs/015-workflow-authoring-and-execution.md#provider-limit-pacing-and-durable-resumption-sa-40) states that a usage-limit resume continues the same attempt on the same `phaseRunId` — together, the two sentences licensed swapping a definition under units already dispatched from the frozen bytes, which explains neither their inputs nor their outputs. The fix is SA-41's sixth rule: a re-pin on any resume that continues an in-flight attempt refuses, naming the explicit fresh-entry action that would discard the attempt. It is a defect provable from this spec alone, and it is independently corroborated — the campaign's surveyed reference runtime shipped the unbounded form, watched an operator resume a parked run twice onto a prompt they had already fixed, and then added the same boundary with the same refusal-toward-fresh-entry shape. Four further rules were folded in from the same survey of that runtime's park machinery: the failed-fire-is-not-an-exit rule and the undecodable-value quarantine (both T2.6), the dispatch-intent / outcome pairing that makes silence diagnostic (T5.13), and the attention fold's fail-open direction — an uncomputable correlation surfaces the run's own record and never reverts the refusal to a retry, which moves SA-40's fold constraints two → three (T2.5) — plus the provider-event-delivery-path deadlock constraint now recorded under `## Risks And Blockers`, a shape that runtime hit twice in independent places. None of the five adds a task, an invariant, an obligation, or a capability, so the amendment's in-swap targeted coverage extends over all of them.
- **2026-08-17 — Codex round 1 adjudicated into the amendment (same vehicle).** Seven findings, all accepted, each folded into normative content rather than catalogued. **(1) The park schedule is per-phase state.** `auto_resume_at` and `park_attention_key` move from `workflow_runs` to `workflow_phase_states`: under SA-32 two parallel branches of one run can park concurrently against different provider accounts with different reset boundaries, and a run-level value could hold only one of them — delaying or stranding the sibling and folding two distinct outages into one record. The move **strengthens** the DDL: the run-level terminal-status CHECK, which had to permit the armed-while-a-sibling-runs case, is replaced by the per-row `state = 'suspended' OR (auto_resume_at IS NULL AND park_attention_key IS NULL)`, making the phase-exit half of SA-40's rule 5 structural (SQLite CHECKs are per-statement, so the transition out of `suspended` and the null-out are one statement), with the run-cancel half code-enforced in the cancel's unit of work and failure-injection-tested at T5.11 — whose preservation claim is now scoped honestly to `state` plus the record columns, since the live columns clear. The two partial indexes follow the columns (`idx_workflow_phase_states_auto_resume`, `idx_workflow_phase_states_park_attention`); the SQLite census stays 55 and the nine-table claim is untouched. **(2) The fold key is a function of the log.** `workflow.phase_suspended` carries an optional `parkAttentionKey` member beside `parkCause` and `autoResumeAt`, so I-015-10's byte-equal rebuild reconstructs the projected key with no special case (I-015-23 amended; T1.9 and T2.5 carry the member and its rebuild tests; still additive-MINOR, the taxonomy unchanged). **(3) The three SA-41 repair refusals are registered codes.** `workflow.repair_not_parked`, `workflow.repair_attempt_in_flight`, and `workflow.repair_version_unaccountable` land in `error-contracts.md §Workflow` as 409s with refusal-is-total semantics — the contract doc is in this vehicle's write set, so the registration is contract work taken now rather than deferred — moving the A-015-15 surface to four-of-nineteen landed and clearing T5.12's typed-refusal dependency; the blocker now covers the remaining fifteen. **(4) A carrier box holds exactly the tasks that read its surface.** A Gate-5 `precondition_box_checked` entry gates its whole declaring phase — there is no task-level leg — so the typed-signal box on Phase 2 held T2.1–T2.4 and the bucket box on Phase 5 held all thirteen Phase-5 tasks, contradicting the plan's own only-the-park-tasks claims. T2.5/T2.6 move to the explicit-label supplement **Phase 2B** (typed-signal box + Phase-2 chain), T5.11/T5.12 to **Phase 5B** (no box; Phase-5 and Phase-2B chains, the two seams they extend), and T5.13 to **Phase 5C** (bucket box + Phase-2B chain, independent of Phase 5) — the Plan-007 Phase 2B / Plan-022 Phase 3B shape; task ids, marker lines, and the invariant matrix are unchanged. The same round corrected the ADR-026/ADR-027 box narratives to state their mechanical breadth honestly: as Gate-5 entries they hold their full declaring phases until the ADR promotions land — an accepted, short-lived breadth, since both promotions are same-repo governance PRs sequenced ahead of any Tier-7 code dispatch, unlike the carrier boxes' other-plan deltas with no landing date. **(5) The engine-record cost fallback is bounded recording, never no recording.** The Rollback bullet's unqualified degrade-to-no-op reading contradicted I-015-22's no-disable-path posture; it now sheds high-frequency classes (ready-set ticks, gate evaluations) while retaining parks, fires, refusals, and intent/outcome pairs, with the no-op reserved for bucket unavailability. **(6) Criterion 2's measurement claim was half false.** SA-43 records engine decisions, never client reads, so "an operator inspected the parked runs individually" is operator-attested, not machine-derived; [Spec-015 §Non-Goals](../specs/015-workflow-authoring-and-execution.md#non-goals) criterion 2 now states which half is which and drops the no-new-instrumentation claim. **(7) I-015-24 carries SA-41's fresh-entry boundary** — the invariant row had named two of the three refusal grounds; the continues-in-flight-attempt refusal is now in the row T5.12 verifies. All seven stay inside the ratified scope — no new capability, no census move beyond the already-recorded refusal count — so the in-swap targeted coverage extends over the round.
- **2026-08-17 — Codex round 2 adjudicated into the amendment (same vehicle).** Ten findings; nine folded into normative content, one adjudicated as already-governed with its rationale on the thread. **(1) The recovery surface has a named gate.** SA-42's recorded residual — no run-cancel / run-resume / re-pin wire operation and no run-cancelled lifecycle type — is now tracked as [BL-151](../archive/backlog-archive.md) (P1), and the spec fixes its structural constraint: the operation set and the run-cancelled event mint **together**, because a durable cancellation the log cannot reconstruct would let a projection rebuild replay the last suspension payload and resurrect a cancelled run (SA-25). T5.11 states that no shipped path durably cancels event-lessly (no producer of `cancelled` exists until BL-151 lands) and the `## Risks And Blockers` residual bullet points at the item. **(2) SA-41's fresh-entry boundary is run-wide.** The pin is run-level and SA-32 parallel branches park independently, so a re-pin admitted through one branch's fresh entry would still swap the definition under a parked sibling's in-flight attempt — SA-41's rule, I-015-24, T5.12 (with a new two-branch refusal fixture), the `error-contracts.md` `workflow.repair_attempt_in_flight` row, the contract-doc enumeration, the spec's §Fallback Behavior bullet and acceptance criterion, and the Done checklist now all refuse while **any** of the run's attempts is in flight. The same refusal point widened, not a new one, so the A-015-15 census holds at nineteen. **(3) The detach-then-write crash interval is closed by reproducibility, not an inbox.** A daemon dying between the delivery-path detach and the off-path park write loses only an in-memory signal: the restart's ordinary mid-phase resume re-dispatches the same attempt and either receives a fresh typed refusal and parks then, or proceeds where the account reset — T2.5 gains the crash-injection arm pinning both directions, and the `## Risks And Blockers` bullet states why no durable inbox is owed. **(4) `waiting-human` producers owe the cause.** T3.2 now writes a bounded `parkCause` through the T1.9 helper on every new suspension, tested separately from legacy-replay acceptance — the schema-optional member exists for pre-growth replay, never as a producer license. **(5) `waiting_on_pool` cancels too.** T5.11 gains the fixture for the waiting state that carries no `park_reason`, so a cancel dispatch spelled over the `park_reason` union alone fails rather than silently skipping pool-waiting runs (SA-42's domain is every suspended or waiting state). **(6) The bucket registration is an amendment, not a row.** CP-015-9 now states that the Plan-018 tier's bucket set is closed at four, contract-tested against unknown names, and SQLite-table-backed — so the fifth bucket's storage shape and any census move are decided at the Spec-018/Spec-020/Plan-018 amendment that checks the box; the schema doc's "file-backed stream" prejudgment is corrected there and marked in place at the earlier entry above. **(7) The contract mirror carries all seven codes.** `api-payload-contracts.md` §Plan-015 moves off its stale "four codes" reading to the seven-code census, naming the three `workflow.repair_*` registrations and the four-of-nineteen landed state. **(8) The SA-41 justification stands on reasoning, not an unverifiable incident.** The normative fresh-entry rule's dated survey anecdote is replaced by the first-principles failure-mode statement (dated Progress Log records keep their day's narrative — they record provenance, not normative force). **Adjudicated with a reply rather than an edit:** Phase 5B's `external_plan_phase_merged 17/2B` chain stands — T5.11/T5.12 EXTEND `park-scheduler.ts`, a file Phase 2B creates, so the seam dependency is structural rather than removable, and the operator-reachability gate for cancellation and repair is BL-151, not the 2B chain. No new capability and no census move, so the in-swap targeted coverage extends over this round as well.
- **2026-08-18 — park-surface and operator-controls amendment; `approved → review`, restored in the same swap.** The workflow-hardening amendment left the park **durable but neither legible nor reachable**: its four columns lived per-phase on `workflow_phase_states` with no wire surface, and the `cancelled` run status it made reachable as an engine rule had carried no named producer since the day it was declared. This amendment closes both, in the shape the residual itself fixed in advance. **Legibility (SA-44).** `PhaseState` gains `parkReason`, `parkCause`, `autoResumeAt`, and `parkAttentionKey` as four additive-**optional** members, pre-licensed by that interface's own additive-optional evolution rule under ADR-018, so a parked run renders from one `workflow.runRead` with no timeline replay. They are **live, not record** — emitted iff the phase is parked at response-build time — which makes `parkReason` presence the wire's park discriminator and is why the five-value `PhaseState.state` union is deliberately **not** widened: a sixth arm would move the I-015-12 lockstep pin and force a DDL, contract, and taxonomy edit to express what an optional member already answers, and would leave two encodings of the same fact free to drift. The accepted consequence is stated rather than left silent: a finished or cancelled run shows no park members even where the record columns still hold what SA-42 preserves, and _why was this parked once_ stays a timeline question. **Reachability (SA-45).** Two operations, not three — `workflow.runCancel` and `workflow.runResume`, with the SA-41 re-pin carried as an optional member of the resume request, since SA-41 defines the repair only as an action on a resume and a separate operation would admit the re-pin-without-resume shape that section refuses. `workflow.cancelled` mints **together** with them (the SA-25 constraint BL-151 recorded), and the round's one substantive hardening made that pairing verifiable: the projector rule was authored **run-scoped and unconditional** — applying the event clears every phase row's four live park fields — after the draft's engine-only spelling would have left I-015-25 quantifying over a rebuild the event alone could not produce, and after a payload-enumerated alternative was rejected for adding a way to be wrong when SA-32 parallel siblings park concurrently. Authorization is two named Cedar operation actions on the SA-39 `Action::"workflow::start"` precedent — separately grantable, because start creates work, cancel destroys it, and resume commits further spend — refusing through one `workflow.control_denied` that names which action was denied. **Censuses re-derived, not carried:** registry eleven → thirteen; taxonomy 23 → 24 with categories unmoved at five; A-015-15 nineteen → twenty-two points with seven carrying codes and the owed extension **unmoved at fifteen**, because all three new points ship with their codes in the same diff; non-security invariants seventeen → nineteen; cross-plan obligations ten → eleven with the upstream-row count unmoved at four, CP-015-11 naming none: its two Cedar actions are registered by this amendment's own Spec-010 leg — [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration extended in place, Spec-010 flip-and-restored `approved` in the same swap, Plan-010 deliberately unflipped (no plan-side enumeration, no task, no behavior change) — so registration and consumer land together, T5.14 is held by nothing, and the CP-015-8 box discharged the same day stays checked and **unwidened** rather than reopened to cover actions it never held. The nine-table schema is unchanged — no table, no column, no index. **Governance leg.** ADR-026 and ADR-027 were promoted `proposed → accepted`, each read in full with its antithesis checked as genuinely steel-manned and its synthesis recorded, its Pre-Implementation Checklist discharged (build-time rows left open and annotated with the owning task rather than checked falsely), and both swept against `docs/archive/superpowers/plans/` and `docs/backlog.md` for scheduled amendments before the flip — none found. Their two born-unchecked `## Preconditions` boxes are checked, releasing the full-phase Gate-5 holds on Phases 1 and 5. **Coherence repairs.** Three defects closed rather than carried: the Phase-1 precondition's narrow "for T1.7 and T1.8 only" reading, which contradicted both the box's own narrative and the Phase-5 precondition's identical phase-wide statement; the two ADR boxes' pointer crediting `## Rollout Order` with sequencing the promotion, when that section's five items map 1:1 onto the code phases and name no governance PR; and the node's omission of the ADR-narrative half of its own round-1 fold. BL-151 is `completed` and archived, and every `[BL-151](../backlog.md)` link in the corpus repointed to the archive in the same diff.
- **2026-08-18 — CP-015-8 upstream leg discharged; recorded no-flip.** The Tier-5 Plan-010 delta this plan has waited on since the chat-start amendment landed as the Spec-010/Plan-010 admitting-principal carrier amendment with its in-swap targeted readiness-audit delta, delivering all three legs of CP-015-8's threefold scope: the Cedar schema registration and the action attribution together at [Spec-010 §Implementation Notes](../specs/010-approvals-permissions-and-trust-boundaries.md#implementation-notes)' named-operation-action enumeration — one site serves both roles in that spec — extended in place on its existing line to carry `Action::"workflow::start"` on the target workflow per [Spec-015 §Start authorization (SA-39)](../specs/015-workflow-authoring-and-execution.md#start-authorization-sa-39), and the admitting-principal-carrier **class** registered in `api-payload-contracts.md §Authenticated Principal And Authorization Model` with three named instances (the shipped `approval_resolutions.approver_id`, the new `interventions.admitting_principal_id`, and this plan's chat-borne workflow start). The `Chat-borne user carrier + workflow::start action registered` §Preconditions box is checked, `CP-015-8`'s Resolution carries the record, the §Upstream-Tier Amendments Required Tier-5 row is annotated landed, and the `## Cross-Plan Obligations` census annotation gains its discharge date — the count of **four** sealed-tier deltas is unchanged, since a discharged row stays a member of that set exactly as `CP-015-5` did after 2026-08-11. The return-cite this row was owed arrives as Plan-010's `CP-010-12`, which names the discharge in its body rather than minting a further CP number. **No Status flip** — this plan's diff is discharge bookkeeping only: no task, invariant, CP row, or behavior is authored here, and Plan-015 still writes no Cedar schema entry, no policy, and no carrier column. It instantiates its own principal column against the now-registered class when the chat-start surface lands (cross-plan-dependencies.md one-writer), which the class registration turns into a transcription rather than a design decision. The same disposition the 2026-08-11 `A-015-07` discharge took on this plan (PR #321, recorded there as a no-flip and verified against that commit's zero Status-line changes). T5.8–T5.10 stay gated on the ADR-027 box, untouched by this discharge; no census moves.
- **2026-08-18 — CP-015-10 upstream leg discharged; recorded no-flip.** The Tier-3 Plan-004 delta this plan has waited on since the workflow-hardening amendment landed as the session-cost-receipt + provider-account amendment with its in-swap combined Spec-004/Plan-004 targeted readiness-audit delta, delivering exactly the carrier CP-015-10 named: [Spec-004 §Fallback Behavior](../specs/004-provider-driver-contract-and-capabilities.md#fallback-behavior) mints the typed provider usage-limit signal as a **sibling axis beside** `RecoveryCondition` rather than a widening of it — the shape this plan specified and the `RecoverySpanClassification` precedent it cited — carrying a closed-set cause and an optional reset boundary that stamps its own provenance, with recognition typed-only and an unrecognized shape yielding **no** signal; [Plan-004 §Phase 3B — Provider-account seam + typed usage-limit signal](./004-provider-driver-contract-and-capabilities.md#phase-3b--provider-account-seam--typed-usage-limit-signal) builds it at T3.16 under `I-004-6`, registered as `CP-004-8`. `RecoveryCondition` is unwidened, as the box required. The `Typed provider usage-limit signal registered` §Preconditions box is checked, `CP-015-10`'s Resolution carries the record, and the §Upstream-Tier Amendments Required Tier-3 row is annotated landed — the count of **four** sealed-tier deltas is unchanged, a discharged row staying a member of that set on the same reading the CP-015-8 discharge above took. T2.5 and T2.6 are released and Phase 2B is dispatch-eligible on its remaining gates. **No Status flip** — this plan's diff is discharge bookkeeping and landed-fact restatement only: no task, invariant, CP row, or behavior is authored here, and Plan-015 still mints no driver vocabulary, consuming a signal Plan-004 owns. The stale pre-landing vocabulary is restated rather than deleted — the Tier-3 row's "no such signal is shipped" clause is put in the past tense, and the §Pitfalls entry is rewritten to survive the carrier's arrival, because the temptation it guards against changes shape rather than disappearing: the signal is silent on shapes a driver cannot name, and that silence means _not known to be limited_, never _known not to be limited_. The same disposition the CP-015-8 discharge took, and verified against this commit's zero Status-line changes. T5.8–T5.10 and the Plan-018 diagnostic-bucket box are untouched; no census moves.

## Done Checklist

Named completion conditions rather than the template defaults (A-015-23). Each is checkable against the tree, not against a reviewer's impression.

- [ ] The thirteen-table migration is applied and the contract ↔ DDL conformance suite (T1.6) is green on every pinned domain
- [ ] Every `workflow.*` event type Spec-015 lists discriminates in the shared `SessionEventSchema` union against its Spec-005 registry entry (T1.3)
- [ ] All four phase types and all four gate types execute end to end, including the `automated` path the pre-audit CHECK constraint rejected
- [ ] `sidekicks workflow verify-gate-chain <run_id>` ships and reports the first divergent `sequence` on a tampered chain (T5.2)
- [ ] The SA-31 replay battery is green — no `DeterminismViolationError`, `deepEqual` final state, and at least one replay-correctness test per event type (T5.1)
- [ ] The full I-015-1…I-015-7 security-regression battery with its CVE corpora gates merge (SA-30)
- [ ] Every §Cross-Plan Obligation is either satisfied with a return-cite from its provider plan or explicitly staged
- [ ] The visual builder ships and refuses all eight graph shapes at edit time while the daemon re-evaluates every one of them at `workflow.definitionCreate` (T1.8, T5.5; I-015-16), a definition round-trips builder → file → CLI → daemon to an identical content hash (T5.6, T5.7), and no tool binding in any persisted definition carries a governance facet (I-015-14)
- [ ] The chat-start surfaces ship under ADR-027: every `/workflow` verb intercepts and never composes into a provider turn on any path, and a slash word the console does not own is forwarded or sent as prose rather than refused (T5.8; I-015-17), the sixteen `workflow_*` callback tools register born-withheld and answer every invocation (T5.9), and every start path adjudicates the named action with the ordered role-then-channel refusal arms (T5.10; I-015-18)
- [ ] The park-and-resume surface ships: a typed usage-limit refusal parks without spending an attempt or minting a `phaseRunId` and never from message text (T2.5; I-015-19), a durable `auto_resume_at` is armed only on a provider-reported boundary and survives a daemon restart via a post-rebuild re-arm (T2.6; I-015-20), every parked run cancels from every waiting state with its record intact (T5.11; I-015-21), a frozen-definition re-pin is explicit, audited on `workflow.resumed`, and refused at all three of its refusal points — a run that is not parked, a repair requested while any of the run's attempts is still in flight (the resuming phase continuing one rather than entering fresh, or a parked parallel sibling holding one), and a target version that cannot account for the phases the run already completed (T5.12; I-015-24), and the engine event record is on for every run with no enable flag and read by no correctness path (T5.13; I-015-22)
- [ ] Related docs updated — what remains owed is the `error-contracts.md §Workflow` code registration under A-015-15 (twenty-two refusal points, seven codes landed: `workflow.start_denied`, the three `workflow.repair_*` codes the workflow-hardening review round registered, and the three SA-45 operator-control codes the park-surface amendment registered) and the T1.9 event-payload additions on the contract doc, which land with the Tier-3 Spec-005 registration leg above. The SQLite schema doc's four park-and-resume columns (all on `workflow_phase_states` after the review round's per-phase re-home), their two partial indexes, and the per-row phase-exit CHECK landed with the workflow-hardening amendment itself, as did that amendment's refusal-census re-derivation across the contract doc and `error-contracts.md`

## References

- [Spec-015: Workflow Authoring And Execution](../specs/015-workflow-authoring-and-execution.md) — paired spec; canonical SA-1…SA-43 narrative (SA-32…SA-37 and C-17 added by the visual-builder amendment; SA-38/SA-39/C-18 by the chat-start amendment; SA-40…SA-43 and C-19 by the workflow-hardening amendment)
- [ADR-015: V1 Feature Scope Definition](../decisions/015-v1-feature-scope-definition.md) — Decision D1/D2 + V1.1 criterion-gated commitments + §Research Conducted (BL-097 primary-source corpus)
- [ADR-012: Cedar Approval Policy Engine](../decisions/012-cedar-approval-policy-engine.md) — `human-approval` gate and per-turn moderation-gate evaluation path (I-015-3; added by the Tier-7 audit)
- [ADR-017: Shared Event-Sourcing Scope](../decisions/017-shared-event-sourcing-scope.md) — the locally-authoritative `session_events` log the `workflow.*` event types and the SA-26 dual anchor ride (I-015-7; added by the Tier-7 audit)
- [ADR-026: Visual Node-Graph Workflow Authoring](../decisions/026-visual-node-graph-workflow-authoring.md) — the node-graph authoring decision governing T1.8 / T5.5 / T5.6 / T5.7 and I-015-14…I-015-16 (`proposed`; added by the visual-builder amendment)
- [ADR-027: Chat-Invoked Workflow Start and Start Authorization](../decisions/027-chat-invoked-workflow-start.md) — the chat-start and start-authorization decision governing T5.8 / T5.9 / T5.10 and I-015-17…I-015-18 (`proposed`; added by the chat-start amendment)
- [Local SQLite Schema §Workflow Tables](../architecture/schemas/local-sqlite-schema.md#workflow-tables-plan-015) — the workflow schema, hash-chain layout, write-amplification estimates
- [Plan-005: Session Event Taxonomy and Audit Log](./005-session-event-taxonomy-and-audit-log.md) — event taxonomy + integrity protocol
- [Plan-010: Approvals, Permissions, and Trust Boundaries](./010-approvals-permissions-and-trust-boundaries.md) — Cedar policy + approval categories
- [Plan-012: Artifacts, Files, and Attachments](./012-artifacts-files-and-attachments.md) — artifact manifests, OWASP upload pipeline
- [Plan-013: Persistence, Recovery and Replay](./013-persistence-recovery-and-replay.md) — single writer worker, `ProjectionRebuild`, replay
- [Plan-014: Multi-Agent Channels and Orchestration](./014-multi-agent-channels-and-orchestration.md) — channel lifecycle, OWN ownership
- [Plan-004: Provider Driver Contract and Capabilities](./004-provider-driver-contract-and-capabilities.md) — the callback-tool host (CP-015-7) and the owed typed usage-limit signal (CP-015-10; added by the workflow-hardening amendment)
- [Plan-018: Observability and Failure Recovery](./018-observability-and-failure-recovery.md) — owner of the bounded-retention diagnostic bucket the SA-43 engine event record lands on (CP-015-9; added by the workflow-hardening amendment)
- [Spec-018: Observability and Failure Recovery](../specs/018-observability-and-failure-recovery.md) — the four-bucket diagnostic tier, its ≤ 7-day TTL, and the `retention_policy_override` warning that bound the engine record (I-015-22)
- [Spec-020: Data Retention and GDPR](../specs/020-data-retention-and-gdpr.md) — the shred fan-out whose Path 3 the engine record's bucket joins through Spec-018
- [CloudEvents v1.0.2 spec](https://github.com/cloudevents/spec/blob/v1.0.2/cloudevents/spec.md) — envelope additive-bump rules (SA-18)
- [OpenTelemetry Semantic Conventions for Events](https://github.com/open-telemetry/semantic-conventions/blob/main/docs/general/events.md) — event-name convention precedent (SA-19)
- [OpenTelemetry AI Agent observability blog (2025)](https://opentelemetry.io/blog/2025/ai-agent-observability/) — LLM-event semantic-convention rationale (SA-19)
- [Argo Workflows architecture — workflow events](https://argo-workflows.readthedocs.io/en/latest/architecture/#workflow-engine) — event-engine industry comparison
- [n8n executions API reference](https://docs.n8n.io/api/api-reference/#tag/Execution) — execution-event industry comparison
- [Argo Workflows — intermediate parameters](https://argo-workflows.readthedocs.io/en/latest/intermediate-inputs/) — human-phase form input pattern
- [Argo Workflows — `suspend-template-outputs.yaml` example](https://github.com/argoproj/argo-workflows/blob/main/examples/suspend-template-outputs.yaml) — output-projection-on-resume pattern
- [argoproj/argo-workflows#8365](https://github.com/argoproj/argo-workflows/discussions/8365) — form-input UX gap (Argo discussion)
- [Camunda 8 — user tasks](https://docs.camunda.io/docs/components/modeler/bpmn/user-tasks/) — human-phase claim-semantics precedent
- [Camunda 8 — handling data in processes](https://docs.camunda.io/docs/components/best-practices/development/handling-data-in-processes/) — form-data persistence pattern
- [GitHub Actions — reviewing deployments](https://docs.github.com/en/actions/managing-workflow-runs-and-deployments/managing-deployments/reviewing-deployments) — approval-gate UX precedent
- [AWS Step Functions — human-approval tutorial](https://docs.aws.amazon.com/step-functions/latest/dg/sample-project-human-approval.html) — approval-gate sample
- [AWS Step Functions — `SendTaskHeartbeat`](https://docs.aws.amazon.com/step-functions/latest/apireference/API_SendTaskHeartbeat.html) — heartbeat-based liveness pattern
- [Temporal — Python message passing](https://docs.temporal.io/develop/python/message-passing) — signal-based human input
- [Temporal — automation of human-in-the-loop workflows](https://pages.temporal.io/webinar-automation-of-human-in-the-loop-workflows-with-temporal.html) — HITL workflow pattern
- [Cloudflare Workflows — `waitForEvent`](https://developers.cloudflare.com/workflows/build/events-and-parameters/) — wait-for-event primitive
- [LangGraph — human-in-the-loop](https://langchain-ai.github.io/langgraph/concepts/human_in_the_loop/) — HITL primitive (LLM stack)
- [Microsoft Agent Framework — AG-UI HITL](https://learn.microsoft.com/en-us/agent-framework/integrations/ag-ui/human-in-the-loop) — HITL primitive (recent industry)
- [W3C WCAG 2.2 §3.3.7 Redundant Entry](https://www.w3.org/TR/WCAG22/#redundant-entry) — accessibility requirement for form-state UX (SA-26)
- [OWASP File Upload Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html) — I6 human-upload minimums (also inline in Test table)
- [Restate — What is Durable Execution](https://restate.dev/what-is-durable-execution) — per-invocation durable-log precedent (C-13)
- [Temporal — custom persistence (2024)](https://temporal.io/blog/higher-throughput-and-lower-latency-temporal-clouds-custom-persistence-layer) — persistence-model precedent
- [Argo Workflows — workflow archive](https://argo-workflows.readthedocs.io/en/latest/workflow-archive/) — persistence-tier precedent
- [Argo Workflows — offloading large workflows](https://argo-workflows.readthedocs.io/en/latest/offloading-large-workflows/) — large-workflow persistence pattern
- [Cadence — cross-DC replication / persistence](https://cadenceworkflow.io/docs/concepts/cross-dc-replication) — persistence-tier industry comparison
- [SQLite — JSON1 extension](https://www.sqlite.org/json1.html) — JSON-column rationale for `workflow_definitions`
- [`fast-check` (model-based testing)](https://github.com/dubzzz/fast-check) — property-test framework pin (SA-29)
- [Jazzer.js (fuzzing)](https://github.com/CodeIntelligenceTesting/jazzer.js) — fuzz-test framework pin (SA-29)
- [Jazzer.js — fuzz-targets docs](https://github.com/CodeIntelligenceTesting/jazzer.js/blob/main/docs/fuzz-targets.md) — fuzz-target shape (SA-29)
- [Endor Labs — Argo CVE-2025-66626 broken-fix analysis](https://www.endorlabs.com/learn/when-a-broken-fix-leads-to-rce-how-we-found-cve-2025-66626-in-argo) — broken-fix-precedent rationale for security-regression category
- [Astronomer — testing Airflow](https://www.astronomer.io/docs/learn/testing-airflow/) — DAG-test precedent
- [Bitovi — replay testing in Temporal](https://www.bitovi.com/blog/replay-testing-to-avoid-non-determinism-in-temporal-workflows) — replay-test pattern (SA-31)
- [Temporal — TypeScript SDK testing suite](https://docs.temporal.io/develop/typescript/testing-suite) — `runReplayHistory` contract (SA-31; also inline)
