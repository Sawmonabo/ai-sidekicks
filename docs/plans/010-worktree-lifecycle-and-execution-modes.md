# Plan-010: Worktree Lifecycle And Execution Modes

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `010` |
| **Slug** | `worktree-lifecycle-and-execution-modes` |
| **Date** | `2026-04-14` (Tier-6 readiness audit 2026-06-10) |
| **Author(s)** | `Codex` |
| **Spec** | [Spec-010: Worktree Lifecycle And Execution Modes](../specs/010-worktree-lifecycle-and-execution-modes.md) |
| **Required ADRs** | [ADR-004](../decisions/004-sqlite-local-state-and-postgres-control-plane.md), [ADR-006](../decisions/006-worktree-first-execution-mode.md), [ADR-015](../decisions/015-v1-feature-scope-definition.md) |
| **Dependencies** | [Plan-009](./009-repo-attachment-and-workspace-binding.md) (workspace infrastructure — canonical repo/workspace contract types per CP-009-1, reprovision primitives per CP-009-2, `assertWritable` per CP-009-3, `markBusy`/`releaseBusy` per CP-009-7, `repo_mounts`/`workspaces` tables), [Plan-001](./001-shared-session-core.md) (daemon SQLite substrate — migration-runner guarded-block seam + `openDatabase`, shipped Tier 1; `SessionIdSchema`), [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (Tier 4 — Plan-010 registers the 5 `worktree.*` event types and consumes `EventLogService.append` + the T1.2 type registry; build-order precondition for Phases 1–2), [Plan-007](./007-local-ipc-and-daemon-control.md)-partial (Tier 1 — `MethodRegistry` + SDK `JsonRpcClient`; Phase-3 registration gated on BL-142 + BL-143), [Plan-005](./005-provider-driver-contract-and-capabilities.md) (Tier 4 — the `RunId` brand from `packages/contracts/src/provider-driver.ts`, an `import type` carrying run provenance on worktree records at T1.2 and read back at T3.2; erased at emit but load-bearing for `tsc`, so it is a build-order dependency — Phase 1's `preconditions:` block gates it on the merged revision, PR #157. Added 2026-07-26 by PR #254: the edge has been traced at task level since the Tier-6 audit but had not propagated to this row), [Plan-004](./004-queue-steer-pause-resume.md) (Tier 5 — `RunSetupGate` seam per Plan-004 T3.10 / CP-004-8 + `QueueItemCreateRequest.workspaceId?`), [Plan-023](./023-desktop-shell-and-renderer.md)-partial (Tier 1 — renderer substrate + `window.sidekicks` bridge stub; live IPC at Tier 8) |
| **Cross-Plan Deps** | [Cross-Plan Dependency Graph](../architecture/cross-plan-dependencies.md) |

## Goal

Implement the four-mode repo execution contract with worktree-first writable defaults plus the lifecycle management around worktree preparation, reuse, retirement, and ephemeral clone preparation.

## Scope

This plan covers execution mode selection, read-only and branch gating, worktree creation, ephemeral clone preparation, reuse validation, lifecycle projection, run-setup execution-root binding, and fallback handling.

## Non-Goals

- PR preparation
- Diff attribution logic
- Non-repo directory execution semantics
- Automatic repository setup-script execution during execution-root preparation

## Preconditions

- [x] Paired spec is approved — re-checked 2026-07-18 by the campaign Task-28 / W1.5 batch spec re-promotion (supersedes the 2026-07-13 re-open note): Spec-010 returned to `approved`; its campaign amendment window closed. **Re-opened 2026-08-09, scoped to the sparse-root capture-closure amendment (PR #307):** the Spec-010 capture bullet's parked sparse-root residual became the normative sparse-aware-staging closure contract, flipping Spec-010 `review` under the runbook's spec-amendment rule; this box tracks the spec's restore by the queued targeted readiness-audit delta — see the 2026-08-09 sparse-root capture-closure §Notes entry. **Delivered 2026-08-09:** the sparse-closure targeted readiness-audit delta (PR #308; §6 node NS-51) ran adversarially over the amendment diff as merged (squash `9b2cea9`), with the three codex rounds' and the pre-merge verification pass's dispositions as input and a seeded-refutation negative control passed. One actionable finding — the restore-mutation claim left unswept after round 3's intent-to-add index drop, plus that drop's unstated failure attribution against the frozen restore-step vocabulary — and five polish findings were amended in the same swap, with six completeness legs stated (the §Notes entry carries the full record). Spec-010 restored `review → approved` (closing PR #308, the promotion vehicle this box named). **Re-opened 2026-08-10, scoped to the boundary-obstruction naming amendment (PR #311):** the same capture bullet gains three Required Behavior legs it did not carry — the pre-mutation boundary-obstruction refusal, the kind-preserving trailer with its kind-scoped delete-exemption widths, and the byte-exact boundary subtraction — flipping Spec-010 `review` again under the runbook's spec-amendment rule; this box tracks that spec's restore by the queued targeted readiness-audit delta, exactly as it tracked the 2026-08-09 one — see the 2026-08-10 boundary-obstruction §Notes entry. **Delivered 2026-08-10:** the boundary-obstruction targeted readiness-audit delta (PR #313; §6 node NS-53) re-audited the naming amendment adversarially over its merged diff, with the amendment PR's eight review dispositions and the parallel audit's findings fed as input and a seeded-refutation negative control passed, and restored Spec-010 `review → approved` in the same swap as its one finding's amendment — the promotion vehicle this box named. The spec's 2026-08-10 amendment banner carries the same dated restoration; §Notes carries the full record. **Re-opened 2026-08-17, scoped to the branch-mode bound-checkout correction (PR #340):** Plan-009's common-directory repo-identity amendment corrected `Spec-010 §Turn-Boundary Snapshots`' branch-mode sentence — "the shared main checkout" named the wrong tree for a worktree-bound workspace under the new keying — flipping Spec-010 `review` under the runbook's spec-amendment rule. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-63 targeted readiness-audit delta's neighbor-swap record (the PR #321/#323 shape — no separate promotion vehicle); the sentence correction renames which tree branch mode roots at without changing snapshot mechanics or restore semantics, and Spec-010's 2026-08-17 amendment blockquote plus this plan's §Progress Log PR #340 entry carry the record. **Re-opened 2026-08-17 (second same-day), scoped to the carried-findings adjudication (§6 node NS-65):** the adjudication amendment grows `Spec-010 §Turn-Boundary Snapshots`' applicability bullet (capture/restore execution-root normalization, CP-009-10 ⇄ CP-010-15) and re-scopes the §Resolved Questions branch-mode sentence to the bound checkout, flipping Spec-010 `review` under the runbook's spec-amendment rule. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-65 targeted readiness-audit delta's neighbor-swap record — Spec-010's 2026-08-17 carried-findings blockquote and Plan-009's §Progress Log adjudication entry carry the record. **Re-opened 2026-08-17 (third same-day), scoped to the normalized-root transport closure (§6 node NS-69):** NS-65 left CP-010-15's durable-value requirement and CP-010-12's pure-callee prohibition with no mechanism connecting them — restore was required to consume a value persisted on the `workspaces` row that the callee was forbidden to read — so this amendment grows `Spec-010 §Turn-Boundary Snapshots`' applicability bullet and the §State And Data Implications tenancy rule with the caller-supplied transport and the node-scoped grounding of the checkout-root comparison (the owning-mount join form since the PR's round-1 fold), flipping Spec-010 `review` under the runbook's spec-amendment rule. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-69 targeted readiness-audit delta's neighbor-swap record (the NS-63 / NS-65 in-swap shape — no separate promotion vehicle); the spec's third 2026-08-17 amendment blockquote and the §Notes normalized-root-transport entry below carry the record.
- [x] Required ADRs are accepted
- [x] Blocking open questions are resolved or explicitly deferred
- [x] **Plan-readiness audit complete per [runbook](../operations/plan-implementation-readiness-audit-runbook.md)** — Tier-6 audit (2026-06-10, 67 findings adjudicated; the Phase 1–4 `#### Tasks` structure, §Invariants I-010-1..20, §Cross-Plan Obligations, and §Ratified Design Decisions authored; reciprocal amendments in Plan-004/Plan-009/Spec-009/Spec-010 landed in the same audit PR). **Delivered 2026-08-04:** the D3/D4 rollup-confirmation delta (closing PR #293; §6 node NS-40) discharging the **2026-07-26** re-opening that PR #254 fired by adding a Plan-005 edge to the Plan-010 dependency row of [`cross-plan-dependencies.md` §3](../architecture/cross-plan-dependencies.md#3-inter-plan-dependency-graph) — every body `Consumes:` clause and every consume-direction §Cross-Plan Obligations row swept against the plan-header `Dependencies` row (**D3**) and the §3 row (**D4**): 8 provider plans resolved, 7 present and correctly typed in both surfaces, **zero missing propagations**, Plan-002 adjudicated correctly absent, and five body clauses trued up (one harness misattribution in its three forms, plus two under-declared consumes). Status stays `approved`. Prior re-opening, now closed: 2026-07-19 scoped to the new **Phase 5** (turn-snapshot service, campaign B22), which post-dated the Tier-6 audit's Phase 1–4 coverage — delivered 2026-07-20 by Plan-010's W2.5 targeted re-audit (#220), the promotion vehicle that also returned Status to `approved`. **Re-opened 2026-08-09, scoped to the sparse-root capture-closure amendment (PR #307):** the amendment adds Phase 6 / T6.1 and invariant I-010-24 (Status Flip Rule rows 3–4), post-dating every prior audit's coverage; its restoring targeted readiness-audit delta — run adversarially over the amendment diff — lands as the follow-on delta PR, minting the §6 audit node and returning Status to `approved`. §Notes carries each delta's findings. **Delivered 2026-08-09:** the restoring delta (PR #308; §6 node NS-51) audited the amendment's growth — Phase 6 / T6.1 / I-010-24 — over the merged diff: G4 traceability re-verified (T6.1's Spec-coverage and Verifies-invariant cites resolve, I-010-24 matching T6.1's contract after the same-swap repairs), spec/plan mechanism parity swept (detection, oracle, seeding, trailer + transport, exemption + pre-drop, observed-subset diagnostic each owned by both surfaces with a Tests-row arm), one actionable and five polish findings amended in-swap with six completeness legs stated, and Status restored `review → approved` on this plan and Spec-010 in the same swap (the flip-and-restore shape this row's precedents name). **Re-opened 2026-08-10, scoped to the boundary-obstruction naming amendment (PR #311):** I-010-24's restore half and the T6.1 contract gain behavior no prior audit's text carried — the pre-mutation typed refusal at `derive-enumerations` over the three measured destructive shapes (`160000` gitlink displacers exempt from the oracle, two shapes deliberately admitted), the kind-scoped delete-exemption widths with their over-protection residual, and the byte-exact boundary subtraction — a Status Flip Rule row-4 flip (a behavior change in the plan body; no new phase, task, invariant, CP row, or Required ADR is minted, and the shipped code is unchanged, this amendment naming what PR #309 already ships). Its restoring targeted readiness-audit delta — run adversarially over this amendment's diff — lands as the follow-on delta PR, minting the §6 audit node and returning Status to `approved` on both files; §Notes carries each delta's findings. **Delivered 2026-08-10:** the restoring delta (PR #313; §6 node NS-53) audited that growth over the merged amendment diff against the shipped code at `develop` `9d3b93b` — every mechanism sentence of I-010-24's restore half, the T6.1 contract, the Spec-010 capture bullet and its banner checked claim-by-claim against `packages/runtime-daemon/src/git/turn-snapshot-service.ts#TurnSnapshotService` (the three width predicates and the one they share, the `160000` displacer carve at the oracle rather than at the refusal, the frozen result unions and restore-step vocabulary, the `latin1` byte-record trailer and its byte-exact subtraction), all twelve quoted suite titles byte-compared against the merged suite, both added `git-ls-files` anchors resolved, and the plan / spec / README / §6 census arithmetic re-derived from the per-file Status headers — with the amendment PR's eight review dispositions and the parallel audit's findings fed as input and a seeded-refutation negative control passed. Zero critical, zero major, zero actionable; one polish finding amended in the same swap (I-010-24's obstruction clause read as though the frozen `partial_restore` arm carried the refusal detail, which rides the restore diagnostic channel instead), and the corpus's forward-looking restoring-delta clauses discharged where this delta satisfies them. Status restored `review → approved` on this plan and Spec-010 in the same swap — the flip-and-restore shape this row's precedents name. **Re-opened 2026-08-17, scoped to CP-010-14 (PR #340):** registering the branch-mode root consent row — a new CP entry, Status Flip Rule row 3 — post-dates every prior audit's coverage, flipping this plan `review`. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-63 targeted readiness-audit delta's neighbor-swap record (the PR #321/#323 shape — no separate delta PR is queued): the row's consumed code executes under Plan-009's audited Phase 2B and is held atomic with the identity swap by Plan-009's carrier box, so the audit surface for the code change is Plan-009's; this plan's §Progress Log PR #340 entry carries the adjudication. **Re-opened 2026-08-17 (second same-day), scoped to CP-010-15 and the I-010-6 / D-010-9 bound-checkout rewrite (§6 node NS-65):** the carried-findings adjudication registers the turn-snapshot normalization consent row (Status Flip Rule row 3) and rewrites the branch-mode immutability scope (row 4 — a plan-body behavior change), post-dating every prior audit's coverage and flipping this plan `review`. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-65 delta's neighbor-swap record: both legs' consumed code executes under Plan-009's audited Phase 2B (T-009-2B-4), held atomic by Plan-009's carrier box, so the audit surface for the code change remains Plan-009's; Plan-009's §Progress Log adjudication entry carries the per-finding dispositions. **Re-opened 2026-08-17 (third same-day), scoped to the normalized-root transport closure (§6 node NS-69):** closing the seam NS-65 left between CP-010-15 and CP-010-12 grows the plan body — `run_execution_contexts.checkout_root` added to D-010-5 with its own Phase-3 table-rebuild migration, T1.3's migration lineage stated, T3.2 grown with the column's capture and its migration files, CP-010-12's caller-resolved enumeration grown to seven inputs, CP-010-15's consent text reconciled to the five-link carrier chain, I-010-14 and I-010-24 amended, and T5.1 / T5.2's callee signatures grown — a Status Flip Rule **row 4** flip (a plan-body behavior change: no phase, task, invariant, CP row, or Required ADR is minted, and no table is added or removed), flipping this plan `review`. **Delivered 2026-08-17:** restored `review → approved` in the SAME diff by the NS-69 targeted readiness-audit delta's neighbor-swap record. The delta's own findings, stated here because this row is the audit record: the column is NOT NULL and unconditional across all four modes, justified in D-010-5 against its mode-agnostic consumers (CP-010-4 tenancy, I-010-14) rather than left to a reviewer to query; the migration is scheduled with its only writer's phase and cannot ship earlier without a populating writer or later without a missing column, with the `NOT NULL`-without-default form admissible only because no production writer of the table has ever shipped — verified against the engine the daemon bundles (SQLite 3.53.0 via `better-sqlite3` 12.9.0: the add succeeds cleanly on an empty table and is refused outright on a populated one, so the precondition fails closed in the engine rather than in prose); the shipped `0004-worktree-lifecycle.ts` is explicitly NOT amended, T1.3 carrying that lineage so the schema doc's block is not misread as version 4's content; the shipped `migration-shape.test.ts` pin is named in T3.2's Files as growing in the same PR rather than being left to drift — it asserts the **exact ordered** column list, so the ALTER breaks it by construction, and the column arrives **last** in CID order rather than in the logical position the schema doc shows it in, a divergence recorded in both surfaces on the `session_events.retention_class` precedent so no implementer reconciles it by re-ordering the doc or rebuilding the table; the I-010-2 contract↔DDL tripwire was checked and is **not** affected (it extracts enum vocabularies and predicate CHECKs from the shipped migration SQL, and this column carries neither, being deliberately outside the mode-conditional CHECK — so no contracts change is implied and none is authored); and the pre-amendment `canonical_root` fallback is stated at the capture site, without which the NOT NULL column would be unsatisfiable for every workspace row predating the `checkoutRoot` key. Two claims on touched surfaces were re-derived rather than carried forward: §Target Areas' `migrations/` entry (now two migrations, not one) and D-010-5's / §Data And Storage Changes' "verbatim migration content" claim (now true per migration, with the one rebuild-added column flagged in place — canonical-position, unlike the `session_events.retention_class` ALTER-appended arm). One cross-plan consequence was found and **reconciled in the same diff rather than left to drift**: growing CP-010-12's input list leaves the caller-side enumerations in Plan-004 stating the pre-amendment field set (I-004-16's resolved-input clause, CP-004-11's supplied-input list, and T3.13's two literal signatures with its turn-boundary test clause), so the same swap restates them to the seven-input contract — a Plan-004 Status Flip Rule row-4 flip restored in-swap; Spec-004 joined the flip at the PR #346 round-2 fold (the vanished-checkout rootless convergence) — with the interim **code** window closed by Plan-004's new born-unchecked "Snapshot-callee checkout-root parameter shipped" box holding T3.13's dispatch until Plan-009 Phase 2B and this plan's Phase 3 have both merged (the CP-004-12 pattern in the code-gating direction; type-checking alone could not close it — the shipped T5.1 / T5.2 signatures stay five-field until the Phase-2B EXTEND, so a five-field caller compiles cleanly today and breaks only when the signature grows). Against **this** plan zero actionable findings remain open, and no reciprocal remains owed. **Round-1 fold (Codex PR #346, 2026-08-17):** the PR's findings against this plan's side are folded in place — the migration re-shaped from `ADD COLUMN` to the documented table-rebuild (T3.2, D-010-5, T1.3's lineage note, §Data And Storage Changes: the ALTER documentation refuses a NOT-NULL-without-default add unconditionally and the observed empty-table allowance is undocumented, so the References row now cites the lang_altertable rebuild procedure), CP-010-15's node-scoping restated as the query's real shape (`workspaces` carries no node column — the hold comparison joins `repo_mount_id` → `repo_mounts.node_id`, the D-009-7 owning-node column; Spec-010's checkout-tenancy sentence and Plan-009 T-009-2B-4's node-scope arm restated to match), and CP-010-12's reciprocal record updated to Plan-004's Phase-3B re-home, which breaks the CP-010-9 ⇄ checkout-root deadlock — this plan's Phase-3 `{ plan: 004, phase: 3 }` entry now resolves on a Plan-004 Phase 3 that carries no rollback-cluster box, and Plan-009 Phase 2B gains the reciprocal `{ plan: 010, phase: 3 }` gate its T-009-2B-4 fixture's `checkout_root` read requires.

Target paths below assume the canonical implementation topology defined in [Container Architecture](../architecture/container-architecture.md).

## Target Areas

- `packages/contracts/src/worktree.ts` (D-010-1 — renamed from the pre-audit `execution-mode.ts`; the file's dominant noun is the worktree/clone lifecycle domain)
- `packages/contracts/src/index.ts` + `packages/contracts/src/event.ts` (EXTEND — barrel + event-union registration)
- `packages/runtime-daemon/src/migrations/` (two guarded-block migrations: the Phase-1 four-table CREATE, then the Phase-3 `run_execution_contexts.checkout_root` table-rebuild migration that lands with T3.2)
- `packages/runtime-daemon/src/git/` (Plan-010-owned directory per cross-plan §2: `worktree-event-emitter.ts`, `worktree-service.ts`, `ephemeral-clone-service.ts`, `worktree-projector.ts`, `worktree-errors.ts`, `turn-snapshot-service.ts` + `__tests__/`)
- `packages/runtime-daemon/src/workspace/execution-root-service.ts` + `packages/runtime-daemon/src/workspace/execution-mode-service.ts` (the exactly-two Plan-010 files in Plan-009-owned `workspace/` per cross-plan §2)
- `packages/runtime-daemon/src/bootstrap/index.ts` (EXTEND — the one sanctioned T5.3 retention-sweeper wiring call, Plan-007-owned per cross-plan §2; a wiring call, not ownership)
- `packages/runtime-daemon/src/ipc/handlers/` (7 `repo.*` binder files + `index.ts` EXTEND)
- `packages/client-sdk/src/worktreeClient.ts` + `packages/client-sdk/src/index.ts` + `packages/client-sdk/test/worktreeClient.integration.test.ts`
- `apps/desktop/src/renderer/src/execution-mode-picker/` (`ExecutionModePicker.tsx`, `ModeSwitchOptions.tsx`, `WorktreeStatusView.tsx`, `WorktreeReuseCheckView.tsx`, `index.ts`, `__tests__/`)

## Data And Storage Changes

- Add local `worktrees`, `ephemeral_clones`, `branch_contexts`, and `run_execution_contexts` tables (D-010-5). No table is added or removed after that — the SQLite table census does not move again for this plan.
- Add `run_execution_contexts.checkout_root` (`TEXT NOT NULL`) — the durable normalized-root carrier CP-010-12's caller reads and CP-010-15's restore consumes (2026-08-17 amendment; D-010-5). **This column ships in its own later migration, not the four-table CREATE:** Phase 1 already shipped that migration as version 4 (`0004-worktree-lifecycle.ts`, PR #253), so the column lands by the documented table-rebuild migration — create the successor with `checkout_root TEXT NOT NULL` in canonical position, copy omitting the column, drop, rename, recreate the index (T3.2's migration paragraph carries the steps; the References row cites the procedure) — in the next-ordinal guarded block belonging to **Phase 3**, the first phase with a writer for it (T3.2's run-setup gate). Scheduling it earlier would ship a NOT NULL column no code can populate; scheduling it later would let T3.2 write a column that does not exist. A column add is not a table add, so no table-census figure moves.
- See [Local SQLite Schema](../architecture/schemas/local-sqlite-schema.md) for column definitions — the audited DDL there is the table's final shape, and is the verbatim migration content for every column of the Phase-1 CREATE; `checkout_root` is the one column carried there ahead of its own Phase-3 table-rebuild migration, flagged as such in its comment (schema-doc order and post-rebuild `PRAGMA table_info` order identical — no `retention_class`-style CID-last divergence).

## API And Transport Changes

- Seven `repo.*` JSON-RPC methods (D-010-3): `repo.executionModeSelect`, `repo.executionRootPrepare`, `repo.worktreeReuseCheck`, `repo.ephemeralClonePrepare`, `repo.ephemeralCloneDispose`, `repo.worktreeRetire`, `repo.worktreeStatusRead` — wire contract in [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-010 + §Repo Method-Name Registry (Tier 6).
- Error vocabulary in [Error Contracts](../architecture/contracts/error-contracts.md) §Worktree + §Ephemeral Clone + §Workspace extension rows (D-010-4).

## Invariants

| ID | Invariant | Tasks |
| --- | --- | --- |
| **I-010-1** | Single-definition imports: `worktree.ts` imports `ExecutionMode`, `WorkspaceState`, branded `RepoMountId`/`WorkspaceId`, and the lifecycle-payload factory `buildRepoWorkspaceLifecyclePayloadSchema` from Plan-009's `repo.ts`; it never redefines them — `WorktreeLifecyclePayloadSchema` is that factory's `WorktreeStateSchema` instantiation, not a redefinition (CP-010-1; PR #250 round 4; import list trued up against the merged file 2026-07-26, PR #253 housekeeping — see §Notes). | T1.1 |
| **I-010-2** | Contract↔DDL lockstep: `WorktreeState`, `EphemeralCloneState`, and the cleanup-policy literal union are byte-identical between `worktree.ts` and the migration `CHECK` constraints — pinned by a conformance test. | T1.3, T1.4 |
| **I-010-3** | Provenance: every worktree row persists `created_by_session_id` (and `created_by_run_id` when run-created); retirement never erases provenance. | T1.3, T2.2 |
| **I-010-4** | Active-branch uniqueness: at most one live worktree (state NOT IN `retired`,`failed`) per (repo mount, branch) — the partial-unique index is the race arbiter; services treat its violation as the typed collision/retry signal, never a crash. | T1.3, T2.2 |
| **I-010-5** | Branch-context representability: every writable-mode run's branch context is persistable — worktree rows reference the worktree, clone rows the clone, branch-mode rows neither; the at-most-one CHECK holds. | T1.3, T2.4 |
| **I-010-6** | The BOUND checkout is never mutated: no Plan-010 code path checks out, creates, switches, or merges branches inside the checkout a `branch`-mode workspace bound — the mount's main checkout or a linked worktree, per `workspaces.metadata.boundRoot` — nor inside the mount's main checkout when the two differ; `branch` mode binds and verifies only (D-010-9; scope rewritten 2026-08-17 by the carried-findings adjudication — the main-checkout-only wording left the bound linked checkout formally unprotected once CP-010-14 re-based branch mode onto `boundRoot`). | T2.2, T2.4, T2.6 |
| **I-010-7** | No silent mode substitution: requested-mode unavailability is a typed refusal (`workspace.mode_unsupported` at select; `worktree.create_failed`/`clone.prepare_failed` at prepare) — never a substituted mode, never a fallback root. | T3.1, T2.4, T2.6 |
| **I-010-8** | Explicit reuse only: a worktree binds as a reuse target only via `reuseWorktreeId`; a dirty candidate additionally requires `acknowledgeDirtyCandidate`; an incompatible candidate never binds; no code path reuses implicitly (D-010-15). | T2.2, T2.4 |
| **I-010-9** | Recorded-then-cleaned: retire/dispose record the state transition and emit before any disk mutation; disk cleanup is asynchronous, idempotent, and stamped via `cleaned_at`. | T2.2, T2.3 |
| **I-010-10** | No repository-controlled code executes during provisioning: every git invocation neutralizes hooks (empty `core.hooksPath` plus `core.fsmonitor=false` via argv — the fsmonitor hook is config-named, outside `core.hooksPath`'s reach) and no setup script runs (D-010-10). | T2.2, T2.3, T2.6 |
| **I-010-11** | Workspace writes ride Plan-009 primitives exclusively (`beginReprovision`/`completeReprovision`/`failReprovision`/`markBusy`/`releaseBusy`/`assertWritable`); zero raw UPDATEs on the `workspaces` table from Plan-010 code (CP-010-2/3/4). | T2.4, T3.1, T3.2 |
| **I-010-12** | A `stale` workspace never receives an execution root: `assertWritable` precedes every writable-mode prepare that finds the workspace outside the open CP-010-2 bracket; a prepare that finds `provisioning` is the bracket's own provisioner and skips the gate together with `beginReprovision` — a `stale` workspace can never sit there, states being exclusive (PR #297). | T2.4, T3.2 |
| **I-010-13** | Exactly-once events: each worktree transition emits its D-010-12-mapped event exactly once, transactionally with the row write; `failed` deliberately emits none (pinned by a regression test); clone transitions emit none (D-010-11). | T2.1, T2.2 |
| **I-010-14** | Run gate: no repo-bound run reaches `running` without a resolved execution root, a `run_execution_contexts` row, and a busy-marked workspace; gate refusal parks the run in `starting` (D-010-16). The row is complete when written — `execution_root`, `checkout_root`, and `git_common_dir` are all captured in the gate's write, none deferred and none nullable, so every later consumer of the row reads a value the gate resolved while the tree existed (2026-08-17 amendment). | T3.2 |
| **I-010-15** | Daemon-side validation: every `repo.*` method validates its request schema and resolves every verdict daemon-side; SDK and renderer never compute cleanliness, compatibility, naming, or roots. | T3.3, T3.4, T3.6 |
| **I-010-16** | SDK marshals, never derives: `worktreeClient` methods are schema-validated pass-throughs; no retry-with-substitution, no client-side defaulting. | T3.5, T3.7 |
| **I-010-17** | One explicit user action → one mutation: the picker fires exactly one `repo.executionModeSelect` per switch and never client-sequences a prepare after it; reuse/retire affordances are click-explicit with no auto-proceed (D-010-14). | T4.1, T4.3 |
| **I-010-18** | The picker renders the daemon's mode set verbatim: options come from `repo.executionModeCapabilitiesRead` (`availableModes` + `restrictions`); no synthesized or hidden modes. | T4.1, T4.4 |
| **I-010-19** | Never-hide rows: status views render every row the status read returns — including `failed` and `retired` — with their states labeled (admit-not-eject posture). | T4.2, T4.4 |
| **I-010-20** | Views render daemon verdicts verbatim and derive nothing (no client-side expiry math, cleanliness inference, or root computation). | T4.1, T4.2, T4.3, T4.4 |
| **I-010-21** | Snapshot refs live only under `refs/sidekicks/runs/<runId>/epoch-<E>/turn-<N>` — never `refs/heads/`; they are invisible to branch history, PR preparation, and diff attribution by construction (Spec-011 no-impact). | T5.1, T5.2, T5.3 |
| **I-010-22** | Create-only, per-epoch snapshot refs: each ref is written with a compare-and-swap against ref absence, so a retried or duplicated capture of the same `(runId, epoch, turnOrdinal)` never repoints an existing ref (idempotent success returns the recorded OID); a post-rollback re-execution's identical turn ordinal mints a fresh ref under its own `epoch-<E>` segment. | T5.1 |
| **I-010-23** | Fail-closed restore, two-phase: the non-mutating `resolveRestoreTarget` refuses (typed refusal, no leg applied) unless the current `HEAD` equals the resolved snapshot's recorded first parent, and `restoreToTurn` re-verifies that precondition under the exclusive execution-root hold at execution time (TOCTOU guard) before any mutation — never an anti-diffed partial restore. A snapshot absent in its owning epoch's territory draws a typed no-snapshot refusal — never a silent parent-epoch fallthrough to a stale same-ordinal ref. A mid-sequence restore failure returns a convergent partial-restore result naming the failed step and carrying the collision-overwrite + divergent-gitlink enumerations for every effect applied before the failure (required, empty-when-none), never a silent success. | T5.2 |
| **I-010-24** | Sparse-faithful capture and restore: a snapshot captured in a sparse-checkout execution root stages exactly what porcelain `git add -A` stages there under the pipeline's pinned exclusion inputs (the in-tree-`.gitignore`-only divergence from host-local ignore sources stands unchanged) — the in-cone side re-stats over a scratch index seeded from the live index, out-of-cone cached entries persist at their live cached state (porcelain never updates entries outside the cone — a `git add --sparse`-staged entry survives capture, where a `<base>` seed was measured to erase it, and an out-of-cone unmerged entry fails `write-tree` typed exactly as porcelain refuses that index state, never a silently erased conflict), and out-of-cone untracked files are never added — their boundary-time path set, out-of-cone intent-to-add paths included, recorded on the snapshot commit's sparse-root trailer channel — the set derived by subtracting the written tree from the candidates on the **listing's own bytes**, never decoded-vs-decoded, so a decode-aliased tracked path can never subtract a distinct boundary path away and cost an intent-to-add entry the pre-drop below — and the trailer carries those same bytes, a `latin1` byte record holding one code point per path byte, which makes its sorted JSON **byte-lexicographic** by construction and a capture-minted key the key matched verbatim at restore, so every restore-side membership, exclusion, subtraction, and scoring comparison over the boundary set is byte-exact and `utf8` decoding survives only where a boundary path is composed for a human to read — written unconditionally in sparse roots (the empty set spells `[]`) as the closure's format marker, with a trailer-absent snapshot refused typed in a detected sparse restore root before any write (vintage undecidable — a pre-closure sparse capture's truncated tree is message-indistinguishable from a safe full-tree vintage) — with in-cone/out-of-cone decided by git's own sparsity matcher — `git sparse-checkout check-rules` over the repository's live sparse definition (pattern-keyed, never `skip-worktree`-bit-keyed; output means in-cone across cone and non-cone modes, negation patterns included; invoked only in a detected sparse root and fail-closed on invocation failure — git below the 2.41 plumbing floor included — through the existing typed capture-failure envelope, never an unpartitioned degrade; the sparse evaluation never conflated with the `.gitignore` exclusion pass), so the snapshot is a full tree and sparseness stays a checkout-time projection; restore re-applies the sparse definition with no new worktree-mutating pipeline step (its one preparation — dropping every live-index entry the recorded set covers at its **full recorded width**, a file entry's exact path and a directory entry's segment-boundary subtree alike, that the snapshot tree does not itself hold, intent-to-add and turn-staged stage-0 entries reached alike, ahead of the destructive checkout — is index-only, instantly superseded, and runs under the `derive-enumerations` step for failure attribution: one atomic `update-index` invocation through git's lock-file protocol, so a failed drop leaves index and worktree untouched and that step's failure-before-any-mutation reading stays true — naming the checkout step would falsely assert a destructive checkout was attempted — while the frozen union does not grow and the diagnostic detail names the drop, the multi-reachable `close-index` precedent), and ahead of that preparation and of every write a recorded boundary path the checkout itself would destroy draws a **typed pre-mutation failure** at `derive-enumerations` with nothing mutated, surfaced on the shipped `partial_restore` arm naming that step, the obstruction detail riding the restore diagnostic channel the frozen arm has no field for — a step failure inside the restore sequence, never a pre-dispatch refusal, the result unions staying frozen — the three destructive shapes the ignored-path collision taxonomy of `Spec-010 §Turn-Boundary Snapshots` measures, transferred whole because boundary paths meet the same `read-tree --reset -u` through the same mechanism: materializing snapshot paths strictly beneath a boundary FILE entry, a materializing blob exactly at a boundary DIRECTORY entry, and a materializing snapshot blob at a proper ancestor of an entry of either kind (each measured destructive at exit 0 on git 2.50.1, the third the shape a sparse root makes ordinary), with two shapes deliberately admitted (a directory entry's strict descendants, which the checkout merges into the preserved directory, and a conflicting snapshot path the live cone leaves unwritten); materialization is scored through the same fail-closed `check-rules` oracle in a sparse restore root and is unconditionally true in a non-sparse one, `160000` gitlink displacers exempt from that scoring because git never marks a gitlink `skip-worktree` (`ls-files -t` reports `H` for an out-of-cone one, measured) and the checkout creates its directory regardless, so scoring them would fail open on the destructive case; the refusal detail names every obstructing boundary path and bounds displacers past the first three to a count, and it rides the existing typed-failure envelope with the frozen unions gaining no arm — the disposition being the typed refusal rather than the ignored-path restore-wins-with-enumeration ruling, which is scoped to paths the snapshot contract excludes from the outset where recorded boundary paths are that contract's protected subject (a user-facing force-override is a possible future enhancement, deliberately not scheduled) — its untracked-delete pass exempts exactly the recorded boundary-time set at each entry's recorded **kind** width — **kind-exact** for a file entry, which matches only a slash-free listing entry at that exact path, so turn content beneath a boundary file path and a turn-created embedded repository standing at one are both still deleted; **spelling-tolerant across the segment-boundary subtree** for a trailing-slash directory entry, which exempts the restore listing's slash-free spelling of that directory as readily as its slashed one (a directory git refused to descend is one capture could not enumerate, so path identity for that entry is its subtree — the sibling skipped-embedded-repositories semantic, carried by a kind-preserving trailer that records `ls-files -o`'s trailing slash verbatim and decodes once into byte-keyed file and directory sets, fail-closed as before) — the residuals this pass carries all survivable or honestly refused: a turn-created file **inside** such a directory is over-protected, a turn-created **file** standing at a recorded directory path is exempted by that arm's equality case and likewise survives (the reverse alias of the first, deliberately retained), and a delete candidate whose decoded name does not re-encode to its recorded bytes refuses the **whole** delete pass before it mutates anything — `partial_restore` at `delete-untracked`, the worktree exactly as the checkout left it — rather than removing a path through a decode alias, the string-typed removal seam being able to refuse such a name honestly but not to address it; the one residual anywhere on this path that runs fail-**open** rather than protective — pre-existing rather than introduced by the obstruction guard — is that guard's presence probe, which addresses the filesystem at a boundary path's **decoded** spelling and is unreachable on any filesystem that rejects non-UTF-8 names (APFS does) — so boundary-time out-of-cone untracked content survives on disk while a turn-created out-of-cone untracked file elsewhere is deleted like its in-cone counterpart — and reports the observed discarded subset of materialized out-of-cone tracked paths on the diagnostic channel (the result unions stay frozen), never silently. A non-sparse root's pipeline is byte-identical to the pre-closure recipe. Capture and restore execution-root inputs are normalized to the enclosing working-tree top level at the snapshot service's entry (CP-010-15, the 2026-08-17 adjudication's consent leg, executed under Plan-009 T-009-2B-4) — capture's cwd-scoped `ls-files` legs and restore's whole-tree `read-tree --reset -u` thereby always address one tree extent, so a rollback from a capture taken in a nested execution root can never erase pre-snapshot state outside the nested prefix, and every staging, boundary, and exemption guarantee above quantifies over the whole working tree. That normalized root is **supplied, never discovered**: it arrives as the caller-resolved `checkoutRoot` parameter CP-010-12 enumerates, read from `run_execution_contexts.checkout_root` (2026-08-17 amendment) — capture, which runs while the tree exists, re-verifies the supplied value against the live tree and fails the capture typed on disagreement rather than silently re-deriving; restore, which may run after the turn deleted the nested execution root, consumes it without touching the filesystem to obtain it, which is the only reason the guarantee holds in the deleted-nested-root case at all. | T6.1 (the sparse closure); the execution-root normalization clause: Plan-009 T-009-2B-4 under CP-010-15, on the `checkout_root` carrier T3.2 captures — unshipped until Phase 2B lands |

## Cross-Plan Obligations

- **CP-010-1 — Import Plan-009's canonical repo/workspace contract types (reciprocal of Plan-009 CP-009-1).** `worktree.ts` imports `ExecutionMode`, `WorkspaceState`, branded `RepoMountId`/`WorkspaceId`, and the lifecycle-payload factory `buildRepoWorkspaceLifecyclePayloadSchema` from `packages/contracts/src/repo.ts` — never redefines; the worktree payload is the factory's `WorktreeStateSchema` instantiation, derived in `worktree.ts` (PR #250 round 4). The mount-state enum was struck from this list 2026-07-26 (PR #253 housekeeping — see §Notes): no Plan-010 wire shape or lifecycle payload carries a mount state, so that import never landed. Plan-009 still owns and exports the symbol; only the claim that Plan-010 imports it is retracted. **Direction:** Consume from [Plan-009](./009-repo-attachment-and-workspace-binding.md). **Tasks:** T1.1.
- **CP-010-2 — Workspace reprovision transitions ride Plan-009 primitives (reciprocal of CP-009-2).** `execution-root-service.ts` / `execution-mode-service.ts` drive `ready -> provisioning -> ready` exclusively through `beginReprovision` / `completeReprovision` / `failReprovision`. **Direction:** Consume from Plan-009. **Tasks:** T2.4, T3.1.
- **CP-010-3 — `assertWritable` precedes every writable-mode root prepare (reciprocal of CP-009-3).** Typed `workspace.stale` refusal per error-contracts.md §Workspace. Scoped to prepares that find the workspace outside the open CP-010-2 bracket: a prepare that finds `provisioning` is the bracket's own provisioner and skips the gate together with `beginReprovision` — Plan-009's gate raises its invariant error for `provisioning` by design, and a `stale` workspace can never sit there (PR #297). **Direction:** Consume from Plan-009. **Tasks:** T2.4, T3.2.
- **CP-010-4 — Busy bracket via `markBusy`/`releaseBusy` (reciprocal of CP-009-7).** The run-setup gate brackets every repo-bound run's root hold; one holding run at a time in V1; a concurrent hold refuses `workspace.busy`. The hold's exclusivity keys on the NORMALIZED checkout root per the amended `Spec-010 §State And Data Implications` rule (2026-08-17 adjudication round — two subdirectory-bound workspaces of one checkout can never hold concurrently, because CP-010-15's snapshot normalization gives every hold whole-tree capture/restore extent; the re-key executes at Plan-009 T-009-2B-4's run-boundary seam, this bracket's provider side). **Direction:** Consume from Plan-009. **Tasks:** T3.2; T2.3 consumes the refusal's carrier identity (`cleanupTick` leg (d) adjudicates skip-vs-abort on `instanceof WorkspaceBusyError`; PR #297).
- **CP-010-5 — Worktree event registration with Plan-006.** Phase 1 registers the 5 `worktree.*` types — `worktree.created`, `worktree.ready`, `worktree.dirty`, `worktree.merged`, `worktree.retired` (all in `Spec-006 §Repo, Workspace, and Worktree Lifecycle (session_lifecycle)`) — into the Plan-006-owned `SessionEventSchema` union as an additive-MINOR EXTEND, carrying `WorktreeLifecyclePayloadSchema` — Plan-009's family shape instantiated over `WorktreeStateSchema` via the exported `buildRepoWorkspaceLifecyclePayloadSchema` factory in `worktree.ts` (the shared shape already carries `worktreeId?`; a third `state` arm in `repo.ts` is refused — it would re-close the node-id.ts eager-cycle class and loosen every family's accept set; PR #250 round 4); type names + `session_lifecycle` category mapping ship in Plan-006 T1.2. Phase 2 consumes `EventLogService.append` as the sole append path. This plan adds no rows to the Spec-006 registry — no `worktree.failed`, no clone events (D-010-11). **Direction:** Extend [Plan-006](./006-session-event-taxonomy-and-audit-log.md) (additive). **Tasks:** T1.1, T2.1.
- **CP-010-6 — `branch_contexts` + `BranchContextId` provided forward to Plan-011.** Plan-010 creates the polymorphic carrier table and brand; Plan-011 extends it for PR/diff attribution (cross-plan §1 row). **Direction:** Provide to [Plan-011](./011-gitflow-pr-and-diff-attribution.md). **Tasks:** T1.1, T1.3, T2.4.
- **CP-010-7 — `packages/runtime-daemon/src/git/` directory creation.** Plan-010 creates and owns the subdirectory (cross-plan §2 git/ row); adjacent plans consume through contracts/services, never by adding files here. The subtree opened at Phase 1, not Phase 2: T1.4's `__tests__/contract-ddl-conformance.test.ts` is its first file (PR #253). **Direction:** Create. **Tasks:** T1.4, T2.1.
- **CP-010-8 — Seven `repo.*` methods registered under Plan-007's `MethodRegistry`.** Same namespace as Plan-009's six (one-namespace-per-domain-aggregate, D-010-3); registration gated on BL-142 (registry regex conformance) and wire-observable typed errors on BL-143 (`DaemonDomainError` projection). Plan-007 owns the registry surface, not these handlers. **Direction:** Register with [Plan-007](./007-local-ipc-and-daemon-control.md). **Tasks:** T3.3, T3.4, T3.6.
- **CP-010-9 — `RunSetupGate` registration (reciprocal of Plan-004 CP-004-8).** Plan-010's execution-root gate registers on the Plan-004 T3.10 seam (`assertRunReady` + `onRunTerminal`), consuming the `QueueItemCreateRequest.workspaceId?` run-binding carrier. **Direction:** Consume from [Plan-004](./004-queue-steer-pause-resume.md). **Tasks:** T3.2.
- **CP-010-10 — Plan-023 owns the `window.sidekicks` preload bridge the Phase 4 views project over.** Views are thin bridge projections (exported + barreled, not App-shell-mounted); live IPC dispatch lands at Plan-023 Tier 8 — until then bridge methods throw `NotImplementedAtTier1Error` and rejected/error branches are the production-observable paths (same posture as Plan-009 CP-009-6). **Direction:** Honor [Plan-023](./023-desktop-shell-and-renderer.md). **Tasks:** T4.1–T4.4.
- **CP-010-13 — Renderer boundary with Plan-009.** Bind-time intended-mode selection lives in Plan-009's `repo-attach/` views; run-time mode switching of an existing workspace lives here in `execution-mode-picker/` (cross-plan §2 renderer row). `ModeSwitchOptions` is this plan's own presentational component — no import from `../repo-attach/` (D-010-18). **Direction:** Honor Plan-009 boundary. **Tasks:** T4.1.
- **CP-010-11 — Worktree envelope roots + per-run execution binding for approval path-scope matching (Tier-6 Plan-012 walk).** `worktrees.fs_root` and the `run_execution_contexts` per-run binding (`run_id -> execution_root`, D-010-5) are what Plan-012's remembered-scope matcher resolves to evaluate path-category prefix containment for a repo-bound run; unknown provenance falls back to strict per-request approval (`Spec-012 §Fallback Behavior`). Plan-012 reads the binding + root only — never writes Plan-010-owned rows. Within-tier ordering: Plan-012 builds after Plan-010 (this binding is a precondition). **Direction:** Provide to [Plan-012](./012-approvals-permissions-and-trust-boundaries.md) (reciprocal CP-012-9). **Tasks:** T1.3 (tables), T3.2 (the gate writes the `run_execution_contexts` row).
- **CP-010-12 — Turn-snapshot production call-site + caller-resolved inputs (B9-owned; reciprocal of the Plan-004 `rollback` consume).** The Phase-5 turn-snapshot service's capture and restore legs are pure callees — they receive **seven** caller-resolved inputs: the execution root, the checkout root (the normalized enclosing working-tree top level CP-010-15 operates on, added 2026-08-17), run id, epoch, turn ordinal, execution `mode`, and (on restore) the `epochLineage` — as parameters, and never resolve them themselves (the T5.3 retention leg's `released_at` / `git_common_dir` reads are a separate concern, outside this obligation). Purity here is specifically the absence of a `workspaces` read from inside the callee: the normalized root the callee needs is caller-supplied precisely because a callee entitled to go find it would have to read workspace state, and on the deleted-nested-root path would have nothing left on disk to read it from. The production caller side is authored by the campaign's B9 bundle (Task 13) into the Plan-004 run engine: the per-turn-boundary `captureTurnSnapshot` call-site; execution-root resolution from the `run_execution_contexts` row (`run_id -> execution_root`, D-010-5); checkout-root supply from that same row's `checkout_root` column (one row read already in hand — the caller gains a field, not a query, and reads no `workspaces` row of its own); execution-`mode` supply (read from that same row); execution-epoch supply (`Spec-004 §Required Behavior` — 0 before any rollback, advanced with each accepted `run.rolled_back`, a confirmed conversation rewind regardless of file-leg disposition); `epochLineage` derivation (the ordered `{epoch, rewindBase}` list built from the run engine's durable epoch / intervention records — the input `resolveRestoreTarget` walks, T5.2); the rollback preflight -> conversation-leg -> bound-restore sequencing under exclusive execution-root tenancy (`Spec-004 §Required Behavior`); and the turn-boundary + rollback integration tests. Because the §5 build-order preamble orders Plan-004 rollback dispatch (the Phase-3B cluster) after Phase 5 (B23) ships, B23's callee-file-surface constraint holds by construction — B9 calls `git/turn-snapshot-service.ts`, never writes it (cross-plan §2 one-writer). **Reciprocal reconciliation on the Plan-004 caller side (2026-08-17 amendment — landed in the same diff).** Growing this obligation's input list would have left Plan-004's caller-side surfaces stating a superseded enumeration — I-004-16's resolved-input clause, CP-004-11's supplied-input list, and T3.13's two literal call signatures (`captureTurnSnapshot` / `resolveRestoreTarget`) together with its turn-boundary test clause all predated the checkout-root parameter — so the same amendment restates all of them to the seven-input contract (a Plan-004 Status Flip Rule row-4 flip, restored in-swap; Spec-004 joined the flip at the PR #346 round-2 fold). No Plan-004 doc surface is therefore ever stale, and the **code** window is closed mechanically rather than by type-checking: the shipped T5.1 / T5.2 signatures stay five-field until the Plan-009 Phase-2B CP-010-15 EXTEND and the `checkout_root` column lands only with this plan's Phase 3, while Plan-004 sits a tier earlier — a T3.13 caller authored today would compile against the five-field form and then be broken in-repo by the Phase-2B growth, in a PR with no `run-engine.ts` consent row — so Plan-004's born-unchecked "Snapshot-callee checkout-root parameter shipped" §Preconditions box (its Phase-3B Gate-5 entry — the rollback cluster re-homed to Plan-004's Phase-3B explicit-label supplement at the NS-69 PR's round-1 fold, because a Phase-3-scoped entry deadlocked against this plan's Phase-3 gate on Plan-004 Phase 3's T3.10 seam, CP-010-9; the CP-004-12 pattern in the code-gating direction) holds T3.13's dispatch until Plan-009 Phase 2B and Plan-010 Phase 3 have both merged. **Direction:** Provide to [Plan-004](./004-queue-steer-pause-resume.md) (reciprocal of the Plan-004 `rollback` consume, campaign B9). **Tasks:** T5.1, T5.2 (callee surface).
- **CP-010-14 — Branch-mode execution root re-based onto the workspace's bound working tree (reciprocal of Plan-009 CP-009-9; consent for the Phase-2B EXTEND).** This plan's shipped `#bindBranchMode` (the T2.4 surface in `execution-root-service.ts`) returns `mount.canonical_root` as the branch-mode execution root, citing `Spec-010 §Turn-Boundary Snapshots`' branch-mode sentence — both written when the canonical root WAS the tree the participant bound. Plan-009's common-directory keying (PR #340, NS-63) moves that column onto the MAIN checkout's root, under which the shipped return would relocate a linked-worktree workspace's branch-mode runs to the main checkout (the relocation Plan-009 I-009-15 names as a bug class). Consent: Plan-009 T-009-2B-4 EXTENDs `packages/runtime-daemon/src/workspace/execution-root-service.ts` INSIDE Phase 2B — atomic with the identity change, so neither half ships alone — re-rooting the branch arm at `workspaces.metadata.boundRoot` (fallback `canonical_root` when the key is absent, keeping pre-amendment rows byte-identical with no backfill migration) and pointing `#verifyBranchModeBind` at the same value; the Spec-010 branch-mode sentence is corrected in the same PR (Spec-010 and this plan flip-and-restored `approved` in-swap). This plan's code comments and tests that still say "shared main checkout" update in that Phase-2B PR, not before — until it lands, shipped behavior remains lawful under the shipped per-directory identity, and Plan-009's carrier box holds the pair. **Direction:** Provide consent to [Plan-009](./009-repo-attachment-and-workspace-binding.md) (reciprocal CP-009-9). **Tasks:** T2.4 (shipped surface; the EXTEND executes under Plan-009 T-009-2B-4).
- **CP-010-15 — Turn-snapshot capture/restore normalized to the enclosing working-tree top level (reciprocal of Plan-009 CP-009-10; consent for the Phase-2B EXTEND).** The Phase-5 snapshot service's capture legs (`git -C <executionRoot> ls-files …`) are cwd-scoped while restore's `read-tree --reset -u` updates the WHOLE working tree regardless of cwd, so once CP-010-14 makes a nested `boundRoot` a lawful branch-mode execution root, an un-normalized nested capture lists only the nested prefix and a later restore erases pre-snapshot state outside it — the rollback data-loss shape the 2026-08-17 carried-findings adjudication closes. Consent: Plan-009 T-009-2B-4 EXTENDs `packages/runtime-daemon/src/git/turn-snapshot-service.ts` INSIDE Phase 2B — shipped together with the CP-009-9/CP-010-14 leg, because the nested shape becomes reachable exactly when branch mode re-roots at `boundRoot` — normalizing BOTH capture and restore execution-root inputs to the enclosing working-tree top level at service entry (one normalization site, both directions, so capture and restore always operate on the same tree extent), with `Spec-010 §Turn-Boundary Snapshots`' applicability bullet recording the rule in the same swap. **Transport of the normalized root (the 2026-08-17 amendment's closure).** The value is derived while the tree exists and reaches the callee down a five-link carrier chain, each link a write the previous link's tree existence justifies: (1) bind-time derivation into `workspaces.metadata.checkoutRoot`; (2) rewrite of that key when provisioning installs a new execution root; (3) capture into `run_execution_contexts.checkout_root` by the T3.2 run-setup gate at context creation, deriving the top level from the just-materialized `execution_root` and healing the carrier when the workspace key is absent on a pre-amendment row (never `canonical_root` — the bind-time checkout, not a provisioned run's own tree; Codex PR #346 round 2) — the heal a **Plan-009-consented delegated write** of the Plan-009-owned metadata key, registered on CP-009-10's ownership sentence: one write path, the gate applying Plan-009's provisioning-completion rewrite rule at the handoff, never a second writer discipline; (4) supply by the B9 caller as the CP-010-12 `checkoutRoot` parameter, read from the same run-context row it already reads `execution_root` and `execution_mode` from; (5) receipt by the callee. Capture re-verifies the supplied value against the live tree (it runs while the tree exists) and fails typed on disagreement; restore consumes it as given, because by then the nested path it describes may be gone. Link (3) is what makes the chain durable rather than merely persistent: like `git_common_dir`, the value is snapshotted per run at context creation, so it survives both a later reprovision of the workspace row and the turn deleting the nested directory — restore is never asked to read a `workspaces` row, let alone a filesystem path, to find out where the tree top is. **CP-010-12's pure-callee contract is therefore unchanged in the strict sense, not merely the compatible one:** the callee gains an input, not a lookup — it reads no `workspaces` row, performs no upward filesystem walk to normalize, and holds no dependency on the execution root still existing; the growth is confined to CP-010-12's caller-resolved enumeration, which now names seven inputs. The normalization carries a concurrency corollary, closed in the adjudication PR's own review round: run tenancy re-keys on the same normalized root (`Spec-010 §State And Data Implications`; the CP-010-4 busy bracket), so no two active runs share one normalized checkout and whole-tree capture/restore never crosses another run's live state. That exclusivity is **node-scoped by the query's shape, not by assumption**: `workspaces` carries no node column, so the hold comparison reads the local daemon's `workspaces` rows joined through their `repo_mount_id` to `repo_mounts.node_id` — the same owning-node column `idx_repo_mounts_active_root` keys canonical-root uniqueness on — filtered to the deciding node (Codex PR #346 round 1: a `workspaces`-only comparison could not even express the filter). The filter is load-bearing, not decorative, because one node's local database lawfully holds rows for more than one node's mounts — D-009-7's active-root uniqueness binds per owning node precisely because the same absolute path on two different nodes both attach, two distinct node-local filesystems (`Spec-009 §State And Data Implications`) — so two nodes' identical absolute spellings of a checkout path never enter one comparison set. No cross-node coordination is implied or required, and none is added. **Direction:** Provide consent to [Plan-009](./009-repo-attachment-and-workspace-binding.md) (reciprocal CP-009-10). **Tasks:** T5.1, T5.2 (shipped callee surface; the EXTEND executes under Plan-009 T-009-2B-4, on the `checkout_root` carrier T3.2 captures).

## Ratified Design Decisions (Tier-6 audit, 2026-06-10)

- **D-010-1 — Contract file is `packages/contracts/src/worktree.ts`** (renamed from the pre-audit `execution-mode.ts`): the file's dominant content is the worktree/clone lifecycle domain; one-file-per-domain per cross-plan §2 contracts row.
- **D-010-2 — Consolidated wire block.** Seven request/response pairs + `EphemeralCloneId`/`BranchContextId` brands + `EphemeralCloneState` enum ratified in api-payload-contracts.md §Plan-010 (brands/enums declared in-block for cite stability). Clone-prepare responses report effective `cleanupPolicy` + `expiresAt`; select responses carry `executionRoot` only when resolved synchronously.
- **D-010-3 — Method namespace is `repo.*`, seven methods** (`repo.executionModeSelect`, `repo.executionRootPrepare`, `repo.worktreeReuseCheck`, `repo.ephemeralClonePrepare`, `repo.ephemeralCloneDispose`, `repo.worktreeRetire`, `repo.worktreeStatusRead`). A `worktree.*` method root was considered and rejected: the Tier-1 ratified namespace-root enumeration admits `repo` and no `worktree` root; mounts/workspaces/worktrees/clones are one repo aggregate; sibling symmetry with `repo.executionModeCapabilitiesRead`. Imperative method strings stay disjoint-by-form from the past-participle Spec-006 event names.
- **D-010-4 — Error vocabulary.** §Worktree: `worktree.not_found` 404, `worktree.create_failed` 500, `worktree.branch_collision` 409, `worktree.reuse_conflict` 409, `worktree.retire_conflict` 409. §Ephemeral Clone: `clone.not_found` 404, `clone.prepare_failed` 500. §Workspace extension rows: `workspace.branch_name_required` 400 (D-010-19), `workspace.branch_mismatch` 409, `workspace.busy` 409, `workspace.execution_root_unresolved` 409. Deliberately NO `worktree.unsupported`: select-time capability refusal is `workspace.mode_unsupported` (D-009-5); prepare-time dynamic unavailability IS `worktree.create_failed` (500, matching `workspace.provisioning_failed`'s class).
- **D-010-5 — Persistence model (4 tables; DDL in local-sqlite-schema.md, verbatim per migration except the one rebuild-added column called out below).** `worktrees` carries `created_by_session_id` NOT NULL + `created_by_run_id` nullable (provenance; session/run ids event-sourced, no FK) + `cleaned_at` + the partial-unique `idx_worktrees_active_branch ON (repo_mount_id, branch_name) WHERE state NOT IN ('retired','failed')` — git-faithful: a `merged` checkout still holds its branch; `failed` creations never materialized one. `ephemeral_clones` carries `branch_name` NOT NULL (head branch inside the clone — the status read exposes branch for clone records per [Spec-010 §Interfaces And Contracts](../specs/010-worktree-lifecycle-and-execution-modes.md#interfaces-and-contracts)), `expires_at` NOT NULL, `updated_at`, `cleaned_at`, CHECKed `cleanup_policy`, and the sweep index `(state, expires_at)`; no `created_by_run_id` (the run edge lives in `run_execution_contexts`). `branch_contexts` is workspace-anchored (`workspace_id` NOT NULL) with nullable `worktree_id`/`ephemeral_clone_id`, an at-most-one CHECK, and the partial-unique binding index `idx_branch_contexts_worktree_workspace ON (worktree_id, workspace_id) WHERE worktree_id IS NOT NULL` — one binding row per (workspace, worktree) per D-010-15's upsert semantics, so the worktree-keyed `BranchContextRead` resolves exactly one row on the (worktreeId, workspaceId) pair. `run_execution_contexts` (NEW) is the per-run execution binding: `run_id` PK (event-sourced, no FK), `session_id`, `workspace_id`, `execution_mode`, `execution_root`, `checkout_root` NOT NULL (2026-08-17 amendment — the enclosing working tree's top level for that `execution_root`, captured at context creation by the T3.2 gate from `workspaces.metadata.checkoutRoot`, derived from the just-materialized `execution_root`'s top level — with the workspace carrier healed — when the key is absent on a pre-amendment workspace row, so the column is always satisfiable and needs no backfill; it is the durable normalized root CP-010-15's restore consumes and the fourth link of that obligation's carrier chain, supplied onward as a CP-010-12 caller-resolved parameter. **This column is the plan's one rebuild-added column:** Phase 1 shipped the four-table CREATE as migration version 4 (`0004-worktree-lifecycle.ts`, PR #253), so `checkout_root` lands in a next-ordinal guarded block with **Phase 3** — the phase of T3.2, its only writer — by the documented table-rebuild, not an `ADD COLUMN`: the ALTER documentation refuses a NOT-NULL-without-default add unconditionally, and the empty-table allowance the bundled engine exhibits is undocumented (Codex PR #346 round 1). The `NOT NULL`-without-default declaration is lawful in a rebuild because the successor table is created, not altered; the copy step omits the column, so a pre-existing row would fail the engine-enforced NOT NULL at copy time — failing the migration closed instead of inventing a value — and the table is in fact row-less in every deployed instance, never having had a production writer), `git_common_dir` NOT NULL (the `git rev-parse --git-common-dir` absolute path captured at context creation — the surviving canonical git dir for snapshot-ref operations, so T5.3 prunes refs through it, never through a possibly-retired `execution_root`; worktree mode resolves to the main repo's git dir, branch/read-only to `<root>/.git`, ephemeral clone to the clone's own git dir — those refs share the clone's disposal lifecycle per T5.3), optional `worktree_id`/`ephemeral_clone_id`/`branch_context_id`, `created_at`, `released_at`, and a mode-conditional CHECK binding id presence to `execution_mode` (worktree ⇒ `worktree_id` set, ephemeral clone ⇒ `ephemeral_clone_id` set — the other root id NULL in each; every writable mode carries `branch_context_id`; read-only carries none of the three). `checkout_root` and `git_common_dir` are the two columns deliberately held OUTSIDE that mode-conditional CHECK: both are captured unconditionally, on read-only rows included, because their consumers are mode-agnostic — the CP-010-4 checkout-keyed run tenancy and the I-010-14 run gate apply to every repo-bound run regardless of mode, and a read-only run whose row lacked a checkout root could not participate in the tenancy comparison it is subject to.
- **D-010-6 — Execution-root placement + trust envelope.** Daemon-provisioned roots live at `<executionRootsDir>/<repoMountId>/{worktrees|clones}/<id>`, never inside the attached checkout; they are in-envelope by construction (`Spec-009 §Local Trust Envelope (V1 Definition)` daemon-provisioned-roots clause). The mount's canonical root hosts the `git -C <canonicalRoot> worktree add <rootPath> …`-shaped invocations only.
- **D-010-7 — Collision policy is provenance-split.** Caller-supplied `branchName` colliding with a live checkout → typed `worktree.branch_collision` refusal (user intent never adapted). Daemon-derived default name colliding → first-free ordinal suffix (`-2`, `-3`, …), persisted + reported verbatim. Service parameter `onCollision: 'refuse' | 'suffix'`: wire prepares always pass `refuse`; the run-setup gate's derived-name path passes `suffix`. The partial-unique index arbitrates races (insert-retry loop). Collision never implies implicit reuse.
- **D-010-8 — Base-ref policy.** Worktree base defaults to the mount's current HEAD branch; `baseRef?` overrides explicitly; a detached-HEAD mount with no explicit base ref → typed refusal (`worktree.create_failed` with detail), never a guess.
- **D-010-9 — `branch` mode is bind-only.** Prepare verifies the BOUND checkout's current branch — read at `workspaces.metadata.boundRoot`, the main checkout or a linked worktree, with the `canonical_root` fallback for pre-amendment rows (CP-010-14) — equals the requested branch context and refuses `workspace.branch_mismatch` otherwise; the daemon never runs `git checkout`/`switch`/`branch` in the bound checkout OR the main checkout. Verification carries a linked-worktree assertion (2026-08-17 carried-findings adjudication, executed with the re-rooting under Plan-009 T-009-2B-4): the branch-mode prepare fixture binds a linked worktree and asserts prepare mutates NEITHER tree — bound worktree and main checkout byte-identical before and after — because the main-checkout-only scope, written when branch mode's root WAS the main checkout, would let an implementation mutate the bound linked checkout and still satisfy the recorded immutability checks.
- **D-010-10 — Hook neutralization.** Every provisioning git invocation runs via `execFile` argv-only (no shell) with `-c core.hooksPath=<empty dir>` and `-c core.fsmonitor=false` — the second flag because the fsmonitor hook is named by config value rather than by a `hooks/`-resident file, so `core.hooksPath` never governs it (index-refreshing verbs such as `status` and `worktree add` invoke a repo-local `core.fsmonitor=<pathname>` despite an empty `core.hooksPath`; empirically confirmed on git 2.50.1, PR #297 round 2); acceptance fixtures install sentinel `post-checkout`/`post-merge` hooks plus a config-named fsmonitor sentinel in a hostile fixture repo and assert no sentinel ever fires for a service invocation.
- **D-010-11 — No new event types.** This plan adds no rows to the Spec-006 registry: no `worktree.failed` event, no ephemeral-clone events — the five `worktree.*` rows Spec-006 already enumerates are union-registered by T1.1 per CP-010-5 (registration of existing rows, not new types). Rationale: worktree rows are table-sourced (not event-replayed); the failure incident is already evented as `workspace.stale` with `metadata.lastError` (single-incident-single-event); failed rows stay queryable via `repo.worktreeStatusRead`; reopening Plan-006's Tier-4-ratified registry needs more than symmetry. The deliberate carve-out is pinned by a T2.1 regression test.
- **D-010-12 — Event-transition mapping.** Row creation → `worktree.created`; `creating -> ready` → `worktree.ready`; `-> dirty` → `worktree.dirty`; `-> merged` → `worktree.merged`; `-> retired` → `worktree.retired`; `-> failed` → none (the coupled `failReprovision` emits `workspace.stale`). Emission is transactional with the row write; the payload is `WorktreeLifecyclePayloadSchema` — Plan-009's family shape instantiated over `WorktreeStateSchema` (`worktreeId` populated; PR #250 round 4).
- **D-010-13 — Cleanup sweep.** One daemon tick drives: (a) TTL-expired clones (`expires_at` past, state in `creating`/`ready`); (b) clones whose owning workspace is `archived` (detach cascade); (c) worktrees whose mount is `detached` (non-terminal states → retired, events emitted, metadata preserved); (d) disk removal for retired-but-uncleaned rows, idempotent, stamping `cleaned_at`. Retiring the clone backing a live (non-archived) clone-mode workspace's current root returns that workspace to `provisioning` via Plan-009 primitives — `stale` is reserved for fault paths (`Spec-009 §Ephemeral Clone Lifecycle`).
- **D-010-14 — Select records; prepare materializes.** `repo.executionModeSelect` records the canonical mode and transitions the workspace (`beginReprovision` for writable targets; synchronous complete for `read-only`); per-task root materialization happens at `repo.executionRootPrepare` or the run-setup gate — architecturally forced, since worktree/clone roots are per-task and a select carries no task context. The picker fires exactly one select mutation per switch; selecting on a `stale` workspace is the repair path.
- **D-010-15 — Explicit reuse + dirty acknowledgement.** `reuseWorktreeId` names the candidate; `acknowledgeDirtyCandidate: true` is the separate consent that binds a DIRTY candidate (TOCTOU-sound: a candidate that turned dirty after the check refuses `worktree.reuse_conflict` when the ack is absent). An incompatible candidate never binds regardless of acknowledgement. Explicit reuse preserves the candidate's existing branch-context row when the candidate already belongs to the requesting workspace; cross-workspace reuse upserts the binding workspace's own `branch_contexts` row — a first binding inserts a NEW row scoped to that workspace (branch values carried over from the candidate's row, which is left untouched as history); a workspace re-binding a worktree it historically bound refreshes its existing pair row in place (carried-over branch values + `updated_at`) instead of inserting a duplicate, so exactly one row exists per (workspace, worktree) (the partial-unique `idx_branch_contexts_worktree_workspace`) — `branch_contexts.workspace_id` must always name the workspace the binding serves, or workspace-scoped consumers (approval path-scope matching, `idx_branch_contexts_workspace`) resolve the wrong workspace.
- **D-010-16 — Run-setup binding.** Plan-010's gate (registered on the Plan-004 T3.10 seam per CP-010-9) implements `assertRunReady`: resolve the queue item's `workspaceId` → `assertWritable` (writable modes; lawfully skipped when the workspace already sits `provisioning` — the open-bracket scoping of I-010-12 / CP-010-3, PR #297) → materialize/bind the root (derived branch name per the slug rule, `onCollision: 'suffix'`) → `markBusy` → write the `run_execution_contexts` row. `onRunTerminal` releases in reverse: `releaseBusy`, stamp `released_at`, fire `on_run_complete` clone retirement (with the D-010-13 workspace disposition). Refusal parks the run in `starting` (`workspace.execution_root_unresolved` wrapping the underlying typed cause); a run on a busy workspace refuses `workspace.busy` (one-holding-run V1 rule).
- **D-010-17 — Status read + stale-diagnostic surface.** `repo.worktreeStatusRead` returns the session's worktree + clone rows with provenance (D-010-2 shape). The owning-workspace stale diagnostic rides Plan-009's `WorkspaceListResponse.lastError?` (additive, ratified into the §Plan-009 block + Plan-009 T1.3 at this audit). No invented wire names: Phase 4 views consume exactly the ratified contract names.
- **D-010-18 — `ModeSwitchOptions` is an own presentational component** (props-only: `availableModes`, `restrictions`, current mode, disabled state) composed by `ExecutionModePicker`; no import from `../repo-attach/` (CP-010-13 boundary).
- **D-010-19 — Wire prepares carry the branch; the slug rule is gate-only.** The Spec-010 slug rule ([Spec-010 §Resolved Questions and V1 Scope Decisions](../specs/010-worktree-lifecycle-and-execution-modes.md#resolved-questions-and-v1-scope-decisions)) derives from run-scoped inputs (originating queue-item summary; `run-<run-short-id>` fallback) that exist only on the run-setup gate path (D-010-16). Wire-initiated prepares are pre-run by definition and carry no derivation seed: `repo.ephemeralClonePrepare` requires `branchName` on the wire, and a writable-mode `repo.executionRootPrepare` carrying neither `branchName` nor a service-side `runId` draws the typed `workspace.branch_name_required` refusal (400) before any git call — `read-only` prepares ignore the field. `ExecutionRootPrepareRequest.branchName?` optionality is therefore mode-conditional requiredness enforced service-side, not schema-optional-everywhere. Below T2.4 the mode services take `branchName` required (`worktree-service.create`, `ephemeral-clone-service.prepare` — neither holds derivation inputs); T2.4 resolves the name first (pass-through when supplied; the `run-<short-8>` fallback via the T2.2 helper when only `runId` arrived) — the queue-summary primary rule fires only at the gate, the sole holder of the summary.

## Implementation Phase Sequence

Contracts: see [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-010 for the typed schemas every phase consumes verbatim.

### Phase 1 — Contracts + persistence

**Goal.** The `worktree.ts` contract domain (seven wire pairs, brands, enums, event registration) and the four-table migration, with contract↔DDL conformance pinned.

**Preconditions.**

- [x] Plan-009 Phase 1 merged (`packages/contracts/src/repo.ts` — the import origin per CP-010-1) — merged 2026-07-26 via PR #250 (squash 0ed3c7f)
- [x] Plan-006 Phase 1 merged (`SessionEventSchema` union seam + `SESSION_EVENT_CATEGORY_BY_TYPE` registry — Tier 4) — merged 2026-07-25 via PR #247 (squash f7d49f3)
- [x] Plan-005 Phase 1 **T1.1** merged (the `RunId` brand at `packages/contracts/src/provider-driver.ts` — T1.2's status-read `createdByRunId` type import; erased at emit but typecheck-blocking) — merged 2026-06-15 via PR #157 (squash a81821e). Recorded PR #254: satisfied six weeks before dispatch, so it never gated in practice, but T1.2 **Consumes** has named the dependency since the Tier-6 audit and neither this box nor the machine block carried it. Round 2 narrowed the claim from the whole phase to the producing task's merged revision — Plan-005 Phase 1 has since grown campaign EXTENDs T1.7/T1.8 that remain unshipped, so "Phase 1 merged" is not true as written, and the whole phase is not what this dependency needs
- [x] D-010-2 wire shapes ratified — api-payload-contracts.md §Plan-010 (Tier-6 audit)
- [x] D-010-5 DDL ratified — local-sqlite-schema.md (Tier-6 audit)
- [x] D-010-11 event-set adjudicated — five types, registry closed (fixes T1.1's registration count)

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the boxes above (preflight Gate 5; PR #251
  # round 2 — the plural prose header is not a recognized gate form, so these
  # build-order dependencies previously failed open and the ticks alone
  # carried the claim).
  - { type: plan_phase, plan: 009, phase: 1, status: merged }
  - { type: plan_phase, plan: 006, phase: 1, status: merged }
  # PR #254 round 1 — T1.2's `RunId` type import binds provider-driver.ts at
  # typecheck, so this is a build-order dependency even though `import type`
  # emits no runtime edge. PR #254 round 2 — gated on the immutable merged
  # revision, not the whole phase: `RunId` shipped in PR #157 (T1.1), but
  # Plan-005 Phase 1 has since grown campaign EXTENDs T1.7/T1.8 that remain
  # unshipped, so a `plan_phase` gate here classifies `partially_shipped` and
  # fails closed on tasks this dependency does not need.
  - { type: pr_merged, ref: 157 }
```

#### Tasks

- **T1.1 — `worktree.ts` contract core + event registration.**
  - **Files:** `packages/contracts/src/worktree.ts` (CREATE), `packages/contracts/src/index.ts` (EXTEND — `export * from "./worktree.js";`), `packages/contracts/src/event.ts` (EXTEND — additive registration of the 5 `worktree.*` types into `SessionEventSchema` per CP-010-5)
  - Define and export: `WorktreeId` + `EphemeralCloneId` + `BranchContextId` branded UUID schemas via `brandedUuidIdSchema`; `WorktreeStateSchema` over the six states; `EphemeralCloneStateSchema` over `"creating" | "ready" | "retired" | "failed"`; `CleanupPolicySchema` over `"on_run_complete" | "manual"`. Import — never redefine — `ExecutionMode`, `WorkspaceState`, branded `RepoMountId`/`WorkspaceId`, and `buildRepoWorkspaceLifecyclePayloadSchema` from `./repo.js` (I-010-1); define `WorktreeLifecyclePayloadSchema` as that factory's `WorktreeStateSchema` instantiation — the worktree events carry the shared family shape (its `worktreeId?` field carries the subject as the family's plain canonical-UUID string: unbranded in `repo.ts` because the brand is this plan's symbol to declare, with the runtime accept set already identical — `WorktreeId`-typed consumers apply the brand at their own parse boundary; the factory's erased `z.ZodType` return admits no `.extend()` re-branding) with the worktree state vocabulary, exact per family (PR #250 round 4).
  - **Tests:** parse-accept/reject per enum; brand inequality (a `WorktreeId` does not accept a raw string without parse); union registration — each of the 5 event types parses through `SessionEventSchema` with a `worktreeId`-bearing payload; a `worktree.failed` literal is REJECTED by the union (D-010-11 pin); a worktree payload carrying a base-vocabulary state (e.g. `attached`) is REJECTED by `WorktreeLifecyclePayloadSchema` (exact per-family vocabulary pin, PR #250 round 4).
  - **Spec coverage:** Spec-010 line 50 (six worktree lifecycle states), Spec-010 line 128 (contract distinguishes the four canonical modes — via the imported `ExecutionMode`), Spec-010 line 144 (no-new-event-types carve-out)
  - **Verifies invariant:** I-010-1, I-010-13 (registration half)
  - **Consumes:** `brandedUuidIdSchema` ← `packages/contracts/src/internal/branded.ts` (package-internal helper on the Plan-001-owned `packages/contracts/src/` path; authored by Plan-002 under the [§Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule) Housekeeping Exception, shipped Tier 2); `ExecutionMode`/`WorkspaceState`/`RepoMountId`/`WorkspaceId`/`buildRepoWorkspaceLifecyclePayloadSchema` + its `RepoWorkspaceLifecyclePayloadOf` return alias ← Plan-009 Phase 1 T1.1 (CP-010-1; PR #250 round 4 — the factory replaces reuse-as-is of the two-vocabulary schema); `SessionEventSchema` union seam + category registry ← Plan-006 Phase 1 T1.2 (CP-010-5, additive-MINOR EXTEND)
- **T1.2 — Seven wire request/response pairs.**
  - **Files:** `packages/contracts/src/worktree.ts` (EXTEND)
  - Zod schemas + inferred types implementing api-payload-contracts.md §Plan-010 verbatim: `ExecutionModeSelectRequest/Response`; `ExecutionRootPrepareRequest/Response` (incl. `baseRef?`, `reuseWorktreeId?`, `acknowledgeDirtyCandidate?` — D-010-8/D-010-15; no wire `runId` — run binding is gate-supplied service-side; `branchName?` stays schema-optional, but a writable-mode wire prepare without it draws the service-side `workspace.branch_name_required` refusal — D-010-19); `WorktreeReuseCheckRequest/Response` (singular candidate; `reason?` populated when not clean or not compatible); `EphemeralClonePrepareRequest/Response` (no TTL on the wire — D-010-2; request requires `branchName` — wire calls are pre-run and carry no slug-rule seed, D-010-19; response reports effective `cleanupPolicy` + `expiresAt` + `branchName`); `EphemeralCloneDisposeRequest/Response`; `WorktreeRetireRequest/Response` (`Extract`-typed `retired`); `WorktreeStatusReadRequest/Response` (provenance-bearing arrays).
  - **Tests:** parse-accept one fixture per shape; parse-reject an out-of-taxonomy `executionMode` on select; parse-reject a clone-prepare carrying a TTL-like extra key under strict parsing; parse-reject a clone-prepare missing `branchName` (D-010-19); assert select-response `executionRoot` optionality and clone-prepare-response `state` narrowing (`creating`/`ready` only); assert status-read worktree items require `createdBySessionId`; assert clone shapes carry `branchName` (prepare response + status-read clone items).
  - **Spec coverage:** Spec-010 line 75 (select records the canonical mode), Spec-010 line 76 (prepare creates-or-binds the root; explicit reuse rides it), Spec-010 line 77 (reuse check reports branch, cleanliness, compatibility), Spec-010 line 78 (clone prepare reports root, lifecycle, cleanup policy), Spec-010 line 79 (explicit dispose interface), Spec-010 line 80 (retire records even with async deletion), Spec-010 line 81 (status read exposes records incl. provenance)
  - **Verifies invariant:** I-010-2 (contract half)
  - **Consumes:** branded ids + enums ← T1.1; `SessionIdSchema` ← Plan-001 (shipped); `RunId` ← Plan-005 Phase 1 `packages/contracts/src/provider-driver.ts` (type import for the status-read provenance field)
- **T1.3 — Four-table migration.**
  - **Files:** `packages/runtime-daemon/src/migrations/` next-ordinal migration file (CREATE), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — guarded-block seam)
  - Verbatim-SQL migration (Plan-009 migration-file convention) creating `worktrees`, `ephemeral_clones`, `branch_contexts`, `run_execution_contexts` + all indexes exactly as ratified in local-sqlite-schema.md (D-010-5), incl. the partial-unique `idx_worktrees_active_branch` and the clone sweep index `(state, expires_at)`.
  - **Migration lineage (2026-08-17 amendment; shipped as version 4, `0004-worktree-lifecycle.ts`, PR #253).** This migration's `run_execution_contexts` CREATE carries every column of the schema-doc block **except `checkout_root`**, which was ratified after version 4 shipped and lands by a next-ordinal table-rebuild migration with Phase 3 / T3.2 (D-010-5). The schema doc therefore shows the table's final shape, not this file's content — do not read the block as a diff target for version 4, and do not amend the shipped migration to include the column (an already-applied guarded block never re-runs, so editing it would change fresh installs only and fork the schema by install date).
  - **Tests:** migration applies on a fresh db and is idempotent under the runner's guard; the partial-unique index admits two rows on the same (mount, branch) when one is `retired` and rejects two live rows; the `branch_contexts` CHECK rejects a row carrying both `worktree_id` and `ephemeral_clone_id`; the `run_execution_contexts` mode-conditional CHECK accepts one in-shape row per mode and rejects a worktree-mode row missing `worktree_id`, a clone-mode row carrying `worktree_id`, and a read-only row carrying `branch_context_id`; `git_common_dir` is `NOT NULL` (a row omitting it is rejected).
  - **Spec coverage:** Spec-010 line 87 (worktree records persist branch, mount, state, provenance), Spec-010 line 88 (per-run execution binding persisted), Spec-010 line 89 (branch context persisted polymorphically per writable mode)
  - **Verifies invariant:** I-010-2 (DDL half), I-010-3, I-010-4, I-010-5
  - **Consumes:** migration-runner guarded-block seam + `openDatabase` ← Plan-001 (shipped Tier 1); `repo_mounts`/`workspaces` referenced tables ← Plan-009 Phase 2 T2.1 migration (a **forward** FK reference, not a Phase-1 build-order gate — SQLite does not check parent-key definitions at CREATE TABLE time and defers the error to statement preparation, so this migration applies cleanly on a parent-less database and shipped ahead of Plan-009 Phase 2 per the ratified campaign order, PR #253; see §References for the primary source and for the `foreign_keys = ON` precondition that makes the deferred check live. The first insert path opens at Plan-010 Phase 2, which does machine-gate on `plan_phase, plan: 009, phase: 2, status: merged` within Tier 6)
- **T1.4 — Contract↔DDL conformance tripwire.**
  - **Files:** `packages/runtime-daemon/src/git/__tests__/contract-ddl-conformance.test.ts` (CREATE)
  - Asserts the `WorktreeState`/`EphemeralCloneState`/cleanup-policy literal sets in `worktree.ts` equal the sets parsed out of the migration's CHECK clauses (string-extraction over the migration source — the Plan-009 conformance-test shape).
  - **Tests:** the assertion IS the test; plus a mutation canary (a deliberately-wrong fixture set fails).
  - **Spec coverage:** Spec-010 line 50 (state set lockstep between contract and persistence)
  - **Verifies invariant:** I-010-2
  - **Consumes:** enums ← T1.1; migration source ← T1.3

### Phase 2 — Daemon git services (worktrees, clones, execution roots)

**Goal.** The `git/` service layer: single-seam event emission, worktree create/reuse/retire with the ratified collision + hook-neutralization + base-ref policies, clone prepare/dispose/sweep with TTL bookkeeping, and the workspace-primitive-bracketed execution-root orchestrator.

**Preconditions.**

- [x] Phase 1 merged (contracts in `packages/contracts/src/worktree.ts` — branded ids, the three Plan-010-owned enum vocabularies, `WorktreeLifecyclePayloadSchema`, the five `worktree.*` union registrations, and the seven wire pairs; the four-table migration `0004-worktree-lifecycle.ts`; the I-010-2 contract↔DDL conformance tripwire) — merged 2026-07-26 via PR #253 (squash `f3c9947`)
- [x] Plan-009 Phase 2 merged (`WorkspaceService` primitives incl. `markBusy`/`releaseBusy` per CP-009-7; `repo_mounts`/`workspaces` rows) — satisfied 2026-08-05 via PR #292 (squash `7531bd5`)
- [x] Plan-006 Phase 3 merged (`EventLogService.append` — sole append path; build-order precondition, same emitter pattern as Plan-009 Phase 2) — satisfied 2026-08-04 via PR #287 (squash `1ed7e1b`)
- [x] D-010-6/7/8/9/10/12/13/19 ratified (Tier-6 audit — placement, collision, base-ref, branch-bind-only, hooks, event mapping, sweep, wire-branch requiredness)

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the boxes above (preflight Gate 5).
  - { type: plan_phase, plan: 010, phase: 1, status: merged }
  - { type: plan_phase, plan: 009, phase: 2, status: merged }
  - { type: plan_phase, plan: 006, phase: 3, status: merged }
```

#### Tasks

- **T2.1 — `git/` directory + `worktree-event-emitter.ts` single emission seam.**
  - **Files:** `packages/runtime-daemon/src/git/worktree-event-emitter.ts` (CREATE), `packages/runtime-daemon/src/git/__tests__/worktree-event-emitter.test.ts` (CREATE)
  - One emission seam (Plan-009 emitter precedent): validates payloads through `WorktreeLifecyclePayloadSchema.parse()` at the boundary (the T1.1 factory instantiation; PR #250 round 4), maps transitions per D-010-12, appends via `EventLogService.append` (never computes integrity bytes). Exposes exactly five emit surfaces — there is no `emitFailed`.
  - **Tests:** each transition emits its mapped type exactly once; the payload carries `worktreeId` + `sessionId`; the `failed` transition path emits NOTHING (D-010-11 regression pin); a malformed payload throws at the seam.
  - **Spec coverage:** Spec-010 line 144 (failed and clone transitions deliberately not separately evented)
  - **Verifies invariant:** I-010-13
  - **Consumes:** `EventLogService.append(envelope)` ← Plan-006 Phase 3 T3.1; 5 type names + category mapping ← Plan-006 Phase 1 T1.2; `WorktreeLifecyclePayloadSchema` ← T1.1 (the `buildRepoWorkspaceLifecyclePayloadSchema` instantiation; PR #250 round 4); union registration ← T1.1
- **T2.2 — `worktree-service.ts` + `worktree-errors.ts`: create / validateReuse / retire / cleanupPass.**
  - **Files:** `packages/runtime-daemon/src/git/worktree-service.ts` (CREATE), `packages/runtime-daemon/src/git/worktree-errors.ts` (CREATE), `packages/runtime-daemon/src/git/__tests__/worktree-service.test.ts` (CREATE)
  - `worktree-errors.ts` holds every Plan-010 typed error class (`worktree.*`, `clone.*`, and the three Plan-010-declared `workspace.*` carriers — `workspace.busy` is a Plan-010-introduced registry row whose CARRIER is Plan-009's live `WorkspaceBusyError` per CP-009-7, and a second class minting the same code would fork `instanceof` discrimination; PR #297), each extending the BL-143 `DaemonDomainError` base (constructor-injected code strings from error-contracts.md; no filesystem paths in messages — sanitization discipline). The file lives in Plan-010-owned `git/`, preserving the cross-plan §2 two-file `workspace/` pin; Phase 3 imports these classes.
  - `worktree-service.ts`: `create({repoMountId, branchName, baseRef?, onCollision, sessionId, runId?})` — `branchName` is required at this seam; the `Spec-010 §Resolved Questions and V1 Scope Decisions` slug rule lives in the exported `deriveWorktreeBranchName(summary?, runId)` helper (daemon-side only) that the derivation-owning callers invoke (the T3.2 gate's queue-summary slug; T2.4's runId-only fallback arm — D-010-19); resolve base per D-010-8 (mount HEAD; detached-HEAD + no `baseRef` → typed refusal); place the root per D-010-6; run `git worktree add` hook-neutralized per D-010-10; collision per D-010-7 (`refuse` → `worktree.branch_collision`; `suffix` → first-free ordinal via index-arbitrated insert-retry); row + `worktree.created`/`worktree.ready` via T2.1; creation failure → row `failed` (no event) + propagate for the caller's `failReprovision`. `validateReuse({reuseWorktreeId, branchName, acknowledgeDirtyCandidate?})` — the candidate must be live, branch-compatible, and clean-or-acknowledged (D-010-15), else `worktree.reuse_conflict`; incompatible never binds. `retire(worktreeId)` — refuse `worktree.retire_conflict` while the root is held busy; else state → `retired` + event (metadata preserved, I-010-9). `cleanupPass()` — detached-mount worktrees → retired (D-010-13c); async disk removal for retired-uncleaned rows stamping `cleaned_at` (D-010-13d, idempotent).
  - **Tests:** slug derivation table-driven (lowercase, collapse, trim, 40-char truncation, `run-<short-8>` fallback); supplied-name collision refuses; derived-name collision suffixes `-2` then `-3` deterministically and reports verbatim; dirty candidate without ack refuses, with ack binds; incompatible candidate refuses despite ack; retire-while-busy refuses; retire emits then cleanup stamps; provenance columns populated and preserved across retire.
  - **Spec coverage:** Spec-010 line 46 (create-or-reuse before execution), Spec-010 line 51 (explicit reuse preserves branch + provenance context), Spec-010 line 55 (naming pattern), Spec-010 line 58 (retirement preserves metadata with async cleanup), Spec-010 line 114 (explicit collision handling), Spec-010 line 139 (slug rule), Spec-010 line 140 (provenance-split collision policy), Spec-010 line 141 (base-ref policy)
  - **Verifies invariant:** I-010-3, I-010-4, I-010-6, I-010-8, I-010-9, I-010-10
  - **Consumes:** emitter ← T2.1; rows ← T1.3; `DaemonDomainError` base ← BL-143 (`packages/runtime-daemon/src/ipc/domain-error.ts` — Plan-007-owned; §Phase-3 Preconditions track it, Phase 2 needs only the class shape); error codes ← D-010-4
- **T2.3 — `ephemeral-clone-service.ts`: prepare / dispose / retireRunClone / cleanupTick.**
  - **Files:** `packages/runtime-daemon/src/git/ephemeral-clone-service.ts` (CREATE), `packages/runtime-daemon/src/git/__tests__/ephemeral-clone-service.test.ts` (CREATE), `packages/runtime-daemon/src/git/worktree-errors.ts` (EXTEND — `ClonePrepareFailedError` takes the closed `ClonePrepareFailureReason` discriminant T2.2 declared it argument-free to leave for this task, its taxonomy owner; PR #297), `packages/runtime-daemon/src/git/__tests__/worktree-service.test.ts` (EXTEND — the carrier-census constructor call takes the new argument; PR #297)
  - Constructor config `{ttlMs = 86_400_000}` — TTL is daemon config, not wire (`Spec-010 §Resolved Questions and V1 Scope Decisions`). `prepare({workspaceId, branchName, cleanupPolicy?})`: `branchName` is required at this seam — the service holds no slug-rule inputs (D-010-19: the wire binder requires it; T2.4 always passes a resolved name); clone the mount's canonical root to the D-010-6 path (hook-neutralized, D-010-10), create the supplied head branch and persist it (`branch_name`), row with `expires_at = now + ttlMs`, report effective policy + expiry + branch + the base branch observed from the CLONE's own HEAD before the cut (`git branch --show-current`; omitted entirely when the clone's own HEAD lands detached — only a source HEAD commit no branch references does that, `git clone` resolving the remote HEAD to a branch naming it otherwise — which is lawful and not a failure; a failed read refuses `base_branch_unreadable`; PR #297). `dispose(cloneId)`: explicit-disposal arm → retired (recorded-then-cleaned). `retireRunClone({workspaceId, cloneId, trigger})`: the run-terminal `on_run_complete` path, scoped to the clone the terminating run's branch context names — the workspace id is a consistency bind; workspace-wide selection was the delayed-terminal hazard, retiring a NEXT run's fresh clone when the terminal call lands late (Phase 3's terminal integration resolves `cloneId` from the run's recorded context; PR #297). `cleanupTick()`: D-010-13 (a)(b)(d) + the live-workspace → `provisioning` disposition via injected Plan-009 primitives.
  - **Tests:** prepare reports policy + expiry + the persisted `branchName` (the caller-supplied name persists verbatim — no derivation arm exists at this seam); prepare reports the observed base branch, and the detached-HEAD clone (a source HEAD commit no branch references) is a standing negative control asserting the field is ABSENT on a successful prepare — distinct from the failed-read refusal, and not redundant with it (PR #297); a TTL-expired clone retires on tick; an archived-workspace clone retires; a live clone-mode workspace returns to `provisioning` on its root's retirement (never `stale`); dispose is idempotent; disk cleanup stamps `cleaned_at`; preparation failure → `clone.prepare_failed` + caller-visible for `failReprovision`.
  - **Spec coverage:** Spec-010 line 47 (clone provisions before writable execution), Spec-010 line 65 (clone-prep failure blocks in setup), Spec-010 line 70 (sweep retirement disposition incl. workspace return-to-provisioning), Spec-010 line 143 (TTL as daemon config)
  - **Verifies invariant:** I-010-9, I-010-10
  - **Consumes:** emitter — none (clone transitions unevented, D-010-11); rows ← T1.3; `beginReprovision` ← Plan-009 Phase 2 T2.4 (CP-010-2, injected); `WorkspaceBusyError` ← Plan-009 Phase 2 T2.4 (the carrier IDENTITY, not only its code: `cleanupTick` leg (d) adjudicates skip-vs-abort on `instanceof` — raised by `beginReprovision` per CP-010-2 when the CP-010-4 / CP-009-7 busy bracket is held; PR #297); error classes ← T2.2 `worktree-errors.ts`
- **T2.4 — `workspace/execution-root-service.ts`: mode-dispatched orchestrator, sole `branch_contexts` writer.**
  - **Files:** `packages/runtime-daemon/src/workspace/execution-root-service.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/execution-root-service.test.ts` (CREATE)
  - `prepare({workspaceId, branchName?, baseRef?, reuseWorktreeId?, acknowledgeDirtyCandidate?, runId?, onCollision = 'refuse'})`: `assertWritable` first for writable modes (CP-010-3, I-010-12 — skipped, together with `beginReprovision`, when the workspace already sits `provisioning`: that prepare is the open CP-010-2 bracket's provisioner, and Plan-009's gate raises for `provisioning` by design); writable-mode calls carrying neither `branchName` nor `runId` (wire-originated pre-run prepares) refuse `workspace.branch_name_required` next — the slug rule's inputs are gate-only (D-010-19); a writable-mode call carrying `runId` without `branchName` (the gate path's lawful shape) resolves the `run-<short-8>` fallback via the T2.2 helper before dispatch, so the mode services below this seam always receive an explicit name; dispatch on the workspace's selected mode — `read-only` resolves the bind root; `branch` verifies bind-only per D-010-9 (`workspace.branch_mismatch`); `worktree` delegates to T2.2 (create or explicit-reuse); `ephemeral clone` delegates to T2.3; write/preserve the `branch_contexts` row (sole writer — CP-010-6; explicit reuse preserves the candidate's row for a same-workspace bind and upserts the binding workspace's pair row on cross-workspace reuse — insert on first binding, in-place refresh when the workspace historically bound this worktree, branch values carried over — D-010-15); bracket workspace state via CP-010-2 primitives (`beginReprovision` when `ready`; first-bind writable workspaces already sit `provisioning` — no double-begin; `completeReprovision(workspaceId, root)` on success; `failReprovision(workspaceId, detail)` on failure — no silent substitution).
  - **Tests:** mode-dispatch table; a stale workspace refuses before any git call; branch-mode mismatch refuses without mutating the checkout; a writable-mode prepare with neither `branchName` nor `runId` refuses `workspace.branch_name_required` before any git call (D-010-19); a runId-only writable prepare resolves the `run-<short-8>` fallback and the delegated mode service receives an explicit name; cross-workspace explicit reuse writes a fresh branch-context row scoped to the binding workspace and leaves the candidate's row untouched, a workspace re-binding a worktree it historically bound refreshes its existing pair row instead of duplicating it (the partial-unique binding index holds), and same-workspace reuse preserves the existing row without duplication (D-010-15); success completes reprovision with the prepared root; failure fail-reprovisions and the workspace's `lastError` carries the detail; `branch_contexts` polymorphism per mode (worktree row / clone row / neither); no raw `workspaces` UPDATE anywhere in the service source (I-010-11 assertion).
  - **Spec coverage:** Spec-010 line 42 (exactly one canonical mode per run binding), Spec-010 line 45 (branch mode is an explicit writable override on the existing checkout), Spec-010 line 48 (no silent main-checkout fallback), Spec-010 line 64 (worktree-creation failure blocks in setup), Spec-010 line 69 (stale refusal), Spec-010 line 89 (branch context persisted per writable mode), Spec-010 line 142 (branch-mode bind-only verification)
  - **Verifies invariant:** I-010-5, I-010-6, I-010-7, I-010-11, I-010-12
  - **Consumes:** `assertWritable`/`beginReprovision`/`completeReprovision`/`failReprovision` ← Plan-009 Phase 2 T2.4 (CP-010-2/3); worktree ops ← T2.2; clone ops ← T2.3; rows ← T1.3
- **T2.5 — `worktree-projector.ts`: pure status-read projections.**
  - **Files:** `packages/runtime-daemon/src/git/worktree-projector.ts` (CREATE), `packages/runtime-daemon/src/git/__tests__/worktree-projector.test.ts` (CREATE)
  - Pure fold from `worktrees` + `ephemeral_clones` rows (session-scoped via `repo_mounts`) to the `WorktreeStatusReadResponse` shape — no IO beyond injected row reads, no derivation beyond projection (daemon-owned projections per `Spec-010 §State And Data Implications`).
  - **Tests:** the projection fixture covers every state incl. `failed`/`retired` (never filtered — I-010-19's daemon half); provenance fields pass through; the `repoMountId` filter narrows.
  - **Spec coverage:** Spec-010 line 81 (status read exposes lifecycle + provenance), Spec-010 line 90 (daemon-owned dirty/merged projections)
  - **Verifies invariant:** I-010-19 (daemon half), I-010-20 (daemon half)
  - **Consumes:** rows ← T1.3; response schema ← T1.2
- **T2.6 — Acceptance suite on real git fixtures.**
  - **Files:** `packages/runtime-daemon/src/git/__tests__/worktree-lifecycle.acceptance.test.ts` (CREATE)
  - Real temp-dir git repos (no mocks): full worktree lifecycle create→dirty→merged→retire; hostile-repo sentinel hooks never fire (D-010-10); derived-name collision suffixes; explicit-reuse dirty/ack matrix; clone prepare→TTL-expire→tick→workspace-provisioning disposition; main-checkout content byte-identical before/after every failure path (I-010-6 ground truth).
  - **Tests:** the suite IS the tests (acceptance tier).
  - **Spec coverage:** Spec-010 line 121 (no hidden main-checkout mutation), Spec-010 line 127 (AC1 — writable run on git defaults to worktree mode at the service layer), Spec-010 line 129 (AC3 — creation failure blocks rather than mutating), Spec-010 line 130 (AC4 — reused worktrees stay linked to branch + prior context)
  - **Verifies invariant:** I-010-6, I-010-7, I-010-8, I-010-10
  - **Consumes:** services ← T2.2, T2.3, T2.4; real-git fixture pattern ← Plan-009 Phase 2 acceptance precedent

### Phase 3 — Mode selection, run-setup gate, IPC namespace + SDK

**Goal.** The selection service, the run-setup execution-root gate on the Plan-004 seam, seven `repo.*` binder files, and the typed `worktreeClient` SDK with registration/dispatch/error round-trip coverage. Two Phase-2-recorded obligations land with this phase's wiring (PR #297): (1) the `-> dirty` / `-> merged` worktree row transitions and their D-010-12-mapped events have no Phase-2 producer — T2.2 ships create/reuse/retire/cleanup only — so the run-integration layer this phase builds owns them, and the Phase-3 dispatch DAG must name the owning task row before dispatch (T3.4's reuse-check registers `{mutating: false}` and cannot carry them); until that task lands, I-010-13's producer half is provable only for the Phase-2 legs. (2) The composition-root handle-identity obligation for the Phase-2 services — one `openDatabase` handle across `WorktreeService`, `EphemeralCloneService`, `ExecutionRootService`, and the event log — because I-010-13's `transactionalPrelude` row/event atomicity holds only within a single connection; same class as the Plan-009 Phase 3 Goal's PR #292 entry, and the structural guard lands with this phase's wiring.

**Preconditions.**

- [x] Phases 1–2 merged — Phase 1 satisfied 2026-07-26 via PR #253 (squash `f3c9947`); Phase 2 satisfied 2026-08-08 via PR #297 (squash `3e98412`)
- [ ] Plan-004 Phase 3 merged (run-engine + the T3.10 `RunSetupGate` seam — Tier 5)
- [x] BL-142 landed — registry regex conformance (camelCase tails: `repo.executionModeSelect`-class strings throw at boot without it)
- [x] BL-143 landed — `DaemonDomainError` projection branch in `mapJsonRpcError` (T3.8's wire assertions observe anonymous `-32603` without it)
- [x] D-010-3 method strings ratified — api-payload-contracts.md §Repo Method-Name Registry (Tier 6)
- [x] D-010-4 error codes ratified — error-contracts.md (Tier-6 audit)

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the boxes above (preflight Gate 5).
  - { type: plan_phase, plan: 010, phase: 1, status: merged }
  - { type: plan_phase, plan: 010, phase: 2, status: merged }
  - { type: plan_phase, plan: 004, phase: 3, status: merged }
  - { type: bl_closed, ref: 142 }
  - { type: bl_closed, ref: 143 }
```

#### Tasks

- **T3.1 — `workspace/execution-mode-service.ts`: select + capability gate.**
  - **Files:** `packages/runtime-daemon/src/workspace/execution-mode-service.ts` (CREATE), `packages/runtime-daemon/src/workspace/__tests__/execution-mode-service.test.ts` (CREATE)
  - `select({workspaceId, executionMode})` per D-010-14: validate against the D-009-5 capability matrix (`workspace.mode_unsupported` refusal — no silent substitution); record the mode; `read-only` completes synchronously (root = bind root; response carries `executionRoot`); writable targets `beginReprovision` only (response `state: 'provisioning'`, no root); selecting on `stale` is the repair path (begin-from-stale allowed per the Plan-009 primitive contract).
  - **Tests:** per-mode select matrix incl. capability refusal; read-only synchronous completion; writable parks in provisioning with no root; the stale-repair path; no raw `workspaces` UPDATE (I-010-11 assertion).
  - **Spec coverage:** Spec-010 line 49 (no silent substitution at selection), Spec-010 line 63 (unsupported-worktree repos offer alternates distinctly — the capability projection IS the distinct marking), Spec-010 line 75 (selection records and transitions; materialization deferred)
  - **Verifies invariant:** I-010-7, I-010-11
  - **Consumes:** capability matrix projection ← Plan-009 Phase 2 (D-009-5); `beginReprovision`/`completeReprovision` ← Plan-009 (CP-010-2); error classes ← T2.2 `worktree-errors.ts`
- **T3.2 — Run-setup execution-root gate (`RunSetupGate` implementation).**
  - **Files:** `packages/runtime-daemon/src/workspace/execution-mode-service.ts` (EXTEND — the gate lands here; no third `workspace/` file per the cross-plan §2 two-file pin), `packages/runtime-daemon/src/workspace/__tests__/run-setup-gate.test.ts` (CREATE), `packages/runtime-daemon/src/migrations/` next-ordinal migration file (CREATE — the `run_execution_contexts.checkout_root` table-rebuild migration; 2026-08-17 amendment, shape corrected at the PR's round-1 fold), `packages/runtime-daemon/src/session/migration-runner.ts` (EXTEND — register the new guarded block), `packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts` (EXTEND — its shipped `run_execution_contexts` pin asserts the **exact ordered** column list, so the rebuild breaks it by construction: the successor table declares `checkout_root` in the schema doc's canonical position. That pin grows in this PR rather than silently in a later one, and it gains the `notnull` / `dflt_value` assertions — in canonical CID order, with **no** last-in-CID-order special case: unlike `session_events.retention_class`'s ALTER-appended arm, the rebuild keeps `PRAGMA table_info` order and the schema doc's declared order identical)
  - Implements D-010-16 in its ratified order: `assertRunReady(context)` — non-repo runs (no `workspaceId`) pass through; repo-bound runs resolve the workspace, materialize via T2.4 `prepare` with the gate-derived branch name (slug rule; `onCollision: 'suffix'`) and `runId` (populating `created_by_run_id`), THEN `markBusy` (CP-010-4; `workspace.busy` on a concurrent hold — Spec-010 scopes `busy` to a root handed to an active run, never to a workspace still materializing), then write the `run_execution_contexts` row (populating `git_common_dir` from `git rev-parse --git-common-dir`, resolved absolute against the just-materialized `execution_root`, so ref operations survive a later retirement of that root; and populating `checkout_root` from the workspace row's `metadata.checkoutRoot`; when the key is absent on a pre-amendment row the gate derives the normalized top level from the just-materialized `execution_root` — live at exactly this moment — via the single-site normalization, persists it, and heals the workspace carrier by writing `metadata.checkoutRoot` back (Spec-010's provisioning-completion rewrite rule applied at this handoff — a Plan-009-consented delegated write of the Plan-009-owned key, CP-009-10 ⇄ CP-010-15), so the NOT NULL column is always satisfiable without a backfill migration and pre-amendment rows converge to the post-amendment invariant on first run; `canonical_root` is never the fallback — it names the bind-time attached checkout, not the tree a worktree- or clone-provisioned run executes in, and would fail capture's live-tree re-verification while keying tenancy to an unrelated checkout — Codex PR #346 round 2); materialization failure parks the run with no busy mark to unwind, and post-mark failure releases the mark before parking (both park as `workspace.execution_root_unresolved` wrapping the typed cause). `onRunTerminal(context)` — `releaseBusy`, stamp `released_at`, `on_run_complete` clone retirement via T2.3 with the D-010-13 disposition. Registers on the Plan-004 T3.10 seam (CP-010-9). **Interim tenancy scope (Codex PR #346 round 2):** at this phase's merge the busy bracket is per-workspace only (CP-009-7) — the checkout-keyed exclusivity re-key is Plan-009 T-009-2B-4's run-boundary leg — and that interim is fail-safe, not fail-open: until Plan-009 Phase 2B lands the CP-010-15 signature growth, capture and restore stay execution-root-scoped, so two workspaces bound to disjoint subdirectories of one checkout hold disjoint capture extents; the whole-checkout extent and the checkout-keyed hold arrive together under Plan-009 Phase 2B's intra-phase merge-order rule.
  - **`checkout_root` column + its migration (2026-08-17 amendment; migration shape corrected at the PR's round-1 fold).** This task carries the column in a next-ordinal guarded block, because this task is the column's only writer and a NOT NULL column must not exist ahead of a writer (D-010-5; the shipped `0004-worktree-lifecycle.ts` is not amended). The block is the **documented table-rebuild shape, not `ALTER TABLE … ADD COLUMN`**: SQLite's ALTER documentation states unconditionally that an added NOT NULL column must carry a non-NULL default, the empty-table allowance the bundled 3.53.0 engine exhibits is undocumented behavior no citation supports, and a load-bearing migration does not ride undocumented behavior (Codex PR #346 round 1 — the References row cites the procedure). The guarded block: (1) `CREATE TABLE` the successor under a temporary name with the full canonical DDL — `checkout_root TEXT NOT NULL` in the schema doc's declared position, no default; (2) `INSERT INTO … SELECT` enumerating the predecessor's columns and **omitting `checkout_root`** — any pre-existing row fails the engine-enforced NOT NULL at copy time, so the fail-closed-by-the-engine property survives with fully documented semantics (and the copy is in fact empty everywhere, because no production writer of `run_execution_contexts` has ever shipped — the only pre-existing writes are test fixtures); (3) `DROP TABLE` the predecessor; (4) `ALTER TABLE … RENAME TO run_execution_contexts`; (5) recreate `idx_run_execution_contexts_workspace` — the lang_altertable §Making Other Kinds Of Table Schema Changes procedure, executed inside the runner's ordinary `BEGIN IMMEDIATE` migration transaction. The procedure's `PRAGMA foreign_keys=OFF` step is deliberately not taken — that pragma is a no-op inside a transaction, and it is unnecessary here because no schema object references the rebuilt table (no inbound foreign key, no trigger, no view — the trigger trio lives on `session_events`), the exposure that step exists to protect; the migration test asserts `PRAGMA foreign_key_check` is clean post-rebuild to pin that reasoning. No sentinel default is introduced; a sentinel would let a row claim a checkout root it never had. Why the value is captured here rather than read later: this is the last moment the tree is guaranteed to exist under a caller that knows which workspace it belongs to, which is exactly the `git_common_dir` argument one row over — the value must outlive the path it describes, since a turn may delete a nested `execution_root` before rollback reads it (CP-010-15's carrier chain, link 3).
  - **Tests:** non-repo pass-through; happy-path worktree run (root + context row + busy bracket + release on terminal; the busy mark lands only AFTER the root materializes — D-010-16 order, spy-ordering assertion); gate failure parks in `starting` with the typed cause and no busy leak (pre-mark failure has no mark to leak; post-mark failure releases); a concurrent second run on the same workspace refuses `workspace.busy`; the dirty-candidate explicit-reuse fixture surfaces `worktree.reuse_conflict`; an `on_run_complete` clone retires at terminal and the workspace returns to `provisioning`; the derived branch name matches the ratified rule (spy on the prepare call); the migration applies on a database already at version 4 and is idempotent under the runner's guard; the rebuild copy refuses a pre-existing row — a fixture that seeds a predecessor `run_execution_contexts` row before the guarded block runs asserts the engine-enforced NOT NULL failure at `INSERT … SELECT` time, the documented fail-closed arm — while `checkout_root` lands `NOT NULL` with no default in the schema doc's canonical `PRAGMA table_info` position, `idx_run_execution_contexts_workspace` survives the rebuild, and `PRAGMA foreign_key_check` is clean post-rebuild; a row omitting `checkout_root` is rejected — the shipped `git_common_dir` rejection arm's twin; the gate writes `checkout_root` from `workspaces.metadata.checkoutRoot` when present; an absent-key worktree-mode fixture derives the just-materialized worktree's own top level — never `canonical_root`, the two values differing in exactly that fixture — and heals the carrier (the workspace row's `metadata.checkoutRoot` written back, asserted post-gate); an absent-key branch-mode workspace bound to a nested subdirectory derives the enclosing top level rather than the nested `execution_root`, the two columns differing in exactly that fixture; a read-only-mode run also carries a non-NULL `checkout_root` (the column is outside the mode-conditional CHECK by design — D-010-5).
  - **Spec coverage:** Spec-010 line 42 (every repo-bound run binds exactly one canonical mode), Spec-010 line 43 (worktree default for repo-bound coding runs — the gate's derived-default path), Spec-010 line 56 (one dedicated worktree per active task), Spec-010 line 66 (blocked-in-setup parks in `starting`, interruptible), Spec-010 line 88 (per-run execution binding persisted), Spec-010 line 91 (busy for the run's duration; one holding run; typed refusal), Spec-010 line 107 (worktree created and bound before provider execution starts)
  - **Verifies invariant:** I-010-11, I-010-12, I-010-14
  - **Consumes:** `RunSetupGate` seam (`assertRunReady` + `onRunTerminal`) ← Plan-004 Phase 3 T3.10 (CP-010-9); `QueueItemCreateRequest.workspaceId?` ← Plan-004 Phase 1 T1.1 (api-payload §Plan-004); `markBusy`/`releaseBusy` ← Plan-009 Phase 2 T2.4 (CP-010-4); root prepare ← T2.4; clone retirement ← T2.3; `RunId` ← Plan-005 Phase 1 `packages/contracts/src/provider-driver.ts`
- **T3.3 — Mutation binder files (select, prepare, clone-prepare, clone-dispose).**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/repo-execution-mode-select.ts`, `packages/runtime-daemon/src/ipc/handlers/repo-execution-root-prepare.ts`, `packages/runtime-daemon/src/ipc/handlers/repo-ephemeral-clone-prepare.ts`, `packages/runtime-daemon/src/ipc/handlers/repo-ephemeral-clone-dispose.ts` (CREATE ×4), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND — barrel)
  - One file per method (NS-26 binder convention; `register<X>` + `<X>Deps` deps-closure): each registers its D-010-3 string with `{mutating: true}`, request/response schemas from Phase 1, domain callback into T3.1/T2.4/T2.3.
  - **Tests:** covered by T3.6.
  - **Spec coverage:** Spec-010 line 75 (select wire surface), Spec-010 line 76 (prepare wire surface), Spec-010 line 78 (clone-prepare wire surface), Spec-010 line 79 (dispose wire surface)
  - **Verifies invariant:** I-010-15
  - **Consumes:** `MethodRegistry` + `registry.register` ← Plan-007-partial (shipped Tier 1); schemas ← T1.2; services ← T2.3, T2.4, T3.1; method strings ← D-010-3; camelCase registration ← BL-142 (§Preconditions)
- **T3.4 — Query/lifecycle binder files (reuse-check, retire, status-read).**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/repo-worktree-reuse-check.ts`, `packages/runtime-daemon/src/ipc/handlers/repo-worktree-retire.ts`, `packages/runtime-daemon/src/ipc/handlers/repo-worktree-status-read.ts` (CREATE ×3), `packages/runtime-daemon/src/ipc/handlers/index.ts` (EXTEND)
  - Reuse-check and status-read register `{mutating: false}`; retire registers `{mutating: true}`. Status-read serves T2.5's projection.
  - **Tests:** covered by T3.6.
  - **Spec coverage:** Spec-010 line 77 (reuse-check wire surface), Spec-010 line 80 (retire wire surface), Spec-010 line 81 (status-read wire surface)
  - **Verifies invariant:** I-010-15
  - **Consumes:** as T3.3; projection ← T2.5; reuse validation ← T2.2
- **T3.5 — `worktreeClient.ts` typed SDK.**
  - **Files:** `packages/client-sdk/src/worktreeClient.ts` (CREATE), `packages/client-sdk/src/index.ts` (EXTEND)
  - `createDaemonWorktreeClient(transport)` factory (the Plan-009/NS-33 `createDaemonRepoClient` precedent): seven methods mapping 1:1 to D-010-3 strings, schema-validating requests before send and responses on receipt; no derivation, no retry-with-substitution.
  - **Tests:** covered by T3.7.
  - **Spec coverage:** Spec-010 line 82 (typed request/response schemas are the contract surface)
  - **Verifies invariant:** I-010-16
  - **Consumes:** `JsonRpcClient` transport ← Plan-007-partial SDK substrate (shipped); schemas ← T1.2; strings ← D-010-3
- **T3.6 — Registration + dispatch tests for the seven binders.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/worktree-handlers.test.ts` (CREATE)
  - Per-method: registers under the exact D-010-3 string; a request-schema violation rejects with the substrate validation error BEFORE the domain callback (spy-asserted); the response validates; mutating flags correct (5 true / 2 false); barrel exports complete.
  - **Tests:** the file IS the tests.
  - **Spec coverage:** Spec-010 line 82 (wire surface conforms to the ratified schemas)
  - **Verifies invariant:** I-010-15
  - **Consumes:** binders ← T3.3, T3.4; registry test harness ← Plan-007-partial precedent
- **T3.7 — SDK integration tests.**
  - **Files:** `packages/client-sdk/test/worktreeClient.integration.test.ts` (CREATE)
  - In-process daemon registry + real transport (NS-33 precedent): seven happy-path round-trips with branded-type preservation; a select→prepare worktree flow yields a root and a status-read row carrying provenance.
  - **Tests:** the file IS the tests.
  - **Spec coverage:** Spec-010 line 128 (AC2 — the contract distinguishes all four modes over the wire), Spec-010 line 130 (AC4 — reuse linkage observable via status read)
  - **Verifies invariant:** I-010-16
  - **Consumes:** SDK ← T3.5; binders ← T3.3, T3.4; services ← Phase 2
- **T3.8 — Typed error round-trips for the spec-named failure modes.**
  - **Files:** `packages/runtime-daemon/src/ipc/handlers/__tests__/worktree-handlers.test.ts` (EXTEND), `packages/client-sdk/test/worktreeClient.integration.test.ts` (EXTEND)
  - Wire-asserts the D-010-4 codes end-to-end: `worktree.branch_collision` (supplied-name collision), `worktree.reuse_conflict` (dirty-no-ack), `workspace.branch_mismatch` (branch-mode mismatch), `workspace.busy` (concurrent hold), `workspace.stale` (stale prepare refusal), `workspace.mode_unsupported` (capability refusal), `workspace.branch_name_required` (writable-mode wire prepare without a branch, D-010-19), `clone.prepare_failed`. Asserts no path echo in any error message (sanitization).
  - **Tests:** the file extensions ARE the tests.
  - **Spec coverage:** Spec-010 line 49 (substitution refusal is typed and observable), Spec-010 line 67 (dirty/incompatible requires explicit choice — refusal observable), Spec-010 line 69 (stale refusal observable), Spec-010 line 129 (AC3 — creation failure blocks with a typed error, never a mutation)
  - **Verifies invariant:** I-010-7, I-010-8, I-010-15
  - **Consumes:** `mapJsonRpcError` + `DaemonDomainError` projection ← BL-143 (§Preconditions — tracked-not-satisfied until the Plan-007-owned PR lands); error classes ← T2.2 `worktree-errors.ts`; codes ← D-010-4

### Phase 4 — Desktop execution-mode picker + worktree status UI

**Goal.** The `execution-mode-picker/` renderer subtree: explicit one-mutation mode switching, worktree/clone status with provenance, and the explicit reuse probe — all bridge-only projections with mock-bridge test coverage now (the Plan-002 P6 mock-bridge pattern on the shipped renderer vitest project) and the Tier-8 manual leg dispositioned.

**Preconditions.**

- [ ] Phase 3 merged
- [x] Plan-023-partial renderer substrate + bridge stub shipped (Tier 1); the renderer vitest project shipped with Plan-001 Phase 5 T5.2 and the per-suite mock-bridge pattern with Plan-002 Phase 6 T6.3 (test-now posture)
- [x] D-010-17 status-read + `lastError` carriers ratified (Tier-6 audit)

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced form of the boxes above (preflight Gate 5).
  - { type: plan_phase, plan: 010, phase: 3, status: merged }
```

#### Tasks

- **T4.1 — `ExecutionModePicker.tsx` + `ModeSwitchOptions.tsx`.**
  - **Files:** `apps/desktop/src/renderer/src/execution-mode-picker/ExecutionModePicker.tsx` (CREATE), `apps/desktop/src/renderer/src/execution-mode-picker/ModeSwitchOptions.tsx` (CREATE)
  - `ModeSwitchOptions` is presentational + bridge-free (D-010-18): renders `availableModes` / `restrictions` / current mode verbatim incl. the space-containing `"ephemeral clone"` token. `ExecutionModePicker` composes it: a capabilities read (`loading | loaded | error`) feeds the options; an explicit click fires exactly ONE `repo.executionModeSelect` (`idle | pending | resolved | rejected`) and renders the response verbatim (`state: 'provisioning'` renders as provisioning — the picker never sequences a prepare, I-010-17/D-010-14); the rejected branch surfaces the typed code verbatim.
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-010 line 75 (one selection mutation per explicit switch), Spec-010 line 49 (unavailable modes render as restrictions, never silently substituted)
  - **Verifies invariant:** I-010-17, I-010-18, I-010-20
  - **Consumes:** `repo.executionModeCapabilitiesRead` ← Plan-009 Phase 3 (D-009-1); `repo.executionModeSelect` ← Phase 3 (D-010-3); `ExecutionModeSelectRequest`/`Response` + `ExecutionMode` ← Phase 1; `window.sidekicks.daemon.call` ← Plan-023-partial (shipped stub; live ← Tier 8 per CP-010-10); ambient `window.sidekicks` type ← Plan-002 Phase 6 T6.0 (shipped — `apps/desktop/src/renderer/src/sidekicks-bridge.d.ts`)
- **T4.2 — `WorktreeStatusView.tsx`.**
  - **Files:** `apps/desktop/src/renderer/src/execution-mode-picker/WorktreeStatusView.tsx` (CREATE)
  - Snapshot-read `repo.worktreeStatusRead` (`loading | loaded | error` — the NodeRoster posture precedent) rendering EVERY worktree + clone row verbatim: state, branch, provenance (`createdBySessionId`, `createdByRunId`), clone policy + `expiresAt` (no client expiry math — I-010-20); `failed`/`retired` rows render labeled, never filtered (I-010-19). The stale owning-workspace context renders the write-blocked label with `lastError` when `repo.workspaceList` carries it (D-010-17). Subscribes to the `workspace.*` + five `worktree.*` daemon events as OPAQUE re-read signals (payload never decoded — the Plan-009 WorkspaceListView precedent); V1 accepts no push signal for TTL retirement (freshness rides re-reads, D-010-11 disposition).
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-010 line 81 (status surface exposes records, lifecycle, provenance), Spec-010 line 123 (provenance survives retirement — rendered for retired rows), Spec-010 line 90 (daemon-owned projections rendered verbatim)
  - **Verifies invariant:** I-010-19, I-010-20
  - **Consumes:** `repo.worktreeStatusRead` ← Phase 3 (D-010-3); `WorktreeStatusReadRequest`/`Response` ← Phase 1; `repo.workspaceList` (+`lastError?`) ← Plan-009 Phase 3 + Phase 1 T1.3 (D-010-17); `daemon.subscribe` + `Unsubscribe` ← Plan-023-partial (shipped stub)
- **T4.3 — `WorktreeReuseCheckView.tsx` + barrel.**
  - **Files:** `apps/desktop/src/renderer/src/execution-mode-picker/WorktreeReuseCheckView.tsx` (CREATE), `apps/desktop/src/renderer/src/execution-mode-picker/index.ts` (CREATE — barrels the three views; App-shell composition is Plan-023's per CP-010-10)
  - Probe view: explicit branch-name input → `repo.worktreeReuseCheck` → renders the singular candidate verdict verbatim (`isClean`, `compatible`, `reason`); NO auto-proceed — the view surfaces the verdict; the explicit reuse decision (naming + dirty acknowledgement) belongs to the caller flow (I-010-17; the view never fires prepare).
  - **Tests:** covered by T4.4.
  - **Spec coverage:** Spec-010 line 77 (reuse check reports branch, cleanliness, compatibility), Spec-010 line 67 (dirty/incompatible surfaces for explicit user choice), Spec-010 line 122 (no implicit-reuse magic)
  - **Verifies invariant:** I-010-17, I-010-20
  - **Consumes:** `repo.worktreeReuseCheck` ← Phase 3 (D-010-3); schemas ← Phase 1; bridge ← Plan-023-partial (CP-010-10)
- **T4.4 — Mock-bridge component test suites.**
  - **Files:** `apps/desktop/src/renderer/src/execution-mode-picker/__tests__/ExecutionModePicker.test.tsx`, `apps/desktop/src/renderer/src/execution-mode-picker/__tests__/WorktreeStatusView.test.tsx`, `apps/desktop/src/renderer/src/execution-mode-picker/__tests__/WorktreeReuseCheckView.test.tsx` (CREATE ×3)
  - Per-suite mock `window.sidekicks` bridge (Plan-002 P6 / Plan-009 Phase 4 precedent): the picker fires exactly one select per click + renders provisioning verbatim + restriction-disabled options; the status view renders the all-states fixture incl. failed/retired + `lastError` labeling + re-read on event signal; the reuse view renders dirty/incompatible verdicts + never auto-fires prepare; every rejected branch surfaces the typed code.
  - **Tests:** the suites ARE the tests.
  - **Spec coverage:** Spec-010 line 128 (AC2 — all four modes distinguished in the rendered contract), Spec-010 line 130 (AC4 — reuse linkage rendered from daemon truth)
  - **Verifies invariant:** I-010-17, I-010-18, I-010-19, I-010-20
  - **Consumes:** views ← T4.1, T4.2, T4.3; vitest `renderer` project ← Plan-001 Phase 5 T5.2 (shipped Tier 1 — `apps/desktop/vitest.config.ts`, happy-dom environment, `globals: true`, include glob auto-discovers these files); per-suite mock-bridge install/teardown pattern ← Plan-002 Phase 6 T6.3 (shipped — duplicated per suite per the standing directive)
- **T4.5 — Tier-8 manual-verification disposition record.**
  - **Files:** this plan §Test And Verification Plan (the manual row below)
  - The "manual verification of worktree lifecycle from create through retire" row is executable only when Plan-023 Tier 8 wires the live bridge; per the Plan-003 T5.4 / Plan-009 T4.5 precedent the row is dispositioned NOW (deferred-to-Tier-8, tracked on the Plan-023 remainder) rather than silently skipped.
  - **Tests:** n/a (disposition record).
  - **Spec coverage:** Spec-010 AC1 (line 127 — end-to-end leg is the Tier-8 manual scope)
  - **Verifies invariant:** none (disposition record — the manual end-to-end leg executes at Plan-023 Tier 8)
  - **Consumes:** live `window.sidekicks` backend ← Plan-023 Tier 8 remainder (tracked-not-satisfied — CP-010-10)

### Phase 5 — Turn-snapshot service

**Goal.** The daemon-side turn-snapshot service (`git/turn-snapshot-service.ts`) implementing `Spec-010 §Turn-Boundary Snapshots`: per-turn worktree snapshot **capture** under the `refs/sidekicks/runs/<runId>/epoch-<E>/turn-<N>` namespace (temp-index recipe, create-only per-epoch ref writes), the **restore-to-ref** op carrying the full fail-closed restore contract with lineage-walk target resolution (consumed by Plan-004's `rollback` intervention), and **retention prune** with the ephemeral-clone-disposal boundary. This is the daemon-side file-restore leg the run time-travel feature (#19) requires; its code ships as the campaign's B23 bundle (Task 26) after Plan-010's W2.5 promotion and must merge before Plan-004 dispatches rollback (the Phase-3B explicit-label supplement since the NS-69 PR #346 round-1 re-home). The production caller side — the run engine's turn-boundary `captureTurnSnapshot` call-site, the execution-root / epoch / `epochLineage` resolution, and the rollback preflight → conversation → restore sequencing — is B9-owned (Task 13); this Phase authors only the callee service (CP-010-12).

**Preconditions.**

- [x] `Spec-010 §Turn-Boundary Snapshots` ratified — the B21 amendment (2026-07-06), re-promoted `approved` 2026-07-18 via the campaign's W1.5 batch spec gate (Task 28); the `epoch-<E>` ref segment and the disposed-clone / post-snapshot-commit rulings landed via campaign B2 (Spec-004 + Spec-010)
- [x] Phases 1 and 2 merged — satisfied: Phase 1 2026-07-26 via PR #253 (squash `f3c9947`), Phase 2 2026-08-08 via PR #297 (squash `3e98412`). Phase 1 for the execution-root contract shapes (the `run_execution_contexts` table + `execution_root` column, D-010-5 / T1.3) and Phase 2 for the hook-neutralized git-invocation discipline (D-010-10) the service reuses per-invocation. **No Phase-3 / Plan-004 dependency:** the capture and restore legs take the execution root as a caller-resolved parameter and never query `run_execution_contexts` themselves — root resolution, epoch supply, and `epochLineage` derivation are the B9-authored call-site's job (CP-010-12). The prune leg's `run_execution_contexts.released_at` read (T5.3) is a Phase-1 schema column (D-010-5 / T1.3); its runtime population by the T3.2 gate is a data-flow, not a build edge — an un-terminated run simply stays unprunable. Build order is therefore Phases 1/2 → Phase 5 (B23) → Plan-004 Phase 3 → Plan-010 Phase 3 → Plan-009 Phase 2B → Plan-004 Phase 3B (rollback dispatch — the cluster re-homed to the explicit-label supplement at the NS-69 PR #346 round-1 fold), acyclic — a Phase-3 precondition here would have deadlocked the as-designed order, in which rollback rode Plan-004 Phase 3 itself and the §5 preamble ordered that phase after this one; post-re-home the rollback consumer sits at Phase 3B, still strictly after this phase
- [x] `<E>` execution-epoch source ratified — `Spec-004 §Required Behavior` (0 before any rollback, advanced with each accepted `run.rolled_back` — a confirmed conversation rewind, position-mismatch-at-the-recorded-floor and the same-position recovery's convergence no-op included, regardless of file-leg disposition; caller-supplied by the Plan-004 run engine, campaign B2)

```yaml
preconditions:
  # Machine-enforced subset (preflight Gate 5); the checkbox rows above stay
  # the human-tracked record. Deliberately no Phase-3 / Plan-004 entry — per
  # the Phases-1-and-2 bullet, a Phase-3 gate here would have deadlocked the
  # Phases 1/2 -> Phase 5 (B23) -> Plan-004 Phase 3 -> Plan-010 Phase 3
  # -> Plan-009 Phase 2B -> Plan-004 Phase 3B order (rollback re-homed to
  # the supplement at the NS-69 PR #346 round-1 fold).
  - { type: plan_phase, plan: 010, phase: 1, status: merged }
  - { type: plan_phase, plan: 010, phase: 2, status: merged }
```

#### Tasks

- **T5.1 — Turn-boundary snapshot capture (`turn-snapshot-service.ts` create leg).**
  - **Files:** `packages/runtime-daemon/src/git/turn-snapshot-service.ts` (CREATE — self-CREATE in Plan-010's owned `git/` namespace per CP-010-7), `packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts` (CREATE)
  - `captureTurnSnapshot({executionRoot, checkoutRoot, runId, epoch, turnOrdinal, mode})` implements the `Spec-010 §Turn-Boundary Snapshots` capture recipe verbatim. `checkoutRoot` is the caller-supplied normalized root the pipeline actually addresses (CP-010-12's seventh input, added by the 2026-08-17 amendment; the caller reads it from `run_execution_contexts.checkout_root`), and capture — which by definition runs while the tree exists — re-verifies it against the live tree and fails the capture typed on disagreement rather than re-deriving it. **The shipped signature is still the five-field form:** this parameter joins it in the Phase-2B PR that executes the CP-010-15 EXTEND under Plan-009 T-009-2B-4, together with the code comments and tests that name the old shape — not before, the CP-010-14 convention. Until then the shipped behavior remains lawful under the shipped per-directory execution roots. `mode` is the run's execution mode — the `ExecutionMode` union (`read-only` / `branch` / `worktree` / `ephemeral clone`, Plan-009's contract per CP-010-1, the literal set D-010-5's `execution_mode` CHECK pins), resolved by the caller from the same `run_execution_contexts` row it reads the root from (CP-010-12); the callee self-guards on it, so a `read-only` root returns a typed no-op that creates no object and no ref, and only `worktree` / `branch` / `ephemeral clone` reach the pipeline. One base OID is resolved at entry — `<base>` = `git rev-parse HEAD`, used for **both** the tree base and the recorded parent so the snapshot stays self-consistent if `HEAD` moves mid-capture (`git commit-tree -p HEAD` re-resolves the symbolic ref independently, so passing symbolic `HEAD` to both legs could record an old-`HEAD` tree under a new-`HEAD` parent — empirically confirmed on git 2.50.1). The out-of-worktree temp-index pipeline (`GIT_INDEX_FILE` at a scratch index outside the worktree → `git read-tree <base>` → `git ls-files -co --exclude-per-directory=.gitignore -z | git -c core.autocrlf=false -c core.safecrlf=false -c core.attributesFile=/dev/null update-index --add --remove -z --stdin` with `GIT_ATTR_NOSYSTEM=1` (`core.safecrlf=false` pins the veto rather than a conversion — present-or-pinned-false stage the identical tree, so what a host `core.safecrlf=true` adds is a fatal that would make snapshot availability a host-config question; check-in leg only, the restore checkouts never consulting it) → the untracked-embedded-repo normalization pass (a non-ignored embedded git repository surfaces in `git ls-files -o --exclude-per-directory=.gitignore` as a single trailing-slash directory entry that `update-index --add` silently drops — `Ignoring path nested/` — so the bare pipeline would omit a repo that existed at the boundary; for each such entry resolve the embedded `HEAD` with `git -C <path> rev-parse HEAD` and record a `160000` gitlink via `git update-index --add --cacheinfo 160000,<sha>,<path>` — [git-update-index](https://git-scm.com/docs/git-update-index) documents the `<mode>,<object>,<path>` form as a direct index insert, and the `160000` gitlink is git's superproject submodule representation ([gitsubmodules](https://git-scm.com/docs/gitsubmodules) names the gitlink tree entry; the `160000` mode is empirically confirmed on git 2.50.1) — matching porcelain `git add -A`'s embedded-repo gitlink ([git-add](https://git-scm.com/docs/git-add) `--no-warn-embedded-repo` documents that `git add` warns when adding an embedded repository; the advice toggle is `advice.addEmbeddedRepo`, [git-config](https://git-scm.com/docs/git-config)); a commitless embedded repo with an unborn `HEAD` cannot be a gitlink, so skip it and enumerate it in the capture diagnostic — `git add -A` itself hard-fails that input (`does not have a commit checked out`), so skipping honors capture-never-blocks; a **second** skip class takes the same typed channel — an embedded repository whose `HEAD` hex length disagrees with the superproject's object format cannot be inserted as that superproject's gitlink, and porcelain does not hard-fail that input, it **fabricates** the gitlink OID (zero-padding a sha1 `HEAD` to 64 hex under a sha256 superproject, truncating a sha256 `HEAD` to 40 in the mirror case), so skip-and-enumerate is degrade-honestly against fabricate-or-fail; the trailing-slash `ls-files -o` listing, the `update-index --add` silent drop, the unborn hard-fail, and the mixed-format fabrication are all empirically confirmed on git 2.50.1 and pinned by the T5.1 tests) → `git write-tree` → `git -c i18n.commitEncoding=utf-8 commit-tree <tree> -p <base> -F -` (the fixed message streamed on a daemon-written-and-closed stdin — the 2026-08-09 amendment's transport correction, converted at the shipped call site by T6.1; message bytes and OID unchanged), plus a second trailer paragraph in the stream carrying the sorted-JSON `Skipped-Embedded-Repositories:` trailer — written **only** when the skip list is non-empty, so a capture that skipped nothing mints a byte-identical message and OID to the trailerless form, and the message stays a pure function of project state, which is what the spec's fixed-message determinism clause requires), stamped with the daemon-owned six-var host-independence env set — `GIT_AUTHOR_NAME`/`GIT_AUTHOR_EMAIL`/`GIT_AUTHOR_DATE` + `GIT_COMMITTER_NAME`/`GIT_COMMITTER_EMAIL`/`GIT_COMMITTER_DATE` at a fixed UTC offset (author/committer dates are OID inputs, so ident env alone would still leak host wall-clock timezone) — then the create-only `git update-ref --no-deref refs/sidekicks/runs/<runId>/epoch-<E>/turn-<N> <commit> ""` (compare-and-swap against ref absence; `--no-deref` is load-bearing, not decoration — git transfers a **symbolic** ref's update, must-not-exist check included, onto its referent, so an unflagged create against a dangling in-namespace symref plants an arbitrary branch at the snapshot commit and reports success, empirically confirmed on git 2.50.1). `<E>` is the run's execution epoch (`Spec-004 §Required Behavior` — 0 before any rollback, advanced with each accepted `run.rolled_back`, a confirmed conversation rewind regardless of file-leg disposition; supplied by the caller, never derived here). Idempotence is **per-epoch**: a retried or duplicated capture of the same `(runId, epoch, turnOrdinal)` finds the ref already written and returns the recorded OID as idempotent success, while a post-rollback re-execution's identical `turnOrdinal` mints a fresh ref under its own `epoch-<E>` segment (the reused position ordinal never collides with, or silently resolves to, the superseded epoch's tree). Capture failure emits an OTel diagnostic and **never blocks or fails the turn** — the turn boundary completes regardless; snapshots are a recovery convenience, not a turn gate. The single-base-OID resolution and the embedded-repo normalization, like the T5.2 checkout-conversion pins, are spec-mirrored: the 2026-07-20 `Spec-010 §Turn-Boundary Snapshots` errata batch folded both into the capture bullet (whose recipe previously wrote symbolic `HEAD` in both legs and asserted `git add -A` equivalence tacitly assuming no untracked embedded repositories), so recipe and spec are in lockstep. The 2026-08-09 errata batch extends that lockstep to what this phase measured on the way in — the second embedded-repository skip class and the skip trailer above, the `--no-deref` create spelling, and, as a **recorded residual rather than a closure**, the sparse-execution-root capture loss the spec's capture bullet characterizes: its two candidate closures (refuse capture in a sparse root, or read the sparse patterns so the staged tree retains the base's out-of-cone content) were a parked Plan-010 adjudication that batch deliberately left open — settled 2026-08-09 for sparse-aware staging by the sparse-root capture-closure amendment (the spec's capture bullet now carries the closure contract; Phase 6 / T6.1 below implements it under I-010-24). Every git invocation is hook-neutralized per D-010-10 (empty `core.hooksPath` plus `core.fsmonitor=false`, `execFile` argv-only, no shell).
  - **Tests:** real-git temp-dir repos (no mocks): capture writes the epoch-namespaced ref whose commit's first parent is `HEAD`; the staged tree is byte-identical to `git add -A` under identical inputs including non-ignored untracked files (a tree-level equivalence — both legs apply in-tree `.gitattributes` conversion identically, so it holds even for conversion-affected paths; the restore-side worktree round-trip is scoped in T5.2); a non-ignored untracked embedded git repository is captured as a `160000` gitlink recording its embedded `HEAD` OID, with the `git add -A` tree-equivalence assertion extended to that committed-embedded-repo fixture; a commitless embedded repo (unborn `HEAD`) is skipped and enumerated in the diagnostic rather than captured or thrown (the input `git add -A` hard-fails, so equivalence is intentionally not asserted there); ignored untracked paths are excluded while a tracked file matching `.gitignore` is still captured; the recorded OID is stable across host `core.autocrlf` / `i18n.commitEncoding` / `core.excludesFile` variation (the pins hold); a duplicate capture of the same `(runId, epoch, turnOrdinal)` is idempotent (ref unmoved, recorded OID returned); a same-`turnOrdinal` capture under a new epoch mints a distinct ref; a `read-only` `mode` returns the typed no-op result and creates zero git objects and zero refs (object-count and ref-count unchanged across the call); a `HEAD` advance injected between the `read-tree` and `commit-tree` legs (via the test-seam command runner) still records a snapshot whose parent equals its resolved `<base>` — not the moved `HEAD` — so a later restore against the advanced `HEAD` draws the typed refusal; an induced capture failure emits the diagnostic without throwing into the turn-boundary caller.
  - **Spec coverage:** Spec-010 §Turn-Boundary Snapshots (capture temp-index recipe, epoch-namespaced create-only refs, writable-modes-only applicability, untracked-embedded-repo gitlink normalization and single-base-OID resolution, both folded into the capture bullet by the 2026-07-20 errata batch); Spec-004 §Required Behavior (execution-epoch definition)
  - **Verifies invariant:** I-010-21, I-010-22
  - **Consumes:** execution root + checkout root + runId + epoch + turnOrdinal ← caller (the B9-authored turn-boundary call-site — CP-010-12 — resolves the root and, per the 2026-08-17 amendment, the normalized checkout root from the same `run_execution_contexts` row a completed run-setup gate wrote, and supplies the epoch per `Spec-004 §Required Behavior`; the capture leg never queries the table itself and reads no `workspaces` row — only the T5.3 retention leg reads the table); `checkout_root` captured at context creation ← T3.2 (the carrier's third link, CP-010-15); hook-neutralized git-invocation discipline ← Phase 2 (D-010-10)
- **T5.2 — Restore-to-ref op + lineage resolution (`turn-snapshot-service.ts` restore leg).**
  - **Files:** `packages/runtime-daemon/src/git/turn-snapshot-service.ts` (EXTEND), `packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts` (EXTEND)
  - The restore path splits into a non-mutating resolver and a mutating applier so `Spec-004 §Required Behavior`'s whole-rollback rejection runs before the conversation leg. The two legs return **named exported result unions** — `TurnSnapshotResolution` from `resolveRestoreTarget` (an accepted resolution, or the typed `head_moved` / `no_snapshot` refusal variants) and `TurnSnapshotRestoreResult` from `restoreToTurn` (`restored` carrying the collision-overwrite + divergent-gitlink enumerations, the execution-time `head_moved` re-verify refusal, or `partial_restore` naming the failed step and carrying the same two enumerations for every effect applied before the failure) — the concrete symbols the B9-authored Plan-004 `rollback` tasks cite for the `files-partially-restored` / `files-unrestored` disposition mapping (CP-010-12); the enumeration fields are pinned name-identical to Plan-004's wire arms — `overwrittenIgnoredPaths` / `divergentGitlinks` on both `restored` and `partial_restore` (required, empty-when-none) and `failedStep` on `partial_restore` — so the T3.13 wire mapping is an identity, never a rename. `resolveRestoreTarget({executionRoot, checkoutRoot, runId, targetPosition, epochLineage})` is **non-mutating**: `checkoutRoot` is the caller-supplied normalized root the applier's whole-tree `read-tree --reset -u` addresses (CP-010-12, 2026-08-17 amendment) — restore **consumes** it as given and never re-derives it, which is the whole point of the carrier: by restore time the nested `execution_root` the turn was working in may have been deleted, so an upward walk from it would fail or, worse, land on an enclosing tree that is not the captured one. (As with T5.1, the shipped signature grows this field in the Phase-2B PR executing the CP-010-15 EXTEND under Plan-009 T-009-2B-4, not before.) It walks the caller-supplied `epochLineage` (an ordered `{epoch, rewindBase}` list the Plan-004 run engine derives from its durable epoch / intervention records per `Spec-004 §Required Behavior` — B9-authored, CP-010-12), resolves the owning ref, and checks the fail-closed precondition. **Lineage walk:** the owning epoch is the newest whose `rewindBase` is strictly below `targetPosition` (positions at or below an epoch's `rewindBase` inherit from its parent epoch, per `Spec-010 §Turn-Boundary Snapshots`); it resolves `refs/sidekicks/runs/<runId>/epoch-<owner>/turn-<targetPosition>`. **No fallthrough:** if that ref is absent (a failure-tolerant capture left a gap in the owning epoch), the resolver returns a typed **no-snapshot refusal** — never a fallthrough to a stale same-ordinal ref in a superseded parent epoch (a failed epoch-1 capture at position 6, rewind base 5, must not resolve epoch-0's `turn-6`). **Fail-closed HEAD precondition:** the current `HEAD` must equal the resolved snapshot's recorded first parent (`<verified-snapshot-OID>^` — the same object the applier's checkout names, so check and act cannot diverge); a branch history advanced past the snapshot draws a typed refusal — never an anti-diffed partial restore (post-snapshot branch commits are Spec-011's reviewable surface; rewind/revert is outside this recipe). `restoreToTurn(resolution)` **binds** a resolver result and, under the exclusive execution-root hold at execution time, **re-verifies the HEAD precondition** (a TOCTOU guard — `HEAD` may move between resolve and dispatch; a moved `HEAD` refuses with no mutation) before the pinned three-step: `git -c core.autocrlf=false -c core.eol=lf -c submodule.recurse=false -c core.attributesFile=/dev/null read-tree --reset -u <verified-snapshot-OID>` (with `GIT_ATTR_NOSYSTEM=1` in the environment; `core.eol=lf` is checkout-leg-only and load-bearing only under the `core.autocrlf` pin — a host `core.autocrlf=true` writes CRLF whatever `core.eol` says, and under the pin an unpinned host `core.eol=crlf` restores CRLF for LF-captured content; `lf` not `native`, which is the host's own answer and would restore CRLF on Windows, while the check-in leg omits the knob because either value stages the identical tree OID; the operand is the commit OID `resolveRestoreTarget` verified, never a ref name re-read at application time, so the object the precondition checked and the tree-ish actually applied cannot be two different commits) → the untracked-delete pass (`git ls-files -o --exclude-per-directory=.gitignore -z`; unlink listed files, recursively remove trailing-slash nested-repo directory entries, prune emptied directories) **repeated to a fixpoint** (a turn-created untracked `.gitignore` shields what it ignores through the pass that deletes it — a single pass under-deletes) → the closing `git read-tree --reset <verified-HEAD-OID>` (index-only, no `-u`, and against the OID the precondition verified rather than the literal name, closing the same check-then-act window: captured-untracked files return to untracked status, tracked edits to unstaged modifications, no fabricated staged additions). The `core.attributesFile=/dev/null` + `GIT_ATTR_NOSYSTEM=1` pins **extend the capture leg's round-7 host-config-independence class to the checkout-conversion path** — the `read-tree --reset -u` leg smudges snapshot blobs to worktree bytes, so a host `*.txt eol=crlf` in a user/system attributes file would otherwise restore different bytes than were captured; in-tree `.gitattributes` stays honored (a project declaration inside the restored tree): attribute-affected paths therefore restore to git-canonical worktree form — identical to any porcelain checkout of the project, not necessarily the pre-capture raw bytes — and content under a non-invertible in-tree clean/smudge filter is out of the byte-fidelity contract (the snapshot preserves exactly what `git add -A` preserves, the spec's own equivalence; `Spec-010 §Turn-Boundary Snapshots` makes no universal raw-byte claim). The 2026-07-20 `Spec-010 §Turn-Boundary Snapshots` errata batch folded these checkout-conversion pins into the restore bullet (which previously pinned only `core.autocrlf` + `submodule.recurse`), so plan and spec are in lockstep. An ignored untracked path colliding with a snapshot-tracked path is overwritten by the `read-tree --reset -u` leg and **enumerated in the restore result** (never silent loss) — where colliding is the three measured shapes the spec's Scope bullet now records (the snapshot tracks a file at the ignored path; a snapshot-tracked **file** sits at an ancestor of it, taking the directory beneath whole; or the ignored path is an ancestor **directory** of a snapshot path, so the ignored file there is unlinked), with segment-wise ancestry, trailing-slash embedded-repo entries enumerated slash-stripped through the first two shapes only, and `160000` gitlinks seeding the required-directory set so the third shape fires at a gitlink's own path or an ancestor of one while the first two never do; an ignored **directory** at a gitlink path is left intact and reports through the divergent-gitlink enumeration instead, and ignored content beneath a materialized gitlink is neither enumerated nor deleted; the submodule gitlink boundary holds (superproject `160000` gitlinks only, `submodule.recurse=false` pinned, each divergent gitlink enumerated). Because T5.1 now records a captured untracked embedded repository as a `160000` gitlink, the untracked-delete pass no longer lists that directory at restore (it is tracked in the restored index) — the deletion-loss path is closed by construction; a snapshot-recorded gitlink whose working copy is absent at restore materializes as an empty directory (`submodule.recurse=false`; the interior object store is unrecoverable and out of contract per the spec's submodule stance) and is enumerated per the divergent-gitlink convention, while an embedded repo created after the snapshot boundary is still removed by the untracked-delete pass (it did not exist at the captured turn). A mid-sequence restore failure returns the convergent `partial_restore` result — `failedStep` naming the failed command plus the two enumerations covering **every effect applied before the failure, the failing command's own partial writes included** (`read-tree -u` updates the working tree as it applies, not transactionally — a failure on a later path leaves earlier collision overwrites on disk, empirically confirmed on git 2.50.1 via a required-smudge-filter failure): the callee derives the prospective collision-overwrite and gitlink-divergence sets before mutating and reports the on-disk-observed subset at failure — observation, not per-step bookkeeping — so both enumerations are empty on two distinct readings — a failure before any mutation, and equally a failure after a checkout that rewrote the whole worktree when the snapshot tree held no colliding ignored path and no gitlink — and both-empty therefore never reads as nothing-was-mutated; they are effect **classes**, not a census, and `failedStep` is what names how far the sequence got (recovery is a fresh restore to the same target re-running to the fixpoint, convergence being a property of the commands rather than a promise that recovery succeeds: resolution now refuses two shapes — an index-close failure under a since-moved `HEAD`, and a failed step whose ref the T5.3 sweep has since pruned); Plan-004's `rollback` maps it to the distinct `files-partially-restored` intervention disposition, never collapsed into `files-unrestored` and never empty-washed. Plan-004's `rollback` intervention (campaign B9) owns the pre-dispatch validation, the exclusive execution-root tenancy the re-verify runs under, and the conversation leg per `Spec-004 §Required Behavior`. **Prune↔restore interleave — resolution freezes the target.** `resolveRestoreTarget` records the snapshot's commit **OID** and every mutating leg names that OID, so a T5.3 sweep deleting the ref **inside** the resolve→application window does not starve the checkout (the commit object outlives its last name until `gc` reclaims it, and the restore completes normally), while a sweep landing **before** resolution draws the ordinary typed no-snapshot refusal with nothing applied. The composite third shape is recorded with the service header's retention residuals: the deletion costs nothing until some unrelated step fails, at which point that `partial_restore` is terminal for its target because the retry's resolution now finds no ref — so the ref named on a result is a correlation handle, not a live one. **Skipped-embedded-repository preservation.** The restore reads the snapshot commit's `Skipped-Embedded-Repositories:` trailer back before mutating anything — an undecodable trailer fails the restore closed at the enumeration-derivation step, ahead of every write — and exempts each recorded path and its subtree (segment-boundary match) from the untracked-delete pass, which would otherwise remove it recursively because the snapshot tree holds no entry at a skipped path; both fixpoint termination checks therefore operate on the **deletable** subset (a check over the raw listing would never converge while a preserved repository keeps reappearing), an embedded repository the turn **created** is absent from the trailer and is still deleted (the discriminator is capture-time knowledge, not what is on disk), and the preserved set rides the `embedded-repositories-preserved` diagnostic rather than the restore result, whose shape is frozen for the T3.13 wire mapping. **Replace-ref pin.** Every leg that interprets a recorded object **id** — the destructive checkout, the delete pass's snapshot-tree listing, the lineage parent read, the trailer read, and T5.1's index seed — pins `-c core.useReplaceRefs=false`, because a hostile or leftover `refs/replace/<oid>` redirects those reads through the substitute while the `HEAD` guards still pass on the unreplaced ids (empirically confirmed on git 2.50.1); ref-**resolving** legs are measured unaffected, and the closing index reset deliberately **honors** the setting, since porcelain is redirected identically there and pinning it would leave the index disagreeing with what every other git command in that worktree calls `HEAD` — fabricated staged modifications, the precise failure that reset exists to prevent. The full host-config disposition for this service — pinned, deliberately honored, measured-irrelevant, and recorded-residual (T5.1's sparse-root capture loss among them until T6.1 converts that row to the sparse-aware-staging closure per the 2026-08-09 amendment) — is the closed table in the `turn-snapshot-service.ts` header: plan and spec state the contract, the header carries the leg-by-leg measurements behind it.
  - **Tests:** real-git temp-dir repos. `resolveRestoreTarget` is non-mutating — the worktree and index are byte-identical after both a refused and an accepted resolve. Full resolve→restore round-trip: worktree bytes byte-identical to the captured turn after restore for paths not subject to in-tree conversion attributes/filters (the conversion carve-out below); a `HEAD` advanced past the snapshot draws the typed refusal from the resolver with no worktree mutation, and a bound resolution whose `HEAD` moved between resolve and `restoreToTurn` draws the execution-time re-verify refusal (TOCTOU) with no mutation; the lineage walk picks the newest epoch whose `rewindBase` is below the target and inherits from the parent epoch at/below the rewind base across a two-epoch fixture; a gap in the owning epoch (a failed epoch-1 capture at position 6, rewind base 5) draws the typed no-snapshot refusal and never resolves epoch-0's `turn-6`; post-snapshot untracked files are deleted including the fixpoint case (a turn-created untracked `.gitignore` plus the directory it ignores both removed across two passes) while snapshot-declared ignored paths (`node_modules`) survive every pass; an ignored path colliding with a snapshot-tracked path is overwritten and enumerated; a global `*.txt eol=crlf` attribute (user/system attributes file) does not alter restored bytes (the host-config checkout-conversion pins hold), while an **in-tree** `*.txt text` attribute is deliberately honored — an untracked CRLF `*.txt` file (`61 0d 0a`) captures LF-normalized and restores to git-canonical LF (`61 0a`), so worktree byte-identity is asserted only for paths free of in-tree conversion (empirically confirmed on git 2.50.1); interior submodule dirty/untracked state survives untouched and a divergent gitlink is enumerated; a restore to a boundary where an untracked embedded repo existed preserves that directory (it is gitlinked in the snapshot, so the untracked-delete pass does not list it), a snapshot-recorded gitlink absent from the worktree at restore materializes as an enumerated empty directory, and an embedded repo created after the snapshot boundary is still deleted on restore; the closing index reset leaves no staged additions; a mid-sequence failure injected after the read-tree leg yields the `partial_restore` result naming the failed step with the leg's collision overwrite enumerated (never empty-washed); an in-command `read-tree` failure — a required smudge filter erroring on a later path after an earlier colliding ignored path was overwritten — still enumerates that overwrite; and a failure injected before any mutation carries both enumerations empty.
  - **Spec coverage:** Spec-010 §Turn-Boundary Snapshots (restore two-step plus index close, fail-closed HEAD precondition, fixpoint untracked-delete, collision-overwrite enumeration, submodule gitlink boundary, lineage-walk target resolution with no-fallthrough refusal; captured-embedded-repo gitlink survives the untracked-delete pass by construction and an absent gitlink materializes as an enumerated empty directory; checkout-conversion host-config neutralization and the partial-restore enumerations, both folded into the restore bullet by the 2026-07-20 errata batch); Spec-004 §Required Behavior (non-mutating pre-dispatch validation, then bound restore under exclusive execution-root tenancy)
  - **Verifies invariant:** I-010-21, I-010-23
  - **Consumes:** capture leg + epoch-namespaced ref layout ← T5.1; execution root + checkout root + `epochLineage` (the `{epoch, rewindBase}` ordering the walk consumes) ← the B9-authored caller (CP-010-12; the roots read from the `run_execution_contexts` row, the lineage derived from durable epoch / intervention records per `Spec-004 §Required Behavior`); `checkout_root` captured at context creation ← T3.2 (CP-010-15's carrier — the reason restore never needs the nested path to still exist); pre-dispatch validation + exclusive execution-root tenancy + conversation leg ← Plan-004's `rollback` intervention (campaign B9, Task 13); hook-neutralized git-invocation discipline ← Phase 2 (D-010-10)
- **T5.3 — Retention prune + clone-disposal boundary (`turn-snapshot-service.ts` prune leg).**
  - **Files:** `packages/runtime-daemon/src/git/turn-snapshot-service.ts` (EXTEND), `packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts` (EXTEND), `packages/runtime-daemon/src/bootstrap/index.ts` (EXTEND — the sanctioned retention-sweeper wiring call only, Plan-007-owned per cross-plan §2; a wiring call, not ownership)
  - No general run-retention owner exists in the V1 corpus — `Spec-006 §Retention Windows` governs event-log compaction and `Spec-015 §Retention` governs SQLite backup files, neither the per-run snapshot refs — so this service owns the mechanism for its own refs (the corpus-true home for a Plan-010 daemon-side duration is daemon config: the `ephemeral clone` TTL precedent in T2.3 is config, not wire). The retention window is a constructor-config default on the service; a run's snapshot refs (`refs/sidekicks/runs/<runId>/…`) become prune-eligible once `run_execution_contexts.released_at` (stamped at run terminal by the T3.2 gate) plus the window has elapsed. The mechanism is **window-based, not terminal-invoked**: `sweepPrunableRuns()` deletes every run whose window has closed, driven by (a) a **daemon-startup reconcile** that prunes runs whose windows elapsed while the daemon was down and (b) a **periodic sweep** on the daemon-owned cadence config; `pruneSnapshotsForRun(runId)` stays the idempotent per-run primitive the sweep calls, running its ref ops (`git for-each-ref refs/sidekicks/runs/<runId>/` → `git update-ref --no-deref -d` per ref — the flag mirrors T5.1's create side for the opposite failure: an unflagged delete resolves its compare-and-swap **through** a planted in-namespace symref and deletes the branch that symref points at, exiting 0 and reporting a clean prune, empirically confirmed on git 2.50.1) through `git --git-dir=<git_common_dir>` — the `run_execution_contexts.git_common_dir` captured at context creation — **never through `execution_root`**, because a `worktree`-mode root can be physically retired (T2.2) before the window closes while its refs persist in the shared common object store; a run whose recorded `git_common_dir` is missing or invalid at sweep time (the repo was removed) is skipped and enumerated in the sweep diagnostic, never fatal. **Clone-disposal boundary:** in `ephemeral clone` mode the snapshot refs live in the clone's own object store, so they additionally share the clone's disposal lifecycle — an `on_run_complete` clone disposal takes the refs with it (possibly before its retention window closes), and any later rollback of that run proceeds **conversation-only** (the file-leg disposition is recorded on the intervention outcome; Plan-004's `rollback` tasks own that detail — campaign B9). Neither disposal nor sweep is a retention violation: a disposed clone leaves nothing to restore into, and the sweep fires only after the window. A sweep that fires **inside** a live restore's resolve→application window is likewise not a violation — T5.2's resolution has already frozen the snapshot's commit OID, so the checkout completes against the object after its last name is gone; the interleave contract and its terminal composite shape are recorded on the T5.2 side. **Production wiring (sanctioned bootstrap edit):** the sweeper has no other trigger — CP-010-12 keeps retention out of the B9 caller and Task 26 (B23) restricts the code surface to Plan-010's files — so T5.3 also makes the one sanctioned wiring-call edit into `packages/runtime-daemon/src/bootstrap/index.ts` (Plan-007-owned; the Plan-026 `registerOnboarding*` precedent in cross-plan §2 — a wiring call, not ownership): construct the retention sweeper with the daemon-config window + cadence (the T2.3 clone-TTL config precedent), run the startup reconcile pass (async, non-blocking, diagnostic-on-failure, ordered after migrations), start the periodic interval, and dispose it on shutdown.
  - **Tests:** a terminal run whose window has **not** elapsed is retained (the terminal-but-still-retained case — refs survive until the window closes, not at terminal); a run whose `released_at` plus the window has elapsed is pruned by the sweep while a still-open run (no `released_at`) is retained; the daemon-startup reconcile prunes a run whose window elapsed while the daemon was down; a **retired-and-removed `worktree`** run (its `execution_root` physically gone, refs still in the common object store) is still pruned via `git_common_dir` from the surviving canonical repo, and a run whose recorded `git_common_dir` no longer exists on disk is skipped and enumerated (never fatal); an ephemeral-clone run's refs vanish with the clone at `on_run_complete` disposal (the sweep then finds nothing to delete); `pruneSnapshotsForRun` is idempotent (a second prune of an already-pruned run no-ops); a `bootstrap/index.ts` wiring integration test (fake clock) proves startup with expired windows reconciles and the periodic interval fires the sweep.
  - **Spec coverage:** Spec-010 §Turn-Boundary Snapshots (retention prune on window close — window as daemon config, the service owns the mechanism absent a run-retention owner; ephemeral-clone-disposal shares the clone lifecycle, post-disposal rollback conversation-only)
  - **Verifies invariant:** I-010-21
  - **Consumes:** capture leg + ref namespace ← T5.1; `run_execution_contexts.released_at` (run-terminal timestamp, the window anchor) + `run_execution_contexts.git_common_dir` (the ref-op git dir) ← T1.3 schema (`released_at` stamped at run terminal, `git_common_dir` at context creation, both by the T3.2 gate at runtime); retention-window + sweep-cadence defaults ← daemon config (the T2.3 clone-TTL precedent); `bootstrap/index.ts` wiring seam ← Plan-007 (cross-plan §2 — a sanctioned wiring call); ephemeral-clone `on_run_complete` disposal ← T2.3 `ephemeral-clone-service.ts`

### Phase 6 — Sparse-Root Capture Closure

**Goal.** Implement the sparse-aware-staging closure the 2026-08-09 `Spec-010 §Turn-Boundary Snapshots` capture-bullet amendment ratified — the settled form of the adjudication the Phase-5 errata batch parked — so a snapshot captured in a sparse-checkout execution root is a full tree byte-identical to porcelain `git add -A`'s in that root: out-of-cone cached entries persist at their live cached state over a live-index-seeded scratch index instead of being dropped and then deleted by restore, and boundary-time out-of-cone untracked and intent-to-add paths are recorded on the snapshot message's sparse-root trailer channel (unconditional in sparse roots — the empty set spells `[]`, the closure's vintage marker — and never written elsewhere). Restore gains no new worktree-mutating pipeline step — `read-tree --reset -u` already re-applies the sparse definition, and its one preparation, the recorded-boundary-set index pre-drop (intent-to-add and turn-staged entries alike), is index-only and instantly superseded — its additions being the untracked-delete pass re-keyed to the recorded boundary-time set at each entry's recorded kind width, the pre-mutation boundary-obstruction refusal (typed at `derive-enumerations`, nothing mutated — the 2026-08-10 naming amendment), and the diagnostic-channel reporting of the observed discarded materialized out-of-cone subset (the `TurnSnapshotRestoreResult` union stays frozen for Plan-004 T3.13). This is a discrete phase rather than a Phase-5 EXTEND deliberately: Phase 5's shipped manifest set stays intact, so `classifyPhaseShipment` keeps it `fully_shipped` and Plan-004's met `plan_phase, plan: 010, phase: 5, status: merged` leg — carried on the Phase-3B rollback supplement since the NS-69 PR #346 round-1 re-home — stays met.

**Preconditions.**

- [x] Phase 5 merged (the capture/restore/prune service this phase extends) — 2026-08-09 via PR #303 (squash `5bcf40c`)
- [x] Sparse-root capture-closure amendment restored `approved` — the `Spec-010 §Turn-Boundary Snapshots` capture bullet's 2026-08-09 sparse-aware-staging contract (detection, oracle, live-index seeding, boundary-set trailer + transport, delete-exemption + intent-to-add pre-drop, porcelain equivalence, materialized-path residual, observed-subset diagnostic) landed via this phase's authoring PR (#307), flipping this plan and Spec-010 `review`; **Delivered 2026-08-09:** the targeted readiness-audit delta (PR #308; §6 node NS-51) restored both in the same swap as its findings' amendments — the plan-level §Preconditions boxes and §Notes carry the full record — so T6.1 now dispatches on tier order plus this phase's `preconditions:` block

<!-- prettier-ignore -->
```yaml
preconditions:
  # Machine-enforced subset (preflight Gate 5); the restore box above has no
  # Gate-5 entry vocabulary (no entry type reads a spec/plan Status row) and
  # rides the gate's checkbox leg instead — any unchecked box halts — with
  # the 2026-08-09 flip window (held plan-wide by Gate 2's re-opened audit box
  # and Gate 7's Status `review` + paired-spec box) closed by the #308 restore.
  # A second such window opened 2026-08-10 with the boundary-obstruction refusal
  # naming amendment, which re-opened those same three legs plan-wide; Phase 6 is
  # already merged, so nothing here rode it, and that window closed the same day
  # by the boundary-obstruction restoring delta (§6 node NS-53).
  - { type: plan_phase, plan: 010, phase: 5, status: merged }
```

#### Tasks

- **T6.1 — Sparse-aware capture staging (`turn-snapshot-service.ts` capture-leg extension).**
  - **Files:** `packages/runtime-daemon/src/git/turn-snapshot-service.ts` (EXTEND), `packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts` (EXTEND)
  - Extend `captureTurnSnapshot` with the amended capture bullet's sparse-aware staging. **Detection:** the root is sparse exactly when `core.sparseCheckout` is true — the config bit alone, never conjoined with pattern-file existence: with the bit set and the rules file deleted or unreadable the worktree can remain pruned with `skip-worktree` bits retained and porcelain's tree still retains the out-of-cone entry (measured), so that state must route to the sparse arm, where the matcher's fatal `unable to load existing sparse-checkout patterns` becomes the typed capture failure — fail-closed, never a not-sparse misclassification into the unpartitioned pipeline, which would drop what porcelain retains (a stale pattern file under `core.sparseCheckout=false` stays not sparse — git ignores it, measured); detection is root-keyed and mode-agnostic — `branch` roots (the user's own checkout) and `worktree` roots alike, the latter inheriting sparsity whenever the canonical mount is sparse because `git worktree add` copies the current worktree's sparse-checkout patterns and worktree config ([git/git `53255916b7`](https://github.com/git/git/commit/53255916b7), shipped in git 2.36), while ephemeral clones start full (`git clone` copies no config — measured); a non-sparse root's capture stays byte-identical to the existing pipeline's — the `-F -` transport conversion touches every root's argv, measured OID-identical (the regression guard below). **Partition:** in a sparse root, the in-cone oracle is git's own sparsity matcher — `git sparse-checkout check-rules -z` ([git-sparse-checkout](https://git-scm.com/docs/git-sparse-checkout); plumbing since git 2.41, [git/git `00408adeac`](https://github.com/git/git/commit/00408adeac)), run in the execution root so it evaluates the worktree's live rules and mode, candidate paths fed on stdin, output meaning in-cone. The oracle runs strictly downstream of Detection, never as the detector — `check-rules` is fatal in a root with no sparse-checkout state (`fatal: unable to load existing sparse-checkout patterns`, measured), while under a stale pattern file with `core.sparseCheckout=false` Detection's not-sparse verdict alone governs (the existing pipeline runs; a disabled root's `check-rules` filters nothing — measured) — and the default live-rules form is deliberate over `--rules-file`, which interprets supplied rules as cone-mode directories unless an explicit `--no-cone` rides along and would therefore re-introduce a mode-detection failure mode the live form has git resolve itself. Availability floor, fail-closed: `check-rules` does not exist below git 2.41, and a failed oracle invocation in a detected sparse root — that shape included — fails the capture through the service's existing typed capture-failure envelope, never a silent degrade to the unpartitioned recipe (which would re-mint the measured loss); the result unions gain no arm, while the daemon-local capture-step vocabulary — unlike the restore's, not wire-pinned — grows two members slotted in execution order, `detect-sparse-root` and `check-sparse-rules`, rather than overloading an existing name, while the re-sourced seed read keeps the shipped `seed-index` member (the same pipeline position, a new source), so the diagnostic names the leg that failed. The same fail-closed rule governs the restore side: an oracle invocation the restore's sparse legs need — the materialized-set diagnostic derivation and the exemption's projection — that fails or is unavailable there (git downgraded below the floor between capture and restore, rules vanished, a root turned sparse since a non-sparse capture) refuses at `derive-enumerations` ahead of every write, the T5.2 undecodable-trailer precedent: the never-silently reporting obligation forbids restoring blind. Both listing sides route through the matcher: the cached side (`git ls-files -c -z` against the live-index-seeded scratch index) and the untracked side (the existing `-o --exclude-per-directory=.gitignore` listing — the gitignore machinery keeps its one job, ignored-file exclusion, and never doubles as the cone test). Gitignore-semantics exclude machinery (`git ls-files -c -i --exclude-from=<pattern-file>`) is refuted as the oracle: sparse patterns share gitignore syntax but not gitignore evaluation, which cannot re-include below an excluded parent, so on a non-cone `/*` + `!/a/b/` definition it calls git's own out-of-cone `a/b/` subtree in-cone — the over-included path enters the re-stat stream, is absent from the pruned worktree, and `--remove` silently drops it from the snapshot tree (measured on git 2.50.1: the exclude-keyed pipeline loses the path porcelain retains, while the check-rules-keyed pipeline is byte-identical to porcelain across cone, non-cone positive-pattern, top-level and nested negation, materialized-edit, out-of-cone-untracked, gitignored-in-cone, and staged-out-of-cone (`git add --sparse` then a cone shrink — the live-index-seed fixture) fixtures — equivalence stated under the pipeline's pinned exclusion and conversion inputs, the deliberate host-local-ignore divergence recorded in the spec's Scope bullet standing unchanged). **Seeding:** in a sparse root the scratch index seeds as a copy of the repository's live index, never `read-tree <base>` — the seed is serialized against writers by taking git's own index lock for the copy's duration — the per-root live index path resolved via `git rev-parse --git-path index` ([git-rev-parse](https://git-scm.com/docs/git-rev-parse)), since a linked worktree's index lives under the main repository's `.git/worktrees/<id>/` where a literal `.git/index.lock` fails against the gitfile or locks another worktree's index (and `worktree` is the default mode); that resolved path is the copy's source, and its sibling `<index>.lock` is created exclusively per git's lockfile protocol ([api-lockfile](https://github.com/git/git/blob/v2.50.1/Documentation/technical/api-lockfile.txt)), released without writing — because a reader alone never observes contention: a writer prepares its replacement index at the lock path and atomically renames it over `.git/index`, so an unlocked mid-boundary seed reads the pre-write index with exit 0 (measured on git 2.50.1: `ls-files` succeeds against the old index while the lock is held) and the racing staged state is silently omitted from a snapshot a later restore then erases; acquisition contention — a writer mid-flight — retries briefly and then fails the capture through the existing typed capture-failure envelope, and git's own index checksum keeps a torn or corrupt seed read an error rather than a silently wrong seed — porcelain updates no entry outside the cone ([git-add](https://git-scm.com/docs/git-add), `--sparse` the porcelain opt-in), so out-of-cone cached entries must persist at their **live cached** state: measured, a `git add --sparse`-staged modification surviving a cone shrink reaches porcelain's tree while the `<base>`-seeded tree reverts it — a divergence whose restore would destroy the staged state — and the live-index-seeded pipeline is byte-identical to porcelain in that state (an out-of-cone unmerged entry now fails `write-tree` typed exactly as porcelain refuses it, where `<base>`-seeding silently erased the conflict). Feed only the in-cone union into the `update-index --add --remove` re-stat stream: out-of-cone cached entries stay untouched at that live state, out-of-cone untracked files are never added — their **boundary-time path set** (the untracked listing's out-of-cone side, a partition the step already derives, plus out-of-cone **intent-to-add** paths — on-disk boundary content whose entries `write-tree` omits exactly as porcelain commit semantics do, measured; [git-add](https://git-scm.com/docs/git-add) documents `-N` as future-addition intent) **is recorded** — derived by subtracting the written tree from the candidate listing on the **listing's own bytes** (the byte-keyed listing bijection), never decoded-vs-decoded, because two distinct paths can decode alike and a decoded subtraction would silently subtract one boundary path away under the other's alias, costing its intent-to-add entry the restore-side pre-drop and destroying the only copy on disk; and the trailer transports those same bytes rather than a decoded rendering of them: its JSON strings are `latin1`-encoded, one code point per path byte, which is what makes the trailer a **byte record** rather than a text one — a `utf8` trailer would collapse every invalid sequence onto `U+FFFD`, and the destructive direction of that collapse is the restore-side pre-drop's snapshot-tree exclusion firing spuriously and leaving the checkout to delete the only copy — and which makes "sorted" **byte-lexicographic** by construction, a JS code-unit sort over `latin1` strings being a sort over the bytes; a key minted at capture is therefore the key matched at restore, and the one sanctioned exit from that byte-keyed world is composing a path for a human to read — as a second sorted-JSON **byte-record** trailer on the snapshot commit — key `Sparse-Boundary-Paths:` (the shipped sibling's contents-describing plural-noun convention), ordered after the skipped-repositories trailer whenever both are present; trailer bytes are OID inputs, so key and order are contract, not formatting — under the `Skipped-Embedded-Repositories:` conventions (single-line sorted JSON array, fail-closed on decode) with three deliberate divergences, the **encoding** one being the `latin1` byte record described above — the sibling's strings are decoded text, since a skipped-repository path is only ever read back to a human, while these strings are byte keys the restore matches on. **Kind-preserving:** a path `ls-files -o` reports with a trailing slash is recorded **with** that slash, where the sibling trailer records the slash-stripped name — the slash is the record that git refused to descend the directory, so capture could not enumerate its contents ([git-ls-files `--others`](https://git-scm.com/docs/git-ls-files#Documentation/git-ls-files.txt---others) is the untracked listing, and a trailing slash is the spelling git gives a directory-classified `other` entry, [documented at `--directory`](https://git-scm.com/docs/git-ls-files#Documentation/git-ls-files.txt---directory)) — and decode splits the recorded set once into file paths and directory paths, fail-closed as before; the format is unreleased before this task, so no vintage consequence rides the divergence (the absent-trailer refusal below is unaffected). **Unconditional:** in a detected sparse root the trailer is written **unconditionally** — an empty set spells `[]` — because it doubles as the closure's format marker; a trailer-absent message cannot prove vintage (a pre-closure sparse capture's truncated tree — the shipped Phase-5 loss a retained snapshot can still carry across the upgrade — shares it with a non-sparse capture's full tree, and tree inspection cannot decide fullness under cone edits), so restore in a detected sparse restore root **refuses** a trailer-absent snapshot typed at `derive-enumerations` ahead of every write — vintage undecidable, a possibly-truncated tree never destructively restored (the root-turned-sparse-since-a-non-sparse-capture shape draws the same refusal; absence cannot prove the safe vintage) — while in a non-sparse restore root an absent trailer decodes as the empty set (every non-sparse capture's shape, restored with no exemption and no phantom preservation), a present-but-undecodable one fails closed everywhere, and a legacy sparse snapshot restored into a since-widened non-sparse root retains the shipped pre-closure behavior — the recorded residual, message-undetectable; non-sparse roots never write the trailer, so the common case's no-trailer OID stability stands, the whole message streamed to `commit-tree` via `-F -` on a daemon-written-and-closed stdin rather than `-m` argv — an unbounded set never meets a platform argv bound such as Windows' ~32K command line; transport-only, message bytes and OID unchanged, and the shipped skipped-repository trailer shared the bound, so the switch closes the class — and the embedded-repo normalization pass runs over the in-cone untracked side only. Pattern-keyed, never `skip-worktree`-bit-keyed: git clears the bit the moment a path materializes while `git add -A` still refuses the edit (the index entry survives at `<base>`), so a bit-keyed filter diverges from porcelain exactly on materialized out-of-cone paths — the measured divergence that fixed the oracle choice. Under `index.sparse=true` the listing transparently expands the sparse index (an advisory on stderr; tolerated or silenced, never a failure). **Restore additions (one re-keying, one refusal, one diagnostic):** the restore's untracked-delete pass exempts exactly the snapshot's recorded boundary-time set at each entry's recorded **kind** width — **kind-exact** for a file entry, matching only a slash-free listing entry at that exact path, so turn content beneath a boundary file path is deleted like any other turn content and a turn-created embedded repository standing at a boundary FILE path is deleted rather than rescued by its slashed spelling — **spelling-tolerant across the segment-boundary subtree** for a trailing-slash directory entry, matching the restore listing's slash-free spelling of that directory as readily as its slashed one, because a directory git refused to descend is one capture could not enumerate, so path identity for that entry is its subtree: the sibling skipped-embedded-repositories semantic, whose concrete case is a boundary-time out-of-cone embedded repository the turn stripped of its `.git`, re-listed at restore as ordinary per-file entries an exact-path exemption would delete; every comparison in that pass runs on the entry's recorded bytes, and what keeps a lossy spelling from ever reaching the removal verb — whose seam is string-typed, the complete fix being `Buffer`-typed paths throughout and deliberately not taken while the seam stays three narrow mutation verbs — is the pass's own **pre-mutation round-trip guard**: every entry the pass is about to delete must re-encode from its decoded name back to its recorded bytes, and if any one does not, the **whole pass** refuses before removing anything (`partial_restore` at `delete-untracked`, the worktree exactly as the checkout left it), so a boundary file whose valid-UTF-8 name holds a literal `U+FFFD` can never be removed through a turn-created neighbour's decode alias — the accepted residuals are the survivable ones: a turn-created file **inside** such a directory is over-protected and survives rollback, and its deliberately retained mirror, a turn-created **file** standing at a recorded directory path, is exempted by the directory arm's equality case and survives too, while turn-created out-of-cone content anywhere else still deletes — membership is path identity fixed at capture, and the live sparse definition governs projection and diagnostics while the recorded set alone governs the exemption, so cone edits between capture and restore never convert boundary-time content into a deletion candidate — preserving boundary-time out-of-cone untracked content on disk (measured: the pass's `ls-files -o` candidate source does list such a file, and an unexempted pass would delete pre-existing boundary content the snapshot never held) while deleting a turn-created out-of-cone untracked file, absent from the set, exactly like its in-cone counterpart (rollback coherence — a blanket oracle exemption would leak turn-created content past rollback; an existing step re-keyed, no new worktree-mutating pipeline step) — with every live-index entry the recorded set covers at its **full recorded width** — a file entry's exact path and a directory entry's segment-boundary subtree, since taking the file half alone reads as a shape-of-the-data argument and is a hole: an index entry beneath a recorded boundary directory is exactly the staged out-of-cone content the checkout would delete — and that the snapshot tree does not itself hold dropped before the destructive checkout, both halves of that test keyed on the recorded bytes (the exclusion is built from the snapshot tree listing's own path bytes, so a decode-aliased tracked path can no more exclude a distinct boundary path from the drop than it could subtract one away at capture, while the exclusion itself stays: a path the snapshot tree holds is one the checkout will write, never one the drop may strand) — out-of-cone intent-to-add entries and boundary-time untracked paths the turn staged alike, `git add --sparse` converting one into an ordinary stage-0 entry an intent-to-add-only drop would miss and the checkout would then delete from disk — (index-only, and the very next command resets that index wholesale; without the drop `read-tree --reset -u` ([git-read-tree](https://git-scm.com/docs/git-read-tree), where `-u` is what carries the reset into the working tree) deletes the on-disk file of an entry absent from the target tree, measured — dropped, the file is plain untracked and the boundary-set exemption preserves it; the drop runs under the `derive-enumerations` step cursor for failure attribution — it is one `update-index` invocation writing the index once through git's lock-file protocol, so a failed drop leaves the live index unrewritten and the worktree untouched, keeping that step's shipped failure-before-any-mutation reading true for the Plan-004 consumer, where reporting the checkout step would falsely assert the destructive checkout was attempted; the frozen five-member restore-step vocabulary does not grow, the diagnostic detail names the drop (the shipped multi-reachable `close-index` precedent), and a drop that succeeded ahead of a later failed step is reported by that later step, whose earlier-effects-on-disk reading already covers the dropped entries); **ahead of that drop and of every write, refuse typed at `derive-enumerations` when the checkout itself would destroy a recorded boundary path** — the three destructive shapes the ignored-path collision taxonomy of `Spec-010 §Turn-Boundary Snapshots` measures, transferred whole because boundary paths meet the same `read-tree --reset -u` through the same mechanism: (a) a boundary FILE entry with materializing snapshot paths strictly beneath it (the checkout **unlinks the boundary file** standing at that path to make room for the directory those paths need), (b) a boundary DIRECTORY entry with a materializing blob exactly at its path, and (c) a boundary entry of either kind whose proper ancestor is a materializing snapshot blob — the shape a sparse root makes ordinary rather than exotic, since a `skip-worktree` out-of-cone blob leaves its path absent on disk, free for the turn to create a directory over while the index keeps the cached blob — each measured destructive at exit 0 on git 2.50.1 before the guard existed; two shapes are deliberately **admitted** rather than refused (a directory entry's strict descendants materializing, which the checkout merges into the preserved directory — the ignored-embedded-repository merge precedent — and a conflicting snapshot path the live cone leaves unwritten); materialization is asked of the same `check-rules` oracle on the same fail-closed floor (a rules-vanished sparse restore root refuses here as everywhere) and is unconditionally true in a non-sparse restore root, while `160000` gitlink displacers are exempt from that scoring — git never marks a gitlink `skip-worktree` (`ls-files -t` reports `H` for an out-of-cone one, measured) and the checkout creates its directory regardless, so oracle-scoring them would fail **open** exactly on the destructive case; the detail is bounded asymmetrically — every obstructing boundary path named, the operator's action item and bounded by the trailer the snapshot already carries, while displacers past the first three collapse to a count, a boundary file high in a tree being displaced by its whole subtree — and the refusal rides the shipped typed-failure envelope from the same pre-mutation position as the trailer-absent vintage refusal and the restore-side oracle floor — surfaced on the existing `partial_restore` arm naming `derive-enumerations`, a step failure inside the restore sequence rather than a pre-dispatch refusal: no result-union arm, no restore-step vocabulary growth, nothing on disk. Every path comparison the scoring makes — recorded-set membership, the proper-ancestor walk, and displacer keying alike — runs on the recorded bytes, and the guard's one named residual runs in the fail-**open** direction and is pre-existing rather than introduced by it: the presence probe addresses the filesystem at a boundary path's **decoded** spelling, that seam being string-typed, a corner unreachable on any filesystem that rejects non-UTF-8 names (APFS does, at `creat`). Disposition, ratified 2026-08-10: the typed refusal rather than the ignored-path restore-wins-with-enumeration ruling, whose scope is paths the snapshot contract excludes from the outset where recorded boundary paths are that contract's protected subject — the refusal being what keeps I-010-24's boundary-content survival clause literally true **across the displacement class** — the index pre-drop and the delete-pass exemption each stop one destructive mechanism, and neither reaches the case where a recorded boundary path and a materializing snapshot path want the same place on disk, which is why this third leg exists at all; a user-facing force-override is a possible future enhancement, deliberately not scheduled here; and before the destructive checkout in a sparse root, derive the materialized out-of-cone set (on-disk paths the oracle marks out-of-cone that the snapshot tree tracks — the bytes `read-tree` will discard) and report its **observed** discarded subset on the restore diagnostic channel under the service's observed-not-bookkept discipline (the partial-restore enumerations' type-aware-fingerprint precedent: each candidate's on-disk state recorded before the mutation, only actually-changed paths named, on completed and `partial_restore` alike) — `read-tree --reset -u` re-applies the sparse definition, discarding those bytes and re-setting `skip-worktree` (measured); the result unions are untouched. **Header true-ups and transport conversion (same file):** the disposition table's sparse recorded-residual row converts to the sparse-aware-staging closure row; the header's `in_place` exposure vocabulary is corrected — `in_place` names no member of the four-mode `ExecutionMode` union; the exposure names sparse execution roots: `branch` canonically, `worktree` by sparse inheritance from the canonical mount; and converting the shipped `commit-tree` call site from `-m` argv to the `-F -` stream is this task's named work, never an implication left to the delta audit — the git seam already carries the capability (the runner's per-invocation stdin buffer, written and closed on every invocation), the stream must be newline-terminated with paragraphs blank-line-joined (measured: the `-m` spellings and exactly that stream mint identical OIDs, an unterminated stream diverges — the non-sparse regression guard below pins the equivalence), and the runner's stdin-documentation comment (naming `update-index --stdin` the only stdin-supplying caller and "always passes `-m`" the hang belt) trues up with the conversion, as does the restore-step union's pre-mutation-by-construction sentence with the drop's atomicity qualification. **Characterization inversion:** the T5.1 suite's sparse-checkout residual case pins the loss today; this task inverts it to assert retention and porcelain equivalence (the suite must never carry both readings).
  - **Tests:** real-git temp-dir sparse fixtures (cone, non-cone positive-pattern, and non-cone negation — the top-level `!/a/` arm and the nested `!/a/b/` arm whose gitignore-vs-sparse evaluation divergence refuted the exclude-machinery oracle): capture's tree is byte-identical to `git add -A`'s in the clean sparse state and in the materialized-out-of-cone-edit state (the retained entry is the `<base>` blob, never the disk bytes); the nested-negation fixture retains its out-of-cone entry at `<base>` exactly as porcelain does (the oracle-divergence regression guard); a worktree created from a sparse canonical mount inherits the sparse definition (the copy-on-add behavior) and its capture takes the sparse-aware path (the `worktree`-mode inheritance guard); a boundary-time out-of-cone untracked file is not captured (porcelain parity), lands in the recorded boundary-set trailer, and survives restore on disk **while** a turn-created out-of-cone untracked file is deleted (the boundary-set exemption guard, both directions — the survival arm holds even when the cone widens over the path before restore); a `git add --sparse`-staged out-of-cone modification followed by a cone shrink round-trips: capture's tree carries the staged blob byte-identically to porcelain's and restore re-materializes the staged entry, never the `<base>` state (the live-index-seed guard); an out-of-cone `git add -N --sparse` intent-to-add path at the boundary is omitted from capture's tree exactly as porcelain omits it, joins the recorded boundary set, and survives restore on disk — unguarded, the checkout leg deletes the only copy, measured (the intent-to-add guard); a boundary-time out-of-cone untracked file the turn then stages with `git add --sparse` survives rollback on disk — its ordinary stage-0 entry is pre-dropped by boundary-set membership where an intent-to-add-only drop would let the checkout delete the only copy (the staged-boundary-path guard); the drop covers each recorded entry at its **full recorded width** — a path the turn staged beneath a recorded boundary DIRECTORY is pre-dropped as well, while a path the snapshot tree itself holds beneath that directory is never dropped (the checkout writes it), and a boundary path a decode-identical **tracked** path would otherwise have excluded is still dropped, both halves of that test keyed on the recorded bytes (the full-width pre-drop guards); capture under `core.sparseCheckout=true` with the rules file deleted yields the typed capture failure, never a not-sparse misclassification (the rules-vanished detection guard); a boundary set larger than a platform argv bound round-trips through the `-F -` message stream (the trailer-transport guard); a `.gitignore`d in-cone file is never staged by the oracle path (the exclude-source separation negative control); a failing oracle invocation (the below-2.41 unknown-subcommand shape, injected through the service's git-runner seam) yields the typed capture failure, never an unpartitioned tree (the fail-closed floor guard); restore in a sparse root leaves out-of-cone paths unmaterialized with `skip-worktree` re-set, restores in-cone content, and leaves `git status` clean of phantom out-of-cone deletions; restore over a materialized out-of-cone edit removes it from disk **and** reports it as an observed effect — a `partial_restore` that never reached the path omits it (the observed-subset diagnostic guard); a cone widened between capture and restore materializes the retained paths at their captured state; capture under `core.sparseCheckout=false` with a stale pattern file present takes the non-sparse pipeline byte-identically (the converse detection-quadrant guard); a sparse capture whose boundary set is empty writes `Sparse-Boundary-Paths: []` and two captures of identical project state mint identical OIDs (the sparse-format-marker guard); a seed attempted while the resolved index lock is held retries and then fails typed, never silently seeding the pre-write index — exercised in a main-checkout fixture and a linked-worktree fixture whose lock sits at `.git/worktrees/<id>/index.lock` (the lock-contention guards); an out-of-cone unmerged entry fails capture typed exactly as porcelain refuses that index (the unmerged-parity guard); restore of a trailer-absent snapshot refuses typed with nothing applied in a detected sparse restore root — the shipped legacy characterization state inverts to refusal there — and restores under the empty-set exemption in a non-sparse root, no phantom preservation, the present-but-undecodable fail-closed arm unchanged everywhere (the absent-trailer vintage guards, both directions); a restore-side oracle failure in a sparse root refuses at `derive-enumerations` with nothing applied (the restore-side floor guard); a failed intent-to-add drop reports `derive-enumerations` with index and worktree untouched and the diagnostic naming the drop (the drop-attribution guard); two boundary paths that decode alike stay distinct through the trailer subtraction (the byte-exact subtraction guard); a non-descended boundary directory is recorded with its trailing slash and its whole subtree survives the delete pass — including when the restore listing spells that same directory slash-free (the spelling-tolerance arm) — a boundary FILE entry stays exact so turn content beneath it is still deleted and a turn-created embedded repository standing at a boundary FILE path is deleted rather than rescued by its slashed spelling, and a path recorded under both kinds keeps the two widths apart (the kind-width guards); the delete pass refuses **whole** rather than remove a boundary file through a decode alias — every entry it is about to delete must re-encode from its decoded name back to its recorded bytes, and one that does not stops the pass at `delete-untracked` with the worktree exactly as the checkout left it (the pre-mutation round-trip guard); the pre-mutation obstruction refusal fires with nothing applied on each of the three destructive shapes — a widened cone checking out over a boundary path, a snapshot blob displacing a boundary directory in a non-sparse root, and an ancestor blob removing the directory of a boundary path or of a boundary directory entry — while both admitted non-shapes stay unrefused (a boundary directory whose descendants merely materialize into it; a conflicting snapshot path the live cone leaves unwritten, pinned at the displacer position and at the ancestor position alike), and the detail names every obstruction while collapsing displacers past the first three to a count (the obstruction-refusal guards); a non-sparse root's capture bytes are identical to the pre-closure pipeline's (regression guard); the inverted characterization case replaces the loss-pinning one.
  - **Spec coverage:** Spec-010 §Turn-Boundary Snapshots (the capture bullet's 2026-08-09 sparse-aware-staging closure — config-bit-alone sparse detection, the check-rules sparsity-matcher oracle, porcelain byte-equivalence in sparse roots over the live-index-seeded scratch index, the `Sparse-Boundary-Paths:` trailer (unconditional in sparse roots — the closure's format marker) and its set-keyed restore delete-exemption with the recorded-boundary-set index pre-drop (intent-to-add and turn-staged entries alike), the `-F -` message-transport conversion, full-tree snapshot with sparseness as a checkout-time projection, the materialized-path residual and its diagnostic enumeration; plus the same bullet's 2026-08-10 boundary-obstruction amendment — the pre-mutation typed refusal at `derive-enumerations` over the three destructive shapes with `160000` gitlink displacers exempt from the oracle, the kind-preserving trailer with its kind-scoped delete-exemption widths and over-protection residual, and the byte-exact boundary subtraction)
  - **Verifies invariant:** I-010-24
  - **Consumes:** capture pipeline + scratch-index recipe + diagnostic sink ← T5.1; restore pipeline + diagnostic channel ← T5.2; hook-neutralized git-invocation discipline ← Phase 2 (D-010-10)

## Parallelization Notes

- Phase 1 contract work and Phase 2 git-service scaffolding can proceed together once the D-010-2/D-010-5 shapes are fixed (they are — this audit).
- T2.2 / T2.3 are independent after T2.1; T2.4 integrates both.
- Phase 4 starts after T2.5's projection + T3.4's status-read binder are stable.

## Test And Verification Plan

- Execution-mode selection tests across read-only, branch, worktree, and ephemeral clone (T3.1)
- Worktree create / reuse / collision / retirement tests (T2.2, T2.6)
- Ephemeral clone prepare / TTL / sweep / disposition tests (T2.3)
- Failure-path tests proving no silent main-checkout mutation (T2.6 byte-identity)
- Tests proving execution-root preparation runs no setup scripts and no repository hooks (T2.6 sentinel fixtures)
- Run-setup gate tests: blocked-in-setup parking, busy bracketing, terminal release (T3.2)
- Wire + SDK round-trips incl. typed error codes (T3.6–T3.8)
- Renderer mock-bridge suites (T4.4)
- Turn-snapshot capture / restore / retention suites on real-git fixtures — `git add -A` tree equivalence with both embedded-repository skip classes, per-epoch create-only idempotence and the symref-hardened ref ops, the fail-closed resolve plus its execution-time re-verify, the lineage walk's no-fallthrough refusal, the fixpoint untracked-delete with skipped-repository preservation, the collision-overwrite and divergent-gitlink enumerations, the prune↔restore interleave, the replace-ref pin, and the cross-version refs-transaction behavior CI's newer git exercises (T5.1–T5.3)
- Sparse-root capture-closure suites — porcelain `git add -A` byte-equivalence in clean and materialized sparse states across cone and non-cone modes including the nested-negation arm that refuted the exclude-machinery oracle, the exclude-source separation negative control, restore's sparse-projection re-application with the observed-subset discarded-materialized-content diagnostic, the boundary-set delete-exemption guard (boundary-time preserved, turn-created deleted), the live-index-seed staged-out-of-cone guard, the intent-to-add boundary guard, the rules-vanished detection guard, the converse detection-quadrant guard, the trailer-transport stream guard, the sparse-format-marker guard, the unmerged-parity guard, the absent-trailer vintage guard, the `worktree`-mode sparse-inheritance guard, the fail-closed oracle-failure floor guard on both legs (capture and the restore-side `derive-enumerations` refusal), the drop-attribution guard, the full-recorded-width pre-drop guards (a turn-staged entry beneath a recorded boundary directory is dropped; a snapshot-tree path beneath one never is), the byte-exactness guards (the capture subtraction, the pre-drop's byte-keyed snapshot-tree exclusion, and the delete pass's whole-pass decode-alias refusal), the kind-width guards (the slash-preserved directory subtree with its slash-free-spelling tolerance, the kind-exact file entry, and the both-kinds separation), the pre-mutation obstruction-refusal guards across all three destructive shapes with both admitted non-shapes and the bounded-detail arm, the widened-cone restore, the non-sparse regression guard, and the characterization-case inversion — every arm in `packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts` (T6.1)
- Manual verification of the full lifecycle — Tier-8 leg, dispositioned per T4.5

## Rollout Order

1. Ship worktree persistence and daemon services (Phases 1–2)
2. Enforce canonical execution-mode resolution in repo-bound run setup (Phase 3)
3. Enable desktop execution-mode controls (Phase 4)

## Rollback Or Fallback

- Disable automatic worktree creation (deregister the run-setup gate) and require explicit branch or read-only mode if rollout blocks too much valid work; the seam-registration design makes the gate removable without touching Plan-004's engine.

## Risks And Blockers

- Branch naming collisions — mitigated by D-010-7 (provenance-split policy + index-arbitrated retry)
- Git edge cases on repos with unusual worktree support — surface as typed `worktree.create_failed`, never substitution (I-010-7)
- Ephemeral clone cleanup may leak disk usage — mitigated by D-010-13 (TTL sweep + `cleaned_at` idempotent stamping); residual risk: daemon down past TTL windows
- Repositories with mandatory bootstrap steps need explicit follow-on setup flows (`Spec-010 §Fallback Behavior`) — out of scope here by design
- BL-142 / BL-143 are Plan-007-owned Phase-3 preconditions; if they slip, Phases 1–2 still land
- The rollback file-restore leg coordinates a provider-side rewind (Codex `thread/rollback` / Claude `--resume-session-at`) whose methods are unconfirmed against a live binary (`thread/rollback` upstream-deprecated beyond the `0.141.0` pin; `--resume-session-at` binary-probe-only) — the daemon-side turn-snapshot file-restore is the durable path regardless, but before any code coordinating the provider rewind lands, re-verify the method or its replacement against the then-installed provider binary per [ADR-017](../decisions/017-shared-event-sourcing-scope.md) Decision Log (2026-07-02) + [`codex.md`](../reference/provider-wire/codex.md) / [`claude.md`](../reference/provider-wire/claude.md) (§Gaps recorded), that same re-verification covering one question the probe record leaves open — what a `--resume-session-at` fork carries across a provider-side context-compaction boundary (whether the forked session reconstructs pre-compaction context or resumes from the compacted summary alone), the case a rewind target older than a compaction boundary lands in
- Field evidence against per-turn git checkpointing, and why neither ground transfers: the campaign's surveyed reference runtime shipped per-message git checkpointing (hidden snapshot refs plus a revert that restored files) and later removed it, on two stated grounds — snapshot refs polluting ordinary repo tooling, and a file-restore path users never took. Ref pollution is foreclosed here by construction rather than by discipline: I-010-21 confines every ref to `refs/sidekicks/runs/<runId>/epoch-<E>/turn-<N>`, invisible to branch history, PR preparation, and diff attribution; I-010-22 makes each write create-only per epoch, so no ref is ever repointed; and T5.3's retention prune bounds the namespace over time (daemon-startup reconcile plus periodic sweep, ref ops routed through `run_execution_contexts.git_common_dir` so a run's refs stay reclaimable after its execution root is gone). The never-used ground is a different product shape, not a weaker version of this one: there, checkpointing was an optional per-message affordance offered to users who can run git themselves, so non-use is a real signal; here the per-turn file leg is a consumed dependency of the governed rollback intervention — `Spec-004 §Required Behavior` makes file restore part of the atomic rollback, with `files-unrestored` / `files-partially-restored` as fail-closed dispositions rather than optional outcomes, and both provider legs explicitly disclaim file restore (`Spec-004 §Driver-Level Rollback Mechanics`) — so dropping it would not retire an unused affordance, it would leave rollback rewinding conversation state while the working tree stays post-rollback

## Progress Log

### Shipment Manifest

<!-- Machine-readable. Housekeeper-emitted, orchestrator-written, preflight-read.
     Schema authoritative in:
       .claude/skills/plan-execution/scripts/lib/manifest.mjs -->

```yaml
manifest_schema_version: 1
shipped:
  - phase: 1
    task: [T1.1, T1.2, T1.3, T1.4]
    pr: 253
    sha: f3c9947
    merged_at: 2026-07-26
    files:
      - packages/contracts/src/__tests__/repo.test.ts
      - packages/contracts/src/__tests__/session-event.test.ts
      - packages/contracts/src/__tests__/worktree.test.ts
      - packages/contracts/src/event.ts
      - packages/contracts/src/index.ts
      - packages/contracts/src/worktree.ts
      - packages/runtime-daemon/src/git/__tests__/contract-ddl-conformance.test.ts
      - packages/runtime-daemon/src/migrations/0004-worktree-lifecycle.ts
      - packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts
      - packages/runtime-daemon/src/session/__tests__/session-service.test.ts
      - packages/runtime-daemon/src/session/migration-runner.ts
    verifies_invariant: [I-010-1, I-010-2]
    spec_coverage:
      [
        "Spec-010 line 50 (six worktree lifecycle states)",
        "Spec-010 line 128 (contract distinguishes the four canonical modes — via the imported ExecutionMode)",
        "Spec-010 line 144 (no-new-event-types carve-out)",
        "Spec-010 line 75 (select records the canonical mode)",
        "Spec-010 line 76 (prepare creates-or-binds the root; explicit reuse rides it)",
        "Spec-010 line 77 (reuse check reports branch, cleanliness, compatibility)",
        "Spec-010 line 78 (clone prepare reports root, lifecycle, cleanup policy)",
        "Spec-010 line 79 (explicit dispose interface)",
        "Spec-010 line 80 (retire records even with async deletion)",
        "Spec-010 line 81 (status read exposes records incl. provenance)",
        "Spec-010 line 87 (worktree records persist branch, mount, state, provenance)",
        "Spec-010 line 88 (per-run execution binding persisted)",
        "Spec-010 line 89 (branch context persisted polymorphically per writable mode)",
        "Spec-010 line 50 (state set lockstep between contract and persistence)",
      ]
    notes: |
      Full-phase single-PR ship (T1.1-T1.4). Codex round 1 on #253 was a clean pass — the +1
      all-clear with zero reviews and zero threads — under the five-round cap. T1.4 pins FOUR
      enum vocabularies rather than the three the plan prose enumerates: the migration also
      CHECKs the Plan-009-owned run_execution_contexts.execution_mode, compared as a set rather
      than a sequence because a Plan-009 reorder is a wire non-event under ADR-018. T1.4 also
      opens the Plan-010-owned src/git/ subtree a phase earlier than CP-010-7 had it, so that
      row's Tasks now reads T1.4, T2.1. The migration's REFERENCES clauses forward-reference
      Plan-009 Phase 2 tables: SQLite does not check parent-key definitions at CREATE TABLE time
      and defers the error to statement preparation (see the plan's References section for the
      primary source and the foreign_keys = ON precondition), so the real ordering gate lives on
      Plan-010 Phase 2's preconditions block, not on this phase. NS-39 auto-created in the
      housekeeping PR, which also trued up four stale plan-body claims against the merged
      surface and corrected a five-consumer Gate-5 precondition count on the NS-37 entry.
      Codex round 1 on that housekeeping PR (#254) returned five findings, all against the
      housekeeping edits rather than this shipment: the NS-35 build-order edge below, a
      reversed inbound/outbound reading on NS-37, an unanchored sink-check description, and
      the two missing primary-source citations now carried in References.
      Round 2 returned three more, again all against the housekeeping edits. (1) This
      verifies_invariant list was the union of the T1.1-T1.4 audit cites and overclaimed:
      per the Invariants table's task column, only I-010-1 (T1.1) and I-010-2 (T1.3, T1.4)
      complete in Phase 1. I-010-3 and I-010-4 pair T1.3 with T2.2, I-010-5 pairs T1.3 with
      T2.4, and I-010-13 is wholly Phase-2 (T2.1, T2.2) with only event-type registration
      landing in T1.1 - so a contracts-and-migration shipment cannot prove retirement
      provenance, typed unique-index collision handling, branch-context writes, or
      exactly-once transactional emission. The list is now the Phase-1-complete pair; the
      remaining four record when Phase 2 ships. This follows the Plan-005 Phase-1 precedent
      (record only what canonically verifies, note the partial contributions) rather than the
      Plan-005 Phase-2 audit-DAG-union reading, which applies only where every cited
      invariant does complete in that phase. (2) The Plan-005 build-order precondition moved
      from a whole-phase gate to pr_merged ref 157 - see the Phase 1 preconditions comment.
      (3) The References code cites took durable #symbol anchors, and the foreign_keys row's
      conflation of two distinct tests was corrected there.
      Round 3 restated two of round 2's findings against the stale commit f2d5429 and added
      one new: the §3 Plan-005 edge this PR authored is itself a targeted re-audit trigger
      under the readiness-audit runbook, a gate distinct from the Status Flip Rule and not
      discharged by it. The Preconditions audit box is unchecked accordingly, blocking Phase
      2 and Phase 5 dispatch until the delta lands. The same round surfaced that the edge had
      never propagated to the plan-header Dependencies row either (D3), repaired here.
  - phase: 2
    task: [T2.1, T2.2, T2.3, T2.4, T2.5, T2.6]
    pr: 297
    sha: 3e98412
    merged_at: 2026-08-08
    files:
      - docs/plans/009-repo-attachment-and-workspace-binding.md
      - docs/plans/010-worktree-lifecycle-and-execution-modes.md
      - packages/contracts/src/worktree.ts
      - packages/runtime-daemon/src/git/__tests__/ephemeral-clone-service.test.ts
      - packages/runtime-daemon/src/git/__tests__/worktree-event-emitter.test.ts
      - packages/runtime-daemon/src/git/__tests__/worktree-lifecycle.acceptance.test.ts
      - packages/runtime-daemon/src/git/__tests__/worktree-projector.test.ts
      - packages/runtime-daemon/src/git/__tests__/worktree-service.test.ts
      - packages/runtime-daemon/src/git/ephemeral-clone-service.ts
      - packages/runtime-daemon/src/git/worktree-errors.ts
      - packages/runtime-daemon/src/git/worktree-event-emitter.ts
      - packages/runtime-daemon/src/git/worktree-projector.ts
      - packages/runtime-daemon/src/git/worktree-service.ts
      - packages/runtime-daemon/src/workspace/__tests__/execution-root-service.test.ts
      - packages/runtime-daemon/src/workspace/execution-root-service.ts
    verifies_invariant:
      [
        I-010-3,
        I-010-4,
        I-010-5,
        I-010-6,
        I-010-7,
        I-010-8,
        I-010-9,
        I-010-10,
        I-010-11,
        I-010-12,
        I-010-13,
        I-010-19,
        I-010-20,
      ]
    spec_coverage:
      [
        "Spec-010 line 144 (failed and clone transitions deliberately not separately evented)",
        "Spec-010 line 46 (create-or-reuse before execution)",
        "Spec-010 line 51 (explicit reuse preserves branch + provenance context)",
        "Spec-010 line 55 (naming pattern)",
        "Spec-010 line 58 (retirement preserves metadata with async cleanup)",
        "Spec-010 line 114 (explicit collision handling)",
        "Spec-010 line 139 (slug rule)",
        "Spec-010 line 140 (provenance-split collision policy)",
        "Spec-010 line 141 (base-ref policy)",
        "Spec-010 line 47 (clone provisions before writable execution)",
        "Spec-010 line 65 (clone-prep failure blocks in setup)",
        "Spec-010 line 70 (sweep retirement disposition incl. workspace return-to-provisioning)",
        "Spec-010 line 143 (TTL as daemon config)",
        "Spec-010 line 42 (exactly one canonical mode per run binding)",
        "Spec-010 line 45 (branch mode is an explicit writable override on the existing checkout)",
        "Spec-010 line 48 (no silent main-checkout fallback)",
        "Spec-010 line 64 (worktree-creation failure blocks in setup)",
        "Spec-010 line 69 (stale refusal)",
        "Spec-010 line 89 (branch context persisted per writable mode)",
        "Spec-010 line 142 (branch-mode bind-only verification)",
        "Spec-010 line 81 (status read exposes lifecycle + provenance)",
        "Spec-010 line 90 (daemon-owned dirty/merged projections)",
        "Spec-010 line 121 (no hidden main-checkout mutation)",
        "Spec-010 line 127 (AC1 — writable run on git defaults to worktree mode at the service layer)",
        "Spec-010 line 129 (AC3 — creation failure blocks rather than mutating)",
        "Spec-010 line 130 (AC4 — reused worktrees stay linked to branch + prior context)",
      ]
    notes: |
      Full-phase single-PR ship (T2.1-T2.6). Codex round 1 closed reply-and-resolve (retire and
      cleanup races closed, clone retirement rescoped to the run's recorded cloneId, detached-snapshot
      helper fold); round 2 likewise (the config-named fsmonitor hook joined the D-010-10
      neutralization argv in all three services; the reuse-cleanliness TOCTOU recorded as an accepted
      residual whose run-start re-proof the Phase-3 root-keyed run-setup gate owns); round 3 closed
      with no response under the session review cap. verifies_invariant scoping: I-010-13 is verified
      for the Phase-2 emission legs only — the dirty/merged producer half is a recorded Phase-3
      obligation (plan Phase 3 Goal); I-010-19 and I-010-20 ride as their daemon halves per the
      T2.5 row (canonical completion stays with Phase 4's views per the invariants task column).
      NS-48 auto-created in the housekeeping PR.
  - phase: 5
    task: [T5.1, T5.2, T5.3]
    pr: 303
    sha: 5bcf40c
    merged_at: 2026-08-09
    files:
      - packages/runtime-daemon/src/bootstrap/index.ts
      - packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts
      - packages/runtime-daemon/src/git/turn-snapshot-service.ts
    verifies_invariant: [I-010-21, I-010-22, I-010-23]
    spec_coverage:
      [
        "Spec-010 §Turn-Boundary Snapshots (capture temp-index recipe, epoch-namespaced create-only refs, writable-modes-only applicability, untracked-embedded-repo gitlink normalization and single-base-OID resolution, both folded into the capture bullet by the 2026-07-20 errata batch)",
        "Spec-004 §Required Behavior (execution-epoch definition)",
        "Spec-010 §Turn-Boundary Snapshots (restore two-step plus index close, fail-closed HEAD precondition, fixpoint untracked-delete, collision-overwrite enumeration, submodule gitlink boundary, lineage-walk target resolution with no-fallthrough refusal; captured-embedded-repo gitlink survives the untracked-delete pass by construction and an absent gitlink materializes as an enumerated empty directory; checkout-conversion host-config neutralization and the partial-restore enumerations, both folded into the restore bullet by the 2026-07-20 errata batch)",
        "Spec-004 §Required Behavior (non-mutating pre-dispatch validation, then bound restore under exclusive execution-root tenancy)",
        "Spec-010 §Turn-Boundary Snapshots (retention prune on window close - window as daemon config, the service owns the mechanism absent a run-retention owner; ephemeral-clone-disposal shares the clone lifecycle, post-disposal rollback conversation-only)",
      ]
    notes: |
      Full-phase single-PR ship (T5.1-T5.3) through /plan-execution: per-task three-reviewer rounds,
      a PR-scope final round, and four external codex rounds. Twelve codex findings fixed - among
      them the replace-ref pin class (-c core.useReplaceRefs=false on the five OID-interpretation
      legs), skipped-embedded-repository restore preservation via the sorted-JSON
      Skipped-Embedded-Repositories snapshot-commit trailer (written only when non-empty; restore
      reads it fail-closed and exempts recorded paths from the untracked-delete fixpoint),
      segment-boundary collision enumeration, the core.eol/core.safecrlf conversion pins, and
      two-sided update-ref --no-deref symref hardening. One finding disputed with rationale (the
      bootstrap wiring seam is the sanctioned single wiring call), one root-caused and recorded as
      a scoped residual (sparse-cone capture loss; closure adjudication parked for Plan-010), one
      reversed by measurement (core.fileMode deliberately honored). A CI follow-up pinned the
      git-2.54 refs-transaction refusal over dangling symrefs as a version-split account beside
      local 2.50.1. Final scoped suite 138 tests; 59-arm mutation battery all KILLED against the
      merged bytes with byte-identical source restore. The restore result unions
      (TurnSnapshotResolution / TurnSnapshotRestoreResult and their enumeration fields) are frozen
      wire identity for Plan-004 T3.13's rollback consumer; the resolve-freezes-the-OID
      prune-restore interleave contract and the ref-store BOUNDARY scoping are recorded in the
      service header, with the governance errata riding this PR's second commit. The retention
      sweep handle is exported for Plan-007's future composition root (interval unref()ed; dispose
      un-wired by design, the recorded residual).
  - phase: 6
    task: T6.1
    pr: 309
    sha: d83ae0e
    merged_at: 2026-08-10
    files:
      - packages/runtime-daemon/src/git/turn-snapshot-service.ts
      - packages/runtime-daemon/src/git/__tests__/turn-snapshot-service.test.ts
    verifies_invariant: [I-010-24]
    spec_coverage:
      [
        "Spec-010 §Turn-Boundary Snapshots (the capture bullet's 2026-08-09 sparse-aware-staging closure — config-bit-alone sparse detection, the check-rules sparsity-matcher oracle, porcelain byte-equivalence in sparse roots over the live-index-seeded scratch index, the `Sparse-Boundary-Paths:` trailer (unconditional in sparse roots — the closure's format marker) and its set-keyed restore delete-exemption with the recorded-boundary-set index pre-drop (intent-to-add and turn-staged entries alike), the `-F -` message-transport conversion, full-tree snapshot with sparseness as a checkout-time projection, the materialized-path residual and its diagnostic enumeration)",
      ]
    notes: |
      Codex round 1 hardened the boundary-set path in-PR: byte-exact trailer subtraction (latin1-keyed), kind-preserving trailer (directory entries slash-suffixed; subtree delete-exemption), and the pre-mutation boundary-obstruction refusal covering all three measured collision shapes. The refusal disposition (typed refusal over restore-wins-enumerate) was ratified by product decision 2026-08-10; the I-010-24/Spec-010 amendment naming it is owed as the next docs PR (NS-52 records the obligation). (Discharged 2026-08-10 by this plan's boundary-obstruction naming amendment: I-010-24's restore half and the T6.1 row now carry the refusal and the kind widths, the Spec-010 capture bullet carries all three legs, and both files sit `review` pending the restoring targeted readiness-audit delta — the 2026-08-10 Notes entry has the record. Both were restored `approved` the same day by that delta (§6 node NS-53). This field stays the shipment-time enumeration; `spec_coverage` above likewise records what PR #309 cited at merge, not the widened task-row cite.) (Three code gaps that naming round's own review surfaced were closed ahead of it, 2026-08-10, by PR #312 — `develop` squash `9d3b93b`, against these same two files: the `latin1` byte-record trailer, the full-recorded-width index pre-drop, and the untracked-delete pass's whole-pass pre-mutation round-trip guard. Recorded here so this entry's shipment-time enumeration is not read as the present state of T6.1's code.)
```

### Notes

<!-- Per-PR human commentary (round-trips, learnings, partial-ship details). Append-only. -->

- 2026-06-10 — Tier-6 plan-readiness audit (NS-18): plan rewritten to the audited four-phase Tasks structure; 67 findings adjudicated (17 critical / 38 major / 11 minor / 1 nit) across 24 cross-phase adjudications; wire block, DDL, error vocabulary, method registry, and the Plan-004/Plan-009/Spec-009/Spec-010 reciprocal amendments ratified in the same audit PR.
- 2026-07-20 — W2.5 targeted re-audit (campaign Task 23, B22 delta — Phase 5 + the T1.3 `git_common_dir` column): zero critical findings (1 major / 5 minor / 2 nit dispositioned). The closing re-audit PR (#220) renumbers the duplicate renderer-boundary obligation CP-010-11 → CP-010-13 (both rows were co-introduced by the Tier-6 audit commit; the envelope-roots row keeps CP-010-11 with its external reciprocal web — Plan-009 CP-009-8, Plan-012 CP-012-9 — untouched), names the T5.2 result unions (`TurnSnapshotResolution` / `TurnSnapshotRestoreResult`), adds the ephemeral-clone `git_common_dir` mapping to D-010-5 + the schema mirror, and restores `review` → `approved` — closing the 2026-07-19 B22 flip (Status Flip Rule row 3), whose flip/restore provenance this bullet records so the header Status cell stays in the bare template shape preflight Gate 7 parses; the same PR ticks the audit checkbox, adds Phase 5's machine-enforced Gate-5 `preconditions:` block, and re-derives the README plan census (20 `approved` / 6 `review` / 1 `completed`).
- 2026-07-25 — Lifecycle-payload mechanism amendment (PR #250, Plan-009 Phase 1, Codex round 4): the Tier-6-ratified reuse-as-is of `RepoWorkspaceLifecyclePayloadSchema` for the five `worktree.*` events was unbuildable as written — the canonical const's two-arm `state` union (`RepoMountStateSchema` ∪ `WorkspaceStateSchema`) rejects five of the six worktree states, its `z.ZodType` isolatedDeclarations annotation erases the `.extend()` surface, and the `repo.ts` FORWARD PATH comment's promised third `state` arm would re-close the node-id.ts eager-cycle class (`worktree.ts` imports `repo.ts` per I-010-1) while loosening every family's accept set. Repaired in the provider PR: Plan-009 T1.1 exports the state-parameterized `buildRepoWorkspaceLifecyclePayloadSchema` factory, and this plan's T1.1 defines `WorktreeLifecyclePayloadSchema` in `worktree.ts` as that factory's `WorktreeStateSchema` instantiation — I-010-1, CP-010-1, CP-010-5, D-010-12, T1.1, and T2.1 amended in place (net-zero; inline `PR #250 round 4` markers). Scope, the Plan-010 → Plan-009 T1.1 dependency edge, the five-row event census (D-010-11), the wire block, and the DDL are unchanged. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row and default rule — no new invariant, CP row, phase, or Required ADR, and no plan-body behavior change (the as-audited mechanism admitted no build at all); the Tier-6 audit checkbox stands for Phases 1–4, and the amended file passes the preflight gates, G4 traceability included. Reciprocal record: Plan-009 §Cross-Plan Amendments (PR #250).
- 2026-07-20 — Partial-restore-enumerations amendment (PR #230; the lead-owned follow-up named by Plan-004's §Preconditions box "Plan-010 partial-restore enumerations registered", the CP-004-12 pattern): T5.2's `partial_restore` variant now carries the same collision-overwrite + divergent-gitlink enumerations as `restored`, covering every effect applied before the failure, with the enumeration fields pinned name-identical to Plan-004's wire arms (`overwrittenIgnoredPaths` / `divergentGitlinks` / `failedStep` — the T3.13 wire mapping is an identity); I-010-23 extended to the enumerations. The same PR lands the four-item `Spec-010 §Turn-Boundary Snapshots` errata batch the T5.1/T5.2 hardenings had run ahead of — single-base-OID capture + untracked-embedded-repo gitlink normalization; restore-leg `core.attributesFile` + `GIT_ATTR_NOSYSTEM` conversion pins; the partial-restore enumerations mandate; and the execution-epoch gloss corrected from "incremented per applied rollback" to advanced-per-accepted-`run.rolled_back` regardless of file-leg disposition, matching `Spec-004 §Required Behavior` (the stale shorthand likewise updated at CP-010-12, the Phase-5 epoch box, and T5.1) — and checks the Plan-004 box. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row and default rule: the spec's Scope mandate already required the never-silent enumerations on the restore result unqualified, the audited Plan-004 wire arm (#228 rounds 2/4) already REQUIREd the fields, T5.2 was the lagging mirror, and the round-4 §Preconditions box named this lead-owned amendment as the registration vehicle (no new invariant, CP row, phase, or Required ADR). Codex round 1 (PR #230) widened the partial-result scoping from completed-steps-only to **every effect applied before the failure** — `read-tree -u` is not transactional, so a mid-command failure leaves earlier collision overwrites on disk (empirically confirmed on git 2.50.1) — across the spec's restore bullet, T5.2, I-010-23, and the Plan-004-side mirrors (§Preconditions box, I-004-16, T1.3, T3.13, the contracts partial-arm comments), and carried the plan's T5.1 primary-source citations into the spec's normalization pass.
- 2026-07-26 — Phase-1 shipment record + true-ups (PR #253 housekeeping; §6 node NS-39). The §Shipment Manifest gains its first entry — phase 1, tasks T1.1–T1.4, PR #253, squash `f3c9947`, 11 files — and Phase 2's `Phase 1 merged` precondition box is ticked with the same merge reference (the Plan-009 Phase 2 precedent from PR #251). The manifest entry is load-bearing, not ceremonial: with `shipped: []` preflight halts on Gate 6 (manifest freshness) naming PR #253 by number, and with the entry Gate 6 passes and Phase 1 correctly drops off the eligible-phase list — verified both directions before this PR was opened. Alongside the record, four stale plan-body claims corrected against the merged Phase-1 surface, no behavior change. (1) I-010-1 and (2) CP-010-1 no longer list Plan-009's `RepoMountState` among the symbols `worktree.ts` imports from `repo.ts` — the merged file imports `ExecutionMode`, `WorkspaceState`, the branded `RepoMountId`/`WorkspaceId`, and `buildRepoWorkspaceLifecyclePayloadSchema` only. No Plan-010 wire shape or lifecycle payload carries a mount state, and T1.1's own task body already omitted it; the enumeration was pre-PR-#250 residue from the two-arm mount-state ∪ workspace-state union the round-4 factory amendment removed (the 2026-07-25 bullet above). Plan-009's ownership and export of the symbol are unchanged — only the claim that Plan-010 imports it is retracted. (3) CP-010-7's **Tasks:** row widens `T2.1` → `T1.4, T2.1`: T1.4's `packages/runtime-daemon/src/git/__tests__/contract-ddl-conformance.test.ts` is the first file in the Plan-010-owned `src/git/` subtree, so the subtree opens at Phase 1, not Phase 2 — the cross-plan §2 `git/` row carries the reciprocal clause. (4) T1.3's **Consumes** parenthetical no longer reads "build order: Plan-009 Phase 2 first within Tier 6": the `REFERENCES repo_mounts` / `REFERENCES workspaces` clauses are deliberate forward references, resolved lazily by SQLite at DML time, so `0004-worktree-lifecycle.ts` applies cleanly on a parent-less database and shipped ahead of Plan-009 Phase 2 per the ratified campaign order. The Consumes edge itself is unchanged and the ordering gate is real — it lives on Phase 2, whose machine-enforced `preconditions:` block already carries `plan_phase, plan: 009, phase: 2, status: merged`; only its attribution to Phase 1 was wrong. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row and default rule: no new invariant, CP row, phase, or Required ADR, and no plan-body behavior change — two false import claims retracted, one task attribution corrected, one build-order parenthetical re-attributed to the phase that carries the gate. Phases 2–5 remain; only Phase 1 has shipped.

- 2026-07-26 — Codex round 1 on the PR #253 housekeeping PR (#254): five findings, none against the merged Phase-1 code. Two land in this plan. (1) **Phase 1's precondition set was incomplete** — T1.2's `RunId` arrives from Plan-005 Phase 1's `provider-driver.ts` as an `import type`, which is erased at emit but still blocks `tsc`, so it is a build-order dependency; the box and the machine-enforced `preconditions:` block now carry it, and §6 records the `NS-35 → NS-39` edge (the §6 entry had adjudicated it a deliberate non-edge on the grounds that no RUNTIME module edge exists — true, but a module-cycle property, not a build-order one). It was satisfied six weeks before dispatch, so nothing was actually mis-gated. (2) **The forward-FK premise carried no primary source** — T1.3's Consumes asserted lazy FK resolution as bare fact; a §References section now cites SQLite Foreign Key Support §5 for the create-time non-checking and the "no such table" message, plus §2 for the `foreign_keys = ON` precondition without which the deferred check would not fire at all, and RFC 8785 for T1.4's set-vs-sequence split. This plan had no References section before; it now follows the Plan-009 placement. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row: a precondition made explicit, no new invariant, CP row, phase, or Required ADR, and no behavior change.

- 2026-07-26 — Codex round 2 on the same housekeeping PR (#254): three findings, again none against the merged Phase-1 code, all three against the round-1 edits above. (1) **The Phase-1 manifest entry overclaimed its verified invariants.** `verifies_invariant` had been written as the union of the T1.1–T1.4 audit `Verifies invariant:` cites — `{I-010-1, I-010-2, I-010-3, I-010-4, I-010-5, I-010-13}` — but the §Invariants task column is the authority on where each invariant canonically verifies, and only **I-010-1** (T1.1) and **I-010-2** (T1.3, T1.4) complete inside Phase 1. I-010-3 and I-010-4 pair T1.3 with T2.2, I-010-5 pairs T1.3 with T2.4, and I-010-13 is wholly Phase-2 (T2.1, T2.2) — T1.1 registers the event types, which is not the same as proving exactly-once transactional emission. A contracts-and-migration shipment cannot demonstrate retirement-preserves-provenance, typed unique-index collision handling, branch-context writes, or transactional emission, so audits reading this manifest would have treated four runtime behaviours as proven before their implementation exists. The entry now records the Phase-1-complete pair and the remaining four record when Phase 2 ships — the **Plan-005 Phase-1** precedent (record only what canonically verifies; note the partial contributions), not the Plan-005 Phase-2 audit-DAG-union reading, which holds only where every cited invariant does complete in that phase. (2) **The round-1 Plan-005 precondition did not resolve satisfied.** `{ type: plan_phase, plan: 005, phase: 1, status: merged }` classifies `partially_shipped`: Plan-005 Phase 1 declares T1.1–T1.8 while PR #157's manifest entry shipped T1.1–T1.6, T1.7/T1.8 being later campaign EXTENDs — confirmed by running `classifyPhaseShipment` against the plan directly. The whole-phase gate is also wider than the dependency, which needs only the `RunId` brand that T1.1 shipped, so both the box and the machine block now name the immutable merged revision — `{ type: pr_merged, ref: 157 }` (the Plan-002 / Plan-008 precedent). (3) **The new §References code cites were not durable.** Both pointed into `packages/` by bare path, which the docs-corpus gate cannot invalidate when a symbol moves; they now carry `#applyPragmas` anchors per `AGENTS.md §Durable-Cite Rule`. Verifying that fix surfaced a further error of this plan's own: the `foreign_keys` row had attributed the pragma-state assertion to the parent-less-database case, but that case pins the `no such table` compile failure, while the pragma state is asserted by a separate DML-enforcement test — the row now names both and says which proves what. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row: a shipment record narrowed to what it can prove, a precondition narrowed to the revision it needs, and two citations made durable — no new invariant, CP row, phase, or Required ADR, and no plan-body behavior change.

- 2026-07-26 — Codex round 3 on the same housekeeping PR (#254): three findings, of which two were restatements of round 2 evaluated against the stale commit `f2d5429` (the fixes had already landed in `e968f36`) and one was new and correct. **The new finding is a governance gate, and it is now open.** Adding the Plan-005 clause to the `cross-plan-dependencies.md` §3 Plan-010 row is an explicit targeted re-audit trigger — the runbook names "§1 / §2 / §3 gains an edge or row affecting an already-`approved` plan" in both [§When To Invoke](../operations/plan-implementation-readiness-audit-runbook.md#when-to-invoke) and [§Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#status-promotion-gate) — and the [Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) this PR applied governs a different gate that does not discharge it. The §Preconditions audit box is therefore **unchecked**, following the shape this plan already used for the 2026-07-19 B22 re-opening (unchecked while owed at `2aade72`, ticked on delivery at `adb8d00`), which mechanically blocks Phase 2 and Phase 5 dispatch through preflight until the delta lands. Scope was established by reading the Tier-6 audit's actual coverage rather than assumed: the edge was traced at dep-trace dimension **D2** with file-and-phase precision (T1.2 and T3.2 have recorded `RunId` ← Plan-005 Phase 1 `provider-driver.ts` since 2026-06-10) and **D5** tier placement holds unperturbed (Plan-005 Tier 4 under Plan-010 Tier 6), so the delta is a **D3/D4 rollup-confirmation pass**, not a dependency discovery. Investigating that scope surfaced a third defect of this PR's own making: §3 now asserted an edge the plan-header `Dependencies` row still omitted, so **D3 is repaired here** — Plan-005 added to that row with the T1.2/T3.2 consume sites and the PR #157 gate. A sweep of every body `Consumes:` clause against the header row found one further candidate, `jsdom workspace config ← Plan-002 Phase 6` at T4.4, left for the audit to adjudicate rather than settled in a housekeeping lane (D3 asks about code imports; a vitest environment config may not be one). The delta lands as its own PR ahead of Plan-010 Phase 2, with Plan-006 Phase 2, Plan-006 Phase 3, and Plan-009 Phase 2 before it in the build order. (Discharged 2026-08-04 by PR #293 — the gate this bullet opened is closed, the §Preconditions box ticked, and the `jsdom` candidate resolved as a misattribution rather than a second propagation instance; see the 2026-08-04 entry below. The build-order sentence held in two of its three parts: Plan-006 Phase 2 (2026-07-27, PR #256) and Plan-006 Phase 3 (2026-08-04, PR #287) both merged ahead of this delta, but Plan-009 Phase 2 had not, which costs nothing — §6 NS-40 records that ordering as sequencing convenience rather than a precondition, its one hard upstream being NS-39.)

- 2026-08-04 — Targeted readiness-audit delta (§6 node NS-40, minted by PR #254 and closed here): the **D3/D4 rollup-confirmation pass** the 2026-07-26 §Preconditions re-opening owed. **Scope held to the re-opening rather than widened.** The Plan-005 edge that fired the trigger was already traced at **D2** with file-and-phase precision, and **D5** tier placement held unperturbed, so what is audited here is the _propagation step_ — whether every traced body edge reaches the two rollup surfaces — rather than the dependency set. The two dimensions, quoted from the [runbook §Main-Agent Dep-Trace Dimensions](../operations/plan-implementation-readiness-audit-runbook.md#main-agent-dep-trace-dimensions): **D3** — "Does the plan's `Dependencies` row enumerate every plan whose code Phase-N imports from?"; **D4** — "Does §3 of `cross-plan-dependencies.md` show every edge Phase-N needs? Are edges typed correctly?". **Census:** all 26 task `Consumes:` clauses across Phases 1–5, plus the 9 consume/honor-direction §Cross-Plan Obligations rows (CP-010-1..5, CP-010-8, CP-010-9, CP-010-10, CP-010-13), resolve to **8 external provider plans** — Plan-001, Plan-002, Plan-004, Plan-005, Plan-006, Plan-007-partial, Plan-009, Plan-023-partial. BL-142 and BL-143 are backlog items rather than plans, and their owning Plan-007 is named in both surfaces already. **7 of the 8 are present and correctly typed in the plan-header `Dependencies` row and the §3 Plan-010 row alike; zero propagations are missing** — so the PR #254 lapse was a single instance rather than a pattern, which is the question this delta existed to settle. **Plan-002 is the one absent plan, and its absence is correct on a single principle** applied to all three of its body appearances: the rollup rows enumerate the plan that _owns_ a consumed surface per the [§2 path-ownership map](../architecture/cross-plan-dependencies.md#2-package-path-ownership-map), not a plan that authored a file inside another plan's owned path under the [§Ownership Rule](../architecture/cross-plan-dependencies.md#ownership-rule) Housekeeping Exception, and not a plan whose _pattern_ was copied — and every such owner is already enumerated. (a) `brandedUuidIdSchema` at T1.1 lives in `packages/contracts/src/internal/branded.ts`, on the **Plan-001** Phase-2-owned contracts path; Plan-002 authored it as a one-time correction its own §Cross-Plan Amendments explicitly disclaims as ongoing edit rights. (b) The ambient `window.sidekicks` type at T4.1 lives in `apps/desktop/src/renderer/src/sidekicks-bridge.d.ts`, authored by Plan-002 Phase 6 T6.0 but declaring in its own header that the renderer-shell substrate is **Plan-023**-owned and this file pre-paid ahead of the Plan-023 Tier-8 remainder — and an ambient global declaration is picked up by the tsconfig include, never imported. (c) The per-suite mock-bridge install/teardown pattern at T4.4 is a copied pattern, not a module edge at all. The sibling **Plan-009**, audited in the same Tier-6 pass, resolves identically: its T1.1 and T4.1 name Plan-002 in exactly these two places while its header and §3 rows omit Plan-002. **The named lead — `jsdom workspace config ← Plan-002 Phase 6` at T4.4 — proved misattributed on its face, so it is repaired rather than merely adjudicated.** The renderer test harness is the `renderer` project in `apps/desktop/vitest.config.ts`, whose environment is **happy-dom, not jsdom** (`apps/desktop/package.json` carries the `happy-dom` devDependency and the repo has no `jsdom` dependency at all), and whose author is **Plan-001 Phase 5 T5.2** — the config's own header comment names that task, and `git log` shows the file created in PR #77 and touched once since by a tooling PR, never by Plan-002 Phase 6. Plan-001 is enumerated in both rollup surfaces already, so the true edge had propagated all along and the header row was never missing it. Independently, and as a fallback rather than a co-equal ground: even as written the clause would not be a D3 edge, since a vitest `environment` setting is not a module T4.4's code imports — the runner reads the config. What Plan-002 Phase 6 does supply here is the mock-bridge pattern (T6.3), which the task body already cited as a precedent. The same error appeared in three forms, each now naming the real provider: T4.4's `Consumes:`, the Phase-4 **Goal**, and the Phase-4 precondition box. **A second defect of the same class surfaced in the opposite direction and is repaired here too.** T4.1 consumed the ambient `window.sidekicks` typing without declaring it while Plan-009's T4.1 declares exactly that edge — an _untraced_ consume where PR #254's was a traced-but-unpropagated one, both defects of the propagation step. T4.1's `Consumes:` now carries it in Plan-009's idiom. T1.1's `brandedUuidIdSchema` clause likewise named a file and a tier but no provider plan, which leaves a D3 sweep unanswerable from the body alone; it now carries the ownership attribution above. Neither repair moves a rollup row — by the principle above both resolve to already-enumerated owners — and that is the point: the body is now auditable at D3 without leaving the plan. **Status stays `approved`** per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row 1 (citation fix, narrowing of ambiguity) and its default rule — five body clauses trued up (one harness misattribution in its three forms, plus the two under-declared consumes at T1.1 and T4.1), with no new invariant, CP row, phase, or Required ADR, no plan-body behavior change, and no edit to either rollup surface — so the plan-status census is unmoved and there is no README / CLAUDE.md consequence. With the box ticked, the plan-level audit gate is clear for every phase; Phase 2 and Phase 5 now dispatch on tier order plus their own `preconditions:` blocks, Phase 2's one unmet leg being `Plan-009 Phase 2 merged` (its Plan-006 Phase 3 leg was satisfied 2026-08-04 via PR #287).

- 2026-08-07 — T2.4 implementation + review round 1 (PR #297): three plan-record amendments surfaced by the execution-root build, all in place, net-zero where mid-file. (1) **Bracket-scoping of the stale gate.** I-010-12, CP-010-3, the T2.4 task row, and D-010-16 as written quantified `assertWritable` over every writable-mode prepare, but the bracket's own provisioner cannot pass it: a prepare that finds the workspace `provisioning` is executing inside the open CP-010-2 bracket (the writable first bind's `provisioning` hold, a swallowed `failReprovision` leftover, or the T2.3 cleanup leg (d) post-retirement return), and Plan-009's gate raises its invariant error for `provisioning` by design. The four rows now scope the obligation to prepares that find the workspace outside the open bracket — the gate is skipped together with `beginReprovision` exactly there, and I-010-12's purpose clause (a `stale` workspace never receives an execution root) survives unconditionally because the states are exclusive. The reciprocal Plan-009 CP-009-3 row carries the same scoping; see §Cross-Plan Amendments. (2) **Clone-mode observed base branch.** T2.4 must record `branch_contexts.base_branch` (NOT NULL) for clone contexts, but no ratified surface supplied a base: D-010-8 is worktree-scoped by its own text, refusal code, and implementation site, so clone mode had no base rule and the initial implementation self-anchored (`base_branch` = the new branch), poisoning the Plan-011 PR-base derivation chain (I-011-2 derives the PR base exclusively from the recorded branch context). T2.3's `PreparedEphemeralClone` now surfaces an optional observed `baseBranch`, read inside the clone via `git branch --show-current` after the clone and before the branch cut — the clone's own HEAD is immutable ground truth, so the read is race-free and adds no main-checkout invocation — absent when the clone's own HEAD lands detached, which takes a source HEAD commit no branch references — `git clone` resolves the remote HEAD to a branch naming it otherwise, empirically confirmed on real git 2.50.1 (empty stdout on exit 0 is lawful absence, not a failure). The failure vocabulary gains `base_branch_unreadable` (invocation failure only, never lawful absence; disposition matches `head_branch_unavailable`), and T2.4 consumes `prepared.baseBranch ?? prepared.branchName`, narrowing the self-anchor residual to the detached-HEAD edge whose durable fix is the Plan-011 recorded-context extension. The T2.3 task rows name the response field and the detached-HEAD negative control. (3) **Branch-mode insert-per-prepare.** Review round 1 struck an unratified one-row-per-workspace refresh arm whose borrowed uniqueness rationale is false in D-010-15's own reuse arm (a workspace binding three worktrees holds three rows) and which no consumer needs — `BranchContextReadRequest` has no workspace-only arm, and `run_execution_contexts.branch_context_id` disambiguates; branch mode now inserts per prepare like the clone arm, preserving history under the plan's identical silence for both arms. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) narrowing row and default rule: (1) and (3) narrow written obligations to what the ratified mechanism already required, (2) is an implementation-discovered mechanism repair in the provider task of the same unmerged PR (the PR #250 class — the as-audited T2.4 row demanded a value the as-audited surfaces could not lawfully supply), extending a failure vocabulary minted by this same PR; no new invariant, CP row, phase, or Required ADR, and no change to the wire block or DDL.
- 2026-08-07 — Phase D final PR-scope review (PR #297): three mechanism repairs and the record true-ups they force, all landing in the same unmerged PR (the PR #250 implementation-discovered class). **Repairs.** (1) T2.2's cleanup leg (d) uncleaned-retired sweep gains the busy-holder deferral T2.3's sweeps already interpolate, keyed on `fs_root` — the retire-time probe decides at the retirement instant and Plan-009's `markBusy` requires only `ready`, so a workspace still pointing at a retired root can become busy afterward, and without the clause the next pass removes a working tree out from under the run holding it; deferral only, `releaseBusy` frees the root to the next pass. (2) T2.4's prepare gains a pre-bracket busy-holder probe on the explicit-reuse CANDIDATE's directory — the `Spec-010 §State And Data Implications` concurrent-root-handoff refusal, root-keyed because severable `branch_contexts` rows cannot anchor it and because CP-010-4's T3.2 gate rides workspace-keyed `markBusy` (that gate must add the same root-keyed probe when it lands; `branch` mode shares the mount's checkout); pre-bracket so the refusal is a wait-and-retry `workspace.busy` naming the holder rather than a `failReprovision` that stales the requester for someone else's live run — accepting the recorded residual that the probe precedes `validateReuse`'s declared refusal order, so a candidate refusable outright (wrong mount, retired, or wrong branch) whose directory has a live holder answers `workspace.busy` first; a misordered refusal is the cheaper defect than staling the requester. (3) T2.2's `onCollision: 'suffix'` arm refuses a candidate that would exceed `WORKTREE_GIT_REF_MAX_LEN` as `branch_name_unavailable` — an accepted-at-cap name gains `-<ordinal>` and a persisted over-cap `branch_name` would fail T2.5's response validation for the WHOLE status read; the contracts-side cap rationale now names the suffix origin it had missed. **Coverage.** T2.6 gains the branch-mode real-git pair: the only writable mode whose execution root IS the user's checkout ran nowhere against real git, and `#verifyBranchModeBind`'s detached-HEAD exit-status contract (`symbolic-ref --quiet` exits 1) was pinned only against a fake — the pair drives the full bracket end-to-end and pins byte-identity plus the `workspace.branch_mismatch` refusal on a real detached checkout. **Record true-ups.** The T2.2 row's `workspace.*` carrier census (three declared carriers; `workspace.busy` is a Plan-010-introduced registry row whose carrier is Plan-009's per CP-009-7 — the row conflated introducing the row with declaring its carrier); the Phase 3 Goal's two recorded obligations (the `-> dirty` / `-> merged` producer ownership with the owning task row owed at Phase-3 dispatch — the unowned-forward-promise class Plan-009's 2026-08-05 T2.5 entry repaired — and the composition-root handle-identity obligation, its PR #292 Phase-D sibling); and test-hygiene convergence across the Phase-2 suites (thunk-form `captureRejection` matching the Plan-009 precedent, options-object seeders ending the same-name/opposite-positional-semantics collisions). Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): mechanism repairs in the provider tasks of the same unmerged PR implementing already-ratified obligations through existing vocabulary (both busy refusals reuse Plan-009's carrier per CP-009-7; the cap guard reuses `branch_name_unavailable`), plus narrowing/record-keeping true-ups; no new invariant, CP row, phase, or Required ADR, and no change to the wire block or DDL.
- 2026-08-07 — Codex review round 1 (PR #297): seven findings, all accepted and fixed in the same unmerged PR (the PR #250 implementation-discovered class). **Mechanism repairs.** (1) T2.2's retire-conflict probe re-keyed from the `branch_contexts` join to `holder.fs_root = worktrees.fs_root` — context rows are retained history, so the join refused retirement for any workspace that EVER bound the worktree while busy on a DIFFERENT root; the `fs_root` key asks CP-009-7's actual question. (2) T2.2's cleanup leg (d) re-decides the busy-holder deferral per row, immediately before that row's removal, through the same statement — the candidate list is a snapshot and a `markBusy` can land during an earlier row's removal await; the residual window during a row's OWN removal await is recorded at the site and owned by the Phase-3 root-keyed gate obligation already recorded at the Phase 3 Goal. (3) T2.4's context write re-proves a REUSED candidate's liveness inside the same synchronous block as the upsert, refusing `worktree.reuse_conflict`/`not_live` through `failReprovision` — `validateReuse` decides across its cleanliness-probe await, so a retirement could commit between verdict and bind and hand the adopting workspace a root leg (d) is entitled to delete. (4) T2.4's preparation catch now compensates a `created` root when the `branch_contexts` write itself fails (`#compensateOrphanedRoot` with no context id — no row landed, so no delete leg): a live created worktree on an attached mount is invisible to both sweep legs, so the leak was permanent. (5) T2.3's run-terminal method rescoped from workspace-wide selection to the named clone — `retireRunClone({workspaceId, cloneId, trigger})`, the T2.3 task row updated in place — because the terminal sequence releases the workspace before it retires, so a delayed call would retire the NEXT run's fresh clone (failing its `creating` compare-and-swap or yanking its `ready` workspace back to `provisioning`); `ephemeral_clones` records no run provenance, so the identity comes from the caller, which Phase 3's terminal integration reads off the run's branch context. (6) T2.4's pre-bracket reuse busy probe gated on `executionMode === "worktree"` — the dispatch consumes `reuseWorktreeId` nowhere else, and the ungated probe turned an inert field into a `workspace.busy` refusal on `branch` and clone prepares. **Record true-up.** (7) The clone-service header's "cloning a hostile repository executes no repository-controlled code" outran Spec-010's hooks-scoped claim: checkout-time filter drivers named by content-carried `.gitattributes` DO run during `worktree add` and `git clone` checkouts — though the commands come from the user's own git config (git-lfs the canonical case), never from repo content, and no blanket git switch disables them without corrupting LFS checkouts — so both services' I-010-10 sections now scope the claim to hooks and record the filter-driver residual. Status stays `approved` per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule): mechanism repairs and a service-level signature change in the provider tasks of the same unmerged PR, implementing already-ratified obligations through existing vocabulary; no new invariant, CP row, phase, or Required ADR, and no change to the wire block or DDL.

- 2026-08-08 — Codex review round 2 (PR #297): two findings, one mechanism repair and one residual disposition. **Mechanism repair.** (1) D-010-10's neutralization missed the one hook git names by CONFIG VALUE rather than by a `hooks/`-resident file: a repo-local non-boolean `core.fsmonitor=<pathname>` IS the fsmonitor hook command, and index-refreshing verbs (`status` for the reuse cleanliness verdict, `worktree add` populating the new checkout) invoke it despite an empty `core.hooksPath` — both legs reproduced empirically on git 2.50.1. All three services' single git entry points now prepend `-c core.fsmonitor=false` alongside the hooksPath flag (structural parity keeps I-010-10's quantifier a property of one shared argv shape); the D-010-10 bullet, the I-010-10 row, and the T5.1 recipe gloss are trued up, and the acceptance fixtures arm a config-named fsmonitor sentinel behind a three-way mechanism-attribution control (live / hooksPath-only / full prefix). Two further repo-local config-named executables were probed and need no flag: `uploadpack.packObjectsHook` is honored only from protected config (git's own documented safety measure), and `core.alternateRefsCommand` fires only on receive-pack's alternate-tip advertisement — a push-target path outside every service's invocation set. **Residual disposition.** (2) The reuse cleanliness verdict can go stale between `validateReuse`'s `status` probe and the bind — accepted and recorded rather than re-probed. D-010-15's TOCTOU sentence is discharged by the probe itself: the "check" in `Spec-010 §Fallback Behavior`'s "becomes dirty between check and bind" is the caller's earlier observation (the `WorktreeReuseCheckRequest` wire probe, or however the candidate was picked), and `validateReuse` re-derives the verdict inside the bind operation, so dirt arriving since the caller looked refuses rather than binding silently. What remains is beyond any probe: dirtiness has no synchronous read (only a git spawn observes filesystem state the user's editor mutates freely), so no re-probe can share the context write's transaction; any bind-adjacent re-probe samples the head of a window whose tail dominates it — a prepared workspace sits `ready` for an unbounded time before a run first touches the root — and would newly refuse prepares that are fine, kicking a clean-validated candidate into `stale` repair over an editor save. The re-proof point that matters is run start, owned by the Phase-3 root-keyed run-setup gate obligation already recorded at the Phase 3 Goal; `validateReuse`'s docblock and the T2.4 bind-site comment carry the asymmetry argument (liveness IS re-proven at the bind — a synchronous row read exists; cleanliness deliberately is not). Status stays `approved` for the same Status Flip Rule reasoning as round 1: a mechanism completion and a recorded residual inside the same unmerged PR, existing vocabulary throughout, no new invariant, CP row, phase, or Required ADR, and no change to the wire block or DDL.

- 2026-08-09 — Phase-5 shipment errata bundle (PR #303 housekeeping; §6 node NS-50): the wording refinements and behavior recordings the Phase-5 review rounds adjudicated, landed on the housekeeping branch rather than in the shipping PR, across this plan's T5.1–T5.3 rows and `Spec-010 §Turn-Boundary Snapshots`. **Wording refinements — narrowings of what was already written.** The partial-restore clause no longer says that only a pre-mutation failure carries both enumerations empty: both are empty just as lawfully after a checkout that rewrote the whole worktree, whenever the snapshot tree held no colliding ignored path and no gitlink, because they are effect **classes** rather than a census — `failedStep` is what names how far the sequence got. The convergence claim is scoped to the commands: re-running the legs is safe, but recovery is not promised, since a fresh rollback re-enters through resolution and two shapes are terminal there (an index-close failure under a since-moved `HEAD`; a failed step whose ref the T5.3 sweep has pruned). Both `read-tree` operands are now spelled as the OIDs the checks verified — `<verified-snapshot-OID>` on the destructive leg, `<verified-HEAD-OID>` on the closing index reset — which satisfies each row's own requirement (which tree lands in the worktree; where the index lands) more exactly than a mutable name, whose re-resolution is precisely the check-then-act gap those rows exist to close. And `--no-deref` is folded into all three governance `update-ref` spellings with its measured two-sided rationale: unflagged, git carries a symbolic ref's update — must-not-exist check included — onto its referent, so the create plants a branch at the snapshot commit while the delete destroys the branch a planted symref points at, each exiting 0 and reporting success. **Behavior recordings — shipped behavior the corpus did not yet state.** The collision enumeration widens to the three measured shapes, with segment-wise ancestry, slash-stripped embedded-repo entries through the first two, and `160000` gitlinks seeding the required-directory set so the third fires at a gitlink path while the first two never do — leaving an ignored **directory** at a gitlink path to the divergent-gitlink enumeration and ignored content beneath a materialized gitlink to neither. The prune↔restore interleave is recorded as resolution-freezes-the-OID, two-sided, with its terminal composite shape. The second embedded-repository skip class (a `HEAD` hex length disagreeing with the superproject's object format) is recorded together with the porcelain comparison that makes skip-and-enumerate the honest answer — `git add -A` fabricates the gitlink OID there while hard-failing the commitless class. The skip list's `Skipped-Embedded-Repositories:` trailer and the restore-side preservation it feeds are recorded, as is the `-c core.useReplaceRefs=false` pin class, scoped to legs that interpret an object **id** rather than to ref resolution. The two remaining shipped conversion pins are folded into the same pass, since a recipe that omits them invites exactly the innocent-looking removal these recordings exist to prevent: `core.safecrlf=false` on the check-in leg — a veto rather than a conversion, staging the identical tree either way, but a host `true` turns whether a turn can be snapshotted at all into a host-config question — and `core.eol=lf` on the checkout leg, load-bearing only under the `core.autocrlf` pin and pinned to `lf` rather than `native` so LF-captured content does not restore as CRLF on a Windows host. **One residual recorded and deliberately not closed.** A sparse execution root loses out-of-cone content across a capture/restore pair and no pin closes it; the record carries the measurement and the scope — daemon-created roots and clones are never sparse, so the exposure is `branch` mode — and names both candidate closures (refuse capture in a sparse root, or read the sparse patterns so the staged tree retains the base's out-of-cone content) **without choosing between them**. That choice is a parked Plan-010 adjudication, not a decision this bundle makes. **Record true-ups.** §Target Areas gains `turn-snapshot-service.ts` in the owned `git/` enumeration and the `bootstrap/index.ts` wiring seam under its Plan-007-owned qualifier (a wiring call, not ownership); §Test And Verification Plan gains the Phase-5 roster row it had never carried. **Status stays `approved`** — for this plan and for Spec-010 alike — per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row 1 and its default rule: every item narrows an existing obligation or records behavior the shipped phase already implements, no task, phase, invariant, CP row, or Required ADR moves, and the wire block and DDL are untouched. The trailer erratum is the one worth arguing rather than assuming, since it changes what the corpus says the snapshot message **is** — a fixed string, plus a state-derived trailer — but the determinism property that clause exists for is preserved exactly (the message stays a pure function of project state, and a capture that skipped nothing mints a byte-identical OID to the trailerless form), and what the clause prohibits, identity and host content, is untouched. The restore-result shape is likewise unmoved: the preserved set rides a diagnostic precisely because that type is frozen for the T3.13 wire mapping.

- 2026-08-09 — Sparse-root capture-closure amendment (PR #307): the parked sparse-root adjudication (the errata-bundle entry above) is **settled for sparse-aware staging** — the product decision the NS-50 record parked — and this amendment lands the closure as governance. The Spec-010 capture bullet's residual passage becomes the closure contract: the in-cone oracle is git's own sparsity matcher (`git sparse-checkout check-rules`, plumbing since git 2.41 — below the floor capture in a sparse root fails closed through the existing typed capture-failure envelope, and the matcher, fatal in a non-sparse root, runs only downstream of sparse detection) over the repository's live sparse definition, measured porcelain-byte-identical (capture tree = `git add -A` tree) on git 2.50.1 across cone and non-cone fixtures, negation patterns included — the class where gitignore-semantics exclude machinery was refuted: gitignore evaluation cannot re-include below an excluded parent, so a nested non-cone negation's out-of-cone subtree is over-included and then silently dropped by the re-stat stream; the materialized-path edge is what rules out a `skip-worktree`-bit oracle — git clears the bit on materialization while porcelain still refuses the edit by pattern; the sparsity matcher reads no gitignore source, so the sparse and `.gitignore` evaluations stay separate by construction; restore needs no new worktree-mutating pipeline step — `read-tree --reset -u` re-applies the sparse definition, discarding materialized out-of-cone bytes, whose observed discarded subset the restore diagnostic channel reports since the result unions are frozen for Plan-004 T3.13. Eight review-round corrections are folded into the same contract: the exposure scope is **any sparse execution root**, not `branch` mode alone — `git worktree add` copies the current worktree's sparse-checkout patterns and worktree config (git/git `53255916b7`, git 2.36), so a `worktree`-mode root off a sparse canonical mount inherits the exposure, while ephemeral clones start full; the scratch index seeds from the **live index**, not `<base>`, so a `git add --sparse`-staged out-of-cone entry survives capture (measured: porcelain preserves the live cached entry and the `<base>`-seeded tree diverged); capture records the boundary-time out-of-cone untracked path set as a conditional snapshot-message trailer and the restore's untracked-delete pass exempts exactly that set — boundary-time content preserved on disk, turn-created content deleted (a blanket oracle exemption, round 1's shape, would leak turn-created files past rollback, and the recorded set also holds under cone edits between capture and restore); the materialized-path diagnostic reports the observed discarded subset under the service's observed-not-bookkept discipline, never the prospective set; detection keys on `core.sparseCheckout` alone — a deleted rules file under a true bit stays a sparse root whose failing matcher is the typed capture failure, never a not-sparse misclassification (porcelain still retains the out-of-cone entry in that state, measured); out-of-cone intent-to-add paths join the boundary set and restore drops their live-index entries (index-only) before the checkout so the on-disk copy survives to the exemption (measured: unguarded, `read-tree --reset -u` deletes the only copy of a path `write-tree` omits exactly as porcelain does); the trailer streams to `commit-tree` via `-F -` on stdin, never `-m` argv, so an unbounded boundary set cannot meet a platform argv bound (transport-only, OID unchanged); and the porcelain-equivalence claims are stated under the pipeline's pinned exclusion inputs, the deliberate host-local-ignore divergence standing. This plan gains Phase 6 / T6.1 and I-010-24 — capture-side implementation, the restore-side diagnostic enumeration, the T5.1 characterization-case inversion, and the service-header true-ups (the residual row converts to the closure row; the header's `in_place` exposure vocabulary is corrected, `in_place` naming no member of the four-mode `ExecutionMode` union — the matching [`cross-plan-dependencies.md` §6](../architecture/cross-plan-dependencies.md#6-active-next-steps-dag) NS-50 entry repairs, the `in_place` exposure-vocabulary correction (now the widened sparse-root scope above, not `branch` alone) and that entry's parked-adjudication clause now settled by this amendment, rode the restoring delta PR — delivered 2026-08-09 via PR #308, both corrected in place with dated records) — and the T5.1/T5.2 parked-adjudication clauses are trued up in place. Phase 6 is deliberately a **new discrete phase** rather than a Phase-5 EXTEND: adding a task to the shipped Phase 5 would reclassify it `partially_shipped` and re-block Plan-004 Phase 3's met `plan_phase, plan: 010, phase: 5, status: merged` leg. **Status flip:** the new phase, task, and invariant is a [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row-3 flip on this plan (the capture-recipe behavior change in sparse roots is row 4), and the capture bullet's Required-Behavior change flips Spec-010 under the runbook's spec-amendment rule — both headers sit `review` from this PR with the §Preconditions boxes above re-opened and tracking the restore. The restoring targeted readiness-audit delta — run adversarially over this amendment's diff — lands as the follow-on delta PR per the corpus's two-PR flip/restore shape (the B22 → #220, #279 → #283, #281 → #282, and #284 → #299 precedents), minting the §6 audit node born-`completed`, restoring both Statuses, moving the README plan and spec censuses back, and re-discharging the [runbook §Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate); its findings ride §Notes per the audit-box convention. **Restoring-delta record (2026-08-09, PR #308 / §6 node NS-51):** the adversarial re-audit over the merged amendment diff (seeded-refutation negative control passed; every prior review round's disposition fed as input) returned one actionable finding — the restore-mutation claim left unswept at four sites after round 3's intent-to-add index drop made it false at index level under T5.2's own worktree-and-index non-mutation definition, repaired to the spec's `worktree-mutating` scoping, with the drop's failure attribution stated: it runs under the `derive-enumerations` step cursor — one atomic `update-index` invocation whose failure leaves index and worktree untouched, keeping that step's failure-before-any-mutation reading true where the checkout step would falsely assert an attempted destructive checkout — under the frozen five-member restore-step vocabulary — and five polish findings, all amended in the same swap: the T5.1/spec `-m` transport stragglers converted to the `-F -` stream form, the config-bit-alone detection predicate and the availability-claim scoping added to the spec bullet, six added test arms (converse detection quadrant, sparse-format-marker determinism, unmerged parity, absent-trailer vintage, restore-side floor, drop attribution), and the live-index seed-read fail-closed disposition. Six completeness legs were stated in the same swap: restore-side oracle refusal at `derive-enumerations`, daemon-local capture-step vocabulary growth (`detect-sparse-root`, `check-sparse-rules`; the re-sourced seed read keeps `seed-index`), the `Sparse-Boundary-Paths:` trailer key and its after-skipped-repositories order as OID contract, absent-trailer-decodes-empty for pre-closure snapshots, out-of-cone unmerged `write-tree` parity, and the widened T6.1 Spec-coverage enumeration. The stale Phase-6 Goal and §Notes fragments — pre-round-2 `<base>`-persistence and single-addition wording the later rounds superseded — were trued up in the same PR. **Codex round 1 (PR #308)** hardened two legs, folded into the same surfaces: the live-index seed is taken under git's own index lock (`.git/index.lock` created exclusively, released without writing — readers never observe a writer, so an unlocked boundary seed silently reads the pre-write index, measured on git 2.50.1, and the omitted racing staged state would be erased by a later restore; contention retries briefly, then fails capture typed), and the sparse trailer became the closure's **unconditional-in-sparse-roots format marker** (the empty set spells `[]`): restore refuses a trailer-absent snapshot typed in a detected sparse restore root — a pre-closure sparse capture's truncated tree is message-indistinguishable from a safe full-tree vintage — while non-sparse restore roots keep absent-decodes-empty and the legacy-sparse-into-non-sparse-root shape stays the recorded pre-closure residual. **Codex round 2** hardened both legs again: the seed's lock and copy now name the per-root index path resolved via `git rev-parse --git-path index` — a linked worktree's index lives under the main repository's `.git/worktrees/<id>/`, where a literal `.git/index.lock` fails against the gitfile or locks another worktree's index, and `worktree` is the default mode — with a linked-worktree contention fixture joining the main-checkout one; and the pre-drop widened from intent-to-add-only to every recorded-boundary-set index entry absent from the snapshot tree, because `git add --sparse` during the turn converts a boundary untracked path into an ordinary stage-0 entry the narrower drop would miss and the checkout would then delete from disk — the staged-boundary-path test arm pins it.

- 2026-08-10 — Boundary-obstruction refusal + kind-scoped exemption naming amendment (PR #311; the obligation the [`cross-plan-dependencies.md` §6](../architecture/cross-plan-dependencies.md#6-active-next-steps-dag) NS-52 entry recorded as **owed** when Phase 6 merged): the **naming** leg for behavior that shipped 2026-08-10 with Phase 6 / T6.1 (PR #309, squash `d83ae0e`) after that PR's Codex round hardened the boundary-set path. Nothing is redesigned here; the three legs are the shipped mechanism, and the disposition question the shipment left open was settled by product decision the same day — **typed refusal over restore-wins-with-enumeration** — routed as a queued amendment on the targeted-amendment channel PR #307 → PR #308 established. **(1) Pre-mutation boundary-obstruction refusal.** In a restore whose snapshot recorded a sparse boundary set, the derivation refuses typed at `derive-enumerations` — before any index or worktree mutation, the position the trailer-absent vintage refusal and the restore-side oracle floor already occupy — when the checkout would destroy recorded boundary content. The destructive shapes are the `Spec-010 §Turn-Boundary Snapshots` Scope bullet's own ignored-path collision taxonomy, transferred whole because boundary paths meet the same `read-tree --reset -u` through the same mechanism: materializing snapshot paths strictly beneath a boundary FILE entry, a materializing blob exactly at a boundary DIRECTORY entry, and a materializing snapshot blob at a proper ancestor of either kind — the third the shape a sparse root makes ordinary, since a `skip-worktree` out-of-cone blob leaves its path absent on disk for the turn to create a directory over while the index keeps the cached blob; each measured destructive at exit 0 on git 2.50.1 before the guard existed, the third measured during the shipping PR's own follow-up sweep. Two shapes are deliberately **admitted**: a directory entry's strict descendants materializing (the checkout merges into the preserved directory — the ignored-embedded-repository precedent) and a conflicting snapshot path the live cone leaves unwritten. Materialization is scored through the same `check-rules` oracle on the same fail-closed floor in a sparse restore root and is unconditionally true in a non-sparse one, with `160000` gitlink displacers exempt from that scoring — git never marks a gitlink `skip-worktree` (`ls-files -t` reports `H` for an out-of-cone one, measured) and the checkout creates its directory regardless, so oracle-scoring them would fail **open** exactly on the destructive case. The detail names every obstructing boundary path and collapses displacers past the first three to a count, and the whole refusal rides the shipped typed-failure envelope: no result-union arm, no restore-step vocabulary growth. **Why refuse rather than report and proceed** is recorded with the behavior: the refusal is what keeps I-010-24's boundary-content survival clause literally true **across the displacement class** — the index pre-drop and the delete-pass exemption each stop one destructive mechanism, and neither reaches the case where a recorded boundary path and a materializing snapshot path want the same place on disk — and the restore-wins-with-enumeration precedent is explicitly scoped to **ignored** paths, which the snapshot contract excludes from the outset, where recorded boundary paths are that contract's protected subject. A user-facing force-override is named as a possible future enhancement and **deliberately not scheduled** — a non-commitment, not a deferral with a gate. **(2) Kind-preserving trailer and kind-scoped exemption widths.** The trailer records `ls-files -o`'s trailing slash verbatim (still a sorted-JSON string array, the `[]` marker unchanged; the format was unreleased before PR #309, so no vintage consequence rides it and the absent-trailer vintage refusal is untouched), decode splits the set once into file paths and directory paths, and the delete-pass exemption is **kind-exact** for file entries — only a slash-free listing entry at the exact path, so a turn-created embedded repository standing at a boundary FILE path still deletes — and **spelling-tolerant across the segment-boundary subtree** for directory entries, exempting the restore listing's slash-free spelling of the directory as readily as its slashed one — a directory git refused to descend is one capture could not enumerate, so path identity for that entry is its subtree, the sibling skipped-embedded-repositories semantic, whose concrete case is a boundary-time out-of-cone embedded repository the turn stripped of its `.git` and the restore-time listing re-reports as per-file entries. Named residuals, both survivable: a turn-created file **inside** such a directory is over-protected — the same accepted shape the sibling exemption carries — and its deliberately retained mirror, a turn-created **file** standing at a recorded directory path, which the directory arm's equality case likewise exempts — while turn-created out-of-cone files anywhere else still delete like their in-cone counterparts, which is why I-010-24's exempts-exactly clause is **rescoped with the kind qualification rather than dropped**. **(3) Byte-exact boundary subtraction.** The trailer derivation subtracts candidates against the written tree through the byte-keyed listing bijection, never decoded-vs-decoded: a decode-aliased tracked path would otherwise subtract a distinct boundary path away, costing that path its intent-to-add pre-drop and destroying the only copy on disk. The aliasing PR #309 left — confined to the trailer's JSON representation and the restore side's decoded keying, protective in direction as shipped — is **closed rather than recorded**, by the same-day code round below. **Status flip:** [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) **row 4** on this plan — a behavior change in the plan body, with row 3 deliberately not engaged (no phase, task, invariant, CP row, or Required ADR is minted; I-010-24 and T6.1 already exist and are extended in place) — and the capture bullet's Required Behavior change flips Spec-010 under the runbook's spec-amendment rule, re-triggering its [§Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate). Both headers sit `review` from this PR with the §Preconditions paired-spec and audit boxes re-opened and tracking the restore; the restoring targeted readiness-audit delta lands as the follow-on delta PR, minting the §6 audit node born-`completed`, restoring both Statuses, and moving the README censuses back. (**Restoring-delta record (2026-08-10, §6 node NS-53):** delivered exactly so — both Statuses restored, both re-opened boxes re-checked, both README census sites moved back together, and the node minted born-`completed`; the findings ride the 2026-08-10 restoring-delta §Notes entry below.) **Surfaces touched, and two deliberately not.** I-010-24's restore half carries the refusal and the rescoped exemption; T6.1's contract carries both plus the byte-exact subtraction, its trailer clause now naming **three** deliberate divergences from the `Skipped-Embedded-Repositories:` conventions rather than one (the kind divergence and the encoding divergence join the unconditional-marker one — the sibling records slash-stripped names where this trailer preserves the slash, and the sibling's strings are decoded text where these are `latin1` byte keys, the second of those closed into place by the same-day code round recorded below), its **Restore additions** parenthetical re-counted to one re-keying, one refusal, one diagnostic, its Spec-coverage enumeration widened, and its Tests roster plus the §Test And Verification Plan Phase-6 row naming the shipped arms this amendment describes (byte-exact subtraction, the three kind-width arms, and the obstruction-refusal arms across all three destructive shapes, both admitted non-shapes, and the bounded-detail case) — named from the merged suite rather than coined. The §Shipment Manifest entry's `notes:` owed-clause is **discharged in place**; its `spec_coverage` mirror is left at the shipment-time enumeration, because that field records what PR #309 cited at merge rather than what the task row says today, and no code moves in this PR. **Census consequence:** both README plan-status sites move together (16 `approved` / 10 `review` → **15 / 11**, Plan-010 rejoining the `review` enumeration in each) and the spec census re-derives with Spec-010's flip (21 / 7 → **20 / 8**); CLAUDE.md's Plan-010 clause gains the dated follow-on, its counts staying delegated to the README. No §6 node is minted here — the restoring delta PR mints it, the PR #308 / NS-51 pattern (minted 2026-08-10 as **NS-53**) — and NS-52's owed sentence is discharged in place. **Code round ahead of this naming (2026-08-10, PR #312, `develop` squash `9d3b93b`):** the three code gaps this amendment's review round surfaced were fixed in code **first**, so what is named above is a repaired contract rather than a ledger of destructive residuals. The `Sparse-Boundary-Paths:` trailer was re-encoded from `utf8` to `latin1`, which is what makes it a byte record: a `utf8` trailer collapses every invalid sequence onto `U+FFFD`, and the destructive direction of that collapse was the restore-side pre-drop's snapshot-tree exclusion firing spuriously and leaving the checkout to delete the only copy — under `latin1` the sorted array is byte-lexicographic by construction and a capture-minted key is matched at restore verbatim (the format is still unreleased, so again no vintage consequence rides the change). The index pre-drop widened from the file half alone — which had read as a shape-of-the-data argument and was a hole — to the recorded set's **full width**, file exact-path and directory subtree alike, the snapshot-tree exclusion retained and both halves byte-keyed. And the untracked-delete pass gained a **whole-pass pre-mutation round-trip guard**: every entry it is about to delete must re-encode from its decoded name back to its recorded bytes, and if any one does not the pass refuses before removing anything (`partial_restore` at `delete-untracked`), rather than removing a path through a decode alias — a string-typed removal seam can refuse such a name honestly but cannot address it, and `Buffer`-typed paths throughout stay deliberately untaken while the seam holds three narrow mutation verbs. What survives is over-protection on the exemption legs — the two residuals named in leg (2) — plus one corner that runs fail-**open** rather than protective and is pre-existing rather than introduced by any of this: the obstruction guard's presence probe addresses the filesystem at a boundary path's **decoded** spelling, that seam being string-typed, and is unreachable on any filesystem that rejects non-UTF-8 names (APFS does, at `creat`). Leg (3)'s recorded aliasing is closed by the first of the three, so no destructive residual remains in the aliasing class the byte-keying governs.

- 2026-08-10 — Boundary-obstruction naming amendment's restoring targeted readiness-audit delta (PR #313; [`cross-plan-dependencies.md` §6](../architecture/cross-plan-dependencies.md#6-active-next-steps-dag) node NS-53, born-`completed`): the restoring leg of the two-PR flip/restore shape the 2026-08-10 entry above opened, run on the same targeted-amendment channel PR #307 → PR #308 established. **Scope and method.** The audit target is the merged amendment diff, read adversarially against the code truth at `develop` — the `packages/runtime-daemon/src/git/turn-snapshot-service.ts#TurnSnapshotService` docblocks and functions as PR #312 (squash `9d3b93b`) left them, which is the state the naming amendment describes. Every prior disposition was fed as input rather than relitigated, and each was verified to have landed: the amendment PR's eight review threads (the typed-failure envelope vocabulary; the shape-(a) displacing party; the added citations; the pre-drop width; the decode-alias direction; the stale NS-52 References parenthetical; case-folding adjudicated out of scope to BL-147; the slash-strip kind alias — three of them closed in code by PR #312 ahead of the naming, the rest in docs) and the parallel audit's findings (the pre-drop width leg, the disposition universals scoped to the displacement class, the sparse-format-marker rename, the NS-52 References and discharge legs, the width spellings, and the third non-shape tied at the §6 discharge clause to byte-exact subtraction). Six axes were run: **(1) claim-vs-code** — every mechanism sentence in I-010-24's restore half, the T6.1 contract, the `Spec-010 §Turn-Boundary Snapshots` capture bullet and its 2026-08-10 banner checked against the shipped functions, including the three width predicates and the one they share (the delete exemption's kind-exact file arm and spelling-tolerant directory arm, the pre-drop's same width minus the slash question, the obstruction guard's proper-ancestor walk), the directory arm's equality case that both named over-protection residuals rest on, the `160000` displacer carve landing at the oracle rather than at the refusal, the single atomic `update-index --force-remove` drop under the `derive-enumerations` cursor, the refusal's position ahead of that drop, the frozen three-arm result unions, the five-member restore-step vocabulary and the two added capture-step members, the `latin1` byte-record trailer written unconditionally in sparse roots, and the subtraction that runs against the whole `ls-tree -r` listing on listing bytes; **(2) internal coherence** across the six touched surfaces; **(3) quoted-title fidelity** — all twelve `it` titles the spec quotes byte-compared against the merged suite, twelve exact matches, the comparator negative-controlled against a perturbed title; **(4) citation-anchor fidelity** — both added `git-ls-files` anchors (`--others`, `--directory`) resolve against the published page's own ids; **(5) census arithmetic** re-derived from the per-file `Status` headers rather than copied, both directions closing to 28 plans and 28 specs; **(6) marker-line integrity** — the Gate-4 `Spec coverage:` / `Verifies invariant:` lines untouched and unbackticked. The mandatory **seeded-refutation negative control** was run on the gitlink carve: the verified-true form is that `160000` displacers are exempt from the _oracle scoring_ (scoring them would fail open on the destructive case), and the plausible-but-wrong twin — that they are exempt from the _refusal_ — predicts the opposite code at two specific sites (the shape-1 displacer map would filter gitlinks, and the materialization predicate would score rather than short-circuit them); the shipped code takes the true form at both, so the method discriminates and the control passed. **Findings: zero critical, zero major, zero actionable; one polish, amended in the same swap.** I-010-24's obstruction clause read as though the frozen `partial_restore` arm carried the refusal detail; the arm carries `failedStep` and the two enumerations and has no detail field, the obstruction detail riding the restore diagnostic channel — the invariant is now scoped to that, matching the T6.1 row and the spec bullet, which were already exact. One near-miss is recorded because the method is the point: the §6 NS-52 entry's `189 file-scoped arms` at the Phase-6 merge appears to disagree with a plain count of `it(` call sites, and reconciles exactly once the suite's `it.skipIf` alias arms are counted with them — the figure is correct and correctly scoped to that PR's own merge, and no surface claims a post-PR-#312 arm count. A stale-pending sweep over the six surfaces discharged every forward-looking restoring-delta clause this delta satisfies (both §Preconditions boxes, the Phase-6 `preconditions:` window comment, the §Shipment Manifest `notes:` discharge clause, the 2026-08-10 §Notes entry's own two, the Spec-010 banner, both README census paragraphs, the CLAUDE.md gate clause, and the §6 NS-52 Status parenthetical, References row and auto-create log line); BL-147's case-sensitivity question stays open and dated, being genuinely future work rather than a claim this diff satisfies. The campaign sweep required before any status promotion found the only committed campaign hit — the capability-enhancement plan's Plan-010 rows (Tasks 11, 20, 23, 26) — all checked and closed, with nothing scheduled against either file, so nothing vetoes or re-times the promotion. **Restoration effected.** Status `review → approved` on this plan and on Spec-010 in the same swap — the spec-side promotion re-discharging the [runbook §Spec-Status Promotion Gate](../operations/plan-implementation-readiness-audit-runbook.md#spec-status-promotion-gate): Spec-010's `Depends On` are two canonical docs plus the `approved` Spec-009, the plan's Required ADR ADR-006 is `accepted`, the spec carries no Open Questions section, and no code PR shipped citing its Required Behavior rows while it sat `review` (PR #312 landed ahead of the PR #311 flip, while the spec stood `approved`); both re-opened §Preconditions boxes re-checked with Delivered records; both README census sites moved together (plans 15 `approved` / 11 `review` → **16 / 10**, specs 20 / 8 → **21 / 7**), with the §Build Order and §Project Status paragraphs re-derived rather than mirrored; CLAUDE.md's Plan-010 clause converted to its delivered form; and §6 gains NS-53 born-`completed` with the `NS52 --> NS53` edge, NS-52's sink quantifier superseded to **{NS-53}** and its completed-set census moved 48 → **49** of the DAG's **53** nodes. **Status stays `approved`** on the restoration itself per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row 1 and its default rule: one narrowing citation-grade repair and the dated records this delta exists to write, with no new invariant, CP row, phase, task or Required ADR, no plan-body behavior change, and no code in the diff.

- 2026-08-16 — Reference-runtime parity survey (2026-08-12): the Plan-010-relevant evidence is recorded in §Risks And Blockers as one bullet carrying the motivating observation from the campaign's surveyed reference runtime — per-message git checkpointing shipped and later removed, on ref-pollution and never-used-file-restore grounds — paired with the structural answers this plan already carries (I-010-21's ref-namespace confinement, I-010-22's create-only per-epoch writes, T5.3's window-based prune) and the reason the never-used ground does not transfer to a file leg the governed rollback intervention consumes rather than offers. The same round narrows the existing provider-binary re-verify obligation in that section with one named question — what a `--resume-session-at` fork carries across a provider-side context-compaction boundary — mirrored in the paired obligation sentence at `Spec-004 §Driver-Level Rollback Mechanics`. **Status stays `approved`** per the [runbook §Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) row 1 and its default rule: risk/rationale prose plus one narrowing of an obligation that already exists, with no new invariant, CP row, phase, task or Required ADR, no plan-body behavior change, and no code in the diff.

- 2026-08-17 — Normalized-root transport closure ([`cross-plan-dependencies.md` §6](../architecture/cross-plan-dependencies.md#6-active-next-steps-dag) node NS-69, born-`completed`; the third same-day amendment to this pair, one seam narrower than NS-65). **The seam.** NS-65's second round persisted the normalized checkout root as `workspaces.metadata.checkoutRoot` and made CP-010-15's restore leg **consume** it rather than re-derive from a possibly-deleted nested path. CP-010-12, meanwhile, makes capture and restore **pure callees** that receive their inputs and resolve nothing themselves. Both rules are right and neither was wrong, but nothing in the corpus transported the value across the boundary between them: the caller-resolved `execution_root` still named the nested path, `workspaces.metadata.checkoutRoot` sat on a row the callee was forbidden to read, and in the one case the persistence exists for — a nested bound directory deleted by the turn before rollback — the callee could reach the durable value by no lawful route. CP-010-15's own "CP-010-12's pure-callee contract is unchanged" sentence was therefore true only in the sense that nothing had yet said how it would remain so. **The closure.** `run_execution_contexts` gains `checkout_root` (`TEXT NOT NULL`), captured at context creation by T3.2 from the workspace's persisted key — deriving the top level from the just-materialized `execution_root`, with the carrier healed, for pre-amendment rows (the round-2 correction: `canonical_root` names the bind-time checkout, the wrong tree for provisioned modes) — and read by the B9 caller from the same row it already reads `execution_root` and `execution_mode` from, then passed as CP-010-12's seventh caller-resolved input. The discriminating requirement was that the value outlive the path it describes, which is precisely the argument `git_common_dir` already won one column over: capture per run, at context creation, while the tree exists, so ref operations (there) and whole-tree restore (here) survive the root's retirement or deletion. Capture, which runs while the tree exists, re-verifies the supplied value and fails typed on disagreement; restore consumes it untouched. The callee gains an input, not a lookup — no `workspaces` read, no upward filesystem walk, no dependency on the execution root still existing — so the pure-callee contract is now unchanged **by construction** rather than by assertion, and CP-010-15 says so in those terms. **Migration scheduling.** Phase 1 already shipped the four-table CREATE as version 4 (`0004-worktree-lifecycle.ts`, PR #253), which is not amended: an applied guarded block never re-runs, so editing it would change fresh installs only and fork the schema by install date. The column lands instead in a next-ordinal guarded block scheduled with **Phase 3**, the phase of T3.2 — its only writer. Earlier would ship a NOT NULL column nothing can populate; later would let T3.2 write a column that does not exist. The block is the documented lang_altertable §8 table-rebuild (create the successor with `checkout_root TEXT NOT NULL` in canonical position → copy omitting the column → drop → rename → recreate the index), not an `ADD COLUMN`: §4 refuses a NOT-NULL-without-default add unconditionally, and the empty-table allowance the bundled engine exhibits (verified against SQLite 3.53.0 via `better-sqlite3` 12.9.0 — the add succeeds empty, and a populated table refuses `Cannot add a NOT NULL column with default value NULL`) is undocumented behavior a load-bearing migration must not ride (Codex PR #346 round 1). The fail-closed arm rides the copy step instead — a pre-existing row fails the engine-enforced NOT NULL at `INSERT … SELECT` time — with no `DEFAULT ''` sentinel that would let a row claim a checkout root it never had. The table is in fact row-less in every deployed instance: no production writer of it has ever shipped (T3.2 is the first), the only pre-existing writes being test fixtures. Two shipped-test consequences were traced rather than left to the implementing PR: `migration-shape.test.ts`'s `run_execution_contexts` pin asserts the exact ordered column list and so breaks by construction, with the rebuilt table declaring the column in the schema doc's canonical CID position — no `session_events.retention_class`-style last-position divergence to state, the doc order and `PRAGMA table_info` order staying identical; and the I-010-2 contract↔DDL tripwire is **unaffected**, because it extracts enum vocabularies and predicate CHECKs from the shipped migration SQL and `checkout_root` carries neither (it sits outside the mode-conditional CHECK by design), so this amendment authors no contracts change. **Node-local grounding.** The tenancy corollary NS-65's review round added compares checkout roots to enforce one-holding-run; that comparison now states its scope as a property of the query rather than an assumption — it reads the local daemon's `workspaces` rows joined through `repo_mount_id` to `repo_mounts.node_id` and filtered to the deciding node — `workspaces` itself carries no node column, and one node's database lawfully holds other nodes' mount rows (D-009-7), so the join filter is load-bearing (Codex PR #346 round 1). The owning-node column is the same one `idx_repo_mounts_active_root` keys canonical-root uniqueness on (`Spec-009 §State And Data Implications`), reused rather than coined; no cross-node coordination is implied or added. **Surfaces.** D-010-5 (the column, its mode-agnostic justification against CP-010-4 / I-010-14, and the rebuild-lineage callout), §Data And Storage Changes and §Target Areas (both re-derived — two migrations, and "verbatim migration content" now stated per migration), T1.3 (migration lineage: version 4 excludes the column and is not to be amended), T3.2 (the capture with its fallback, the migration files, the shipped `migration-shape.test.ts` pin named as growing in the same PR, and five new test arms), CP-010-12 (seven caller-resolved inputs; purity restated as the absence of a `workspaces` read), CP-010-15 (the five-link carrier chain and the node-scoped query-shape sentence), I-010-14 (the row is complete when written), I-010-24 (the normalized root is supplied, never discovered), T5.1 / T5.2 (`checkoutRoot` on both callee signatures, flagged as joining the shipped signatures in the Phase-2B PR per the CP-010-14 convention), plus `local-sqlite-schema.md` (the DDL column, the table's migration-lineage header, and the `workspaces.metadata` transport cross-reference) and Plan-009 T-009-2B-4 / CP-009-10 on the consuming side. **Status adjudication.** Spec-010 and this plan flip `approved → review` and are restored `approved` in the SAME diff by the NS-69 targeted readiness-audit delta's neighbor-swap record — this plan under [Status Flip Rule](../operations/plan-implementation-readiness-audit-runbook.md#status-flip-rule) **row 4** (a plan-body behavior change; no phase, task, invariant, CP row, or Required ADR minted), Spec-010 under the spec-amendment rule, Plan-009 likewise row 4 for its T-009-2B-4 growth, and [Plan-004](./004-queue-steer-pause-resume.md) likewise row 4 for its caller-side seven-input restatement (Spec-004 joining at the PR #346 round-2 fold — the vanished-checkout rootless convergence — with the paired-spec box taking a scoped Re-opened/Delivered record). The pair's fifth flip-and-restore, third of 2026-08-17. No table is added or removed (a column add is not a table add), so the SQLite table census holds at 55; both §Preconditions boxes carry the Re-opened/Delivered records with the audit findings stated in the audit box. **Reciprocal reconciled in the same diff, code-gated.** Growing CP-010-12's input list would strand Plan-004's caller-side enumerations on the pre-amendment field set — I-004-16, CP-004-11, and T3.13's two literal signatures plus its turn-boundary test clause — so this same diff restates them to the seven-input contract: Plan-004 flips `approved → review` under Status Flip Rule row 4 and is restored `approved` in-swap by the NS-69 delta's neighbor-swap record (Spec-004 later joining at the PR #346 round-2 fold, the paired-spec box taking a scoped Re-opened/Delivered record). What the doc repair cannot close — the shipped callee signatures stay five-field until the Plan-009 Phase-2B EXTEND, the `checkout_root` column is absent until this plan's Phase 3, and Plan-004 sits a tier earlier than both — Plan-004's new born-unchecked "Snapshot-callee checkout-root parameter shipped" box closes mechanically: T3.13 does not dispatch until both producer phases have merged, so no caller is ever authored against the superseded five-field form for the Phase-2B signature growth to break. **Round-1 fold (Codex PR #346, 2026-08-17).** The PR's review corrected three of this record's design facts in place: (1) the migration is the documented lang_altertable §8 table-rebuild, not the `ADD COLUMN` first specified — the fail-closed arm moved from the undocumented empty-table add to the documented copy step, the References row now cites the procedure, and the column lands in canonical CID position; (2) the checkout-tenancy comparison's node scoping is the query's join (`workspaces.repo_mount_id → repo_mounts.node_id`, filtered to the deciding node), not a whole-database assumption — Spec-010's tenancy sentence and Plan-009 T-009-2B-4's node-scope arm restated to match; (3) the checkout-root box's Gate-5 entry was mechanically deadlocked phase-scoped on Plan-004 Phase 3, whose T3.10 seam this plan's Phase 3 gates on (CP-010-9) — Plan-004 re-homed its rollback cluster (T3.12–T3.14, T3.16, T3.17, T4.7) to a Phase-3B explicit-label supplement carrying the box (its T3.13 conversation-only short-circuit narrowed to the released-context class in the same fold), and Plan-009 Phase 2B gained the reciprocal `{ plan: 010, phase: 3 }` gate its deleted-bound-directory fixture's `checkout_root` read requires; the un-deadlocked order is Plan-004 Phase 3 → this plan's Phase 3 → Plan-009 Phase 2B → Plan-004 Phase 3B.

### Cross-Plan Amendments

**PR #297 (2026-08-07) — CP-009-3 bracket-scoping reciprocal ([Plan-009 §Cross-Plan Obligations](./009-repo-attachment-and-workspace-binding.md#cross-plan-obligations)).**

T2.4's implementation surfaced that Plan-009's CP-009-3 row — the provider-side statement of the obligation this plan's CP-010-3 consumes — carried the same unconditional quantifier the 2026-08-07 §Notes entry above scopes: it obliged the gate before preparing an execution root for ANY writable mode. The bracket's own provisioner cannot satisfy that sentence, because Plan-009's own gate raises its invariant error for `provisioning` by design. The row now scopes the obligation to prepares that find the workspace outside the open reprovision bracket, names the lawful skip, and cross-cites Plan-010 I-010-12 / CP-010-3 as the same scoping. One sentence-level edit, disclosed inline in the row (`PR #297` marker) and recorded on the amended side at Plan-009 §Notes. Plan-009's status is unmoved — the edit narrows a written obligation to what its own gate's ratified behavior already enforced; Plan-009 retains ownership of the row and Plan-010 claims no ongoing edit rights.

**Refs:** Plan-009 CP-009-3; this plan's I-010-12 / CP-010-3 / D-010-16 rows and the 2026-08-07 §Notes entry.

**PR #340 (2026-08-17) — CP-010-14 branch-mode root consent + Spec-010 branch-mode sentence correction ([Plan-009 §Cross-Plan Obligations](./009-repo-attachment-and-workspace-binding.md#cross-plan-obligations), CP-009-9).**

Plan-009's common-directory repo-identity amendment (§6 node NS-63) moves `repo_mounts.canonical_root` onto the MAIN checkout's root, invalidating the premise under which two of this plan's surfaces were written: `#bindBranchMode` returns `mount.canonical_root` as the branch-mode execution root, and `Spec-010 §Turn-Boundary Snapshots` named that root "the shared main checkout" — both correct only while the canonical root meant the tree the participant bound. This entry registers CP-010-14 — consent for Plan-009 T-009-2B-4's Phase-2B EXTEND of `execution-root-service.ts` (branch arm re-rooted at `workspaces.metadata.boundRoot`, fallback `canonical_root` for pre-amendment rows) — and the Spec-010 sentence is corrected to the workspace's bound checkout in the same PR. Status adjudication: Spec-010 and this plan flip `approved → review` for the correction and are restored `approved` in the SAME diff (the PR #321/#323 neighbor-swap shape) — the correction renames which tree the sentence points at without changing snapshot mechanics, applicability, restore semantics, or any wire/DDL surface, and the consuming code change is held by Plan-009's Phase-2B carrier box, landing atomically with the identity swap. This plan's §Preconditions boxes track the flip exactly as the PR #308/#313 precedents do: the paired-spec box re-opens scoped to the Spec-010 sentence correction and the audit box re-opens scoped to the new CP row (Status Flip Rule row 3 — a new CP-NNN-M entry), each Re-opened and Delivered in this same diff with the NS-63 delta's in-swap neighbor-swap record as the restoring vehicle; the new obligation row's code executes under Plan-009's audited Phase 2B.

**Refs:** Plan-009 CP-009-9 / NS-63; this plan's CP-010-14 row; Spec-010 §Turn-Boundary Snapshots and the 2026-08-17 amendment blockquote in Spec-010 §Resolved Questions and V1 Scope Decisions.

**Carried-findings adjudication (2026-08-17) — CP-010-15 turn-snapshot normalization consent + I-010-6 / D-010-9 bound-checkout rewrite ([Plan-009 §Cross-Plan Obligations](./009-repo-attachment-and-workspace-binding.md#cross-plan-obligations), CP-009-10; §6 node NS-65).**

Two of the eleven PR #340 round-3/4 findings carried on Plan-009's Phase 2B carrier box name surfaces this plan owns, and both land here in the same swap as the Spec-009/Plan-009 adjudication amendment. (1) The snapshot service's capture legs are cwd-scoped while restore's `read-tree --reset -u` updates the whole working tree regardless, so a nested `boundRoot` — lawful once CP-010-14 re-bases branch mode — would capture only the nested prefix and then restore the whole tree against it, erasing pre-snapshot state outside the prefix; CP-010-15 consents Plan-009 T-009-2B-4's EXTEND of `git/turn-snapshot-service.ts`, normalizing BOTH capture and restore execution-root inputs to the enclosing working-tree top level at service entry, with `Spec-010 §Turn-Boundary Snapshots`' applicability bullet recording the rule. (2) I-010-6 and D-010-9 scoped the no-mutating-git-verbs prohibition and its verification to the main checkout — written when branch mode's root WAS the main checkout — leaving the bound linked checkout formally unprotected under CP-010-14; both are rewritten around the BOUND checkout, D-010-9 gaining the linked-worktree byte-identical assertion executed under Plan-009 T-009-2B-4, and the `Spec-010 §Resolved Questions and V1 Scope Decisions` branch-mode sentence re-scoped in the same swap. The adjudication PR's own review round then added the normalization's concurrency corollary on this plan's surfaces: `Spec-010 §State And Data Implications`' one-holding-run rule re-keys on the NORMALIZED checkout root (recorded on the CP-010-4 and CP-010-15 rows; executed at Plan-009 T-009-2B-4's run-boundary seam), so two workspaces bound to disjoint subdirectories of one checkout can never hold active runs concurrently. Its second round persisted the key: the normalized top level is derived at bind while the tree exists, rewritten at provisioning completion, and carried as `workspaces.metadata.checkoutRoot`, consumed by restore and the busy comparison alike, so neither ever re-derives the checkout from a nested bound path a turn may have deleted. Status adjudication: Spec-010 and this plan flip `approved → review` and are restored `approved` in the SAME diff by the NS-65 targeted readiness-audit delta's neighbor-swap record (the pair's fourth flip-and-restore, second of 2026-08-17); this plan's §Preconditions boxes carry the Re-opened/Delivered records, and the per-finding dispositions live in Plan-009's §Progress Log adjudication entry.

**Refs:** Plan-009 CP-009-10 / NS-65; this plan's CP-010-15 row, I-010-6, and D-010-9; Spec-010 §Turn-Boundary Snapshots applicability bullet and the 2026-08-17 carried-findings amendment blockquote.

## Done Checklist

- [ ] Code changes implemented
- [ ] Tests added or updated
- [ ] Verification completed
- [ ] Related docs updated

## References

- [SQLite — Foreign Key Support §5 "CREATE, ALTER and DROP TABLE commands"](https://www.sqlite.org/foreignkeys.html#fk_schemacommands) — the primary source for T1.3's deliberate forward FK references to the Plan-009 Phase 2 tables `repo_mounts` and `workspaces`, which is what makes Plan-009 Phase 2 a Phase-**2** gate rather than a Phase-1 one. Create time is unchecked: "A CREATE TABLE command operates the same whether or not foreign key constraints are enabled. The parent key definitions of foreign key constraints are not checked when a table is created. There is nothing stopping the user from creating a foreign key definition that refers to a parent table that does not exist." Detection is deferred to statement preparation — such errors "prevent the application from preparing SQL statements that modify the content of the child or parent tables in ways that use the foreign keys" — and the page names the exact message the daemon sees: "The English language error message for foreign key DML errors is usually 'foreign key mismatch' but can also be 'no such table' if the parent table does not exist." That last clause is why `packages/runtime-daemon/src/migrations/0004-worktree-lifecycle.ts#WORKTREE_LIFECYCLE_MIGRATION_SQL` — the exported SQL constant carrying the forward `REFERENCES` clauses, which is the evidence this row rests on — documents a premature insert as `SQLITE_ERROR: no such table`, not `SQLITE_CONSTRAINT_FOREIGNKEY` (confirmed 2026-07-26).
- [SQLite — Foreign Key Support §2 "Enabling Foreign Key Support"](https://www.sqlite.org/foreignkeys.html#fk_enable) — the precondition the clause above depends on, and the reason the premise is not vacuous here: "Foreign key constraints are disabled by default (for backwards compatibility), so must be enabled separately for each database connection." The daemon enables them for every connection in `packages/runtime-daemon/src/session/migration-runner.ts#applyPragmas` (`db.pragma("foreign_keys = ON")`), so the DML-time enforcement above is the live path rather than the default-off one. Two tests split that proof between them, each anchored at the exported identifier that supplies its own `it(...)` title: `packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#WRITE_INERT_ON_PARENTLESS_DB_TEST` and `packages/runtime-daemon/src/session/__tests__/migration-shape.test.ts#FORWARD_REFERENCES_DML_ENFORCEMENT_TEST`. **Both** open by asserting `pragma("foreign_keys", { simple: true }) === 1`, so neither claim rests on the default-off path; what differs is the behaviour each then proves. The first pins the `no such table` statement-preparation failure described above, on a handle migrated through version 4 in isolation — the only state where the parents are absent since Plan-009's version-10 migration landed. The second inserts a child row whose `repo_mount_id` names no existing parent row — the parent tables are Plan-009's real version-10 `repo_mounts` / `workspaces`, seeded with fixture rows — and pins `FOREIGN KEY constraint failed`, then shows a parent-backed insert succeeding; its pragma assertion is labelled in-test as a negative control for exactly that fixture-row pattern, establishing that the accept passes because the parent row exists rather than because enforcement is silently off. The anchors are identifiers rather than the spaced test titles because the gate's symbol matcher is identifier-shaped (`[A-Za-z_$][\w$]*`); binding each title to an exported constant is what makes this citation break when a test is renamed or deleted. The previous `#applyPragmas` anchor did not: it named a helper merely _imported_ into the test file, and the gate matches a symbol present anywhere in the file, so the cite kept validating with the evidence gone — presenting the behaviour as covered when it was not. Correction history for this row: within PR #254, round 2 fixed an initial conflation of the pair; round 4 fixed that fix, which had assigned the pragma assertion exclusively to the second test when the first makes it too; round 5 replaced the import-only anchor and corrected "dangling parent" to what the test actually exercises, a present parent table with a missing parent row (confirmed 2026-07-26). A PR #292 true-up (2026-08-04) re-grounded the fixture description when Plan-009's version-10 migration replaced the stub parents this row previously described with the real tables and fixture rows, and re-scoped the first test onto its isolation handle (disclosed at [Plan-009 §Cross-Plan Amendments](./009-repo-attachment-and-workspace-binding.md#cross-plan-amendments), the PR #292 (2026-08-04) session-substrate-suites block).
- [SQLite — ALTER TABLE](https://sqlite.org/lang_altertable.html) — the primary source for T3.2's `checkout_root` migration shape (2026-08-17 amendment; corrected at the NS-69 PR's round-1 fold). §4 "ALTER TABLE ADD COLUMN" states the constraint unconditionally — "If a NOT NULL constraint is specified, then the column must have a default value other than NULL." — with no empty-table exception documented, so the `ADD COLUMN` form the amendment first specified rested on undocumented engine behavior (the bundled 3.53.0 admits the add on a row-less table; nothing citable guarantees it). T3.2's guarded block instead follows §8 "Making Other Kinds Of Table Schema Changes": create the successor table with the canonical DDL, `INSERT INTO … SELECT` the predecessor's columns (omitting `checkout_root`, so any pre-existing row fails the engine-enforced NOT NULL at copy time — the documented fail-closed arm), drop, rename, recreate `idx_run_execution_contexts_workspace`. The procedure's `PRAGMA foreign_keys=OFF` step is not taken: that pragma is a no-op inside the runner's `BEGIN IMMEDIATE` migration transaction, and its purpose — protecting schema objects that reference the rebuilt table — is vacuous here (no inbound foreign key, trigger, or view references `run_execution_contexts`), which the migration test pins by asserting a clean `PRAGMA foreign_key_check` post-rebuild.
- [RFC 8785 — JSON Canonicalization Scheme (JCS)](https://datatracker.ietf.org/doc/html/rfc8785) — the canonicalization standard behind T1.4's set-vs-sequence split. The canonical bytes carry an enum member's literal string, so reordering Plan-009's `execution_mode` vocabulary is a wire non-event and that vocabulary compares as an unordered set, while the three Plan-010-owned vocabularies compare as ordered sequences because a reorder there is a Plan-010 authoring change the tripwire must catch. Same standard `Spec-006 §Canonical Serialization Rules` mandates for the envelope.
