# Spec-009: Repo Attachment And Workspace Binding

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `009` |
| **Slug** | `repo-attachment-and-workspace-binding` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Depends On** | [Repo Workspace Worktree Model](../domain/repo-workspace-worktree-model.md), [Session Model](../domain/session-model.md), [Component Architecture Local Daemon](../architecture/component-architecture-local-daemon.md) |
| **Implementation Plan** | [Plan-009: Repo Attachment And Workspace Binding](../plans/009-repo-attachment-and-workspace-binding.md) |

## Purpose

Define how repositories are attached to sessions and how workspaces are bound to execution.

## Scope

This spec covers repo mount creation, canonical root resolution, workspace binding, and non-git directory fallback.

## Non-Goals

- Worktree lifecycle details
- PR creation policy
- Artifact rendering

## Domain Dependencies

- [Repo Workspace Worktree Model](../domain/repo-workspace-worktree-model.md)
- [Session Model](../domain/session-model.md)

## Architectural Dependencies

- [Component Architecture Local Daemon](../architecture/component-architecture-local-daemon.md)
- [Data Architecture](../architecture/data-architecture.md)
- [ADR-006: Worktree First Execution Mode](../decisions/006-worktree-first-execution-mode.md)

## Required Behavior

- Attaching a repository to a session must be an explicit action that creates a durable `RepoMount`.
- Repo attach must resolve and persist the canonical repository root — the MAIN checkout's working-tree root, identified by the repository's git common directory rather than by the top level of whatever working tree the supplied path sits in — not only the user-entered path (§Repo Identity And Common-Directory Keying (V1 Definition)).
- The system must support multiple repo mounts in one session.
- Workspace binding must be explicit and must resolve to one concrete execution root before a run begins.
- Git-backed workspace binding must support the canonical execution-mode taxonomy `read-only`, `branch`, `worktree`, and `ephemeral clone`.
- The system must reject path traversal or workspace binding outside the declared local trust envelope.
- Non-git directory workspaces must be supported as a fallback, but with reduced git-aware capabilities.

## Default Behavior

- Attaching a git repository defaults to creating one repo mount and one default workspace view rooted at the enclosing working tree of the supplied path — the main checkout only when attach originates there (§Repo Identity And Common-Directory Keying (V1 Definition) surfaces both values for exactly this split).
- Newly attached workspaces default to `read-only` context until a run explicitly selects a writable execution mode.
- Repo metadata defaults to background refresh through daemon-owned git services.

## Fallback Behavior

- If a path is not a git repository, the system may bind it as a plain directory workspace with git-specific features disabled.
- If a workspace cannot support one or more git-backed execution modes, the daemon must expose that capability gap explicitly rather than silently substituting a different mode.
- If canonical root resolution fails, repo attach must fail explicitly rather than guessing; a supplied path that does not exist, and a path whose git metadata is present but unusable, are both resolution failures and must never be attributed to an ancestor repository (§Repo Identity And Common-Directory Keying (V1 Definition)).
- If a workspace path becomes unavailable after binding, the workspace transitions to `stale` and new write runs must be blocked until repair.

## Interfaces And Contracts

- `RepoAttach` must accept a local path, session id, and owning runtime node.
- `RepoMountRead` must expose canonical root, VCS metadata, and current health.
- `WorkspaceBind` must accept repo mount or directory root plus intended execution mode from the canonical mode set where applicable. Directory-root binding is expressed by first attaching the directory as a plain-directory repo mount (`vcs_type = 'none'`); `WorkspaceBind` always references a repo mount.
- `WorkspaceExecutionModeCapabilitiesRead` must expose which execution modes are currently valid for the bound repo mount or workspace.
- `WorkspaceList` must expose workspace health and current binding state.
- See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed request/response schemas.
- See [Error Contracts](../architecture/contracts/error-contracts.md) for error response schemas and error codes.

## State And Data Implications

- Repo mount records must persist canonical root, owner node, lifecycle state, and — for git-backed mounts — the attach-time repository identity anchor (§Repo Identity And Common-Directory Keying (V1 Definition)'s persisted common directory, carried in existing mount metadata with no schema change).
- Workspace records must persist execution root, repo association, health, and the admitted bind origin (the bound-root carrier reprovisioning never clears — the value `branch` mode roots at, carried in existing workspace metadata with no schema change).
- Repo health and git metadata belong to daemon-owned projection state rather than client cache.

## Example Flows

- `Example: A participant attaches a repository to a session. The daemon resolves the canonical repo root, stores a repo mount, and exposes a default workspace for inspection.`
- `Example: A participant later selects worktree mode for a coding run. The workspace remains the same session-bound concept, but the daemon provisions an isolated execution root before the run starts.`
- `Example: A plain directory is attached for planning work. It becomes a valid workspace, but git-specific commands remain unavailable.`

## Execution Mode Transitions

When a workspace switches execution mode (e.g., `read-only` to `worktree`), the workspace entity is re-provisioned in place. The workspace ID remains the same; only the execution root changes.

State progression: `ready -> provisioning -> ready`.

- The old execution root is released and a new one is provisioned for the target mode.
- This is NOT a new workspace. The `workspaces` row keeps its `id`; `execution_mode` and `fs_root` are updated, and `state` cycles through `provisioning` before returning to `ready`.
- If the switch fails (e.g., the daemon cannot provision a worktree for the target mode), the workspace transitions to `stale` with an error detail recorded in the workspace's metadata. New write runs must be blocked until the workspace is repaired or the mode switch is retried.

### Ephemeral Clone Lifecycle

Ephemeral clones follow a linear lifecycle managed by the `ephemeral_clones` table (Plan-010).

States: `creating -> ready -> retired -> (deleted from disk)`.

- `creating`: Clone is being set up on disk. The workspace that owns it remains in `provisioning` until the clone reaches `ready`.
- `ready`: Clone is available for execution. The owning workspace transitions to `ready`.
- `retired`: Clone is marked for removal. No new runs may use it.
- `failed`: Handles creation failures. The owning workspace transitions to `stale`.

Cleanup triggers (any one is sufficient):

- Run completion (default, per `cleanup_policy = 'on_run_complete'`).
- Session archive (and workspace archival generally — e.g. the repo-detach cascade in §Detach Semantics; enforcement is the Plan-010 cleanup sweep).
- Explicit disposal by participant or daemon.
- TTL expiry (configurable as daemon configuration — not a wire parameter; default 24 hours).

Cleanup is asynchronous. The `ephemeral_clones` table marks the clone `retired` immediately. A background job removes the filesystem clone after the state change. This decouples the user-facing state transition from potentially slow disk I/O. Retiring the clone backing a live (non-archived) `ephemeral clone`-mode workspace's current root returns the owning workspace to `provisioning` via the workspace transition primitives — root unbound, awaiting the next per-run prepare; `stale` is reserved for fault paths such as preparation failures (`Spec-010 §Fallback Behavior`).

## Implementation Notes

- Repo attach should not assume that the user-selected path is already the repo root, nor that the working tree enclosing it is the repository's identity: a linked worktree's own top level is not its repository's main checkout root.
- Workspace binding must remain explicit even when a session has only one repo mount.
- Repo mount ownership belongs to the runtime node that can actually access the filesystem path.

## Pitfalls To Avoid

- Treating a user-entered path as canonical without resolution, or treating the enclosing working tree's top level as the repository's identity
- Auto-writing to a workspace before execution mode is explicitly chosen
- Allowing client-only workspace binding with no daemon validation

## Acceptance Criteria

- [ ] Attaching a repository yields a durable repo mount with canonical root metadata.
- [ ] A session can contain multiple repo mounts and multiple bound workspaces.
- [ ] Non-git directory workspaces remain usable without pretending to support git-only features.
- [ ] Attaching from a linked worktree and attaching from the same repository's main checkout resolve to one identical canonical root for every layout where git records the main working tree (the ordinary and submodule layouts — the `--separate-git-dir` layout's linked worktrees are the deliberate refusal arm of §Repo Identity And Common-Directory Keying (V1 Definition), with attach-from-the-main-checkout as the recovery and the worktree bindable through the registration arm), so N working trees of one repository yield ONE repo mount while each bound workspace keeps its own per-worktree execution root.
- [ ] A supplied path that does not exist, a path whose git metadata — at the path itself or at any directory strictly between it and the resolved working-tree top level — is present but unusable, a working tree whose registration is missing or does not name it back, a derived main-checkout root that fails its bidirectional verification with no lawful own-toplevel fallback (the linked worktree of a `--separate-git-dir` repository lands here), and a git pointer file that is not a regular file each fail repo attach explicitly rather than being attributed to an ancestor repository.
- [ ] A git-backed mount whose root's `.git` entry is moved or retargeted after attach refuses subsequent binds and run handoffs through the persisted-identity match and projects `identity_mismatch` — never `healthy` — on every subsequent health-reporting read until re-attach, while a mount whose canonical root cannot be probed at all projects `unreachable`, which takes precedence (§Repo Mount Health (V1 Definition)).

## ADR Triggers

- If repo mounts and workspaces cease to be separate concepts, create or update `../decisions/006-worktree-first-execution-mode.md` or a replacement architecture decision.

## Resolved Questions and V1 Scope Decisions

- No blocking open questions remain for v1.
- V1 decision: the first implementation binds existing local checkouts only. Clone-from-URL attach flows are out of scope for v1.

## Local Trust Envelope (V1 Definition)

For repo attachment and workspace binding, the declared local trust envelope of a session is the set of ADMITTED ROOTS of its attached repo mounts: each mount's fully resolved canonical root (plain-directory mounts included), the working trees the mount's repository itself registers, and daemon-provisioned execution roots admitted by provenance — the same union the defining rule below states in full (2026-08-17 adjudication round: the previous sentence named the canonical roots alone, leaving the arms below formally outside the governing definition they extend). Envelope admission for user-supplied roots originates in the explicit `RepoAttach` action; no path enters the envelope implicitly.

- A workspace execution root is inside the envelope iff its fully resolved form (absolute, symlink-resolved, platform-normalized) is path-contained within an ADMITTED ROOT of a repo mount attached to the same session — the mount's fully resolved canonical root, a working tree the repository itself registers (admitted under the registration arm below), or a daemon-provisioned execution root (admitted by provenance, below); the defining rule and the arms below are ONE contract, stated here so no admission exists that the definition does not name (2026-08-17 adjudication — the prior sentence named the canonical root alone, leaving the two later arms formally outside the definition they extend). Containment is path-component-boundary-aware (`/repo-evil` is not within `/repo`), case-folded on case-insensitive filesystems (Windows tier per [ADR-019](../decisions/019-windows-v1-tier-and-pty-sidecar.md)), and — for git-backed mounts — working-tree-boundary-aware: a candidate whose own enclosing working tree is NOT the admitted root it is tested against (a linked worktree NESTED beneath the canonical root is the concrete shape) is never admitted by lexical containment in that root; it is admitted only as, or within, a registered working tree through the registration arm — so de-registering a nested worktree removes it from the envelope exactly as it does a sibling one, and lexical containment can never resurrect it.
- `WorkspaceBind`'s optional `directory` carries two forms over the one existing string field — no schema change. A RELATIVE value is resolved against the mount's canonical root, exactly as shipped. An ABSOLUTE value is the wire form that names a registered working tree (or any other admitted root) — the only expressible form where no relative spelling exists at all, as for a worktree on another drive under the Windows tier ([ADR-019](../decisions/019-windows-v1-tier-and-pty-sidecar.md)). Both forms are admitted solely by post-symlink-resolution containment within an admitted root of the same mount — the canonical root, or a registered working tree admitted under the bullet below; `..` traversal, redirection, or symlink escape that resolves outside every admitted root is rejected with the typed `repo.outside_trust_envelope` error.
- The node-level trust envelope governed by approval policy (`Spec-012 §Default Behavior`) is a separate run-time authorization layer; it is not this spec's bind-time validation and is out of scope here.
- Daemon-provisioned execution roots (Plan-010 worktrees and ephemeral clones under the daemon's execution-roots directory, keyed by the owning repo mount) are inside the envelope by construction: they are daemon-created derivatives of an admitted mount, never user-supplied paths, so the containment rule above governs user-supplied bind paths while provisioned roots are admitted by provenance (`Spec-010 §State And Data Implications`).
- A git-backed mount's envelope is its canonical root PLUS the working-tree paths the repository itself registers. Because the canonical root is the main checkout's root (§Repo Identity And Common-Directory Keying (V1 Definition)), a linked worktree of that repository is generally NOT path-contained within it, so containment alone would reject binds to working trees the repository demonstrably owns. Each registered path is admitted only when it passes the same bidirectional-link and physical-containment screens resolution applies; registration is read from the repository at bind time and never cached, so a working tree git no longer registers is no longer in the envelope. Containment within an admitted registered working tree is then checked exactly as it is within the canonical root (the containment rule above), and a user-supplied path that is neither contained in the canonical root nor in an admitted registered working tree is rejected with `repo.outside_trust_envelope` as before. On the wire, a registered working tree is named by the ABSOLUTE form of `WorkspaceBind.directory` (the containment bullet above); no selector field is added. Leaving the envelope is additionally enforced at the next RUN boundary, not only at bind: run handoff re-verifies a user-supplied execution root's membership — canonical-root containment or currently-registered working tree — before work starts, and a root that no longer verifies blocks the run and transitions the workspace to `stale` with the typed error recorded (§Fallback Behavior's blocked-until-repair shape), so a worktree unregistered after bind cannot persist as a silently trusted root. This registration arm governs USER-SUPPLIED bind paths only; it does not narrow the provenance rule above. A daemon-provisioned execution root stays admitted by provenance regardless of its registration state, because the daemon knows it created that root — so a provisioned worktree whose registration git has pruned (the window between an external `git worktree prune` and the Plan-010 retirement sweep) remains in the envelope, is exempt from the run-boundary re-verification, and is retired by that sweep rather than being silently ejected from the envelope mid-run.

## Repo Mount Health (V1 Definition)

Repo mount health is the daemon-probed integrity of the mount's canonical root, exposed as a daemon-owned projection (`RepoMountHealth { status: "healthy" | "unreachable" | "identity_mismatch"; checkedAt: string }` — see [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-009). `status` is `unreachable` when the canonical root is absent or unreadable at probe time — the verdict that takes precedence, because no further question can be asked of a root that cannot be probed; `identity_mismatch` when the root is reachable but the common directory re-derived from it no longer equals the attach-persisted identity anchor (`repo_mounts.metadata.commonDir` — §Repo Identity And Common-Directory Keying (V1 Definition)) — or cannot be re-derived at all, a removed or malformed `.git` entry under a reachable root being the same moved identity, an explicit verdict rather than an error escaping a health read — the health face of a post-attach `.git` move, removal, or retarget, standing on every subsequent health-reporting read until re-attach (the named recovery) — required because the mount's binds and runs are already refusing through the persisted-identity match, and a projection still answering `healthy` for a mount that can never bind again is a lying read model; and `healthy` when the root is reachable and the re-derived identity matches the anchor; `checkedAt` is the ISO-8601 instant of the probe that produced the verdict. Health is derived at read time (every health-reporting read surface probes synchronously — the on-read floor; the identity comparison is part of the probe wherever an anchor is persisted) and refreshed by the daemon-owned background metadata refresh (§Default Behavior); it is never persisted as a row column and is distinct from the lifecycle `state` axis (§State And Data Implications). A mount that persists no identity anchor — a plain-directory mount, which has no repository identity at all — never projects `identity_mismatch`; the member requires an anchor to mismatch.

## Detach Semantics (V1 Definition)

`RepoDetach` must accept a repo mount id and transition the mount to `detached` without deleting the durable record (request/response shapes in [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) §Plan-009).

- Detach is refused with `repo.detach_conflict` while any dependent workspace is `busy`; active work must finish or be cancelled first. There is no force-detach in V1.
- Otherwise, detach transitions the mount to `detached` and archives all dependent workspaces (`workspaces.state -> 'archived'`), emitting `repo.detached` plus one `workspace.archived` per dependent workspace (Spec-006 `session_lifecycle` taxonomy). Archived workspaces remain historically linked to completed runs. Dependent non-terminal worktree and ephemeral-clone rows follow via the Plan-010 asynchronous retirement sweep (`Spec-010 §Fallback Behavior` — retirement recorded and evented, metadata preserved).
- `detached` is terminal for the row: there is no `detached -> attached` transition. Re-attaching the same canonical root creates a NEW repo mount row (the active-mount uniqueness index constrains only `state = 'attached'` rows). Because the canonical root is the repository's main-checkout root (§Repo Identity And Common-Directory Keying (V1 Definition)), re-attaching from ANY working tree of a detached repository is a re-attach of that same canonical root and yields one new row for the repository, never one row per working tree.
- The desktop renderer exposes no detach surface in V1; detach is SDK/CLI-surfaced only (renderer behavior is unspecified by this spec).

## Repo Identity And Common-Directory Keying (V1 Definition)

A repository's identity is its git COMMON directory, not the top level of whatever working tree the supplied path happens to sit in. Git gives one repository many working trees — one main checkout plus N linked worktrees — and they share exactly one common directory (normally the main checkout's own `.git` directory; a repository initialized with `--separate-git-dir` keeps it elsewhere, behind a `.git` pointer file — [git-init(1)](https://git-scm.com/docs/git-init)), which is why it is the only stable identity for that repository. The canonical root persisted on a repo mount is therefore the working-tree root of the MAIN checkout: the directory whose own `.git` entry resolves to that common directory. Attaching from a linked worktree resolves to that same main-checkout root, so N working trees of one repository resolve as ONE repo mount, and every daemon cache, filesystem watch, remote fetch, and throttle window keyed on repo identity is shared across them instead of duplicated per working tree.

- Identity keying does not move where work happens. A workspace's execution root (`workspaces.fs_root`) remains the per-working-tree path the participant bound or the daemon provisioned; only the repo-mount identity key (`repo_mounts.canonical_root`) collapses onto the main-checkout root. Resolution must therefore surface BOTH values — the repository identity and the enclosing working tree's own root — because attach persists the first and the default workspace of §Default Behavior roots at the second.
- The main-checkout root is DERIVED from git's own records, then VERIFIED — never assumed — and the derivation runs BEFORE any own-toplevel fallback, because the fallback is spoofable where the derivation is not: a planted `.git` pointer file naming a repository's common directory directly makes git report gitDir == commonDir with the planted holder as the top level, so an accept-own-toplevel-first rule would persist the planted directory as that repository's root. Candidate derivation, in order: when the common directory IS its parent's own `.git` entry — decided by SAME-FILE IDENTITY against the filesystem (the parent's `.git` child resolves to the same directory entry as the common directory), never by comparing name strings, because a `.GIT` spelling names the same entry wherever the filesystem folds case (default macOS, and the Windows tier per [ADR-019](../decisions/019-windows-v1-tier-and-pty-sidecar.md)) and no single string rule is right on both a case-folding and a case-sensitive volume, while the identity test is true exactly where the filesystem folds, with no per-platform table — the candidate is its parent (the ordinary layout); otherwise, when the common directory's own `config` records `core.worktree` (read file-scoped from `<commonDir>/config`, never via discovery), the candidate is that value resolved against the common directory — the submodule layout, whose module directory `<superproject>/.git/modules/<name>` has a non-`.git` basename yet genuinely records its working tree ([git-config(1)](https://git-scm.com/docs/git-config), [gitsubmodules(7)](https://git-scm.com/docs/gitsubmodules)). A candidate is accepted only when its own `.git` entry resolves back to that same common directory — a directory canonically equal to it, or a pointer file naming it. Only when NO candidate is derivable, or the derived candidate fails its back-check, does resolution consult the supplied tree: the supplied path's own git directory canonically equal to the common directory means the enclosing working tree is the repository's main working tree and the canonical root is that tree's own top level — the first-attach-defines-root arm a `--separate-git-dir` MAIN checkout takes, because git records no main-working-tree pointer for that layout (no `core.worktree` is written, and its own `git worktree list` reports the git directory itself as the main working tree — [git-worktree(1)](https://git-scm.com/docs/git-worktree)); anything else is refused with the typed root-resolution error, naming attach-from-the-main-checkout as the recovery — the linked worktree of a `--separate-git-dir` repository lands on exactly that refusal arm by design: identity for that repository is established by attaching its main checkout, after which the worktree binds through the registration arm of §Local Trust Envelope (V1 Definition) — which is why §Acceptance Criteria's one-identical-canonical-root criterion quantifies over the layouts where git records the main working tree, not over every working tree of every layout. Residual, named rather than hidden: for the `--separate-git-dir` layout git records no working-tree root at all, so a direct-to-common pointer-file holder is indistinguishable from the true main checkout at first attach; the working-tree membership screen (a supplied tree must canonically equal the derived canonical root or be a registered working tree) closes the spoof for every layout git does record, and the persisted-identity match below bounds the residual after attach.
- Because the active-mount uniqueness index is keyed on the canonical root (§State And Data Implications), attaching a second working tree of an already-attached repository is a re-attach of the SAME repository and is refused as already attached rather than admitted as a second mount. Uniqueness is additionally enforced on the identity itself: inside the attach transaction, the resolution's canonicalized common directory is compared against every active mount of the same session and node — each mount's persisted identity anchor, an absent anchor re-derived from that mount's canonical root and repaired in the same transaction (the anchor-repair rule below) — and equality refuses as already attached — the canonical-root index cannot see two mounts with DIFFERENT canonical roots and ONE common directory, exactly the pair the first-attach residual above would otherwise mint (a direct-to-common pointer-file holder attached beside the true main checkout), so the identity comparison bounds that residual to one active mount per repository. The check is service-level by design: the anchor lives in the metadata JSON column, where a unique index would demand a generated-column migration for a value the daemon's single-writer attach transaction already compares race-free, and the canonical-root index stays as the structural backstop for the ordinary same-root shape. Binding that working tree is a workspace operation on the existing mount, not a second attach.
- The repository identity that resolution derives is PERSISTED at attach — `repo_mounts.metadata.commonDir`, the canonicalized common directory inside the existing metadata JSON column, no schema change — and every later re-derivation must MATCH it: workspace bind and the run-boundary re-verification re-derive the common directory from the mount's canonical root, and a value that no longer equals the attach-persisted one refuses with the typed root-resolution error and projects `identity_mismatch` per §Repo Mount Health (V1 Definition) on every subsequent health-reporting read rather than silently following the moved or retargeted `.git` entry, with re-attach as the named recovery. Without the persisted anchor, a post-attach retarget of the root's `.git` pointer file would walk every subsequent bind and run onto a different repository while the mount row still claims the old identity. An ABSENT anchor on a git-backed mount — a row attached before this contract — is REPAIRED at first contact rather than tolerated indefinitely: the first successful common-directory re-derivation after upgrade at a MUTATING seam — bind, the run-boundary re-verification, or the attach transaction's dedup comparison — persists the derived value as the mount's anchor; a health-reporting read NEVER writes it, because the wire read surfaces register `mutating: false` and an advertised pure read must stay one, so an unanchored row's health read simply has no identity question to ask until the first mutating contact repairs the anchor (a bounded, named window). First observation defines identity, so a retarget that PREdates the repair is undetectable by construction — nothing was recorded to contradict it — and stands as the named residual of the upgrade window; a re-derivation that fails while the anchor is still absent leaves the row unanchored and refuses the operation that needed it rather than guessing. Plain-directory mounts have no repository identity, persist no anchor, and take neither the repair nor the match.
- Resolution must refuse a supplied path that does not exist instead of walking up from it. Walking up lexically from a deleted or never-created path attributes it to whatever repository happens to contain its parent — a dotfiles repository checked out at the user's home directory is enough to make that attribution wrong for essentially every path beneath it.
- Resolution must distinguish "no git metadata here" from "git metadata here is unusable", and must NOT lean on git to make that distinction: empirically (git 2.50.1), and confirmed at source in git's own `setup.c` (§References), git's own upward discovery hard-stops only on the unusable POINTER-FILE shape — a dangling or malformed `gitdir:` file fails discovery outright, because `read_gitfile_gently` classifies it as not-a-repository and the ascent loop in `setup_git_directory_gently_1` terminates on every gitfile error except the stat failure that means no `.git` entry exists at that level — while an unusable `.git` DIRECTORY at an ancestor draws no gitfile verdict at all and is silently skipped, the ancestor's own parent repository discovered instead, which is precisely how a damaged repository gets silently attributed to its parent repository. The daemon therefore enforces the stop itself: after git answers, the supplied path's OWN `.git` entry and every directory strictly BETWEEN it and the resolved working-tree top level are screened, and an entry that classifies as present but unreadable, malformed, or naming something that is not a usable git directory fails the attach explicitly — the at-the-path arm matters because git skips an unusable `.git` directory at the supplied path exactly as it skips one at an ancestor, and without it a supplied path sitting directly under its repository's top level (an empty strictly-between set) would sail past the screen wearing the exact damaged-metadata shape the acceptance criterion refuses. The boundary is the same on both resolution arms: when git finds no repository at all, only the supplied path's own entry is screened — a path whose own git metadata is unusable is refused, not classified as inside no repository, so the refusal never conflates with the no-repository fallback — and neither arm ever walks above git's answer. The screen is bounded and post-hoc — finitely many components below a root git already resolved, never a walk above it — so it can refuse a wrong answer but never manufacture a root, which keeps it compatible with the no-upward-walk rule above.
- A linked worktree is accepted as belonging to a repository only when git's link is BIDIRECTIONAL: the working tree's `.git` file names a git directory, AND that git directory's registration under `<common-dir>/worktrees/<name>/gitdir` names this working tree's own `.git` back. The registration must additionally be physically contained by the common directory it claims — the registered git directory's grandparent must canonically equal that common directory. A pointer file alone is attacker-writable and must never establish repository identity by itself.
- Every pointer file resolution reads — the working tree's `.git`, and the `commondir` / `gitdir` files under a git directory — must be opened WITHOUT blocking, validated as a REGULAR file by `fstat` on the OPENED descriptor, and read through that same descriptor under a fixed byte ceiling. A pre-open `stat` screen alone leaves a TOCTOU window: the checked path can be swapped for a FIFO or device node between check and open, and the open then blocks indefinitely and wedges the scan — the exact hazard the screen exists to prevent. Anchoring the regular-file check to the opened descriptor screens the object actually read, and reading through that descriptor bounds the bytes regardless of what pre-open metadata claimed.
- Both sides of every path comparison — bind containment, worktree-registration matching, and common-directory containment — must be canonicalized (symlink-resolved and absolutized) before they are compared. Comparing a canonicalized path against an uncanonicalized one produces false mismatches wherever the platform symlinks standard directories: on macOS `/tmp` is `/private/tmp`, so an uncanonicalized `/tmp/...` registration never equals its canonicalized peer and a legitimate worktree is refused.
- Each refusal above is a canonical-root resolution failure under §Fallback Behavior: attach fails explicitly with the typed root-resolution error rather than guessing a root. A path that is genuinely inside no repository remains the plain-directory fallback of §Fallback Behavior — a different outcome from a refusal, and the two must never be conflated, because classifying a refusal as a plain directory is exactly the silent misattribution this section exists to prevent.

## Notes

- **2026-08-16 — Repo-identity amendment (common-directory keying + resolution hardening).** This spec's canonical-root contract previously said only "resolve and persist the canonical repository root" and left the working-tree-vs-repository distinction unstated, so the shipped resolver read it as the enclosing working tree's own top level and gave every linked worktree of one repository its own repo identity. This amendment states the contract the daemon actually needs: identity keys on the git common directory (new §Repo Identity And Common-Directory Keying (V1 Definition)), execution roots stay per-working-tree, and resolution refuses nonexistent paths, unusable git metadata, unregistered or one-way worktree links, and non-regular pointer files rather than misattributing them to an ancestor repository. §Required Behavior, §Fallback Behavior, §Implementation Notes, and §Pitfalls To Avoid are amended in place at zero net line change so no inbound line cite moves; §Local Trust Envelope (V1 Definition) gains the git-registered-working-tree admission rule the identity change would otherwise have made unreachable — scoped to user-supplied bind paths so it never narrows the pre-existing provenance admission of daemon-provisioned execution roots, which would otherwise have ejected a provisioned worktree from the envelope mid-run in the window between an external `git worktree prune` and the Plan-010 retirement sweep — §Detach Semantics (V1 Definition) records that re-attach is per-repository rather than per-working-tree, and §Acceptance Criteria gains two criteria after the existing three. Per the audit runbook's spec-amendment rule this spec was flipped `approved -> review` for the amendment's review window and restored `approved` in the SAME diff by the targeted readiness-audit delta that accompanies it (the PR #278 same-PR flip-and-restore precedent); Plan-009's Preconditions audit checkbox re-opened scoped to this growth and re-checked with a Delivered record in the same diff. Ratified by the user 2026-08-16. The PR's Codex review round then hardened four surfaces in place: the bind wire rule for naming registered working trees (the absolute-path form of the existing `directory` string — no schema change), the derive-then-verify main-checkout rule with its `--separate-git-dir` refusal arm (git records no main-working-tree pointer for that layout, so the parent-of-common-directory derivation is verified bidirectionally or refused — §Acceptance Criteria's refusal enumeration carries that fifth shape), descriptor-anchored (`fstat`-validated, non-blocking-open) pointer-file screening in place of the racy stat-then-open form, and run-boundary re-verification of user-supplied execution roots with the `stale` transition on membership loss; §References gained the upstream git primary sources for the identity contract.
- **2026-08-17 — PR #340 Codex round 2 (derivation-first arm order, ancestor unusable screen, persisted identity, branch-mode root).** Second review round, same review window. Four contract-level repairs, each empirically grounded on git 2.50.1 during triage: §Repo Identity And Common-Directory Keying (V1 Definition) inverts the resolution arms — the main-checkout candidate is derived and verified from git's OWN records (parent of the common directory, or the submodule module directory's `core.worktree` back-pointer) BEFORE any own-toplevel fallback, because the fallback-first order accepted a planted direct-to-common `.git` pointer file as a repository root — with the own-toplevel arm surviving only for the `--separate-git-dir` main checkout, where git records no working-tree root, and that arm's residual named in place; the git-stops-on-unusable rationale is corrected to what git actually does (hard-stop on the pointer-file shape only; silent skip of an unusable `.git` directory at the supplied path or an ancestor) and the daemon now screens the supplied path's own `.git` entry and the strictly-between directories itself, bounded and post-hoc, on both resolution arms; the derived identity is persisted at attach (`repo_mounts.metadata.commonDir` — no schema change) and matched on every later re-derivation, refusing on retarget; and §Default Behavior's default-workspace sentence is reconciled with §Repo Identity's both-values rule (rooted at the enclosing working tree, not the main checkout — the sentence the working-tree-vs-repository split had left behind). Ripple recorded where it lands: `Spec-010 §Turn-Boundary Snapshots` named "the shared main checkout" as branch mode's execution root — true only under the pre-amendment per-directory identity — and is corrected in the same swap (Spec-010 and Plan-010 flip-and-restored `approved`, the PR #321/#323 neighbor-swap shape), with the bound-root carrier (`workspaces.metadata.boundRoot`) and the Plan-010-consented `execution-root-service.ts` branch-arm re-rooting registered as Plan-009 CP-009-9 / Plan-010 CP-010-14 and held atomically with Phase 2B by the plan's carrier box. §Acceptance Criteria's unusable-metadata shape gains the at-the-path-or-strictly-between clause in place.
- **2026-08-17 — PR #340 carried-findings adjudication (rounds 3+4).** PR #340's final two Codex review rounds raised eleven findings after the amendment text froze; they were carried forward on Plan-009's Phase 2B carrier box and are adjudicated into normative contract here, in the same one-diff flip-and-restore discipline as the entries above (Spec-010 and Plan-010 neighbor-swapped again for the shared legs). Spec-side outcomes: §Local Trust Envelope (V1 Definition)'s defining containment rule now names ALL admitted roots — canonical root, registered working trees, and provenance-admitted provisioned roots — so the arms extend a definition that includes them, and containment is working-tree-boundary-aware: a linked worktree NESTED beneath the canonical root admits only through the registration arm, never by lexical containment, so de-registration ejects it regardless of where it sits. §Repo Mount Health (V1 Definition) grows the third `identity_mismatch` status member — derived on read from the persisted identity anchor, `unreachable` taking precedence, re-attach the named recovery — replacing the prior "marks the mount" promise a two-member reachability-only projection could not honor. §Repo Identity And Common-Directory Keying (V1 Definition) adds service-level identity-keyed active-mount dedup inside the attach transaction (bounding the `--separate-git-dir` first-attach residual to one active mount per repository), decides the ordinary-layout `.git` derivation by same-file identity against the filesystem (case-insensitive spellings included), and grounds the discovery-asymmetry claim in git's own `setup.c` (§References). §Acceptance Criteria's one-canonical-root criterion is narrowed in place to the layouts where git records the main working tree, and a sixth criterion pins the retarget-projects-`identity_mismatch` behavior. Plan-side outcomes land in Plan-009 (branch-arm `boundRoot` revalidation, the CP-009-10/CP-010-15 turn-snapshot execution-root normalization consent pair, invariant I-009-17 with task T-009-2B-5, and the `WorkspaceBindResponse.fsRoot` comment corrected to the exact admitted resolved bind directory) and Plan-010 (I-010-6/D-010-9 rewritten around the BOUND checkout). The findings were review inputs; the amended sections are the adjudicated outputs — per-finding dispositions live in Plan-009's Phase 2B Progress Log. The adjudication PR's own Codex rounds then hardened this growth in place across two review rounds: the ordinary-layout derivation decides by same-file identity against the filesystem rather than any string case-fold (the daemon's shipped comparison primitives fold only under the win32 path module); pre-contract rows with no persisted anchor are repaired at first successful re-derivation (first observation defines identity, the pre-repair retarget the named residual) instead of degrading past the match indefinitely; a reachable root whose identity cannot be re-derived at all projects `identity_mismatch` explicitly; run tenancy is re-keyed on the normalized checkout root in `Spec-010 §State And Data Implications` (the snapshot normalization's concurrency consequence); and the `fsRoot` response comment names the exact admitted resolved bind directory, with the `git-ls-files`/`git-read-tree` primary sources added to Plan-009 §References. The second round then scoped the anchor repair to MUTATING seams only (the wire read surfaces register `mutating: false` and stay pure), persisted the normalized checkout root at bind (`workspaces.metadata.checkoutRoot`, rewritten at provisioning completion) so restore and run tenancy consume a durable value rather than re-deriving from a nested bound path a turn may have deleted, narrowed the health-projection surface to mount-read (the workspace list carries no health member — a mismatching mount surfaces there as the existing `stale` + `lastError` transition at the next run handoff), and grounded the same-file-identity test in the POSIX / Windows / Node primary sources (Plan-009 §References).

## References

- [Repo Workspace Worktree Model](../domain/repo-workspace-worktree-model.md)
- [Component Architecture Local Daemon](../architecture/component-architecture-local-daemon.md)
- [git-worktree(1)](https://git-scm.com/docs/git-worktree) (accessed 2026-08-16) — one repository, many working trees; the `worktrees/<id>/` registration model and prune semantics behind §Repo Identity And Common-Directory Keying (V1 Definition) and the §Local Trust Envelope (V1 Definition) registration arm
- [gitrepository-layout(5)](https://git-scm.com/docs/gitrepository-layout) (accessed 2026-08-16) — the `commondir` and `gitdir` layout files the bidirectional worktree-link verification reads
- [git-rev-parse(1)](https://git-scm.com/docs/git-rev-parse) (accessed 2026-08-16) — `--git-common-dir`, `--show-toplevel`, `--absolute-git-dir`, and `--path-format=absolute` (the discovery queries; `--path-format` per the git 2.31 release notes)
- [git-init(1)](https://git-scm.com/docs/git-init) (accessed 2026-08-16) — `--separate-git-dir` moves the repository out of the working tree behind a filesystem-agnostic `.git` pointer file, the layout behind the derive-then-verify refusal arm
- [git-config(1)](https://git-scm.com/docs/git-config) (accessed 2026-08-17) — `core.worktree`, the working-tree back-pointer the submodule candidate derivation of §Repo Identity And Common-Directory Keying (V1 Definition) reads file-scoped from the common directory's own `config`
- [gitsubmodules(7)](https://git-scm.com/docs/gitsubmodules) (accessed 2026-08-17) — the `$GIT_DIR/modules/<name>/` submodule git-directory layout whose non-`.git` basename the candidate derivation's second arm exists for
- [git v2.50.1 `setup.c`](https://github.com/git/git/blob/v2.50.1/setup.c) (accessed 2026-08-17) — `read_gitfile_gently` and `setup_git_directory_gently_1`, the upstream discovery source for the asymmetry §Repo Identity And Common-Directory Keying (V1 Definition)'s ancestor screen compensates for: every gitfile read error except the stat failure meaning "no `.git` entry here" terminates the ascent, while an unusable `.git` directory draws no gitfile verdict and the ascent silently continues. Tag-pinned so the cited functions stay at the cited source
