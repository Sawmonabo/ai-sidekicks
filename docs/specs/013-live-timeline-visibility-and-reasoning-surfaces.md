# Spec-013: Live Timeline Visibility And Reasoning Surfaces

| Field | Value |
| --- | --- |
| **Status** | `approved` |
| **NNN** | `013` |
| **Slug** | `live-timeline-visibility-and-reasoning-surfaces` |
| **Date** | `2026-04-14` |
| **Author(s)** | `Codex` |
| **Depends On** | [Artifact Diff And Approval Model](../domain/artifact-diff-and-approval-model.md), [Observability Architecture](../architecture/observability-architecture.md), [Session Event Taxonomy And Audit Log](../specs/006-session-event-taxonomy-and-audit-log.md), [Queue Steer Pause Resume](../specs/004-queue-steer-pause-resume.md), [Provider Accounts And Credential Homes](../specs/029-provider-accounts-and-credential-homes.md), [Control Plane Relay And Session Join](../specs/008-control-plane-relay-and-session-join.md), [Local IPC And Daemon Control](../specs/007-local-ipc-and-daemon-control.md) |
| **Implementation Plan** | [Plan-013: Live Timeline Visibility And Reasoning Surfaces](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) |

> **Amendment (2026-07-20, campaign B9 CP-004-13 consumer registration — flips the previously-`approved` spec to `review` per the audit runbook's spec-amendment rule, since it changes Required Behavior, Acceptance Criteria, and Depends On; restored `approved` 2026-08-10 by the Tier-8 readiness audit (§6 node NS-20); Plan-013 flipped to `review` with it under the runbook's plan behavior-change row and was restored `approved` by that same audit, its Preconditions box re-checked).** [Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior) already mandates that rolled-back turns stay in the timeline **marked superseded by projection**, and [Spec-004 §Driver-Level Rollback Mechanics](004-queue-steer-pause-resume.md#driver-level-rollback-mechanics) that clients render the rewound history distinctly rather than dropping it. This amendment registers the timeline-surface half of that approved contract on its owning spec: the superseded-turn-rendering Required Behavior bullet (live boundary-entry rule + read/replay marker + attribution-ranked late stragglers), the `run.rolled_back` run-state subtype row, the compacted-stub composition, the provenance rule, and the acceptance criterion — consumed from Plan-004 T3.14's exported `supersededTurns(runId)` read seam (CP-004-13).

> **Amendment (2026-08-18, provider-account plane — the account-scoped quota-display leg. Flips the previously-`approved` spec to `review` per the audit runbook's spec-amendment rule, since it replaces normative display behavior in §Rate-Limit Display and adds Spec-029 to Depends On; restored `approved` in this same swap by the paired plan's targeted readiness-audit delta, which rides this diff together with the `- [x]` flip of Plan-013's born-unchecked rate-limit-carrier box — the whole amendment-class swap taken at once rather than split across diffs.)** Provider quota is an **account-plane** fact, not a session's: [Spec-029 §Provider quota is account-scoped](029-provider-accounts-and-credential-homes.md#provider-quota-is-account-scoped) keys it on `(accountId, credentialGeneration)` and carries it on the `usage.rate_limit_update` event ([Spec-006 §Usage Telemetry](006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)), which is where a client reads it. §Rate-Limit Display previously described a control-plane-header-sourced, session-framed figure built from absolute `remaining` / `limit` counts — a shape no carrier in this corpus emits and no client surface observes, and one that would present a single node-wide meter for a node that may hold several independently-billed accounts per provider. That section is replaced here with the event-sourced, account-scoped, node-local contract the registered carrier actually supports, including the honest statement of where it diverges from the session-wide cost receipt. The heading text is unchanged, so every `Spec-013 §Rate-Limit Display` cite still resolves.

> **Amendment (2026-08-26, per-limit quota and the registry Source — the [Spec-029](029-provider-accounts-and-credential-homes.md) sign-in and health-observation reciprocal (CP-029-9), landed with its restoring targeted readiness-audit delta riding the same diff, cross-plan §6 node NS-83).** Flips the previously-`approved` spec to `review` per the audit runbook's spec-amendment rule and **restores `approved` in the same diff**, the in-swap shape this spec's 2026-08-18 leg took. Three changes to §Rate-Limit Display, all consumer-side. **(1) A second sanctioned Source**: the daemon's per-account quota-window registry, carried on the provider-account read and its subscription with `source: 'probe'`, beside the existing event. It exists because the event is only available to a client that was connected when the traffic happened — a renderer opened after a restart would otherwise show nothing and be unable to tell that apart from a healthy account. **Precedence is by observation time, not by source**: newest `observedAt` per key wins and `source` breaks only exact ties, because preferring a source outright would let a stale reading mask real consumption. A stored reading also carries the generation it was observed under, and a behind-generation reading renders **stale rather than current** rather than being dropped, because a credential-home rebuild does not stop the provider-side window it describes. **(2) The display re-keys per limit**, matching the [Spec-006](006-session-event-taxonomy-and-audit-log.md) re-key this same swap lands: every consumer obligation — the drop-a-lower-reading rule included — now applies per `(providerAccountId, limitId)` and never across limits, and limit labeling becomes mandatory on the same grounds account labeling already was. **(3) The visibility rule's absence clause is widened** from a two-way reading to a four-way one: with an observer that can be silenced and homes that are not plan-limited at all, _no indicator_ now has four possible causes and only one of them is _healthy_ — a surface rendering the absence as health would misreport three. **No census move**: no wire member, event type, error code, table, or column is minted here; the producers are Spec-006's and Spec-029's.

> **Amendment (2026-08-26, provider-switch timeline note — amends the previously-`approved` spec; the header flips to `review` under the audit runbook's spec-amendment rule and is restored `approved` by the paired [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) targeted readiness-audit delta riding this same diff; [cross-plan-dependencies.md §6](../architecture/cross-plan-dependencies.md) node NS-84).** Two run-state subtype rows: `agent.provider_switched` and `agent.provider_switch_failed` each render as a **single status line**, not a message and not a block. The consumer rule is the one this spec already applies everywhere — each row is composed from the event payload verbatim and the renderer derives nothing, so a switch that declared no losses renders without a loss clause rather than being decorated with a generic caution, and the three continuity arms differ only in whether that clause is present. The failed row is the one caution of the pair and must never read as a switch that happened: it names the binding the agent **stayed on**. Adds [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) T4.9 under CP-016-17's consumer side; mints no event type, wire member, or error code **here** (both types are minted by Spec-006 under Plan-016's ownership), so no census moves in this spec.

> **Amendment + restoration (2026-08-29, desktop-console parity — the input-ask card and the participant-triggered compaction affordance; in-swap flip-and-restore riding the [Spec-005](005-provider-driver-contract-and-capabilities.md) desktop-console parity amendment's own diff; the header flips to `review` under the audit runbook's spec-amendment rule and is restored `approved` by the paired [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) targeted readiness-audit delta in this same swap; [cross-plan-dependencies.md §6](../architecture/cross-plan-dependencies.md) node NS-93).** Two legs, both closing a gap between what the daemon already emits and what a participant can see or do about it. **(1) The input-ask card.** The `driver_ask.*` family has been fully specified daemon-side since campaign B1 — four event types, a `kind: 'permission' | 'input'` discriminator, a stamped expiry, a fail-closed park on timeout, and the `waiting_for_input` run state — and named in **no renderer surface anywhere in this corpus**. A permission-kind ask already rides the approvals surface and that routing is untouched; an **input**-kind ask had no home at all, so a provider blocking on a structured question produced a run that stopped with nothing on screen to answer. §`driver_ask` adds that home. It **mints no wire method**: the answer ingress is the already-registered client-facing `driver.respondToRequest`, whose `response` is already `unknown`, so free text is representable today. One additive-optional payload member is registered — `options?` on the `driver_ask` shape, produced by the driver's own normalize boundary where the provider's ask declares a closed choice set — so that this section's structured-options arm is reachable rather than dead text; it mints no event type and the taxonomy census does not move. **(2) The compaction affordance.** §Context Window and Usage Meters has told a participant since it was written that their context is nearly full and that the system will not act on it, while offering them no way to act either. That sentence is **explicitly reconciled rather than quietly revised**: the hint stays informational, the system still **never** compacts automatically, and a participant-initiated trigger is stated as a **different thing** beside it. The affordance dispatches the capability-gated `driver.compactContext` verb `Spec-005 §Participant-triggered context compaction` mints, renders from the provider's own typed compaction evidence rather than from the request being accepted, and is absent — not disabled-with-a-tooltip — where the bound driver declares `context_compaction: false`. No event type, error code, table, or column is minted here, and no census moves.

> **Amendment + restoration (2026-09-01, the child-run incompleteness marker — the Tier-8 plan-readiness-audit residual ([cross-plan-dependencies.md §6](../architecture/cross-plan-dependencies.md) node NS-20) shaped; the header flips to `review` under the audit runbook's spec-amendment rule and is restored `approved` by the paired [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) targeted readiness-audit delta riding this same diff; [cross-plan-dependencies.md §6](../architecture/cross-plan-dependencies.md) node NS-98).** §Fallback Behavior has said since this spec was written that a failed child-run detail fetch leaves the summary row "visible and marked incomplete rather than disappearing", and **no member ever carried that mark** — the only signal of incompleteness was a low event count, which is indistinguishable from a child run that genuinely did little. The mark becomes a **required** discriminated member on the child-run summary whose incomplete arm names a **cause** and the **time** that cause was observed. The cause set is **closed at three**, because three is what the daemon can actually distinguish, and every member is a term this corpus already owns rather than one minted here: `detail_fetch_failed` is this section's own naming of the condition; `pending_backfill` is [Spec-008 §Peer History Backfill On Join (V1)](008-control-plane-relay-and-session-join.md#peer-history-backfill-on-join-v1)'s coverage-state term, applied at the narrower scope of one child run's rows rather than a session's whole history — the same condition, so reusing the word is accuracy and not overloading, and Spec-008 joins Depends On for it; and `compacted` is already the shipped vocabulary on this spec's own reasoning-surface availability arm. Naming the cause is what makes the mark **actionable** — two of the three are transient and one is terminal, so the consumer's right response differs per cause, which a bare boolean could never convey — and the summary's event count on that arm is stated to be a **lower bound**, the count the daemon holds rather than the child run's true total, which while rows are missing it cannot know. Required rather than optional: an absent marker would be a third state meaning "probably fine", and this section's rule is that incompleteness is **stated**. Typed shapes stay the contract doc's ([API Payload Contracts](../architecture/contracts/api-payload-contracts.md)); built by [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) T1.2 under the new I-013-10. **No census moves**: no event type, wire method, error code, Cedar action, table, or column is minted. **A second leg rides this same amendment: page framing.** §Interfaces And Contracts has required bounded windows and cursor-based continuation of `TimelineRead` since this spec was written and required **neither** of `ReasoningSurfaceRead` nor `ChildRunExpand` — each answered with a whole set and offered the caller no way to ask for a remainder. That is not an ergonomics gap. The local IPC substrate bounds a message body absolutely ([Spec-007 §Wire Format](007-local-ipc-and-daemon-control.md#wire-format), "Maximum message size: 1 MB"), and an oversized **response** is a framing fault rather than a request failure: the reply that cannot be encoded is the very thing that would have carried the error, so the substrate closes the connection instead. An unpageable reply is therefore a **dropped session**, not a recoverable error the caller can retry at a smaller size — and it is reachable from contract-valid data alone, since a row's free-form text fields and its opaque payload are each bounded independently of any row count, so no count ceiling can bound the encoded page. All three reads accordingly state the same three rules. **(1) Continuation on every one of them**: each takes an optional after-cursor and answers with an explicit more-to-come flag, so an over-large result is a **page** rather than a refusal or a truncation the caller cannot detect; a page that reports more to come carries the cursor that continues it, while a terminal page may carry one and is never required to — the asymmetry is deliberate, so a caller that follows a completed read with a live subscription has an after-cursor to hand it wherever the daemon has one. **(2) The bound is on the encoded size of the page**, checked once per reply against the substrate's own declared limit rather than against a proxy for it, and the producer's page fill stops at whichever of the row ceiling or the byte budget trips first. **(3) A page is internally consistent with the header it carries**: entries never move backwards in sequence, and every run-scoped entry in a child-run expansion belongs to the run the response names. Built by [Plan-013](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md) T1.3 and the new T1.5 under the new I-013-11 and I-013-12, with the substrate's frame constant becoming the single declared source both the framer and the page builder read ([Plan-007](../plans/007-local-ipc-and-daemon-control.md) CP-007-16; Plan-007 records a no-flip, its enforcement and its value unchanged). **No census moves** on this leg either: no wire method, event type, error code, Cedar action, table, or column is minted — the growth is request and response members on reads this spec already owns. `Spec-013 §Acceptance Criteria` grows eight → nine by one appended-last criterion, re-derived by counting, and §Depends On gains [Local IPC And Daemon Control](007-local-ipc-and-daemon-control.md) for the substrate limit this leg states its bound against. **A third leg rides the same node, and it is what makes the second one true.** The page bound above is stated against the substrate's declared limit minus a reserve for the framing the reply becomes — and part of that reserve was the request `id` the reply must echo, which nothing enforced: `JsonRpcId` is a bare union with no schema anywhere in the corpus, so an id large enough to displace a page was contract-valid and the bound was an allowance rather than a derivation. It is bounded at the **request** boundary instead, on the JSON-encoded form so escaping is covered, by [Spec-007 §Wire Format](007-local-ipc-and-daemon-control.md#wire-format) and refused before dispatch — no response schema can bound a member the response does not choose. The subscription half is closed on the same principle: `TimelineSubscribe`'s binder now routes every emission and completion through the shared subscribe-init ordering barrier that `session.subscribe` already carried, so [Spec-007](007-local-ipc-and-daemon-control.md)'s acknowledgement-precedes-notification rule holds for this read's live arm by construction rather than by a projection's good behavior — the failure it prevents is silent, a client dropping frames for a subscription id it has not yet been told. And `ReasoningSurfaceRead`'s principal, settled above as a member the request does not carry, is given the carrier that makes that settlement implementable: the daemon stamps it on the dispatch context ([Plan-007](../plans/007-local-ipc-and-daemon-control.md) CP-007-17, its I-007-21), and this spec's reasoning read waits on that carrier rather than shipping a policy-aware surface with no caller to evaluate. Spec-007 and Plan-007 flip and are restored `approved` in this same swap for those three legs; the frame-constant move of the second leg keeps its recorded no-flip. **Still no census moves** on any of them. **A fourth leg closes the class both of the first two belong to: an answer that is well-formed and about the wrong subject.** The three reads above are bounded, paged, and internally consistent — and every one of those rules is checked by a response schema, which by construction never sees the request that provoked it. That leaves one defect no schema in this corpus can reach: a read for session A returning a page of valid rows from session B, an expansion of run A answering self-consistently about run B, a live subscription forwarding another session's rows under the subscription id its client trusts. Each parses, each correlates to the caller's own request id, and nothing downstream can tell any of them from a correct answer. The rule is therefore stated on the reply rather than in the shape, and enforced at the daemon binder where the parsed request is still in hand — refused as a **daemon** failure, since the caller's question was well-formed and it is the answer's subject that is wrong, and refused before delivery on the subscription arm because the acknowledgement it would ride behind has already settled and a notification frame carries no error channel. The same leg closes the run-attribution half: run-scopedness stops being decided by category alone — which was only ever right for `run_lifecycle` — and is decided by three tests, the third of which is the row's own payload, because the corpus registers run identity as OPTIONAL on every `artifact_publication` type, on five `usage_telemetry` types, and on `user.message`, so the same type is run-scoped on one row and session-scoped on the next and no type-level rule can be correct for both. And where a projection echoes the canonical origin keys, they must agree with the row's own triple, which is [I-013-3](../plans/013-live-timeline-visibility-and-reasoning-surfaces.md)'s no-second-source rule reaching the one place it had not: the payload. Built by T1.1 and T1.4 under the extended I-013-1 and I-013-3 and the new **I-013-13**; the census of run-naming types is derived from Plan-006's own per-category arrays rather than transcribed, and pinned by count so a taxonomy growth fails a test instead of silently changing what the timeline refuses. Plan-007 and Spec-007 take **no** flip for this leg and are not edited: the refusal reuses the substrate's already-registered result-validation code, mints no substrate file, and the handler-barrel export line is the seam five plans already append to. **A fifth leg closes the paging contract on the two ends it left open.** The continuation rule above said what a page that reports more must carry and said nothing about how little it may carry, so a page promising more and delivering nothing was contract-valid — a cursor that never advances, re-asked forever, with the offending row never named. A continuing page now carries at least one entry, which forces the decision the byte budget had deferred: the budget bounds **aggregation**, never a page below one entry, so a first candidate that alone exceeds it is paged as that single entry and refused at the response boundary for its size, where the refusal names the member and rides an error frame the substrate can deliver — rather than returned as a bare zero each producer had to remember to special-case. The other end is the reasoning surface's own floor, which was stated as an absolute and is true only of a **first** read: an empty `available` page collapses onto `unavailable` when it is the first thing a caller sees, and is the only honest answer when the caller's cursor had already reached the end, since all three other states would misstate it. So the floor splits — the schema keeps the half it can see, the continuing arm that can loop, and the daemon binder keeps the half that needs the request, joining the fourth leg's correlation checks rather than minting a mechanism. The same leg completes the payload-attribution rule of the fourth on the one spelling it had not read: `targetRunId`, which is how the intervention family names its run, checked against the row's own attribution on every arm that carries one. **Still no census moves**: no wire method, event type, error code, Cedar action, table, or column is minted, and the growth is arm-scoped validation on replies this spec already owns.

## Purpose

Define the user-facing contract for live timeline visibility, background work surfacing, and reasoning disclosure.

## Scope

This spec covers the canonical timeline read model, child-run visibility, reasoning surfaces, and replay-aware live updates.

## Non-Goals

- Notification rules
- Artifact storage internals
- Provider-specific reasoning formats

## Domain Dependencies

- [Agent Channel And Run Model](../domain/agent-channel-and-run-model.md)
- [Artifact Diff And Approval Model](../domain/artifact-diff-and-approval-model.md)
- [Run State Machine](../domain/run-state-machine.md)

## Architectural Dependencies

- [Observability Architecture](../architecture/observability-architecture.md)
- [Component Architecture Desktop App](../architecture/component-architecture-desktop-app.md)

## Required Behavior

- The system must expose one live timeline per session or channel view built from canonical events.
- Timeline rows must cover at least messages, handoffs, run state changes, tool activity, approval events, interventions, artifacts, and child-run activity.
- Background runs and child runs must be visible in the primary session experience, even when details are lazy-loaded.
- Reasoning surfaces must be normalized and policy-aware; unavailable or redacted reasoning must still produce a visible reason surface.
- Durable reasoning surfaces must be limited to normalized reasoning summaries, state transitions, tool-intent or tool-result summaries, and policy-redaction markers. Provider-native detailed reasoning is not guaranteed durable.
- Live delivery must support replay catch-up so clients can recover missing timeline state.
- **Superseded-turn rendering (campaign B9, CP-004-13 — 2026-07-20).** Rolled-back (superseded) turns remain in the timeline and MUST render distinctly from current rows — an explicit superseded treatment, never dropped and never re-rendered as current ([Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior) owns the supersede semantics; marks are epoch-scoped, so a re-executed turn reusing a superseded ordinal renders current). The marking reaches the surface on two legs with one outcome: `TimelineRead` and replay rows carry a projection-computed `superseded` marker (sourced from the Plan-004 supersede projection's exported `supersededTurns(runId)` read — the CP-004-13 seam), and on the live stream the accepted `run.rolled_back` boundary entry (§Timeline Entry Types) — delivered to every filtered subscription whose filter admits any of the affected run's rows (projection-resolved visibility, never the event's optional channel field) — instructs the client to apply the same treatment to that run's **already-delivered** rows whose carried run position exceeds the carried rewind cutoff (run-scoped rows expose their run identity, projection-resolved originating run position, and execution epoch as typed row fields required together — a partial attribution row fails schema validation, never a silently incomparable cached row; the session event sequence is never a run position, and the boundary entry is a typed arm of the discriminated row union the read and subscribe surfaces return: its payload is validated into the `run.rolled_back` event shape at projection, so the cutoff is never read through an untyped cast) — an idempotent rule across multi-rollback sequences (a row already superseded re-marks as a no-op), with each newly-marked row's marker being just the boundary's cutoff against the row's own exposed identity — identical to the replay-computed marker by construction, a previously-current row's first remover being that boundary, so a subscriber that joined from a bounded window after earlier rollbacks marks its cached rows without recovering epochs from reused ordinals whose delivery-order scoping needs no epoch tag, because every row delivered **after** the boundary arrives with its marker already projection-computed: a late pre-rollback straggler appends pre-marked exactly when its stamped attribution ranks it above the run's effective cutoff for its epoch — the minimum rewind cutoff among accepted rollbacks at its epoch or later in the run's lineage, since a later rollback that rewinds below an earlier retained prefix supersedes the inherited rows — and current when it ranks into the run's surviving history ([Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior); the CP-004-12 `sourcePosition` companion exists precisely for this ranking) — and a new-epoch row appends unmarked — so live and replay views converge on identical marking. A compacted superseded row keeps the treatment: its audit stub renders with both the compaction placeholder and the superseded marker (stub-preserved attribution — [Spec-006 §Compacted Event Format](006-session-event-taxonomy-and-audit-log.md#compacted-event-format)).

## Default Behavior

- Timeline rows default to chronological order from oldest to newest within the current view.
- Row details default to collapsed when the payload is large or high-volume.
- Child-run activity defaults to summarized rows with explicit expansion for detailed inspection.
- If provider reasoning is available and permitted, the system may render a structured reasoning surface tied to the relevant run or message row.
- Detailed provider-native reasoning defaults to ephemeral rendering or bounded diagnostic retention only when policy allows it; it is not part of the durable canonical timeline contract.

## Fallback Behavior

- If live delivery gaps occur, the client must request replay from the canonical event source.
- If detailed reasoning or tool payload is unavailable or policy-restricted, the timeline must show a placeholder row with the reason for unavailability.
- If a child-run detail fetch fails, the summary row remains visible and marked incomplete rather than disappearing. The mark is **explicit and required**, never inferred from a low event count: it names a **cause** from the closed set `detail_fetch_failed` (this daemon's own expansion read failed) / `pending_backfill` (the producing node's rows have not reached this node — [Spec-008 §Peer History Backfill On Join (V1)](008-control-plane-relay-and-session-join.md#peer-history-backfill-on-join-v1)'s term, at the scope of one child run) / `compacted` (the rows were compacted away), together with the time that cause was observed, because the first two are transient and the third terminal and a consumer's right response differs between them. On the incomplete arm the summary's event count is a **lower bound** — what the daemon holds, not the child run's total.
- If detailed reasoning has been compacted or was never retained, the durable reasoning summary or policy placeholder remains the canonical visible surface.
- If a superseded row's payload has been compacted, the stub placeholder retains the superseded treatment — compaction never launders a rewound row back to current. A vacuous-attribution-era legacy stub (its position unknowable) renders the compaction placeholder alone, exempt from marking by construction — a run carrying one can never admit a rollback ([Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior)'s standing refusal), so no marker can ever apply to it.

## Timeline Entry Types

> These timeline entry types are projection-layer constructs derived from canonical session events (see [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md)). They do not create new event types.

### `handoff`

Agent-to-agent and participant-to-agent handoffs rendered as discrete timeline rows, visually distinct from normal messages.

- **Payload**: `{fromActor: string, toActor: string, reason?: string, channelId?: ChannelId}`
- A `handoff` entry is emitted when:
  - Orchestration transfers a run between agents.
  - A participant explicitly delegates to an agent.
  - An agent spawns a child run on a different node.

### `driver_ask`

A provider-initiated **input**-kind ask, rendered as a discrete, actionable question card in the run's timeline (2026-08-29). Permission-kind asks are **not** rendered here — they normalize into the canonical approval model and ride the approvals surface ([Spec-012](012-approvals-permissions-and-trust-boundaries.md)), and this section changes that routing in no way. The `kind` discriminator on the event is what selects between the two surfaces, so exactly one of them renders any given ask and neither has to guess.

- **Source events**: the four `driver_ask.*` types ([Spec-006 §Driver Ask Events](006-session-event-taxonomy-and-audit-log.md#driver-ask-events)), filtered to `kind: 'input'`.
- **Renders**: the ask's `prompt` as the question; the ask's structured `options` as selectable answers **when the ask carries them**; and — on every ask, always, with or without options — a **free-text answer arm**. The free-text arm is not a fallback the renderer chooses between: it is unconditional, because a provider that declares no closed choice set is the ordinary case on at least one pinned driver, and a card that rendered only options would leave such an ask unanswerable while the run sits blocked. Where both are present the options are a shortcut into the same answer, not a different one.
- **Expiry**: a countdown rendered from the ask's `expiresAt`, which the daemon stamps at creation and never extends. The countdown is a display of that stamp and never a second clock — the renderer does not decide when an ask expires, and a card whose countdown reaches zero waits for the `driver_ask.expired` row rather than settling itself, so the surface can never show an ask resolved that the daemon still considers open.
- **Terminal states render from the event stream verbatim**: `responded`, `expired`, and `canceled` each render from their own `driver_ask.*` row, showing the delivered answer for `responded` and, for the other two, saying which happened. The renderer derives **no** terminal state of its own, on this spec's standing wire-verbatim discipline. This matters more here than on a status row: an `input` ask that expires **parks its run resumable with no fabricated input** ([Spec-012 §Resolved Questions and V1 Scope Decisions](012-approvals-permissions-and-trust-boundaries.md#resolved-questions-and-v1-scope-decisions)), and a card that locally decided it had timed out could show a park that had not happened, or hide one that had.
- **Answer dispatch**: the already-registered client-facing `driver.respondToRequest`. No timeline method and no new wire verb is minted for this card — the ingress exists, is client-facing, and already carries an unconstrained response value.
- **Unrecognized values render rather than vanish**: an option shape or a state value this spec does not enumerate is shown as itself, so a later amendment's member is never silently invisible.

### Run-State Subtypes

Run-state subtypes are rendering types that map from underlying run lifecycle events. They exist so the timeline can render distinct visual treatments without inspecting raw event payloads.

| Entry Type | Rendered As | Source Condition |
| --- | --- | --- |
| `run.paused` | Status row with pause icon | Run transitions to `paused` state |
| `run.resumed` | Status row with resume icon | Run transitions from `paused` to `running` |
| `run.blocked` | Status row with block indicator | Run enters `waiting_for_approval` or `waiting_for_input` |
| `run.unblocked` | Status row with unblock indicator | Approval or input resolves the block |
| `run.rolled_back` | Status row with rewind indicator; the run's already-delivered rows above the carried rewind cutoff take the superseded treatment (§Required Behavior) | Accepted rollback rewinds the run — the forward `run.rolled_back` event ([Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior)) |
| `agent.provider_switched` | **One-line status row**, never a message row and never a block: the agent's name, the binding it moved from and to, and — when `continuity` is `'memo'` — the fact that the new provider is working from a summary rather than the full conversation, with the declared losses named in plain words. A switch that carried everything renders the same single line without a loss clause; it does not render as a warning, because nothing was lost. All three continuity arms render the same one line and differ only in that clause: `'in_place'` and `'replayed'` carry none, `'memo'` carries it. The row is rendered from the event payload's `from*` / `to*` members and `declaredLosses` **verbatim** — the renderer derives nothing, on this spec's standing wire-verbatim discipline, and in particular does not infer loss from the presence of a switch, nor a difference in treatment from the difference between `'in_place'` and `'replayed'`, which is a mechanism detail and not something the participant is asked to act on | The `agent.provider_switched` event ([Spec-006 §Channel and Agent Lifecycle](006-session-event-taxonomy-and-audit-log.md#channel-and-agent-lifecycle-session_lifecycle); [Spec-016 §Same-Agent Provider Switch](016-multi-agent-channels-and-orchestration.md#same-agent-provider-switch)) |
| `agent.provider_switch_failed` | **One-line status row** on the same shape, and the one arm of the pair that is a caution: the agent's name, the binding it **stayed on**, and the closed `reason` rendered in plain words. It never reads as a switch — the participant asked for a change that did not happen, and a row that implied otherwise would be worse than none. The reason is rendered from the payload verbatim, an unrecognized value showing the value itself rather than being dropped, so a later amendment's member is never silently invisible | The `agent.provider_switch_failed` event ([Spec-006 §Channel and Agent Lifecycle](006-session-event-taxonomy-and-audit-log.md#channel-and-agent-lifecycle-session_lifecycle); [Spec-016 §Same-Agent Provider Switch](016-multi-agent-channels-and-orchestration.md#same-agent-provider-switch)) |

## Context Window and Usage Meters

The session composer area must always display a context-window meter reflecting the current provider conversation state.

- **Fields**:
  - `usagePercent` (0-100): current context window consumption as a percentage of the provider limit.
  - `tokenCount`: combined input + output token count for the active conversation.
  - `maxTokens`: the provider's context window limit for the active model.
- **Auto-compaction hint**: when `usagePercent` exceeds 80%, the meter must display a warning suggesting conversation compaction. The warning is informational; compaction is not triggered automatically.
- **Visibility**: the context-window meter is always visible in the session composer area regardless of usage level.
- **Participant-triggered compaction is a separate thing from the hint, and the hint is unchanged (2026-08-29).** The bullet above is **reconciled here explicitly rather than revised**: the 80% warning stays informational, and the system still **never** compacts automatically — no threshold, no timer, and no heuristic anywhere in this product initiates a compaction. What is added beside it is a **participant-initiated** trigger, which is a different act with a different actor: the participant decides, and the system does what it was asked. Conflating the two would give up the property the original sentence was protecting — that a participant's conversation is never silently reshaped underneath them — and this affordance does not touch it.
  - **The affordance** is a composer-area control offered beside the meter. It is **capability-gated** on the bound driver's `context_compaction` flag ([Spec-005 §Participant-triggered context compaction](005-provider-driver-contract-and-capabilities.md#participant-triggered-context-compaction)) and is **absent** where the flag is `false` — not present-and-disabled, because a disabled control asserts that the capability exists and is momentarily unavailable, which would be false. It is offered at every usage level rather than unlocking at the warning threshold: a participant who wants a clean context before a long task should not have to fill one first.
  - **It dispatches the already-minted `driver.compactContext`** and renders **only** from what comes back through the event stream. A dispatch shows an in-progress state; the completed state renders on the `usage.context_compacted` row, and the meter's own new reading arrives on the ordinary `usage.context_window_update` path with no special case. The renderer never treats the request's acceptance as a compaction, and never redraws the meter from a value it predicted — both provider mechanisms answer before the work is done, so a surface that trusted the acknowledgment would show a compaction that had not happened. A refusal renders as a refusal. **The in-progress state clears on the operation's own settlement, not on the boundary row (2026-08-29).** The two are separate moments and conflating them would hang the control: the daemon's compaction wait is bounded and can settle `failed` — an expired wait, a lost binding, a provider error — with no boundary row ever arriving ([Spec-005 §Participant-triggered context compaction](005-provider-driver-contract-and-capabilities.md#participant-triggered-context-compaction)). So the in-progress state ends when the call settles, in **every** arm, and the settlement's own reason renders where it is not `applied`; the **completed** state still renders only on the `usage.context_compacted` row, unchanged. The two rules compose without conflict because they answer different questions — whether the request is still outstanding, and whether a compaction actually happened — and a late boundary row arriving after a `failed` settlement moves the meter exactly as an unsolicited provider-initiated compaction would, which is what it is indistinguishable from at the point it is normalized.
- **Update mechanism**: the meter is updated via `usage.context_window_update` events from the canonical event stream (see [Spec-006](../specs/006-session-event-taxonomy-and-audit-log.md)).
- **Compaction boundary and superseded marking** (2026-08-16, the [Spec-004](004-queue-steer-pause-resume.md) rewind-hardening consumer leg — a clarification of rules already stated here, minting no new obligation and no acceptance criterion). A **provider-side** context compaction is recorded as a `usage.context_compacted` row ([Spec-006 §Usage Telemetry](006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)) — a different thing from the daemon retention pass's `event.compacted` audit stubs that §Required Behavior's superseded-turn bullet already covers, and not something the 80% hint above ever triggers. Two consequences follow from existing rules rather than from any new one: the row is run-scoped, so it takes §Required Behavior's superseded treatment on exactly the same terms as every other run-scoped row, with no exemption; and this meter, which reflects the **current** provider conversation state, therefore stops reading a superseded compaction marker as the run's live compaction state. [Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior) additionally treats the newest current such row as a **rewind boundary** and refuses any target below it, so in V1 no accepted rewind supersedes one and the case does not arise; the consistency stated here is what keeps the two surfaces aligned wherever a driver's re-verified boundary-crossing semantics later makes it reachable.

### Rate-Limit Display

A rate-limit indicator shows the remaining provider quota **of one named provider account, for one named limit**. The figure is an account-plane fact, never a session-level or per-run one, and it must never be rendered as either.

- **Two sanctioned Sources, and no third** (amended 2026-08-26). **(a)** The `usage.rate_limit_update` event on the canonical event stream ([Spec-006 §Usage Telemetry](006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry)), emitted from real traffic. **(b)** The **newest stored reading** in the daemon's per-account quota-window registry ([Spec-029 §Per-limit provider quota](029-provider-accounts-and-credential-homes.md#per-limit-provider-quota)), carried on the provider-account read and its subscription. **A stored reading keeps the provenance it was observed under, not the transport that delivered it**: the registry is fed by both the real-traffic event and the deliberate probe, so a window served from it may carry `source: 'run'` exactly as legitimately as `source: 'probe'`. A consumer accepts both on a stored snapshot and never infers provenance from the fact that a reading arrived on a read rather than on the event stream. The second Source exists because the first is only available to a client that was connected when the traffic happened: a renderer opened after a reading was taken, or after a restart, would otherwise show nothing and be unable to distinguish that from a healthy account. **Precedence between them is by observation time, not by source**: newest `observedAt` per `(providerAccountId, limitId)` wins, and `source` breaks **only exact ties**. Preferring one source outright would let a stale reading mask real consumption. A stored reading additionally carries the credential generation it was observed under, and a reading whose generation is behind the account's current generation is rendered **stale rather than current** rather than dropped — a credential-home rebuild does not stop the provider-side window the reading describes ([Spec-029 §Per-limit provider quota](029-provider-accounts-and-credential-homes.md#per-limit-provider-quota)).
- **Neither Source is an HTTP response header.** The indicator is not extracted from any control-plane response header — the control plane's own rate limiting meters this product's API, which is a different quota from the provider's and must not be displayed in its place.
- **Fields** (as carried by the event, plus the account identity added by [Spec-029 §Provider quota is account-scoped](029-provider-accounts-and-credential-homes.md#provider-quota-is-account-scoped)):
  - `provider`: the provider whose quota window this reading describes.
  - `providerAccountId` and the credential generation observed with it: together the `(accountId, credentialGeneration)` account-plane key of the account the reading belongs to (see [Spec-029 §Account identity and credential generation](029-provider-accounts-and-credential-homes.md#account-identity-and-credential-generation)).
  - `limitId` (optional on the wire, defaulted on read): the provider's own identifier for **which** limit this reading describes. **This is the reading's key, together with the account** — absent means the reserved identifier `default`. It is required here because a single account may stand against several limits at once: the pinned Claude surface publishes five, of which **three share a 10080-minute window**, so a surface keyed on the window length would render one weekly limit as the operator's weekly standing and silently drop the other two, with the survivor decided by arrival order.
  - `windowMins`: the length of the provider quota window this reading describes. A **property of the reading, not part of its key** — two limits of one account may share a length.
  - `usedPercent` (0-100): the fraction of that window's quota consumed.
  - `resetsAt` (optional): ISO-8601 timestamp when the window resets, present only when the provider supplied one.
- **No absolute counts**: the carrier reports a percentage, not request or token counts. The surface must render the percentage it was given and must not synthesize a `remaining` or `limit` figure, which is not derivable from a percentage alone.
- **No run to join through**: the event carries no `runId`. Quota is consumed by an account across every run and every session that account executes, so there is no run whose row could supply the account — which is exactly why the event carries the account identity directly rather than leaving it to a join.
- **Account labeling is mandatory**: every rendered quota figure must be labeled with the account it describes, using that account's operator-chosen display label ([Spec-029 §The account registry](029-provider-accounts-and-credential-homes.md#the-account-registry)). A node may hold several accounts for one provider, and each has its own independent quota; an unlabeled or merged meter would show an operator a healthy quota while the account actually executing their run is exhausted. Readings for different accounts are never summed, averaged, or otherwise collapsed into one figure.
- **Limit labeling is mandatory on the same grounds, and readings are never collapsed across limits** (added 2026-08-26): where the provider publishes a display label for a limit it is rendered, and where it does not the limit identifier is rendered verbatim. **Every consumer obligation on this display applies per `(providerAccountId, limitId)` and never across limits** — including the drop-a-lower-reading rule, which now requires the same `limitId` as well as the same `resetsAt` before a decrease may be discarded. Two limits of one account move independently, so a comparison spanning them was never meaningful, and collapsing them would reproduce inside one account exactly the harm the account-labeling rule prevents across accounts.
- **Threshold coloring**, computed from the remaining share (`100 - usedPercent`) of the account's window:
  - Green: >50% remaining.
  - Yellow: 20-50% remaining.
  - Red: <20% remaining.
- **Reset timing**: when the indicator is visible and the reading carried a `resetsAt`, it displays a countdown to that time. A reset time is never estimated or fabricated when the provider did not supply one.
- **Visibility**: the indicator is shown for **any `(account, limit)` pair** whose remaining quota is below 50%, and hidden while that pair is healthy. An account standing against several limits may therefore show several indicators, or one, or none, independently.
- **What absence means, restated (amended 2026-08-26 — the second Source widened it, and the widening is not cosmetic).** Absence of an indicator has never been an affirmative statement that quota is healthy, and it is now less of one than before: it means no constraining reading is held for that pair, and there are **three** distinct ways for that to be true — the pair is genuinely healthy; no reading has ever been taken from either sanctioned Source; or this home is **not plan-limited at all**, which the provider itself reports for API-key and third-party-inference sessions where plan limits are simply not a concept. A surface that rendered "no limits reported" as "healthy" would misreport both of the last two. **Silencing the background health observer is deliberately not on this list**: [Spec-029 §Per-limit provider quota](029-provider-accounts-and-credential-homes.md#per-limit-provider-quota) admits exactly two quota Sources and excludes that observer by name, because reading quota on the pinned Codex leg traverses a path documented to refresh proactively. Listing the opt-out as a quota-absence cause would both misattribute the absence and invite an implementation to satisfy the explanation by having the observer fetch quota — the one thing its no-refresh invariant forbids. Where the daemon knows which of the three applies, the surface may say so; where it does not, it says nothing rather than guessing.
- **Update mechanism**: readings are keyed by `(providerAccountId, limitId)`, exactly as the event registers them ([Spec-006 §Usage Telemetry](006-session-event-taxonomy-and-audit-log.md#usage-telemetry-usage_telemetry) — re-keyed 2026-08-18 from `(provider, windowMins)` to `(providerAccountId, windowMins)`, closing the collision **across** two registered accounts of one provider, and again 2026-08-26 to `(providerAccountId, limitId)`, closing the collision **within** one account that the previous key still admitted), so a reading is held per account and per limit rather than once per node and is updated as later readings for that same pair arrive. A reading observed under a credential generation that the account has since moved past is stale and must not be shown as the account's current quota.
- **Node-local, and deliberately not symmetric with the session cost receipt**: the quota event binds to the reserved node-scope sentinel rather than to a session, and is excluded from control-plane anchor upload and from peer history backfill ([Spec-006 §Daemon-Scope Event Binding And Node-Scope Anchoring](006-session-event-taxonomy-and-audit-log.md#daemon-scope-event-binding-and-node-scope-anchoring)). A participant therefore sees quota only for accounts registered on their own node, and never for the accounts of a peer executing in the same session. This does **not** generalize to spend: the session cost receipt ([Spec-016 §Session Cost Receipt](016-multi-agent-channels-and-orchestration.md#session-cost-receipt)) is session-wide, because the usage events it folds are session-scoped and relay to peers normally. A reader must not assume the two surfaces share a reach — one meters an account on this machine, the other meters a session across every machine in it.

## Interfaces And Contracts

- `TimelineRead` must support bounded windows and cursor-based continuation.
- `TimelineSubscribe` must support live append plus replay recovery.
- `ReasoningSurfaceRead` must identify availability status and policy reason when content is withheld.
- `ChildRunExpand` must read detailed activity for a summarized child-run row.
- `ReasoningSurfaceRead` and `ChildRunExpand` must support the same cursor-based continuation `TimelineRead` does: each takes an optional after-cursor, and each answers with an explicit more-to-come flag so that an over-large result is a page the caller can continue rather than a refusal or a silent truncation.
- Every page a read returns must fit in one transport message body. The substrate's limit ([Spec-007 §Wire Format](007-local-ipc-and-daemon-control.md#wire-format)) is absolute and an oversized response closes the connection rather than failing the request, so the bound is stated on the **encoded size of the page** and checked once per reply — never on a row count alone, which cannot bound a page whose per-row text fields and opaque payload are bounded independently of it. A producer fills a page until the row ceiling or the byte budget trips, whichever comes first, and reports the remainder as a continuation.
- A page that reports more to come must carry the cursor that continues it **and at least one entry**. A page promising more and delivering none advances nothing while reading like progress: the caller obeys the contract, re-asks from the same cursor, receives the same answer, and loops. The byte budget above therefore bounds **aggregation** and never bounds a page below one entry — where the first candidate alone exceeds it, the producer pages that entry rather than reporting an empty page, and the reply is refused for its size at the response boundary, which names the offending member instead of leaving a caller to loop on an unconsumed cursor. A terminal page may carry a cursor and is never required to, so a caller moving from a completed read to a live subscription has an after-cursor wherever the daemon has one; a terminal page may also be **empty**, which is the honest answer to a continuation whose cursor already sat at the end and to a filtered read that matched nothing.
- A page must be internally consistent with the header it carries: its entries never move backwards in sequence, and every run-scoped entry of a child-run expansion belongs to the run the response names.
- **Every reply and every emission is scoped to the request that asked for it, and answers the question it asked.** A timeline read returns rows of the requested session only; a child-run expansion names the requested run; a live subscription carries rows of the subscribed session only. One further reply is judged against the request rather than against itself: an `available` reasoning surface carrying **no entries** is the correct answer to a continuation whose cursor already sat at the end of the surface, and a defect on a **first** read, where it renders as a surface that exists and shows nothing — indistinguishable from the unavailable state while asserting the opposite, the state collapse the availability vocabulary exists to prevent. A producer with nothing to serve at all must answer with the state that is true. A third is judged the same way: a page may not exceed the **window the caller asked for**, which is the request's own row limit where it named one and the default ceiling where it did not — a response schema can bound a page only at the default, so a read for ten rows answering with the maximum is otherwise well-formed, and a client sizing a viewport, a budget, or a render pass from its own request is handed more with nothing on the reply saying so. None of this can be a schema rule — a response schema never sees the request — so it is enforced where both are in hand, at the daemon's method binder, on the caller's own parsed request. A cross-scope reply is a **daemon** failure and never a client one: the caller asked a well-formed question, the answer is well-formed, and it correlates to the caller's own request id, so nothing downstream can tell it apart from a correct answer. A cross-scope emission is refused before it is written rather than reported, because the acknowledgement it would ride behind has already settled and a notification frame carries no error channel.
- **A row is filed under a run wherever the corpus says it belongs to one, and it may not say so twice differently.** Whether a row is run-scoped is decided by three tests and not by its category alone: a category closed on the run-scoped side, a canonical event type whose registered payload names a run in every form ([Spec-006 §Event Type Enumeration](006-session-event-taxonomy-and-audit-log.md#event-type-enumeration)), or — for the types whose run identity is registered optional, which no type-level test can decide — the row's own payload naming one. A row answering any of the three while carrying the attribution-free kind is refused, because it would be reachable by no run filter and permanently current under rollback projection. Where a projection echoes the canonical origin keys into the payload, they must agree with the row's own attribution — under **either** registered spelling of run identity, since the intervention family names its run `targetRunId` and a rule reading only `runId` would leave every intervention row free to be filed under one run while naming another; echoing stays optional, and disagreement is refused.
- `ReasoningSurfaceRead`'s policy decision is evaluated against the **daemon-resolved calling principal** carried on the dispatch context, and the request carries no principal member of its own. A body field naming a participant would be metadata the authorization never reads ([api-payload-contracts.md §Authenticated Principal And Authorization Model](../architecture/contracts/api-payload-contracts.md#authenticated-principal-and-authorization-model)), and the request schema is strict, so one supplied is refused rather than stripped — a stripped member would let a caller believe it had scoped the read. A read dispatched with no resolved principal is **refused**, never answered against a default identity: a placeholder is indistinguishable from a real caller to the policy check, which would turn a fail-closed read into a fail-open one.
- See [API Payload Contracts](../architecture/contracts/api-payload-contracts.md) for typed request/response schemas.
- See [Error Contracts](../architecture/contracts/error-contracts.md) for error response schemas and error codes.

## State And Data Implications

- Timeline rows are read projections, not canonical events themselves.
- Reasoning disclosure decisions must be traceable to policy and artifact visibility state.
- Child-run summaries and detail windows must preserve provenance to parent run and producing runtime node.
- Durable timeline reasoning rows must remain reconstructible from canonical summaries and policy markers even when detailed reasoning payloads are unavailable.
- Superseded marking is projection-derived, never stored ad hoc: provenance traces through the Plan-004 supersede projection to the accepted `run.rolled_back` boundary event (its run and carried rewind cutoff) plus the projection-derived source epoch, and survives projection rebuilds and compaction ([Spec-006 §Compacted Event Format](006-session-event-taxonomy-and-audit-log.md#compacted-event-format)).

## Example Flows

- `Example: A session timeline shows a run start, command execution, approval request, diff artifact publication, and completion, all as ordered timeline rows.`
- `Example: A background reviewer run appears as a summarized child-run row that can later be expanded to show findings.`
- `Example: A provider emits detailed reasoning during a run. The timeline stores a durable reasoning summary and policy marker, while the detailed payload remains ephemeral or subject to bounded diagnostic retention.`

## Implementation Notes

- Timeline virtualization or pagination is allowed, but it must not alter canonical ordering.
- Redacted reasoning should still be visible as an event that something was intentionally withheld.
- Live timeline and replay logic should share the same projection schema.

## Pitfalls To Avoid

- Hiding child-run work because it happened in the background
- Flattening every structured event into plain chat text
- Rendering reasoning as if it were always available and safe to show
- Dropping rewound turns from the timeline instead of rendering them superseded — the authoritative log never truncates ([Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior))
- Re-rendering superseded output as current after replay, a projection rebuild, or compaction — or marking a re-executed turn that reuses a superseded ordinal (marks are epoch-scoped)
- Applying that treatment optimistically — truncating, removing, or renumbering rendered rows when an edit-and-resend intervention is _admitted_ or _dispatched_ rather than when its accepted `run.rolled_back` boundary entry appends (2026-08-16, a restatement of the rule above rather than a new obligation: the boundary entry is the only thing that applies the treatment, and a composite whose conversation leg never confirms rewinds nothing at all, so an optimistic edit would drop history the run still holds — [Spec-004 §Required Behavior](004-queue-steer-pause-resume.md#required-behavior) owns the rule, and dimming the affected rows while the intervention is in flight is a permitted affordance)

## Acceptance Criteria

- [ ] A client can see live run, approval, artifact, and child-run activity in one timeline surface.
- [ ] Missing live updates can be recovered through replay without rebuilding state from free-form text.
- [ ] Reasoning surfaces clearly distinguish available, unavailable, and policy-redacted cases.
- [ ] Handoff and run-state entries render as distinct timeline rows with appropriate visual treatment.
- [ ] Rolled-back turns render with the distinct superseded treatment identically on live delivery (the `run.rolled_back` boundary entry's already-delivered-rows rule keyed on each row's exposed run position and epoch against the boundary's typed cutoff — delivered to every filtered subscription admitting the run's rows, the live marker identical to replay by construction — plus attribution-ranked late stragglers — pre-marked above the run's effective lineage-minimum cutoff for their epoch, current within the surviving history, a later rollback below an earlier retained prefix superseding the inherited rows), `TimelineRead` windows, and replay recovery; a re-executed turn reusing a superseded ordinal renders current; and a compacted superseded row composes the stub placeholder with the superseded marker.
- [ ] An input-kind driver ask renders an answerable card with the ask's prompt, a free-text answer arm present on **every** such card, and its structured options additionally where the ask carries them; a permission-kind ask renders on the approvals surface and never as this card.
- [ ] An input-ask card's countdown is a display of the daemon's stamped `expiresAt`, and its resolved, expired, and canceled states each render from the corresponding `driver_ask.*` row — a card whose countdown reaches zero shows no terminal until that row arrives.
- [ ] The composer offers a compaction control exactly where the bound driver declares `context_compaction`, absent rather than disabled otherwise, and the completed state renders only on the `usage.context_compacted` row — never on the dispatch being accepted. The control's in-progress state clears on the call's own settlement in every arm — a settled failure leaves no control waiting — while a boundary row arriving after that settlement still moves the meter. No threshold, timer, or heuristic initiates a compaction anywhere in the surface.
- [ ] Every timeline read answers within one transport message body: an over-large result comes back as a page carrying an explicit more-to-come flag and the cursor that continues it, never as a dropped connection and never as a truncation the caller cannot detect. A returned page's entries do not move backwards in sequence, and a child-run expansion's run-scoped entries all belong to the run the response names.

## ADR Triggers

- If reasoning visibility or audit exposure materially changes the observability boundary, create or update `../decisions/004-sqlite-local-state-and-postgres-control-plane.md` or a replacement observability ADR.

## Resolved Questions and V1 Scope Decisions

- No blocking open questions remain for v1.
- V1 decision: per-session verbose reasoning opt-in is out of scope. Reasoning visibility follows the canonical product or organization policy without session-level overrides.
- V1 decision: durable reasoning visibility in v1 is summary-first. Provider-native detailed reasoning may be rendered transiently or retained only as bounded non-canonical diagnostics when policy permits it.

## References

- [Observability Architecture](../architecture/observability-architecture.md)
- [Session Event Taxonomy And Audit Log](../specs/006-session-event-taxonomy-and-audit-log.md)
- [Queue Steer Pause Resume](../specs/004-queue-steer-pause-resume.md)
- [Multi-Agent Channels And Orchestration](../specs/016-multi-agent-channels-and-orchestration.md)
- [Provider Accounts And Credential Homes](../specs/029-provider-accounts-and-credential-homes.md)
- [Provider Driver Contract And Capabilities](../specs/005-provider-driver-contract-and-capabilities.md) — the `context_compaction` gate and the `driver.compactContext` verb the composer affordance dispatches
- [Approvals Permissions And Trust Boundaries](../specs/012-approvals-permissions-and-trust-boundaries.md) — the permission-kind ask routing this spec's input-ask card deliberately does not touch
