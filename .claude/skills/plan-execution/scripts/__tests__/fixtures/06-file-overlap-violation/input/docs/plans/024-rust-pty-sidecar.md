# Plan-024 — Rust PTY Sidecar (Test Fixture Stub)

This stub exists so References in NS-01 resolve to a real file, so refs.files is non-empty and verifyFileOverlap can fire `file_overlap_zero` instead of the `file_overlap_unverifiable_for_sparse_body` soft-warn path.
