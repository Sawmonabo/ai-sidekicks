# Plan-024 — Rust PTY Sidecar (Test Fixture Stub)

This stub exists so References in NS-01 resolve to a real file (file-overlap passes), forcing verifyPlanIdentity to be the next halt point — args.plan=999 mismatches the heading's `Plan-024` substring.
