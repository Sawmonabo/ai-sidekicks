// The send router's door.
//
// One of the composer's three zones. The router resolves Send to the one wire call
// the addressed target admits, so the input, the action, and that resolution are
// one lane's work and live behind one barrel.
//
// The zone forwards its BODY and nothing else. The router class, the refusal
// vocabulary, the directive-line model, and the controller hook are all reached
// deeply from inside this family, which is what this package's structure rules ask
// for — a barrel is the cross-family door, and a re-export of a symbol only this
// family reads would advertise a seam that does not exist.

export { ComposerSendBar } from "./ComposerSendBar.js";
