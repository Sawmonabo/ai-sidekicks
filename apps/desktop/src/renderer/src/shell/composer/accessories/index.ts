// The accessory rail's door.
//
// One of the composer's three zones: the plan-owned meters slot, the
// discovery-only autocomplete, and the draft indicator. Behind its own barrel so
// the lane that fills it edits a directory no sibling lane touches.

export { ComposerAccessoryRail } from "./ComposerAccessoryRail.js";
