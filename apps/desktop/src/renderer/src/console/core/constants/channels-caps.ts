// The channel activity line's one bound: how many working runs are named before the
// line folds to a count.

/**
 * Concurrent runs rendered by name before the line folds to a count.
 *
 * Above three the line stops being information and starts being motion: the names
 * churn faster than they can be read, and what a person actually wants from a fourth
 * working run is the fact that the channel is busy.
 */
export const AGENT_RUNS_NAMED_CAP = 3;
