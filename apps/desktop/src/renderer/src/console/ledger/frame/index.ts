// The ledger frame's door — the scroll chokepoint, reading anchor, reveal engine,
// rail, and replay controls.
//
// RESERVED, NOT STUBBED. The skeleton lane (T-023p-1C-2, L2.0) creates this door
// so the lanes that fill the frame land in a directory that already exists and add
// one re-export line each, at distinct positions, without conflicting. Nothing is
// declared here yet, because a placeholder frame would be a body a reviewer has to
// tell apart from the real one.
//
// `export {}` is what makes an empty file a module rather than a script: the family
// door re-exports this one, so it has a dependent from the moment it exists, and
// the layering gate's orphan rule never sees a disconnected file. The first lane to
// fill it replaces this line with its own export.
export {};
