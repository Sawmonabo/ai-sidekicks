// The ledger's structure door — chapters, seams, and the run threads drawn between
// them.
//
// RESERVED, NOT STUBBED, on `ledger/frame/index.ts`'s terms: the skeleton lane
// creates the directory and its door; the lane that authors chapters and seams
// replaces the line below with its own exports.
export {};
