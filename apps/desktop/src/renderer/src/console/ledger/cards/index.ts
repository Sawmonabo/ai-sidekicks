// The ledger's card door — the streaming-markdown body, the tool card, and the
// inline card seats the repos family fills.
//
// RESERVED, NOT STUBBED, on `ledger/frame/index.ts`'s terms: the skeleton lane
// creates the directory and its door; the lane that authors the cards replaces the
// line below with its own exports.
export {};
