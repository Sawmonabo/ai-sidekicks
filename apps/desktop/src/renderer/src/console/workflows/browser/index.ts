// The definitions browser: the surface that lists what this node can start.
//
// A DIRECTORY BECAUSE THE SUITES ARE THE MODULE'S OWN. Four suites cover one
// component — its announcements, its chat-start seat, its outcome arms and its
// paging — and at the zone level they read as four more files beside the family's
// other surfaces rather than as one subject with four claims.
//
// THE DOOR EXISTS BECAUSE A SIBLING TAKES FROM IT. `destination/` mounts this
// browser, and a sub-module door publishes what a sibling takes and nothing else:
// the props type is the component's own parameter and has no reader outside this
// directory, so it is deliberately absent. `workflows/index.ts` re-exports nothing
// through here — it registers panes and surfaces and reaches neither name.
export { WorkflowsBrowser } from "./WorkflowsBrowser.js";
