// The builder pane's door: the body the deck mounts for the `workflow-builder` kind.
//
// The run pane door's shape, for the run pane door's reasons: one reader
// (`workflows/index.ts`, by a deep intra-family specifier) and one forwarded body.
//
// THE PANE'S STYLESHEET ENTERS HERE, for the run pane door's reason: the barrel of the
// surface that owns the sheet, rather than the family sheet that used to `@import` it
// across a directory boundary.

import "./builder-authoring.css";

export { WorkflowBuilderPane } from "./WorkflowBuilderPane.js";
