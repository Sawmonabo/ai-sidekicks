// The refusal sub-module door: what a SIBLING inside `primitives/` takes from here.
//
// The three shapes a daemon refusal wears — inline beside a control, a banner across
// the frame, a card in the ledger — the recovery shell every family's remedy table
// renders through, the remedy join above them, and the contract that states the
// grammar all of them obey.
//
// ONE LINE, BECAUSE ONE EDGE EARNED IT. `reading/ReadingNotice.tsx` nests the inline
// shape for a refused read, and that is the whole sibling traffic into this
// directory. Every other name here leaves through `primitives/index.ts`, which
// re-exports from the declaring module and never through this file.
//
// AND THE SHEET IS NOT HERE, deliberately. `refusal.css` dresses all five shapes, and
// four of them leave through `primitives/index.ts` from their declaring modules — so
// with the sheet owned by this door its only way into the document was the one line
// `reading/ReadingNotice.tsx` holds, and a family rendering a banner or a card got the
// components styled by an edge that has nothing to do with either. The sheet sits at
// `primitives/refusal.css` beside the door that publishes what it styles, which is what
// its own header has always said, and ownership follows it there rather than staying
// with the deeper barrel, on the module-shape rule in `apps/desktop/AGENTS.md`: either
// move the entry with the sheet or leave the sheet at the door.

export { InlineRefusal } from "./InlineRefusal.js";
