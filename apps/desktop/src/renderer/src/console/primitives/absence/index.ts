// The absence sub-module door: what a SIBLING inside `primitives/` takes from here.
//
// The console's one empty-state shape and the window-cap vocabulary that says a list
// is showing part of itself — a different fact from a read's completeness, which is
// `reading/`'s. The surface-scale frame that wraps a whole absent destination is not
// here: `SurfaceAbsence` sits at the family root beside the error boundary, the two
// shells `frame/` mounts around a surface that has nothing to show or could not draw.
//
// ONE LINE FOR THREE SIBLINGS. `posture/`, `reading/`, and `restore/` each render
// the empty-state shape; none of them touches the window-cap vocabulary, so it is not
// published here and leaves through `primitives/index.ts` from the module that
// declares it.
//
// AND THE SHEET ENTERS HERE, for the reason the rule keys on: a directory carrying a
// door owns its own rules, whatever its depth.

import "./nothing.css";

export { Nothing } from "./Nothing.js";
