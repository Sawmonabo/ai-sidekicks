// The route both `RouteSurface` picker suites name before they assert anything.
//
// `BARE_TIMELINE_ROUTE` is here because the timeline's grammar — one identifier is
// enough — is the contrast the agent-console suite's last negative control is built
// on, so both suites name the same route. The act settlement both of them also need
// used to sit beside it and is now `primitives/act-settlement.test-support.ts`'s: it
// was one of six copies across three families, and the home a helper belongs in is the
// lowest family every consumer sits above.

import { type ConsoleRoute } from "../routing/index.js";

/** The bare auxiliary address a Window-menu open lands on. */
export const BARE_TIMELINE_ROUTE: ConsoleRoute = { kind: "auxiliary", route: "timeline" };
