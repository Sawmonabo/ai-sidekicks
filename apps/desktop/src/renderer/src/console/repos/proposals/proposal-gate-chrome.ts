// The gate's own chrome measurements.
//
// A MODULE OF ITS OWN because the gate and the conflict choice inside it draw glyphs
// at one size, and a size declared in the parent and imported by the child closes a
// cycle the layering gate refuses.

export const GATE_GLYPH_SIZE = 14;
