// The act row group's door.
//
// A SUB-MODULE DOOR AND NOT A SECOND FAMILY DOOR, on `bridge/growth-values/`'s
// precedent: it publishes to `repos/` alone, it is reached by the deep intra-family
// specifier `./proposal-gate-acts/index.js`, and nothing outside this family names it.
// What is behind it is one concern — the acts a gate arm offers, the confirmation that
// stands between a press and the wire, and the value that says what an open
// confirmation is an approval OF.

export { ProposalActionGroup } from "./ProposalActionGroup.js";

// The gate composes the scope, because the group is handed a list of acts and never
// sees the proposal those acts would send.
export { proposalConfirmationScope } from "./proposal-confirmation-scope.js";
