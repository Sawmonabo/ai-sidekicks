// The diff-create sub-module's door.
//
// WHAT CROSSES IT IS THE SURFACE AND THE SUBJECT RESOLUTION, and nothing else. The
// controller, its binding, its reading and the payload reader are this directory's own
// — the pane never names any of them — and a door line for one would be a name no
// production module outside this directory types, which the barrel census fails.
//
// NEITHER SHAPE IS PUBLISHED WITH ITS FUNCTION, for that same rule read strictly. The
// pane hands the surface its props inline and infers the subject off the resolver's
// return, so `DiffCreateSurfaceProps` and `DiffCreateSubject` are names no production
// module outside this directory writes — published for symmetry they would be two more
// door lines with no reader.
//
// THE SHEET ENTERS HERE, because this directory owns it. `apps/desktop/AGENTS.md` keys
// that rule on ownership rather than on depth: a directory carrying a door owns itself,
// and the pane's own `diff.css` dresses the change set rather than the form that asks
// for one.

import "./diff-create.css";

export { DiffCreateSurface } from "./DiffCreateSurface.js";
// The pane reads the address and decides whether a create can be keyed at all, which is
// the one question it has to answer before it can render this surface conditionally.
export { diffCreateSubjectFor } from "./diff-create-subject.js";
