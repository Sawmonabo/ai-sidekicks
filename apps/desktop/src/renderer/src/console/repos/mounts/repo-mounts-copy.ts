// What the repo section says before its one read has answered.
//
// A MODULE OF ITS OWN for `attachment-carrier-copy.ts`'s reason: the mount list and
// the collapsed summary both render this absence, and either of them declaring it
// would make one part's private copy the other's contract.

/** What an unread section says, in both shapes. */
export const NOT_READ_TITLE = "Repo mounts have not been read.";
