// The artifact pane's door.
//
// Same shape and same reason as `panes/diff/index.ts`: one directory, one barrel,
// the body reached only through it, and no stylesheet — the family's single sheet
// is imported from `repos/index.ts`, the module that composes both panes.

export { ArtifactPane } from "./ArtifactPane.js";
