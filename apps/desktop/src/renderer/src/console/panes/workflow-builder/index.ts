// The builder pane's door.
//
// One export, for the reason the run pane's barrel gives: a closed pane kind whose
// body has a home of its own is a kind a reader can find from the set. The canvas
// the pane frames is Plan-017's body and is not exported from here, because it is
// not authored here.

// The props type is not re-exported, for the run pane barrel's reason.
export { WorkflowBuilderPane } from "./WorkflowBuilderPane.js";
