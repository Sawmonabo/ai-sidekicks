# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Project Is

**AI Sidekicks** is a desktop runtime where humans and multiple AI agents share live sessions, co-edit code through proper git flow, and collaborate in real time. The first-class primitive is the **session**, not the agent ([ADR-001](docs/decisions/001-session-is-the-primary-domain-object.md)). A session contains participants, agents, runs, channels, repo mounts, approvals, artifacts, and a unified event timeline. People and machines are both first-class participants.

Three layers:

- **Local Runtime Daemon** — machine-local execution authority. Owns provider processes (`claude-driver`, `codex-driver`), git worktrees, terminal sessions, tool execution, SQLite persistence (55 tables). Worktree-first execution mode ([ADR-006](docs/decisions/006-worktree-first-execution-mode.md)).
- **Collaboration Control Plane** — hosted or self-hosted; auth (PASETO v4 + WebAuthn + DPoP, [ADR-010](docs/decisions/010-paseto-webauthn-mls-auth.md)), invites, presence (Yjs Awareness), E2E-encrypted relay (X25519 + XChaCha20-Poly1305 in V1, MLS RFC 9420 in V2), shared metadata (Postgres, 26 tables).
- **Clients** — CLI (`sidekicks`, first delivery track) and Electron desktop shell with React/Vite renderer, both over a typed SDK + JSON-RPC IPC ([ADR-009](docs/decisions/009-json-rpc-ipc-wire-format.md)).

Stack: TypeScript across daemon/CLI/desktop/contracts; XState v5 state machines; tRPC v11 control-plane API ([ADR-014](docs/decisions/014-trpc-control-plane-api.md)); Zod validation; Cedar policy engine for approvals ([ADR-012](docs/decisions/012-cedar-approval-policy-engine.md)); OpenTelemetry; Rust PTY sidecar on Windows ([ADR-019](docs/decisions/019-windows-v1-tier-and-pty-sidecar.md)). Apache-2.0 ([ADR-020](docs/decisions/020-v1-deployment-model-and-oss-license.md)).

V1 ships 23 features ([ADR-015](docs/decisions/015-v1-feature-scope-definition.md), amended 2026-07-02 and 2026-07-08; feature-14's offline-availability guarantee additionally threat-model-scoped 2026-08-08 per that ADR's §Decision Log — not an amendment, no count moves) across 28 implementation plans in 9 dependency tiers — feature #18 (MCP governance) is governed by [Spec-028](docs/specs/028-mcp-server-configuration-and-governance.md) + [Plan-028](docs/plans/028-mcp-server-configuration-and-governance.md), landed 2026-07-22 via the capability-enhancement campaign's B18 bundle as a Tier-7 leaf (`draft` at landing; promoted `review` 2026-08-12 by its targeted readiness audit, §6 node NS-61, and `approved` 2026-08-14 by its §Rollout Order step-2 promotion PR), with MCP-governance code dispatching in tier order per Plan-028's §Preconditions. Features #19–#23's governing spec amendments are in-tree via the campaign's B1/B2/B3/B6/B20 bundles and re-promoted `approved` through the W1.5 batch gate (2026-07-18; Spec-004 (#19), Spec-012 (#21/#22), and Spec-016 (#20/#23) were re-flipped `review` 2026-08-03 by the V1 product-vision reconciliation amendment bundle — Spec-016 having already been briefly re-flipped `review` 2026-07-21 by B15's §Stop Conditions amendment and restored `approved` the same day by Plan-016's W2.5 targeted re-audit — each restoring `approved` via its queued targeted readiness-audit delta: Spec-004's landed 2026-08-08 (PR #299 / NS-49), restoring it and Plan-004 `approved`, and Spec-012's landed 2026-08-10 (PR #317 / NS-54), restoring it and Plan-012 `approved` and closing #21's and #22's spec layer, and Spec-016's landed 2026-08-11 (PR #321 / NS-56), restoring it and Plan-016 `approved` and closing #20's and #23's — the spec layer is closed across all of #19–#23 (Spec-005 remains `approved`; Spec-006 was flip-and-restored `approved` in the same swap for its channel-lifecycle payload growth (the `channel.created` kind + member-pair mirror, the D-016-22 origin-ordering keys, and the run-interrupt trigger-union member))), so rollback / goals / callback-tool / execution-posture / realtime implementation dispatches with its owning plans' code phases in tier order (the campaign's W2 plan-task bundles are all in-tree; the campaign plan closed `completed` 2026-08-15) — and rollback (#19) additionally gated on the B21→B23 turn-snapshot file-restore leg (Plan-010) before Plan-004 rollback dispatch per the [build-order preamble](docs/architecture/cross-plan-dependencies.md) (met 2026-08-09: the B23 leg shipped via PR #303, leaving Plan-004's rollback dispatch — the Phase-3B cluster since the NS-69 round-1 re-home — on tier order plus its precondition legs), with its superseded-turn timeline rendering riding the Spec-013/Plan-013 CP-004-13 consumer leg (flipped `review` 2026-07-20; restored `approved` 2026-08-10 by the Tier-8 plan-readiness audit, NS-20). The feature list and tier graph live in [`README.md`](README.md); the build-order + shared-resource ownership map lives in [`docs/architecture/cross-plan-dependencies.md`](docs/architecture/cross-plan-dependencies.md).

## Current State: Tier 2-4 Code Execution; Plan-Readiness Audit Chain Complete (Tiers 2-9)

Code execution started 2026-04-26 with the V1 monorepo scaffold (PR #6) — the doc-first gate cleared when [ADR-023](docs/decisions/023-v1-ci-cd-and-release-automation.md) (V1 CI/CD, pre-commit hooks, release automation) was accepted on the same date, closing [BL-100](docs/archive/backlog-archive.md). Feature branches cut off `develop` and squash-merge back per the [GitFlow-lite branch-model amendment](docs/decisions/023-v1-ci-cd-and-release-automation.md#decision-log).

All five [Plan-001](docs/plans/001-shared-session-core.md) phases have shipped (Phase 5 across Lanes A–D):

| GitHub PR | Phase | Package |
| --- | --- | --- |
| #6 | Phase 1 — Workspace Bootstrap | repo bootstrap (pnpm + Turbo + Vitest + ESLint per [ADR-022](docs/decisions/022-v1-toolchain-selection.md)) |
| #8 | Phase 2 — Contracts | `packages/contracts` (session / event / error payload schemas) |
| #9 | Phase 3 — Daemon Migration + Projection | `packages/runtime-daemon` (migration, projector, append/replay) |
| #10 | Phase 4 — Control Plane Directory | `packages/control-plane` (session directory service: create/read/join) |
| #30/#36/#38 (A), #48 (B), #77 (C), #83 (D), #87 (completion) | Phase 5 — Client SDK + Desktop Bootstrap (Lanes A–D) | `packages/client-sdk` + `apps/desktop` (session bootstrap, renderer wiring, sidecar lifecycle) |

`package.json` is real (`pnpm@10.33.2`, Node `>=22.12.0`). Use the wired scripts: `pnpm install`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` (Turbo-driven), `pnpm format` / `pnpm format:check`. **Do not invoke `npm`** — the engines field requires pnpm. Pre-commit hooks (lefthook + lint-staged + commitlint + gitleaks) install via `pnpm prepare`. The unit of work is now mixed: `.md` files under `docs/` for governance and TypeScript under `packages/` + `apps/` for code phases. Doc-first ordering still holds for plan-task shipment — a lane-1 PR cites the plan / spec / ADR(s) it implements; enhancement and tooling PRs take the lighter lanes per [CONTRIBUTING.md](CONTRIBUTING.md) §How Code Lands: Work Classification.

**Active gates blocking the next code PRs:**

- **Plan-024 Phases 4-5** (CI cross-compile + signing; measurement substrate) are procurement-blocked on [BL-108](docs/backlog.md) — tracked as §6 nodes NS-09 + NS-10, the only hard-blocked code lanes on the dependency DAG.
- **Plan-readiness audit** ([runbook](docs/operations/plan-implementation-readiness-audit-runbook.md), methodology PR #14, Tier 1 pilot PR #15) gates code from Tiers 2-9 — each tier's plans must clear the audit before that tier's code PRs land. The plan-template Precondition checkbox blocks `draft → review` for new plans; pre-audit `approved` plans need a retroactive audit pass first. **Tiers 2-9 audits are complete** (NS-14 + NS-15..NS-21; the Tier-9 audit landed via PR #331, closing the tier-PR chain), so those tiers' `approved` plans have cleared the audit gate — with reopened exceptions: Plan-014's 2026-07-08 relay scope growth re-opened its Preconditions checkbox, restored `approved` 2026-08-12 by the relay-scope targeted readiness-audit delta (§6 node NS-59), which re-checked both boxes, minted one born-unchecked scoped box at its Codex fold (the Task-8 fetch-selector daemon carrier — the NS-54 carrier-box class), and cleared the audit and Status gates on Tasks 7–10 — their dispatch now rides Tier-7 order (Plan-008-remainder and Plan-018 at Tier 5, Plan-021 at Tier 6; the Tier-7-wide phase-decomposition obligation that delta escalated was discharged 2026-08-15 — Plans 011/014/015 now carry §Implementation Phase Sequence); Plan-014 and Spec-014 were then flipped and restored `approved` again 2026-08-16 by the four-leg artifact-lifecycle amendment and its in-swap targeted readiness-audit delta (§6 node NS-64), which re-specified the Task-8 fetch selector as the caller's artifact-encryption key-thumbprint set — **checking the carrier box by making a carrier unnecessary** rather than registering one, so Task 8's mint leg carries no hold and the Plan-003 edge narrows to corroboration-only — and added local Tasks 11–13 (the OWASP ingest-validation pipeline, `ArtifactDelete` + derived CAS refcount, and the typed attachment-reference contract) plus an additive unique index on `artifact_relay_recipients` that moves no table census; Tasks 1–6 were covered throughout — and flipped and restored `approved` a third time 2026-08-17 by the ingest-protocol hardening amendment and its in-swap targeted readiness-audit delta (§6 node NS-70): five accepted findings (Codex PR #341 round 5) against the plan's own round-4 ingest stream protocol — the completion record making `AttachmentIngestComplete` retry-idempotent, both ingest byte ledgers serialized reserve-then-install, per-`ingestId` single-flight with the reaper's eviction under the same exclusion, the `pinned` → `expired` manifest write-back on either unobtainable-payload fetch refusal (grounding the unresolved-marker `expired` cause), and the Init-refusal zero-state criterion made runnable through admission probes — no interface, wire member, error code, table, or column minted, so no census moves, with BL-152 minted for the TTL-sweep blob-row disposition the amendment flags without settling, Plan-006's 2026-07-29 CP-006-7 T4.10 signing-key-roster growth likewise re-opened its checkbox and flipped it `review`, restored `approved` 2026-08-01 by the T4.10 targeted readiness-audit delta (PR #278), re-flipped `review` 2026-08-02 by the T3.1 ingest-halt seam amendment (PR #281), and restored `approved` the same day by the T3.1-seam targeted readiness-audit delta (PR #282, NS-43) — lane-1 dispatch (the Phase-1 remainder and Phase 3 included) rides tier order and the plan's §Preconditions — Plan-024's 2026-08-02 CP-024-5 credential-seam registration (PR #279) likewise re-opened its checkbox scoped to Phase 3B and flipped it `review`, restored `approved` 2026-08-03 by the CP-024-5 targeted readiness-audit delta (PR #283, NS-44) — Phase 3B dispatch now rides its Tier-4 remainder row, the plan's §Preconditions, and its `plan_phase` gate on Plan-006 Phase 3 — the 2026-08-03 V1 product-vision reconciliation (PR #284) flipped Plans 002 / 003 / 004 / 008 / 012 / 016 and their paired specs `review`, re-opening each plan's Preconditions checkbox scoped to its amendment growth (cross-user intervention authority, typing presence + the `ChannelList` non-disclosure filter, peer history backfill + per-channel relay recipient scoping, and channel audience + `direct` channels among the legs), each restoring `approved` via its own queued targeted readiness-audit delta — Plan-004/Spec-004's landed 2026-08-08 (PR #299, NS-49), the cross-user intervention authority leg, restoring both and re-checking the plan's box, and Plan-012/Spec-012's landed 2026-08-10 (PR #317, NS-54), that leg's approvals-side second half — the remembered-grant actor axis and the turn-scoped effective principal — restoring both, re-checking the plan's box, and minting one born-unchecked scoped box that holds Plan-012 T2.2 / T2.7 until an admitting-principal carrier is registered, and Plan-016/Spec-016's landed 2026-08-11 (PR #321, NS-56), the channel-audience + `direct`-channels leg — carrying the Spec-006 channel-lifecycle payload growth (the `channel.created` kind + member-pair mirror, the D-016-22 origin-ordering keys, and the run-interrupt trigger-union member) (Spec-006 flip-and-restored `approved` in the same swap), the D-016-22 channel-directory publication producer, and the D-016-23 Plan-017 provider legs — restoring both, re-checking the plan's box, and minting one born-unchecked carrier box that held Plan-016 T2.14, and Plan-002/Spec-002's landed 2026-08-11 (PR #322, NS-57), the typing-presence + `ChannelList` `direct`-filter leg — carrying the run-keyed `activity.runs` contract, the `invite.preview` + invite wire-error contract (BL-133 exit criteria (a)+(b); Spec-021 flip-and-restored `approved` in the same swap for its two anonymous invite registry rows, and Spec-016 flip-and-restored `approved` in the same swap for the channel-directory fold redesign + publication-shape growth its Codex round 1 drove — the per-origin candidate-retention fold and the wire-carried `lifecycleEventKind`), and the channel-directory ingest carrier (the `session_channel_directory` table + `channel.directoryPublish` mutation under I-002-7 / CP-002-10 — Postgres census 24 → 25, BL-149 completed, Plan-016's carrier box checked and T2.14's hold released) — restoring both and re-checking the plan's box, and Plan-008/Spec-008's landed 2026-08-11 (PR #323, NS-58), the peer-history-backfill + per-channel-recipient-scoping leg — carrying the two mechanism legs its paired-spec box pinned: the Spec-006 received-row provenance amendment (the `session_events.received_from_node_id` marker column and the conditional `receivedFromNodeId` canonical member; Spec-006 and Plan-006 flip-and-restored `approved` in the same swap) and the admission-time signing-key registration decoupling (the never-shipped attachment-delivery machinery deleted doc-only — local SQLite census 56 → 55), plus the R4 backfill task T-008r-4-14 with CP-008-13 and the audit of the PR #322-routed invite-leg growths — restoring both and re-checking the plan's boxes, and Plan-003/Spec-003's landed 2026-08-12 (PR #327, NS-60), the dual-scope leg pairing that reconciliation growth with the BL-141 caller-authorization growth under the campaign's Dual-flip gate — carrying [ADR-025](docs/decisions/025-runtime-node-control-plane-caller-authorization.md) `accepted` (the runtime-node control-plane caller-authorization model: Option A, transaction-interior row-locked ownership-and-membership predicate, Reading 1's uniform `runtimenode.permission_denied` 403), Plan-003 tasks T3.10–T3.12 with I-003-3 amended and I-003-6 new, a **zero-net-line** Spec-003 amendment (174 lines before and after, so none of Plan-003's `Spec-003 line NNN` cites moved), and the Plan-008 I-008-4 gated-endpoint leg that plan's own PR #323 vehicle did not carry (Plan-008 flip-and-restored `approved` in the same swap) — restoring both, re-checking the plan's boxes, and closing the 2026-08-03 reconciliation cohort, whose six pairs are now all restored — Plan-010's 2026-08-09 sparse-root capture-closure amendment (PR #307) likewise flipped it and Spec-010 `review`, re-opening its Preconditions boxes scoped to the amendment's growth (new Phase 6 / T6.1 / invariant I-010-24) — its restoring targeted readiness-audit delta landed the same day (PR #308, NS-51), restoring both `approved` and re-checking the boxes, and the 2026-08-10 boundary-obstruction refusal naming amendment then re-flipped both `review` again — naming in I-010-24 and the same capture bullet the typed pre-mutation refusal, the kind-scoped delete-exemption widths, and the byte-exact boundary subtraction that Phase 6's merged code (PR #309) already ships — re-opening the same two boxes, with its own restoring targeted readiness-audit delta landing the same day (PR #313, §6 node NS-53), restoring both `approved` and re-checking the boxes, and the 2026-08-17 Spec-009/Plan-009 common-directory repo-identity amendment (PR #340, §6 node NS-63 — its restoring targeted readiness-audit delta riding the same diff, the in-swap flip-and-restore shape) then corrected Spec-010's branch-mode execution-root sentence and registered the CP-009-9 / CP-010-14 consent pair, flip-and-restoring Spec-010 and Plan-010 `approved` a third time in the same swap (the PR #321/#323 neighbor-swap shape; both boxes Re-opened and Delivered in-diff, no census move), and the 2026-08-17 carried-findings adjudication amendment (§6 node NS-65 — its restoring targeted readiness-audit delta likewise riding the same diff) then adjudicated the eleven PR #340 round-3/4 findings into normative contract — the `identity_mismatch` mount-health member (I-009-17 + T-009-2B-5), the admitted-root trust-envelope defining rule with working-tree-boundary containment, identity-keyed active-mount dedup, branch-arm `boundRoot` revalidation, and the CP-009-10 / CP-010-15 turn-snapshot execution-root normalization consent pair among them — flip-and-restoring Spec-009/Plan-009 `approved` again and Spec-010/Plan-010 `approved` a fourth time in the same swap (no census move; Plan-009's carried-findings carrier box checked, so Phase 2B's adjudication gate passes), and the 2026-08-17 cost display-consistency amendment (§6 node NS-66 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-016/Plan-016 `approved` again for the `Spec-016 §Cost Figure Display Consistency` growth — every surfaced cost figure supplied by the committed-spend fold enforcement uses, with the four `OrchestrationBudgetState` decomposition members served from the accountant's single accessor on both budget replies (I-016-24 / CP-016-16 / D-016-24; no census move), and the 2026-08-17 rewind-hardening amendment (§6 node NS-67 — its restoring targeted readiness-audit delta likewise riding the same diff; both legs user-ratified 2026-08-16) then flip-and-restored Spec-004/Plan-004 `approved` again for the two rollback legs — compaction-boundary-aware rewind targeting (fail-closed, pre-dispatch, classified against the target run's newest provider-side `usage.context_compacted` boundary, liftable only by a driver declaring re-verified boundary-crossing rewind semantics) and the atomic edit-and-resend composite (an optional `replacementSend` leg on the existing `rollback` intervention, the V1 control set restated unwidened; I-004-20 / I-004-21, one born-unchecked wire-arm carrier box holding T1.2 / T1.3 / T3.17 / T4.7 until the api-payload §Plan-004 registration lands), with two erratum-class Spec-013 consumer narrowings moving no Status (no census move) — that PR's round-2 fold then adding the run-bound delivery redesign (`queue_items.target_run_id`, drained by T3.5), the fourth structural refusal guard (non-resumable rootless), the two-point boundary classification (settlement reclassification naming `boundary-diverged` + the `run.compaction_boundary_diverged` resume backstop), the schema-optional, producer-obligated `resendDisposition` member, the decrypt-and-compare idempotency rule, the Spec-022 PII-lifecycle reciprocals for `interventions.pii_payload` (map row, export, 90-day NULL-out sweep), a second born-unchecked box (the Plan-004-side admitting-principal producer leg), and the Spec-012 §Required Behavior replacement-send principal rebinding, Spec-012/Plan-012 flip-and-restored `approved` in the same swap — and the last campaign-flipped pair that was awaiting its restoring re-audit (Spec-013/Plan-013 per B9's CP-004-13 consumer leg) was restored `approved` 2026-08-10 by the Tier-8 audit (NS-20, PR #318), which also promoted Plan-017 `review → approved` and flip-and-restored Spec-017 + Plan-023 `approved` in the same swap — Plan-017 and Spec-017 re-flipped `review` 2026-08-11 by the chat-start amendment (ADR-027, landing `proposed`) and restored `approved` the same day by their chat-start targeted readiness-audit delta (PR #320, NS-55), then flip-and-restored `approved` again by the 2026-08-16 workflow-hardening amendment landing 2026-08-17 with its in-swap targeted readiness-audit delta (§6 node NS-68 — the park-and-resume surface: SA-40 typed provider-limit pacing with durable auto-resume, SA-41 audited frozen-definition repair, SA-42 park integrity and cancellability, SA-43 the always-on engine event record; six tasks and I-017-19…I-017-24 minted, with two born-unchecked carrier boxes holding T2.5/T2.6 on the Spec-005/Plan-005 typed-signal delta and T5.13 on the Plan-020 diagnostic bucket; no census move), and the 2026-08-17 normalized-root transport-closure amendment (§6 node NS-69 — its restoring targeted readiness-audit delta likewise riding the same diff) then closed the CP-010-12 ⇄ CP-010-15 transport gap — `run_execution_contexts` gains `checkout_root` (TEXT NOT NULL), captured per run by the Plan-010 T3.2 gate from the workspace's recorded normalized checkout root and supplied as CP-010-12's **seventh** caller-resolved input (the restore leg consumes it untouched; the shipped `0004` migration is not amended, the column landing by a Phase-3 next-ordinal table-rebuild migration — the documented lang_altertable shape, corrected from `ADD COLUMN` at the PR's round-1 fold) — flip-and-restoring Spec-010/Plan-010 `approved` a fifth time, Plan-009 `approved` again (Spec-009 untouched), and Plan-004 `approved` again for its caller-side seven-input restatement (Spec-004 joining the flip at the PR's round-2 fold — the vanished-checkout rootless convergence, its third flip cycle; Plan-004's born-unchecked checkout-root box holds T3.13 until Plan-009 Phase 2B and Plan-010 Phase 3 both merge — the rollback cluster re-homed to a Plan-004 Phase-3B explicit-label supplement at that same round-1 fold, breaking the CP-010-9 reciprocal deadlock and gating Plan-009 Phase 2B on Plan-010 Phase 3; no census move) (the README census is the authoritative list) — and audit clearance only, not code-readiness: plan-task code (lane 1 per [CONTRIBUTING.md](CONTRIBUTING.md) §How Code Lands) still dispatches in tier order and on each plan's §Preconditions — enhancement and tooling lanes are exempt (execution is underway through Tier 4). **The tier-audit chain is closed** — the Tier-9 audit (NS-21, the chain's final entry) landed 2026-08-15 via PR #331, auditing Plan-026 + Plan-027 with both staying `approved`; Plan-028's targeted readiness audit cleared 2026-08-12 (§6 node NS-61), promoting it `draft → review`, and its `review → approved` promotion landed 2026-08-14 — the Plan-028 doc gates are closed (campaign B18). No audit work remains on the DAG and no doc gate is outstanding: the last two Status promotions both landed 2026-08-15 — Plan-025's via the §Status Promotion Gate (PR #333) and Plan-018's via the NS-62 promotion pass (PR #334, which carries the CP-018-12 / CP-018-13 targeted readiness-audit delta, authors Plan-018 Phase 5, and adds the `participant_identity_keys` table — Postgres census 25 → 26); per the README census the plan-promotion queue is empty.
- **Plan status promotion** — a code-execution plan ships its first PR only after it — and every cross-referenced spec, ADR, and plan — has completed the status promotion its [Documentation Corpus](#documentation-corpus) row requires, and every blocking backlog item is `completed` (or deferred with a named gate), per [AGENTS.md §Doc-First Discipline](AGENTS.md#doc-first-discipline). This is a separate gate, not satisfied by audit clearance or tier order alone: a plan that is audit-cleared and tier-eligible but not yet promoted stays blocked. The [README](README.md) census lists the plans not yet promoted.

## Cross-Tool Conventions

Cross-tool conventions for AI agents in this repo (Claude, Codex, Cursor, Aider) live in [`AGENTS.md`](AGENTS.md). It owns the citation standard, the transient research-artifact pattern under `.agents/tmp/research/<topic>/<axis>.md`, parallel-subagent dispatch rules, and doc-first ordering.

Branch naming, commit format, and PR workflow conventions live in [`CONTRIBUTING.md`](CONTRIBUTING.md). It owns the GitFlow-lite branch model (feature branches off `develop`; squash-merge into `develop`; `develop` → `main` only at release), [Conventional Branch](https://conventional-branch.github.io/) 2-segment shape (`<type>/<topic>`), [Conventional Commits 1.0](https://www.conventionalcommits.org/en/v1.0.0/) message format with package-noun scope, footer-trailer conventions (`Refs: ADR-NNN, BL-NNN, Plan-NNN` and `Co-Authored-By:`), and squash-merge workflow per [ADR-023](docs/decisions/023-v1-ci-cd-and-release-automation.md).

Read `AGENTS.md` on demand before:

- Authoring a new spec, ADR, plan, or architecture doc that requires primary-source citations
- Dispatching parallel research subagents
- Committing any doc whose content was drafted with subagent research artifacts (the surface-forward-then-delete step)

**Anti-pattern**: never cite `.agents/tmp/...` paths from committed docs. The directory is gitignored and manually pruned — no hook deletes it; surface citations forward into the consuming doc's References section, then delete the research file yourself before the consuming-doc commit lands (per AGENTS.md "Surface-Forward-Then-Delete").

## Worktrees

Git worktrees live under `.worktrees/<name>/` at the repo root. The harness enforces this via `WorktreeCreate` and `PreToolUse` hooks in `.claude/settings.json` — `git worktree add` or `git worktree move` to any path outside `.worktrees/` is denied. Use `git worktree add .worktrees/<name> -b worktree-<name>` when creating manually to match the harness branch-naming convention.

**Removal discipline.** Deleting a worktree that is a live process's working directory breaks every later command spawn in the occupying session (`ENOENT posix_spawn '/bin/sh'` — 2026-07-07 incident). Removals (`git worktree remove`, recursive `rm` into `.worktrees/`, harness-initiated removals via `worktree.sh`) are occupancy-checked by `command-guard.py`: an occupied removal is denied with the occupant list. Occupancy can be transient — retry once; otherwise exit the occupying session or kill the listed PIDs; the deliberate override is prefixing the exact command with `WORKTREE_REMOVE_ALLOW_OCCUPIED=1`. Bulk cleanups remove only worktrees you created, after checking `python3 .claude/hooks/command-guard.py --occupancy <path>` (empty output = unoccupied). Honest limit: a session whose _tracked_ Bash cwd is inside a worktree with no live process there at check time is invisible to the guard — the only-remove-what-you-created rule is the mitigation. Active on macOS/Linux/WSL2; inert-but-safe on native Windows (the OS itself locks in-use directories).

## Documentation Corpus

| Tree | Purpose | Template | Status Lifecycle |
| --- | --- | --- | --- |
| `docs/specs/NNN-kebab.md` | Feature specifications (the design contract) | `docs/specs/000-spec-template.md` | `draft` → `review` → `approved` (or `superseded`) |
| `docs/plans/NNN-kebab.md` | Implementation plans (executable build steps) | `docs/plans/000-plan-template.md` | `draft` → `review` → `approved` → `completed` |
| `docs/decisions/NNN-kebab.md` | ADRs (decisions with antithesis + synthesis) | `docs/decisions/000-adr-template.md` | `proposed` → `accepted` (or `deprecated` / `superseded by ADR-NNN`) |
| `docs/domain/` | Domain models, state machines, glossary | `docs/domain/template.md` | canonical when merged |
| `docs/architecture/` | Schemas, contracts, system context, deployment, security | `docs/architecture/template.md` | canonical when merged |
| `docs/operations/` | Runbooks, on-call routing, SLOs | `docs/operations/template.md` | canonical when merged |
| `docs/backlog.md` | Active work items (`BL-NNN`) | inline template in file header | `todo` / `in_progress` / `blocked` / `completed` |

Non-governance docs sit alongside the corpus and are not subject to the status lifecycle above: [`docs/vision.md`](docs/vision.md) (long-form product vision) and [`docs/reference/`](docs/reference/) (excerpted upstream materials).

ADRs are classified `Type 1` (two-way door, reversible — skip [T2] sections) or `Type 2` (one-way door, hard to reverse — complete every section).

## When Writing Documents

- **Copy the template.** New spec / plan / ADR? Start from `000-{type}-template.md`. Number sequentially within the tree (next free `NNN`).
- **Check the ownership map first.** Before adding a column, file, or directory that another plan might own, consult [`docs/architecture/cross-plan-dependencies.md`](docs/architecture/cross-plan-dependencies.md). The owning plan `CREATE`s; dependent plans `EXTEND`.
- **Cross-link aggressively.** Every spec names its plan; every plan names its spec and Required ADRs; every backlog item names the docs that govern it.
- **Status promotion is load-bearing.** Do not flip `review` → `approved` without addressing review notes — downstream plans/specs treat the prior state as stable. ADR moves to `accepted` only after antithesis is steel-manned and synthesis is recorded.
- **Backlog discipline.** `BL-NNN` items must include References, Summary, and Exit Criteria. Move completed items to `docs/archive/backlog-archive.md` after the canonical docs are updated. Do not let `backlog.md` accumulate historical entries — rewrite or remove stale items.
