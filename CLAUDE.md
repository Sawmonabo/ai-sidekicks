# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Project Is

**AI Sidekicks** is a desktop runtime where humans and multiple AI agents share live sessions, co-edit code through proper git flow, and collaborate in real time. The first-class primitive is the **session**, not the agent ([ADR-001](docs/decisions/001-session-is-the-primary-domain-object.md)). A session contains participants, agents, runs, channels, repo mounts, approvals, artifacts, and a unified event timeline. People and machines are both first-class participants.

Three layers:

- **Local Runtime Daemon** — machine-local execution authority. Owns provider processes (`claude-driver`, `codex-driver`), git worktrees, terminal sessions, tool execution, SQLite persistence (59 tables). Worktree-first execution mode ([ADR-006](docs/decisions/006-worktree-first-execution-mode.md)).
- **Collaboration Control Plane** — hosted or self-hosted; auth (PASETO v4 + WebAuthn + DPoP, [ADR-010](docs/decisions/010-paseto-webauthn-mls-auth.md)), invites, presence (Yjs Awareness), E2E-encrypted relay (X25519 + XChaCha20-Poly1305 in V1, MLS RFC 9420 in V2), shared metadata (Postgres, 28 tables).
- **Clients** — CLI (`sidekicks`, first delivery track) and Electron desktop shell with React/Vite renderer, both over a typed SDK + JSON-RPC IPC ([ADR-009](docs/decisions/009-json-rpc-ipc-wire-format.md)).

Stack: TypeScript across daemon/CLI/desktop/contracts; XState v5 state machines; tRPC v11 control-plane API ([ADR-014](docs/decisions/014-trpc-control-plane-api.md)); Zod validation; Cedar policy engine for approvals ([ADR-012](docs/decisions/012-cedar-approval-policy-engine.md)); OpenTelemetry; Rust PTY sidecar on Windows ([ADR-019](docs/decisions/019-windows-v1-tier-and-pty-sidecar.md)). Apache-2.0 ([ADR-020](docs/decisions/020-v1-deployment-model-and-oss-license.md)).

V1 ships 23 features ([ADR-015](docs/decisions/015-v1-feature-scope-definition.md), amended 2026-07-02 and 2026-07-08; feature-14's offline-availability guarantee additionally threat-model-scoped 2026-08-08 and time-bounded to a live relay pin 2026-08-26 per that ADR's §Decision Log — neither an amendment, no count moves) across 30 implementation plans in 9 dependency tiers — feature #18 (MCP governance) is governed by [Spec-028](docs/specs/028-mcp-server-configuration-and-governance.md) + [Plan-028](docs/plans/028-mcp-server-configuration-and-governance.md), landed 2026-07-22 via the capability-enhancement campaign's B18 bundle as a Tier-7 leaf (`draft` at landing; promoted `review` 2026-08-12 by its targeted readiness audit, §6 node NS-61, and `approved` 2026-08-14 by its §Rollout Order step-2 promotion PR), with MCP-governance code dispatching in tier order per Plan-028's §Preconditions. Features #19–#23's governing spec amendments are in-tree via the campaign's B1/B2/B3/B6/B20 bundles and re-promoted `approved` through the W1.5 batch gate (2026-07-18; Spec-004 (#19), Spec-012 (#21/#22), and Spec-016 (#20/#23) were re-flipped `review` 2026-08-03 by the V1 product-vision reconciliation amendment bundle — Spec-016 having already been briefly re-flipped `review` 2026-07-21 by B15's §Stop Conditions amendment and restored `approved` the same day by Plan-016's W2.5 targeted re-audit — each restoring `approved` via its queued targeted readiness-audit delta: Spec-004's landed 2026-08-08 (PR #299 / NS-49), restoring it and Plan-004 `approved`, and Spec-012's landed 2026-08-10 (PR #317 / NS-54), restoring it and Plan-012 `approved` and closing #21's and #22's spec layer, and Spec-016's landed 2026-08-11 (PR #321 / NS-56), restoring it and Plan-016 `approved` and closing #20's and #23's — the spec layer is closed across all of #19–#23 (Spec-005 remains `approved`; Spec-006 was flip-and-restored `approved` in the same swap for its channel-lifecycle payload growth (the `channel.created` kind + member-pair mirror, the D-016-22 origin-ordering keys, and the run-interrupt trigger-union member))), so rollback / goals / callback-tool / execution-posture / realtime implementation dispatches with its owning plans' code phases in tier order (the campaign's W2 plan-task bundles are all in-tree; the campaign plan closed `completed` 2026-08-15) — and rollback (#19) additionally gated on the B21→B23 turn-snapshot file-restore leg (Plan-010) before Plan-004 rollback dispatch per the [build-order preamble](docs/architecture/cross-plan-dependencies.md) (met 2026-08-09: the B23 leg shipped via PR #303, leaving Plan-004's rollback dispatch — the Phase-3B cluster since the NS-69 round-1 re-home — on tier order plus its precondition legs), with its superseded-turn timeline rendering riding the Spec-013/Plan-013 CP-004-13 consumer leg (flipped `review` 2026-07-20; restored `approved` 2026-08-10 by the Tier-8 plan-readiness audit, NS-20). The feature list and tier graph live in [`README.md`](README.md); the build-order + shared-resource ownership map lives in [`docs/architecture/cross-plan-dependencies.md`](docs/architecture/cross-plan-dependencies.md).

## Current State: Tier 2-4 Code Execution; Plan-Readiness Audit Chain Complete (Tiers 2-9)

Code execution started 2026-04-26 with the V1 monorepo scaffold (PR #6) — the doc-first gate cleared when [ADR-023](docs/decisions/023-v1-ci-cd-and-release-automation.md) (V1 CI/CD, pre-commit hooks, release automation) was accepted on the same date, closing [BL-100](docs/archive/backlog-archive.md). Feature branches cut off `develop` and squash-merge back per the [GitFlow-lite branch-model amendment](docs/decisions/023-v1-ci-cd-and-release-automation.md#decision-log).

All five [Plan-001](docs/plans/001-shared-session-core.md) phases have shipped (Phase 5 across Lanes A–D):

| GitHub PR | Phase | Package |
| --- | --- | --- |
| #6 | Phase 1 — Workspace Bootstrap | repo bootstrap (pnpm + Turbo + Vitest + ESLint per [ADR-022](docs/decisions/022-v1-toolchain-selection.md)) |
| #8 | Phase 2 — Contracts | `packages/contracts` (session / event / error payload schemas) |
| #9 | Phase 3 — Daemon Migration + Projection | `packages/runtime-daemon` (migration, projector, append/replay) |
| #10 | Phase 4 — Control Plane Directory | `packages/control-plane` (session directory service: create/read/join) |
| #30/#36/#38 (A), #48 (B), #77 (C), #83 (D), #87 (completion) | Phase 5 — Client SDK + Desktop Bootstrap (Lanes A–D) | `packages/client-sdk` + `apps/desktop` (session bootstrap, renderer wiring, sidecar lifecycle) |

`package.json` is real (`pnpm@10.33.2`, Node `>=22.14.0` — moved from `>=22.12.0` by Plan-023 T-023p-1B-4, the `better-sqlite3` 13.x pin whose Node-API-10 prebuild needs it; ADR-022 §Decision Log 2026-09-01). Use the wired scripts: `pnpm install`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` (Turbo-driven), `pnpm format` / `pnpm format:check`. **Do not invoke `npm`** — the engines field requires pnpm. Pre-commit hooks (lefthook + lint-staged + commitlint + gitleaks) install via `pnpm prepare`. The unit of work is now mixed: `.md` files under `docs/` for governance and TypeScript under `packages/` + `apps/` for code phases. Doc-first ordering still holds for plan-task shipment — a lane-1 PR cites the plan / spec / ADR(s) it implements; enhancement and tooling PRs take the lighter lanes per [CONTRIBUTING.md](CONTRIBUTING.md) §How Code Lands: Work Classification.

**Active gates blocking the next code PRs:**

- **Plan-024 Phases 4-5** (CI cross-compile + signing; measurement substrate) are procurement-blocked on [BL-108](docs/backlog.md) — tracked as §6 nodes NS-09 + NS-10, the only hard-blocked code lanes on the dependency DAG.
- **Plan-readiness audit** ([runbook](docs/operations/plan-implementation-readiness-audit-runbook.md), methodology PR #14, Tier 1 pilot PR #15) gates code from Tiers 2-9 — each tier's plans must clear the audit before that tier's code PRs land. The plan-template Precondition checkbox blocks `draft → review` for new plans; pre-audit `approved` plans need a retroactive audit pass first. **Tiers 2-9 audits are complete** (NS-14 + NS-15..NS-21; the Tier-9 audit landed via PR #331, closing the tier-PR chain), so those tiers' `approved` plans have cleared the audit gate — with reopened exceptions: Plan-014's 2026-07-08 relay scope growth re-opened its Preconditions checkbox, restored `approved` 2026-08-12 by the relay-scope targeted readiness-audit delta (§6 node NS-59), which re-checked both boxes, minted one born-unchecked scoped box at its Codex fold (the Task-8 fetch-selector daemon carrier — the NS-54 carrier-box class), and cleared the audit and Status gates on Tasks 7–10 — their dispatch now rides Tier-7 order (Plan-008-remainder and Plan-018 at Tier 5, Plan-021 at Tier 6; the Tier-7-wide phase-decomposition obligation that delta escalated was discharged 2026-08-15 — Plans 011/014/015 now carry §Implementation Phase Sequence); Plan-014 and Spec-014 were then flipped and restored `approved` again 2026-08-16 by the four-leg artifact-lifecycle amendment and its in-swap targeted readiness-audit delta (§6 node NS-64), which re-specified the Task-8 fetch selector as the caller's artifact-encryption key-thumbprint set — **checking the carrier box by making a carrier unnecessary** rather than registering one, so Task 8's mint leg carries no hold and the Plan-003 edge narrows to corroboration-only — and added local Tasks 11–13 (the OWASP ingest-validation pipeline, `ArtifactDelete` + derived CAS refcount, and the typed attachment-reference contract) plus an additive unique index on `artifact_relay_recipients` that moves no table census; Tasks 1–6 were covered throughout — and flipped and restored `approved` a third time 2026-08-17 by the ingest-protocol hardening amendment and its in-swap targeted readiness-audit delta (§6 node NS-70): five accepted findings (Codex PR #341 round 5) against the plan's own round-4 ingest stream protocol — the completion record making `AttachmentIngestComplete` retry-idempotent, both ingest byte ledgers serialized reserve-then-install, per-`ingestId` single-flight with the reaper's eviction under the same exclusion, the `pinned` → `expired` manifest write-back on either unobtainable-payload fetch refusal (grounding the unresolved-marker `expired` cause), and the Init-refusal zero-state criterion made runnable through admission probes — no interface, wire member, error code, table, or column minted, so no census moves, with BL-152 minted for the TTL-sweep blob-row disposition the amendment flags without settling — **settled and archived 2026-08-26** by the relay TTL-sweep disposition amendment (§6 node NS-82), which flip-and-restored Spec-014/Plan-014 `approved` a fifth time for the retained payload-free tombstone at `state = 'expired'`, its shred-then-reclaim-then-confirm ordering (the publisher identity held to the reclaim confirmation so still-stored bytes stay attributable), the `relay_tombstone_grace` purge, the read-time liveness predicate, the re-publish fence over an in-flight reclaim, and the advisory-lock exclusion that keeps two sweep passes off one digest, minting I-014-15 plus the additive nullable `artifact_relay_blobs.bytes_reclaimed_at` and one partial index, neither of which moves a census, Plan-006's 2026-07-29 CP-006-7 T4.10 signing-key-roster growth likewise re-opened its checkbox and flipped it `review`, restored `approved` 2026-08-01 by the T4.10 targeted readiness-audit delta (PR #278), re-flipped `review` 2026-08-02 by the T3.1 ingest-halt seam amendment (PR #281), and restored `approved` the same day by the T3.1-seam targeted readiness-audit delta (PR #282, NS-43) — lane-1 dispatch (the Phase-1 remainder and Phase 3 included) rides tier order and the plan's §Preconditions — Plan-024's 2026-08-02 CP-024-5 credential-seam registration (PR #279) likewise re-opened its checkbox scoped to Phase 3B and flipped it `review`, restored `approved` 2026-08-03 by the CP-024-5 targeted readiness-audit delta (PR #283, NS-44) — Phase 3B dispatch now rides its Tier-4 remainder row, the plan's §Preconditions, and its `plan_phase` gate on Plan-006 Phase 3 — the 2026-08-03 V1 product-vision reconciliation (PR #284) flipped Plans 002 / 003 / 004 / 008 / 012 / 016 and their paired specs `review`, re-opening each plan's Preconditions checkbox scoped to its amendment growth (cross-user intervention authority, typing presence + the `ChannelList` non-disclosure filter, peer history backfill + per-channel relay recipient scoping, and channel audience + `direct` channels among the legs), each restoring `approved` via its own queued targeted readiness-audit delta — Plan-004/Spec-004's landed 2026-08-08 (PR #299, NS-49), the cross-user intervention authority leg, restoring both and re-checking the plan's box, and Plan-012/Spec-012's landed 2026-08-10 (PR #317, NS-54), that leg's approvals-side second half — the remembered-grant actor axis and the turn-scoped effective principal — restoring both, re-checking the plan's box, and minting one born-unchecked scoped box that holds Plan-012 T2.2 / T2.7 until an admitting-principal carrier is registered, and Plan-016/Spec-016's landed 2026-08-11 (PR #321, NS-56), the channel-audience + `direct`-channels leg — carrying the Spec-006 channel-lifecycle payload growth (the `channel.created` kind + member-pair mirror, the D-016-22 origin-ordering keys, and the run-interrupt trigger-union member) (Spec-006 flip-and-restored `approved` in the same swap), the D-016-22 channel-directory publication producer, and the D-016-23 Plan-017 provider legs — restoring both, re-checking the plan's box, and minting one born-unchecked carrier box that held Plan-016 T2.14, and Plan-002/Spec-002's landed 2026-08-11 (PR #322, NS-57), the typing-presence + `ChannelList` `direct`-filter leg — carrying the run-keyed `activity.runs` contract, the `invite.preview` + invite wire-error contract (BL-133 exit criteria (a)+(b); Spec-021 flip-and-restored `approved` in the same swap for its two anonymous invite registry rows, and Spec-016 flip-and-restored `approved` in the same swap for the channel-directory fold redesign + publication-shape growth its Codex round 1 drove — the per-origin candidate-retention fold and the wire-carried `lifecycleEventKind`), and the channel-directory ingest carrier (the `session_channel_directory` table + `channel.directoryPublish` mutation under I-002-7 / CP-002-10 — Postgres census 24 → 25, BL-149 completed, Plan-016's carrier box checked and T2.14's hold released) — restoring both and re-checking the plan's box, and Plan-008/Spec-008's landed 2026-08-11 (PR #323, NS-58), the peer-history-backfill + per-channel-recipient-scoping leg — carrying the two mechanism legs its paired-spec box pinned: the Spec-006 received-row provenance amendment (the `session_events.received_from_node_id` marker column and the conditional `receivedFromNodeId` canonical member; Spec-006 and Plan-006 flip-and-restored `approved` in the same swap) and the admission-time signing-key registration decoupling (the never-shipped attachment-delivery machinery deleted doc-only — local SQLite census 56 → 55), plus the R4 backfill task T-008r-4-14 with CP-008-13 and the audit of the PR #322-routed invite-leg growths — restoring both and re-checking the plan's boxes, and Plan-003/Spec-003's landed 2026-08-12 (PR #327, NS-60), the dual-scope leg pairing that reconciliation growth with the BL-141 caller-authorization growth under the campaign's Dual-flip gate — carrying [ADR-025](docs/decisions/025-runtime-node-control-plane-caller-authorization.md) `accepted` (the runtime-node control-plane caller-authorization model: Option A, transaction-interior row-locked ownership-and-membership predicate, Reading 1's uniform `runtimenode.permission_denied` 403), Plan-003 tasks T3.10–T3.12 with I-003-3 amended and I-003-6 new, a **zero-net-line** Spec-003 amendment (174 lines before and after, so none of Plan-003's `Spec-003 line NNN` cites moved), and the Plan-008 I-008-4 gated-endpoint leg that plan's own PR #323 vehicle did not carry (Plan-008 flip-and-restored `approved` in the same swap) — restoring both, re-checking the plan's boxes, and closing the 2026-08-03 reconciliation cohort, whose six pairs are now all restored — Plan-010's 2026-08-09 sparse-root capture-closure amendment (PR #307) likewise flipped it and Spec-010 `review`, re-opening its Preconditions boxes scoped to the amendment's growth (new Phase 6 / T6.1 / invariant I-010-24) — its restoring targeted readiness-audit delta landed the same day (PR #308, NS-51), restoring both `approved` and re-checking the boxes, and the 2026-08-10 boundary-obstruction refusal naming amendment then re-flipped both `review` again — naming in I-010-24 and the same capture bullet the typed pre-mutation refusal, the kind-scoped delete-exemption widths, and the byte-exact boundary subtraction that Phase 6's merged code (PR #309) already ships — re-opening the same two boxes, with its own restoring targeted readiness-audit delta landing the same day (PR #313, §6 node NS-53), restoring both `approved` and re-checking the boxes, and the 2026-08-17 Spec-009/Plan-009 common-directory repo-identity amendment (PR #340, §6 node NS-63 — its restoring targeted readiness-audit delta riding the same diff, the in-swap flip-and-restore shape) then corrected Spec-010's branch-mode execution-root sentence and registered the CP-009-9 / CP-010-14 consent pair, flip-and-restoring Spec-010 and Plan-010 `approved` a third time in the same swap (the PR #321/#323 neighbor-swap shape; both boxes Re-opened and Delivered in-diff, no census move), and the 2026-08-17 carried-findings adjudication amendment (§6 node NS-65 — its restoring targeted readiness-audit delta likewise riding the same diff) then adjudicated the eleven PR #340 round-3/4 findings into normative contract — the `identity_mismatch` mount-health member (I-009-17 + T-009-2B-5), the admitted-root trust-envelope defining rule with working-tree-boundary containment, identity-keyed active-mount dedup, branch-arm `boundRoot` revalidation, and the CP-009-10 / CP-010-15 turn-snapshot execution-root normalization consent pair among them — flip-and-restoring Spec-009/Plan-009 `approved` again and Spec-010/Plan-010 `approved` a fourth time in the same swap (no census move; Plan-009's carried-findings carrier box checked, so Phase 2B's adjudication gate passes), and the 2026-08-17 cost display-consistency amendment (§6 node NS-66 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-016/Plan-016 `approved` again for the `Spec-016 §Cost Figure Display Consistency` growth — every surfaced cost figure supplied by the committed-spend fold enforcement uses, with the four `OrchestrationBudgetState` decomposition members served from the accountant's single accessor on both budget replies (I-016-24 / CP-016-16 / D-016-24; no census move), and the 2026-08-17 rewind-hardening amendment (§6 node NS-67 — its restoring targeted readiness-audit delta likewise riding the same diff; both legs user-ratified 2026-08-16) then flip-and-restored Spec-004/Plan-004 `approved` again for the two rollback legs — compaction-boundary-aware rewind targeting (fail-closed, pre-dispatch, classified against the target run's newest provider-side `usage.context_compacted` boundary, liftable only by a driver declaring re-verified boundary-crossing rewind semantics) and the atomic edit-and-resend composite (an optional `replacementSend` leg on the existing `rollback` intervention, the V1 control set restated unwidened; I-004-20 / I-004-21, one born-unchecked wire-arm carrier box holding T1.2 / T1.3 / T3.17 / T4.7 until the api-payload §Plan-004 registration lands), with two erratum-class Spec-013 consumer narrowings moving no Status (no census move) — that PR's round-2 fold then adding the run-bound delivery redesign (`queue_items.target_run_id`, drained by T3.5), the fourth structural refusal guard (non-resumable rootless), the two-point boundary classification (settlement reclassification naming `boundary-diverged` + the `run.compaction_boundary_diverged` resume backstop), the schema-optional, producer-obligated `resendDisposition` member, the decrypt-and-compare idempotency rule, the Spec-022 PII-lifecycle reciprocals for `interventions.pii_payload` (map row, export, 90-day NULL-out sweep), a second born-unchecked box (the Plan-004-side admitting-principal producer leg), and the Spec-012 §Required Behavior replacement-send principal rebinding, Spec-012/Plan-012 flip-and-restored `approved` in the same swap — and the last campaign-flipped pair that was awaiting its restoring re-audit (Spec-013/Plan-013 per B9's CP-004-13 consumer leg) was restored `approved` 2026-08-10 by the Tier-8 audit (NS-20, PR #318), which also promoted Plan-017 `review → approved` and flip-and-restored Spec-017 + Plan-023 `approved` in the same swap — Plan-017 and Spec-017 re-flipped `review` 2026-08-11 by the chat-start amendment (ADR-027, landing `proposed`) and restored `approved` the same day by their chat-start targeted readiness-audit delta (PR #320, NS-55), then flip-and-restored `approved` again by the 2026-08-16 workflow-hardening amendment landing 2026-08-17 with its in-swap targeted readiness-audit delta (§6 node NS-68 — the park-and-resume surface: SA-40 typed provider-limit pacing with durable auto-resume, SA-41 audited frozen-definition repair, SA-42 park integrity and cancellability, SA-43 the always-on engine event record; six tasks and I-017-19…I-017-24 minted, with two born-unchecked carrier boxes holding T2.5/T2.6 on the Spec-005/Plan-005 typed-signal delta and T5.13 on the Plan-020 diagnostic bucket; no census move), and the 2026-08-17 normalized-root transport-closure amendment (§6 node NS-69 — its restoring targeted readiness-audit delta likewise riding the same diff) then closed the CP-010-12 ⇄ CP-010-15 transport gap — `run_execution_contexts` gains `checkout_root` (TEXT NOT NULL), captured per run by the Plan-010 T3.2 gate from the workspace's recorded normalized checkout root and supplied as CP-010-12's **seventh** caller-resolved input (the restore leg consumes it untouched; the shipped `0004` migration is not amended, the column landing by a Phase-3 next-ordinal table-rebuild migration — the documented `lang_altertable` shape, corrected from `ADD COLUMN` at the PR's round-1 fold) — flip-and-restoring Spec-010/Plan-010 `approved` a fifth time, Plan-009 `approved` again (Spec-009 untouched), and Plan-004 `approved` again for its caller-side seven-input restatement (Spec-004 joining the flip at the PR's round-2 fold — the vanished-checkout rootless convergence, its third flip cycle; Plan-004's born-unchecked checkout-root box holds T3.13 until Plan-009 Phase 2B and Plan-010 Phase 3 both merge — the rollback cluster re-homed to a Plan-004 Phase-3B explicit-label supplement at that same round-1 fold, breaking the CP-010-9 reciprocal deadlock and gating Plan-009 Phase 2B on Plan-010 Phase 3; no census move), and the 2026-08-18 admitting-principal carrier and queue-payload PII envelope amendment (§6 node NS-71 — its restoring targeted readiness-audit delta likewise riding the same diff) then registered the carrier both born-unchecked boxes had named as their vehicle since NS-54 (the Plan-012 consumer side) and NS-67's round-2 fold (the Plan-004 producer side) — `interventions` gaining the daemon-resolved, transport-authenticated `admitting_principal_id` beside an explicit `origin` discriminator whose table-level CHECK requires the principal exactly on the participant arm (and which carries no DEFAULT, so an unstamped insert fails closed at the database rather than defaulting into the system arm), `queue_items` gaining `admitting_intervention_id` plus the participant-keyed `pii_payload` / `pii_participant_id` envelope that first makes the table addressable by a per-participant erasure selector at all (queue rows stay durable and never-evented), and the steer directive text taking `interventions.pii_payload` as its encrypted home with Spec-006 event shapes deliberately unwidened (D-004-4 / I-004-22 / I-004-23 / CP-004-14 ⇄ CP-012-12 / D-012-20 / I-012-25, with a CP-016-7 doc-only reciprocal mirror on the B13 one-clause precedent) — flip-and-restoring Spec-004/Plan-004 `approved` again, Spec-012/Plan-012 `approved` again, and Spec-022 `approved` for the three queue-item PII reciprocals and the participant-reference posture (Plan-022 taking the reciprocal durable-tier inventory row on a recorded no-flip), and checking three boxes: Plan-004's admitting-principal producer box, Plan-012's consumer box (releasing T2.2 / T2.7), and Plan-017's chat-borne participant carrier box (releasing the chat-start leg and discharging CP-017-8's threefold scope on a recorded no-flip) — the class rule landing in `api-payload-contracts.md §Authenticated Principal And Authorization Model` with three named instances; a wire-vs-daemon principal disagreement reuses the existing `auth.principal_mismatch`, so no interface, wire member, error code, or table is minted and no census moves (SQLite 55 and Postgres 26 unmoved), and the 2026-08-18 park-surface and operator-controls amendment (§6 node NS-72 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-017/Plan-017 `approved` again, closing the operator-reachability residual NS-68 recorded and the park-legibility gap its per-phase columns left: `Spec-017 §Park surfacing on the read model (SA-44)` adds four additive-**optional**, **live-scoped** `PhaseState` park members — emitted iff the phase is parked at response-build time, so `parkReason` presence is the wire's park discriminator and the five-value phase-state union stays deliberately unwidened against the I-017-12 lockstep — and `Spec-017 §Operator run control (SA-45)` mints `workflow.runCancel` / `workflow.runResume` (the SA-41 re-pin riding the resume request as an optional member rather than a third operation) **together** with the `workflow.cancelled` lifecycle event under a run-scoped projector clearing rule, authorized as the named Cedar actions `Action::"workflow::cancel"` / `Action::"workflow::resume"` on the SA-39 start precedent and registered by this same swap's own Spec-012 leg — extending `Spec-012 §Implementation Notes`' named-operation-action enumeration in place, Spec-012 flip-and-restored `approved` with Plan-012 unflipped (no plan-side enumeration exists), so the NS-71-discharged CP-017-8 box stays checked and unwidened and no box holds T5.14 — with tasks T5.14/T5.15, invariants I-017-25/I-017-26, and obligation CP-017-11 naming no upstream row; **two censuses move** — the workflow wire registry eleven → thirteen and the `workflow.*` event taxonomy 23 → 24 across five unchanged categories, with three refusal codes registered so the A-017-15 owed extension holds at fifteen, while the nine-table workflow schema and the SQLite 55 / Postgres 26 censuses are unmoved; the same swap promotes ADR-026 and ADR-027 `proposed → accepted` (ADR census 24 → 26 accepted, none now `proposed`), checks Plan-017's two born-unchecked ADR boxes and releases the full-phase Gate-5 holds they carried, and completes and archives BL-151, and the 2026-08-18 desktop edit-affordance placement amendment (§6 node NS-73 — its restoring targeted readiness-audit delta likewise riding the same diff) then settled the one named residual the rewind-hardening amendment left in `Plan-004 §Risks And Blockers` — where the renderer's edit-message entry point belongs: a hover-revealed pencil affordance in the participant `user.message` timeline row's footer, opening an inline editor whose confirm action dispatches the **existing** `rollback` intervention carrying `replacementSend`, an entry point into a control the corpus already has rather than a sixth run control, so Spec-004's V1 control set stays closed and Spec-004 takes nothing normative — Plan-004 authoring the component and its eight-conjunct, **authorship-blind** eligibility predicate (guard (iii) constrains the target row, the role check constrains the caller, so any owner or collaborator may correct another participant's boundary) in its own `run-controls/` subtree as **T4.8** under the new **I-004-24** — the affordance is a fail-closed projection of the daemon predicate, never a second source of eligibility truth — and handing it to Plan-013's T4.2 mount under **CP-004-15**, with the renderer-placement sentence, two materially re-derived Timeline/Runs View interaction enumerations, and one appended-last criterion landing in `Spec-023 §Signature Feature Composition Sketches` and `Spec-023 §Acceptance Criteria` — flip-and-restoring Spec-023 `approved` (its first flip) and Plan-004 and Plan-013 `approved` again, and minting BL-153 as the named sequencing gate (the affordance ships only behind the replacement-send wire-arm registration, held task-scoped on T4.8); no interface, wire member, method string, error code, table, or column is minted, so no census moves — and the 2026-08-18 session-cost-receipt and provider-account amendment (§6 node NS-74 — its first-time targeted readiness audit riding the same diff, the in-swap shape extended from an amendment to a new-plan mint) then **minted a new pair**, [Spec-029](docs/specs/029-provider-accounts-and-credential-homes.md) and [Plan-029](docs/plans/029-provider-accounts-and-credential-homes.md), both promoted `draft → review → approved` in that swap: the node-local provider-account registry (`provider_accounts`), per-provider defaults, isolated per-account credential homes, fail-closed spawn validation, credential-refresh brokering that stores no credential material, and the billing-mode label, with account identity a daemon-minted opaque immutable `accountId` paired with a monotonic `credentialGeneration` and cross-account concurrency specified as **probed, not assumed** (per-provider-account serialization is the normative degrade-honestly floor if the probe refutes it) — flip-and-restoring Spec-005/Plan-005 `approved` (the typed provider usage-limit signal as a sibling axis beside `RecoveryCondition`, unwidened, plus additive-optional account identity at spawn), Spec-016/Plan-016 `approved` (the session cost receipt as a decomposition of the existing committed-spend fold along per-run, per-caused-by, and per-paying-account axes, derivation staying emit-once with no read-time repricing), Spec-006/Plan-006 `approved` (`run.queued` gains `admittedProviderAccountId`; `usage.rate_limit_update` gains `providerAccountId` + `credentialGeneration` and is re-keyed `(provider, windowMins)` → `(providerAccountId, windowMins)`), and Spec-013/Plan-013 `approved` (the §Rate-Limit Display rewrite onto the event carrier) — and **discharging two upstream carrier boxes** with full amendment ceremony rather than bare marker flips: Plan-017's typed-usage-limit-signal box, releasing T2.5/T2.6 and `CP-017-10` (recorded a no-flip on Plan-017/Spec-017, discharge bookkeeping and landed-fact restatement only), and Plan-013's rate-limit-carrier box, releasing T4.5; census moves re-derived from the files — local SQLite **55 → 56** (`provider_accounts`), plans **28 → 29**, specs **28 → 29**, Postgres **26** unchanged — with [BL-154](docs/archive/backlog-archive.md) minted for the first-run provider-authentication gap the bundle flags but does not settle (the README census is the authoritative list) — and the 2026-08-25 first-run provider-authentication surfacing amendment (§6 node NS-77 — its restoring targeted readiness-audit delta likewise riding the same diff) then **settled that gap**, closing and archiving BL-154 and restating all four of its flag sites: `Spec-029 §Node provider readiness and the sign-in handoff` adds a per-provider readiness projection on the existing `providerAccount.list` reply — a **derivation** of the registry rather than a second source of truth, computed by the same resolution the spawn path performs, served from the account row's **stored** last-probe result so a registry read spawns no provider process, and **advisory only** against the unchanged I-029-3 spawn gate — with detection credential-free, never running a sign-in command, and the remedy disclosed in operator-facing message text only (the `mcp.oauth_unsupported` / `mcp.config_scope_unsupported` discipline) — carried, since the PR's round-1 fold, on the readiness entry itself as the schema-optional, producer-obligated `remedy` member, composed at read time and stored nowhere, the message-text-only rule re-scoped to the **reader** rather than the encoding, while `Spec-029 §Vendor authentication-policy constraints` records at **Documented** trust grade the four published vendor rules that make this a handoff rather than a login form (I-029-9 / I-029-10 / CP-029-7 ⇄ CP-026-6 / T2.5 / AC-11–AC-14), and `Spec-026 §Provider Authentication (Group B)` restates §Required Behavior as two independently-triggered step groups and adds the provider step — three triggers including **after** an account-plane refusal, never ahead of a run that would otherwise have been admitted, **offered and never demanded** (onboarding completes with zero accounts, the completion summary naming which providers are not ready and what the first run will do), a provider reported set up only on the authenticated arm, and no field, flag, or environment variable anywhere for a provider token — carried by Plan-026's I-026-11..13, Implementation Steps 23-25, and a terminal **Phase 7** whose `external_plan_phase_merged` entry on Plan-029 Phase 3 gates it mechanically instead of by a born-unchecked box (raised from Phase 2 at that same fold, T7.4 asserting that a provider run starts; the same fold routed the post-refusal trigger into both clients and gave the desktop step its host-performed action path), with Group B deliberately persisting nothing (a drafted config marker was removed rather than shipped, since writing it would have required a sixth `onboarding.*` verb for a UI dismissal and created a second record of a step whose truth lives in the registry); flip-and-restoring Spec-026/Plan-026 `approved` (their first flip) and Spec-029/Plan-029 `approved` again, re-deriving Plan-026's closed-set quantifiers by counting (seven phases, 25 Implementation Steps, 26 tasks, and §Touched But Not Owned eight → ten across the Codex folds, for the Plan-007-owned `apps/cli/src/exit-codes.ts` call that routes an account-plane refusal into Group B and the Plan-001-owned `packages/client-sdk/src/index.ts` barrel line that exports the relocated Group-B coordinator), and **moving no census** — the `providerAccount` namespace unmoved at seven verbs, the five `onboarding.*` methods unmoved, and no event type, error code, or table minted — the one minted column pair being the stored health reading and its observation timestamp on Plan-029's unshipped `provider_accounts` CREATE statement, which consumes no migration ordinal (SQLite 56 and Postgres 26 unmoved), and the 2026-08-25 clipanion terminal-pin amendment (§6 node NS-78 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Plan-007 `approved` for the Phase-R3 CLI-scaffold growth, Spec-007 untouched and unmoved: `clipanion@4.0.0-rc.4` is ratified a **terminal** exact pin in the new `Plan-007 §Decision Log` — the RC is simultaneously the newest of 84 published versions and the `latest` dist-tag (2024-09-06), the upstream repo has not been pushed since, publishes no GitHub Releases at all, and its tag stream stops at `v0.16.1`, so [BL-134](docs/archive/backlog-archive.md)'s stable-v4 watch could never fire and the item archives premise-void with each exit criterion mapped and no successor filed — with five named reachable re-evaluation triggers replacing the dormant watch, the pin's real enforcement named (`--save-exact` specifier + resolved lockfile entry + CI `--frozen-lockfile`; no `pnpm.overrides` and no `allowBuilds` entry warranted), four verified behaviors of the pinned artifact adjudicated into T-007r-3-1/2/3 rather than cataloged (parse refusals and escaped command-body errors both reach **stdout**, the latter with a stack trace under a hardcoded exit `1` bypassing I-007-14; a command's static `schema` cascade is applied inside `validateAndExecute()` and throws before `execute()` runs, so the base class's error boundary overrides the former, not the latter; `enableCapture: true` throws under a pure-ESM bundle whenever context streams are injected; the parse-error classes are exported through no entry point), **I-007-20** minted for CLI stream-role separation on a result-versus-diagnostic discriminator rather than a zero-versus-non-zero one — enforcement **structural first and lint second**, that ordering itself the claim: the single CLI base command class is the only error boundary and lives in its own leaf module `apps/cli/src/base-command.ts` (authoring it in `main.ts` would close a static-import cycle whose eagerly-evaluated `extends` clause aborts the CLI at startup, a hazard the single-file ESM bundle concatenates rather than resolves), while the three lint rules are explicitly non-exhaustive depth, the `node:process` `importNames` entry riding **I-007-13's own `no-restricted-imports` invocation** because ESLint flat config replaces a rule's options at the last matching config object and a second block would silently drop the import allow-list for exactly the command files that need it — and the workspace-wide binding on the six plans that author into `apps/cli/src/commands/` is discharged by **CP-007-15**, registered on all six consuming §2 rows beside a new `base-command.ts` ownership row (the swap also registers the missing §2 row + `main.ts` extender cell for the sixth, Plan-029, whose T4.3 command files landed at NS-74 with neither) — **I-007-14** re-specified as a closed union of exactly two typed failure sources, daemon-originated `JsonRpcErrorCode` envelopes and a closed two-member CLI-local kind (`usage` / `software`), with the mapper's own `UnmappedExitCodeError` landing inside that union as `software` rather than beside it, so no call site invents an exit code — test R3-T8b appended beside R3-T8a so the `R3-T1..R3-T8` range claim does not move, and the pre-existing `.eslintrc.json` / `import/no-restricted-paths` contradiction repaired to the corpus-wide `apps/cli/eslint.config.mjs` / `no-restricted-imports` form; no interface, wire member, method string, error code, table, or column is minted, so no census moves (SQLite **56**, Postgres **26**, plans **29**, specs **29** all unchanged) — and the 2026-08-25 rate-limit wiring amendment (§6 node NS-80 — its restoring targeted readiness-audit delta likewise riding the same diff) then gave owners to the three [Spec-021](docs/specs/021-rate-limiting-policy.md) enforcement legs that had been priced since the Tier-6 audit and assigned to no task: the `event.subscribe` concurrency cap to a new **Plan-008 Phase R5** (T-008r-5-1..3 with I-008-15 and CP-008-17, Tier-6-deferred on the Plan-002 Phase-4 precedent so the Plan-021 edge stays acyclic; the refusal lands pre-tRPC in the host fetch handler because a subscription procedure's middleware error is emitted in-stream under an already-200 `text/event-stream`, and slot release ships as `finally` + `signal` abort + a TTL backstop keyed to `SSE_HEARTBEAT_INTERVAL_MS` since workerd #6832 leaves stream `cancel()` unfired on disconnect; an unavailable lease store takes Spec-021 §Fallback Behavior unmodified (grace-period `degraded` admission, then `503` `ratelimit.backend_unavailable`), the outage-versus-cap-trip distinction carried by telemetry rather than by the status — a draft status deviation was withdrawn once its `EventSource`-reconnection premise was checked against the processing model and found false; cap re-priced 5 → 10 concurrent, Tier-5 test census 117 → 135; the lease store serializes its whole prune → count → insert under `state.blockConcurrencyWhile()` rather than resting on Durable Object single-threading, sweeps expired rows with a self-re-arming alarm, keeps the slot owned by the host until dispatch's single-shot take so a pre-generator refusal cannot hold it for a TTL, and closes the stream in the same act as relinquishing the slot at the renewal ceiling), the six `artifact.*` relay rows to **Plan-014 T14.14 / T14.15** split by landing phase with I-014-14 and CP-014-9 (admission stated transport-invariantly — before any request-body byte is consumed, and never before the caller is authenticated, since a sender-constrained fetch token's `sub` is not a proven identity until its DPoP proof validates — rather than inventing the untyped upload transport, and narrowing T14.9's conflation of request-rate with byte-quota backpressure; task count thirteen → fifteen), and `invite.pending_cap` to **Plan-002 T4.3** (store-side inside the shipped `InviteService.createInvite` transaction under the parent-session row lock, counting only pending-and-unexpired), with **Plan-021 CP-021-6** minting the concurrency-cap contract both store-side owners consume — including `checkCapAdmission` + `recordCapViolation`, since the shipped `check` fuses block lookup with counter consumption and `recordViolation` is internal, so without the pair a cap owner would have to double-price its row or bypass the escalation ladder and `artifact.publish` moving to Spec-021's dormant class under a new §ADR Triggers re-arm bullet (no control-plane procedure for it is registered anywhere in the corpus) — flip-and-restoring Spec-021, Plan-021, Plan-008, Plan-014, Plan-002, and Spec-002 `approved` in the same swap (Spec-002 for the one-part narrowing its `§Rate Limiting` snippet owed — it typed `retryAfter` as required for every invite row, which is false for the `concurrency_cap` row T4.3 now enforces), and completing and archiving BL-120, BL-144, and BL-146; the registry stays 25 rows and no interface, wire member, error code, table, or column is minted, so no census moves — and the 2026-08-25 rollback replacement-send wire-arm registration (§6 node NS-75) then landed the follow-up registration the 2026-08-16 rewind-hardening amendment had named as its producer and deferred under CP-004-12's write-ordering precedent, closing [BL-153](docs/archive/backlog-archive.md) `completed`: `api-payload-contracts.md §Plan-004` gains the `replacementSend?: { content: string }` member on the `rollback` payload arm, the `boundary-diverged` member of `RollbackDegradedResult` (carrying `confirmedPosition` + `newestBoundaryPosition`, typed `number | null` so the position-less-compaction-row settlement Spec-004 routes into this same disposition is representable) beside the `resend-unapplied` member — carrying its required `resendDisposition` and, because it displaces a completed file leg, `files-restored`'s two never-silent enumerations — surfaced by **both** client renders, T4.7's CLI degraded render and T4.8's affordance settlement feedback, so neither surface hides a restore that mutated the tree — but no locator for the caller's staged text, which is reached through the response's own `interventionId` — and `resendDisposition` as a pair of class-scoped fragments intersected at the two terminal state-split arms, so the round-5 state-scoping is preserved rather than widened and the state-determined V1 value mapping is encoded rather than asserted (the section's degraded-class count re-derived five → seven with the arms), with Plan-004's born-unchecked wire-arm box checked on a **recorded no-flip** — the registration mirrors content Spec-004 and Plan-004 already carry as `approved` into a canonical architecture doc that has no Status lifecycle, `Spec-004 §Interfaces And Contracts` naming this exact edit a merge prerequisite rather than an edit of its own (that sentence takes a landed-fact annotation recording the discharge — the runbook's citation / narrowing row, nothing normative), so no Status moves and no targeted readiness-audit delta is owed — releasing the Gate-5 holds on T1.2 / T1.3 / T3.17 / T4.7 and meeting T4.8's task-scoped hold while T4.8 still ships dormant and Phase 3B's own gate set still stands; no error-contract row is added (every code the arm can reach was already registered, and the composite's four structural refusal guards settle as `rejected` results carrying `rejectionReason`), and no wire method, error code, table, or column is minted — the two added result-type declarations carry a single already-specified member rather than a new state or disposition — so no census moves (SQLite 56 and Postgres 26 unmoved), and the 2026-08-25 provider-bound text-neutrality amendment (§6 node NS-76 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-005/Plan-005 `approved` again for a delivery hazard neither doc had named: a provider CLI whose programmatic input surface also parses client-side commands consumes a message whose first word is command-shaped and answers with a **zero-turn success** (verified first-party against Claude Code `2.1.245`; codex-cli `0.149.1` passes the same text verbatim), so the participant's words never reach the model while every layer above reads a completed turn — settled as three `Spec-005 §Required Behavior` rules (a driver-boundary **transport-only** neutralization that never mutates persisted, evented, replayed, or participant-visible text, with a named escalation ladder and invisible/zero-width sentinels prohibited; a closed **frame-origin** discriminator whose absent or unrecognized arm neutralizes, deliberately not a capability flag whose undeclared state would fail open; and a **runtime tripwire** failing the turn rather than recording the provider's zero-turn success), a `§Parity Capability Mechanism Grades` row (both legs `emulated` — the Codex cell recording that its only verbatim-delivery evidence is `codex exec`, a different frontend from the `turn/start` transport the driver drives, so a confirming probe of that transport re-grades it `native` by ordinary amendment), two `§Pitfalls To Avoid` prohibitions, and AC8, built by Plan-005 **T3.18** under **I-005-7** in the already-gated Phase 3 (dispatching with that phase, whose gates are met) with **no** CP and **no** carrier box minted (the intervention-response leg widens the existing CP-004-1 by one clause, Plan-004 taking that mirror on a recorded no-flip); **one census move** — `driver.text_neutralization_failed` registered reserved in [error-contracts.md](docs/architecture/contracts/error-contracts.md) at 409, taking the driver namespace to seven codes — with the driver-result vocabulary restated unwidened in its `status` at `applied | degraded`, its envelope taking **one additive-optional wire member**, the closed-literal `refusalCode` (the refusal lands on the run's own existing `run.failed` terminal — the guarantee on both paths — and best-effort on that member, never as a third driver result and never as a JSON-RPC error; a trip also disposes the run's provider binding, quarantining the process rather than adding a `runtime_bindings` column), so SQLite 56, Postgres 26, plans 29, and specs 29 are all unmoved, and [BL-148](docs/archive/backlog-archive.md) completed and archived with its gate re-homed onto the landed contract across Spec-017 (C-18), Plan-017 I-017-17, and ADR-027 as an erratum-class repoint carrying no Status flip, and the 2026-08-26 provider-account sign-in, health-observation, and per-limit-quota amendment (§6 node NS-83 — its restoring targeted readiness-audit delta likewise riding the same diff) then reversed the one absolute the provider-account plane had carried since it was minted, and did it under a **new ADR rather than an amendment** because `Spec-029 §ADR Triggers` demanded exactly that: [ADR-028](docs/decisions/028-provider-credential-custody-posture.md) lands `accepted` in the same swap on the ADR-025 same-PR precedent (ADR census 26 → **27** accepted, still none `proposed`), carrying **D1**, brokered interactive sign-in that is deliberately _not_ custody — the daemon spawns the provider's **unmodified** first-party login binary with that account's credential home pinned, streams the URL and code to the client, and reads nothing the flow writes, adopted from the ADR's Option C so it stands alone if D2 retreats — and **D2**, bounded custody of exactly one class of vendor-minted non-interactive token under four conjunctive conditions: admitted on exactly one write-only wire input that appears on no reply, sealed through [ADR-021](docs/decisions/021-cli-identity-key-storage-custody.md)'s already-ratified ladder rather than into the credential home (daemon-owned bytes written into provider-owned space are indistinguishable from the provider's own to every later reader), and injected only into that account's child environment — with antithesis points 1, 3, and 5 **accepted as recorded residuals rather than rebutted**; the plane gains `providerAccount.login` / `providerAccount.loginCancel` and a read-shaped `providerAccount.subscribe` whose completion travels as a **wire notification and never a durable event**, so the registry stays un-evented and no Spec-006 type is minted, a per-account credential-home **health observer** specified as a freshness-and-liveness observer over local credential state that **never spends a credential rotation** (both pinned providers rotate refresh tokens single-use with no grace window, so a refresh on a cadence does not degrade a credential but ends the login — the usability residual is named rather than papered over), and a per-limit quota re-key from `(providerAccountId, windowMins)` to `(providerAccountId, limitId)`, forced by a pinned provider surface publishing **three distinct 10080-minute windows** that the old key cannot hold — flip-and-restoring Spec-029/Plan-029, Spec-006/Plan-006, Spec-013/Plan-013, Spec-022, and Spec-023 `approved` in the same swap, Plan-022 taking the reciprocal durable-tier inventory row on a recorded no-flip; **two censuses move** — the `providerAccount` namespace seven → **ten** verbs (six → **eight** mutating) and local SQLite 56 → **57** for the new node-local `provider_account_usage_windows` table — beside four refusal codes taking that plane to eleven and four columns added to the still-unshipped `provider_accounts` CREATE, while Postgres stays 26 and plans and specs stay 29 and 29, since ADR-028 is an ADR rather than a plan/spec mint, and the 2026-08-26 canonical-transcript-authority and same-agent-provider-switch amendment (§6 node NS-84 — its restoring targeted readiness-audit deltas likewise riding the same diff across all five flipped pairs) then settled three things the corpus had left unstated: **[ADR-029](docs/decisions/029-canonical-transcript-is-authoritative.md) lands `accepted`** (ADR census 27 → 28, none `proposed`), ruling the daemon's canonical transcript — a projection rebuilt from `session_events`, never a store — authoritative for the content of a provider session and every provider session a **replay target**, with provider-native resume / rewind / fork demoted to capability-gated optimizations each carrying a named daemon-side fallback in a five-row enumerated table (the synthesis made checkable by count rather than assurance, its strongest antithesis answered by **narrowing the claim** to visibility sufficiency + declared loss + named non-portability, and its Option E recording why no off-the-shelf format was adopted — ACP replays agent→client only and accepts no history at session creation, OTel GenAI is `stability: development` with plaintext reasoning, and both organizations that built adapters for these two CLIs declined to define a portable transcript), carried into `Spec-005 §Canonical Transcript Export And Replay` as an **ordered** five-step pipeline (identity map → strip → pairing repair, in that order, because stripping reasoning orphans tool calls and an unpaired call is accepted silently by an untyped injection surface and then rejected on **every subsequent request**), a post-replay assertion that is the only admissible evidence a replay worked, a memo-projection floor with four normative properties, and a permanent-refusal classification that never retries a poisoned history — built by Plan-005 T3.19–T3.22 under I-005-8 / I-005-9 and CP-005-10, with the matrix's Codex `model_mutation` cell corrected `false → true` (it contradicted this same spec's §Provider Parameter Vocabularies) and the `transcript_replay` Claude cell left deliberately **probe-valued** rather than asserted, since no stable seeding contract is published for that provider; **the same-agent provider switch** (`Spec-016 §Same-Agent Provider Switch`, D-016-26), where `agent.configUpdate` gains `driverName?` / `providerAccountId?` / `effort?` / `interruptAndSwitch?` and the switch applies at the next boundary the target axis permits — a **turn** boundary for an axis the driver takes as a per-turn override (Codex `turn/start` carries per-turn `model` / `effort`), a **run** boundary for a spawn-bound one, which `driverName` and `providerAccountId` are on every driver because Spec-029 binds a run's account for the run's lifetime; the split is read from the driver's declared vocabulary rather than assumed, preserves Spec-029's run-binding rule instead of carving it out, and keeps the receipt's per-paying-account key exact, with mutation and application two moments, so the mutation's response is a **discriminated union** rather than a settlement — a `pending` arm naming the resolved boundary, the daemon-minted `switchId`, and any superseded intent, and an `applied` arm reachable only through `interruptAndSwitch`, which is the one case that collapses the two moments — and the pending intent itself **durable and singular** on a third `agents` column, so a daemon restart re-arms it rather than breaking a promise already acknowledged and a later provider-axis update supersedes rather than queues; every non-superseded pending switch reaches exactly one terminal, `agent.provider_switched` at application (its `continuity` closed at **three** arms — `in_place` for a per-turn override carried on the running process, `replayed`, `memo` — with the declared-loss list arm-scoped so an empty list keeps meaning "nothing was dropped") or `agent.provider_switch_failed` where the deferred application could not be honoured, the second type minted because a caller acknowledged and then disconnected can be reached only by an event and an event named `switched` is the wrong record of a switch that did not happen — with the immediate arm dispatching the **existing** `interrupt` intervention so Spec-004's V1 control set stays closed (the NS-73 edit-affordance precedent) and **no Cedar action minted**, the mutation authorizing as `agent.configUpdate` already does and the interrupt leg as `Action::"intervene"` already does, so Spec-012 and Plan-012 do not flip; and **the turn-scoped effective-principal carrier**, where `usage.cost_update` gains the daemon-resolved `effectivePrincipal` (a closed two-arm value whose participant reference is required exactly on the participant arm) resolved by Plan-004 T3.19 from the NS-71 `interventions` rows under I-004-25 / CP-004-16 ⇄ CP-005-10 and registered as instance **(4)** of the `api-payload-contracts.md §Authenticated Principal` class rule — riding the **metered row itself** rather than a turn-boundary event the fold would have to order against, with **no per-turn table minted** because a table holding one derived identity would be a second record of boundaries the log already orders, which ADR-029 rejects on its own terms — checking the two born-unchecked Phase-4B boxes on Plan-016 and Plan-029 that this carrier had held since 2026-08-18 (Plan-029 on a recorded no-flip) and releasing both Phase 4Bs; flip-and-restoring Spec-004/Plan-004, Spec-005/Plan-005, Spec-006/Plan-006, Spec-013/Plan-013, and Spec-016/Plan-016 `approved` in the same swap; **census moves re-derived by direct count** — Spec-006 event types **156 → 158** across 20 unchanged categories (`session_lifecycle` (channel/agent) 7 → 9; the Plan-006 ownership-registry aggregate 31 → 33 and its Total row 156 → 158), the Spec-005 capability-flag matrix **thirteen → fourteen** and its driver-operation surface **fourteen → sixteen** (`exportTranscript` / `replayTranscript`, mirrored onto the canonical `ProviderDriver` interface in the same swap) (`transcript_replay` only — a `session_fork` flag was drafted and **withdrawn** rather than shipped, since no V1 capability branches a session and a gate nothing consults is a flag minted ahead of its reader, which that spec's discovery rule now forbids in this same swap; ADR-029's fork row keeps its fallback and records that no gate is registered), ADRs **27 → 28** accepted, and the agent error namespace two → three (`agent.provider_axis_invalid`, 400; the `providerAccountId` axis deliberately refusing in the account plane's own namespace instead) — while SQLite **57**, Postgres **26**, plans **29**, and specs **29** are all unmoved (the three `agents` columns join an unshipped `CREATE`, consuming no migration ordinal — the NS-77 precedent) and no wire method is minted — and the 2026-08-26 sidekick-definitions and peer-invocation amendment (§6 node NS-86 — its first-time targeted readiness audit riding the same diff, the NS-74 in-swap mint shape) then **minted a new pair**, [Spec-030](docs/specs/030-sidekick-definitions-and-peer-invocation.md) and [Plan-030](docs/plans/030-sidekick-definitions-and-peer-invocation.md), both promoted `draft → review → approved` in that swap: the node-local `sidekick_definitions` registry keyed by a daemon-minted opaque immutable `definitionId` (never the mutable `name`), attach-by-reference on `agent.attach` resolved daemon-side into a **snapshot** — a definition edit or delete reaches no running agent, because a live reference would widen an already-attached agent's allowlist, posture, or paying account without passing the attach-time authorization that admitted it — with explicitly-present request members merging **per field** over the definition and the effective resolved configuration echoed back, and fail-closed resolution that substitutes nothing (a pinned account that has left the registry refuses rather than silently becoming whichever account is default now, while account _readiness_ is deliberately not consulted, authentication staying settled at spawn by I-029-3's live probe); plus peer invocation — `ask_sidekick` (link `spawn`) and `delegate_to_sidekick` (link `delegate`) served into the existing callback-tool registry, targeting a **sidekick** and never a provider, registered unconditionally at spawn and filtered only by the resolved agent's tool allowlist (I-030-10), with each invocation adjudicated per call as `Action::"sidekick::invoke"` against the session's projected enablement flag supplied as Cedar context — the opt-in persisting as the minted `session.peer_invocation_set` event and being the user request `Spec-016 §Default Behavior` requires (Spec-016 flip-and-restored `approved` for a third child-run-origin arm; Spec-012 flip-and-restored for the named-action enumeration five → seven with `Action::"sidekick::manage"` / `Action::"sidekick::invoke"`; Plan-016 flip-and-restored for CP-016-18 and a Phase 3B supplement holding T3.21, plus CP-016-19 and the additive-nullable `run_links.invoking_principal_id` column it carries, Plan-023 for the CP-023-6 editor mount, and Spec-006 and Plan-006 for the `session.peer_invocation_set` registration — eight flipped documents in all; the PR's round-2 review fold then folded nine findings plus a ripple audit into normative contract without moving a table, event, method, or error census, taking the columns this swap adds from two to **six** — the four attach-time `agents` snapshot axes `execution_posture_mode` / `tool_allowlist` / `instructions` / `goal` under CP-030-7 ⇄ the reciprocal CP-016-20, stamped by Plan-016 T2.1 and carried on its `agent.attached` payload so `I-016-4`'s byte-equal rebuild still holds, since a column the log cannot rebuild is not replay-complete — while `agent.attach` became a two-arm union so a definition-only attach parses, `session.peer_invocation_set` gained origin keys and a last-writer-wins fold over its own third register, the `ask_sidekick` waiter became gap-free by capturing an event cursor before admission, the client SDK re-homed T5.3 → T2.4 to land with its first consumer, and `SessionReadResponse` gained the additive-optional `peerInvocationEnabled` projection under CP-030-8; **one** born-unchecked box was minted, holding Plan-030 T4.1 / T4.3 directly and Phase 5 transitively until Plan-005 registers the CP-030-6 named-action callback dispatch route — the Plan-005 callback-tool host builds `approval.requestCreate`-shaped inputs keyed by `ApprovalCategory` and a named-operation action has none, so a peer invocation is not merely prompted but unrepresentable, and that registering swap additionally owes the one-clause narrowing of the _every-invocation_ universals `Spec-005 §Scope` and Plan-005 T3.15 leg 3 still state), with a peer-invoked run admitted through the ordinary pipeline so the one-layer nesting rule and `orchestration.depth_exceeded` apply unrestated and its spend lands on the child's **own** receipt row under the target's paying account, leaving `aggregationScope` REQUIRED and closed at `"run-only"` and owing no receipt **contract** growth (`Spec-016 §Session Cost Receipt` takes one added per-caused-by resolution rule — such spend attributes to the effective principal of the turn that issued the call, never the `system` arm — a narrowing on an existing axis, not a fold change) — and the same swap **discharged Plan-028's born-unchecked effective-binding box the NS-64 way, by making the carrier unnecessary rather than registering one**: T28.4.6 already builds the composed snapshot from the post-drift read, so new **T28.4.11** derives the session's effective binding in-plan and Plan-005's `driver_tools` seam is never touched, while the durable side — where a carrier could not help, recovery running with no session and re-resolution being what Spec-015 forbids — stamps one additive-nullable `command_receipts.mcp_binding_digest` (CP-028-7), a `"b3:"`-prefixed keyed BLAKE3 over the RFC 8785 JCS canonicalization of the `McpServerBindingRef` tuple that deliberately **excludes** the config hash so a drift-triggered revocation still matches receipts stamped before the drift and carries no filesystem path into a durable row, under new **I-028-6** placing the neutralization in T28.4.4, the single revocation writer both the operator and drift entries funnel through (Spec-028 and Plan-028 flip-and-restored `approved`, **T28.4.9 released**; Plan-004 and Plan-007 recorded no-flip) — censuses re-derived from the files by direct count: local SQLite **57 → 58** (`sidekick_definitions`), the Spec-006 event taxonomy **158 → 159** across 20 unchanged categories (`session.peer_invocation_set` under `session_lifecycle`, registered by Plan-030 T4.2 through the `event.ts` union-registration seam per CP-030-5), plans **29 → 30**, specs **29 → 30**, Postgres **26** and accepted ADRs **28** unmoved, with five `sidekick.*` refusal codes registered for the definition and attach paths and none on the peer-invocation path, which rides the callback-tool result arms — and the 2026-08-27 Phase-3 transcript-quartet errata adjudication (§6 node NS-89 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Plan-005 `approved` alone (Spec-005 untouched — both growths implement rules it already states) for the growth PR #374's review folds minted beyond its erratum baseline: T3.22 owns both arms of the permanent-vs-transient refusal distinction — the transient arm narrowed to **definitely-unsent** retries, with an applied-but-unacknowledged outcome reconciled **positionally** (the target's readback turn count against the daemon's acknowledged count, through the same readback surface T3.21 reconciles through — never content matching, since ordinary turns carry no identity marker — an unreadable target settling as a visible turn failure, never a blind retry or an assumed delivery) — with the classifier wiring re-homed to each driver's request-dispatch seam (EXTEND T3.1 / T3.6) so classification precedes reconstitution and a replay-interior refusal settles on the memo floor, never on a second reconstitution, the AC12 acceptance-mapping entry re-homed onto T3.20, whose Tests gain the positive/negative capability-discovery pair that makes the mapping non-vacuous, and the **replay-target lifecycle** homed on T3.20 (the round-4 recurring-finding reallocation): a partially-seeded target is abandoned never reused — on mid-seeding ambiguity and equally on an interior refusal after an accepted prefix — the memo settlement always lands in a fresh target, and both callers' Consumes rows name T3.21's floor so the task DAG cannot dispatch either ahead of it; no interface, wire member, error code, table, or column is minted, so no census moves (SQLite 58 and Postgres 26 unmoved) — and the 2026-08-28 usage-delta accounting and child-thread frame-routing amendment (§6 node NS-91 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-005 and Plan-005 `approved` again, giving owners to two verified provider behaviors the corpus had priced at nothing: both pinned provider surfaces report usage as a **cumulative total for the provider session** that resets at no turn boundary, no compaction, and no resume, and both fold cache-read tokens **inside** their input figure, so T3.11's unshipped telemetry producers would have emitted per-turn totals into the `Spec-016 §Session Cost Receipt` committed-spend fold and overstated session spend by the conversation's length (measured **22×** on a long thread) — settled as three `Spec-005 §Required Behavior` rules (as re-specified by the PR #377 round-1 fold, eight accepted Codex findings taken as one redesign): per-axis **deltas** from one base register per provider **thread** and axis advanced in stream order, each interval attributed to the turn the metered frame itself **names** — never a base snapshot copied at turn dispatch, which double-counts interleaved turns — with establishment two-armed (a fresh session, replay-seeded included, bases at zero and meters its first reading **in full**, since seeding spends nothing and the first turn's spend is real; a provider-native resume bases at the daemon's own prior-emitted cumulative sum, never the first post-resume reading), **no** re-base at a compaction since the provider's counter is unaffected by one, a decrease on a declared-cumulative axis floored at zero with a recorded diagnostic rather than emitted as negative spend, and a wire-declared per-turn figure consumed as a corroborating cross-check never a substitute; a **non-overlapping** token partition whose input axis is uncached input, subtracted only where the breakdown's own arithmetic identity confirms containment; and a **family-scoped**, fail-closed **child-frame routing** rule — both providers multiplex subagent, review, and compaction child threads over the session's own connection, so thread-scoped families route by explicit thread identity while censused connection- and account-scoped families route without one, the session's own thread registers at establishment and child identities from the provider's parent-linked announcements, a present-but-unregistered identity is held in a bounded pending-registration buffer released on registration and shed on timeout, an absent or unrecognized identity is quarantined under a bounded buffer rather than projected or guessed into the parent, and **both** usage and child-raised interactive requests are carved out **ahead of** suppression — provider-attributed subagent spend riding the already-registered (`runId`, `provider`, `subagentId`) triple while provider-internal child spend attributes to the parent run at run scope, and child requests routing through the same approval pipeline as the parent's per `Spec-016 §Provider-Native Subagents` — built by Plan-005 **T3.11** under new **I-005-11** / **I-005-12** in the already-gated Phase 3, with **T3.14 recording the consumes-not-owns split** (its terminal-emission boundary consumes that routing and adds no second source of truth for whose stream a frame came from) and **no CP and no carrier box minted on Plan-005** — both rules constrain only what it emits over already-registered `usage_telemetry` members and `subagent.*` kinds — while the round-1 fold carried the cross-document registration to its owners: Spec-006's §Usage Telemetry re-describes `usage.token_count` as a **per-turn figure folding by summation** (flip-and-restored `approved` in the same swap, description-only, taxonomy census unmoved at 159; Plan-006 carries no mirror and does not flip) and Plan-016's CP-016-9 registers the budget accountant's sum-fold consumption of those rows on a recorded no-flip, with the evidence re-graded honestly — the vendor's own generated schema (codex-cli `0.150.1`) carries the Verified shape claims and the reference-implementation audit re-grades Derived; the cache-read / cache-write split is **recorded as a known limit rather than absorbed**, retained on the driver diagnostic band because `usage_telemetry` registers no per-cache-axis member and minting one here would mint a member ahead of its reader; no interface, wire member, method string, error code, table, or column is minted, so **no census moves** — SQLite **58**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159**, the driver error namespace **seven**, and the ownership map's `provider/drivers/` **thirteen**-file row all unmoved (the two new daemon modules are `provider/`-level siblings on the NS-85 precedent) — with Spec-005's acceptance criteria **17 → 19** and Plan-005's invariants **10 → 12** the only counts that move, both inside that pair's own closed sets — and the 2026-08-29 transcript-fold declared-loss vocabulary amendment (§6 node NS-92 — its restoring targeted readiness-audit delta likewise riding the same diff) then widened the closed `DeclaredLossKind` wire vocabulary by its fifth member, `'turn_content_unavailable'`, for the canonical-transcript fold (Plan-005 T3.19) reaching a logged turn whose body cannot be read when it runs — the turn carried with its structural position and an empty body and the loss **named** rather than either silent disposition taken, since an empty body alone reads as "the author said nothing" and a dropped turn reads as "the turn never happened" and both are false, and since the vocabulary's own rule makes a new loss kind an amendment and never a free string — flip-and-restoring Spec-006 `approved` (the `agent.provider_switched` enumeration it carries is normative), with Plan-016 taking its two count mirrors (four-member → five-member) on a recorded no-flip, arm-scoped requiredness untouched, and `DriverTranscriptExportResult`'s steps-3-and-4 scoping left standing because this is the one member produced daemon-side at pipeline step 1; no event type, category, wire method, error code, table, or column is minted, so **no census moves** — the Spec-006 taxonomy stays **159** across 20 categories, SQLite **58**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, and the 2026-08-29 desktop-console parity amendment (§6 node NS-93 — its restoring targeted readiness-audit deltas likewise riding the same diff across every flipped pair) then gave corpus homes to four user-ratified provider-console capabilities and one operator page the corpus had specified everywhere except where a person could reach them: **participant-triggered context compaction** (`compactContext`, capability-gated, `native` on Codex through `thread/compact/start` and `emulated` on Claude through a `driver_command` frame carrying the provider's own `/compact` — settling on the provider's typed compaction evidence and **never** on the request being accepted, since both mechanisms answer before the work is done, and **reconciling rather than revising** `Spec-013 §Context Window and Usage Meters`' standing sentence: the 80% hint stays informational, automatic compaction stays prohibited, and a participant-initiated trigger is stated as a different act by a different actor beside it; the Claude leg is the **first V1 producer of a `driver_command` frame**, so that spec's standing negative claim is **re-derived by count** against its two nearest candidates rather than restated, and because the arm is exempt from the neutralization tripwire the amendment names its substitute guard — pre-dispatch presence in the provider's own enumerated command set, post-dispatch typed evidence — with prompt-injected self-summarization prohibited as a substitute, since it would report success for work that did not happen), **the provider command and skill surface** (`listProviderCommands`, a **live read held as driver-session state and never a stored registry** — the decision that keeps the capability storage-free, because a stored copy would need invalidation, staleness, and reconciliation machinery whose only purpose is to re-derive what one read gives — with every entry carrying the `(driverName, providerAccountId)` it was read under as a **routing invariant enforced at the daemon rather than trusted to the renderer**, so a Claude-enumerated command is never offered to or sent through a Codex agent), **the output-speed axis** (a **single-provider** flag: one pinned CLI declares a fast-output state and the other publishes no speed axis at all, so the Codex `false` is a complete declaration whose analog is the `model` / `effort` axis `agent.configUpdate` already carries and nothing is emulated onto it; declared **`static`** rather than `probed` because reading the declared state costs a user-message request and the admissibility test is conjunctive on **zero-turn**, with the declared state and never the request treated as the truth; reaching participants as `outputSpeed?`, a fifth provider axis on the existing `agent.configUpdate`, **spawn-bound and therefore resolving to a `run` boundary** — stated explicitly because it is the minority case on a mutation whose other axes are per-turn), **the input-ask card** (the `driver_ask.*` family has been fully specified daemon-side since campaign B1 and named in **no** renderer surface anywhere — permission-kind asks already ride the approvals surface, but an **input**-kind ask had no home at all, so a provider blocking on a structured question produced a run that stopped with nothing on screen to answer; the card mints **no wire method**, the answer ingress being the already-registered client-facing `driver.respondToRequest` whose `response` is unknown-typed, and one additive-optional `options?` member joins the Spec-006 driver-ask shape so the structured-options arm is reachable rather than dead text, with the free-text arm **unconditional** because exactly one of the two pinned mechanisms can supply a choice set), and **the settings-level MCP-servers operator page** (Spec-028 has carried eleven IPC operations, five event types, and ten error codes since it was written and named its own operator surface once, in passing, inside a bullet about a subscription; `Spec-028 §The operator surface` plus a Spec-023 sub-sketch now say what it renders, what it may not render — no configuration value, env-var value, header value, or token — and which rules it is forbidden to re-derive, taking `mcp.subscribe` as its update channel rather than reintroducing above the daemon the polling that spec forbids below it, and deriving no eligibility of its own because a renderer recomputing effective idempotency class would be a second source of truth for a decision Spec-015 recovery depends on) — **two client-facing verbs are minted**, `driver.compactContext` and `driver.listProviderCommands`, taking that namespace seven → **nine**, admitted by `Plan-005 §Phase 4 — Client SDK exposure + degraded-fallback` decision #2's **own governing principle** rather than in spite of it (both operate on an already-existing session; neither establishes, restores, starts, or tears one down) and differing from the R8 parity operations that decision kept daemon-internal in the one respect that decided those — those had existing client routes and these have none, while inventing a sixth intervention for compaction would widen Spec-004's deliberately closed V1 control set (the NS-73 / NS-84 precedent) — with CP-007-6's mirror updated on a **recorded no-flip** for Plan-007, whose `register()` substrate is unchanged by two more callers; flip-and-restoring Spec-005/Plan-005, Spec-013/Plan-013, Spec-016/Plan-016, Spec-023/Plan-023, Spec-028/Plan-028, Spec-006, and Plan-012 `approved` in the same swap — **twelve** flipped documents, re-derived by listing rather than asserted (five pairs plus Spec-006 and Plan-012 singly), with Plan-007 taking a recorded no-flip for its CP-007-6 count mirror and Plan-006 not edited at all, since it carries no mirror of a payload member and the `driver_ask.*` payload schemas are Plan-012 T1.5's (the NS-91 `Plan-006 carries no mirror` precedent) — with CP-028-8 ⇄ CP-023-7 registered on the NS-73 owner-authors / host-mounts precedent, CP-005-12 and CP-023-8 registering the renderer consumers, and new invariants I-005-13 and I-013-9; **census moves re-derived by direct count** — the Spec-005 capability-flag matrix **fourteen → seventeen**, its `ProviderDriver` operation surface **sixteen → eighteen**, the client-facing `driver.*` namespace **seven → nine**, Spec-005 acceptance criteria nineteen → **twenty-four**, Plan-005 invariants twelve → **thirteen** and obligations eleven → **twelve**, and the Spec-023 sub-sketch count six → **eight** — while SQLite **58**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** across 20 categories, the driver error namespace **seven**, and Spec-028's eleven operations / five events / ten error codes are all unmoved, because the enumeration is a live read that stores nothing and the one storage-adjacent consequence — the `driver_capabilities.capability_flag` CHECK widening fourteen → seventeen — rebuilds an **already-shipped** table (migration `0011`) through a next-ordinal `lang_altertable` migration that adds no table and no column, while a cross-binding or unsupported dispatch refuses as the already-registered `driver.capability_unsupported`, so no error code is minted either — and the PR's **review fold** then adjudicated the round's findings into the same twelve documents rather than cataloging them, moving two counts and no census: the compaction wait becomes **bounded and twice-terminated** (a per-driver declared bound and the binding ceasing to be live), settling `failed` on expiry while a late compaction frame still normalizes into a `usage.context_compacted` boundary — bounding the **operation** never bounds the **boundary's record**, and the Spec-013 control's in-progress state accordingly clears on the call's own settlement rather than on the boundary row; `DriverCompactionResult` becomes a **discriminated union** requiring `boundaryPosition` on `applied` and **dropping** `capability_undeclared`, which the static capability gate already refuses before dispatch; the command surface is settled as **enumeration and discovery** — V1 sends exactly one enumerated entry, the compaction command through its own control, because C-18 refuses leading-`/` composer text outright and the tripwire-exempt `driver_command` arm is admitted only against post-dispatch typed evidence, so a dispatch route is **named and gated** on both rather than left implicit and the "exactly one `driver_command` producer" count stays exact by construction; the output-speed axis **publishes** its value set as `outputSpeedLevels` (static like its flag, since a vocabulary obtained by reading would contradict the conjunct that makes it `static`, and client-visible unlike `detectionSource`) and travels the **durable** switch record, taking `AgentProviderSwitchTarget` **four → five** axes and the shared application-failure vocabulary **six → seven** (`output_speed_unavailable`), with a later fold giving it the **effective** home the round-1 no-column claim had left it without — `agents.output_speed` on Plan-016's own still-unshipped `CREATE`, the one column this swap adds, consuming no migration ordinal and moving no table census, plus its `AgentListResponse` echo, since a spawn-bound axis handed a durable **pending** slot and no durable **effective** one is unappliable under an application procedure whose terminal clears the slot; authorization is **stated and mints nothing** — a non-participant takes the already-registered `session.not_found` on both verbs, the enumeration needs only active membership, and compaction runs the identical `Action::"intervene"` evaluation `run.pause` / `run.resume` already share (its non-local hosting-node leg unreachable because a binding this daemon holds is hosted on this node), so the `Spec-012` named-action enumeration stays **seven** and Spec-012/Plan-012 take no flip for it — with the reuse recorded on the row it reuses rather than only beside it: the Security Architecture permission-matrix run-control row's subject now names compaction and the `Action::"intervene"` coverage enumeration widens with it, the matrix gaining no row; and `Spec-028 §The operator surface` is corrected on three overstatements — mutation is uniformly Cedar-gated but neither uniformly trusted-server-only (a first observation upserts an **untrusted** binding and safety-**tightening** overrides need no trust) nor uniformly audited (`mcp.reconnect` carries no dedicated governance event, `mcp.oauthLogin` is receipted and deliberately not evented), **state** is projected fail-closed while **eligibility is not projected at all** (no field reports it, so every control is offered and the daemon's typed refusal renders), and configuration content **splits three ways** — collected input with credential-bearing values write-only in the renderer, read-back that is exactly the redacted config view, and values the daemon does not serve rendered nowhere — because a page that could render and collect none of it could not register a server at all, and the 2026-08-30 machine-authored prose amendment (§6 node NS-94 — its restoring targeted readiness-audit deltas likewise riding the same diff across all four flipped pairs) then gave assistant- and tool-generated prose the durable encrypted home the corpus had never given it: `assistant.message` and `assistant.thinking_update` carried `contentType?` / `contentLength?` and no body, the three live `tool.*` rows carried no bodies, and that prose landed **only** in the bounded-retention diagnostic tier, which [Spec-020](docs/specs/020-observability-and-failure-recovery.md) makes explicitly non-canonical and purges in `≤ 7` days — a gap that is a **compile-time fact rather than a doc reading**, the shipped `packages/contracts/src/event.ts` registering 25 `SessionEventSchema` payload variants of which none is assistant or tool, so both shipped driver normalizers resolve every one of these types `payload-variant-pending` and **forbid envelope construction**, and no variant could be registered because the payload shapes named no member carrying the body and nowhere durable existed to put it, leaving [ADR-029](docs/decisions/029-canonical-transcript-is-authoritative.md)'s ruling that the canonical transcript is authoritative for the content of a provider session standing over a log holding none of that content and making NS-92's `'turn_content_unavailable'` — minted for the **exceptional** unreadable turn — the condition of **every** assistant turn; settled as `session_events.content_payload`, a **sibling of `pii_payload` rather than a widening of it** (the two columns partition one row's text by authorship, so erasing a person never requires erasing a session), sealed AES-256-GCM under a **session**-scoped key — a random 32-byte DEK held **wrapped** in the new node-local `session_content_keys` (the `participant_keys.encrypted_key_blob` custody shape), re-designed at the PR #383 round-1 fold from a daemon-master-key derivation that [Spec-022](docs/specs/022-data-retention-and-gdpr.md)' rotate-on-shred would have destroyed on any unrelated participant's erasure — taking every existing session body with it — and now re-wrapped inside that rotation's **existing** `BEGIN EXCLUSIVE` rather than in a pass of its own — with AAD = `session_id || event_id`, excluded from the canonical bytes and committed to by a payload-embedded `contentCiphertextDigest` under the existing Encrypt-Then-Digest-Then-Sign order re-run with **one** embedded member instead of two — there is deliberately **no owner-stamp sibling column**, `session_id` being canonical and signed already, which is why the home costs one column and not two and why no `content_owner_stamp_unbound` check pairs with `pii_owner_stamp_unbound` — with `pii-indirection.ts` kept the sole write path for both codecs and the column unconstructible for `audit_integrity` / `event_maintenance`, whose never-compacted / never-shredded invariant would otherwise leave a destroyable body no mechanism may ever destroy, and cleared at compaction alongside `pii_payload` (its digest needing **no** stub field and deliberately given none, the closed stub enumeration being replaced wholesale — the disposition stated at mint time rather than retroactively, the lesson of the 2026-07-27 owner-stamp amendment); bounded at `CONTENT_PAYLOAD_PLAINTEXT_MAX = 262144` bytes because unlike human-typed `pii_payload` a tool result is routinely a file dump, with an over-bound body **truncated at a codepoint boundary, never refused and never dropped** (`contentTruncated: true` beside a pre-truncation `contentLength`, no invisible sentinel), which is a real transcript loss and so mints `DeclaredLossKind`'s **sixth** member `'turn_content_truncated'` rather than borrowing `'context_truncated'` (scoped to whole exchanges, never halves) or overstating as `'turn_content_unavailable'`; Spec-022 taking the full PII-lifecycle reciprocals on the 2026-08-18 `interventions.pii_payload` pattern — a durable-tier map row, an export disposition, a place in the artifact-payload posture, and two re-described diagnostic rows that stop being the only home for provider and tool bodies — and placing the column **outside** the participant-keyed fan-out because a machine-authored body is session work product co-owned by every member, while declining to overclaim (no per-row key means no crypto-shred, and a NULL-out is not a secure overwrite, so the ciphertext dies with its row and is not claimed provably destroyed on demand); owned by Plan-006 on the `pii_participant_id` precedent through a new **Phase 3B** supplement (T3.6, T3.7, T3.8, I-006-3-05 … I-006-3-08, CP-006-15) since Phase 3 merged 2026-08-04 and a merged phase is not amendable in place, and consumed by Plan-005 under CP-005-13 with one **task-scoped hold on T3.20** — T3.19 deliberately **not** held, its fold and export being correct over the body-less log they were authored against, while T3.20 is, because its post-replay assertion is Spec-005's only admissible evidence a replay worked and over content-free turns cannot separate a provider that accepted every frame from one that silently dropped them all — with NS-92's residuals (1) and (2) explicitly **not** discharged and recorded as still owed by the swap that registers the fold's emission (in flight as PR #379 at authoring), this amendment settling only the **condition** those clauses will name; and then **substantially hardened at the PR #383 round-1 Codex fold, whose eight findings were all accepted**: the sealing key became a stored, re-wrappable per-session DEK (a derived key would have been destroyed by rotate-on-shred on any unrelated participant's erasure, taking every existing body with it — the artifact-payload posture defeated by the key schedule rather than by the sweep); T3.6 took the five body-bearing `SessionEventSchema` variants (`assistant.message`, `assistant.thinking_update`, `tool.invoked`, `tool.result`, `tool.error`) without which the column could be created and sealed but never written to; T3.7 took `content_payload` in **both** shipped byte-accounting statements (without which a content-bearing session reaches ~12.5 GiB before the count trigger fires, the round-1 claim that the 500 MB trigger becomes binding having been simply false) and the audit stub's surviving `contentLength` / `contentTruncated`; a new **T3.8** took verify-on-read as the **sixteenth** verification mode `content_ciphertext_digest_unbound` — provenance-dispatched on `received_from_node_id` exactly as NS-58's fourteenth is, a verifier `failureMode` and not a wire error code — plus the `HydratedSessionEvent` read projection that never splices a body into signed `payload`; the column was confirmed **node-local**, joining `pii_payload` in Spec-008's never-backfilled set (Spec-008 flip-and-restored, Plan-008 recorded no-flip) with a peer's `'turn_content_unavailable'` a named residual owed by the key-distribution swap the stored DEK now makes possible; and the Spec-022 export claim was **narrowed rather than defended** — a participant mentioned only inside an encrypted machine body is not discoverable by the metadata selector, every alternative being a worse trade; **one census moves** — local SQLite **58 → 59** for the new `session_content_keys`, while Postgres holds at **26**, no event type or category is minted so the Spec-006 taxonomy stays **159** across **20**, no wire method, Cedar action, or error code is added, and plans **30**, specs **30**, and accepted ADRs **28** all hold, the other counts moving being `DeclaredLossKind` **five → six** and Plan-005's cross-plan obligations **twelve → thirteen**, each re-derived by counting, and the 2026-08-30 per-model effort-vocabulary, non-probeable-enumeration, and outbound-answer-bound amendment (§6 node NS-95 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-005/Plan-005 `approved` again for three legs, two of which correct claims the corpus carried: **(1)** `Spec-005 §Provider Parameter Vocabularies` asserted Claude `low | medium | high | max` and Codex `minimal | low | medium | high | xhigh` with no `max`, mirrored in Plan-005 C-8 and the [api-payload-contracts.md](docs/architecture/contracts/api-payload-contracts.md) `ProviderModel.effortLevels` comment, and **both halves are false at this spec's own pins** on first-party zero-turn probes dated 2026-08-30 (**Binary probe**, **Verified**) — Claude Code `2.1.251` publishes `low | medium | high | xhigh | max` on three of four models and **no effort surface at all** on `claude-haiku-4-5-20251001`, codex-cli `0.150.1` publishes **three** distinct lists across eight models with **no `minimal` anywhere**, and one of them (`gpt-5.6-luna`) carries `max` without `ultra`, which makes the correction structural rather than a value refresh: no provider-wide list is right for every model in the provider's own reply, so the rule becomes the seam's own — the vocabulary is what the model publishes on the provider's catalog read, carried verbatim on `ProviderModel.effortLevels`, absent meaning the model exposes no effort selection, with each driver's pinned declared catalog answering a composition that binds **no** live read rather than standing in for one that **failed**; **(2)** `Spec-005 §Capability discovery` and Plan-005 T3.24 both named exactly two non-probeable entries, written when probing was expected to be the common case — counting the shipped detection tables inverts it, **thirty-four** entries over seventeen flags on two drivers, **four** `probed` (all Codex) and **thirty** `static` in four failing-conjunct shapes, with the Claude leg declaring **zero** probes as a measured finding rather than an omission, so both sentences now enumerate the small probeable set and state the non-probeable one as its complement, and the spec settles what AC14's negative control asserts on a leg that declares no probe (**declared and dormant**, re-arming when an entry there becomes `probed`); and **(3)** `Spec-005 §Required Behavior` gains the **outbound-answer bound** — the inbound line ceiling bounded what the transport would accept and said nothing about what it would send, so an answer composed from daemon-side content had no ceiling and an oversized write left the ask permanently unanswered — every driver answering provider-raised asks now applying an explicit bound measured on the **encoded** frame, an over-bound answer replaced by that ask's **own refusal shape** carrying a bounded **constant** reason (never a slice of what overflowed, which can overflow again), and oversized, unencodable, and **write-rejected** attempts each emitting a driver diagnostic, retiring the swallowed write rejection whose justification — that the exit path reports it — records a dead process rather than a named unanswered ask, with the one residual stated rather than papered over (a request `id` itself past the bound sends nothing and leaves two records: unanswerable, never _silently_ unanswered), built by Plan-005 T3.15 leg 3 under the new **I-005-14** and realized in the in-flight Phase-3 wave-2b slice; **no census moves** — no capability flag, driver operation, wire method, event type, error code, Cedar action, table, or column is minted, so SQLite **59**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** across 20, the capability matrix **seventeen**, the `ProviderDriver` operation surface **eighteen**, the client-facing `driver.*` namespace **nine**, and the driver error namespace **seven** all hold, the only counts that move sitting inside that pair's own closed sets: `Spec-005 §Acceptance Criteria` twenty-four → **twenty-five** and Plan-005's invariants thirteen → **fourteen**, each re-derived by counting, and the 2026-08-31 Codex service-tier surface amendment (§6 node NS-96 — its restoring targeted readiness-audit delta likewise riding the same diff) then flip-and-restored Spec-005/Plan-005 `approved` again for a correction that moves the **ground** of a cell without moving the cell: the 2026-08-29 desktop-console parity amendment gave the Codex `output_speed` cell `false` and justified it with an absolute — that codex-cli "publishes no speed axis at all" — resolved from a census of the generated method root, and **that absolute is false at the pin**, the `0.150.1` **default, non-experimental** generation carrying `TurnStartParams.serviceTier` (not field-gated the way `permissions` is, so a default connection can set it) and a per-model tier catalog on `Model` (`serviceTiers: Array<ModelServiceTier>` beside a required-nullable `defaultServiceTier`, with the older `additionalSpeedTiers` carrying its own deprecation), so the census was never wrong — 185 method strings across the four default roots still match no speed, fast, turbo, or accel member — it was **the wrong instrument**, and `Spec-005 §Capability discovery` already says why in its own words (absence in a censused registry bounds availability from below and not above), the claim held deliberately narrow because that rule governs how a **driver resolves a flag at runtime** and the Codex driver resolves `static` from its table exactly as it must, so no driver violated it and what happened is that the spec's own justification prose did to a method census what the rule forbids a driver to do; **the cell stays `false`** on ground that survives — the axis is conjunctive on a **statically declarable level vocabulary** and a **declared-state read** and the pinned surface supplies neither (`ModelServiceTier` is three free-form strings, so which tiers exist is runtime catalog data the corpus's pinned declared Codex catalog does not carry; nothing on that wire reports a current tier back, `ThreadUsageBreakdownGroup.speed` being a usage-**attribution** grouping key that sets nothing; and the only speed-differentiated tier value established anywhere at the pin, `ModelServiceTier { id: "priority", name: "Fast", description: "1.5x speed, increased usage" }`, lives in two upstream test fixtures read at the **floor** tag `rust-v0.141.0`, graded **Upstream source, Derived** and deliberately not Verified, since a `false` resting on a Derived fixture would be as wrong as one resting on a method census) — with the three readings **graded separately rather than fused** on the parity row (method-level negative **Verified** at the pin, service-tier surface **Generated schema, Verified** at the pin, tier value **Upstream source, Derived** at the floor), the detection entry unmoved at `static` with **decisiveness** as its failing conjunct so NS-95's thirty-four / four / thirty derivation and its twenty-four-member decisiveness cohort stay exact, its **reason** replaced because the superseded one (the driver's only zero-turn channel is the method enumeration) is itself false now that `model/list` is measured zero-turn — the replacement stronger on the same conjunct, a zero-turn channel bearing on the axis existing and still not deciding it, because reading a returned tier as an output-**speed** tier is a semantic judgment over free-form strings rather than a decidable read — and a **named re-arm trigger** rather than a vague later: a pinned declared catalog read carrying a speed-differentiated `ModelServiceTier` re-grades the cell by ordinary amendment, which additionally owes two decisions this one does not pre-empt (whether `outputSpeedLevels` stays statically declared or becomes per-model catalog-carried like `effortLevels`, the static rule's justification being that reading costs a turn-bearing request and not transferring to a zero-turn catalog read; and which boundary the axis resolves to there, `serviceTier` being a **per-turn** override while the Claude mechanism is spawn-bound), with Spec-016 and Plan-016 **not edited and not flipped** on the NS-91 `carries no mirror` precedent (their `output_speed` text is scoped to the declaring driver and to the mutation's validation against the declared vocabulary, and no provider-wire rationale lives there) and the sweep completed in code as well as prose — widened once when a second pass found carriers the first had missed — across the Codex detection-table `rationale` string, the Codex capability-flag / vocabulary / omission comments, the Codex and Claude capability-test comments, the shared vocabulary table's comment, the `DRIVER_CAPABILITY_FLAGS` `output_speed` comment in `packages/contracts`, and the Claude composition root's `observedOutputSpeedFor` doc comment, none of them a behavior change, while the two **non-governance** `docs/reference/provider-wire/` §Output speed sections take a pointer-only repair for the opposite reason (PR #400 had already corrected them about the wire, but each closed by calling the divergence an unresolved governing-spec question, which this diff answers — no wire fact and no evidence grade in either file moves); **no census moves** — no capability flag, driver operation, wire method, event type, error code, Cedar action, table, or column is minted, so SQLite **59**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** across 20, the capability matrix **seventeen**, the `ProviderDriver` operation surface **eighteen**, the client-facing `driver.*` namespace **nine**, the driver error namespace **seven**, `Spec-005 §Acceptance Criteria` **twenty-five**, and Plan-005's invariants **fourteen** and obligations **thirteen** all hold, so the preflight Gate-4 invariant-reference census pin does not move either, and the 2026-09-01 console-design amendment (§6 node NS-97 — its restoring targeted readiness-audit delta likewise riding the same diff) then landed the design track [Spec-023](docs/specs/023-desktop-shell-and-renderer.md) had deferred to since it was authored: a normative `§Console Design (Meridian)` (the four product bars, the nine-part design language, the eight rules every console surface obeys, the surface set with its **closed** eleven-member pane-kind set and two auxiliary windows shipping as hardened `BrowserWindow`s with their own bridge instance, the `define`-gated fixture bridge shape-identical to `SidekicksBridge`, the RAM / CPU / frame / bundle budgets that gate every console PR, and the renderer-scheme persistence rule), `§Console Libraries` (eighteen axes, each ADOPT / ADOPT-with-constraints / OWN-BUILD / AVOID with its evidence surfaced per axis into §References) and `§Console Test Tiers`; the Electron floor moving to **44.x** on 41's 2026-08-25 end-of-support, with [ADR-016 §Decision](docs/decisions/016-electron-desktop-shell.md#decision) amended in-swap to the 42 / 43 / 44 subset with 44.x the only admissible V1 runtime (Status unmoved at `accepted`) and the packaging pins re-derived (`electron-builder` 26.15.x, `electron-updater` 6.8.9), with [ADR-022](docs/decisions/022-v1-toolchain-selection.md) amended in the same swap because Electron 44's Node ABI 149 has no `better-sqlite3` 12.x prebuild — the daemon moves to the 13.x Node-API line in or before the pin-move PR; [Plan-023](docs/plans/023-desktop-shell-and-renderer.md) registering the `apps/desktop/src/renderer/src/console/` subtree as the Tier-1 explicit-label supplement **Phase 1C** on lane 1 (T-023p-1C-1..8 — a projection over already-registered bridge namespaces inside the approved envelope, gated only on Phase 1B merged, adopted by Phase 6 through mount-and-move; the enhancement lane the design track proposed was declined because CONTRIBUTING's lane 2 covers only code a shipped phase introduced), invariants I-023-11 (renderer scheme registered `standard: true` before `app.ready`), I-023-12 (auxiliary windows share no store), I-023-13 (fixture-bridge shape identity and growth-slate live-status coherence), and I-023-14 (the budgets and test tiers as CI gates) — the first two built on Tier 1 by the new explicit-label supplement **Phase 1B** (the renderer-load substrate: privileged scheme, bundle handler with root containment and the locked CSP header, window load, auxiliary-window factory and application menu, `main-unit` test project, and the Electron-44 / `better-sqlite3`-13.0.3 pin move as T-023p-1B-4, which carries the `Spec-015 §Driver Pin` amendment in-swap and moves the tier-1 Node floor 22.12 → 22.14.0, the 13.x Node-API-10 prebuild segfaulting at first open on any lower host (measured 2026-09-01); gated only on Phase 1 merged, so the console runs in the shell without waiting on Plan-007 R3) — and a `§Console growth slate` naming every wire the console builds against the fixture and does not yet have with its owning document; three conflict resolutions each deferring to the owning spec (global-only mute per Spec-019, a local **Not now** hide because Spec-002 mints no decline verb, no renderer detach control per Spec-009); `axe-core`'s MPL-2.0 admitted as a never-distributed test dependency in ADR-020's Decision Log; and an errata sweep making the "sixth run control" phrasing count-free across Spec-004 / Plan-004 / Spec-016 / Spec-023 and the payload-contract comment because Spec-004's own V1 control enumeration had omitted `cancel` — flip-and-restoring Spec-023/Plan-023 `approved` in the same swap with **no census move** (SQLite **59**, Postgres **26**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** all unmoved; Plan-023's invariants ten → **fourteen** inside its own closed set — I-023-11..14, re-derived by counting the table; the clause previously read twelve, which counted only the two named in the Spec-023 blockquote), and the 2026-09-01 WebAuthn per-platform-strategy amendment (§6 node NS-99 — its restoring targeted readiness-audit delta likewise riding the same diff) then settled the platform-coverage question `Spec-023 §Open Questions` had carried since 2026-04 and **withdrew** the accepted scope degradation `Plan-023 §Scope` recorded as its consequence: the question resolves **negatively** — no cross-platform Electron WebAuthn module exists, `@electron-webauthn/native` being deprecated in favour of `electron-webauthn`, whose own description is "Cross-platform loader for native WebAuthn support **on macOS**" and whose sole implementation dependency declares `"os": ["darwin"]` — but **neither non-macOS leg needs one**, since Windows exposes the public Win32 `webauthn.dll` API to any process holding an `HWND` (documented with no entitlement, package-identity, or browser restriction, with two independent non-browser consumers in the wild) and Linux's only authenticator class is the roaming FIDO2 key Yubico `libfido2` already drives, taken from the distribution package and **never bundled** — with the round-1 fold correcting this amendment's own first rationale, which said the `uaccess` udev rules ship with that package: they do not, and no supported Debian or Ubuntu release ships `70-u2f.rules` at all, so device access is a property of the host's `systemd` / `udev` (≥ 244, descriptor autodetection) that the packaged build declares, and the `uaccess` grant is a **seat ACL** rather than a permission bit, reaching no headless or non-seat session — so V1 ships **all three legs native**, the macOS one an **own-built** universal Swift N-API addon over **both** relying-party providers — `ASAuthorizationPlatformPublicKeyCredentialProvider` and `ASAuthorizationSecurityKeyPublicKeyCredentialProvider`, probed and attempted independently, with **two** PRF floors (macOS 15.0 for the platform authenticator, **26.4** for security keys) — in `apps/desktop/native/webauthn-darwin/`, with its own §2 ownership row, Vault12 `electron-webauthn-mac` recorded as the **reference implementation and rejected** on two measured disqualifiers (it generates the WebAuthn challenge internally, so no assertion it produces is server-verifiable — defeating both passkey sign-in and the Security Architecture's step-up re-authentication — and its prebuilt binary is arm64-only and non-fat with no arch guard, so a `darwin-x64` install succeeds and fails at `require`), with the Device Authorization Grant loopback **restored to what `Spec-023 §Fallback Behavior` always made it**, the degradation for a host whose probe finds no usable authenticator or no PRF on any of the three platforms rather than "the Windows / Linux path", and **detection a per-host probe at ceremony time and never `process.platform`** (new **I-023-15**), which selects a binding module and settles nothing else; **four claims the corpus carried are corrected against first-party measurement** on Electron 44.1.1 / Chromium 152.0.7977.65 — "Electron does not provide native platform-authenticator WebAuthn prompts on macOS, Windows, or Linux" is false for macOS since electron/electron #51255, backported by #51411 and present on the `44-x-y` branch V1 targets; the renderer path is closed by **Chromium's origin allowlist, not by CSP and not by the secure-context test** (from the `sidekicks-renderer://` scheme registered `standard: true, secure: true` the renderer reports `isSecureContext === true` with `PublicKeyCredential` defined and still takes `SecurityError`, so scheme privilege cannot open it); `Spec-023 §Main Process Responsibilities` routed through "Electron's WebAuthn bindings", an API that does not exist for this path; and the acceptance criterion demanded a **platform** authenticator on Linux, which no Linux desktop surface provides — while **three residuals are named rather than accepted wholesale**: the macOS end-to-end PRF verification stays [BL-108](docs/backlog.md)-gated on signing, entitlements, and an RP-hosted `apple-app-site-association` file (there is no localhost exemption on native macOS), no hosted CI runner can produce a PRF byte on any platform so end-to-end PRF is a recorded manual matrix, and the Windows Hello PRF build floor is graded **`Source`, not Primary** (vendor blog, corroborated only by a third-party issue thread, with the KB notes for that exact build mentioning neither WebAuthn nor PRF), so **no gate encodes that build number** and the leg reads `WebAuthNGetApiVersionNumber()` and probes instead; Plan-023 gains **I-023-15** and — at the Codex round-1 fold, which adjudicated fourteen threads into the same swap — **I-023-16**, which closes a trust-boundary inversion in the preload contract: `webAuthn.createCredential(options)` / `getAssertion(options)` / `deriveKeyMaterial(input)` let the **untrusted renderer** choose the challenge, the `rpId`, and the PRF salt and handed derived key material back across the bridge, so the surface narrows to `register()` / `signIn()` taking no argument, with main fetching server-issued options from the control plane and validating them against the build-pinned RP; tasks **T-023r-4-5** / **T-023r-4-6** / **T-023r-4-7** (the last carrying the password-derived-KEK custody leg `ADR-010 §Consequences` had already decided for no-PRF hosts, which the plan had never carried, so such a host is reauthenticated and can still unwrap its Ed25519 identity key), a rewritten **T-023r-4-3**, a **fourth** silent-failure guard (CTAP 2.1 mints two per-credential secrets and selects on the `uv` bit **in the response**, so libfido2's `FIDO_OPT_OMIT` default is a non-deterministic key derivation), two E2E spec files with two more re-scoped, and the **`koffi`** FFI pin (exact, floor `3.1.6`, MIT — per-platform prebuilt `optionalDependencies`, so nothing compiles at install and **no `onlyBuiltDependencies` entry is warranted**, with `asarUnpack` naming `**/node_modules/@koromix/**` because the binary lives in a sibling package, and out-structs copied via `koffi.decode()` because `koffi.view()` throws under Electron), while [ADR-016](docs/decisions/016-electron-desktop-shell.md) takes its dated **engine-scoped narrowings** and then, at the **Codex round-2 fold**, has its whole deciding set **re-derived by listing** — because once every ceremony is shell-native the engine comparison stops deciding anything (a Rust or Go host binds `AuthenticationServices` / `webauthn.dll` / `libfido2` too), so the WebAuthn constraint is **struck**, §Synthesis's "the one constraint that cannot be engineered around" is corrected as false-by-this-amendment, and the surviving deciders are enumerated with their evidence (the single Chromium renderer the console's single-number budgets and `@xterm/xterm` WebGL tier are measured against; the `browser` pane's in-process `WebContentsView` plus `webContents.debugger` CDP channel on all three platforms; `utilityProcess` over a Node main hosting the `better-sqlite3`-bearing daemon and its PTY-sidecar supervision; and the reversal cost of shipped Tier-1 code already bound to `app` / `BrowserWindow` / `contextBridge`), with the **Status outcome stated rather than assumed** — the remaining set still decides, so `accepted` holds, and an emptied set would have moved the ADR to `proposed` — and the sweep carried to §Thesis point 1, the §Consequences positive bullet, Option A's steel man, Options B and C point 1 (the WebAuthn gap **withdrawn** as a rejection reason), Assumption 1, and Revisit Trigger 1, while that same round-2 fold made the macOS probe **advisory and gating nothing** (`canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics)` is documented false with no Touch ID hardware, no enrolled biometric, and system-wide after repeated failures, and a platform passkey ceremony still completes in all three states, so the platform provider is attempted on every host and `.deviceOwnerAuthentication` is read as a diagnostic — a **direct** first-party availability answer may gate its own arm, a proxy may gate nothing), gave T-023r-4-7 the **secret-entry window** that makes the KEK leg reachable at all (main has no text-entry affordance and Electron's dialogs carry none; its own hardened `BrowserWindow` through the same private `constructLockedWindow`, its own preload exposing exactly one write-only operation that delivers the passphrase once and resolves `void`, neither a `SidekicksBridge` namespace nor an auxiliary window, so that set stays at two), narrowed `Spec-023 §Main Process Responsibilities` off "Return the authenticator assertion through the bridge" onto the typed `WebAuthnCeremonyOutcome` with main submitting the assertion over its own authenticated control-plane channel, and struck **BLE** from the Linux transport set (`libfido2` has no BLE backend under any flag — `fido_dev_info_manifest` runs `hid` unconditionally with `nfc` / `pcsc` under flags that default ON for Linux — so the row states HID plus NFC exactly and a BLE-only key takes the fallback path), and ADR-010 and the Security Architecture need no edit, while the **Codex round-3 fold** — under the three-round documentation cap — moved guard 4 from the request to the response (a `userVerification` preference binds no authenticator, so every leg reads bit 2 of the returned authenticator-data flags byte against the mode recorded at registration and derives nothing on a mismatch, deliberately **not** requiring UV everywhere, which would refuse every non-UV key), made a binding that will not load a **negative capability result** rather than a crash under a third detection rule, gave **T-023r-4-3** the macOS addon's universal `swift build` + `lipo` build that no task owned (at the **Xcode 26.4** / Swift 6.3 / macOS 26.4 SDK floor the security-key `prf` symbol sets, deployment target still 12.0), rewrote Implementation Step 9 off `WebAuthnPrfUnsupportedError` and refresh-token-only reauthentication onto the typed `fallback-required` arm followed by the DAG and then the T-023r-4-7 custody leg, and registered **CP-023-9** — the main-owned, bridge-unreachable, injected `PrfKeyDerivationService` port — to give [Plan-022](docs/plans/022-data-retention-and-gdpr.md) CP-022-3 back the producer the `deriveKeyMaterial` retirement had deleted, flip-and-restoring Plan-022 `approved` in the same swap and taking Plan-023's cross-plan obligations eight → **nine** (Spec-022 unedited; no census moves), and ADR-010 and the Security Architecture need no edit, the latter already reading "where available", which is the probe rule; and the **Codex round-4 fold —** — then settled the one thing every earlier round had assumed: a repo-wide search established that **no** route, task, credential store, or cross-plan obligation anywhere in the corpus issues a WebAuthn challenge or verifies a response, so the bindings all three rounds had been hardening reached an authenticator with no server to call, and [Spec-018](docs/specs/018-identity-and-participant-state.md)/[Plan-018](docs/plans/018-identity-and-participant-state.md) take the relying party's half as a new **Phase 6** (T6.1–T6.4, I-018-15..I-018-17, CP-018-14 ⇄ Plan-023 CP-023-10) — `webauthn_credentials` + `webauthn_challenges`, four ceremony routes on the shipped Plan-008 tRPC `sessionRouter` skeleton, and a single-use challenge fence, with three decisions settled rather than deferred: the challenge is a **row** and not a sealed stateless token (single-use is the property that matters, and a self-contained token stays replayable until expiry unless a durable fence records its consumption, so the stateless design adds a secret to rotate beside a write it still has to perform), the user-verification mode is **server-held** on the credential row and returned on the assertion-options reply (a client-remembered mode is gone after a reinstall and absent on a second desktop using a synced credential, and CTAP 2.1 selects between two per-credential secrets on that bit, so a wrong mode is a silently different key), and verification runs through a **maintained library** rather than local COSE/CBOR parsing (`@simplewebauthn/server` 13.3.3, MIT, zero Node built-ins across its shipped build and transitive tree so it runs on `workerd` — support its JSR metadata declares and its own CI does not exercise, which T6.2 closes with a deployment-target smoke test rather than assuming), the consumer gated mechanically by `{ type: external_plan_phase_merged, plan: 018, phase: 6 }` on Plan-023 Phase 4 rather than by a born-unchecked box; the same fold replaced the Windows `dwVersion = min(desired, supported)` formula — which compared numbers from two unrelated sequences, so an API-4 host would have been sent `GET_ASSERTION_OPTIONS` version 4 and Windows would have ignored the PRF request on exactly the builds that can serve it — with an explicit API-version → maximum-struct-version table read from the pinned header's version-history block, the **only** place an API version and a struct version appear together since the header carries no `added in WEBAUTHN_API_VERSION_N` comment inside any struct body, each PRF floor cross-checked against the response member returning its result at the same API version; gave the Linux leg a **verifiable attestation** (it read back only a credential id, which is not a registration a relying party can verify, and `libfido2` exposes `fido_cred_set_attobj` as a setter with no getter for the whole object, so the leg composes `{fmt, attStmt, authData}` itself — from `fido_cred_authdata_raw_ptr`, the `_raw_` accessor being load-bearing because its sibling returns the CBOR-byte-string-wrapped form and a guard-4 flags-byte read there is off by the header's width — with the `_x5c_list_` family for a full chain and `fido_cred_set_clientdata` taking the unhashed bytes); moved **both** `koffi` legs onto the library's asynchronous call path so a ceremony no longer blocks Electron's main thread for the length of a human interaction, with cancellation stated **asymmetrically rather than smoothed over** (Windows has the documented `WebAuthNGetCancellationId` / `WebAuthNCancelCurrentOperation` pair issued while the call is pending; Linux does not, `fido_dev_cancel`'s entire documentation being one sentence on a shared page, absent from that page's own return-value section, with no thread-safety statement anywhere in the project — so the leg bounds the wait with the documented `fido_dev_set_timeout` instead and names the residual); and completed the `deriveKeyMaterial` retirement sweep rather than declaring it — Plan-022's reciprocal-obligations bullet still required Plan-023 to expose it, CP-023-9's transport is corrected off "injection over a process boundary" onto the typed **`shell.deriveKek`** request registered in [api-payload-contracts.md](docs/architecture/contracts/api-payload-contracts.md) §Shell-Hosted Method Registry (the corpus's **first shell-hosted method**, namespaced `shell` because the registry's canonical regex forbids uppercase in a first segment), and T-023r-2-5 takes the contracts-side narrowing of the still-shipped three-method `webAuthn` stub; **one census moves** — Postgres **26 → 28** for the two new Plan-018 tables, with Spec-022's Path-2 enumerations and the erasure runbook widened in the same swap (Spec-022 flip-and-restored, Plan-022 recording a no-flip) — while SQLite **59**, plans **30**, specs **30**, accepted ADRs **28**, and the Spec-006 taxonomy **159** across 20 all hold, no wire method beyond `shell.deriveKek` and no event type or Cedar action is minted, the preload bridge's `webAuthn` namespace is unchanged, and the ceremony routes are control-plane procedures reached by main over its own authenticated channel rather than daemon methods, so the five-method `participant.*` JSON-RPC registry does not move either (two refusal codes are registered, taking that error namespace two → **four**); the other counts that move sit inside the two plans' own closed sets — Plan-023's invariants fourteen → **sixteen**, Phase-4 tasks four → **seven**, E2E spec files nine → **eleven**, and cross-plan obligations eight → nine → **ten**; Plan-018's tasks 27 → **31**, phases five → **six**, invariants 14 → **17**, obligations 13 → **14** — beside one added §2 package-path ownership row; and the **Codex round-5 fold —** — then corrected the ceremony's shape rather than adding to it, folding twelve findings and moving **no** census: the UV mode rode the authentication-**options** reply, which cannot work when guard 2 requires **discoverable** credentials and a discoverable sign-in offers no credential list, so the mode moves to the **verification** reply and the whole flow re-orders to options → assertion → verify → derive, with the client's PRF bytes **provisional** until that verdict and **zeroized on every refusal arm** (Plan-023 **I-023-17**); a participant **cancellation becomes terminal** and is no longer conflated with incapacity — no second provider arm, no DAG loopback, which would have offered a browser sign-in to someone who had just declined to sign in — carried with a **per-binding single-flight slot** held to the native callback and released on timeout but never on the caller's cancel, so a cancel-then-retry takes a typed **busy** refusal instead of putting a second `fido_dev_get_assert` on a device handle the first call still owns (**I-023-18**); the relying-party identifier stops being a **build constant**, which would have failed the origin check on every self-hosted control plane, and derives from the control-plane origin pinned at pairing, with the macOS **platform** provider's compiled `webcredentials:` associated-domain entitlement named an **ADR-020 self-hosting residual** — unavailable to a stock build, degrading honestly to the security-key arm and then the loopback, while the Windows, Linux, and macOS security-key arms are relying-party-agnostic; a **fifth** silent-failure guard lands for a **minted-once per-credential PRF evaluation input** persisted at registration and served back on every options reply (`webauthn_credentials.prf_eval_input`), without which a fresh input each ceremony derives a fresh key and every previously wrapped envelope stops opening at the next **restart** — invisible until then; the **two authentication operations are stated unauthenticated**, since sign-in is what mints the credential and demanding one first makes the flow unreachable on a cold install, bounded instead by a server-minted single-use `webauthn_challenges.transaction_id` and throttled under the **existing** `Spec-021` `auth.endpoint` row so that registry stays **25** rows (the per-transaction axis the invite-redemption pair would suggest has nothing to bound when single-use consumption already caps attempts at one), with the registration pair staying authenticated and all four registered in `api-payload-contracts.md` §WebAuthn Ceremony Procedure Registry — control-plane tRPC procedures, so the five-method `participant.*` registry still does not move; the **signature-counter check becomes a locked read-and-conditional-write** (`FOR UPDATE`, **I-018-19**), an unlocked read-then-write being defeated by exactly the cloned-authenticator replay a counter exists to detect, with the ceremony's `webauthn_challenges` → `webauthn_credentials` order registered in the corpus's single §Lock Ordering home under a one-clause preamble widening that admits a plan's own uncontested tables; the round-4 `shell.deriveKek` registration gains the **modules** it had named on neither side (`packages/contracts/src/desktop/shell-rpc.ts`, `apps/desktop/src/main/shell-rpc.ts`, the `daemon-supervisor.ts` extension, and Plan-022's own `crypto/shell-rpc-client.ts`) plus a duplex round-trip test over a real `MessagePort` pair, and the **HKDF is pinned to the shell side** under the contract-fixed info string `"ai-sidekicks/daemon-master/v1"` so raw PRF output never crosses the RPC into the process that does not own the authenticator — which narrows rather than deletes Plan-022 D-022-2's "HKDF is removed from this plan entirely", true of what that plan performs and false of its ladder; the CP-022-6 Path-2 closure is re-derived fifteen → **seventeen** with the per-column foil beside its counting rule seventeen → **nineteen**, **withdrawing** round 4's recorded no-flip for Plan-022 (the closure is rule-exhaustive in its **set** and hand-stated in its **counts**, and the counts had gone stale); and the `@simplewebauthn/server` claims are **sourced into a new Plan-018 §References with two of them withdrawn as false** — there is no JSR `runtimeCompat` `workerd` declaration for 13.x (the README and docs site name Node and Deno only, the sole primary-source mention is a 2023 v8.0.0 changelog line calling Workers "periodically tested but unofficially supported", and the one CI workflow runs `deno test` with the Node matrix entries executing no suite), leaving the **architectural** claim the tarball evidence does support — zero `node:` specifiers and zero bare built-in imports across the package and all 21 transitive dependencies, crypto solely from `globalThis.crypto` with no Node fallback branch — as a hypothesis T6.2's deployment-target smoke test falsifies rather than a vendor guarantee, while the "stated support or security policy" claim is withdrawn outright (no `SECURITY.md` exists anywhere, and the maintainer count is one), and two further library facts become normative: `VerifiedAuthenticationResponse` does **not** narrow on its own `verified` boolean, so T6.3 branches on it before touching the payload, and `packages/server/package.json` is absent from the repository (the manifest is `dnt`-generated), so the `engines` floor cites the registry; **Codex round 6, the last fold, then reverses two of round 5's own rules rather than refining them**, taking its eight findings as one redesign: the per-credential PRF evaluation input is **unserveable** on this deployment — the authentication-options reply is composed before a discoverable sign-in names a credential, so there is nothing to look one up for, and returning every candidate's would publish the participant's enrolled-credential set on a reply that is unauthenticated on purpose, while the daemon holds no credential row to supply one from either — so it becomes a **contract-fixed public constant** (`"ai-sidekicks/kek/v1"`, registered in `api-payload-contracts.md` beside a [W3C WebAuthn Level 3 `prf`](https://www.w3.org/TR/webauthn-3/#prf-extension) citation), the column is withdrawn from the unshipped `CREATE`, **I-018-18 is restated to its opposite** (the relying party mints none), the `shell.deriveKek` request loses that member and carries only Plan-022's salt, and the guard list returns four → five → **four** with guard 3 absorbing the reproducibility obligation — security unmoved, since the secret is the authenticator's per-credential `CredRandom` and the input only selects which evaluation to take, and Yubico's contrary per-credential-salt recommendation **recorded and declined with its reason** rather than quietly contradicted, because it presumes an `allowCredentials` flow this deployment does not have; what that salt was actually being asked to do gets a mechanism that works, Plan-023's new **T-023r-4-8** custody root key (**I-023-19**) — one per-installation key under which the refresh-token and identity-key envelopes are wrapped **once**, with every wrapping key (each credential's PRF-derived KEK and the passphrase-derived fallback) wrapping only a 32-byte **copy** kept in the OS keystore keyed by credential id, so enrolling an authenticator writes a copy and removing one deletes a copy and neither touches an envelope, the daemon's KEK deriving from that root so it survives an enrolment, and two consequences stated rather than discovered (no server escrow, so losing every credential loses the root; and the root is per **installation**, so a reinstall mints a new one) — which is why sign-in on a cold install changes too: `Spec-023 §WebAuthn Credential Flow` step 6 now takes a **freshly issued**, DPoP-bound PASETO pair off the verification reply (Plan-018 T6.3) instead of unwrapping a stored token a fresh install does not have, with three smaller corrections riding along (guard 1's surviving `dwVersion = min(desired, supported)` sentence deleted in favour of the explicit API-version → struct-version table, guard 4's Linux read named `fido_assert_authdata_raw_ptr` and never the CBOR-wrapped `fido_assert_authdata_ptr`, and T-023r-4-6 **owning** the `.deb` / `.rpm` `udev (>= 244)` / `systemd-udev` declaration in `electron-builder.yml` with a unit asserting it out of the built artifacts' own metadata); **six documents flip and restore** across all six rounds and the set gains no member — Spec-023, Plan-023, Spec-018, Plan-018, Spec-022, Plan-022 — with Plan-018's invariants moving 17 → **19** and Plan-023's sixteen → **nineteen**, Plan-023's Phase-4 tasks four → **eight**, a net **three** columns joining still-unshipped `CREATE` statements and consuming no migration ordinal, and Postgres holding at **28**, SQLite **59**, plans **30**, specs **30**, and accepted ADRs **28**; and **Codex round 7, the closing round, then flips the same six a fourth time** for five gaps — three of them consequences round 6 created and did not follow, two carried since the amendment began: the custody root's mint trigger widens from an installation's first _enrolment_ to the first time a **verified** ceremony finds no local root, since a cold install after a reinstall arrives by **sign-in** and would otherwise wrap its freshly issued pair under a root that does not exist; the **DPoP proof key** takes its own [ADR-021](docs/decisions/021-cli-identity-key-storage-custody.md)-ladder keystore entry explicitly **outside** that root (**I-023-20**), because main signs the token-request proof _before_ the verification reply that would let it unwrap anything, so a key sealed under the root is unreachable exactly when it is used — a circularity round 6 never faced because it issued no token; the sign-in **options fetch is stated uncredentialed** and its reply validated against the **pairing-pinned** control-plane origin rather than any origin the response names; the verify leg's proof is pinned to [RFC 9449](https://datatracker.ietf.org/doc/html/rfc9449) **§5**, the token-request form carrying **no** `ath` — `ath` **required to be absent** rather than merely unread, so an oversupplied proof refuses instead of being accepted-and-ignored — because the corpus's shipped validator is the §4.3 resource-request form and _requires_ the hash of a token this route has not yet decided to issue, with `cnf.jkt` computed from the proof's own JWK thumbprint (**I-018-20**); and a credential finally becomes **revocable** — every earlier round could enrol an authenticator and none could retire one, and deleting the desktop's local key material removes nothing a different machine's sign-in consults — through a fifth registry route `WebAuthnCredentialRevoke` (**T6.5** / **I-018-21**), authenticated, resolving the row under the caller's **own** participant id so an id the caller does not own is indistinguishable from one that does not exist, stored as a hard `DELETE` rather than a `revoked_at` tombstone (which would hand the verify leg the _revoked_-versus-_unknown_ oracle every other arm withholds, and mint a column to obtain it), and permitted even for the participant's **last** credential, since the Device Authorization Grant loopback is a way back in and a rule that keeps a compromised key enrolled is worse than the state it protects — while **CP-023-9 stops being macOS-scoped**, its Resolution re-derived on resolved host capability (I-023-15) with Plan-022's four macOS-only sites corrected and `Spec-022 §Daemon Master Key`'s desktop branch fixed on three counts it had carried (the HKDF now runs over the **custody root** and not raw PRF output, so the daemon's KEK survives an enrolment it cannot re-wrap across; the renderer's `CredentialsContainer` is struck for a ceremony that runs in Electron **main**; and the citation moves from a 2026-02-10 Candidate Recommendation to the 2026-08-25 W3C Recommendation), its rotate-on-credential-change and threat-model bullets corrected with it — **one plan-local registry moves and no census does**: the `api-payload-contracts.md` §WebAuthn Ceremony Procedure Registry four → **five** rows (four ceremony operations plus one lifecycle operation, the heading text deliberately unchanged because six documents cite its anchor), with Plan-018's invariants 19 → **21** and tasks 31 → **32** and Plan-023's invariants nineteen → **twenty** inside their own closed sets, while Postgres holds at **28**, SQLite **59**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** across 20, and the `Spec-021` registry **25** rows, and the 2026-09-02 `apps/desktop` structure-enforcement amendment (§6 node NS-100 — its restoring targeted readiness-audit delta likewise riding the same diff) then gave the console family DAG a mechanical owner: `Spec-023 §Console Design (Meridian)` stated that ordering as a design property and named no mechanism for it, and `§Console Test Tiers`' architecture row owns tripwires that read source text — which an ordering over N families is not — so `§Console Libraries` gains a nineteenth axis, **structure enforcement**, ADOPTing `knip` (a cross-reference to the Tests row that already adopts it for dead exports, scoped and CI-gated here rather than re-admitted) and ADOPT-with-constraints `dependency-cruiser`, both scoped to `apps/desktop` and both wired as **two** CI tasks run with `--continue` because the claims are disjoint — reachability from an entry point versus whether an import may exist — and one red leg must not hide the other's findings, while `jscpd` and `eslint-plugin-boundaries` are AVOIDed on reasons **corrected against primary sources** rather than carried from the proposal that raised them (jscpd's documented `minLines` / `minTokens` minimums of 5 and 50 are the size floor of a reported duplication and are larger than the one-to-four-line duplications this tree actually carries, so it would miss the defect class while firing on the house style's parallel data tables, its publication record additionally carrying a 557-day gap with zero releases in calendar 2025; `eslint-plugin-boundaries` fails **open** rather than merely resolving badly, its own guide stating that under flat config — which this repo uses — no resolver is active automatically and an import no configured resolver resolves is classified `"external"`, so making it correct would mean pulling the `eslint-plugin-import` resolver family into a lint stack deliberately kept core-ESLint-only), with `§Console Test Tiers` taking one normative paragraph naming the DAG low to high — `core → tokens → routing → primitives → store / persistence → bridge → palette → frame → view families` — and stating that an upward edge is a CI failure, as are a cycle, an orphan module, a renderer-to-main import or its reverse, and an import from `src/shared/` into anything but the contracts package, and `§References` taking **D.11**, the per-tool evidence; **I-023-14 is widened in place** rather than a second invariant minted — an existing invariant already says these gates run in CI, and a second id would split one obligation across two — so the preflight Gate-4 invariant-reference census pin does not move, with `Plan-023 §Target Areas` Build / Config registering the four files that carry the gates (`knip.json`, `.dependency-cruiser.mjs`, and the `AGENTS.md` / `CLAUDE.md` pair holding the package's structure standard, whose rules those gates mechanize), the wiring itself lane-3 tooling adding no Shipment Manifest row, and both gates proven by planted negative control before landing; **no census moves** — no wire method, event type, error code, Cedar action, table, column, invariant, task, phase, cross-plan obligation, or ADR is minted, so SQLite **59**, Postgres **28**, plans **30**, specs **30**, accepted ADRs **28**, the Spec-006 taxonomy **159** across 20, Plan-023's invariants **twenty**, and its cross-plan obligations **ten** all hold, the one count that moves sitting inside the flipped pair's own closed set: `Spec-023 §Console Libraries` eighteen → **nineteen** axes, extended 2026-09-02 in the same node's console-substrate follow-on, where `Plan-023 §Console growth slate` gains a twenty-fifth row (the typed session snapshot read for a store's base state and the participant's session directory read, over the daemon method union) mirrored in the console's own in-tree ledger — a registry row and neither an invariant, task, phase, cross-plan obligation, nor ADR, so no Status moves (a recorded no-flip) and no census does either — and audit clearance only, not code-readiness: plan-task code (lane 1 per [CONTRIBUTING.md](CONTRIBUTING.md) §How Code Lands) still dispatches in tier order and on each plan's §Preconditions — enhancement and tooling lanes are exempt (execution is underway through Tier 4). **The tier-audit chain is closed** — the Tier-9 audit (NS-21, the chain's final entry) landed 2026-08-15 via PR #331, auditing Plan-026 + Plan-027 with both staying `approved`; Plan-028's targeted readiness audit cleared 2026-08-12 (§6 node NS-61), promoting it `draft → review`, and its `review → approved` promotion landed 2026-08-14 — the Plan-028 doc gates are closed (campaign B18). No audit work remains on the DAG and no doc gate is outstanding: the last two Status promotions both landed 2026-08-15 — Plan-025's via the §Status Promotion Gate (PR #333) and Plan-018's via the NS-62 promotion pass (PR #334, which carries the CP-018-12 / CP-018-13 targeted readiness-audit delta, authors Plan-018 Phase 5, and adds the `participant_identity_keys` table — Postgres census 25 → 26); per the README census the plan-promotion queue is empty.
- **Plan status promotion** — a code-execution plan ships its first PR only after it — and every cross-referenced spec, ADR, and plan — has completed the status promotion its [Documentation Corpus](#documentation-corpus) row requires, and every blocking backlog item is `completed` (or deferred with a named gate), per [AGENTS.md §Doc-First Discipline](AGENTS.md#doc-first-discipline). This is a separate gate, not satisfied by audit clearance or tier order alone: a plan that is audit-cleared and tier-eligible but not yet promoted stays blocked. The [README](README.md) census lists the plans not yet promoted.

## Cross-Tool Conventions

Cross-tool conventions for AI agents in this repo (Claude, Codex, Cursor, Aider) live in [`AGENTS.md`](AGENTS.md). It owns the citation standard, the transient research-artifact pattern under `.agents/tmp/research/<topic>/<axis>.md`, parallel-subagent dispatch rules, and doc-first ordering.

Branch naming, commit format, and PR workflow conventions live in [`CONTRIBUTING.md`](CONTRIBUTING.md). It owns the GitFlow-lite branch model (feature branches off `develop`; squash-merge into `develop`; `develop` → `main` only at release), [Conventional Branch](https://conventional-branch.github.io/) 2-segment shape (`<type>/<topic>`), [Conventional Commits 1.0](https://www.conventionalcommits.org/en/v1.0.0/) message format with package-noun scope, footer-trailer conventions (`Refs: ADR-NNN, BL-NNN, Plan-NNN` and `Co-Authored-By:`), and squash-merge workflow per [ADR-023](docs/decisions/023-v1-ci-cd-and-release-automation.md).

Read `AGENTS.md` on demand before:

- Authoring a new spec, ADR, plan, or architecture doc that requires primary-source citations
- Dispatching parallel research subagents
- Committing any doc whose content was drafted with subagent research artifacts (the surface-forward-then-delete step)

**Anti-pattern**: never cite `.agents/tmp/...` paths from committed docs. The directory is gitignored and manually pruned — no hook deletes it; surface citations forward into the consuming doc's References section, then delete the research file yourself before the consuming-doc commit lands (per AGENTS.md "Surface-Forward-Then-Delete").

## Worktrees

Git worktrees live under `.worktrees/<name>/` at the repo root. The harness enforces this via `WorktreeCreate` and `PreToolUse` hooks in `.claude/settings.json` — `git worktree add` or `git worktree move` to any path outside `.worktrees/` is denied. Use `git worktree add .worktrees/<name> -b worktree-<name>` when creating manually to match the harness branch-naming convention.

**Removal discipline.** Deleting a worktree that is a live process's working directory breaks every later command spawn in the occupying session (`ENOENT posix_spawn '/bin/sh'` — 2026-07-07 incident). Removals (`git worktree remove`, recursive `rm` into `.worktrees/`, harness-initiated removals via `worktree.sh`) are occupancy-checked by `command-guard.py`: an occupied removal is denied with the occupant list. Occupancy can be transient — retry once; otherwise exit the occupying session or kill the listed PIDs; the deliberate override is prefixing the exact command with `WORKTREE_REMOVE_ALLOW_OCCUPIED=1`. Bulk cleanups remove only worktrees you created, after checking `python3 .claude/hooks/command-guard.py --occupancy <path>` (empty output = unoccupied). Honest limit: a session whose _tracked_ Bash cwd is inside a worktree with no live process there at check time is invisible to the guard — the only-remove-what-you-created rule is the mitigation. Active on macOS/Linux/WSL2; inert-but-safe on native Windows (the OS itself locks in-use directories).

## Documentation Corpus

| Tree | Purpose | Template | Status Lifecycle |
| --- | --- | --- | --- |
| `docs/specs/NNN-kebab.md` | Feature specifications (the design contract) | `docs/specs/000-spec-template.md` | `draft` → `review` → `approved` (or `superseded`) |
| `docs/plans/NNN-kebab.md` | Implementation plans (executable build steps) | `docs/plans/000-plan-template.md` | `draft` → `review` → `approved` → `completed` |
| `docs/decisions/NNN-kebab.md` | ADRs (decisions with antithesis + synthesis) | `docs/decisions/000-adr-template.md` | `proposed` → `accepted` (or `deprecated` / `superseded by ADR-NNN`) |
| `docs/domain/` | Domain models, state machines, glossary | `docs/domain/template.md` | canonical when merged |
| `docs/architecture/` | Schemas, contracts, system context, deployment, security | `docs/architecture/template.md` | canonical when merged |
| `docs/operations/` | Runbooks, on-call routing, SLOs | `docs/operations/template.md` | canonical when merged |
| `docs/backlog.md` | Active work items (`BL-NNN`) | inline template in file header | `todo` / `in_progress` / `blocked` / `completed` |

Non-governance docs sit alongside the corpus and are not subject to the status lifecycle above: [`docs/vision.md`](docs/vision.md) (long-form product vision) and [`docs/reference/`](docs/reference/) (excerpted upstream materials).

ADRs are classified `Type 1` (two-way door, reversible — skip [T2] sections) or `Type 2` (one-way door, hard to reverse — complete every section).

## When Writing Documents

- **Copy the template.** New spec / plan / ADR? Start from `000-{type}-template.md`. Number sequentially within the tree (next free `NNN`).
- **Check the ownership map first.** Before adding a column, file, or directory that another plan might own, consult [`docs/architecture/cross-plan-dependencies.md`](docs/architecture/cross-plan-dependencies.md). The owning plan `CREATE`s; dependent plans `EXTEND`.
- **Cross-link aggressively.** Every spec names its plan; every plan names its spec and Required ADRs; every backlog item names the docs that govern it.
- **Status promotion is load-bearing.** Do not flip `review` → `approved` without addressing review notes — downstream plans/specs treat the prior state as stable. ADR moves to `accepted` only after antithesis is steel-manned and synthesis is recorded.
- **Backlog discipline.** `BL-NNN` items must include References, Summary, and Exit Criteria. Move completed items to `docs/archive/backlog-archive.md` after the canonical docs are updated. Do not let `backlog.md` accumulate historical entries — rewrite or remove stale items.
