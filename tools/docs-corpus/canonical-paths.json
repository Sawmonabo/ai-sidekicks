[
  {
    "canonical": "apps/desktop/",
    "deprecated": ["apps/desktop/shell", "apps/desktop/renderer"],
    "note": "Plan-021 collapsed shell/renderer into one apps/desktop workspace; the forms are written without a trailing slash so a substring match also catches the CLI filter form."
  },
  {
    "canonical": "packages/runtime-daemon/src/provider/drivers/",
    "deprecated": ["packages/runtime-daemon/src/drivers"],
    "note": "The provider driver tree is packages/runtime-daemon/src/provider/drivers/; the deprecated spelling never existed on disk."
  },
  {
    "canonical": "packages/runtime-daemon/src/provider/",
    "deprecated": ["packages/runtime-daemon/src/providers"],
    "note": "The daemon provider directory is singular; the plural spelling is dead."
  },
  {
    "canonical": "sidekicks",
    "deprecated": [
      "(^|[^[:alnum:]@/_-]|(^|[][[:space:]\"\\'`(=])[.~]{0,2}/[^[:space:]:]*)ai-sidekicks[[:space:]]+(config|cp|daemon|db|driver|events|init|login|mcp|onboarding|policy|relay|replay|run|self-update|session|settings|telemetry|workflow|workspace|worktree)([^[:alnum:]_-]|$)",
      "(^|[^[:alnum:]@/_-]|(^|[][[:space:]\"\\'`(=])[.~]{0,2}/[^[:space:]:]*)ai-sidekicks[[:space:]]+-",
      "\"ai-sidekicks\"[[:space:]]*:[[:space:]]*\"",
      "bin:[[:space:]]*\"ai-sidekicks\"",
      "(^|[^[:alnum:]@/_-]|(^|[][[:space:]\"\\'`(=])[.~]{0,2}/[^[:space:]:]*)ai-sidekicks://",
      "(^|[^[:alnum:]@/_-]|(^|[][[:space:]\"\\'`(=])[.~]{0,2}/[^[:space:]:]*)ai-sidekicks[[:space:]]+\\\\$",
      "setAsDefaultProtocolClient\\([[:space:]]*['\"`]ai-sidekicks['\"`][[:space:]]*\\)"
    ],
    "note": "The CLI executable and the deep-link scheme are spelled sidekicks; the project name, npm scope, config directories and keystore prefix keep ai-sidekicks, so every form here is a POSIX extended regex, matched with git grep -E."
  }
]
